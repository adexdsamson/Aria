/**
 * Temporary token stub. Plan 01b's Task 3 replaces this with the real
 * D-13 palette (neutral palette + one accent, type scale, radii, spacing,
 * system light/dark).
 *
 * This stub exists only so `tailwind.config.ts` resolves at plan 01a
 * typecheck time. Do NOT add real values here — they belong to plan 01b.
 */
export const tokens = {
  light: {},
  dark: {},
} as const;
