---
phase: 260609-o8e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/features/voice/useVoiceSession.ts
  - src/renderer/features/voice/useVoiceSession.spec.ts
autonomous: true
requirements:
  - VOICE-TRANSCRIPT-ANSWER
must_haves:
  truths:
    - "A final STT transcript with non-empty text fires voiceFeedAnswer IPC with sessionId and question"
    - "An empty/whitespace final transcript does NOT fire voiceFeedAnswer (silence is not a question)"
    - "The pendingApprovalId !== null branch (voiceConfirmApproval) is completely unchanged"
    - "final=false incremental transcripts fire neither voiceFeedAnswer nor voiceConfirmApproval"
  artifacts:
    - path: src/renderer/features/voice/useVoiceSession.ts
      provides: "setTranscript else-branch calls voiceFeedAnswer when text.trim().length > 0"
      contains: "voiceFeedAnswer"
    - path: src/renderer/features/voice/useVoiceSession.spec.ts
      provides: "4 new test cases covering the voiceFeedAnswer routing"
      contains: "voiceFeedAnswer"
  key_links:
    - from: "useVoiceSession.ts setTranscript else-branch"
      to: "window.aria.voiceFeedAnswer"
      via: "direct call with { sessionId: currentSessionId, question: text }"
      pattern: "voiceFeedAnswer.*sessionId.*currentSessionId"
---

<objective>
Wire the normal voice turn's final transcript to the answer pipeline.

Purpose: setTranscript(text, final=true) with pendingApprovalId===null is currently a no-op stub whose comment claims voiceFeedAnswer is "called externally by the capture layer" — it is not. Main-side VOICE_FEED_ANSWER handler (voice.ts ~line 513) is fully wired to voiceSessionManager.startAnswer; it just needs the renderer to call it.

Output: Modified useVoiceSession.ts (setTranscript else-branch fires voiceFeedAnswer) + extended spec (4 new cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire setTranscript else-branch + extend spec</name>
  <files>src/renderer/features/voice/useVoiceSession.ts, src/renderer/features/voice/useVoiceSession.spec.ts</files>
  <behavior>
    - final=true + pendingApprovalId===null + non-empty text → window.aria.voiceFeedAnswer called once with { sessionId: currentSessionId, question: text }
    - final=true + pendingApprovalId===null + empty string → voiceFeedAnswer NOT called
    - final=true + pendingApprovalId===null + whitespace-only string → voiceFeedAnswer NOT called
    - final=true + pendingApprovalId non-null → voiceConfirmApproval called, voiceFeedAnswer NOT called (existing behaviour, unchanged)
    - final=false → neither voiceFeedAnswer nor voiceConfirmApproval called
  </behavior>
  <action>
In src/renderer/features/voice/useVoiceSession.ts, locate the setTranscript else-branch (the pendingApprovalId === null block, lines ~289-294). Replace the no-op comment block with a guard: if text.trim().length > 0, call (window.aria as AriaApi).voiceFeedAnswer?.({ sessionId: currentSessionId, question: text }) — fire-and-forget (no await). Wrap the call in the same guard pattern used by voiceAbort at ~line 339: if (typeof window !== 'undefined' && window.aria). Replace the stale "called externally by the capture layer" comment with a comment explaining that this is the normal answer turn path. Do NOT change anything in the pendingApprovalId !== null branch above it. Do NOT add async to setTranscript.

In src/renderer/features/voice/useVoiceSession.spec.ts, extend the makeAriaApi helper to accept voiceFeedAnswer as an optional override (vi.fn() default). Add four new it() cases inside the existing describe block:

1. "setTranscript final=true + non-empty text calls voiceFeedAnswer with sessionId and question": create a store, inject window.aria mock with voiceFeedAnswer spy, call startTurn(), then setTranscript('what day is it', true); assert voiceFeedAnswer called once with { sessionId: expect.any(String), question: 'what day is it' }.

2. "setTranscript final=true + empty text does NOT call voiceFeedAnswer": same setup, call setTranscript('', true); assert voiceFeedAnswer NOT called.

3. "setTranscript final=true + whitespace-only text does NOT call voiceFeedAnswer": same setup, call setTranscript('   ', true); assert voiceFeedAnswer NOT called.

4. "setTranscript final=true + pendingApprovalId set does NOT call voiceFeedAnswer": same setup but call setPendingApproval('appr-1') before setTranscript; assert voiceFeedAnswer NOT called. (voiceConfirmApproval may be a spy too — just assert voiceFeedAnswer NOT called.)

For test cases 1-3 the window.aria mock must be set before the store is created OR the store's subscribeToIpc call must NOT be needed — since window.aria is read inside setTranscript at call time, assign the mock to globalThis.aria (or Object.defineProperty(window, 'aria', ...)) in a beforeEach/afterEach or inline inside each test, and restore it in afterEach. Mirror the existing makeAriaApi pattern used by the IPC subscription tests; do not import React or render hooks — this spec tests the pure store factory only.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/features/voice/useVoiceSession.spec.ts --no-file-parallelism 2>&1 | tail -20</automated>
  </verify>
  <done>All existing tests pass; 4 new tests pass; voiceFeedAnswer is called on non-empty final transcript and not called on empty/whitespace/pendingApproval paths; no new TypeScript errors (typecheck baseline remains at 84: run pnpm typecheck or npx tsc --noEmit and confirm error count does not increase)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main via IPC | voiceFeedAnswer carries user-spoken question text; treated as trusted (local, same-machine renderer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o8e-01 | Tampering | setTranscript question payload | accept | local renderer; no cross-origin surface; question text originates from user's own microphone |
| T-o8e-02 | Denial of Service | empty/whitespace transcript fires IPC | mitigate | text.trim().length > 0 guard prevents empty-question IPC calls |
</threat_model>

<verification>
Run the spec in isolation: npx vitest run src/renderer/features/voice/useVoiceSession.spec.ts --no-file-parallelism
All 4 new cases pass; all pre-existing cases in the file pass.
Typecheck: pnpm typecheck (or npx tsc --noEmit) — error count must not exceed 84.
</verification>

<success_criteria>
- window.aria.voiceFeedAnswer is called with { sessionId, question } when a non-empty final transcript arrives on a normal turn
- Empty and whitespace transcripts do not fire voiceFeedAnswer
- The pendingApprovalId confirmation path is unmodified
- All useVoiceSession.spec.ts tests pass (existing + 4 new)
- Typecheck baseline unchanged (84 errors max)
</success_criteria>

<output>
After completion, create .planning/quick/260609-o8e-voice-transcript-answer/260609-o8e-01-SUMMARY.md

Note in SUMMARY: a normal voice turn now routes to the RAG /ask answer pipeline (streamVoiceAnswer via VOICE_FEED_ANSWER → voiceSessionManager.startAnswer). The next known gap is multi-intent routing: VoiceIntentRouter (src/main/voice/VoiceIntentRouter.ts) exists and handles triage/schedule/draft intents but is currently invoked nowhere — all normal turns go directly to streamVoiceAnswer. Wiring VoiceIntentRouter is a separate Phase-17 follow-up.
</output>
```
