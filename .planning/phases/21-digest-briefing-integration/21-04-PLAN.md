---
phase: 21-digest-briefing-integration
plan: 04
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-03
files_modified:
  - src/main/ipc/briefing.ts
autonomous: true
requirements:
  - WA-08
  - WA-09
  - WA-10

must_haves:
  truths:
    - "readWhatsAppDigests(db, date, logger) is a module-local function annotated '// read-only, no model' (D-13)"
    - "row.whatsApp is attached in BRIEFING_TODAY handler AFTER the thisWeekInsights try/catch block, BEFORE return row (D-11)"
    - "The enrichment is wrapped in try/catch that logs warn + never re-throws — briefing always returns (WA-10/D-10)"
    - "D-07.3 briefing-read fallback: if no digest rows exist for today, void triggerDigestIfMissing fires fire-and-forget (never awaited)"
    - "D-10 state matrix fully implemented in readWhatsAppDigests: not-linked→undefined, zero-tracked→undefined, all-no-activity→undefined, any-failed/model-offline→unavailable, some-summarized→ready"
    - "connection? field populated from provider_account.status when 'degraded' or 'needs-auth' (D-10)"
    - "WHATSAPP_GENERATE_DIGEST_NOW IPC handler registered — calls digestHandle.runNow() fire-and-forget, returns {ok:true} immediately"
  artifacts:
    - path: "src/main/ipc/briefing.ts"
      provides: "readWhatsAppDigests helper + BRIEFING_TODAY enrichment + WHATSAPP_GENERATE_DIGEST_NOW handler"
      contains: "readWhatsAppDigests"
  key_links:
    - from: "src/main/ipc/briefing.ts (row.whatsApp)"
      to: "src/shared/ipc-contract.ts (BriefingPayload.whatsApp union)"
      via: "TypeScript type assignment"
      pattern: "row\\.whatsApp"
    - from: "src/main/ipc/briefing.ts (BRIEFING_TODAY handler)"
      to: "runBriefing in generate.ts"
      via: "enrichment is AFTER runBriefing returns — frontier never sees whatsApp content (D-11)"
      pattern: "readWhatsAppDigests"
---

<objective>
Wire the read path: add readWhatsAppDigests helper to briefing.ts (pure SELECT, no model), attach row.whatsApp in the BRIEFING_TODAY handler, and register the WHATSAPP_GENERATE_DIGEST_NOW IPC handler for the Generate-now retry flow.

Purpose: Makes briefing-whatsapp-enrichment.spec.ts tests (Plan 21-02) turn GREEN. Completes the data path from whatsapp_group_digest table to BriefingPayload — the briefing now carries WhatsApp digests to the renderer.

Output: Modified src/main/ipc/briefing.ts with three additions: readWhatsAppDigests module-local function, enrichment block in BRIEFING_TODAY, and WHATSAPP_GENERATE_DIGEST_NOW handler registration.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-digest-briefing-integration/21-CONTEXT.md
@.planning/phases/21-digest-briefing-integration/21-RESEARCH.md
@.planning/phases/21-digest-briefing-integration/21-PATTERNS.md
@.planning/phases/21-digest-briefing-integration/21-01-SUMMARY.md
@.planning/phases/21-digest-briefing-integration/21-03-SUMMARY.md

<interfaces>
<!-- Key seams in briefing.ts to modify. Extracted from live source (RESEARCH.md verified). -->

From src/main/ipc/briefing.ts lines 215-248 (BRIEFING_TODAY handler + thisWeekInsights enrichment to mirror):
```typescript
// line 215: ipcMain.handle(CHANNELS.BRIEFING_TODAY, async (_e, payload?) => {
// ...
// lines 223-244: thisWeekInsights enrichment block (try/catch shape to mirror exactly)
try {
  const weekYmd = weekStartYmdFor(new Date(), tz);
  const ins = readLatestInsights(db, weekYmd);
  // ... build row.thisWeekInsights ...
} catch (err) {
  logger.warn({ scope: 'briefing-today-insights', err: (err as Error).message }, '...');
}
return row;  // ← insert whatsApp enrichment block BEFORE this return
```

From src/main/ipc/briefing.ts (import pattern for readLatestInsights, line 41):
```typescript
import { readLatestInsights } from './insights';
// Phase 21: readWhatsAppDigests is module-local (not a cross-module import) per D-13
```

From src/main/whatsapp/digest-cron.ts (WhatsAppDigestHandle type — from Plan 21-03):
```typescript
export interface WhatsAppDigestHandle { stop(): void; runNow(): Promise<void>; }
```

From src/shared/ipc-contract.ts (CHANNELS.WHATSAPP_GENERATE_DIGEST_NOW and BriefingPayload.whatsApp — from Plan 21-01):
```typescript
CHANNELS.WHATSAPP_GENERATE_DIGEST_NOW: 'aria:whatsapp:generate-digest-now'
BriefingPayload.whatsApp?: | { state: 'ready'; groups: WhatsAppGroupSummaryDto[]; connection?: ... } | { state: 'unavailable'; reason: 'model-offline'; connection?: ... }
WhatsAppGroupSummaryDto: { jid, displayName, state: 'summarized'|'no-activity'|'failed', summaryText? }
```

readWhatsAppDigests logic (from PATTERNS.md Pattern 4):
- Step 1: SELECT status FROM provider_account WHERE provider_key='whatsapp' LIMIT 1 → not found = undefined
- Step 2: SELECT jid, display_name FROM whatsapp_group WHERE tracked=1 → empty = undefined
- Step 3: SELECT jid, summary_text FROM whatsapp_group_digest WHERE date=? → build digestMap
- Step 4: Map tracked groups to WhatsAppGroupSummaryDto: no row→'no-activity', null→'failed', text→'summarized'
- Step 5: hasAnyContent=some summarized, hasAnyFailed=some failed; connection from account.status
- Step 6: !hasAnyContent && !hasAnyFailed → undefined; !hasAnyContent && hasAnyFailed → unavailable; else → ready
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add readWhatsAppDigests module-local helper and BRIEFING_TODAY enrichment block</name>
  <files>src/main/ipc/briefing.ts</files>
  <read_first>
    - src/main/ipc/briefing.ts lines 1-60 (imports + module setup — understand existing import structure before adding)
    - src/main/ipc/briefing.ts lines 215-250 (BRIEFING_TODAY handler body — find the exact thisWeekInsights block end + return row line)
    - src/main/ipc/briefing.ts lines 40-45 (readLatestInsights import pattern — to mirror with module-local readWhatsAppDigests)
    - src/shared/ipc-contract.ts lines 540-570 (BriefingPayload.whatsApp + WhatsAppGroupSummaryDto from Plan 21-01)
    - .planning/phases/21-digest-briefing-integration/21-PATTERNS.md section briefing.ts Pattern 2 (enrichment block) and Pattern 4 (readWhatsAppDigests implementation)
    - .planning/phases/21-digest-briefing-integration/21-RESEARCH.md Pattern 4 (readWhatsAppDigests full SQL logic)
  </read_first>
  <behavior>
    - Test: no provider_account row → payload.whatsApp is undefined
    - Test: provider_account exists, zero tracked groups → undefined
    - Test: digest row with summary_text non-null → state='ready', groups[0].state='summarized'
    - Test: digest row with summary_text=null → state='unavailable', reason='model-offline'
    - Test: provider_account.status='degraded' → connection:'degraded' on union arm
    - Test: readWhatsAppDigests throws internally → BRIEFING_TODAY still resolves (never re-throws)
  </behavior>
  <action>
    Add two things to src/main/ipc/briefing.ts. Read the full file first to understand the existing structure.

    Addition 1 — readWhatsAppDigests module-local function:
    Insert before the BRIEFING_TODAY handler (or near the bottom of the module before the export/handler block — match the existing file's convention for module-local helpers like readLatestInsights). The function signature: readWhatsAppDigests(db: Db, date: string, logger: Pick<Logger, 'warn'>): BriefingPayload['whatsApp']

    Annotate the function with a JSDoc comment that includes: // read-only, no model (D-13 annotation).

    Implement the D-10 state matrix exactly as described in PATTERNS.md Pattern 4 and RESEARCH.md Pattern 4:
    1. Query provider_account for provider_key='whatsapp'; return undefined if not found
    2. Query whatsapp_group WHERE tracked=1; return undefined if empty
    3. Query whatsapp_group_digest WHERE date=?; build a Map<jid, summary_text|null>
    4. Map tracked groups to WhatsAppGroupSummaryDto: !digestMap.has(jid) → state:'no-activity'; null→state:'failed'; text→state:'summarized',summaryText:text
    5. hasAnyContent = some 'summarized', hasAnyFailed = some 'failed'
    6. connection = (account.status==='degraded'||account.status==='needs-auth') ? account.status : undefined
    7. Return decisions: !hasAnyContent && !hasAnyFailed → undefined; !hasAnyContent && hasAnyFailed → {state:'unavailable',reason:'model-offline',...(connection?{connection}:{})}; else → {state:'ready',groups,...(connection?{connection}:{})}

    Addition 2 — enrichment block in BRIEFING_TODAY handler:
    Insert AFTER the thisWeekInsights try/catch block, BEFORE the final `return row` statement. Follow Pattern 2 from PATTERNS.md exactly:
    try {
      const wa = readWhatsAppDigests(db, date, logger);
      if (wa !== undefined) row.whatsApp = wa;
      // D-07.3: if no rows for today, trigger async (fire-and-forget)
      // Use: if (wa === undefined && digestHandle) { void digestHandle.runNow(); }
      // digestHandle is passed as a dep or imported from module scope — see Task 2 for wiring
    } catch (err) {
      logger.warn({ scope: 'briefing-today-whatsapp', err: (err as Error).message }, 'failed to enrich briefing with whatsapp digests');
    }
    return row;

    For D-07.3 fallback wiring: the briefing IPC module needs access to the digest handle. The cleanest approach is to accept an optional WhatsAppDigestHandle (or a getter function) as part of the registerBriefingHandlers deps interface. Check how registerBriefingHandlers is currently structured and add an optional digestHandle?: WhatsAppDigestHandle | null to its deps. Then in the enrichment block: if (row.whatsApp === undefined && deps.digestHandle) { void deps.digestHandle.runNow(); }

    Check if there is already a deps pattern in registerBriefingHandlers. If briefingHandlers does not use a deps object, add one that accepts the digestHandle as an optional field. Do NOT add the digest handle import at module top level (it must be injected via deps to keep tests isolated).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts passes all 6 test cases (exits 0)
    - grep -c "readWhatsAppDigests" src/main/ipc/briefing.ts returns at least 2 (definition + call site)
    - grep -c "read-only, no model" src/main/ipc/briefing.ts returns at least 1 (D-13 annotation)
    - grep -c "row\.whatsApp" src/main/ipc/briefing.ts returns at least 1
    - grep -c "briefing-today-whatsapp" src/main/ipc/briefing.ts returns at least 1 (error scope string)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>readWhatsAppDigests helper added with D-10 state matrix; row.whatsApp enrichment in BRIEFING_TODAY; all 6 briefing-whatsapp-enrichment.spec.ts tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Register WHATSAPP_GENERATE_DIGEST_NOW IPC handler in briefing.ts</name>
  <files>src/main/ipc/briefing.ts</files>
  <read_first>
    - src/main/ipc/briefing.ts (current state after Task 1 — the registerBriefingHandlers deps pattern just established)
    - src/main/ipc/whatsapp.ts (WHATSAPP_CHANNELS const array pattern — check if digest-now channel belongs there or in briefing handlers)
    - src/shared/ipc-contract.ts lines 200-215 (CHANNELS.WHATSAPP_GENERATE_DIGEST_NOW from Plan 21-01)
    - src/renderer/features/briefing/GenerateNowAffordance.tsx (how briefingGenerateNow IPC handler is structured — the model for the digest-now handler shape)
    - .planning/phases/21-digest-briefing-integration/21-PATTERNS.md Critical Warning #5 (MUST NOT call briefingGenerateNow — different channel)
  </read_first>
  <action>
    Register the WHATSAPP_GENERATE_DIGEST_NOW handler within the registerBriefingHandlers function (or wherever the briefing IPC handlers are registered). The handler:
    - Channel: CHANNELS.WHATSAPP_GENERATE_DIGEST_NOW
    - Action: if deps.digestHandle is available, void deps.digestHandle.runNow() (fire-and-forget — this is async but the handler returns immediately)
    - Return: { ok: true } immediately (same shape as briefingGenerateNow)
    - If deps.digestHandle is null/undefined: return { ok: false, error: 'digest handle not available' }

    This is NOT a replacement for briefingGenerateNow — it calls only the per-group digest cron (no frontier LLM, local-only). It must NOT call any briefing regeneration function.

    The handler must NOT be added to WHATSAPP_CHANNELS (that array is for the 6 WhatsApp-connection channels registered in whatsapp.ts). This handler logically belongs in briefing.ts because it enriches the briefing payload.

    If briefing.ts has a BRIEFING_CHANNELS const array (or equivalent for removeHandler loop), add WHATSAPP_GENERATE_DIGEST_NOW to it.

    Verify that the preload.ts CHANNEL_METHODS mapping already includes whatsAppGenerateDigestNow from Plan 21-01 — if not, this is a gap to flag and fix.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "WHATSAPP_GENERATE_DIGEST_NOW" src/main/ipc/briefing.ts returns at least 1
    - Handler returns { ok: true } without awaiting digest completion (fire-and-forget void call)
    - npx tsc --noEmit exits 0
    - grep -c "briefingGenerateNow" src/main/ipc/briefing.ts shows the existing briefingGenerateNow and the new handler are distinct (new handler does NOT call briefingGenerateNow)
  </acceptance_criteria>
  <done>WHATSAPP_GENERATE_DIGEST_NOW handler registered in briefing.ts, returns {ok:true} immediately while running digest fire-and-forget. Zero compile errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider_account.status DB read → BriefingPayload | Reads account status from SQLCipher-encrypted DB; result enters IPC payload to renderer |
| WHATSAPP_GENERATE_DIGEST_NOW IPC | Renderer-triggered re-run of local digest; no user-supplied data in payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Information Disclosure | readWhatsAppDigests reads summary_text from DB and includes it in IPC payload | mitigate | Data stays local (SQLCipher); summary_text was already local-model-generated; IPC payload goes renderer-only (no network); D-11 ensures this enrichment is POST-frontier-call so WA text never enters frontier prompt |
| T-21-09 | Tampering | WHATSAPP_GENERATE_DIGEST_NOW allows renderer to trigger LLM call | accept | Calls local model only (digest-cron.ts is under SC3 ratchet); no user-supplied data in request; at worst re-runs an already-idempotent operation (INSERT OR REPLACE) |
| T-21-10 | Denial of Service | Renderer spamming GENERATE_DIGEST_NOW triggers repeated Ollama calls | accept | p-queue concurrency:1 in digest cron prevents concurrent Ollama requests; call is fire-and-forget so renderer is never blocked waiting for Ollama; Ollama offline = immediate catch + NULL row |
</threat_model>

<verification>
After both tasks:
- npx vitest run tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts -x exits 0 (all 6 cases green)
- grep "readWhatsAppDigests" src/main/ipc/briefing.ts shows definition + call site
- grep "read-only, no model" src/main/ipc/briefing.ts shows the D-13 annotation
- grep "WHATSAPP_GENERATE_DIGEST_NOW" src/main/ipc/briefing.ts shows handler registration
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
- readWhatsAppDigests implements D-10 state matrix fully (WA-08, WA-10)
- row.whatsApp enrichment is post-frontier by construction — WA content never enters runBriefing (WA-09/D-11)
- BRIEFING_TODAY never throws on readWhatsAppDigests failure (D-07.3/WA-10)
- D-07.3 async fallback fires fire-and-forget when no digest rows exist for today
- WHATSAPP_GENERATE_DIGEST_NOW handler registered and calls digest-cron runNow fire-and-forget (SC4 retry)
</success_criteria>

<output>
After completion, create `.planning/phases/21-digest-briefing-integration/21-04-SUMMARY.md`
</output>
