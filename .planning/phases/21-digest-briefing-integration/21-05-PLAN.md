---
phase: 21-digest-briefing-integration
plan: 05
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
files_modified:
  - src/renderer/features/briefing/BriefingScreen.tsx
autonomous: true
requirements:
  - WA-08
  - WA-10

must_haves:
  truths:
    - "BriefingScreen renders 'briefing-whatsapp' section when payload.whatsApp.state='ready' (WA-08)"
    - "BriefingScreen renders 'briefing-whatsapp-unavailable' + 'briefing-whatsapp-retry' when state='unavailable' (WA-10/D-10)"
    - "No section rendered when payload.whatsApp is undefined (D-10 omit states)"
    - "Degraded copy is exactly: 'Digest unavailable — the local model was offline this morning. Aria will retry tonight.' (D-10 locked copy)"
    - "Retry affordance calls window.aria.whatsAppGenerateDigestNow() NOT window.aria.briefingGenerateNow() (PATTERNS Critical Warning 5)"
    - "Per-group sections render group.displayName as sub-heading with state-based content: summarized shows parsed sections, no-activity and failed are handled without crashing"
    - "Connection degraded/needs-auth shows quiet inline note pointing to Settings when connection field present"
  artifacts:
    - path: "src/renderer/features/briefing/BriefingScreen.tsx"
      provides: "WhatsApp section render switch over payload.whatsApp union"
      contains: "briefing-whatsapp"
  key_links:
    - from: "BriefingScreen.tsx (payload.whatsApp switch)"
      to: "src/shared/ipc-contract.ts (BriefingPayload.whatsApp union)"
      via: "TypeScript discriminated union exhaustive check"
      pattern: "payload\\.whatsApp"
    - from: "BriefingScreen.tsx (retry button)"
      to: "window.aria.whatsAppGenerateDigestNow (WHATSAPP_GENERATE_DIGEST_NOW IPC)"
      via: "preload CHANNEL_METHODS mapping"
      pattern: "whatsAppGenerateDigestNow"
---

<objective>
Implement the renderer WhatsApp section — a dumb switch over payload.whatsApp that renders the correct section for each union state. Includes the Generate-now retry affordance for the unavailable state and per-group sub-sections for the ready state.

Purpose: Makes the three BriefingScreen.spec.tsx test cases (Plan 21-02) turn GREEN. This is the user-facing surface — what the exec sees in the morning briefing after Phase 21 ships.

Output: Modified BriefingScreen.tsx with a WhatsApp section render switch inserted after the thisWeekInsights block. The retry affordance is a self-contained inline component (not the existing GenerateNowAffordance which calls a different IPC).
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-digest-briefing-integration/21-CONTEXT.md
@.planning/phases/21-digest-briefing-integration/21-RESEARCH.md
@.planning/phases/21-digest-briefing-integration/21-PATTERNS.md
@.planning/phases/21-digest-briefing-integration/21-01-SUMMARY.md
@.planning/phases/21-digest-briefing-integration/21-02-SUMMARY.md

<interfaces>
<!-- Render seams in BriefingScreen.tsx to mirror. Extracted from live source (RESEARCH verified). -->

From src/renderer/features/briefing/BriefingScreen.tsx lines 661-725 (thisWeekInsights render switch to mirror):
```tsx
{payload.thisWeekInsights?.state === 'locked' && (
  <section data-testid="briefing-insights-locked" data-aria-cascade="4" style={{ marginBottom: 28 }}>
    <SectionHead>This week</SectionHead>
    <p style={{ fontSize: 13.5, color: 'var(--gray)', fontStyle: 'italic', margin: 0 }}>...</p>
  </section>
)}
{payload.thisWeekInsights?.state === 'unlocked' && payload.thisWeekInsights.rows.length > 0 && (
  <section data-testid="briefing-insights" data-aria-cascade="4" style={{ marginBottom: 36 }}>
    <SectionHead>This week</SectionHead>
    ...
  </section>
)}
```
Insert WhatsApp section immediately after this block.

From src/renderer/features/briefing/GenerateNowAffordance.tsx (retry button shape to mirror):
```typescript
// Structure: useState(busy) + onClick → setBusy(true) → window.aria.IPC_CALL() → result.ok → setBusy(false)
// BUT: WhatsApp retry uses window.aria.whatsAppGenerateDigestNow() NOT briefingGenerateNow()
```

From src/shared/ipc-contract.ts (WhatsAppGroupSummaryDto from Plan 21-01):
```typescript
export interface WhatsAppGroupSummaryDto {
  jid: string; displayName: string;
  state: 'summarized' | 'no-activity' | 'failed';
  summaryText?: string; // present only when state='summarized'
}
```

From .planning/phases/21-digest-briefing-integration/21-RESEARCH.md (parseDigestSections helper):
```typescript
function parseDigestSections(text: string): { keyPoints, decisions, openQuestions, mentions }
// Split on '### KEY POINTS', '### DECISIONS', '### OPEN QUESTIONS', '### MENTIONS'
// Partial-parse tolerant — missing sections return empty string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WhatsApp section render switch to BriefingScreen.tsx</name>
  <files>src/renderer/features/briefing/BriefingScreen.tsx</files>
  <read_first>
    - src/renderer/features/briefing/BriefingScreen.tsx lines 650-740 (thisWeekInsights render region + surrounding context — find exact insertion point after the last thisWeekInsights block)
    - src/renderer/features/briefing/BriefingScreen.tsx lines 1-50 (imports — what's already imported: SectionHead, useState, etc.)
    - src/renderer/features/briefing/GenerateNowAffordance.tsx (full file — retry button shape to mirror for DigestGenerateNowAffordance inline component)
    - src/shared/ipc-contract.ts lines 540-570 (BriefingPayload.whatsApp + WhatsAppGroupSummaryDto from Plan 21-01)
    - .planning/phases/21-digest-briefing-integration/21-PATTERNS.md section BriefingScreen.tsx (exact JSX pattern with testids)
    - .planning/phases/21-digest-briefing-integration/21-CONTEXT.md section D-10 (exact copy string for unavailable state)
  </read_first>
  <behavior>
    - Test: payload.whatsApp.state='ready' → data-testid='briefing-whatsapp' section present
    - Test: payload.whatsApp.state='unavailable' → data-testid='briefing-whatsapp-unavailable' + data-testid='briefing-whatsapp-retry' present
    - Test: payload.whatsApp=undefined → no 'briefing-whatsapp' or 'briefing-whatsapp-unavailable' in DOM
  </behavior>
  <action>
    Read BriefingScreen.tsx to find the exact insertion point (after the last thisWeekInsights block, approximately line 725). Insert the WhatsApp section render switch.

    First, add a module-local helper function parseDigestSections(text: string) that splits on the four ### headers. The function is partial-parse-tolerant: iterate lines, track current section header, accumulate content. Return { keyPoints, decisions, openQuestions, mentions } as trimmed strings. This helper is defined in BriefingScreen.tsx (or extracted to a sibling file if the component file is already very large — check the file length first).

    Second, add a module-local inline component DigestGenerateNowAffordance({ connection }: { connection?: 'degraded' | 'needs-auth' }) that:
    - Has useState(busy, setBusy) and useState(error, setError)
    - On click: setBusy(true), calls window.aria.whatsAppGenerateDigestNow() (NOT briefingGenerateNow), then setBusy(false)
    - Shows a small button with data-testid='briefing-whatsapp-retry' (the testid that BriefingScreen.spec.tsx Case 2 checks)
    - If connection === 'degraded' or 'needs-auth', renders a quiet one-line note pointing to Settings before the retry button

    Third, insert the JSX switch after the thisWeekInsights blocks:

    {payload.whatsApp?.state === 'unavailable' && (
      <section data-testid="briefing-whatsapp-unavailable" style={{ marginBottom: 28 }}>
        <SectionHead>WhatsApp</SectionHead>
        <p style={{ fontSize: 13.5, color: 'var(--gray)', fontStyle: 'italic', margin: 0 }}>
          Digest unavailable — the local model was offline this morning. Aria will retry tonight.
        </p>
        <DigestGenerateNowAffordance connection={payload.whatsApp.connection} />
      </section>
    )}

    {payload.whatsApp?.state === 'ready' && payload.whatsApp.groups.length > 0 && (
      <section data-testid="briefing-whatsapp" style={{ marginBottom: 36 }}>
        <SectionHead>WhatsApp</SectionHead>
        {payload.whatsApp.groups.map((g) => {
          if (g.state === 'no-activity') return null;
          if (g.state === 'failed') return (
            <div key={g.jid} style={{ marginBottom: 12 }}>
              <strong style={{ fontSize: 13, textTransform: 'uppercase', letterSpacing: '0.06em' }}>{g.displayName}</strong>
              <p style={{ fontSize: 13, color: 'var(--gray)', fontStyle: 'italic', margin: '4px 0 0' }}>Digest unavailable for this group.</p>
            </div>
          );
          // state === 'summarized'
          const sections = parseDigestSections(g.summaryText ?? '');
          return (
            <div key={g.jid} style={{ marginBottom: 16 }}>
              <strong style={{ fontSize: 13, textTransform: 'uppercase', letterSpacing: '0.06em' }}>{g.displayName}</strong>
              {sections.keyPoints && <p style={{ fontSize: 13, margin: '4px 0 0', whiteSpace: 'pre-wrap' }}>{sections.keyPoints}</p>}
              {sections.decisions && <p style={{ fontSize: 13, margin: '4px 0 0', color: 'var(--text-muted)', whiteSpace: 'pre-wrap' }}><em>Decisions:</em> {sections.decisions}</p>}
              {sections.openQuestions && <p style={{ fontSize: 13, margin: '4px 0 0', color: 'var(--text-muted)', whiteSpace: 'pre-wrap' }}><em>Open:</em> {sections.openQuestions}</p>}
              {sections.mentions && <p style={{ fontSize: 13, margin: '4px 0 0', color: 'var(--text-muted)', whiteSpace: 'pre-wrap' }}><em>Mentions:</em> {sections.mentions}</p>}
            </div>
          );
        })}
      </section>
    )}

    The exact inline styles should follow the existing editorial conventions in the file. Match fontSize, color tokens (var(--gray), var(--text-muted)), and marginBottom patterns to the nearest existing section. Do NOT introduce new CSS classes or Tailwind utilities that aren't already in use in BriefingScreen.tsx.

    The connection degraded/needs-auth inline note in DigestGenerateNowAffordance should say: "WhatsApp connection is {degraded/needs relink} — check Settings to reconnect." in a small gray italic style. Keep it unobtrusive (this is the quiet inline line from D-10).

    Do NOT modify any existing section's render logic. Append only.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx passes ALL cases including the 3 new Phase 21 cases (exits 0)
    - grep -c "briefing-whatsapp" src/renderer/features/briefing/BriefingScreen.tsx returns at least 2 (unavailable + ready testids)
    - grep -c "whatsAppGenerateDigestNow" src/renderer/features/briefing/BriefingScreen.tsx returns at least 1 (retry affordance)
    - grep -c "briefingGenerateNow" src/renderer/features/briefing/BriefingScreen.tsx returns 0 in the new WhatsApp section (must NOT call the full briefing regenerate)
    - grep -c "local model was offline" src/renderer/features/briefing/BriefingScreen.tsx returns at least 1 (exact D-10 copy string)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>BriefingScreen.tsx renders WhatsApp section for all three union states; all BriefingScreen.spec.tsx tests (original + 3 new) pass; exact D-10 degraded copy present; retry button calls whatsAppGenerateDigestNow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BriefingPayload.whatsApp (IPC) → Renderer JSX | WhatsApp summary_text from SQLite enters renderer as display text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11 | Tampering | LLM-generated summaryText rendered as text in JSX (potential XSS via dangerouslySetInnerHTML) | mitigate | Use whiteSpace:'pre-wrap' text rendering only — summaryText is rendered as a plain text node (not dangerouslySetInnerHTML); React escapes HTML entities in text nodes by default |
| T-21-12 | Tampering | Retry button triggers re-run of local digest | accept | Calls only whatsAppGenerateDigestNow (local model, no frontier); idempotent INSERT OR REPLACE; worst case is a redundant Ollama call |
| T-21-13 | Information Disclosure | WhatsApp group message summaries visible in Aria UI | accept | User explicitly linked WhatsApp and selected groups to track; summaries are local-only; no external transmission |
</threat_model>

<verification>
After task completion:
- npx vitest run tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx -x exits 0 (all cases including 3 new)
- npx tsc --noEmit exits 0
- BriefingScreen.tsx contains "briefing-whatsapp" and "briefing-whatsapp-unavailable" testids
- BriefingScreen.tsx contains "whatsAppGenerateDigestNow" (correct retry IPC call)
- BriefingScreen.tsx contains exact copy "local model was offline this morning" (D-10)
</verification>

<success_criteria>
- WhatsApp section renders per-group digest content (WA-08)
- Unavailable state shows locked degraded copy + retry affordance (WA-10/D-10)
- Retry calls the correct IPC (whatsAppGenerateDigestNow, not briefingGenerateNow)
- Undefined payload.whatsApp produces no section (D-10 omit states)
- Connection degraded/needs-auth shows quiet inline note
- Zero new TypeScript compile errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-digest-briefing-integration/21-05-SUMMARY.md`
</output>
