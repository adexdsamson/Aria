---
phase: 21-digest-briefing-integration
plan: 06
type: execute
wave: 4
depends_on:
  - 21-03
  - 21-04
files_modified:
  - src/main/index.ts
  - src/main/lifecycle/powerMonitor.ts
autonomous: true
requirements:
  - WA-08
  - WA-09
  - WA-10

must_haves:
  truths:
    - "_digestHandle module-scope let variable declared adjacent to whatsAppManager in index.ts (RESEARCH Open Question 3)"
    - "startWhatsAppDigest called in bootPoll block immediately after startWhatsAppRetention (line 623 region) and handle stored in _digestHandle (D-12)"
    - "runChannelOnce has a real 'whatsapp-digest' case that calls _digestHandle.runNow() (RESEARCH Pitfall 1 — the load-bearing fix)"
    - "powerMonitor onResume hook triggers _digestHandle.runNow() when MAX(date) < today (D-07.2 missed-tick catch-up)"
    - "digest handle passed to registerBriefingHandlers deps so D-07.3 briefing-read fallback is wired (D-11 connection)"
    - "The existing unlock drain (registerOnUnlock lines 741-766) requires NO changes — runChannelOnce dispatches correctly via the new switch case"
  artifacts:
    - path: "src/main/index.ts"
      provides: "_digestHandle module-scope variable + startWhatsAppDigest bootstrap + runChannelOnce real switch case"
      contains: "startWhatsAppDigest"
    - path: "src/main/lifecycle/powerMonitor.ts"
      provides: "D-07.2 missed-tick onResume hook"
      contains: "_digestHandle.runNow"
  key_links:
    - from: "src/main/index.ts (runChannelOnce 'whatsapp-digest' case)"
      to: "src/main/lifecycle/pendingCatchup.ts (drain() output)"
      via: "unlock drain loop calling runChannelOnce per channel"
      pattern: "whatsapp-digest"
    - from: "src/main/index.ts (_digestHandle)"
      to: "src/main/ipc/briefing.ts (digestHandle dep for D-07.3)"
      via: "registerBriefingHandlers call with _digestHandle passed as deps.digestHandle"
      pattern: "_digestHandle"
    - from: "src/main/lifecycle/powerMonitor.ts (onResume)"
      to: "_digestHandle.runNow() in index.ts closure"
      via: "registerLifecycleCallbacks callback"
      pattern: "onResume"
---

<objective>
Wire the digest cron into the application bootstrap and lifecycle. This is the final plan — it connects all prior plans into a running system: the cron starts at boot, the unlock drain has a real dispatch, the powerMonitor wakes up missed 05:00 ticks, and the briefing handler receives the digest handle for the async fallback.

Purpose: Without this plan, digest-cron.ts exists but is never instantiated. The runChannelOnce stub (the documented no-op for all channels) is replaced with a real switch that dispatches the 'whatsapp-digest' drain. The powerMonitor hook covers the missed-while-sleeping case.

Output: Modified index.ts (3 additions: module-scope variable, bootPoll call, runChannelOnce switch) + modified powerMonitor.ts (onResume hook registration).
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-digest-briefing-integration/21-CONTEXT.md
@.planning/phases/21-digest-briefing-integration/21-RESEARCH.md
@.planning/phases/21-digest-briefing-integration/21-PATTERNS.md
@.planning/phases/21-digest-briefing-integration/21-03-SUMMARY.md
@.planning/phases/21-digest-briefing-integration/21-04-SUMMARY.md

<interfaces>
<!-- Critical seams in index.ts verified against live source (RESEARCH.md §11). -->

From src/main/index.ts (whatsAppManager module-scope variable, ~line 590 region):
```typescript
// Existing pattern (module-scope let for session manager):
let whatsAppManager: WhatsAppSessionManager | null = null;
// Phase 21 adds adjacent to this:
let _digestHandle: WhatsAppDigestHandle | null = null;
```

From src/main/index.ts lines 617-623 (startWhatsAppRetention bootstrap):
```typescript
startWhatsAppRetention({
  db: waDb,
  logger,
  scheduler,
  dbHolder,
});
// Phase 21 inserts immediately after line 623:
_digestHandle = startWhatsAppDigest({
  db: waDb,
  logger,
  scheduler,
  dbHolder,
});
```

From src/main/index.ts lines 869-882 (runChannelOnce NO-OP stub — the load-bearing fix):
```typescript
async function runChannelOnce(
  chan: CatchupChannel,
  _db: import('./db/connect').Db,
  logger: import('pino').Logger,
): Promise<void> {
  logger.info({ scope: 'catchup', channel: chan }, 'catchup run starting');
  await Promise.resolve();  // ← Phase 21 replaces this with real switch
  logger.info({ scope: 'catchup', channel: chan }, 'catchup run complete');
}
// Phase 21 change:
  switch (chan) {
    case 'whatsapp-digest':
      if (_digestHandle) await _digestHandle.runNow();
      break;
    default:
      await Promise.resolve();
  }
```

From src/main/index.ts lines 741-766 (unlock drain — no changes needed):
```typescript
registerOnUnlock(async (db) => {
  const channels = pendingCatchup.drain();
  if (channels.length === 0) { _trayHandle?.clearBadge(); return; }
  for (const chan of channels) {
    try { await runChannelOnce(chan, db, logger); } catch (err) { ... }
  }
  _trayHandle?.clearBadge();
});
```

From src/main/lifecycle/powerMonitor.ts lines 22-45 (registerLifecycleCallbacks signature):
```typescript
export function registerLifecycleCallbacks(cbs: LifecycleCallbacks): () => void
// LifecycleCallbacks: { onSuspend?: () => void; onResume?: () => void; }
```

From src/main/index.ts (registerBriefingHandlers call — find the actual call site to add digestHandle dep):
The call must pass _digestHandle so briefing.ts D-07.3 fallback fires.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _digestHandle bootstrap in index.ts — module-scope variable, bootPoll call, runChannelOnce switch</name>
  <files>src/main/index.ts</files>
  <read_first>
    - src/main/index.ts lines 75-90 (import block — find where startWhatsAppRetention is imported; add startWhatsAppDigest to same import or new import statement)
    - src/main/index.ts lines 585-630 (bootPoll block with startWhatsAppRetention + whatsAppManager initialization — insertion point for _digestHandle assignment)
    - src/main/index.ts lines 860-895 (runChannelOnce stub — the exact function to modify)
    - src/main/index.ts (find where registerBriefingHandlers is called — search for 'registerBriefingHandlers' — to add _digestHandle to its deps)
    - src/main/whatsapp/digest-cron.ts (WhatsAppDigestHandle type export from Plan 21-03)
    - .planning/phases/21-digest-briefing-integration/21-PATTERNS.md section index.ts (exact insertion points with line cites)
    - .planning/phases/21-digest-briefing-integration/21-RESEARCH.md section 11 (verified bootstrap location + runChannelOnce lines)
  </read_first>
  <action>
    Make four surgical modifications to src/main/index.ts. Read the full relevant regions (especially around lines 585-630 and 860-895) before editing.

    Modification 1 — Import: Add import { startWhatsAppDigest, type WhatsAppDigestHandle } from './whatsapp/digest-cron' near the existing import { startWhatsAppRetention } line (~line 82).

    Modification 2 — Module-scope variable: Declare let _digestHandle: WhatsAppDigestHandle | null = null at the top of the module, adjacent to where whatsAppManager (or similar module-scope manager variables) is declared.

    Modification 3 — Bootstrap call: In the bootPoll setTimeout block, immediately after the startWhatsAppRetention({...}) call (around line 623), add:
      _digestHandle = startWhatsAppDigest({ db: waDb, logger, scheduler, dbHolder });
    Pass the same deps as startWhatsAppRetention: db (as waDb), logger, scheduler, dbHolder. Do NOT pass generateTextFn or getLocalModelFn here (production always uses real functions — those are test seams only).

    Modification 4 — runChannelOnce: Replace the await Promise.resolve() line in runChannelOnce with a switch statement:
      switch (chan) {
        case 'whatsapp-digest':
          if (_digestHandle) await _digestHandle.runNow();
          break;
        default:
          await Promise.resolve();
      }
    The log lines before and after ("catchup run starting" / "catchup run complete") must remain unchanged.

    Modification 5 — Wire _digestHandle to registerBriefingHandlers: Find the call site of registerBriefingHandlers in index.ts. Add _digestHandle as the digestHandle dep. The exact shape depends on what registerBriefingHandlers accepts after Plan 21-04 — check 21-04-SUMMARY.md for the exact deps interface. Typically: registerBriefingHandlers({ ..., digestHandle: _digestHandle }).

    Note: the registerBriefingHandlers call is in the bootPoll block post-unlock. _digestHandle is assigned earlier in the same block (Modification 3), so it will be available when the briefing handler fires at BRIEFING_TODAY time (which is always post-unlock).

    All 5 modifications are one-line or small insertions. Do NOT restructure the file, do NOT reorder existing code.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/digest-cron.spec.ts tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "startWhatsAppDigest" src/main/index.ts returns at least 2 (import + call)
    - grep -c "_digestHandle" src/main/index.ts returns at least 3 (declaration + assignment + runChannelOnce use)
    - grep -c "whatsapp-digest" src/main/index.ts returns at least 1 (runChannelOnce case)
    - grep -c "await Promise.resolve" src/main/index.ts returns 0 in the runChannelOnce function (the stub is now a real switch)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>_digestHandle declared module-scope; startWhatsAppDigest called in bootPoll; runChannelOnce dispatches 'whatsapp-digest' channel; _digestHandle passed to registerBriefingHandlers. Zero compile errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add D-07.2 powerMonitor onResume missed-tick hook</name>
  <files>src/main/lifecycle/powerMonitor.ts</files>
  <read_first>
    - src/main/lifecycle/powerMonitor.ts (full file — registerLifecycleCallbacks signature + onSuspend/onResume callback array pattern)
    - src/main/index.ts (find the registerLifecycleCallbacks call site in the bootPoll block — to see where to add the onResume callback for digest. It may already exist for other purposes like retention)
    - .planning/phases/21-digest-briefing-integration/21-PATTERNS.md section powerMonitor.ts (exact onResume handler code)
    - .planning/phases/21-digest-briefing-integration/21-RESEARCH.md section 11 (powerMonitor registration pattern)
  </read_first>
  <action>
    Register the D-07.2 missed-tick check in src/main/index.ts (NOT in powerMonitor.ts itself — the hook registration goes in index.ts where _digestHandle is in scope; powerMonitor.ts only needs to be read to understand registerLifecycleCallbacks).

    Actually — since powerMonitor.ts is in files_modified, check if the registration call site is naturally in index.ts (more likely) or if a new helper should be added to powerMonitor.ts. The PATTERNS.md shows the call as:
      registerLifecycleCallbacks({ onResume: () => { ... } })
    called from index.ts in the bootPoll block after _digestHandle is assigned.

    If the registerLifecycleCallbacks call already exists in index.ts for other purposes (e.g. a prior phase added onResume for something else), extend that call's onResume callback. If no call exists, add a new registerLifecycleCallbacks call.

    The onResume handler (in the registerLifecycleCallbacks call from index.ts):
      onResume: () => {
        if (!_digestHandle) return;
        const db = dbHolder.db;
        if (!db) return;
        try {
          const today = new Date().toISOString().slice(0, 10);
          const row = db.prepare(
            'SELECT MAX(date) AS maxDate FROM whatsapp_group_digest'
          ).get() as { maxDate: string | null } | undefined;
          if (!row?.maxDate || row.maxDate < today) {
            void _digestHandle.runNow();
          }
        } catch {
          // best-effort — never block resume
        }
      }

    The per-day guard (row.maxDate < today) prevents wake-storm. The UNIQUE(jid,date) constraint absorbs overlap if the pendingCatchup drain also fires on the same day.

    If powerMonitor.ts itself requires a change (e.g., to expose a new hook registration API), update it accordingly. But the likely case is that powerMonitor.ts needs no change — only index.ts needs the new registerLifecycleCallbacks call.

    Update files_modified to reflect actual changes: if only index.ts changes, that is fine (powerMonitor.ts is a read target only in that case). Document in the SUMMARY.

    The UNIQUE(jid,date) constraint and the per-day guard together ensure this never results in duplicate rows even if both the pendingCatchup drain and the powerMonitor resume fire on the same unlock-after-sleep sequence.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/ tests/unit/main/ipc/ tests/unit/renderer/features/briefing/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "onResume" src/main/index.ts returns at least 1 (new callback registration for D-07.2)
    - grep -c "maxDate" src/main/index.ts returns at least 1 (the MAX(date) guard query)
    - npx tsc --noEmit exits 0
    - npx vitest run tests/unit/main/whatsapp/ tests/unit/main/ipc/ tests/unit/renderer/features/briefing/ exits 0 (all Phase 21 specs green)
    - npx vitest run tests/unit/main/whatsapp/no-frontier.ratchet.spec.ts exits 0 (SC3 ratchet still GREEN)
  </acceptance_criteria>
  <done>powerMonitor onResume hook registers missed-tick guard for 'whatsapp-digest'; all Phase 21 unit specs green; SC3 ratchet GREEN; zero compile errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| powerMonitor resume event → digest runNow() | OS wake event triggers a local LLM call; no network/IPC crossing |
| pendingCatchup drain → runChannelOnce dispatch | DB unlock event triggers digest generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-14 | Denial of Service | powerMonitor wakes repeatedly (e.g. laptop wakes on lid open/close) triggering multiple digest runs | mitigate | Per-day guard (row.maxDate < today) prevents redundant runs; INSERT OR REPLACE is idempotent; p-queue concurrency:1 prevents concurrent Ollama calls |
| T-21-15 | Tampering | runChannelOnce 'whatsapp-digest' case calls _digestHandle.runNow() which calls getLocalModel()/generateText() | accept | Local model only (SC3 ratchet on src/main/whatsapp/** already enforces no frontier imports); no user-supplied input |
| T-21-16 | Denial of Service | Unlock drain fires 'whatsapp-digest' while Ollama is offline — could block unlock | mitigate | runDigest() wraps generateText in try/catch and never re-throws; runChannelOnce calls await _digestHandle.runNow() but runDigest itself is bounded by p-queue + catch; worst case is a brief delay on unlock for NULL rows to be written |
</threat_model>

<verification>
Final phase gate — run all Phase 21 specs:
- npx vitest run tests/unit/main/whatsapp/ tests/unit/main/ipc/ tests/unit/renderer/features/briefing/ exits 0 (full suite green)
- npx vitest run tests/unit/main/whatsapp/no-frontier.ratchet.spec.ts exits 0 (SC3 GREEN)
- npx tsc --noEmit exits 0
- grep "startWhatsAppDigest" src/main/index.ts shows import + call
- grep "_digestHandle" src/main/index.ts shows declaration + bootPoll assignment + runChannelOnce use
- grep "whatsapp-digest" src/main/index.ts shows the case branch
</verification>

<success_criteria>
- Digest cron bootstraps at app boot (WA-08)
- pendingCatchup unlock drain has a real dispatch for 'whatsapp-digest' (D-07.1 — the load-bearing fix)
- powerMonitor onResume covers missed 05:00 ticks after sleep (D-07.2)
- _digestHandle passed to briefing handlers for D-07.3 async fallback
- All Phase 21 unit tests pass
- SC3 no-frontier ratchet stays GREEN (WA-09)
- Zero TypeScript compile errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-digest-briefing-integration/21-06-SUMMARY.md`
</output>
