---
phase: 21-digest-briefing-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - tests/unit/main/whatsapp/digest-cron.spec.ts
  - tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts
  - tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx
autonomous: true
requirements:
  - WA-08
  - WA-09
  - WA-10

must_haves:
  truths:
    - "digest-cron.spec.ts exists and all test stubs are RED (WA-10/D-06/D-07.1/D-07.2/D-09 per VALIDATION.md Wave 0)"
    - "briefing-whatsapp-enrichment.spec.ts exists and all test stubs are RED (WA-08/WA-10/D-07.3/D-10)"
    - "BriefingScreen.spec.tsx extended with three new WhatsApp union-state cases that compile but are RED"
    - "All three spec files follow the in-memory DB + vi.fn() injectable seam pattern (no real Ollama, no real electron)"
  artifacts:
    - path: "tests/unit/main/whatsapp/digest-cron.spec.ts"
      provides: "Test stubs for digest cron unit of work"
      min_lines: 80
    - path: "tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts"
      provides: "Test stubs for readWhatsAppDigests + BRIEFING_TODAY enrichment"
      min_lines: 80
    - path: "tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx"
      provides: "Extended with WhatsApp section render tests"
      contains: "briefing-whatsapp"
  key_links:
    - from: "digest-cron.spec.ts"
      to: "src/main/whatsapp/digest-cron.ts (Plan 21-03)"
      via: "startWhatsAppDigest import"
      pattern: "startWhatsAppDigest"
    - from: "briefing-whatsapp-enrichment.spec.ts"
      to: "src/main/ipc/briefing.ts (Plan 21-04)"
      via: "BRIEFING_TODAY handler + readWhatsAppDigests"
      pattern: "BRIEFING_TODAY"
---

<objective>
Create the three Wave-0 spec files required by VALIDATION.md before any implementation ships. Stubs RED now, GREEN after the corresponding implementation plans complete. This is the Nyquist gate for Phase 21 — automated verification exists before every behavior is implemented.

Purpose: Forces each implementation plan to make specific failing tests pass rather than drift from spec. The digest-cron spec uses injectable vi.fn() seams (no real Ollama). The briefing enrichment spec uses an in-memory better-sqlite3 DB. The BriefingScreen spec extends the existing makePayload/installAria pattern.

Output: Three spec files — two new (RED on import), one extended (new cases RED, existing cases GREEN).
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-digest-briefing-integration/21-CONTEXT.md
@.planning/phases/21-digest-briefing-integration/21-RESEARCH.md
@.planning/phases/21-digest-briefing-integration/21-PATTERNS.md
@.planning/phases/21-digest-briefing-integration/21-VALIDATION.md
@.planning/phases/21-digest-briefing-integration/21-01-SUMMARY.md

<interfaces>
<!-- Analog spec files — exact patterns to mirror. -->

From tests/unit/main/whatsapp/whatsapp-retention.spec.ts (in-memory DB setup pattern):
```typescript
// In-memory DB setup (lines 79-91):
beforeEach(() => {
  const dataDir = createTempUserDataDir('aria-retention');
  const dbKey = crypto.randomBytes(32);
  db = openDb({ dataDir, dbKey, runMigrationsOnOpen: false });
  runMigrations(db, { dir: MIGRATIONS_DIR });
  // Seed a tracked group:
  db.prepare(
    `INSERT INTO whatsapp_group (jid, display_name, member_count, tracked) VALUES (?, ?, ?, ?)`
  ).run('test-group@g.us', 'Test Group', 3, 1);
});

// Logger mock (line 109):
const loggerMock = { info: () => undefined, warn: () => undefined, debug: () => undefined, error: () => undefined };
```

From tests/unit/main/ipc/briefing-regenerate.spec.ts (IPC + vi.doMock electron pattern):
```typescript
// makeStubIpcMain (lines 24-37)
function makeStubIpcMain() {
  const handlers = new Map();
  return {
    ipcMain: {
      handle: (channel, h) => handlers.set(channel, h),
      removeHandler: (channel) => handlers.delete(channel),
    },
    invoke: (channel, payload) => {
      const h = handlers.get(channel);
      if (!h) throw new Error(`no handler for ${channel}`);
      return h({}, payload);
    },
  };
}

// vi.doMock('electron', ...) pattern (lines 39-66)
async function setupModules(dataDir) {
  vi.resetModules();
  vi.doMock('electron', () => ({
    app: { isReady: () => true, getPath: () => dataDir },
    safeStorage: { isEncryptionAvailable: () => true, ... },
  }));
  const briefingIpc = await import('../../../../src/main/ipc/briefing');
  const { CHANNELS } = await import('../../../../src/shared/ipc-contract');
  return { briefingIpc, CHANNELS };
}
```

From tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx (makePayload/installAria pattern):
```typescript
// makePayload override (lines 38-64):
function makePayload(over: Partial<BriefingPayload> = {}): BriefingPayload {
  return { date: '2026-05-20', generatedAt: '...', tz: 'UTC', calendar: [...], email: [...],
           news: [...], errors: {}, route: 'FRONTIER', reason: '...', model: '...', ...over };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create digest-cron.spec.ts with failing stubs for WA-10, D-06, D-07.1, D-07.2, D-09</name>
  <files>tests/unit/main/whatsapp/digest-cron.spec.ts</files>
  <read_first>
    - tests/unit/main/whatsapp/whatsapp-retention.spec.ts (full file — in-memory DB + logger mock + cron test pattern, the exact analog)
    - src/shared/ipc-contract.ts (CHANNELS.WHATSAPP_GENERATE_DIGEST_NOW — for the digest-now IPC test if needed)
    - .planning/phases/21-digest-briefing-integration/21-PATTERNS.md section digest-cron.spec.ts (key test cases)
    - .planning/phases/21-digest-briefing-integration/21-VALIDATION.md section Per-Task Verification Map
  </read_first>
  <action>
    Create tests/unit/main/whatsapp/digest-cron.spec.ts. Mirror the whatsapp-retention.spec.ts structure exactly: same imports (openDb, runMigrations, MIGRATIONS_DIR, createTempUserDataDir, crypto), same beforeEach in-memory DB setup, same loggerMock shape.

    Seed the DB in beforeEach: INSERT a tracked whatsapp_group row (jid='grp1@g.us', display_name='Alpha', member_count=5, tracked=1) and at least one whatsapp_message row with sent_at as an ISO 8601 string (new Date().toISOString()) and body_text='Hello world from sender' and wa_id='msg1'. Also insert a second tracked group (jid='grp2@g.us', display_name='Beta', tracked=1) with at least 3 message rows so both groups exceed the min-activity threshold.

    The mock LLM seams:
      const mockGenerateText = vi.fn().mockResolvedValue({ text: '### KEY POINTS\n- Point A\n### DECISIONS\n(nothing to report)\n### OPEN QUESTIONS\n(nothing to report)\n### MENTIONS\n(nothing to report)' })
      const mockGetLocalModel = vi.fn().mockReturnValue({})

    Import startWhatsAppDigest from 'src/main/whatsapp/digest-cron' — this import will fail (file not yet created) making these stubs RED by construction.

    Write the following it() cases (all inside a describe('WhatsApp digest cron')):

    1. "runNow() writes a digest row with non-NULL summary_text for a tracked group" — calls handle.runNow(), then queries whatsapp_group_digest for jid='grp1@g.us', asserts row exists with non-NULL summary_text. (WA-08/D-06)

    2. "generateText throws → digest row written with summary_text=NULL (Ollama-down path)" — mockGenerateText.mockRejectedValueOnce(new Error('connect ECONNREFUSED')), calls handle.runNow(), asserts row has summary_text=null. (WA-10/Pitfall 2)

    3. "re-running runNow() on a NULL row overwrites it with the new result (INSERT OR REPLACE)" — first run with rejected generateText (null row), then run again with resolved generateText, assert final row has non-NULL summary_text. (D-06/Pitfall 3)

    4. "dbHolder.db===null at cron tick → pendingCatchup adds 'whatsapp-digest'" — create handle with dbHolder={db:null} passed, fire the cron callback, import pendingCatchup and assert pendingCatchup.has('whatsapp-digest') is true. (D-07.1)

    5. "partial p-queue failure: grp1 succeeds, grp2 generateText throws → grp1 row non-NULL, grp2 row NULL" — mockGenerateText resolves on first call, rejects on second, calls runNow(), queries both groups and asserts per-group rows. (D-09)

    The startWhatsAppDigest factory receives: { db, logger: loggerMock as never, cron: '0 5 * * *', scheduler: null as never, dbHolder: null as never, generateTextFn: mockGenerateText, getLocalModelFn: mockGetLocalModel }

    Use afterEach to reset vi mocks. All tests are expected to be RED (import fails) until Plan 21-03 creates the implementation file.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/digest-cron.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/main/whatsapp/digest-cron.spec.ts exists with at least 80 lines
    - File contains the string "startWhatsAppDigest" (import from digest-cron.ts)
    - File contains the five describe/it blocks named above
    - File contains "INSERT OR REPLACE" expectation (either as a comment or direct DB assertion)
    - Running the spec returns a FAIL due to missing import, not a syntax error (the stubs are structurally valid)
  </acceptance_criteria>
  <done>digest-cron.spec.ts exists with 5 failing test stubs — all RED because the implementation file does not exist yet. No syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create briefing-whatsapp-enrichment.spec.ts with failing stubs for WA-08, WA-10, D-07.3, D-10 state matrix</name>
  <files>tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts</files>
  <read_first>
    - tests/unit/main/ipc/briefing-regenerate.spec.ts (full file — makeStubIpcMain + vi.doMock('electron') + in-memory DB pattern, the exact analog)
    - src/shared/ipc-contract.ts lines 200-215 (CHANNELS.BRIEFING_TODAY channel name)
    - src/shared/ipc-contract.ts lines 540-560 (BriefingPayload.whatsApp field just added in Plan 21-01)
    - .planning/phases/21-digest-briefing-integration/21-PATTERNS.md section briefing-whatsapp-enrichment.spec.ts
  </read_first>
  <action>
    Create tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts. Mirror briefing-regenerate.spec.ts: same makeStubIpcMain() helper, same vi.doMock('electron') pattern with app.getPath returning the temp dataDir, same dynamic import of briefing IPC module and CHANNELS via vi.resetModules().

    In setupModules, after vi.doMock('electron'), also vi.doMock the digest handle module to prevent it from running real cron:
      vi.doMock('../../../../src/main/whatsapp/digest-cron', () => ({ startWhatsAppDigest: () => ({ stop: vi.fn(), runNow: vi.fn().mockResolvedValue(undefined) }) }))

    The beforeEach creates: in-memory DB with migrations, seeds a provider_account row for whatsapp with status='ok', seeds a whatsapp_group row (jid='g1@g.us', tracked=1), and the test registers the briefing BRIEFING_TODAY handler by calling briefingIpc.registerBriefingHandlers({ ipcMain: stubIpcMain, db, logger: loggerMock }).

    Write the following it() cases:

    1. "no provider_account row → payload.whatsApp is undefined (D-10 not-linked omit)" — delete the provider_account row, invoke BRIEFING_TODAY, assert payload.whatsApp is undefined.

    2. "provider_account exists, zero tracked groups → payload.whatsApp is undefined (D-10 zero-groups omit)" — set tracked=0 on all groups, invoke BRIEFING_TODAY, assert undefined.

    3. "digest row with non-NULL summary_text → state='ready', groups[0].state='summarized'" — insert whatsapp_group_digest row for today with non-NULL summary_text, invoke BRIEFING_TODAY, assert payload.whatsApp.state==='ready' and payload.whatsApp.groups[0].state==='summarized'.

    4. "digest row with summary_text=NULL → state='unavailable', reason='model-offline'" — insert row with null summary_text, invoke BRIEFING_TODAY, assert payload.whatsApp.state==='unavailable' and reason==='model-offline'. (WA-10)

    5. "provider_account.status='degraded' → connection:'degraded' on the union arm" — set status='degraded', insert a summarized digest row, assert payload.whatsApp.connection==='degraded'.

    6. "BRIEFING_TODAY never throws even when readWhatsAppDigests internally throws" — use vi.spyOn to make the internal DB prepare throw, invoke BRIEFING_TODAY, assert it resolves (not rejects) and payload.whatsApp is undefined. (D-07.3 resilience)

    The BRIEFING_TODAY channel name is CHANNELS.BRIEFING_TODAY. Get the today ISO date string with new Date().toISOString().slice(0,10) for seeding rows.

    All stubs are RED until Plan 21-04 adds readWhatsAppDigests to briefing.ts and implements row.whatsApp enrichment.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts exists with at least 80 lines
    - File contains "BRIEFING_TODAY" reference
    - File contains "state==='unavailable'" assertion (WA-10)
    - File contains "never throws" or resilience test case (D-07.3)
    - File contains at least 5 distinct it() cases
    - Running the spec returns FAIL (missing readWhatsAppDigests in briefing.ts) or FAIL (payload.whatsApp undefined when it should be defined) — not a syntax error
  </acceptance_criteria>
  <done>briefing-whatsapp-enrichment.spec.ts exists with 6 failing test stubs covering the full D-10 state matrix and the D-07.3 never-throw invariant.</done>
</task>

<task type="auto">
  <name>Task 3: Extend BriefingScreen.spec.tsx with three new WhatsApp union-state test cases</name>
  <files>tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx</files>
  <read_first>
    - tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx (full file — makePayload + installAria pattern + existing describe block structure)
    - src/shared/ipc-contract.ts lines 540-560 (BriefingPayload.whatsApp field from Plan 21-01)
    - .planning/phases/21-digest-briefing-integration/21-PATTERNS.md section BriefingScreen.spec.tsx
  </read_first>
  <action>
    Add three new it() cases to the existing describe('BriefingScreen') block. Append them at the end of the existing test suite. These cases use the existing makePayload() helper (spread override with whatsApp field) and the existing screen query methods.

    Case 1: "Phase 21 — whatsApp.state=ready → renders briefing-whatsapp section"
      installAria(makePayload({ whatsApp: { state: 'ready', groups: [{ jid: 'g1@g.us', displayName: 'Team Leads', state: 'summarized', summaryText: '### KEY POINTS\n- Shipped v1' }] } }))
      render(<BriefingScreen />)
      expect(await screen.findByTestId('briefing-whatsapp')).toBeTruthy()

    Case 2: "Phase 21 — whatsApp.state=unavailable → renders unavailable note + retry button"
      installAria(makePayload({ whatsApp: { state: 'unavailable', reason: 'model-offline' } }))
      render(<BriefingScreen />)
      expect(await screen.findByTestId('briefing-whatsapp-unavailable')).toBeTruthy()
      expect(await screen.findByTestId('briefing-whatsapp-retry')).toBeTruthy()

    Case 3: "Phase 21 — whatsApp=undefined → WhatsApp section absent from DOM"
      installAria(makePayload()) // no whatsApp field
      render(<BriefingScreen />)
      await screen.findByTestId('briefing-section-calendar') // wait for render
      expect(screen.queryByTestId('briefing-whatsapp')).toBeNull()
      expect(screen.queryByTestId('briefing-whatsapp-unavailable')).toBeNull()

    The BriefingPayload import already includes the whatsApp? field from Plan 21-01. The data-testid values used (briefing-whatsapp, briefing-whatsapp-unavailable, briefing-whatsapp-retry) are the exact testids that Plan 21-05 must implement in the renderer. Cases 1 and 2 are RED until Plan 21-05 adds the render switch; Case 3 should already pass since the section is absent by default.

    Do NOT modify any existing test cases — append only. Preserve the existing file structure exactly.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains "briefing-whatsapp" testid query (at least once)
    - File contains "briefing-whatsapp-unavailable" testid query
    - File contains "briefing-whatsapp-retry" testid query
    - Existing test cases still pass (the three new cases fail, but pre-existing cases remain green)
    - npx vitest run tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx 2>&1 shows no syntax error (only assertion failures for the new cases)
  </acceptance_criteria>
  <done>BriefingScreen.spec.tsx has three new Phase 21 WhatsApp cases appended. Existing tests remain green. New cases are RED pending Plan 21-05 renderer implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files | Unit tests; no production trust boundary affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | Test seam allows mock generateText injection | accept | Test-only; the injectable seam (deps.generateTextFn) is the correct production pattern per ask.ts precedent; production path always uses real generateText from 'ai' |
| T-21-04 | Information Disclosure | Spec files contain mock WhatsApp message bodies | accept | Test-only fixture strings; no real user data; in-memory DB disposed after each test |
</threat_model>

<verification>
After all three tasks:
- npx vitest run tests/unit/main/whatsapp/digest-cron.spec.ts 2>&1 shows FAIL (missing import), not syntax error
- npx vitest run tests/unit/main/ipc/briefing-whatsapp-enrichment.spec.ts 2>&1 shows FAIL (missing impl), not syntax error
- npx vitest run tests/unit/renderer/features/briefing/BriefingScreen.spec.tsx 2>&1 shows partial FAIL (new cases only), existing cases GREEN
- All three files exist on disk
</verification>

<success_criteria>
- Three Wave-0 spec files created/extended per VALIDATION.md requirements
- All new test stubs structurally valid (no syntax errors)
- Existing BriefingScreen tests remain green
- New cases are RED (implementation not yet written) — this is correct
- Nyquist gate is satisfied: automated verify exists for every Phase 21 behavior before implementation
</success_criteria>

<output>
After completion, create `.planning/phases/21-digest-briefing-integration/21-02-SUMMARY.md`
</output>
