---
phase: 10-knowledge-folders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db/migrations/132_knowledge_folders.sql
  - src/main/folder-ingestion/folder-registry.ts
  - src/main/folder-ingestion/folder-registry.test.ts
  - src/main/folder-ingestion/parsers/index.ts
  - src/main/folder-ingestion/parsers/text.ts
  - src/main/folder-ingestion/parsers/markdown.ts
  - src/main/folder-ingestion/parsers/csv.ts
  - src/main/folder-ingestion/parsers/docx.ts
  - src/main/folder-ingestion/parsers/xlsx.ts
  - src/main/folder-ingestion/parsers/pdf.ts
  - src/main/folder-ingestion/parsers/parsers.test.ts
  - src/main/folder-ingestion/prescan.ts
  - src/main/folder-ingestion/prescan.test.ts
  - src/main/folder-ingestion/ingestion-service.ts
  - src/main/folder-ingestion/ingestion-service.test.ts
  - src/main/ipc/knowledge-folders.ts
  - src/main/ipc/index.ts
  - src/preload/index.ts
  - package.json
autonomous: true
requirements:
  - SPEC-§3
  - SPEC-§4
  - SPEC-§5
  - SPEC-§6
  - SPEC-§8
  - CONTEXT-reconciliation
must_haves:
  truths:
    - "Migration 132 runs cleanly on existing DB and preserves all rag_chunk rows and rag_embedding rows"
    - "rag_chunk CHECK constraint accepts source_kind='folder' after migration"
    - "knowledge_folders and knowledge_files tables exist with the canonical column shapes from spec §4"
    - "FolderRegistry CRUD persists folders and files and exposes them via stable IPC channels"
    - "Each of .txt/.md/.csv/.docx/.xlsx/.pdf parses to a ParsedDocument under golden-file tests"
    - "Pre-scan walks a folder counting files and bytes without parsing and returns {fileCount,totalBytes} below the 5k/2GB thresholds or above"
    - "FolderIngestionService writes folder chunks into rag_chunk via the existing createIndexWriter pipeline with source_kind='folder'"
    - "rag_chunk.sensitivity is written as 'folder:high' or 'folder:low' according to knowledge_folders.sensitivity at index time, with sensitivity_model='folder-rule:v1'"
    - "bytesIndexed IPC returns SUM(knowledge_files.size) with no status filter"
  artifacts:
    - path: "src/main/db/migrations/132_knowledge_folders.sql"
      provides: "knowledge_folders + knowledge_files tables; rag_chunk extension to accept source_kind='folder'; folder_id + file_id columns; FK file_id->knowledge_files ON DELETE CASCADE"
      contains: "PRAGMA foreign_keys=OFF"
    - path: "src/main/folder-ingestion/folder-registry.ts"
      provides: "CRUD over knowledge_folders and knowledge_files"
      exports: ["createFolderRegistry", "FolderRegistry"]
    - path: "src/main/folder-ingestion/parsers/index.ts"
      provides: "Parser registry mapping extension -> parse(path) -> ParsedDocument"
      exports: ["getParserFor", "PARSERS"]
    - path: "src/main/folder-ingestion/ingestion-service.ts"
      provides: "Scan folder, parse each file, hand ParsedDocument to existing createIndexWriter with source_kind='folder', folder_id, file_id; writes sensitivity='folder:high'/'folder:low' based on folder flag"
      exports: ["createFolderIngestionService"]
    - path: "src/main/folder-ingestion/prescan.ts"
      provides: "Read-only file/byte counting walk respecting exclude globs"
      exports: ["prescanFolder"]
    - path: "src/main/ipc/knowledge-folders.ts"
      provides: "IPC channels: aria:knowledge:pick-folder, aria:knowledge:prescan-folder, aria:knowledge:add-folder, aria:knowledge:list-folders, aria:knowledge:remove-folder, aria:knowledge:set-sensitivity, aria:knowledge:folder-stats, aria:knowledge:reindex"
      exports: ["registerKnowledgeFolderIpc"]
  key_links:
    - from: "src/main/folder-ingestion/ingestion-service.ts"
      to: "src/main/rag/index-writer.ts"
      via: "createIndexWriter().upsertSource(doc) with sourceKind='folder'"
      pattern: "createIndexWriter|upsertSource"
    - from: "src/main/ipc/knowledge-folders.ts"
      to: "src/main/folder-ingestion/folder-registry.ts"
      via: "registry.* invoked from each handler"
      pattern: "registry\\."
    - from: "src/main/ipc/index.ts"
      to: "src/main/ipc/knowledge-folders.ts"
      via: "registerKnowledgeFolderIpc(...)"
      pattern: "registerKnowledgeFolderIpc"
---

<objective>
Lay the storage + parsing + ingestion foundation for Phase 10 Knowledge Folders.

Purpose: Allow the rest of Phase 10 (gate widening, watcher, UI) to assume that a registered folder can be walked, parsed, and turned into `rag_chunk` rows with `source_kind='folder'` and pre-tagged `sensitivity` via the existing index-writer pipeline. No retrieval-time JOIN against `knowledge_folders`; sensitivity is baked into the chunk row at write time.

Output: Migration 132, `src/main/folder-ingestion/` service (registry + parsers + prescan + ingestion-service), and an IPC surface ready for the Wave 3 UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-knowledge-folders/10-CONTEXT.md
@docs/superpowers/specs/2026-05-21-knowledge-folders-design.md
@src/main/db/migrations/126_rag_index.sql
@src/main/db/migrations/127_rag_source_dirty_dedupe.sql
@src/main/db/migrations/131_entitlement.sql
@src/main/rag/index-writer.ts
@src/main/rag/sensitivity-cache.ts
@src/main/rag/chunk-types.ts
@src/main/ipc/index.ts

<interfaces>
From src/main/rag/index-writer.ts:
- createIndexWriter(deps: { db, logger, strategy, classify, classifierModelId }): IndexWriter
- IndexWriter.upsertSource(doc: SourceDoc): Promise<UpsertResult>
- SourceDoc has sourceKind (extending to include 'folder'), sourceId, title, text, charStart, charEnd, providerKey, accountId, parentRef, sourceUpdatedAt

From src/main/rag/chunk-types.ts (extend with care — folder is a new SourceKind):
- SourceKind currently 'email'|'event'|'note'|'action'; this plan widens the type alias to include 'folder'

Folder ingestion must NOT bypass createIndexWriter; instead inject a deterministic `classify` that returns the per-folder rule sensitivity ('folder:high' if folder.sensitivity='sensitive' else 'folder:low') with classifierModelId='folder-rule:v1'. This gives folder chunks the same write-time persisted sensitivity as other corpora.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 132 — knowledge_folders, knowledge_files, rag_chunk extension</name>
  <files>src/main/db/migrations/132_knowledge_folders.sql</files>
  <read_first>
    - src/main/db/migrations/126_rag_index.sql
    - src/main/db/migrations/127_rag_source_dirty_dedupe.sql
    - src/main/db/migrations/131_entitlement.sql
    - .planning/phases/10-knowledge-folders/10-CONTEXT.md (schema_reconciliation section)
  </read_first>
  <action>
    Author `132_knowledge_folders.sql` (next-in-sequence after `131_entitlement.sql`). The migration MUST follow the exact pattern of `127_rag_source_dirty_dedupe.sql` (PRAGMA foreign_keys=OFF + create-new / copy / drop / rename). Steps in order:

    1. `PRAGMA foreign_keys=OFF;` at the very top.
    2. Create `knowledge_folders` per spec §4: `id TEXT PRIMARY KEY` (sha256 of absolute path), `path TEXT NOT NULL`, `label TEXT NOT NULL`, `sensitivity TEXT NOT NULL CHECK (sensitivity IN ('general','sensitive'))`, `status TEXT NOT NULL CHECK (status IN ('active','paused','error'))`, `last_scan_at TEXT`, `last_error TEXT`, `created_at TEXT NOT NULL`, `updated_at TEXT NOT NULL`, UNIQUE(path).
    3. Create `knowledge_files`: `id TEXT PRIMARY KEY` (sha256(folder_id||relative_path)), `folder_id TEXT NOT NULL REFERENCES knowledge_folders(id) ON DELETE CASCADE`, `relative_path TEXT NOT NULL`, `absolute_path TEXT NOT NULL`, `size INTEGER NOT NULL`, `mtime TEXT NOT NULL`, `content_hash TEXT`, `status TEXT NOT NULL CHECK (status IN ('pending','indexed','error','tombstoned','skipped'))`, `last_error TEXT`, `tombstoned_at TEXT`, `created_at TEXT NOT NULL`, `updated_at TEXT NOT NULL`, UNIQUE(folder_id, relative_path). Add `CREATE INDEX idx_knowledge_files_folder ON knowledge_files(folder_id);` and `CREATE INDEX idx_knowledge_files_tombstoned ON knowledge_files(status, tombstoned_at) WHERE status='tombstoned';`.
    4. Recreate `rag_chunk` to extend the CHECK and add nullable `folder_id TEXT` and `file_id TEXT`:
       - `CREATE TABLE rag_chunk_new (...)` with ALL existing columns from 126_rag_index.sql in identical order and types, with `source_kind` CHECK widened to `IN ('email','event','note','action','folder')`, plus two new trailing columns `folder_id TEXT` and `file_id TEXT`, plus `FOREIGN KEY (file_id) REFERENCES knowledge_files(id) ON DELETE CASCADE`.
       - `INSERT INTO rag_chunk_new (<all existing columns>, folder_id, file_id) SELECT <all existing columns>, NULL, NULL FROM rag_chunk;` — list every column explicitly.
       - `DROP TABLE rag_chunk;`
       - `ALTER TABLE rag_chunk_new RENAME TO rag_chunk;`
       - Recreate all four existing indexes from 126: `idx_rag_chunk_source`, `idx_rag_chunk_dirty`, `idx_rag_chunk_account`, `idx_rag_chunk_alive` (verbatim definitions from 126).
       - Recreate the three FTS triggers `rag_chunk_ai`, `rag_chunk_ad`, `rag_chunk_au` verbatim from 126.
       - Add `CREATE INDEX idx_rag_chunk_file_id ON rag_chunk(file_id) WHERE file_id IS NOT NULL;`.
    5. `PRAGMA user_version = 132;`
    6. `PRAGMA foreign_keys=ON;` at the very end.

    Do NOT use `ALTER TABLE chunks` anywhere. Do NOT use `CREATE TABLE IF NOT EXISTS rag_chunk`. The discriminator is `source_kind`, not `corpus`.
  </action>
  <verify>
    <automated>npm test -- src/main/db/migrations/__tests__/132_knowledge_folders.test.ts</automated>
    <manual>Inspect migration file for PRAGMA foreign_keys=OFF, recreate-table pattern, all four 126 indexes, three triggers, and new idx_rag_chunk_file_id index.</manual>
  </verify>
  <acceptance_criteria>
    - File `src/main/db/migrations/132_knowledge_folders.sql` exists.
    - `grep -c "PRAGMA foreign_keys=OFF" src/main/db/migrations/132_knowledge_folders.sql` >= 1.
    - `grep -c "PRAGMA user_version = 132" src/main/db/migrations/132_knowledge_folders.sql` == 1.
    - `grep -c "CREATE TABLE knowledge_folders" src/main/db/migrations/132_knowledge_folders.sql` == 1.
    - `grep -c "CREATE TABLE knowledge_files" src/main/db/migrations/132_knowledge_folders.sql` == 1.
    - `grep -c "CREATE TABLE rag_chunk_new" src/main/db/migrations/132_knowledge_folders.sql` == 1.
    - `grep -c "DROP TABLE rag_chunk" src/main/db/migrations/132_knowledge_folders.sql` == 1.
    - `grep -c "ALTER TABLE rag_chunk_new RENAME TO rag_chunk" src/main/db/migrations/132_knowledge_folders.sql` == 1.
    - `grep -E "source_kind IN \\('email','event','note','action','folder'\\)" src/main/db/migrations/132_knowledge_folders.sql` returns a match.
    - `grep -c "ALTER TABLE chunks" src/main/db/migrations/132_knowledge_folders.sql` == 0.
    - `grep -c "CREATE TABLE IF NOT EXISTS rag_chunk" src/main/db/migrations/132_knowledge_folders.sql` == 0.
    - `grep -c "REFERENCES knowledge_files(id) ON DELETE CASCADE" src/main/db/migrations/132_knowledge_folders.sql` == 1.
    - `grep -c "idx_rag_chunk_file_id" src/main/db/migrations/132_knowledge_folders.sql` == 1.
    - Test asserts: after running 132 against a fixture DB pre-populated with 5 rag_chunk rows and matching rag_embedding rows, all 5 chunk rows and embedding rows survive; `INSERT INTO rag_chunk (... source_kind='folder' ...)` succeeds; `INSERT INTO rag_chunk (... source_kind='garbage' ...)` fails with a CHECK violation.
  </acceptance_criteria>
  <done>Migration committed; integration test demonstrates pre-existing rag_chunk + rag_embedding rows survive; source_kind='folder' is now an accepted value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parser registry + FolderRegistry + prescan</name>
  <files>
    src/main/folder-ingestion/parsers/index.ts,
    src/main/folder-ingestion/parsers/text.ts,
    src/main/folder-ingestion/parsers/markdown.ts,
    src/main/folder-ingestion/parsers/csv.ts,
    src/main/folder-ingestion/parsers/docx.ts,
    src/main/folder-ingestion/parsers/xlsx.ts,
    src/main/folder-ingestion/parsers/pdf.ts,
    src/main/folder-ingestion/parsers/parsers.test.ts,
    src/main/folder-ingestion/folder-registry.ts,
    src/main/folder-ingestion/folder-registry.test.ts,
    src/main/folder-ingestion/prescan.ts,
    src/main/folder-ingestion/prescan.test.ts,
    package.json
  </files>
  <read_first>
    - src/main/db/migrations/126_rag_index.sql
    - src/main/rag/chunk-types.ts
    - .planning/phases/10-knowledge-folders/10-CONTEXT.md
    - docs/superpowers/specs/2026-05-21-knowledge-folders-design.md (sections §5, §6)
  </read_first>
  <behavior>
    - text.parse: reads UTF-8 .txt, returns {text, sectionLocators=[]}.
    - markdown.parse: returns {text, sectionLocators=[{label,charStart,charEnd}]} from H1/H2/H3 lines.
    - csv.parse via papaparse: returns header-row-prefixed flat text; each row carries a sectionLocator {label: `row ${i+1}`, charStart, charEnd}.
    - docx.parse via mammoth: returns extracted text, paragraph-derived locators.
    - xlsx.parse via exceljs: returns sheet-by-sheet flattened text with sheet-name locators.
    - pdf.parse via pdfjs-dist legacy build: returns page-concatenated text with `page N` locators; on `last_error='likely_scanned_no_ocr'` returns empty text and an error.
    - Each parser obeys 50MB hard skip (rejects with `size_exceeds_limit`) and 5MB extracted-text truncate.
    - FolderRegistry: addFolder, listFolders, removeFolder (cascade by knowledge_files FK then by folder_id ON DELETE CASCADE), setSensitivity, addFile, markFileIndexed, markFileError, tombstoneFile, resurrectFile, listFilesForFolder, sumBytesForFolder (NO status filter — `SELECT COALESCE(SUM(size),0) FROM knowledge_files WHERE folder_id=?`).
    - prescanFolder(absolutePath, excludes): walks the dir without parsing, returns {fileCount,totalBytes}, respects exclude globs from spec §6 (`node_modules`, `.git`, `dist`, `out`, `.next`, OS junk).
  </behavior>
  <action>
    Add to `package.json` dependencies: `papaparse`, `mammoth`, `exceljs`, `pdfjs-dist` (legacy build entry), and `@types/papaparse` to devDependencies. Lock versions to the latest stable at planning time; pin majors so reproducible installs work.

    Build the parser registry under `src/main/folder-ingestion/parsers/`. `index.ts` exports `PARSERS: Record<Extension, Parser>` and `getParserFor(filename: string): Parser | null`. Each parser exports `parse(absolutePath: string): Promise<ParsedDocument>` where `ParsedDocument = { text: string; sectionLocators: Array<{label, charStart, charEnd}>; truncated: boolean }`.

    Build `folder-registry.ts` taking a `Database` handle. All writes are synchronous prepared statements. `removeFolder(id)` performs `DELETE FROM knowledge_folders WHERE id=?` — cascade handles knowledge_files and (via file_id FK) rag_chunk rows. `setSensitivity(folderId, sensitivity)` updates the row (the chunk-update is owned by plan 10-02). `sumBytesForFolder(folderId)` does `SELECT COALESCE(SUM(size),0) AS bytes FROM knowledge_files WHERE folder_id = ?` — NO status filter.

    Build `prescan.ts`: a non-recursive iterative directory walk (queue of dirs) using `fs.promises.readdir(..., { withFileTypes: true })` and `fs.promises.stat`. Skip entries matching exclude globs. Return `{ fileCount, totalBytes }`. Cap absolute walk at 60s; if exceeded throw `prescan_timeout`.

    Golden-file tests for each parser use fixtures under `tests/fixtures/folder-ingestion/` (create as needed). Registry tests use in-memory sqlite seeded with migration 132. Prescan tests use a temp dir built with `fs.mkdtemp`.
  </action>
  <verify>
    <automated>npm test -- src/main/folder-ingestion/</automated>
  </verify>
  <acceptance_criteria>
    - `getParserFor("foo.txt")`, `("foo.md")`, `("foo.csv")`, `("foo.docx")`, `("foo.xlsx")`, `("foo.pdf")` all return a non-null parser; `("foo.pptx")` returns null.
    - Each parser test verifies the produced `ParsedDocument.text` matches a golden string for a fixture file.
    - PDF parser test asserts `last_error='likely_scanned_no_ocr'` path for a fixture with no extractable text.
    - 50MB hard skip: parser called with a fixture > 50MB rejects with `size_exceeds_limit` without reading the entire file (verify by stubbing fs size).
    - FolderRegistry: addFolder, addFile, listFilesForFolder roundtrip; removeFolder cascades and `SELECT COUNT(*) FROM knowledge_files WHERE folder_id=?` returns 0; `sumBytesForFolder` includes rows where `status='error'` (test seeds one with size=1000 status=error and expects the sum to include the 1000).
    - `grep -c "AND status" src/main/folder-ingestion/folder-registry.ts` reading `sumBytesForFolder` returns 0 inside that function (no status filter on the SUM query).
    - Prescan test: temp dir with 7 files + a `node_modules` containing 100 files; result is `{fileCount: 7, totalBytes: <sum>}`.
  </acceptance_criteria>
  <done>All parser, registry, and prescan tests pass; package.json updated; folder-ingestion module exports stable interfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FolderIngestionService + Knowledge IPC surface</name>
  <files>
    src/main/folder-ingestion/ingestion-service.ts,
    src/main/folder-ingestion/ingestion-service.test.ts,
    src/main/ipc/knowledge-folders.ts,
    src/main/ipc/index.ts,
    src/preload/index.ts
  </files>
  <read_first>
    - src/main/rag/index-writer.ts
    - src/main/rag/sensitivity-cache.ts
    - src/main/rag/chunk-types.ts
    - src/main/rag/chunk-strategies.ts
    - src/main/ipc/index.ts
    - src/main/ipc/rag.ts
    - src/preload/index.ts
    - .planning/phases/10-knowledge-folders/10-CONTEXT.md
  </read_first>
  <behavior>
    - createFolderIngestionService({db, logger, registry, parsers, writer}) returns an object with `ingestFolderOnce(folderId)` and `ingestFile(folderId, absolutePath)`.
    - For each file: parser produces ParsedDocument, service derives sectionLocators-aware chunks via the existing chunking strategy, then calls `writer.upsertSource(doc)` with `doc.sourceKind='folder'`, `doc.sourceId=fileId`, `doc.folderId=folderId`, `doc.fileId=fileId`, `doc.title=relativePath`, `doc.text=parsed.text`.
    - The `classify` callback injected into createIndexWriter for folder ingestion returns a deterministic rule: if `folder.sensitivity==='sensitive'` then `'folder:high'` else `'folder:low'`; `classifierModelId='folder-rule:v1'`. NO LLM call.
    - IPC channels (registered in `src/main/ipc/knowledge-folders.ts`):
      - `aria:knowledge:pick-folder` -> `{path: string} | {canceled: true}` (uses `dialog.showOpenDialog` with `properties: ['openDirectory']`)
      - `aria:knowledge:prescan-folder` -> input `{path}`, output `{fileCount, totalBytes, exceedsThreshold: boolean}` where exceedsThreshold = `fileCount > 5000 || totalBytes > 2*1024*1024*1024`
      - `aria:knowledge:add-folder` -> input `{path, label, sensitivity}`, output `{folderId}`
      - `aria:knowledge:list-folders` -> output `Array<{id, path, label, sensitivity, status, fileCount, bytesIndexed, lastScanAt, lastError}>`. `bytesIndexed = registry.sumBytesForFolder(id)`.
      - `aria:knowledge:remove-folder` -> input `{folderId}`, output `{ok: true}` (caller confirms; this handler does NOT prompt)
      - `aria:knowledge:set-sensitivity` -> input `{folderId, sensitivity}`, output `{ok: true}`. Phase 10-01 implements the registry write only; the rag_chunk UPDATE is plan 10-02.
      - `aria:knowledge:folder-stats` -> input `{folderId}`, output `{fileCount, bytesIndexed, indexedCount, errorCount, pendingCount, tombstonedCount, lastScanAt}`
      - `aria:knowledge:reindex` -> input `{folderId}`, output `{ok: true}` (queues ingestion via Wave 2's worker; this plan registers a stub that calls ingestionService.ingestFolderOnce directly)
    - Preload exposes them under `window.aria.knowledge.*`.
    - `src/main/ipc/index.ts` calls `registerKnowledgeFolderIpc(...)` during startup, with the dependency-injection pattern already used by sibling IPC modules.
  </behavior>
  <action>
    Author `ingestion-service.ts` with `createFolderIngestionService(deps)` returning `{ingestFolderOnce, ingestFile}`. The service walks `knowledge_files` for the folder (or one file path), runs the parser registry, and dispatches each result through a per-call `createIndexWriter(...)` invocation whose `classify` is the deterministic folder-rule function. Use the existing chunking strategy from `src/main/rag/chunk-strategies.ts` (passed in via deps so tests can swap).

    Author `ipc/knowledge-folders.ts` exporting `registerKnowledgeFolderIpc({ipcMain, registry, ingestionService, dialog})`. Wire each channel listed above. The `list-folders` handler MUST compute `bytesIndexed` via `registry.sumBytesForFolder(id)` (no status filter).

    Update `src/main/ipc/index.ts` to import and call `registerKnowledgeFolderIpc` alongside other registrations.

    Update `src/preload/index.ts` to expose the new channels under `window.aria.knowledge` with `invoke` wrappers (mirror the pattern of the existing `window.aria.rag` exposure).

    Tests use an in-memory sqlite with migration 132 applied, a stub parser registry, and a minimal real `createIndexWriter`. The integration test seeds two folders (one sensitive, one general) plus three files, runs `ingestFolderOnce` on each, then asserts:
    1. Every produced `rag_chunk` row has `source_kind='folder'`, the correct `folder_id`, `file_id`, and `sensitivity` ('folder:high' for the sensitive folder, 'folder:low' for the general one).
    2. `sensitivity_model='folder-rule:v1'` on all produced rows.
    3. `rag_embedding` cascade rows survive folder/file existence; deleting the file cascades chunks and embeddings.
    4. The `aria:knowledge:list-folders` channel returns `bytesIndexed` equal to the sum of all `knowledge_files.size` for that folder including a seeded row with `status='error'`.
  </action>
  <verify>
    <automated>npm test -- src/main/folder-ingestion/ingestion-service.test.ts src/main/ipc/knowledge-folders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/main/folder-ingestion/ingestion-service.ts` exists and exports `createFolderIngestionService`.
    - `grep -E "sourceKind:\\s*'folder'" src/main/folder-ingestion/ingestion-service.ts` returns at least one match.
    - `grep -E "'folder-rule:v1'" src/main/folder-ingestion/ingestion-service.ts` returns at least one match.
    - `grep -E "'folder:high'|'folder:low'" src/main/folder-ingestion/ingestion-service.ts` returns at least two matches.
    - File `src/main/ipc/knowledge-folders.ts` exists and registers exactly the eight channels listed in `<behavior>`.
    - `grep -c "aria:knowledge:" src/main/ipc/knowledge-folders.ts` >= 8.
    - `grep -c "registerKnowledgeFolderIpc" src/main/ipc/index.ts` >= 1.
    - `grep -c "knowledge" src/preload/index.ts` >= 1.
    - Phase 8 14d gate untouched: `grep -E "source_kind\s+IN" src/main/insights/gate.ts` does NOT contain `'folder'` (assert via test).
    - Phase 7 reconciler untouched: `git diff src/main/rag/model-swap-reconciler.ts` is empty after this plan commits.
    - Integration test passes all four assertions in `<action>`.
  </acceptance_criteria>
  <done>FolderIngestionService writes folder chunks through the existing pipeline with correct sensitivity tagging; IPC surface complete; preload bindings exposed; Phase 7 reconciler and Phase 8 gate files unchanged.</done>
</task>

</tasks>

<verification>
- Migration 132 idempotent and preserves rag_chunk + rag_embedding rows
- knowledge_folders / knowledge_files tables conform to spec §4
- rag_chunk now accepts source_kind='folder' under CHECK
- Parsers cover all six file types under golden-file tests
- FolderIngestionService writes via existing createIndexWriter
- Folder chunks carry sensitivity='folder:high'|'folder:low' + sensitivity_model='folder-rule:v1'
- 8 IPC channels registered and reachable via preload
- bytesIndexed includes errored files (no status filter)
- src/main/rag/model-swap-reconciler.ts diff = empty
- src/main/insights/gate.ts diff = empty
</verification>

<success_criteria>
A subsequent plan (10-02) can register a folder, walk it, parse files, and find their chunks in `rag_chunk` with the correct sensitivity tag — without any further schema work or new index-writer code.
</success_criteria>

<output>
After completion, create `.planning/phases/10-knowledge-folders/10-01-SUMMARY.md`
</output>
