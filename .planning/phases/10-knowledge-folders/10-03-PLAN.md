---
phase: 10-knowledge-folders
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/renderer/features/settings/KnowledgeFoldersSection.tsx
  - src/renderer/features/settings/KnowledgeFoldersSection.test.tsx
  - src/renderer/features/settings/SettingsScreen.tsx
  - src/renderer/app/routes.tsx
  - tests/integration/knowledge-folders-e2e.spec.ts
  - tests/integration/knowledge-folders-inflight-flip.spec.ts
  - .planning/phases/10-knowledge-folders/10-UAT.md
autonomous: false
requirements:
  - SPEC-§8
  - SPEC-§10
  - SPEC-§12
  - CONTEXT-reconciliation
must_haves:
  truths:
    - "KnowledgeFoldersSection.tsx is imported by SettingsScreen.tsx and reachable from the Settings nav"
    - "User can pick a folder, see pre-scan results, get the threshold confirm dialog at 5k files OR 2GB, and add the folder"
    - "User can flip a folder's sensitivity in the UI and the chunk-tags update in the same transaction"
    - "User can remove a folder; a confirm dialog gates the IPC; cancel preserves the folder; confirm removes it (3-split assertion)"
    - "Listed folders show fileCount, bytesIndexed (no status filter), lastScanAt, status"
    - "End-to-end test exercises: add folder -> ingest -> /ask returns citation -> flip sensitivity -> next /ask is LOCAL"
    - "In-flight flip integration test passes: pending answer-service call completes with pre-flip routing; next call respects new value"
  artifacts:
    - path: "src/renderer/features/settings/KnowledgeFoldersSection.tsx"
      provides: "Editorial Knowledge Folders Settings section"
      min_lines: 200
    - path: "tests/integration/knowledge-folders-e2e.spec.ts"
      provides: "End-to-end happy path including sensitivity flip"
    - path: "tests/integration/knowledge-folders-inflight-flip.spec.ts"
      provides: "In-flight flip case 5 from the sensitivity test contract"
    - path: ".planning/phases/10-knowledge-folders/10-UAT.md"
      provides: "Manual UAT checklist"
  key_links:
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "src/renderer/features/settings/KnowledgeFoldersSection.tsx"
      via: "import + route entry"
      pattern: "KnowledgeFoldersSection"
    - from: "src/renderer/features/settings/KnowledgeFoldersSection.tsx"
      to: "src/main/ipc/knowledge-folders.ts"
      via: "window.aria.knowledge.* invokes"
      pattern: "window\\.aria\\.knowledge"
---

<objective>
Ship the user-facing surface for Knowledge Folders and prove the whole stack end-to-end (including the in-flight flip case from the sensitivity test contract).

Purpose: Without a reachable UI, the verifier blindspot ([[feedback_verifier_blindspot_ui_wiring]]) bites — the section must be wired into SettingsScreen and grep-detectable.

Output: `KnowledgeFoldersSection.tsx` rendered from the Settings nav, two integration suites covering the happy path and the in-flight flip, and a 10-UAT.md checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-knowledge-folders/10-CONTEXT.md
@docs/superpowers/specs/2026-05-21-knowledge-folders-design.md
@src/renderer/features/settings/SettingsScreen.tsx
@src/renderer/features/settings/RagIndexSection.tsx
@src/renderer/features/settings/IntegrationsSection.tsx
@src/renderer/components/AddAccountModal.tsx
@src/renderer/features/onboarding/BackupRestoreSection.tsx
@src/main/ipc/knowledge-folders.ts
@src/main/rag/answer-router.ts
@.planning/phases/10-knowledge-folders/10-01-SUMMARY.md
@.planning/phases/10-knowledge-folders/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KnowledgeFoldersSection UI + threshold confirm + destructive-remove dialog</name>
  <files>
    src/renderer/features/settings/KnowledgeFoldersSection.tsx,
    src/renderer/features/settings/KnowledgeFoldersSection.test.tsx,
    src/renderer/features/settings/SettingsScreen.tsx,
    src/renderer/app/routes.tsx
  </files>
  <read_first>
    - src/renderer/features/settings/SettingsScreen.tsx
    - src/renderer/features/settings/RagIndexSection.tsx
    - src/renderer/features/settings/IntegrationsSection.tsx
    - src/renderer/features/onboarding/BackupRestoreSection.tsx
    - src/renderer/components/AddAccountModal.tsx
    - src/main/ipc/knowledge-folders.ts
    - docs/superpowers/specs/2026-05-21-knowledge-folders-design.md (§8, §10)
  </read_first>
  <behavior>
    - On mount, calls `window.aria.knowledge.listFolders()` and renders one card per folder with: label, path (truncated middle), sensitivity badge (gold for sensitive, neutral for general), `fileCount`, `bytesIndexed` (formatted), status, lastScanAt, lastError if present.
    - "Add folder" button -> `pick-folder` -> if path returned, calls `prescan-folder` -> if `exceedsThreshold`, renders a confirm dialog `"This folder has {fileCount} files ({formatted size}). Initial indexing will take a while. Continue?"` with [Cancel, Continue]; below threshold, opens the add-folder modal directly.
    - Add-folder modal collects label + sensitivity radio (general / sensitive); confirm calls `add-folder`; cancel discards.
    - Per-folder controls: Reindex (button -> `reindex` IPC), Flip sensitivity (toggle -> `set-sensitivity` IPC), Remove (button -> confirm dialog -> `remove-folder` IPC).
    - Remove confirm dialog wording: `"Remove '{label}'? This deletes the folder's index and citations from /ask. The files on disk are not touched."` with [Cancel, Remove].
    - On focus/visibility, refreshes the list.
  </behavior>
  <action>
    Author `KnowledgeFoldersSection.tsx` mirroring the editorial card style of `RagIndexSection.tsx` and the destructive-confirm pattern of `BackupRestoreSection.tsx`. Use the existing animation patterns ([[feedback_aria_animation_patterns]]).

    Update `SettingsScreen.tsx`: import `KnowledgeFoldersSection`, add it to the `SECTIONS` ReadonlyArray (group: same as RagIndex — "Knowledge & Memory" or whatever the existing label is), add a `<Route path="knowledge-folders" element={<KnowledgeFoldersSection />} />` entry in the Routes block.

    Update `src/renderer/app/routes.tsx` only if Settings sub-routes are declared there; otherwise leave untouched.

    Tests (`KnowledgeFoldersSection.test.tsx`) using vitest + Testing Library against a stub `window.aria.knowledge`:
    - Renders existing folders from `listFolders` stub.
    - Above-threshold add path: prescan returns `{exceedsThreshold:true, fileCount:6000, totalBytes:3GB}` -> confirm dialog renders with the file count + size in the text; Cancel does NOT call `add-folder`; Continue opens the modal then `add-folder` is called.
    - Below-threshold add path: prescan returns `{exceedsThreshold:false}` -> modal opens directly without the threshold dialog.
    - Destructive-remove (3-split assertion per [[feedback_destructive_actions_require_consent]]):
        a) Clicking Remove renders the confirm dialog (assert text contains label).
        b) Clicking Cancel does NOT call `remove-folder` and the folder remains in the rendered list.
        c) Clicking confirm-Remove calls `remove-folder` with the correct folderId and the folder disappears from the list after a refresh.
    - Sensitivity toggle calls `set-sensitivity` with the new value; UI reflects new badge.
  </action>
  <verify>
    <automated>npm test -- src/renderer/features/settings/KnowledgeFoldersSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/renderer/features/settings/KnowledgeFoldersSection.tsx` exists.
    - `grep -c "KnowledgeFoldersSection" src/renderer/features/settings/SettingsScreen.tsx` >= 2 (import + usage).
    - `grep -c "knowledge-folders" src/renderer/features/settings/SettingsScreen.tsx` >= 1 (route path).
    - `grep -E "window\\.aria\\.knowledge" src/renderer/features/settings/KnowledgeFoldersSection.tsx` returns at least 6 matches (one per IPC: listFolders, pickFolder, prescanFolder, addFolder, removeFolder, setSensitivity).
    - `grep -E "exceedsThreshold|fileCount.*5000|totalBytes.*2" src/renderer/features/settings/KnowledgeFoldersSection.tsx` returns at least one match.
    - The 3-split destructive-remove assertion is in the test file (grep for three separate `it(` or `test(` blocks naming dialog / cancel / confirm).
    - Reachability: `KnowledgeFoldersSection` appears in the SECTIONS array or Routes element list in `SettingsScreen.tsx`.
  </acceptance_criteria>
  <done>Section is reachable from Settings, all IPC channels wired, threshold confirm + destructive-remove dialog both verified by tests, sensitivity toggle round-trips.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration + in-flight flip case</name>
  <files>
    tests/integration/knowledge-folders-e2e.spec.ts,
    tests/integration/knowledge-folders-inflight-flip.spec.ts
  </files>
  <read_first>
    - src/main/folder-ingestion/ingestion-service.ts
    - src/main/folder-ingestion/folder-flip.ts
    - src/main/rag/answer-router.ts
    - src/main/rag/answer-service.ts
    - .planning/phases/10-knowledge-folders/10-CONTEXT.md (schema_reconciliation 5-case contract)
  </read_first>
  <action>
    Author `knowledge-folders-e2e.spec.ts`:
    1. Boot an in-memory DB with all migrations through 132.
    2. Create a temp dir with three files (one .md, one .txt, one .csv) containing distinct keywords.
    3. Add the folder via FolderRegistry + run FolderIngestionService.ingestFolderOnce.
    4. Query rag_chunk: expect rows for each file with `source_kind='folder'`, sensitivity='folder:low'.
    5. Run a stubbed answer-service query (inject a fake LLM that echoes a fixed answer) targeting one keyword; assert citations include the folder source AND `RouteDecision.route === 'FRONTIER'`.
    6. flipFolderSensitivity(db, folderId, 'sensitive'); assert all 3+ chunk rows now have sensitivity='folder:high'.
    7. Re-run the same answer-service query; assert `RouteDecision.route === 'LOCAL'` and the lock-badge reason string matches `rag-answer:sensitivity-folder:high`.

    Author `knowledge-folders-inflight-flip.spec.ts` (case 5 of the sensitivity contract):
    1. Same DB setup; folder is currently 'general' (sensitivity='folder:low').
    2. Stage an answer-service call where chunk retrieval returns a chunks[] snapshot, but LLM dispatch is artificially delayed (use a deferred promise as the LLM stub).
    3. While the call is pending, invoke `flipFolderSensitivity` to 'sensitive'. Assert the DB now has 'folder:high'.
    4. Resolve the deferred LLM stub. Assert the original call's RouteDecision was made from the pre-flip snapshot (route='FRONTIER' for that completion).
    5. Issue a second answer-service call (fresh retrieval). Assert it sees 'folder:high' and routes LOCAL.
  </action>
  <verify>
    <automated>npm test -- tests/integration/knowledge-folders-e2e.spec.ts tests/integration/knowledge-folders-inflight-flip.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both spec files exist and pass.
    - e2e test asserts a FRONTIER -> LOCAL route transition across a flip.
    - in-flight test asserts the pending call's snapshot is independent of the DB flip.
    - `grep -E "rag-answer:sensitivity-folder:high" tests/integration/knowledge-folders-e2e.spec.ts` returns a match.
    - `grep -E "flipFolderSensitivity" tests/integration/knowledge-folders-inflight-flip.spec.ts` returns a match.
  </acceptance_criteria>
  <done>Two integration suites green; the five-case sensitivity contract is now fully covered (cases 1-4 in router unit tests from 10-02; case 5 here).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT walkthrough</name>
  <what-built>
    - Settings -> Knowledge Folders section with editorial card style
    - Add-folder flow with pre-scan + threshold confirm
    - Per-folder sensitivity toggle that flips chunk routing live
    - Destructive Remove with confirm dialog
    - Background watcher (chokidar) reflecting filesystem changes
    - Daily tombstone sweep
    - Q&A from /ask citing folder sources
  </what-built>
  <how-to-verify>
    Follow the steps in `.planning/phases/10-knowledge-folders/10-UAT.md`. Author the UAT doc as part of this task with the following sections:

    1. **Add a small folder (below threshold)**
       - Open Settings -> Knowledge Folders -> Add folder.
       - Pick a directory with < 100 files.
       - Verify modal opens without a threshold prompt.
       - Set label "Test Small", sensitivity General. Submit.
       - Expect the card to appear with fileCount, byte size, lastScanAt.

    2. **Add a large folder (above threshold)**
       - Add a directory containing > 5000 files OR > 2GB (e.g. a node_modules-heavy dir if excludes are off — or assemble fixtures).
       - Expect the threshold confirm dialog with the count + size.
       - Cancel: card does NOT appear.
       - Repeat and Continue: card appears, indexing progresses.

    3. **/ask returns folder citations**
       - Put a uniquely-keyworded .md file in the Test Small folder.
       - Wait for ingestion (watcher fires within ~2s after stabilityThreshold).
       - Open /ask, query for the unique keyword.
       - Expect a citation pointing back to the folder file.

    4. **Sensitivity flip routes LOCAL**
       - Flip Test Small to Sensitive.
       - Re-query /ask for the same keyword.
       - Expect the lock-badge LOCAL routing indicator.
       - Flip back to General; re-query; FRONTIER routing returns (no badge).

    5. **Destructive remove**
       - Click Remove on Test Small.
       - Cancel the dialog -> folder remains.
       - Click Remove again -> confirm -> folder disappears, /ask no longer returns its citations.

    6. **Watcher live edits**
       - Edit the file (add a new keyword), save.
       - Within ~5s (1.5s stabilityThreshold + ingest), /ask returns the new keyword.

    7. **Tombstone**
       - Delete the file on disk; verify the card's fileCount decrements after the next watcher event.
       - /ask no longer returns its citations.

    8. **Verifier checks (run before sign-off)**
       - `git diff src/main/rag/model-swap-reconciler.ts` is empty.
       - `grep -E "source_kind\s+IN" src/main/insights/gate.ts` does NOT contain `'folder'`.
       - `grep -c "KnowledgeFoldersSection" src/renderer/features/settings/SettingsScreen.tsx` >= 2.
  </how-to-verify>
  <resume-signal>Type "approved" or list issues.</resume-signal>
</task>

</tasks>

<verification>
- KnowledgeFoldersSection reachable from SettingsScreen and Routes
- Threshold confirm dialog appears for >5k files OR >2GB
- 3-split destructive-remove dialog asserted in tests
- End-to-end test: add folder -> ingest -> ask -> flip -> LOCAL
- In-flight flip test passes (case 5 of sensitivity contract)
- UAT signed off by user
- Phase 7 reconciler and Phase 8 gate files diff = empty
</verification>

<success_criteria>
A user can add a local folder via Settings, see its files become /ask citations within seconds, flip the folder to sensitive and watch routing switch to LOCAL, and remove the folder behind a confirm dialog — all with the existing answer-router pure-function semantics intact.
</success_criteria>

<output>
After completion, create `.planning/phases/10-knowledge-folders/10-03-SUMMARY.md`
</output>
