---
phase: 10-knowledge-folders
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/main/rag/answer-router.ts
  - src/main/rag/answer-router.test.ts
  - src/main/folder-ingestion/folder-flip.ts
  - src/main/folder-ingestion/folder-flip.test.ts
  - src/main/folder-ingestion/folder-watcher.ts
  - src/main/folder-ingestion/folder-watcher.test.ts
  - src/main/folder-ingestion/sweep-cron.ts
  - src/main/folder-ingestion/sweep-cron.test.ts
  - src/main/folder-ingestion/boot-reconciler.ts
  - src/main/folder-ingestion/boot-reconciler.test.ts
  - src/main/folder-ingestion/lifecycle.ts
  - src/main/ipc/knowledge-folders.ts
  - src/main/index.ts
  - src/preload/index.ts
  - package.json
autonomous: true
requirements:
  - SPEC-§5
  - SPEC-§7
  - SPEC-§9
  - SPEC-§11
  - CONTEXT-reconciliation
must_haves:
  truths:
    - "answer-router's FORCE_LOCAL_PREFIXES contains 'folder:high' so any folder chunk tagged sensitive forces LOCAL routing"
    - "'folder:low' is NOT in FORCE_LOCAL_PREFIXES; pure-general folder retrieval permits FRONTIER"
    - "RouterChunk.sourceKind in answer-router.ts compiles when fed folder chunks (either via re-export from chunk-types.ts or via local union widening — pick one explicitly)"
    - "aria:knowledge:set-sensitivity IPC is registered in this plan and wired DIRECTLY to flipFolderSensitivity (no intermediate-state hole — the IPC and the chunk-bulk-flip ship together)"
    - "Setting a folder's sensitivity flips ALL its rag_chunk rows' sensitivity column inside a single transaction without re-embedding"
    - "chokidar watches each registered folder with the documented awaitWriteFinish settings and feeds events through a p-queue concurrency-2 worker into the existing FolderIngestionService"
    - "Resurrecting a tombstoned file with identical content_hash within the 24h window does NOT produce duplicate rag_chunk rows"
    - "node-cron 03:00 sweep deletes tombstoned files older than 24h, which cascades their rag_chunk rows via FK"
    - "On boot the reconciler scans every active folder for missed events while powerMonitor suspends the watcher on sleep and resumes on wake"
    - "src/main/rag/model-swap-reconciler.ts diff is empty"
    - "src/main/insights/gate.ts diff is empty"
  artifacts:
    - path: "src/main/rag/answer-router.ts"
      provides: "Widened FORCE_LOCAL_PREFIXES including 'folder:high'; RouterChunk.sourceKind reconciled with chunk-types.ts SourceKind"
      contains: "folder:high"
    - path: "src/main/folder-ingestion/folder-flip.ts"
      provides: "flipFolderSensitivity(db, folderId, nextSensitivity) — single UPDATE in transaction"
      exports: ["flipFolderSensitivity"]
    - path: "src/main/ipc/knowledge-folders.ts"
      provides: "Eighth IPC channel aria:knowledge:set-sensitivity wired to flipFolderSensitivity"
      contains: "aria:knowledge:set-sensitivity"
    - path: "src/main/folder-ingestion/folder-watcher.ts"
      provides: "Per-folder chokidar watcher feeding the ingestion service via p-queue"
      exports: ["createFolderWatcher"]
    - path: "src/main/folder-ingestion/sweep-cron.ts"
      provides: "node-cron 03:00 daily sweep deleting tombstoned files past the 24h window"
      exports: ["startTombstoneSweep"]
    - path: "src/main/folder-ingestion/boot-reconciler.ts"
      provides: "On-boot reconciliation pass over all active folders"
      exports: ["runBootReconciliation"]
    - path: "src/main/folder-ingestion/lifecycle.ts"
      provides: "powerMonitor wire-up + lifecycle orchestration for watchers/cron/reconciler"
      exports: ["startKnowledgeFolderLifecycle", "stopKnowledgeFolderLifecycle"]
  key_links:
    - from: "src/main/folder-ingestion/folder-watcher.ts"
      to: "src/main/folder-ingestion/ingestion-service.ts"
      via: "ingestionService.ingestFile(folderId, absolutePath)"
      pattern: "ingestFile|ingestFolderOnce"
    - from: "src/main/folder-ingestion/folder-flip.ts"
      to: "src/main/rag/answer-router.ts"
      via: "rag_chunk.sensitivity column read by routeAnswer"
      pattern: "rag_chunk.*sensitivity|FORCE_LOCAL_PREFIXES"
    - from: "src/main/ipc/knowledge-folders.ts"
      to: "src/main/folder-ingestion/folder-flip.ts"
      via: "aria:knowledge:set-sensitivity handler delegates to flipFolderSensitivity"
      pattern: "flipFolderSensitivity"
    - from: "src/main/index.ts"
      to: "src/main/folder-ingestion/lifecycle.ts"
      via: "startKnowledgeFolderLifecycle(...) on app ready, stop on quit"
      pattern: "startKnowledgeFolderLifecycle"
---

<objective>
Wire folder chunks into the existing sensitivity routing path AND make the folders live: watch for filesystem changes, sweep tombstones, reconcile on boot, suspend/resume on sleep/wake. No new gate file. No JOIN against `knowledge_folders` at retrieval time. The one-line widening of `FORCE_LOCAL_PREFIXES` plus the index-time `sensitivity` tag (from plan 10-01) is the entire gate.

This plan also OWNS the registration of `aria:knowledge:set-sensitivity` (intentionally deferred from plan 10-01) so the IPC and the chunk-row bulk-flip arrive in the same wave — eliminating the intermediate-state hole where the IPC would exist but produce stale routing.

Purpose: After this plan, Aria treats folder content as live RAG context with the same sensitivity guarantees as email/calendar.

Output: A widened router, a folder-flip primitive, the eighth IPC channel wired through it, a chokidar watcher, a node-cron tombstone sweep, a boot reconciler, and a powerMonitor-aware lifecycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-knowledge-folders/10-CONTEXT.md
@docs/superpowers/specs/2026-05-21-knowledge-folders-design.md
@src/main/rag/answer-router.ts
@src/main/rag/sensitivity-cache.ts
@src/main/rag/index-writer.ts
@src/main/rag/chunk-types.ts
@src/main/rag/model-swap-reconciler.ts
@src/main/insights/gate.ts
@src/main/ipc/knowledge-folders.ts
@src/main/folder-ingestion/folder-registry.ts
@src/main/folder-ingestion/ingestion-service.ts
@src/main/index.ts
@.planning/phases/10-knowledge-folders/10-01-SUMMARY.md

<interfaces>
From src/main/rag/answer-router.ts (current):
- const FORCE_LOCAL_PREFIXES = ['hr:med','hr:high','legal:med','legal:high','financial:med','financial:high'];
- interface RouterChunk { ...; sourceKind: 'email'|'event'|'note'|'action'; ... }  // currently a redeclared union, NOT a re-export
- routeAnswer(question, chunks): RouteDecision — pure function; on first match of FORCE_LOCAL_PREFIXES.includes(c.sensitivity), returns LOCAL.

Extensions in this plan (TWO changes to answer-router.ts):
1. Append 'folder:high' to FORCE_LOCAL_PREFIXES. 'folder:low' is intentionally NOT added (general folders can route FRONTIER unless another corpus chunk forces local).
2. Reconcile RouterChunk.sourceKind with the widened SourceKind union from plan 10-01's chunk-types.ts.
   PREFERRED approach (pick ONE explicitly — do not do both):
     (a) Re-export: change the `sourceKind` field type to `import('./chunk-types').SourceKind` so it tracks the canonical union and never needs maintenance again.
     OR
     (b) Local widen: extend the literal union to `'email'|'event'|'note'|'action'|'folder'`.
   The acceptance criteria below cover both — pick one and the corresponding grep MUST match.

From plan 10-01 deliverables:
- registry.setSensitivity(folderId, nextSensitivity) updates the knowledge_folders row only (NOT exposed via IPC in 10-01).
- index-writer.ts now persists folder_id + file_id (from plan 10-01 Task 3).
- flipFolderSensitivity (this plan) updates rag_chunk.sensitivity in the SAME transaction.
- ingestionService.ingestFile(folderId, absolutePath), ingestFolderOnce(folderId).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen FORCE_LOCAL_PREFIXES, reconcile RouterChunk, folder-flip primitive, register set-sensitivity IPC</name>
  <files>
    src/main/rag/answer-router.ts,
    src/main/rag/answer-router.test.ts,
    src/main/folder-ingestion/folder-flip.ts,
    src/main/folder-ingestion/folder-flip.test.ts,
    src/main/ipc/knowledge-folders.ts,
    src/preload/index.ts
  </files>
  <read_first>
    - src/main/rag/answer-router.ts
    - src/main/rag/chunk-types.ts
    - src/main/rag/sensitivity-cache.ts
    - src/main/folder-ingestion/folder-registry.ts
    - src/main/ipc/knowledge-folders.ts
    - .planning/phases/10-knowledge-folders/10-CONTEXT.md (schema_reconciliation)
  </read_first>
  <behavior>
    - routeAnswer with a single chunk where sensitivity='folder:high' returns LOCAL with reason `rag-answer:sensitivity-folder:high`.
    - routeAnswer with a single chunk where sensitivity='folder:low' returns FRONTIER (no force-local).
    - routeAnswer with a hybrid set [{sensitivity:'folder:high'}, {sensitivity:null}] returns LOCAL.
    - RouterChunk accepts `sourceKind:'folder'` at the type level (TypeScript compiles when folder chunks flow into routeAnswer).
    - flipFolderSensitivity(db, folderId, 'sensitive') executes one transaction containing:
        UPDATE knowledge_folders SET sensitivity='sensitive', updated_at=? WHERE id=?
        UPDATE rag_chunk SET sensitivity='folder:high', sensitivity_model='folder-rule:v1', sensitivity_at=? WHERE folder_id=?
    - flipFolderSensitivity(db, folderId, 'general') is symmetric writing 'folder:low'.
    - In-flight router calls are unaffected by a concurrent flip — routeAnswer is a pure function over a chunks[] snapshot already in memory.
    - `aria:knowledge:set-sensitivity` IPC is REGISTERED in this plan (first time — not in 10-01). Handler signature: input `{folderId, sensitivity: 'general'|'sensitive'}`, output `{ok: true, folderUpdated: number, chunksUpdated: number}`. Delegates directly to `flipFolderSensitivity` — does NOT call `registry.setSensitivity`.
  </behavior>
  <action>
    Edit `src/main/rag/answer-router.ts`:

    1. Append `'folder:high'` to the `FORCE_LOCAL_PREFIXES` constant. Do NOT add `'folder:low'`. This is a one-line array-literal edit.

    2. Reconcile `RouterChunk.sourceKind` with `SourceKind` from `chunk-types.ts`. Pick ONE option and apply it:
       - **Option (a) — preferred:** Add `import type { SourceKind } from './chunk-types';` at the top, then change the `sourceKind` field on `RouterChunk` to `sourceKind: SourceKind;`. This keeps the union single-sourced and prevents future drift.
       - **Option (b) — fallback:** Extend the literal union on the `RouterChunk` interface to `'email'|'event'|'note'|'action'|'folder'`.
       Pick one. The acceptance criteria check BOTH possibilities and require at least one to match — but pick one explicitly in code, don't half-do both.

    No other functional logic changes in this file. The reviewer should be able to confirm: the diff edits only (1) the FORCE_LOCAL_PREFIXES literal and (2) RouterChunk's sourceKind field type. No new exports beyond what (a) implies (a type import).

    Author `src/main/folder-ingestion/folder-flip.ts` exporting `flipFolderSensitivity(db, folderId, nextSensitivity: 'general'|'sensitive'): {folderUpdated: number; chunksUpdated: number}`. Use `db.transaction(() => { ... })()` (better-sqlite3 idiomatic). Compute `tag = nextSensitivity==='sensitive' ? 'folder:high' : 'folder:low'`. Run both UPDATEs inside the transaction. Return the affected row counts.

    Edit `src/main/ipc/knowledge-folders.ts` to register `aria:knowledge:set-sensitivity`. The handler calls `flipFolderSensitivity(db, folderId, sensitivity)` and returns `{ok: true, folderUpdated, chunksUpdated}`. With this addition the IPC module now exposes EIGHT channels total (the seven from 10-01 plus this one).

    Update `src/preload/index.ts` to expose `setSensitivity` on `window.aria.knowledge.*` (mirror the existing invoke wrappers from plan 10-01).

    Tests:
    - `answer-router.test.ts`: extend existing test suite with three new cases (folder:high alone -> LOCAL, folder:low alone -> FRONTIER, hybrid forced LOCAL). Add a TypeScript-only compile-time check: instantiate a RouterChunk literal with `sourceKind: 'folder'` — the test compiles iff the reconciliation in step 2 above was applied.
    - `folder-flip.test.ts`: seed in-memory db with migration 132, a folder, 3 files, 6 rag_chunk rows (sensitivity='folder:low', folder_id set per plan 10-01 Task 3). Flip to sensitive. Assert all 6 rows now have sensitivity='folder:high' AND sensitivity_model='folder-rule:v1' AND sensitivity_at is set. Flip back; all rows revert. Verify atomicity by injecting an UPDATE failure mid-transaction (use a SAVEPOINT trick or wrap a stmt that throws) and assert the folder row's sensitivity also rolled back.
    - In-flight no-interruption test: snapshot a chunks[] array, fire flipFolderSensitivity, then call routeAnswer on the snapshot. Assert routeAnswer returns the route consistent with the snapshot's sensitivity (not the new DB value) — proves the snapshot is independent.
    - IPC round-trip test: invoke `aria:knowledge:set-sensitivity` with `{folderId, sensitivity: 'sensitive'}`; assert the handler called `flipFolderSensitivity` (not `registry.setSensitivity`) and that the rag_chunk rows for that folder all read 'folder:high' afterward.
  </action>
  <verify>
    <automated>npm test -- src/main/rag/answer-router.test.ts src/main/folder-ingestion/folder-flip.test.ts src/main/ipc/knowledge-folders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'folder:high'" src/main/rag/answer-router.ts` >= 1 within the FORCE_LOCAL_PREFIXES array literal.
    - `grep -c "'folder:low'" src/main/rag/answer-router.ts` == 0 (low is permitted frontier; must NOT be in the list).
    - RouterChunk reconciliation — at least ONE of the following greps must match:
        (a) `grep -E "import type \\{ ?SourceKind ?\\}" src/main/rag/answer-router.ts` returns a match AND `grep -E "sourceKind:\\s*SourceKind" src/main/rag/answer-router.ts` returns a match (re-export approach), OR
        (b) `grep -E "sourceKind:\\s*'email'\\s*\\|\\s*'event'\\s*\\|\\s*'note'\\s*\\|\\s*'action'\\s*\\|\\s*'folder'" src/main/rag/answer-router.ts` returns a match (local widen).
    - The diff of `src/main/rag/answer-router.ts` modifies only: (i) the FORCE_LOCAL_PREFIXES array literal line, (ii) the RouterChunk sourceKind field (and the import line if option (a) chosen). No other functional changes — verified by reviewer; if any other logic line changes, plan fails.
    - File `src/main/folder-ingestion/folder-flip.ts` exists; `grep -c "db.transaction" src/main/folder-ingestion/folder-flip.ts` >= 1.
    - `grep -E "UPDATE rag_chunk SET sensitivity" src/main/folder-ingestion/folder-flip.ts` returns a match.
    - `grep -E "folder-rule:v1" src/main/folder-ingestion/folder-flip.ts` returns a match.
    - `grep -E "WHERE folder_id = \\?" src/main/folder-ingestion/folder-flip.ts` returns a match.
    - `grep -c "aria:knowledge:set-sensitivity" src/main/ipc/knowledge-folders.ts` >= 1 (NEW in this plan — was 0 after 10-01).
    - `grep -c "aria:knowledge:" src/main/ipc/knowledge-folders.ts` >= 8 (the seven from 10-01 plus set-sensitivity).
    - `grep -c "flipFolderSensitivity" src/main/ipc/knowledge-folders.ts` >= 1 (the IPC handler delegates to it).
    - `grep -c "registry.setSensitivity" src/main/ipc/knowledge-folders.ts` == 0 (the IPC must NOT go through the registry-only path — that's the intermediate-state hole we're closing).
    - `git diff src/main/rag/model-swap-reconciler.ts` is empty.
    - `grep -E "source_kind\s+IN" src/main/insights/gate.ts` does NOT contain `'folder'`.
    - All five sensitivity test cases from `<context>.schema_reconciliation` pass: pure-sensitive (LOCAL), pure-general (FRONTIER), hybrid (LOCAL), multi-turn no-stickiness (each call independent — covered by router's pure-function semantics test), in-flight flip (snapshot test).
  </acceptance_criteria>
  <done>Folder sensitivity flows through the existing routeAnswer with no new gate code; flip is transactional; RouterChunk reconciled with chunk-types SourceKind; set-sensitivity IPC ships in the same wave as the chunk-bulk-flip (no intermediate-state hole); reconciler and Phase 8 gate untouched; all five cases of the sensitivity test contract green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chokidar watcher + p-queue worker</name>
  <files>
    src/main/folder-ingestion/folder-watcher.ts,
    src/main/folder-ingestion/folder-watcher.test.ts,
    package.json
  </files>
  <read_first>
    - src/main/folder-ingestion/folder-registry.ts
    - src/main/folder-ingestion/ingestion-service.ts
    - src/main/rag/index-writer.ts
    - docs/superpowers/specs/2026-05-21-knowledge-folders-design.md (sections §9, §11)
    - .planning/phases/10-knowledge-folders/10-CONTEXT.md
  </read_first>
  <behavior>
    - createFolderWatcher({registry, ingestionService, logger, queueConcurrency=2}) returns {addFolder(folderId, path), removeFolder(folderId), close()}.
    - chokidar instantiated with `{ awaitWriteFinish: { stabilityThreshold: 1500, pollInterval: 200 }, ignored: <exclude globs>, ignoreInitial: true }`.
    - 'add' / 'change' events enqueue `ingestionService.ingestFile(folderId, absolutePath)` on a p-queue with concurrency 2.
    - 'unlink' events call `registry.tombstoneFile(folderId, relativePath)` (sets status='tombstoned', tombstoned_at=now). The chunks are NOT yet deleted — that happens on cron sweep.
    - 'add' of a previously tombstoned file within 24h calls `registry.resurrectFile(folderId, relativePath)` and re-enqueues ingestion. The ingestion path computes content_hash on parse and short-circuits when content_hash matches the existing knowledge_files row — in that case the existing rag_chunk rows are preserved (replace-only-on-change) and NO duplicate rows are produced.
    - Errors in the queue worker mark the file `status='error'` with `last_error` and continue (per-file isolation; folder stays healthy).
  </behavior>
  <action>
    Add `chokidar` and `p-queue` to package.json dependencies (chokidar 4.x, p-queue 8.x per CLAUDE.md stack).

    Author `folder-watcher.ts`. Exclude globs match spec §6 plus what prescan uses. Map chokidar 'add'/'change'/'unlink' to handler functions that compute the file's relative_path and dispatch.

    Tests use a temp directory + a stub `ingestionService` and `registry`. Drive chokidar via real fs events (write a file, modify it, delete it). With awaitWriteFinish, expect ingestion calls to coalesce — assert exactly one ingestion call per stable file. Tombstone test: delete a file, assert `tombstoneFile` invoked with the right (folderId, relative_path); recreate it within the same test, assert `resurrectFile` invoked.

    **NEW — duplicate-chunk-prevention test (W1):** Set up an in-memory DB with migrations through 132 and a real ingestionService (not stub). Seed a folder + a file with known bytes; ingest it; record the rag_chunk row count for `WHERE file_id=?`. Tombstone the file (simulate unlink). Resurrect the file (simulate add) with byte-for-byte identical content (same content_hash) within the 24h window — drive this through the watcher's add handler. After the queue drains, assert `SELECT COUNT(*) FROM rag_chunk WHERE file_id=?` is UNCHANGED from the pre-tombstone count. This proves the resurrect path does not duplicate chunks (the ingestion path's content_hash short-circuit + the index-writer's delete-then-insert-by-(source_kind,source_id) pattern combine to ensure idempotency).

    Error isolation test: stub ingestion to throw on file `bad.txt`, assert `markFileError` called and queue continues processing other files.
  </action>
  <verify>
    <automated>npm test -- src/main/folder-ingestion/folder-watcher.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "awaitWriteFinish" src/main/folder-ingestion/folder-watcher.ts` returns a match.
    - `grep -E "stabilityThreshold:\\s*1500" src/main/folder-ingestion/folder-watcher.ts` returns a match.
    - `grep -E "pollInterval:\\s*200" src/main/folder-ingestion/folder-watcher.ts` returns a match.
    - `grep -E "PQueue|p-queue" src/main/folder-ingestion/folder-watcher.ts` returns a match.
    - `grep -E "concurrency:\\s*2" src/main/folder-ingestion/folder-watcher.ts` returns a match.
    - Test asserts: add/change events trigger ingestion exactly once after stabilization; unlink calls tombstoneFile; re-add within window calls resurrectFile; error in one file does not block subsequent files.
    - **Duplicate-chunk-prevention test passes:** rag_chunk count for the file_id is identical pre-tombstone and post-resurrect when content_hash is unchanged.
    - `package.json` lists `chokidar` and `p-queue` as dependencies (not devDependencies).
  </acceptance_criteria>
  <done>Per-folder watcher feeds the ingestion service through a bounded queue with documented chokidar settings; per-file failure isolation verified; same-content resurrect within the 24h window does NOT duplicate rag_chunk rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tombstone sweep cron + boot reconciler + powerMonitor lifecycle</name>
  <files>
    src/main/folder-ingestion/sweep-cron.ts,
    src/main/folder-ingestion/sweep-cron.test.ts,
    src/main/folder-ingestion/boot-reconciler.ts,
    src/main/folder-ingestion/boot-reconciler.test.ts,
    src/main/folder-ingestion/lifecycle.ts,
    src/main/index.ts,
    package.json
  </files>
  <read_first>
    - src/main/folder-ingestion/folder-registry.ts
    - src/main/folder-ingestion/folder-watcher.ts
    - src/main/folder-ingestion/ingestion-service.ts
    - src/main/index.ts
    - docs/superpowers/specs/2026-05-21-knowledge-folders-design.md (§9, §11)
  </read_first>
  <behavior>
    - startTombstoneSweep({db, logger, cron, windowHours=24}) schedules a node-cron job at `0 3 * * *` (03:00 local). The job runs `DELETE FROM knowledge_files WHERE status='tombstoned' AND tombstoned_at < ?` with `?` = now - 24h ISO. FK ON DELETE CASCADE removes the associated rag_chunk rows.
    - runBootReconciliation({registry, ingestionService, logger}): for each folder with status='active', list files, diff against the live filesystem (compare relative_path + size + mtime), and: enqueue ingestion for new/changed files; tombstone now-missing files (status='tombstoned', tombstoned_at=now).
    - startKnowledgeFolderLifecycle({app, powerMonitor, ...deps}): on app ready, calls runBootReconciliation, then for each active folder calls watcher.addFolder, then startTombstoneSweep. Subscribes to powerMonitor 'suspend' (watcher.close + cron.stop) and 'resume' (re-run boot reconciler then re-attach watchers + restart cron).
    - stopKnowledgeFolderLifecycle: idempotent shutdown for app quit.
  </behavior>
  <action>
    Ensure `node-cron` is in package.json (per CLAUDE.md stack — already present from Phase 1; only add if missing).

    Author `sweep-cron.ts`: thin wrapper around node-cron scheduling the SQL above. Export the scheduled task handle so tests can run the job immediately via `task.now?.()` or by invoking the underlying function directly.

    Author `boot-reconciler.ts`: iterate `registry.listFolders({status:'active'})`, for each `folder` list the on-disk filesystem (reuse prescan walker but capture per-file size+mtime) and the registry's `listFilesForFolder`. Compute the symmetric diff. Enqueue via `ingestionService.ingestFile` for new/changed (size or mtime delta); call `registry.tombstoneFile` for missing.

    Author `lifecycle.ts` orchestrating the three pieces and binding to `powerMonitor`.

    Wire `startKnowledgeFolderLifecycle` into `src/main/index.ts` at app-ready, and `stopKnowledgeFolderLifecycle` on `before-quit`.

    Tests:
    - sweep-cron.test.ts: seed knowledge_files with status='tombstoned' + tombstoned_at older than 24h (and one within window). Also seed corresponding rag_chunk rows with `file_id` pointing at the tombstoned files. Run the sweep function directly. Assert old tombstoned files are deleted, recent ones survive; assert the rag_chunk rows cascaded.
    - boot-reconciler.test.ts: simulate a folder with 3 registry rows; on disk there are 4 files (one new, one with changed mtime, two unchanged) and one registry row is missing on disk. Assert `ingestionService.ingestFile` called for the new file and the changed file; `registry.tombstoneFile` called for the missing one.
    - Lifecycle test: stub powerMonitor; verify 'suspend' calls watcher.close + cron.stop; 'resume' triggers reconciler then watcher.addFolder for each active folder.
  </action>
  <verify>
    <automated>npm test -- src/main/folder-ingestion/sweep-cron.test.ts src/main/folder-ingestion/boot-reconciler.test.ts src/main/folder-ingestion/lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "0 3 \\* \\* \\*" src/main/folder-ingestion/sweep-cron.ts` returns a match.
    - `grep -E "DELETE FROM knowledge_files WHERE status='tombstoned'" src/main/folder-ingestion/sweep-cron.ts` returns a match.
    - `grep -E "tombstoned_at < " src/main/folder-ingestion/sweep-cron.ts` returns a match.
    - `grep -E "listFolders" src/main/folder-ingestion/boot-reconciler.ts` returns a match.
    - `grep -E "tombstoneFile|ingestFile" src/main/folder-ingestion/boot-reconciler.ts` returns at least two matches.
    - `grep -E "powerMonitor" src/main/folder-ingestion/lifecycle.ts` returns a match.
    - `grep -E "'suspend'|'resume'" src/main/folder-ingestion/lifecycle.ts` returns at least two matches.
    - `grep -c "startKnowledgeFolderLifecycle" src/main/index.ts` >= 1.
    - `grep -c "stopKnowledgeFolderLifecycle" src/main/index.ts` >= 1.
    - All three test files pass; cascade deletion of rag_chunk on knowledge_files DELETE is verified by the sweep test.
    - `git diff src/main/rag/model-swap-reconciler.ts` is empty.
    - `grep -E "source_kind\s+IN" src/main/insights/gate.ts` does NOT contain `'folder'`.
  </acceptance_criteria>
  <done>Daily tombstone sweep deletes expired tombstones and cascades chunks; boot reconciliation catches missed events; powerMonitor suspends/resumes the watcher and cron cleanly; lifecycle wired into the Electron app entry.</done>
</task>

</tasks>

<verification>
- FORCE_LOCAL_PREFIXES widened by exactly one entry ('folder:high')
- RouterChunk.sourceKind reconciled with chunk-types.ts SourceKind (one approach chosen)
- aria:knowledge:set-sensitivity IPC registered in same wave as chunk-bulk-flip (no intermediate-state hole)
- Folder-flip transactional UPDATE of rag_chunk; no re-embed
- 5-case sensitivity contract green
- chokidar watcher with documented awaitWriteFinish + p-queue concurrency 2
- Same-content resurrect within 24h does not duplicate rag_chunk rows
- Tombstone sweep cron at 03:00 with 24h window; cascade via FK
- Boot reconciler catches drift; powerMonitor suspend/resume wired
- src/main/rag/model-swap-reconciler.ts diff empty
- src/main/insights/gate.ts diff empty
</verification>

<success_criteria>
After this plan, /ask retrieval over folder chunks routes LOCAL for sensitive folders and FRONTIER for general folders, the folder reflects filesystem changes within seconds (subject to awaitWriteFinish), tombstoned files vanish from disk-attributed chunk storage 24h later, and resurrected-with-identical-content files leave the chunk count unchanged — all without modifying retrieval code or Phase 7's reconciler or Phase 8's 14d gate. The eight IPC channels (seven from 10-01 plus set-sensitivity here) are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/10-knowledge-folders/10-02-SUMMARY.md`
</output>
