---
phase: 20-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/main/whatsapp/passive-posture.ratchet.spec.ts
  - tests/unit/main/whatsapp/no-frontier.ratchet.spec.ts
  - tests/unit/main/whatsapp/auth-state.spec.ts
  - tests/unit/main/whatsapp/ingest-privacy.spec.ts
  - tests/unit/main/whatsapp/session-reconnect.spec.ts
  - tests/unit/main/whatsapp/session-recycle.spec.ts
  - tests/unit/main/whatsapp/whatsapp-retention.spec.ts
  - tests/unit/main/db/migration-138.spec.ts
  - tests/unit/renderer/whatsapp-consent.spec.ts
autonomous: true
requirements: [WA-02, WA-06, WA-07, WA-11]

must_haves:
  truths:
    - "The two static ratchet specs (passive-posture, no-frontier) are GREEN immediately (missing-dir existsSync guard) and turn load-bearing once src/main/whatsapp/ has source"
    - "Every Wave 0 spec file from VALIDATION.md exists so downstream implementation tasks have a real automated verify target (Nyquist)"
  artifacts:
    - path: "tests/unit/main/whatsapp/passive-posture.ratchet.spec.ts"
      provides: "WA-11 send-ban static grep (gates 1/2)"
      contains: "sendMessage"
    - path: "tests/unit/main/whatsapp/no-frontier.ratchet.spec.ts"
      provides: "frontier-import ban static grep (gate 3)"
      contains: "getFrontierModel"
    - path: "tests/unit/main/whatsapp/ingest-privacy.spec.ts"
      provides: "CRITICAL WA-06 privacy-filter assertions (gates 7/8/9)"
      contains: "@g.us"
  key_links:
    - from: "passive-posture.ratchet.spec.ts"
      to: "src/main/whatsapp/"
      via: "walk() + existsSync guard"
      pattern: "existsSync"
    - from: "no-frontier.ratchet.spec.ts"
      to: "src/main/whatsapp/"
      via: "walk() grep for frontier imports"
      pattern: "@ai-sdk"
# Decision coverage:
# D-03 (untracked by default privacy boundary) — ingest-privacy spec asserts untracked group writes 0 rows
# D-13 (syncFullHistory:false explicit) — passive-posture ratchet asserts the config flag once source lands
---

<objective>
Seed every Wave 0 test-infrastructure file the Nyquist validation strategy (VALIDATION.md) requires, BEFORE the implementation plans run. Two of these are static-grep ratchets that must be GREEN immediately — they guard the (not-yet-created) src/main/whatsapp/ directory with an existsSync guard so the build is protected from the moment the directory appears. The rest are spec scaffolds (describe + failing/pending assertions) so that each downstream implementation task has a concrete `npx vitest run <spec>` target the moment it writes code.

Purpose: Nyquist requires every implementation task to have an automated verify. Creating the spec files first means no implementation task ships without a feedback signal, and the privacy-critical ingest-privacy.spec.ts exists before the first message-ingest code is written.
Output: 2 immediately-green static ratchets + 7 spec scaffolds bound to downstream plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation/20-VALIDATION.md
@.planning/phases/20-foundation/20-RESEARCH.md
@.planning/phases/20-foundation/20-PATTERNS.md
@tests/static/voice-streaming-no-write.spec.ts
@tests/static/no-bare-cron-schedule.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two static ratchet specs (passive-posture + no-frontier), green-before-dir</name>
  <read_first>
    - tests/static/voice-streaming-no-write.spec.ts (the walk() + stripComments + identifier-boundary-RE structural template, ~lines 26-45; copy its missing-dir existsSync guard)
    - tests/static/no-bare-cron-schedule.spec.ts (ratchet style precedent)
    - .planning/phases/20-foundation/20-PATTERNS.md § tests/unit/main/whatsapp/*.ratchet.spec.ts
    - .planning/phases/20-foundation/20-RESEARCH.md § Hard Gates 1/2/3 + § Validation Architecture
    - MEMORY reference_gsd_decision_coverage_gate (grep counts comments — filter header prose; do not gate `== 0` on unfiltered files)
  </read_first>
  <action>
    Create tests/unit/main/whatsapp/passive-posture.ratchet.spec.ts (gates 1/2, WA-11): walk('src/main/whatsapp') with the voice-streaming-no-write existsSync guard so it returns green when the dir does not yet exist. After strip-comments, assert ZERO matches for `\.sendMessage\(`, `\.sendReceipt\(`, `\.readMessages\(`, and `sendPresenceUpdate\((?!\s*['"]unavailable)` (the only permitted presence arg is 'unavailable'). When source exists, also assert the makeWASocket config literal contains `markOnlineOnConnect: false`, `emitOwnEvents: false`, and `syncFullHistory: false` (D-13) — guard these assertions behind an existence check on session-manager.ts so they are inert until Plan 20-04 lands. Create tests/unit/main/whatsapp/no-frontier.ratchet.spec.ts (gate 3): same walk + guard; assert ZERO matches for `getFrontierModel`, `getFrontierKey`, and `@ai-sdk/(anthropic|openai|google)` imports (the digest is Phase 21 but the directory + ratchet land NOW per the locked anti-pattern). Use strip-comments before counting so doc/comment prose is not self-invalidating.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/whatsapp/passive-posture.ratchet.spec.ts` passes (green now via existsSync guard; the send-ban grep finds 0 matches).
    - `npx vitest run tests/unit/main/whatsapp/no-frontier.ratchet.spec.ts` passes (green now; frontier grep 0 matches).
    - Both specs use stripComments before counting and reference src/main/whatsapp via walk().
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/passive-posture.ratchet.spec.ts</automated>
  </verify>
  <done>WA-11 send-ban + gate-3 frontier-ban ratchets exist and are green before any whatsapp source.</done>
</task>

<task type="auto">
  <name>Task 2: ingest-privacy + auth-state + reconnect + recycle + retention spec scaffolds</name>
  <read_first>
    - .planning/phases/20-foundation/20-VALIDATION.md § Per-Task Verification Map (R-WA06, R-G04, R-G05, R-G06, R-WA07) + § Wave 0 Requirements
    - .planning/phases/20-foundation/20-RESEARCH.md § Validation Architecture (concrete assertions for WA-06, gates 4/5/6/7, WA-07)
    - .planning/phases/20-foundation/20-PATTERNS.md § ingest.ts + auth-state.ts + session-manager.ts + retention.ts (so the specs assert the real intended behavior/signatures)
    - MEMORY reference_vitest_parallel_projects_quirk (run ONE spec at a time)
    - tests/setup.ts / createTempUserDataDir helper for SQLCipher temp DB fixtures
  </read_first>
  <action>
    Create the five remaining whatsapp spec scaffolds with describe blocks and concrete assertions, each importing from the (not-yet-existing) module path so it RED-fails until its implementation plan lands (Nyquist target). (1) tests/unit/main/whatsapp/ingest-privacy.spec.ts — the CRITICAL WA-06/gates 7/8/9 integration: feed a fake messages.upsert with (a) a 1:1 `@s.whatsapp.net` msg, (b) an untracked `@g.us` msg, (c) a tracked `@g.us` msg; assert ONLY (c) is written to whatsapp_message, assert (a)/(b) produce ZERO rows AND that no logger call received their body text, and assert a `type:'append'` batch writes nothing. (2) auth-state.spec.ts — gate 4: keys.set() with a throw injected mid-loop persists ZERO rows (transaction rollback). (3) session-reconnect.spec.ts — gate 5: 401/403/440/500 → no reconnect scheduled (→needs-auth); 408/515 → backoff scheduled; 5th consecutive failure → status='degraded'. (4) session-recycle.spec.ts — gate 6: recycle cron registered in scheduler.cronRegistry (no bare cron.schedule); firing the task calls disconnect() then start(). (5) whatsapp-retention.spec.ts — WA-07: extractText returns null for image/audio/video/document/sticker (no row); runNow() deletes rows with sent_at < now-30d and keeps newer. Mark assertions that depend on unwritten modules with `it(...)` (not `it.skip`) so they fail RED until their plan turns them green — these are the per-task verify targets, not permanently-skipped tests.
  </action>
  <acceptance_criteria>
    - All five spec files exist and `npx vitest run tests/unit/main/whatsapp/ingest-privacy.spec.ts` RED-fails with a clear "cannot find module ../../../../src/main/whatsapp/ingest" style error (target exists, implementation pending) — confirming the verify target is wired.
    - The ingest-privacy spec encodes the 3-line filter order (notify / @g.us / is_tracked) and the no-log-of-body assertion verbatim from VALIDATION.md R-WA06.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/ingest-privacy.spec.ts 2>&1 | Select-String -Pattern "ingest-privacy|Cannot find|FAIL|PASS"</automated>
  </verify>
  <done>5 spec scaffolds wired to their downstream module paths; ingest-privacy encodes the CRITICAL privacy assertions.</done>
</task>

<task type="auto">
  <name>Task 3: migration-138 + whatsapp-consent spec scaffolds</name>
  <read_first>
    - .planning/phases/20-foundation/20-PATTERNS.md § No Analog Found (migration-138 + electron-vite-config guidance) + § WhatsAppConsentModal
    - .planning/phases/20-foundation/20-RESEARCH.md § Hard Gate 12 + WA-02 test row
    - src/main/db/migrations/runner.ts (migration runner API for the integration test)
    - src/renderer/features/onboarding/MnemonicShow.tsx (the editorial-Checkbox ack-gate test shape: disabled-until-checked)
  </read_first>
  <action>
    Create tests/unit/main/db/migration-138.spec.ts (gate 12): apply migrations through 138 against a temp SQLCipher DB via the migration runner; assert the 138 SQL file text contains `legacy_alter_table=ON`; assert `PRAGMA user_version` === 138; assert an INSERT into `provider_sync_state` succeeds after 138 (proving no dangling `provider_account_old` FK — the migration-135 regression mode); assert the provider_account CHECK now admits a `provider_key='whatsapp'` row and rejects an unknown key. Create tests/unit/renderer/whatsapp-consent.spec.ts (WA-02 / D-07): render WhatsAppConsentModal; assert the "Show QR code" button is `disabled` until the consent Checkbox (testid `whatsapp-consent-ack`) is checked, and that `onShowQr` is not invoked while unchecked. Both RED-fail until Plans 20-03 / 20-07 land. NOTE the handler-count test (tests/unit/main/ipc/index.spec.ts) is NOT touched here — it already reads Object.keys(CHANNELS).length dynamically and is updated implicitly when channels are stubbed in Plan 20-06.
  </action>
  <acceptance_criteria>
    - tests/unit/main/db/migration-138.spec.ts exists and encodes the legacy_alter_table assertion, user_version==138, and the provider_sync_state-insert regression check.
    - tests/unit/renderer/whatsapp-consent.spec.ts exists and asserts disabled-until-checked + onShowQr-not-callable-while-unchecked (D-07 hard gate).
    - Running either RED-fails on the missing source module/component (verify target wired).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/db/migration-138.spec.ts 2>&1 | Select-String -Pattern "migration-138|Cannot find|FAIL|PASS"</automated>
  </verify>
  <done>migration-138 (gate 12) + consent (WA-02/D-07) spec scaffolds wired to their downstream targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI build → merged code | Static ratchets are the gate that blocks a send/frontier regression from merging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Repudiation / DoS-to-user | passive posture regression (a future edit adds a send/presence call → ban) | mitigate | passive-posture ratchet greps src/main/whatsapp for send/receipt/non-'unavailable'-presence; fails the build on any match (WA-11). |
| T-20-05 | Info disclosure | third-party group PII reaching a frontier API | mitigate | no-frontier ratchet greps for getFrontierModel/@ai-sdk imports; directory + ratchet land in Phase 20 before the Phase-21 digest exists. |
| T-20-06 | Info disclosure | untracked-group / DM body logged or written | mitigate | ingest-privacy spec asserts ZERO rows AND no log of body for untracked/DM messages before the filter (gate 9). |
</threat_model>

<verification>
- `npx vitest run tests/unit/main/whatsapp/passive-posture.ratchet.spec.ts` + `...no-frontier.ratchet.spec.ts` GREEN immediately.
- The 7 scaffold specs exist; running any RED-fails on its missing source target (confirming Nyquist verify wiring).
- ingest-privacy.spec.ts encodes the 3-line filter and the no-log-of-body assertion.
</verification>

<success_criteria>
- 2 static ratchets green-before-dir; 7 spec scaffolds bound to downstream module paths.
- The CRITICAL ingest-privacy.spec.ts exists before any ingest code is written.
- Every downstream implementation task has a concrete `npx vitest run` target (Nyquist sampling continuity).
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation/20-02-SUMMARY.md`
</output>
