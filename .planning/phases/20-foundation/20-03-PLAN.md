---
phase: 20-foundation
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/main/db/migrations/138_whatsapp.sql
  - src/main/db/embedded.ts
  - src/main/whatsapp/auth-state.ts
autonomous: true
requirements: [WA-04, WA-07, WA-11]

must_haves:
  truths:
    - "Migration 138 rebuilds provider_account (from migration 125's 12-col schema) adding 'whatsapp' to the CHECK, wrapped in PRAGMA legacy_alter_table=ON so provider_sync_state FKs do not dangle"
    - "Migration 138 creates whatsapp_auth_state (4-col), whatsapp_group, whatsapp_message, whatsapp_group_digest with ON DELETE CASCADE from group → message + digest"
    - "auth-state.ts persists Signal creds+keys inside SQLCipher; every keys.set() runs in one db.transaction() that rolls back fully on a mid-loop throw"
  artifacts:
    - path: "src/main/db/migrations/138_whatsapp.sql"
      provides: "4 WA tables + provider_account CHECK rebuild + legacy_alter_table guard"
      contains: "legacy_alter_table=ON"
    - path: "src/main/whatsapp/auth-state.ts"
      provides: "useSQLiteAuthState(db): creds + transactional signal-key store"
      contains: "db.transaction"
  key_links:
    - from: "src/main/whatsapp/auth-state.ts keys.set()"
      to: "whatsapp_auth_state"
      via: "db.transaction() prepared upsert/del"
      pattern: "db\\.transaction"
    - from: "migration 138 whatsapp_message"
      to: "whatsapp_group"
      via: "FOREIGN KEY ... ON DELETE CASCADE"
      pattern: "ON DELETE CASCADE"
# Decision coverage:
# Gate 12 (legacy_alter_table=ON) — migration 138 wraps the provider_account RENAME
# Gate 4 (db.transaction on every keys.set) — auth-state.ts set() rolls back on throw
# A2 (auth-state 4-col shape locked) — key_type,key_id,value,updated_at composite PK
---

<objective>
Build the encrypted-storage layer: migration 138 (the four WhatsApp tables + the provider_account CHECK-constraint rebuild that admits 'whatsapp') and the SQLCipher-backed Baileys auth-state adapter whose every key write is transactional.

Purpose: The session-manager (Plan 20-04) cannot create a socket without an auth store, and no WhatsApp data can be stored without the tables. This plan is the DB foundation everything else writes to. The legacy_alter_table guard is load-bearing: without it the provider_sync_state FK silently repoints to provider_account_old (the migration-135 failure mode), breaking every mail/calendar sync.
Output: Migration 138 applied + embedded canonical DDL updated; auth-state.ts with gate-4 atomicity; migration-138.spec + auth-state.spec turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation/20-RESEARCH.md
@.planning/phases/20-foundation/20-PATTERNS.md
@src/main/db/migrations/125_todoist_tasks.sql
@src/main/db/migrations/135_repair_approval_child_fks.sql

<interfaces>
<!-- provider_account 12-col schema source = migration 125 (NOT 011 which is stale 2-col). -->
<!-- provider_sync_state has an FK into provider_account; the RENAME must be guarded. -->
<!-- whatsapp_auth_state shape = STACK.md 4-col (Assumption A2 LOCKED):
       (key_type TEXT, key_id TEXT, value TEXT, updated_at INTEGER) PRIMARY KEY (key_type,key_id) -->
<!-- Baileys auth contract (6.7.23): state.creds + state.keys.{get,set};
     SignalDataSet keyed by type then id; serialize values with BufferJSON.replacer
     (imported from @whiskeysockets/baileys), wrap the store in makeCacheableSignalKeyStore. -->
<!-- DB type: import type Database from 'better-sqlite3-multiple-ciphers'; type Db = Database.Database -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 138 — 4 tables + provider_account CHECK rebuild (legacy_alter_table guard)</name>
  <read_first>
    - src/main/db/migrations/125_todoist_tasks.sql (lines 9-34 — verbatim provider_account 12-col DDL + the explicit-column INSERT idiom to copy)
    - src/main/db/migrations/135_repair_approval_child_fks.sql (the PRAGMA legacy_alter_table=ON wrapper at lines ~22-24, 78-80)
    - .planning/phases/20-foundation/20-PATTERNS.md § 138_whatsapp.sql (the exact rebuild + the 4 new-table DDL)
    - .planning/phases/20-foundation/20-RESEARCH.md § Data Model (the 4-table DDL) + correction #3 + Assumption A2 (use the 4-col auth-state shape)
    - src/main/db/embedded.ts (how canonical DDL / EMBEDDED_MIGRATIONS is appended in ascending order)
    - MEMORY reference_sqlite_rename_fk_rewrite + reference_esbuild_skips_typecheck
  </read_first>
  <action>
    Create src/main/db/migrations/138_whatsapp.sql. Wrap the whole thing: `PRAGMA foreign_keys=OFF; PRAGMA legacy_alter_table=ON; BEGIN; ... COMMIT; PRAGMA legacy_alter_table=OFF; PRAGMA foreign_keys=ON; PRAGMA user_version = 138;`. Inside: rename provider_account → provider_account_old, recreate provider_account copying migration 125's 12 columns verbatim BUT add `'whatsapp'` to the provider_key CHECK (`IN ('google','microsoft','todoist','whatsapp')`); use the EXPLICIT column-list INSERT (not `INSERT ... SELECT *` — 125 lines 25-34 style, fragile against column-order drift); DROP provider_account_old; recreate `idx_provider_account_status`. Then CREATE the 4 WhatsApp tables per RESEARCH Data Model: whatsapp_auth_state with the LOCKED 4-col shape `(key_type TEXT NOT NULL, key_id TEXT NOT NULL, value TEXT NOT NULL, updated_at INTEGER NOT NULL DEFAULT (unixepoch()), PRIMARY KEY (key_type, key_id))` (Assumption A2); whatsapp_group (jid PK, display_name, description, tracked INTEGER DEFAULT 0 CHECK IN (0,1), member_count, updated_at) + idx on tracked; whatsapp_message (id AUTOINCREMENT, jid, sender_jid, wa_id, sent_at, body_text, ingested_at, UNIQUE(jid,wa_id), FK jid→whatsapp_group ON DELETE CASCADE) + the two indexes; whatsapp_group_digest (Phase-21 table, lands now, FK jid→whatsapp_group ON DELETE CASCADE, UNIQUE(jid,date)) + date index. Append migration 138 at the tail of EMBEDDED_MIGRATIONS in embedded.ts (ascending order) so new installs apply it. Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/db/migration-138.spec.ts` passes: file contains `legacy_alter_table=ON`; `PRAGMA user_version`==138 after apply; INSERT into provider_sync_state succeeds post-138 (no dangling FK); provider_account admits a `provider_key='whatsapp'` row and rejects an unknown key.
    - Migration uses explicit-column INSERT (grep: no `INSERT INTO provider_account SELECT *`).
    - whatsapp_message + whatsapp_group_digest both declare `ON DELETE CASCADE` from whatsapp_group.
    - embedded.ts EMBEDDED_MIGRATIONS includes 138 after 137 (ascending).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/db/migration-138.spec.ts</automated>
  </verify>
  <done>Migration 138 green (gate 12); 4 tables + CASCADE + guarded provider_account rebuild; canonical DDL updated.</done>
</task>

<task type="auto">
  <name>Task 2: auth-state.ts — SQLCipher Baileys auth store with transactional keys.set() (gate 4)</name>
  <read_first>
    - src/main/entitlement/service.ts (deps-injected class, prepared statements at construction, db.transaction discipline — the role-match analog)
    - src/main/ipc/provider-accounts.ts (lines 72-79 — the db.transaction(() => {...}) wrapper idiom)
    - .planning/phases/20-foundation/20-PATTERNS.md § auth-state.ts (the transaction-wrapped set() with BufferJSON.replacer)
    - .planning/phases/20-foundation/20-RESEARCH.md § Code Examples (auth-state set()) + § Don't Hand-Roll (BufferJSON, makeCacheableSignalKeyStore)
    - The whatsapp_auth_state DDL from Task 1 (4-col composite PK)
  </read_first>
  <action>
    Create src/main/whatsapp/auth-state.ts exporting `useSQLiteAuthState(db, logger)` returning `{ state: { creds, keys }, saveCreds }` matching the Baileys 6.7.23 AuthenticationState contract. Persist creds under a reserved (key_type='creds', key_id='me') row and signal keys under their SignalDataTypeMap (key_type, key_id). Serialize ALL values with `JSON.stringify(value, BufferJSON.replacer)` and revive with `BufferJSON.reviver` (both imported from @whiskeysockets/baileys — do NOT hand-roll Buffer handling or creds corrupt). Prepare the upsert / del / get statements ONCE at construction (entitlement style). keys.set(data) — HARD GATE 4 — must run the whole loop inside ONE `db.transaction(() => {...})()`: iterate type→id; null value → del.run(type,id); else upsert.run(type,id,serialized); a throw mid-loop must roll back EVERY row. Provide a `clear()` (or rely on the disconnect cascade DELETE) and expose the raw store so the caller wraps it with `makeCacheableSignalKeyStore(store, logger)` before passing to makeWASocket. Do NOT use useMultiFileAuthState (flat JSON, demo-only — anti-pattern). Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/whatsapp/auth-state.spec.ts` passes: a throw injected mid-loop in keys.set() leaves ZERO rows persisted (transaction rollback proven).
    - grep auth-state.ts: `db.transaction(` wraps the set() loop; `BufferJSON.replacer` used for serialization; NO `useMultiFileAuthState` import.
    - `npm run typecheck` clean over baseline.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/auth-state.spec.ts</automated>
  </verify>
  <done>auth-state.ts persists creds+keys in SQLCipher with gate-4 transactional atomicity; auth-state.spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Baileys socket → auth store | Signal creds/keys (impersonation material) cross into persistence here |
| migration → existing FK graph | provider_sync_state FK must survive the provider_account rebuild |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering / Elevation | auth-state corruption on unclean shutdown → Bad MAC / impersonation surface | mitigate | Every keys.set() in one db.transaction() (gate 4); SQLCipher AES-256 envelope (existing); BufferJSON serialization prevents creds corruption. |
| T-20-08 | Info disclosure | Signal creds written to flat JSON outside the encrypted DB | mitigate | useSQLiteAuthState only; useMultiFileAuthState forbidden (anti-pattern); all auth rows live in the SQLCipher DB. |
| T-20-09 | DoS (to all sync) | provider_sync_state FK repointed to provider_account_old by the RENAME | mitigate | PRAGMA legacy_alter_table=ON wrapper (gate 12); migration-138 integration test inserts into provider_sync_state post-138 to prove no dangling FK. |
</threat_model>

<verification>
- `npx vitest run tests/unit/main/db/migration-138.spec.ts` green (gate 12).
- `npx vitest run tests/unit/main/whatsapp/auth-state.spec.ts` green (gate 4).
- `npm run typecheck` clean.
- passive-posture ratchet still green (auth-state introduces no send/presence call).
</verification>

<success_criteria>
- Migration 138 creates 4 tables with CASCADE + guarded provider_account rebuild admitting 'whatsapp'.
- auth-state.ts stores Signal material transactionally inside SQLCipher; rollback proven on mid-loop throw.
- Canonical EMBEDDED_MIGRATIONS updated so new installs get 138.
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation/20-03-SUMMARY.md`
</output>
