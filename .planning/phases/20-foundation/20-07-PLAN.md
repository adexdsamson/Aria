---
phase: 20-foundation
plan: 07
type: execute
wave: 6
depends_on: [01, 06]
files_modified:
  - src/renderer/components/WhatsAppConsentModal.tsx
  - src/renderer/components/WhatsAppQrModal.tsx
  - src/renderer/components/WhatsAppGroupPickerModal.tsx
  - src/renderer/components/AccountRow.tsx
  - src/renderer/features/settings/IntegrationsSection.tsx
  - src/preload/index.ts
autonomous: true
requirements: [WA-01, WA-02, WA-03, WA-05, WA-12]

must_haves:
  truths:
    - "The pre-QR consent modal shows 3-4 ban-risk bullets + an emphasized secondary-number callout, and the 'Show QR code' action is disabled until the editorial Checkbox is acknowledged (QR generation is gated, not just visibility)"
    - "After ack, the QR modal renders the WHATSAPP_QR_UPDATE data-URL with a countdown; it is the linking affordance until the provider_account row exists"
    - "The WhatsApp AccountRow maps session status to existing chip styles (needs-auth amber / degraded red), wires the Reconnect button to WHATSAPP_LINK, and shows a 'Manage groups' link (with a count badge) opening the group-picker"
    - "The group-picker modal lists groups with a search field and per-group track toggle firing WHATSAPP_SET_TRACKED; no toast on connection drop (D-09)"
  artifacts:
    - path: "src/renderer/components/WhatsAppConsentModal.tsx"
      provides: "editorial-Checkbox ack-gated consent (D-05/06/07)"
      contains: "disabled"
    - path: "src/renderer/components/WhatsAppGroupPickerModal.tsx"
      provides: "search + per-group track toggle (D-01/02/03/04)"
      contains: "WHATSAPP_SET_TRACKED"
  key_links:
    - from: "WhatsAppConsentModal Show-QR button"
      to: "onShowQr"
      via: "disabled={!acknowledged}"
      pattern: "disabled"
    - from: "AccountRow Reconnect"
      to: "WHATSAPP_LINK"
      via: "onReconnect onClick"
      pattern: "whatsapp"
# Decision coverage:
# D-01 Manage-groups link; D-02 search; D-03 untracked-default toggle; D-04 count badge;
# D-05/06/07 consent ack-gate; D-08 chip mapping; D-09 no toasts; D-11 JID number display; D-12 QR-only
---

<objective>
Build the renderer surface for WhatsApp: the ban-risk consent modal (ack-gates QR generation), the QR/linking modal, the AccountRow extension (chip mapping + Reconnect + Manage-groups link), the group-picker modal (search + per-group track toggle), the IntegrationsSection orchestration (connect → consent → QR; render row; disconnect), and the preload bridge for the new channels.

Purpose: This is everything the user touches. The consent gate is SC-1's hard requirement (no QR until acknowledgement). The group-picker is the privacy-authorization UI (the track toggle is the only thing that authorizes ingestion). Reuses the Phase-9 editorial system and the existing AccountRow/AddAccountModal/DisconnectConfirmDialog patterns — no new design system.
Output: 5 renderer files + preload wiring; whatsapp-consent.spec turns GREEN; AccountRow + group toggle behavior testable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation/20-CONTEXT.md
@.planning/phases/20-foundation/20-PATTERNS.md
@src/renderer/components/AccountRow.tsx
@src/renderer/components/DisconnectConfirmDialog.tsx
@src/renderer/features/onboarding/MnemonicShow.tsx
@src/renderer/components/editorial/Checkbox.tsx

<interfaces>
<!-- Plan 20-01 DTOs: WhatsAppGroupDto, WhatsAppStatusDto, WhatsAppQrUpdateDto, WhatsAppSetTrackedReq, WhatsAppStateChangedDto -->
<!-- Plan 20-06 channels: WHATSAPP_LINK, WHATSAPP_DISCONNECT, WHATSAPP_LIST_GROUPS,
     WHATSAPP_SET_TRACKED, WHATSAPP_STATUS (invoke); WHATSAPP_QR_UPDATE, WHATSAPP_STATE_CHANGED (push) -->
<!-- AccountRow (HEAD): needsAuth → '#c98a3a', degraded → '#b34', else green; chipStyle(needsAuth,degraded);
     providerDisplayName() at ~123-127; the Reconnect button (needs-auth) currently has NO onClick. -->
<!-- DisconnectConfirmDialog: modal shell role=dialog aria-modal, Escape-closes, borderTop 2px var(--rose) callout. -->
<!-- MnemonicShow: useState(false) ack + disabled={!acknowledged} on the primary action (the ack-gate precedent). -->
<!-- D-09: NO ToastHost usage for connection drops. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WhatsAppConsentModal + WhatsAppQrModal (the link flow, SC-1/SC-2)</name>
  <read_first>
    - src/renderer/features/onboarding/MnemonicShow.tsx (the editorial-Checkbox ack-gate: useState(false) + disabled={!acknowledged}; the borderTop callout ~line 109)
    - src/renderer/components/DisconnectConfirmDialog.tsx (modal shell 45-87: role="dialog", aria-modal, fixed overlay, Escape-closes, borderTop 2px var(--rose))
    - src/renderer/components/editorial/Checkbox.tsx (the consent ack primitive)
    - .planning/phases/20-foundation/20-PATTERNS.md § WhatsAppConsentModal + § WhatsAppQrModal
    - .planning/phases/20-foundation/20-CONTEXT.md D-05/D-06/D-07/D-11/D-12 + the "no history before this moment" notice (specifics)
    - tests/unit/renderer/whatsapp-consent.spec.ts (assertions to satisfy)
  </read_first>
  <action>
    Create src/renderer/components/WhatsAppConsentModal.tsx: a DisconnectConfirmDialog-shell modal with 3-4 ban-risk bullets (unofficial protocol; account-ban possibility; recommend a secondary number — Claude's-discretion exact copy), the secondary-number recommendation as an EMPHASIZED callout (a box with `borderTop: '2px solid var(--rose)'`, D-06 — not a buried bullet), an editorial Checkbox (data-testid `whatsapp-consent-ack`) using `useState(false)`, and a primary "Show QR code" button (data-testid `whatsapp-show-qr`) with `disabled={!acknowledged}` and `onClick={onShowQr}` — this disabled prop IS the SC-1 hard gate (QR generation gated, not just visibility, D-07). Create src/renderer/components/WhatsAppQrModal.tsx: same modal shell; subscribe to the WHATSAPP_QR_UPDATE push, render the data-URL `<img>`, show a countdown to expiry + a refresh affordance (Pitfall 12 QR expiry); on WHATSAPP_STATE_CHANGED → status 'ok', show the one-sentence "No WhatsApp history before this moment is imported" notice and the linked number (D-11 JID), then allow close. QR string→data-URL conversion happens in MAIN (Plan 20-04); this modal only displays. Per Open-Question #4 the QR modal IS the linking affordance — there is no AccountRow chip until the row exists. QR-only (D-12 — no pairing-code UI).
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/renderer/whatsapp-consent.spec.ts` passes: "Show QR code" disabled until the ack Checkbox is checked; onShowQr not invoked while unchecked.
    - WhatsAppConsentModal has the emphasized secondary-number callout (borderTop var(--rose)) and 3-4 risk bullets.
    - WhatsAppQrModal renders the WHATSAPP_QR_UPDATE data-URL + countdown and the "no history" notice on link success; no pairing-code UI.
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/renderer/whatsapp-consent.spec.ts</automated>
  </verify>
  <done>Consent ack-gate (SC-1/WA-02) + QR linking modal (WA-01) landed; consent spec green.</done>
</task>

<task type="auto">
  <name>Task 2: AccountRow extension — WhatsApp chip mapping + Reconnect + Manage-groups link (WA-03)</name>
  <read_first>
    - src/renderer/components/AccountRow.tsx (full file — needsAuth/degraded dotColor 14-20, chipStyle, providerDisplayName ~123-127, the action-button row, the Reconnect button which currently has no onClick)
    - .planning/phases/20-foundation/20-PATTERNS.md § AccountRow.tsx (MOD) — the 3 concrete edits
    - .planning/phases/20-foundation/20-CONTEXT.md D-08 (chip mapping) + D-01 (Manage-groups link) + D-04 (count badge) + D-09 (no toasts)
  </read_first>
  <action>
    MODIFY src/renderer/components/AccountRow.tsx: (1) providerDisplayName — add `if (providerKey === 'whatsapp') return 'WhatsApp';`. (2) Chip/dot — confirm the existing needs-auth `#c98a3a` and degraded `#b34` styles flow through for WhatsApp status values (D-08 maps WA session states onto these EXISTING styles — no new chip code). (3) Wire the Reconnect button's onClick (currently absent) to dispatch WHATSAPP_LINK when the account is whatsapp + needs-auth — add an optional `onReconnect?(account)` prop and call it. (4) Add a "Manage groups" link button (use the existing action-row link style) shown ONLY for `providerKey === 'whatsapp'`, calling an optional `onManageGroups?(account)` prop, with a count badge rendering newly-joined-untracked count (D-04) when > 0. Do NOT add any ToastHost call for connection drops (D-09). Add/extend the AccountRow spec asserting: WhatsApp row shows chip `#c98a3a` for needs-auth and `#b34` for degraded; Reconnect present + onClick dispatches when needs-auth; Manage-groups link present only for whatsapp. Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/renderer/AccountRow.spec.tsx` (or the AccountRow spec path) passes: needs-auth→`#c98a3a`, degraded→`#b34`; Reconnect dispatches WHATSAPP_LINK; Manage-groups link only for whatsapp; count badge when >0.
    - No ToastHost import added for connection drops (D-09).
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/renderer/AccountRow.spec.tsx</automated>
  </verify>
  <done>AccountRow maps WA status to chips + wires Reconnect + Manage-groups link/badge (WA-03/D-01/D-04/D-08/D-09).</done>
</task>

<task type="auto">
  <name>Task 3: WhatsAppGroupPickerModal + IntegrationsSection orchestration + preload bridge (WA-05)</name>
  <read_first>
    - src/renderer/features/settings/IntegrationsSection.tsx (how it orchestrates AddAccountModal connect, AccountRow render, DisconnectConfirmDialog → providerAccountDisconnect)
    - src/renderer/components/DisconnectConfirmDialog.tsx (modal shell) + editorial/Checkbox.tsx (per-group toggles) + MnemonicShow list-row styling
    - src/preload/index.ts (the existing invoke + push-listener bridge wiring pattern)
    - .planning/phases/20-foundation/20-PATTERNS.md § WhatsAppGroupPickerModal + § IntegrationsSection + § preload
    - .planning/phases/20-foundation/20-CONTEXT.md D-01/D-02/D-03/D-04
  </read_first>
  <action>
    Create src/renderer/components/WhatsAppGroupPickerModal.tsx: a modal-shell list over WHATSAPP_LIST_GROUPS rows with a search/filter field (D-02) and a per-group editorial Checkbox/toggle that fires WHATSAPP_SET_TRACKED immediately on change (D-03 — the toggle is the privacy authorization); newly-joined untracked groups sort to the top (D-04). MODIFY src/renderer/features/settings/IntegrationsSection.tsx: add the WhatsApp branch — the AddAccountModal WhatsApp entry opens WhatsAppConsentModal → on ack, calls WHATSAPP_LINK and opens WhatsAppQrModal (NOT an OAuth BrowserWindow — WhatsApp links via QR); render the WhatsApp AccountRow with onReconnect (WHATSAPP_LINK) + onManageGroups (open WhatsAppGroupPickerModal); route disconnect through the existing DisconnectConfirmDialog → providerAccountDisconnect IPC. Subscribe to WHATSAPP_STATE_CHANGED to refresh the row status (no toast, D-09). MODIFY src/preload/index.ts: expose the 5 invoke channels (whatsappLink/Disconnect/ListGroups/SetTracked/Status) and the 2 push subscriptions (onWhatsAppQrUpdate, onWhatsAppStateChanged) on the contextBridge api object, mirroring the existing invoke/push wiring. Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/renderer/whatsapp-groups.spec.tsx` (or the group-picker spec) passes: search filters rows; toggling a group fires WHATSAPP_SET_TRACKED; picker opens from the AccountRow Manage-groups link; newly-joined untracked sort to top.
    - IntegrationsSection connects WhatsApp via consent→QR (no OAuth window) and routes disconnect through DisconnectConfirmDialog.
    - preload exposes the 5 invoke + 2 push channels; no send/reply channel exposed.
    - `npm run typecheck` clean; all 3 static ratchets still green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/renderer/whatsapp-groups.spec.tsx</automated>
  </verify>
  <done>Group-picker (search + track toggle) + IntegrationsSection connect/disconnect + preload bridge (WA-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → consent gate | The only "outbound" action (QR link) must be explicitly acknowledged |
| renderer → preload bridge | Only the read-only WhatsApp channels may be exposed (no send) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-24 | Repudiation / informed-consent | user links without seeing ban risk | mitigate | Consent modal gates QR generation behind the ack Checkbox (D-07/SC-1); emphasized secondary-number callout (D-06). |
| T-20-25 | Elevation | preload exposes an outbound/send channel | mitigate | preload exposes only the 5 read-only invoke + 2 push channels; no WHATSAPP_SEND exists (WA-11). |
| T-20-26 | Info disclosure | a group tracked without explicit user action | mitigate | Track toggle defaults off (D-03); only the explicit toggle fires WHATSAPP_SET_TRACKED. |
| T-20-27 | DoS-to-UX | toast noise on a flaky unofficial-protocol connection | accept | D-09 — no toasts on connection drop; status surfaces only via the AccountRow chip (passive/degradable posture). |
</threat_model>

<verification>
- `npx vitest run tests/unit/renderer/whatsapp-consent.spec.ts` green (SC-1/WA-02).
- AccountRow + group-picker specs green (WA-03/WA-05).
- `npm run typecheck` clean; passive-posture + no-frontier + no-bare-cron ratchets still green.
- No ToastHost on connection drop (D-09); no send/pairing channel in preload.
</verification>

<success_criteria>
- Consent ack hard-gates QR generation; QR modal links and shows the "no history" notice.
- AccountRow chip + Reconnect + Manage-groups link; group-picker search + track toggle (the privacy authorization).
- preload exposes only read-only channels; disconnect routes through the existing confirm dialog.
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation/20-07-SUMMARY.md`
</output>
