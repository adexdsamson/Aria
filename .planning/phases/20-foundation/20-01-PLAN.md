---
phase: 20-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - electron.vite.config.ts
  - src/shared/provider.ts
  - src/shared/ipc-contract.ts
  - tests/unit/main/electron-vite-config.spec.ts
  - tests/unit/main/whatsapp/supply-chain-pin.spec.ts
autonomous: true
requirements: [WA-01, WA-03, WA-04, WA-05, WA-11]

must_haves:
  truths:
    - "@whiskeysockets/baileys@6.7.23 exact (no caret/tilde) + qrcode@1.5.4 resolve in a committed pnpm-lock.yaml"
    - "electron.vite.config.ts main section bundles Baileys ESM via a NEW main.plugins externalize exclude"
    - "Shared ProviderKey union includes 'whatsapp' and CHANNELS exposes all 7 WHATSAPP_* channels with Zod DTOs"
  artifacts:
    - path: "electron.vite.config.ts"
      provides: "main.plugins externalizeDepsPlugin exclude ['@whiskeysockets/baileys']"
      contains: "@whiskeysockets/baileys"
    - path: "src/shared/provider.ts"
      provides: "ProviderKey += 'whatsapp'"
      contains: "whatsapp"
    - path: "src/shared/ipc-contract.ts"
      provides: "7 WHATSAPP_* channel constants + DTOs"
      contains: "WHATSAPP_LINK"
  key_links:
    - from: "package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm install --lockfile-only"
      pattern: "@whiskeysockets/baileys.*6\\.7\\.23"
    - from: "electron.vite.config.ts main.plugins"
      to: "@whiskeysockets/baileys bundle"
      via: "externalizeDepsPlugin exclude"
      pattern: "externalizeDepsPlugin"
# Decision coverage:
# D-11 (account_id = creds.me.id JID) — DTO carries displayEmail = JID-derived number
# D-12 (QR only, no pairing-code) — only WHATSAPP_LINK/QR channels, no pairing-code channel
---

<objective>
Lay the supply-chain, build-config, and shared-contract foundation for WhatsApp group-tracking: pin and lock the two production packages, add the Baileys ESM bundling exclude to the electron-vite main section (a NEW main.plugins key — it does not exist yet), extend the shared ProviderKey union, and declare all 7 WHATSAPP_* IPC channels + their Zod DTOs.

Purpose: Every downstream plan (DB, session-manager, IPC, renderer) imports the channel constants, DTO types, and ProviderKey union from here, and the Baileys import will only resolve at build time once the main externalize exclude lands. This is the contract-first layer.
Output: Locked dependencies, a buildable main config that bundles Baileys, shared types, and two green gate specs (10 + 11 + D-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation/20-RESEARCH.md
@.planning/phases/20-foundation/20-PATTERNS.md

<interfaces>
<!-- electron.vite.config.ts main section CURRENTLY (HEAD, verified) has NO plugins key: -->
<!-- main: { define: oauthDefine, build: {...}, resolve: {...} } -->
<!-- The preload section is the precedent for the exclude pattern (line 67): -->
<!--   preload: { plugins: [externalizeDepsPlugin({ exclude: ['zod'] })], ... } -->
<!-- externalizeDepsPlugin is already imported at line 2. -->

<!-- src/shared/provider.ts line 1 (verified): -->
<!--   export type ProviderKey = 'google' | 'microsoft' | 'todoist'; -->

<!-- src/shared/ipc-contract.ts: CHANNELS is a const object; Object.keys(CHANNELS).length
     is read by tests/unit/main/ipc/index.spec.ts handler-count assertion. Adding 7
     keys here is tracked automatically by that test once the channels are stubbed (Plan 20-06). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin + lock baileys 6.7.23 and qrcode 1.5.4; write the supply-chain ratchet</name>
  <read_first>
    - package.json (current dependency block + the existing pnpm config)
    - .planning/phases/20-foundation/20-RESEARCH.md § Standard Stack (exact-pin rationale, install commands, MUST NOT add list) + Hard Gate 10
    - MEMORY reference_pnpm_lockfile_ci_drift (adding deps without `pnpm install` breaks CI frozen-lockfile → use `pnpm install --lockfile-only`)
  </read_first>
  <action>
    Add the two production deps with EXACT pins (no caret/tilde): `@whiskeysockets/baileys` at `6.7.23` and `qrcode` at `1.5.4`, plus `@types/qrcode` as a dev dep. Run `pnpm add @whiskeysockets/baileys@6.7.23 qrcode@1.5.4` then `pnpm add -D @types/qrcode`, then `pnpm install --lockfile-only` to refresh pnpm-lock.yaml. Confirm package.json shows `"@whiskeysockets/baileys": "6.7.23"` with NO `^`/`~` prefix (per STACK.md pin strategy). Do NOT add any of the prohibited packages (`sharp`, `jimp`, `audio-decode`, `link-preview-js`, `whatsapp-web.js`, or any `baileys`/`lotusbail`/`*-baileys-*` fork — supply-chain attack vector per PITFALLS Pitfall 8). Then create tests/unit/main/whatsapp/supply-chain-pin.spec.ts (gate 10): read package.json, assert baileys dependency string === '6.7.23' (regex reject `^`/`~`), assert qrcode === '1.5.4'; read pnpm-lock.yaml and assert it contains a resolved `@whiskeysockets/baileys@6.7.23` entry; assert no dependency key matches `/lotusbail|(^|\/)baileys$/` outside the `@whiskeysockets/` scope.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/whatsapp/supply-chain-pin.spec.ts` passes.
    - package.json baileys pin is the literal string `6.7.23` (grep confirms no caret/tilde).
    - pnpm-lock.yaml is updated and committed in this plan's changeset; `pnpm install --frozen-lockfile` would succeed (lockfile in sync with package.json).
    - No prohibited package name appears in package.json dependencies.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/supply-chain-pin.spec.ts</automated>
  </verify>
  <done>baileys@6.7.23 + qrcode@1.5.4 exact-pinned, lockfile refreshed, gate-10 ratchet green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the main externalize exclude for Baileys + write the config gate spec</name>
  <read_first>
    - electron.vite.config.ts (full file — note main: at lines 46-60 has only define/build/resolve, NO plugins key; preload: line 67 is the externalizeDepsPlugin exclude precedent)
    - .planning/phases/20-foundation/20-RESEARCH.md correction #2 + Hard Gate 11 + D-13
    - .planning/phases/20-foundation/20-PATTERNS.md § electron.vite.config.ts (MOD — config)
    - MEMORY reference_esbuild_skips_typecheck (run `npm run typecheck` after this edit)
  </read_first>
  <action>
    ADD a `plugins` key to the `main:` section of electron.vite.config.ts (it currently has none) containing `externalizeDepsPlugin({ exclude: ['@whiskeysockets/baileys'] })` — mirror the preload `exclude: ['zod']` pattern at line 67; `externalizeDepsPlugin` is already imported at line 2. This bundles Baileys ESM→CJS via Rollup while keeping every other dep external (Correction #2 recommends option (a)). Do NOT remove the existing `define: oauthDefine`, `build`, or `resolve` keys — add `plugins` alongside them. Then create tests/unit/main/electron-vite-config.spec.ts (gate 11 + D-13): import the config (or read the file as text), assert `config.main.plugins` exists and that an externalize plugin's exclude list contains `@whiskeysockets/baileys`. Because the D-13 `syncFullHistory:false` flag lives in session-manager.ts (Plan 20-04), assert D-13/passive flags via a source-text check there is deferred to the passive-posture ratchet (Plan 20-02/20-04) — in THIS spec only assert the externalize exclude. Run `npm run typecheck` after the edit (esbuild skips typecheck).
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/electron-vite-config.spec.ts` passes (asserts main.plugins externalize exclude contains the baileys id).
    - `npm run typecheck` produces no NEW errors over the recorded baseline (the config edit is type-clean).
    - electron.vite.config.ts main section retains define/build/resolve AND now has plugins.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/electron-vite-config.spec.ts</automated>
  </verify>
  <done>Baileys bundles into out/main via the new main.plugins exclude; gate-11 config spec green; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Extend ProviderKey + declare the 7 WHATSAPP_* channels and DTOs</name>
  <read_first>
    - src/shared/provider.ts (line 1 ProviderKey union)
    - src/shared/ipc-contract.ts (CHANNELS const-object shape + existing Zod DTO patterns, e.g. ProviderAccountDto, knowledge channel DTOs)
    - .planning/phases/20-foundation/20-PATTERNS.md § src/shared/provider.ts + ipc-contract.ts
    - .planning/phases/20-foundation/20-RESEARCH.md § Architecture Patterns (channel list: 5 invoke + 2 push = 7) + Data Model (group/status/QR DTO shapes)
  </read_first>
  <action>
    In src/shared/provider.ts add `'whatsapp'` to the ProviderKey union (becomes `'google' | 'microsoft' | 'todoist' | 'whatsapp'`). In src/shared/ipc-contract.ts add 7 channel constants to CHANNELS following the existing naming convention: WHATSAPP_LINK, WHATSAPP_DISCONNECT, WHATSAPP_LIST_GROUPS, WHATSAPP_SET_TRACKED, WHATSAPP_STATUS (5 invoke), WHATSAPP_QR_UPDATE, WHATSAPP_STATE_CHANGED (2 push). Add Zod DTOs + exported types: a WhatsAppGroupDto (jid, displayName, tracked boolean, memberCount nullable), a WhatsAppStatusDto (status union 'ok'|'degraded'|'needs-auth'|'disconnected', accountId nullable = the JID per D-11, displayNumber nullable), a WhatsAppQrUpdateDto (dataUrl string, expiresAt nullable for the countdown), a WhatsAppSetTrackedReq (jid, tracked boolean), a WhatsAppStateChangedDto (status + accountId). NO send/reply channel exists (WA-11 passive; D-12 QR-only — no pairing-code channel). Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/ipc-contract` (or the project's contract spec, if present) and `npm run typecheck` pass with no new errors.
    - grep confirms CHANNELS contains exactly the 5 invoke + 2 push WHATSAPP_* keys and no WHATSAPP_SEND / WHATSAPP_PAIRING_CODE key exists.
    - ProviderKey union includes 'whatsapp'.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck 2>&1 | Select-String -Pattern "whatsapp|ipc-contract|provider" -SimpleMatch</automated>
  </verify>
  <done>ProviderKey + 7 channels + DTOs declared; no send/pairing channel; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Untrusted package supply chain crosses here at install time |
| build config → out/main bundle | Decides which deps are bundled vs left as external require |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering / Info disclosure | dependency install (malicious `lotusbail`/`baileys` fork exfiltrates session tokens) | mitigate | Exact pin `@whiskeysockets/baileys@6.7.23` only; committed pnpm-lock.yaml; supply-chain-pin ratchet rejects caret/tilde and any non-`@whiskeysockets/` baileys-ish name; CI `--frozen-lockfile` (gate 10). |
| T-20-02 | Tampering | electron-vite main bundle (wrong externalize → Baileys fails to load or pulls unexpected transitive at runtime) | mitigate | Scoped exclude bundles only Baileys; gate-11 spec asserts the exclude list contents. |
| T-20-03 | Elevation (future) | new IPC channel surface | accept (here) / mitigate (20-06) | Channels are declared only; real handlers + Zod validation land in Plan 20-06. No outbound/send channel is declared (WA-11). |
</threat_model>

<verification>
- `npx vitest run tests/unit/main/whatsapp/supply-chain-pin.spec.ts` green (gate 10).
- `npx vitest run tests/unit/main/electron-vite-config.spec.ts` green (gate 11).
- `npm run typecheck` shows no new errors over baseline after provider/contract/config edits.
- package.json baileys pin == `6.7.23` exact; pnpm-lock.yaml committed.
</verification>

<success_criteria>
- baileys@6.7.23 + qrcode@1.5.4 exact-pinned, lockfile committed (gate 10 green).
- main.plugins externalize exclude for Baileys present (gate 11 green).
- ProviderKey union + 7 WHATSAPP_* channels + DTOs available to all downstream plans.
- No outbound send / pairing-code channel declared.
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation/20-01-SUMMARY.md`
</output>
