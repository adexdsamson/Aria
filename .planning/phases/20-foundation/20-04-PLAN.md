---
phase: 20-foundation
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/main/whatsapp/session-manager.ts
autonomous: true
requirements: [WA-01, WA-03, WA-11, WA-12]

must_haves:
  truths:
    - "WhatsAppSessionManager owns exactly one Baileys socket; a new link first disconnects any existing socket (440 two-socket guard)"
    - "Socket config is passive: markOnlineOnConnect:false + emitOwnEvents:false + syncFullHistory:false, and the only presence call is sendPresenceUpdate('unavailable') on connection:'open'"
    - "Close events are classified: 401/403/440/500 → no reconnect → needs-auth; 408/515 → backoff (5s/15s/60s/300s/600s cap, ±20% jitter) → degraded after 5 failures"
    - "A nightly socket-recycle cron is registered via scheduler.cronRegistry at 03:00 (no bare cron.schedule); socket startup failure never rejects app boot"
  artifacts:
    - path: "src/main/whatsapp/session-manager.ts"
      provides: "createWhatsAppSessionManager singleton: makeWASocket, QR push, reconnect classify, recycle cron, provider_account.status writes"
      contains: "markOnlineOnConnect: false"
  key_links:
    - from: "session-manager connection.update(open)"
      to: "provider_account.status='ok' + WHATSAPP_STATE_CHANGED push"
      via: "creds.me.id JID insert"
      pattern: "connection"
    - from: "session-manager recycle cron"
      to: "scheduler.cronRegistry"
      via: "cronRegistry.set('whatsapp-socket-recycle', task)"
      pattern: "cronRegistry"
# Decision coverage:
# D-11 (account_id = creds.me.id JID) — provider_account row inserted on connection:'open' with the JID
# D-13 (syncFullHistory:false explicit) — in the makeWASocket config literal
# Gates 1/2/5/6 + A5 (PITFALLS backoff curve) covered here
---

<objective>
Build the WhatsAppSessionManager singleton: the one Baileys socket, the passive-posture config, the QR-string→data-URL push, the reconnect-classification state machine, the nightly socket-recycle cron, and the provider_account.status writes that the AccountRow chip reads. Socket failure is wrapped so it never blocks app boot (WA-12 degradable).

Purpose: This is the heart of the integration — the only component that talks to WhatsApp. It must be passive by construction (the WA-11 hard gate), reconnect like a well-behaved client (not a credential-stuffing storm), and degrade silently. Group-sync and ingest (Plan 20-05) attach their event handlers to the socket this manager owns.
Output: session-manager.ts; the passive-posture ratchet becomes load-bearing (config flags now present); session-reconnect.spec + session-recycle.spec turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation/20-RESEARCH.md
@.planning/phases/20-foundation/20-PATTERNS.md
@src/main/entitlement/service.ts
@src/main/folder-ingestion/sweep-cron.ts
@src/main/whatsapp/auth-state.ts

<interfaces>
<!-- From Plan 20-03: useSQLiteAuthState(db, logger) → { state:{creds,keys}, saveCreds } -->
<!-- From Plan 20-01: WhatsAppStatusDto / WhatsAppQrUpdateDto / WhatsAppStateChangedDto in ipc-contract;
     CHANNELS.WHATSAPP_QR_UPDATE / WHATSAPP_STATE_CHANGED (push) -->
<!-- Deps shape (mirror EntitlementServiceDeps + sweep-cron deps):
     { db: Db; scheduler: SchedulerHandle; logger: Logger; emitToRenderer?: (channel, payload)=>void } -->
<!-- scheduler.cronRegistry.set(KEY, task) is the no-bare-cron registration (sweep-cron.ts:82-84) -->
<!-- Baileys 6.7.23: makeWASocket, DisconnectReason, makeCacheableSignalKeyStore, fetchLatestBaileysVersion(pin, do NOT call per-connect) -->
<!-- powerMonitor suspend/resume → pause/resume reconnect (3-5s delay on resume) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Passive socket + QR push + provider_account.status + singleton guard</name>
  <read_first>
    - src/main/entitlement/service.ts (class + injected deps + the bootstrapInflight single-flight promise guard ~line 61 — mirror for the single-socket guard)
    - .planning/phases/20-foundation/20-PATTERNS.md § session-manager.ts (class+deps shape, passive config, pin comment, singleton guard)
    - .planning/phases/20-foundation/20-RESEARCH.md § Code Examples (passive-posture config) + § Architecture Patterns + Open-Question #4 (provider_account row inserted on connection:'open' with creds.me.id JID)
    - src/main/whatsapp/auth-state.ts (the useSQLiteAuthState return contract from Plan 20-03)
  </read_first>
  <action>
    Create src/main/whatsapp/session-manager.ts exporting `createWhatsAppSessionManager(deps)` (factory returning a manager handle with start/stop/startLink/disconnect/getStatus) or a class — mirror the EntitlementService shape. In startLink(): if a socket already exists, `await this.disconnect()` FIRST (440 two-socket guard); build the auth store via useSQLiteAuthState(db, logger) wrapped in makeCacheableSignalKeyStore; call makeWASocket with the EXACT passive config — `markOnlineOnConnect: false`, `emitOwnEvents: false`, `syncFullHistory: false` (D-13), the cacheable key store, and the pinned version (do NOT call fetchLatestWaWebVersion per-connect — fingerprint/incompat anti-pattern). Add the required pin comment: `// Pinned 6.7.23 (legacy tag). v7 migration blocked on LID API + WASM-asar.`. On `connection.update`: when `qr` present → `qrcode.toDataURL(qr, { errorCorrectionLevel: 'L' })` → emit WHATSAPP_QR_UPDATE push (data-URL + expiry for the countdown); when `connection==='open'` → `void sock.sendPresenceUpdate('unavailable')` (the ONLY permitted presence call), insert/upsert the provider_account row with `account_id = creds.me.id` JID (D-11), `provider_key='whatsapp'`, `status='ok'`, `capabilities_json='{"messaging":1}'` (NOT NULL, no default — must supply), display_email = the readable number derived from the JID, and emit WHATSAPP_STATE_CHANGED. Call saveCreds on creds.update. Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/whatsapp/passive-posture.ratchet.spec.ts` passes AND now exercises the config-flag assertions (markOnlineOnConnect:false, emitOwnEvents:false, syncFullHistory:false all present; only presence arg is 'unavailable').
    - grep session-manager.ts: the pin comment is present; no `.sendMessage(` / `.sendReceipt(` / `.readMessages(`; no per-connect `fetchLatestWaWebVersion`.
    - provider_account row uses creds.me.id as account_id and supplies capabilities_json.
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/passive-posture.ratchet.spec.ts</automated>
  </verify>
  <done>Passive socket + QR push + provider_account.status + 440 singleton guard; passive ratchet load-bearing & green.</done>
</task>

<task type="auto">
  <name>Task 2: Reconnect classification + nightly recycle cron + boot-safe degradation</name>
  <read_first>
    - src/main/folder-ingestion/sweep-cron.ts (CRON_KEY const, scheduler.cronRegistry.set(KEY, task) at 82-84, the dbHolder seal-guard 67-77, powerMonitor integration)
    - .planning/phases/20-foundation/20-PATTERNS.md § session-manager.ts (reconnect classification + nightly recycle 03:00; retention is 03:30 in Plan 20-05 — must not collide)
    - .planning/phases/20-foundation/20-RESEARCH.md § Pattern 4 (DisconnectReason table) + Assumption A5 (use the PITFALLS curve) + Hard Gates 5/6 + WA-12
    - tests/static/no-bare-cron-schedule.spec.ts (no bare cron.schedule)
  </read_first>
  <action>
    Add the reconnect state machine to session-manager.ts. On `connection.update` with `connection==='close'`, read the DisconnectReason from the lastDisconnect error: 401 loggedOut / 403 forbidden / 440 connectionReplaced / 500 badSession → DO NOT reconnect → set provider_account.status='needs-auth' + emit WHATSAPP_STATE_CHANGED (the AccountRow Reconnect button drives re-link). 408 timedOut/connectionLost / 515 restartRequired → schedule a reconnect with the PITFALLS anti-ban backoff curve (Assumption A5): 5s, 15s, 60s, 300s, 600s cap, each with ±20% jitter; after 5 consecutive failures stop and set status='degraded'. Wire powerMonitor: on 'suspend' cancel pending reconnects; on 'resume' wait 3-5s then attempt once. Register the nightly socket-recycle cron via `scheduler.cronRegistry.set('whatsapp-socket-recycle', task)` at `'0 3 * * *'` (03:00) — firing it calls disconnect() then start() (heap-leak mitigation, Pitfall: storing raw events). NO bare nodeCron.schedule without cronRegistry.set (ratchet). In start(): no-op if no whatsapp provider_account row exists; wrap socket creation so any throw is caught/logged and NEVER rejects (WA-12 — same defensive .catch() shape as startKnowledgeFolderLifecycle). stop(): end the socket and delete the recycle cron from cronRegistry. Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/whatsapp/session-reconnect.spec.ts` passes: 401/403/440/500 → no reconnect (→needs-auth); 408/515 → backoff scheduled; 5th failure → degraded.
    - `npx vitest run tests/unit/main/whatsapp/session-recycle.spec.ts` passes: recycle cron registered in scheduler.cronRegistry; firing → disconnect() then start().
    - `npx vitest run tests/static/no-bare-cron-schedule.spec.ts` still green (recycle uses cronRegistry).
    - The backoff curve in source matches 5s/15s/60s/300s/600s with jitter (A5 PITFALLS curve, not the 1/2/4/8/30 ARCHITECTURE curve).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/session-reconnect.spec.ts</automated>
  </verify>
  <done>Reconnect classification (gate 5) + recycle cron (gate 6) + boot-safe degradation (WA-12); both specs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WhatsApp server → socket | Untrusted protocol events (close codes, QR, presence) cross here |
| socket lifecycle → app boot | A socket failure must not crash or block Aria boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Repudiation / DoS-to-user (ban) | passive posture — any send/presence frame on a flagged personal number → fastest ban | mitigate | markOnlineOnConnect:false + emitOwnEvents:false + the single 'unavailable' presence call; passive-posture ratchet enforces 0 send frames (gates 1/2, WA-11). |
| T-20-11 | Spoofing (to WhatsApp anti-abuse) | reconnect storm looks like credential-stuffing | mitigate | Classify 401/403/440/500 (no reconnect) vs 408/515 (backoff); anti-ban curve + cap-5 → degraded; powerMonitor pause on suspend (gate 5). |
| T-20-12 | DoS | two-socket 440 conflict | mitigate | Single-socket guard: `if (socket) await disconnect()` before makeWASocket; one persistent socket only. |
| T-20-13 | DoS | unbounded heap from retained Baileys event objects (#2090) | mitigate | Nightly socket recycle at 03:00 via cronRegistry; never store raw event objects beyond the handler. |
| T-20-14 | DoS (to whole app) | socket startup throw rejects app boot | mitigate | start() wrapped in defensive .catch(); WA-12 degradable — briefing/email/calendar/tasks unaffected. |
</threat_model>

<verification>
- `npx vitest run tests/unit/main/whatsapp/passive-posture.ratchet.spec.ts` green (now config-flag-bearing).
- `npx vitest run tests/unit/main/whatsapp/session-reconnect.spec.ts` green (gate 5).
- `npx vitest run tests/unit/main/whatsapp/session-recycle.spec.ts` green (gate 6).
- `npx vitest run tests/static/no-bare-cron-schedule.spec.ts` green.
- `npm run typecheck` clean.
</verification>

<success_criteria>
- One passive Baileys socket; QR pushed as data-URL; provider_account.status drives the chip.
- Reconnect classified + anti-ban backoff + recycle cron; socket failure never blocks boot.
- WA-11 passive posture enforced by the now-load-bearing ratchet.
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation/20-04-SUMMARY.md`
</output>
