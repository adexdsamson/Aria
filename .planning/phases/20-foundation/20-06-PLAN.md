---
phase: 20-foundation
plan: 06
type: execute
wave: 5
depends_on: [01, 03, 04, 05]
files_modified:
  - src/main/ipc/whatsapp.ts
  - src/main/ipc/index.ts
  - src/main/index.ts
  - src/main/ipc/provider-accounts.ts
  - tests/unit/main/ipc/index.spec.ts
autonomous: true
requirements: [WA-01, WA-03, WA-04, WA-05, WA-12]

must_haves:
  truths:
    - "ipc/whatsapp.ts exports WHATSAPP_CHANNELS (the 5 invoke channels) as the single source of truth for the removeHandler loop and the handler-count test"
    - "ipc/index.ts registers pre-unlock stubs for the 5 invoke channels (always skip.add'd) and appends the 2 push channels to pushOnlyChannels — handler-count stays Object.keys(CHANNELS).length"
    - "main/index.ts bootPoll (NOT onDbReady) constructs the manager, loops removeHandler(WHATSAPP_CHANNELS), registers the real handlers, and starts the 03:30 retention cron — wrapped so a socket throw never rejects boot"
    - "provider-accounts.ts disconnect cascade has a 'whatsapp' branch that calls manager.stop() then deletes auth_state + group (FK CASCADE removes message + digest) + the provider_account row"
  artifacts:
    - path: "src/main/ipc/whatsapp.ts"
      provides: "registerWhatsAppHandlers + WHATSAPP_CHANNELS const array"
      contains: "WHATSAPP_CHANNELS"
    - path: "src/main/index.ts"
      provides: "bootPoll removeHandler loop + manager construction + retention start"
      contains: "WHATSAPP_CHANNELS"
  key_links:
    - from: "main/index.ts bootPoll"
      to: "registerWhatsAppHandlers"
      via: "removeHandler(WHATSAPP_CHANNELS) loop then real register"
      pattern: "removeHandler"
    - from: "provider-accounts.ts disconnect"
      to: "manager.stop() + WA-row deletes"
      via: "db.transaction whatsapp branch"
      pattern: "whatsapp"
# Decision coverage:
# WA-04 disconnect cascade; WA-12 boot-safe wiring; IPC double-register safety (WHATSAPP_CHANNELS removeHandler loop)
# D-11 (account_id JID) read by STATUS/LIST handlers
---

<objective>
Wire the WhatsApp module into Aria's IPC and bootstrap spine: the channel registrar (exporting the canonical WHATSAPP_CHANNELS array), the pre-unlock stubs in ipc/index.ts, the post-unlock construction + real-handler registration in main/index.ts bootPoll (the CORRECTED location — not onDbReady), and the disconnect cascade branch in provider-accounts.ts. Update the handler-count test.

Purpose: This is the integration spine that makes the renderer able to drive linking, group toggling, status, and disconnect. The IPC double-register pattern is mandatory — all 4 sites broke on 2026-06-09 from getting this wrong; the WHATSAPP_CHANNELS const + removeHandler loop is the fix. The cascade is WA-04's deletion guarantee.
Output: ipc/whatsapp.ts + ipc/index.ts stubs + main/index.ts bootPoll + provider-accounts.ts cascade; handler-count test green for +7 channels; no double-register throw.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation/20-RESEARCH.md
@.planning/phases/20-foundation/20-PATTERNS.md
@src/main/ipc/provider-accounts.ts
@src/main/whatsapp/session-manager.ts

<interfaces>
<!-- VERIFIED HEAD: ipc/index.ts knowledgeChannels stub block at 488-533; pushOnlyChannels at 666-679. -->
<!-- VERIFIED HEAD: main/index.ts bootPoll setInterval at 501-559; KNOWLEDGE_FOLDER_CHANNELS removeHandler loop at 532-534;
     onDbReady (ipc/index.ts:123-130) ONLY does startSyncOrchestrator — do NOT wire WhatsApp there (correction #1). -->
<!-- VERIFIED HEAD: provider-accounts.ts disconnect at 58-82; db.transaction wrapper at 72-79;
     providerKey type cast at lines 42 + 61 (TWO sites) must add | 'whatsapp'. -->
<!-- VERIFIED HEAD: tests/unit/main/ipc/index.spec.ts:71-81 asserts handlers.size === Object.keys(CHANNELS).length
     — auto-tracks +7 once channels are in CHANNELS AND stubbed/registered. No hardcoded count. -->
<!-- knowledge-folders.ts is the registrar + KNOWLEDGE_FOLDER_CHANNELS export precedent. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ipc/whatsapp.ts registrar + WHATSAPP_CHANNELS + ipc/index.ts pre-unlock stubs</name>
  <read_first>
    - src/main/ipc/knowledge-folders.ts (KNOWLEDGE_FOLDER_CHANNELS export ~29-46 + registrar fn ~48-58 — the canonical pattern)
    - src/main/ipc/index.ts (knowledgeChannels stub block 488-533 — note skip.add OUTSIDE the if(db) guard, db-null skip-trap; pushOnlyChannels 666-679)
    - .planning/phases/20-foundation/20-PATTERNS.md § ipc/whatsapp.ts + § ipc/index.ts (MOD)
    - MEMORY reference_electron_ipc_double_register (export canonical channel array; removeHandler loop) + reference_ipc_db_null_skip_trap (skip.add co-located with registration, inside guard for real / outside for stubs)
  </read_first>
  <action>
    Create src/main/ipc/whatsapp.ts. Export `WHATSAPP_CHANNELS` const array = the 5 INVOKE channels only (WHATSAPP_LINK, WHATSAPP_DISCONNECT, WHATSAPP_LIST_GROUPS, WHATSAPP_SET_TRACKED, WHATSAPP_STATUS) — the 2 push channels (WHATSAPP_QR_UPDATE, WHATSAPP_STATE_CHANGED) are NOT in this array (they go to pushOnlyChannels). Export `registerWhatsAppHandlers({ ipcMain, logger, dbHolder, scheduler, manager })`: WHATSAPP_LINK → manager.startLink(); WHATSAPP_DISCONNECT → delegate to the provider-accounts cascade (or manager.stop() + the same cascade); WHATSAPP_LIST_GROUPS → `if (!db) return { error:'DB_NOT_OPEN' }` then SELECT whatsapp_group rows mapped to WhatsAppGroupDto; WHATSAPP_SET_TRACKED → validate the Zod req, UPDATE whatsapp_group.tracked; WHATSAPP_STATUS → read provider_account.status + the JID. Every handler uses the notReady() `if (!db) return { error:'DB_NOT_OPEN' }` idiom. Then MODIFY src/main/ipc/index.ts: add a whatsappChannels stub block mirroring knowledgeChannels else-branch EXACTLY — register `() => ({ ok:false, error:'db-locked' })` stubs for each invoke channel when not already skip'd, and `whatsappChannels.forEach((c) => skip.add(c))` ALWAYS (outside any if(db) guard) so the handler-count test passes; append WHATSAPP_QR_UPDATE + WHATSAPP_STATE_CHANGED to the pushOnlyChannels array. Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - ipc/whatsapp.ts exports WHATSAPP_CHANNELS (exactly the 5 invoke channels) + registerWhatsAppHandlers.
    - `npx vitest run tests/unit/main/ipc/index.spec.ts` passes: handlers.size === Object.keys(CHANNELS).length with the +7 WHATSAPP channels stubbed/registered (5 invoke stubs + 2 push stubs).
    - grep ipc/index.ts: whatsappChannels skip.add is outside the if(db) guard (db-null skip-trap avoided); the 2 push channels are in pushOnlyChannels.
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/ipc/index.spec.ts</automated>
  </verify>
  <done>Registrar + WHATSAPP_CHANNELS + pre-unlock stubs landed; handler-count test green for +7 channels.</done>
</task>

<task type="auto">
  <name>Task 2: main/index.ts bootPoll wiring (removeHandler loop + manager + retention) — boot-safe</name>
  <read_first>
    - src/main/index.ts (bootPoll 501-559; the Knowledge Folders block 516-557 — the EXACT template: lifecycleBooted guard, removeHandler(KNOWLEDGE_FOLDER_CHANNELS) loop 532-534, real register, .catch() defensive start 551-556)
    - .planning/phases/20-foundation/20-PATTERNS.md § main/index.ts (MOD) — corrected location is bootPoll, NOT onDbReady
    - .planning/phases/20-foundation/20-RESEARCH.md correction #1 (onDbReady only does startSyncOrchestrator)
    - src/main/whatsapp/session-manager.ts (createWhatsAppSessionManager) + src/main/whatsapp/retention.ts (startWhatsAppRetention)
  </read_first>
  <action>
    MODIFY src/main/index.ts bootPoll. Add a `whatsappBooted` guard (mirror lifecycleBooted at 516). Once `dbHolder.db` is truthy and not yet booted: `for (const ch of WHATSAPP_CHANNELS) ipcMain.removeHandler(ch);` (remove the pre-unlock stubs — mirrors 532-534); construct `whatsAppManager = createWhatsAppSessionManager({ db: dbHolder.db, scheduler, logger, emitToRenderer })`; call `whatsAppManager.start()` (no-op if no whatsapp provider_account row); `registerWhatsAppHandlers({ ipcMain, logger, dbHolder, scheduler, manager: whatsAppManager })`; `startWhatsAppRetention({ db: dbHolder.db, logger, scheduler, dbHolder })` (the 03:30 cron). Wrap start() / the whole block so any throw is caught and logged (mirror the startKnowledgeFolderLifecycle .catch() at 551-556) — a socket failure must NEVER reject app boot (WA-12). The QR/state push needs the renderer emitter — bind emitToRenderer the same way other push channels reach the renderer (mainWindow.webContents.send). Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - main/index.ts bootPoll constructs the manager + loops removeHandler(WHATSAPP_CHANNELS) + registers real handlers + starts retention, behind a whatsappBooted guard.
    - The block is wrapped so a thrown socket error is caught/logged and does not reject boot (grep: a .catch or try/catch around start()).
    - `npx vitest run tests/unit/main/ipc/index.spec.ts` still green (no double-register: stubs removed before real register).
    - WhatsApp wiring is in bootPoll, NOT in ipc/index.ts onDbReady (correction #1).
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck 2>&1 | Select-String -Pattern "index.ts|whatsapp|error TS" -SimpleMatch</automated>
  </verify>
  <done>bootPoll wires manager + real handlers + retention, boot-safe; no double-register throw (WA-12 + IPC safety).</done>
</task>

<task type="auto">
  <name>Task 3: provider-accounts.ts disconnect cascade for 'whatsapp' (WA-04)</name>
  <read_first>
    - src/main/ipc/provider-accounts.ts (full file — the two providerKey type casts at 42 + 61; the db.transaction cascade at 72-79)
    - .planning/phases/20-foundation/20-PATTERNS.md § provider-accounts.ts (MOD) — TWO type sites + the whatsapp branch
    - .planning/phases/20-foundation/20-RESEARCH.md § WA-04 + the migration-138 CASCADE (group → message + digest)
    - src/main/whatsapp/session-manager.ts (the manager.stop() contract)
    - tests/unit/main/whatsapp (a disconnect integration spec — add if not present from Plan 20-02; assert 0 rows across all 4 WA tables + provider_account)
  </read_first>
  <action>
    MODIFY src/main/ipc/provider-accounts.ts. Add `| 'whatsapp'` to the providerKey type cast at BOTH sites (line 42 PROVIDER_ACCOUNT_UPDATE, line 61 PROVIDER_ACCOUNT_DISCONNECT). In the PROVIDER_ACCOUNT_DISCONNECT handler, add a `'whatsapp'` branch: call `whatsAppManager?.stop()` (tear down the socket — the manager must be reachable here; pass it via deps or a module-level ref set in bootPoll) BEFORE the delete transaction; inside the existing `db.transaction(() => {...})`, add `DELETE FROM whatsapp_auth_state` and `DELETE FROM whatsapp_group WHERE` (the group delete CASCADEs whatsapp_message + whatsapp_group_digest via the FK from migration 138 — so no explicit message/digest delete needed), then the existing `DELETE FROM provider_account WHERE provider_key=? AND account_id=?` covers the WA row. Guard the WA-specific deletes so they only run for `r.providerKey === 'whatsapp'`. Add/complete a disconnect integration spec asserting: after WHATSAPP_DISCONNECT, 0 rows in whatsapp_auth_state, whatsapp_group, whatsapp_message, whatsapp_group_digest, AND the provider_account row gone, AND manager.stop() was called. Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/whatsapp/whatsapp-disconnect.spec.ts` (or the disconnect integration spec) passes: 0 rows across all 4 WA tables + provider_account row removed + manager.stop() called.
    - provider-accounts.ts has `| 'whatsapp'` at both type-cast sites; the whatsapp delete branch is inside the existing db.transaction wrapper.
    - The group delete relies on the migration-138 ON DELETE CASCADE for message + digest (no orphan rows).
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/whatsapp-disconnect.spec.ts</automated>
  </verify>
  <done>Disconnect cascade tears down socket + deletes all WA data via CASCADE (WA-04); integration spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main IPC | Untrusted invoke payloads cross here (LIST/SET_TRACKED/LINK/DISCONNECT) |
| stub→real handler swap | Double-registration crashes the app if not deduped via removeHandler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-20 | DoS | ipcMain.handle double-register throw (all 4 sites broke 2026-06-09) | mitigate | WHATSAPP_CHANNELS const + bootPoll removeHandler loop before real register; handler-count test asserts exact count. |
| T-20-21 | Tampering | malformed SET_TRACKED / LINK payload | mitigate | Zod DTO validation on each handler; notReady() guard for db-null. |
| T-20-22 | Info disclosure (residual) | WhatsApp PII left after disconnect | mitigate | Cascade deletes auth_state + group (→CASCADE message + digest) + provider_account in one transaction; integration test asserts 0 rows (WA-04). |
| T-20-23 | DoS (to whole app) | bootPoll wiring throw rejects boot | mitigate | start()/block wrapped in .catch(); WA-12 degradable. |
</threat_model>

<verification>
- `npx vitest run tests/unit/main/ipc/index.spec.ts` green (+7 channels, no double-register).
- `npx vitest run tests/unit/main/whatsapp/whatsapp-disconnect.spec.ts` green (WA-04).
- `npm run typecheck` clean.
- All 3 static ratchets (passive-posture, no-frontier, no-bare-cron) still green.
</verification>

<success_criteria>
- WHATSAPP_CHANNELS canonical array + removeHandler loop prevents the double-register crash.
- bootPoll (not onDbReady) wires manager + handlers + retention, boot-safe (WA-12).
- Disconnect cascade deletes every WA row + the provider_account row (WA-04).
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation/20-06-SUMMARY.md`
</output>
