---
phase: 20-foundation
plan: 05
type: execute
wave: 4
depends_on: [03, 04]
files_modified:
  - src/main/whatsapp/group-sync.ts
  - src/main/whatsapp/ingest.ts
  - src/main/whatsapp/retention.ts
autonomous: true
requirements: [WA-05, WA-06, WA-07, WA-11]

must_haves:
  truths:
    - "groups.upsert writes whatsapp_group rows with tracked=0 by default (privacy boundary); newly-joined groups stay untracked"
    - "messages.upsert is filtered in 3 lines BEFORE any write OR log: type!=='notify' → drop; !@g.us → drop DMs; !is_tracked → drop untracked groups"
    - "Tracked-group text-only messages are buffered in memory and flushed via scheduler.queue.add in ONE transaction (~2s window) — never a sync db.run() in the event handler"
    - "A dedicated retention cron at 03:30 (NOT 03:00) deletes whatsapp_message older than 30 days, registered via scheduler.cronRegistry"
  artifacts:
    - path: "src/main/whatsapp/ingest.ts"
      provides: "privacy-filtered + batched message ingest"
      contains: "@g.us"
    - path: "src/main/whatsapp/retention.ts"
      provides: "30-day rolling sweep at 03:30 via cronRegistry"
      contains: "whatsapp-retention-sweep"
    - path: "src/main/whatsapp/group-sync.ts"
      provides: "groups.upsert → whatsapp_group untracked-default"
      contains: "whatsapp_group"
  key_links:
    - from: "ingest.ts messages.upsert"
      to: "whatsapp_message (INSERT OR IGNORE)"
      via: "scheduler.queue.add(() => tx())"
      pattern: "scheduler\\.queue\\.add"
    - from: "ingest.ts isTracked"
      to: "whatsapp_group.tracked"
      via: "prepared .get()"
      pattern: "tracked"
# Decision coverage:
# D-03 (untracked by default) — group-sync inserts tracked=0
# D-04 (newly-joined surfaces untracked at top) — group-sync never default-tracks
# Gates 7/8/9 (batch flush + notify line-1 + @g.us/is_tracked lines 2-3) covered here
---

<objective>
Build the data-capture layer attached to the manager's socket: group discovery (groups.upsert → whatsapp_group, untracked by default), the privacy-filtered + batched message ingest (the CRITICAL WA-06 boundary), and the 30-day retention sweep cron at 03:30.

Purpose: This is where third-party PII is gated. The 3-line privacy filter — executed before any write OR log — is the single privacy boundary for the whole milestone (WA-06). Getting the order and the no-log discipline right here is non-negotiable. Batching via the scheduler queue keeps the single-writer model intact under a message flood.
Output: group-sync.ts + ingest.ts + retention.ts; ingest-privacy.spec (CRITICAL) + whatsapp-retention.spec turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation/20-RESEARCH.md
@.planning/phases/20-foundation/20-PATTERNS.md
@src/main/integrations/google/sync-gmail.ts
@src/main/folder-ingestion/sweep-cron.ts
@src/main/whatsapp/session-manager.ts

<interfaces>
<!-- Plan 20-04 session-manager owns the socket; this plan exports handler-registration
     functions the manager calls, e.g. registerGroupSync(sock, deps) and
     registerIngest(sock, deps) — attach to sock.ev. Keep socket ownership in the manager. -->
<!-- whatsapp_group: jid PK, display_name, description, tracked INTEGER DEFAULT 0, member_count, updated_at -->
<!-- whatsapp_message: id, jid, sender_jid, wa_id, sent_at, body_text, ingested_at, UNIQUE(jid,wa_id) -->
<!-- scheduler.queue.add(() => { const tx = db.transaction(...); tx(); }) — sync-gmail.ts:170-172 precedent -->
<!-- scheduler.cronRegistry.set(KEY, task) — sweep-cron.ts:82-84; recycle is 03:00, retention MUST be 03:30 -->
<!-- Baileys 6.7.23 message shape: msg.key.remoteJid, msg.message.{conversation|extendedTextMessage.text|imageMessage.caption|...} -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ingest.ts — 3-line privacy filter + text extraction + p-queue batch flush (gates 7/8/9)</name>
  <behavior>
    - type:'append' or type:'notify'==false → handler returns immediately, ZERO writes (gate 8)
    - 1:1 `@s.whatsapp.net` message → skipped, ZERO rows, NO log of body (gate 9, WA-06)
    - untracked `@g.us` message → skipped, ZERO rows, NO log of body (WA-06 boundary)
    - tracked `@g.us` text message → buffered, then written exactly once (INSERT OR IGNORE on UNIQUE(jid,wa_id))
    - image/audio/video/document/sticker in a tracked group → extractText returns null → no row (WA-07 text-only)
    - N messages within a flush window → exactly ONE scheduler.queue.add transaction (gate 7), never a sync db.run() in the handler
  </behavior>
  <read_first>
    - src/main/integrations/google/sync-gmail.ts (lines 16-19, 128, 170-172 — scheduler.queue.add(() => tx()) single-writer batch precedent)
    - .planning/phases/20-foundation/20-PATTERNS.md § ingest.ts (the 3-line filter + batch flush, in order)
    - .planning/phases/20-foundation/20-RESEARCH.md § Pattern 3 (privacy filter) + Hard Gates 7/8/9 + WA-06/WA-07 test rows
    - tests/unit/main/whatsapp/ingest-privacy.spec.ts (the assertions to satisfy — written in Plan 20-02)
  </read_first>
  <action>
    Create src/main/whatsapp/ingest.ts exporting a registration fn the manager calls, attaching to sock.ev.on('messages.upsert', ({ messages, type }) => ...). The filter MUST be exactly these statements in this order, BEFORE any write OR any logger call referencing message content: LINE 1 `if (type !== 'notify') return;` (gate 8 — drop history/append batches); per-message LINE 2 `if (!jid.endsWith('@g.us')) continue;` (gate 9 — drop 1:1 DMs); LINE 3 `if (!isTracked(jid)) continue;` where isTracked is a prepared `.get()` against whatsapp_group.tracked (DB is the single source of truth). Then `const text = extractText(msg); if (text == null) continue;` — extractText whitelists conversation / extendedTextMessage.text / image|video|document caption (return the caption text), and returns null for media-without-caption / audio / sticker (WA-07 text-only; media shown as placeholders downstream, not stored as blobs). Push survivors to an in-memory buffer; flush on a ~2s timer (Claude's-discretion interval) via `scheduler.queue.add(() => { const tx = db.transaction(() => { for (const r of batch) insertStmt.run(...); }); tx(); })` using `INSERT OR IGNORE` against UNIQUE(jid,wa_id). NEVER call db.run() synchronously inside the messages.upsert callback (gate 7). NEVER logger.* a message body before the filter passes (gate 9). Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/whatsapp/ingest-privacy.spec.ts` passes ALL assertions: only the tracked-group text msg is written; DM + untracked produce ZERO rows AND no log of body; type:'append' writes nothing; media → no row.
    - grep ingest.ts: no `db.*\.run(` inside the messages.upsert callback (gate 7); the 3 filter lines appear in order before any write/log.
    - passive-posture + no-frontier ratchets still green (ingest adds no send/frontier call).
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/ingest-privacy.spec.ts</automated>
  </verify>
  <done>CRITICAL privacy filter + batched ingest landed; ingest-privacy.spec fully green (gates 7/8/9, WA-06/07).</done>
</task>

<task type="auto">
  <name>Task 2: group-sync.ts (untracked-default upsert) + retention.ts (03:30 sweep)</name>
  <read_first>
    - src/main/folder-ingestion/sweep-cron.ts (entire shape: CRON_KEY const, SweepCronDeps/SweepHandle, runSweep()+runNow(), cronRegistry.set 82-84, dbHolder seal-guard 67-77)
    - src/main/ipc/provider-accounts.ts (the prepared-statement UPDATE/upsert idiom)
    - .planning/phases/20-foundation/20-PATTERNS.md § group-sync.ts + § retention.ts
    - .planning/phases/20-foundation/20-RESEARCH.md § Data Model + D-03/D-04 + D-14 addendum (03:30, NOT 03:00; sibling cron, do NOT extend knowledge sweep-cron)
    - tests/unit/main/whatsapp/whatsapp-retention.spec.ts (assertions to satisfy)
  </read_first>
  <action>
    Create src/main/whatsapp/group-sync.ts exporting a registration fn attaching to sock.ev: on `groups.upsert`, upsert each group into whatsapp_group (jid, display_name=g.subject ?? g.id, description=g.desc, member_count=g.size) via `INSERT ... ON CONFLICT(jid) DO UPDATE` — tracked is NEVER set here, so it defaults to 0 in the schema (D-03 privacy boundary; D-04 newly-joined groups stay untracked and surface via a badge later). Also handle `group-participants.update` to refresh member_count. Create src/main/whatsapp/retention.ts COPYING the sweep-cron.ts shape: `const CRON_KEY = 'whatsapp-retention-sweep'`; schedule at `deps.cron ?? '30 3 * * *'` (03:30 — must NOT share the 03:00 recycle minute, D-14 addendum); register via `scheduler.cronRegistry.set(CRON_KEY, task)` (no bare cron.schedule); include the dbHolder seal-guard (if dbHolder present and db null → pendingCatchup + return). runSweep deletes `whatsapp_message WHERE sent_at < datetime('now','-30 days')`; expose `runNow()`. Do NOT modify the knowledge sweep-cron.ts (D-14 addendum: this is a distinct sibling cron). Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/whatsapp/whatsapp-retention.spec.ts` passes: runNow() deletes rows with sent_at < now-30d and keeps newer; cron registered in scheduler.cronRegistry with the '30 3 * * *' schedule.
    - `npx vitest run tests/static/no-bare-cron-schedule.spec.ts` green (retention uses cronRegistry).
    - grep group-sync.ts: the groups.upsert upsert never sets tracked (defaults to 0).
    - retention CRON time is '30 3 * * *' (03:30), confirmed distinct from the 03:00 recycle; src/main/folder-ingestion/sweep-cron.ts is unmodified.
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/whatsapp-retention.spec.ts</automated>
  </verify>
  <done>group-sync untracked-default + retention 03:30 sweep landed; retention.spec + no-bare-cron green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Baileys messages.upsert → DB | Third-party group + DM content (PII) crosses here — the privacy filter IS the access-control boundary |
| event handler → single-writer DB | Sync writes in the handler would block the writer under flood |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-15 | Info disclosure | untracked-group or 1:1 DM content persisted or logged | mitigate | 3-line filter (notify / @g.us / is_tracked) executed before ANY write or log; ingest-privacy integration asserts ZERO rows + no body log (gate 9, WA-06). |
| T-20-16 | Info disclosure | media blobs (images/docs) stored | mitigate | extractText whitelists text/captions only, returns null for media → dropped (WA-07 text-only). |
| T-20-17 | DoS | sync db.run() per message freezes the single-writer under a flood | mitigate | In-memory buffer + scheduler.queue.add one-transaction flush; static grep bans db.run() in the handler (gate 7). |
| T-20-18 | Info disclosure | unbounded retention of PII | mitigate | 30-day rolling sweep at 03:30 via cronRegistry (WA-07). |
| T-20-19 | Elevation | a group auto-tracked by default leaks before user consent | mitigate | tracked defaults to 0 in schema; group-sync never sets it (D-03 — track toggle is the only authorization). |
</threat_model>

<verification>
- `npx vitest run tests/unit/main/whatsapp/ingest-privacy.spec.ts` green (CRITICAL — gates 7/8/9, WA-06/07).
- `npx vitest run tests/unit/main/whatsapp/whatsapp-retention.spec.ts` green (WA-07).
- `npx vitest run tests/static/no-bare-cron-schedule.spec.ts` + passive-posture + no-frontier ratchets all green.
- `npm run typecheck` clean.
</verification>

<success_criteria>
- The 3-line privacy filter (before any write/log) is the enforced WA-06 boundary; only tracked-group text is stored.
- Groups default untracked (D-03); batched single-writer flush (gate 7); 30-day sweep at 03:30 (D-14).
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation/20-05-SUMMARY.md`
</output>
