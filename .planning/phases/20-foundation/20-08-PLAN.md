---
phase: 20-foundation
plan: 08
type: execute
wave: 7
depends_on: [04, 05, 06, 07]
files_modified:
  - tests/unit/main/whatsapp/whatsapp-degradable.spec.ts
autonomous: false
requirements: [WA-01, WA-02, WA-12]

must_haves:
  truths:
    - "isMailCalendarAccount({providerKey:'whatsapp'}) === false (WhatsApp is never routed through SyncOrchestrator)"
    - "Briefing / email / calendar / tasks IPC succeed while WhatsApp status='degraded' and the socket is down"
    - "A socket-startup throw does not reject app boot"
    - "Live: a real secondary-number link completes, the AccountRow shows connected + number, a tracked-group message appears in whatsapp_message within 60s, and the consent copy reads clearly with QR gated behind the ack"
  artifacts:
    - path: "tests/unit/main/whatsapp/whatsapp-degradable.spec.ts"
      provides: "WA-12 degradable integration assertions"
      contains: "isMailCalendarAccount"
  key_links:
    - from: "sync-orchestrator isMailCalendarAccount"
      to: "whatsapp exclusion"
      via: "providerKey check"
      pattern: "whatsapp"
# Decision coverage:
# WA-12 degradable proven by integration; WA-01/WA-02 closed by live UAT (manual-only per VALIDATION.md)
---

<objective>
Close the phase: prove WA-12 (degradable capability) by automated integration test, then run the manual-only live verifications (real QR link + consent-copy review) that cannot run in CI.

Purpose: WA-12 is the safety property that makes WhatsApp's unofficial-protocol flakiness acceptable — the rest of Aria must keep working when the socket is down. The live link (WA-01) and the consent-copy/secondary-number review (WA-02) are flagged manual-only in VALIDATION.md because they require a real phone + subjective UX judgement.
Output: whatsapp-degradable.spec green + a signed-off live UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation/20-VALIDATION.md
@src/main/integrations/sync-orchestrator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: WA-12 degradable integration test</name>
  <read_first>
    - src/main/integrations/sync-orchestrator.ts (lines 57-61 — isMailCalendarAccount already excludes whatsapp; assert it)
    - .planning/phases/20-foundation/20-VALIDATION.md R-WA12 + .planning/phases/20-foundation/20-RESEARCH.md WA-12 row
    - src/main/whatsapp/session-manager.ts (the boot-safe start() wrapper from Plan 20-04)
  </read_first>
  <action>
    Create tests/unit/main/whatsapp/whatsapp-degradable.spec.ts: (1) assert `isMailCalendarAccount({ providerKey: 'whatsapp', ... })` returns false (no SyncOrchestrator routing). (2) With a whatsapp provider_account row at status='degraded' and no live socket, assert representative briefing / email / calendar / tasks IPC handlers still return success (degraded WhatsApp does not break them). (3) Simulate a socket-startup throw in manager.start() and assert it is caught (resolves/no-throw) — boot is not rejected. Run `npm run typecheck`.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/unit/main/whatsapp/whatsapp-degradable.spec.ts` passes all three assertions.
    - `npm run typecheck` clean; full `pnpm test` green; all 3 static ratchets + handler-count test green (phase gate).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/unit/main/whatsapp/whatsapp-degradable.spec.ts</automated>
  </verify>
  <done>WA-12 degradable proven: whatsapp excluded from SyncOrchestrator, other surfaces unaffected, boot-safe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Live WhatsApp link + consent-copy + privacy spot-check + disconnect (WA-01/02/04/06)</name>
  <action>Pause for the user to run the live verification steps below against the running app with a real secondary WhatsApp number; resume on "approved" or route issues per the resume-signal.</action>
  <what-built>
    The full WhatsApp foundation: consent gate, QR linking, group selection + ingestion, AccountRow status, disconnect cascade, 30-day retention. All automated specs + 3 static ratchets + handler-count are green. The two items below cannot run in CI (real phone + subjective copy review).
  </what-built>
  <how-to-verify>
    1. Launch the app (`pnpm dev` — first `Remove-Item Env:\ELECTRON_RUN_AS_NODE` per MEMORY; run `pnpm rebuild:native:electron` if native ABI complains).
    2. Settings → Integrations → Add account → WhatsApp. Confirm the consent modal shows 3-4 ban-risk bullets and an emphasized secondary-number callout, and that the "Show QR code" button is DISABLED until you check the acknowledgement box (WA-02 / SC-1).
    3. Acknowledge → Show QR. Scan with a SECONDARY WhatsApp number (recommended — ban risk). Confirm linking completes, the AccountRow shows "connected"/ok with the phone number (WA-01), and the one-sentence "no history before this moment" notice appears (SC-2).
    4. Open "Manage groups" → toggle one group tracked. Send a message in that group from another phone. Confirm it appears in whatsapp_message within ~60s (inspect via a diagnostics query or the Phase-21-precursor). Confirm a message in an UNtracked group and a 1:1 DM produce NO rows (WA-06).
    5. Confirm the briefing / email / calendar / tasks screens still work normally (WA-12).
    6. Disconnect WhatsApp via the AccountRow → confirm dialog → confirm all WhatsApp data is gone (WA-04).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (BLOCKER → loop back to a gap-closure plan; MINOR → backlog).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WhatsApp socket health → rest of app | A degraded/down socket must not degrade unrelated surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-28 | DoS (to whole app) | a down/degraded WhatsApp socket breaks briefing/email/calendar/tasks | mitigate | isMailCalendarAccount excludes whatsapp; degradable integration test proves other IPC succeed at status='degraded'; boot-safe start() (WA-12). |
| T-20-29 | Repudiation / DoS-to-user (ban) | live link uses the primary number | accept (with control) | UAT instruction recommends a SECONDARY number; consent gate surfaces the risk before QR (WA-02). |
</threat_model>

<verification>
- `npx vitest run tests/unit/main/whatsapp/whatsapp-degradable.spec.ts` green (WA-12).
- Phase gate: full `pnpm test` green + 3 static ratchets (passive-posture, no-frontier, no-bare-cron) + handler-count test green + `npm run typecheck` clean.
- Live UAT signed off (WA-01/WA-02 + the WA-06 privacy spot-check + WA-04 disconnect).
</verification>

<success_criteria>
- WA-12 degradable proven by automated integration test.
- Live link + consent-copy review + privacy spot-check + disconnect verified by the user (WA-01/02/04/06).
- Phase 20 gate met: all specs + ratchets + handler-count + typecheck green.
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation/20-08-SUMMARY.md` and the UAT result in `.planning/phases/20-foundation/20-UAT.md`.
</output>
