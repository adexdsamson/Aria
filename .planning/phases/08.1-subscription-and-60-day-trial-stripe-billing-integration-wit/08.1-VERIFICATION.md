---
phase: 08.1-subscription-and-60-day-trial-stripe-billing-integration-wit
verified: 2026-05-20T10:35:00Z
status: human_needed
score: 9/9 must-haves verified (live UAT pending)
overrides_applied: 0
human_verification:
  - test: "Trial start on fresh install"
    expected: "POST /v1/trial/start succeeds, signed JWT written to entitlement row, Settings → Subscription shows 'Trial · 60 days left'"
    why_human: "Requires live Cloudflare Worker hit; mocked in unit tests, never exercised end-to-end in CI"
  - test: "Day-59 banner + Stripe Checkout open"
    expected: "Banner reads 'N days left.' in urgent tone; Subscribe opens Stripe Checkout in system browser"
    why_human: "Requires ARIA_STRIPE_CHECKOUT_URL baked at release time (currently returns 'no-checkout-url' — KNOWN STUB, by design)"
  - test: "Trial-locked paywall + paste activation"
    expected: "At 61d post-trial-start, write routes render PaywallScreen; settings/briefing reachable; paste valid ARIA-… key transitions UI within 1 frame"
    why_human: "Real Cloudflare Worker /v1/license/activate exercise required; E2E spec exists but skipped (NO_HARNESS — needs test public-key override + __e2eSeedEntitlement IPC hook + MSW license server)"
  - test: "aria:// deep-link activation"
    expected: "Click aria://activate?key=ARIA-… email link; PaywallScreen disappears within 2s via push event"
    why_human: "Requires installed/packaged build with registered URL scheme; E2E spec exists but skipped (NO_HARNESS)"
  - test: "Customer Portal + 14d grace transition to pro-locked"
    expected: "Manage subscription opens portal; after 14d failed refreshes, state goes pro-locked and next nav renders 'We couldn't verify your subscription' paywall"
    why_human: "Requires real Stripe customer + multi-day calendar walk; the state-machine transition is unit-tested, the live integration is not"
---

# Phase 08.1: Subscription + 60-day Trial — Verification Report

**Phase Goal:** Ship a 60-day no-card trial → hard cutoff with read-only escape → Stripe subscription via license-key activation, with the assertEntitled chokepoint mirroring the assertApproved shape from Phases 3/4/6.

**Verified:** 2026-05-20T10:35:00Z
**Status:** human_needed (code complete; live UAT outstanding — same pattern as Phase 8)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Migration 131 creates `entitlement` + `entitlement_audit` and is registered in `embedded.ts` | VERIFIED | `src/main/db/migrations/embedded.ts` lines 1291–1325 register version 131 with CREATE TABLE entitlement (singleton id=1) + entitlement_audit + idx_entitlement_audit_ts + `PRAGMA user_version = 131`. File `src/main/db/migrations/131_entitlement.sql` also exists on disk. |
| 2 | `assertEntitled` is the FIRST guard on all 5 write surfaces | VERIFIED | `send.ts:145` (email_send) precedes assertApproved; `write-event.ts:93` (calendar_change) precedes assertApproved; `push-actions.ts:32` (task_push) precedes assertApproved; `generate.ts:352` (briefing_generate) is first non-comment statement of `runBriefing`; `ask.ts:101` (rag_ask) is first non-comment statement of the IPC handler (wrapped in try/catch returning typed `entitlement-*` IpcError). |
| 3 | Static-grep ratchet exists and is green | VERIFIED | `tests/static/single-entitlement-gate-site.test.ts` — 7/7 PASS. Confirms 5 sites + no extra assertEntitled callers in src/main + no client-side `tier:'pro'` literal. |
| 4 | Read-only paths are NOT gated | VERIFIED | `routes.tsx` lines 26–43 define `READ_ONLY_ALLOW_LIST`. `LockedGuard` (line 56) renders `<PaywallScreen/>` ONLY for routes not in the allow-list. `/settings`, `/briefing`, `/inbox`, `/calendar` (read views), `/insights`, `/recap`, `/learning`, `/tasks`, `/ask` etc. allow-listed per matrix in 08.1-03-SUMMARY. **Important**: read-only IPC channels (e.g., briefing-read-cached, gmail-list, settings) are not in the 5-site list — guarded only at write surfaces, as designed. |
| 5 | Trial-start, 24h cron, deep-link, Ed25519 JWT verify all wired | VERIFIED | `EntitlementService` (`service.ts`) constructed in `index.ts:275` after migrations. `entitlement-refresh` cron key in `schedule.ts:18` registered via `scheduler.cronRegistry`. `aria://` registered via `app.setAsDefaultProtocolClient('aria')` in `single-instance.ts:47` + `package.json:137` build.protocols entry. `handleActivateDeepLink` called in `index.ts:308` with `emitStateChanged` push. `jose@^6.2.3` imported in `jwt-verify.ts:16`; `ENTITLEMENT_PUBLIC_KEY_HEX` matches 08.1-01 (`67c9a785…6a37f6`) at `jwt-verify.ts:23-24`; `PROD_URL` is `https://aria-license-server.adexdsamson.workers.dev`. |
| 6 | Paywall + TrialBanner + SubscriptionSection reachable | VERIFIED | `App.tsx:82` wraps tree in `<EntitlementProvider>` and renders `<TrialBanner/>` at line 95. `routes.tsx:13` imports `PaywallScreen`; `SettingsScreen.tsx:29` imports `SubscriptionSection` and registers `<Route path="subscription">` at line 108. Reachability ratchet `tests/static/settings-reachability.test.ts` PASS (5/5). |
| 7 | LockedGuard enforces read-only escape | VERIFIED | `routes.tsx:56-65` — LockedGuard reads useEntitlement, returns PaywallScreen unless route is in READ_ONLY_ALLOW_LIST. All write routes (`/approvals`, `/scheduling`, `/tasks`, `/meetings`, etc.) wrapped. `locked-guard.test.tsx` — PASS (2 cases: paywall on `/scheduling`, allow on `/settings/subscription`). |
| 8 | Entitlement test suite passes | VERIFIED | 121/121 PASS across `src/main/entitlement/*.test.ts`, `src/renderer/features/entitlement/*.test.tsx`, `SubscriptionSection.test.tsx`, `entitlement-actions.test.ts`, `locked-guard.test.tsx`, `settings-reachability.test.ts`, `entitlement-boot-order.test.ts`, `enforcement.test.ts`. (One transient 30s timeout on the googleapis-heavy enforcement test when run with full sweep; passes 4/4 in isolation — documented as known flake in 08.1-02 SUMMARY deviation #5.) |
| 9 | `pnpm lint:guard` PASS | VERIFIED | All 5 guard scripts return OK: prose-no-raw, verify-audit-view, no-network-from-signals, migration-callsite, no-fixture-leak. |

**Score:** 9 / 9 must-haves verified at the code layer.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/main/db/migrations/131_entitlement.sql` + embedded.ts entry | Migration with entitlement + entitlement_audit | VERIFIED | Both present; SQL identical between standalone and embedded. |
| `src/main/entitlement/gate.ts` | assertEntitled chokepoint | VERIFIED | Exports `assertEntitled(db, action)` + `EntitlementError`. JWT re-verified every call (security invariant 1). |
| `src/main/entitlement/jwt-verify.ts` | Ed25519 verifier via jose | VERIFIED | Uses jose's `jwtVerify` + `importJWK`; embedded pubkey hex matches 08.1-01. |
| `src/main/entitlement/service.ts` | bootstrap / refresh / activate | VERIFIED | Wired in `index.ts:275`. |
| `src/main/entitlement/schedule.ts` | 24h cron registration | VERIFIED | cronRegistry key `entitlement-refresh`. |
| `src/main/entitlement/deep-link.ts` | `aria://activate?key=…` parser | VERIFIED | Called from `index.ts:308` with `emitStateChanged` (Rule-3 fix in 08.1-03 commit 6320dc3). |
| `src/main/entitlement/install-id.ts` | safeStorage UUIDv4 install_id | VERIFIED | Linux basic_text fallback documented. |
| `src/main/entitlement/license-server-client.ts` | 5-endpoint HTTP client | VERIFIED | retry + timeout + typed errors. |
| `src/main/ipc/entitlement.ts` + IPC contract entries | 5 channels + 1 push event | VERIFIED | Channels: GET_STATE, ACTIVATE, OPEN_CHECKOUT, OPEN_PORTAL, REFRESH_NOW + STATE_CHANGED event. Zod schemas in `shared/ipc-contract.ts`. |
| `tests/static/single-entitlement-gate-site.test.ts` | Static-grep ratchet | VERIFIED | 7/7 PASS. |
| `src/renderer/features/entitlement/*` | Paywall + Provider + Banner + Activate + Restore | VERIFIED | All 6 components present + tested. |
| `src/renderer/features/settings/sections/SubscriptionSection.tsx` | Settings subscription tab | VERIFIED | Registered route in SettingsScreen.tsx:108. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| Aria client | License server | LicenseServerClient → PROD_URL | WIRED | `https://aria-license-server.adexdsamson.workers.dev` baked. |
| JWT verify | Public key | ENTITLEMENT_PUBLIC_KEY_HEX | WIRED | Hex matches 08.1-01 SUMMARY; unit test asserts equality (jwt-verify.test.ts:102). |
| Renderer | Main entitlement state | ENTITLEMENT_STATE_CHANGED event | WIRED | preload `entitlementOnStateChanged` real `ipcRenderer.on` listener; EntitlementProvider subscribes once. |
| Deep-link activate | Renderer refresh | emitStateChanged callback in index.ts:315 | WIRED | Fix-up commit 6320dc3 closed the latent 08.1-02 gap. |
| Routes | Entitlement state | LockedGuard via useEntitlement | WIRED | All write routes guarded; allow-list explicit. |

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|---|---|---|---|
| `src/main/ipc/entitlement.ts` (OPEN_CHECKOUT handler) | Returns `{ok:false, error:'no-checkout-url'}` until ARIA_STRIPE_CHECKOUT_URL is baked | Info (documented stub) | Phase 08.1 cannot drive a real Stripe Checkout from inside Aria yet. This is GA-blocking but explicitly deferred per CONTEXT.md "bake at release time" decision. |
| `src/renderer/lib/entitlement-actions.ts` `signOutLicense` | Placeholder helper — points users to Customer Portal | Info (documented stub) | Local revocation handled server-side via Stripe Customer Portal; acceptable for v1. |
| `RestoreLicenseSection.tsx` | Help URL `https://aria.app/help/restore` is placeholder | Info | Cosmetic; operator confirms real URL pre-release. |
| Renderer pre-existing TS errors | RecapScreen.tsx:45, SchedulingRulesSection.tsx:437 | Info | OUT OF SCOPE per user instruction — predate this phase, tracked separately. |

No BLOCKER or WARNING anti-patterns introduced by this phase. No unreferenced TBD/FIXME/XXX markers found in modified files.

### Requirements Coverage

Phase 08.1 was inserted via `/gsd mvp-phase` and is not enumerated in `.planning/REQUIREMENTS.md` (which closed at Phase 8). The phase-internal contract item XCUT-08 ("single-entitlement-gate-site ratchet") is SATISFIED by `tests/static/single-entitlement-gate-site.test.ts`. The 5-surface coverage is documented in 08.1-02 SUMMARY table and verified above.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Static-grep ratchet | `pnpm vitest run tests/static/single-entitlement-gate-site.test.ts` | 7/7 tests, 6.48s | PASS |
| Settings reachability + LockedGuard | `pnpm vitest run tests/static/settings-reachability.test.ts src/renderer/app/locked-guard.test.tsx` | 11/11 tests | PASS |
| Full entitlement suite (isolated enforcement) | `pnpm vitest run tests/unit/main/entitlement/enforcement.test.ts` | 4/4 tests | PASS |
| Lint guards | `pnpm lint:guard` | All 5 OK | PASS |
| Cross-phase regression suite (entitlement + render) | `pnpm vitest run src/main/entitlement … (8 globs)` | 120/121 (1 timeout flake on googleapis import; passes in isolation) | PASS |

### Probe Execution

No phase-declared probes (Phase 08.1 is not a migration/tooling phase; it uses vitest static + unit + integration tests as its "probes," all run above).

### Human Verification Required

See frontmatter `human_verification` — 5 live UAT scenarios from 08.1-03-SUMMARY § "Live UAT script":

1. **Trial start** — fresh install hits real Cloudflare Worker; Settings shows "Trial · 60 days left".
2. **Day-59 banner + Stripe Checkout** — system clock walk + Subscribe opens browser. **Requires `ARIA_STRIPE_CHECKOUT_URL` bake** before this works end-to-end.
3. **Trial-locked paywall + paste activation** — 61d clock walk + valid ARIA-… key paste.
4. **Deep-link activation** — packaged build + click `aria://activate?key=…` from email.
5. **Customer Portal + 14d grace** — real Stripe customer + multi-day refresh-fail walk to `pro-locked`.

Two E2E specs (`tests/e2e/paywall-block-write-actions.spec.ts`, `tests/e2e/activate-via-deep-link.spec.ts`) exist as `test.skip` with explicit `NO_HARNESS` rationale documented in 08.1-03-SUMMARY; closing them is a separate plan (test public-key override + `__e2eSeedEntitlement` IPC + MSW license server fixture).

### Gaps Summary

**Zero code-layer gaps.** All 9 must-haves verified. All artifacts exist, are substantive, are wired, and have flowing data paths. Static-grep ratchet enforces the 5-surface invariant. Lint guards green. Test suite green (1 known timeout flake, passes in isolation).

**GA-blocking surfaces (outside phase scope, expected):**

1. **`ARIA_STRIPE_CHECKOUT_URL` not baked** — by design; the env var is the only missing piece in `ENTITLEMENT_OPEN_CHECKOUT`. Without it, scenario 2 of the live UAT cannot fire Stripe Checkout from inside Aria.
2. **Live UAT not yet run** — same pattern as Phase 8: SCs deferred to live release verification. Operator must walk all 5 UAT scenarios against the deployed Worker before declaring Phase 08.1 done-done.
3. **Apple / Windows EV code signing** — already deferred to Phase 8 release-prep per memory note `project_aria_phase8_context`; not a Phase 08.1 concern.
4. **Two E2E `test.skip`** — operator-gated; closing them is a separate plan (NO_HARNESS rationale is sound).
5. **Real `ENTITLEMENT_PUBLIC_KEY_HEX`** — already present and verified against 08.1-01 production deployment.

## Overall Verdict

**PASS at the code layer. HUMAN_NEEDED for live UAT.**

Phase 08.1 has delivered the security spine: a 5-surface chokepoint that mirrors the well-tested `assertApproved` shape, enforced by a static-grep ratchet, backed by JWT re-verification on every call (the SQLCipher tamperer is defeated). The renderer surfaces the gate as PaywallScreen for locked states, TrialBanner for the day-50/55/59/grace countdown, and SubscriptionSection for management — all reachable via App.tsx + routes.tsx + SettingsScreen.tsx with the read-only allow-list as single source of truth.

The phase goal — "60-day no-card trial → hard cutoff with read-only escape → Stripe subscription via license-key activation" — is achieved in code. Live release verification (UAT items 1–5) is the only remaining work, and that follows the same defer pattern Phase 8 used for its packaged-build SCs.

---

_Verified: 2026-05-20T10:35:00Z_
_Verifier: Claude (gsd-verifier)_
