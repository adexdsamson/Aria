---
phase: 15-audio-i-o-model-runtime
plan: 07
type: execute
wave: 3
depends_on: [15-01, 15-04, 15-06]
files_modified:
  - src/renderer/features/voice/VoiceStatusDot.tsx
  - src/renderer/features/voice/VoiceStatusDot.spec.tsx
  - src/renderer/features/voice/VoiceHUDBand.tsx
  - src/renderer/features/voice/VoiceHUDBand.spec.tsx
  - src/renderer/features/voice/VoicePTTButton.tsx
  - src/renderer/features/voice/VoicePTTButton.spec.tsx
  - src/renderer/app/App.tsx
  - src/renderer/components/Topbar.tsx
autonomous: true
requirements: [VOICE-01, VOICE-07]
user_setup: []

must_haves:
  truths:
    - "A persistent VoiceStatusDot in the Topbar right cluster encodes every voice state via the existing StatusDot color vocabulary — no new design tokens (D-14)"
    - "A transient VoiceHUDBand mounts in the App shell at the TrialBanner structural slot (in-flow, not a floating overlay) and shows live per-utterance transcription with role=status aria-live=polite aria-atomic=false (D-15)"
    - "PTT supports hold-to-talk (DOM keydown/keyup, keyup=hard turn-end) AND click-toggle on the same control, with NO globalShortcut (D-10); VAD dual-role config switches by mode (D-11)"
    - "prefers-reduced-motion replaces pulse/spinner/expansion transitions with instant toggle + static fill; state stays encoded in color + mono label (D-16)"
    - "PTT start is visually blocked (muted) while Aria is speaking, reflecting the micGated gate (D-13/VOICE-07)"
    - "D-12: push-to-talk is focused-window-only — no globalShortcut and no uiohook-napi dependency this phase (VoicePTTButton uses DOM keydown/keyup + click only; the no-globalShortcut/no-uiohook source guard is asserted in Task 2's acceptance criteria); global/tray activation + wake-word are deferred to Phase 18"
  artifacts:
    - path: "src/renderer/features/voice/VoiceStatusDot.tsx"
      provides: "Topbar mic state dot mapping VoiceState→StatusDotKind (D-14)"
      contains: "StatusDot"
    - path: "src/renderer/features/voice/VoiceHUDBand.tsx"
      provides: "in-flow live-transcription HUD at the TrialBanner slot (D-15)"
      contains: "aria-live"
    - path: "src/renderer/features/voice/VoicePTTButton.tsx"
      provides: "hold + click-toggle PTT, no globalShortcut, VAD mode switch (D-10/D-11)"
      contains: "keyup"
  key_links:
    - from: "src/renderer/app/App.tsx"
      to: "VoiceHUDBand"
      via: "in-flow mount adjacent to TrialBanner"
      pattern: "VoiceHUDBand"
    - from: "src/renderer/components/Topbar.tsx"
      to: "VoiceStatusDot + PTT compact icon"
      via: "right-cluster insertion (⌘K → bell → PTT → dot → AvatarMenu)"
      pattern: "VoiceStatusDot"
    - from: "VoicePTTButton"
      to: "useVoiceSession.startTurn / setVadMode"
      via: "keydown/keyup + click handlers"
      pattern: "startTurn|setVadMode"
---

<objective>
Build the mic-state visible surface — the primary visible deliverable of this phase (CONTEXT §Specifics)
— and wire it into the App shell and Topbar. VoiceStatusDot encodes every voice state in the existing
editorial color vocabulary (D-14, no new tokens); VoiceHUDBand is the in-flow live-transcription band at
the TrialBanner slot (D-15); VoicePTTButton is the hold + click-toggle control (D-10) with VAD dual-role
mode switching (D-11). All three honor prefers-reduced-motion (D-16) and reflect the micGated gate (D-13).

Purpose: VOICE-01 ("sees their words transcribed live on screen") and VOICE-07 ("mic state always
visible") are satisfied here. Hold it to Aria's editorial bar — calm ivory/ink/gold, Plex Mono labels,
NOT a flashy consumer-assistant orb (CONTEXT §Specifics). Output: the three components, their specs, and
the App.tsx + Topbar.tsx wiring.

Depends on Plan 15-06 (the useVoiceSession store + micGated) for state, Plan 15-04 (useMicCapture) for
the capture the PTT button drives, and Plan 15-01 (VoiceState contract). The download UI + onboarding +
disabled-PTT affordance is Plan 15-08 (separate file ownership). This plan owns the live surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-audio-i-o-model-runtime/15-PATTERNS.md
@.planning/phases/15-audio-i-o-model-runtime/15-UI-SPEC.md

<interfaces>
<!-- Editorial primitives + shell slots to mirror. -->
From src/renderer/components/editorial/StatusDot.tsx (the 4-color vocabulary to map onto — D-14, NO new tokens):
```typescript
const KIND_TO_COLOR: Record<StatusDotKind, string> = {
  ok: 'var(--moss)',   // → speaking
  warn: 'var(--gold)', // → listening (pulse) / processing (spinner arc)
  err: 'var(--rose)',  // → error
  idle: 'var(--gray-faint)', // → idle / muted-during-playback
};
```

From src/renderer/app/App.tsx:106 — <TrialBanner/> mounts in-flow, first child of <main>, above <Topbar/>. Mount <VoiceHUDBand/> immediately before <TrialBanner/> (UI-SPEC §Layout Integration).
From src/renderer/components/Topbar.tsx — right cluster order ⌘K(L122) → bell(L146) → AvatarMenu(L177). New order: ⌘K → bell → PTT compact(28px) → VoiceStatusDot(6px) → AvatarMenu (UI-SPEC §Topbar).
From src/renderer/app/App.tsx:133-136 — the prefers-reduced-motion precedent (.gate-logo/.gate-bar) to mirror for D-16.
From src/renderer/features/voice/useVoiceSession.ts (Plan 15-06): state, micGated, startTurn(), setVadMode('hold'|'toggle'), stopTurn().
UI-SPEC testids: aria-topbar-voice-dot, voice-hud-band, voice-hud-state-label, voice-hud-transcript, voice-ptt-button, aria-topbar-ptt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VoiceStatusDot + VoiceHUDBand (state + live transcription surface)</name>
  <files>src/renderer/features/voice/VoiceStatusDot.tsx, src/renderer/features/voice/VoiceStatusDot.spec.tsx, src/renderer/features/voice/VoiceHUDBand.tsx, src/renderer/features/voice/VoiceHUDBand.spec.tsx</files>
  <behavior>
    - VoiceStatusDot maps VoiceState→StatusDotKind per UI-SPEC: idle→idle, listening→warn(pulse), processing→warn(spinner arc), speaking→ok, muted-during-playback→idle+struck-mic, error→err; aria-label="Microphone: {STATE}"; testid aria-topbar-voice-dot
    - VoiceHUDBand mounts unconditionally, collapsed (grid-template-rows 0fr) when idle, expanded (1fr) when active; role=status aria-live=polite aria-atomic=false; renders the mono uppercase state label + per-utterance transcript text; testids voice-hud-band / voice-hud-state-label / voice-hud-transcript
    - both: under prefers-reduced-motion, no keyframe/transition animation — static fill + instant toggle (D-16); state still encoded via color + mono label
    - specs use React Testing Library, drive state via the store (or props), assert the kind mapping, the aria contract, and the collapsed/expanded class/style
  </behavior>
  <action>
    Create VoiceStatusDot.tsx wrapping the existing StatusDot editorial primitive (D-14 — NO new tokens; map
    onto ok/warn/err/idle per UI-SPEC §State-to-color). Add the listening slow-pulse (2s), processing spinner-arc
    (10×10 SVG, 700ms), and muted struck-mic overlay; suppress all under prefers-reduced-motion (mirror
    App.tsx:133-136). aria-label="Microphone: {STATE}" on the wrapper; announce via a visually-hidden aria-live
    sibling, NOT the dot (avoid announcing pulse ticks). Create VoiceHUDBand.tsx mirroring TrialBanner.tsx
    structure + role=status; use grid-template-rows 0fr→1fr expansion (UI-SPEC resolved discretion), overflow
    hidden inner wrapper, 200ms cubic-bezier transition, instant under reduced-motion. Render per-utterance
    transcript (honest model — no word-by-word partials; "Listening…"/"Transcribing…"/utterance/"Speaking…"/
    "Mic muted during playback"/error copy from UI-SPEC §Copywriting). aria-live=polite aria-atomic=false. Use
    only existing tokens (--ivory/--paper/--gold/--moss/--rose/--gray-faint/--rule/--f-mono). Render transcript as
    plain text content (never HTML — transcript injection mitigation). Run npm run typecheck.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/features/voice/VoiceStatusDot.spec.tsx src/renderer/features/voice/VoiceHUDBand.spec.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - source: VoiceStatusDot maps all 6 voice states to ok/warn/err/idle StatusDotKind; no new CSS custom properties introduced
    - source: VoiceHUDBand has role=status aria-live=polite aria-atomic=false and grid-template-rows 0fr/1fr collapse/expand; mounts unconditionally
    - source: both suppress animation under @media (prefers-reduced-motion: reduce)
    - test-command: `npx vitest run src/renderer/features/voice/VoiceStatusDot.spec.tsx -x` passes (state→kind mapping, aria-label)
    - test-command: `npx vitest run src/renderer/features/voice/VoiceHUDBand.spec.tsx -x` passes (aria contract, collapsed/expanded, per-state copy, plain-text transcript)
    - test-command: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The mic-state dot + live-transcription HUD render every state in the editorial vocabulary with the D-15 aria contract and D-16 reduced-motion fallback (VOICE-01/VOICE-07); specs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VoicePTTButton — hold + click-toggle, VAD mode switch, no globalShortcut</name>
  <files>src/renderer/features/voice/VoicePTTButton.tsx, src/renderer/features/voice/VoicePTTButton.spec.tsx</files>
  <behavior>
    - keydown Space (when focus is not in an input/textarea) starts a hold turn (setVadMode('hold') + startTurn); keyup Space ends the turn (hard turn-end per D-10)
    - clicking the button toggles a turn (setVadMode('toggle') + startTurn / stopTurn) for hands-free dictation (D-10)
    - while micGated/speaking the button is visually muted and PTT-start is rejected (D-13) with tooltip "Aria is speaking"
    - NO globalShortcut / uiohook-napi — purely DOM keydown/keyup on the focused window (D-10/D-12)
    - renders the hint label "Space · Click to toggle" and aria-label "Push to talk — hold Space or click to toggle"; testid voice-ptt-button
    - spec drives keydown/keyup + click against a mocked store and asserts the hold vs toggle paths + the speaking-block
  </behavior>
  <action>
    Create VoicePTTButton.tsx. Implement DOM keydown/keyup hold-to-talk (keyup = hard turn-end) PLUS click-toggle
    on the same control (D-10). Switch VAD role via the store's setVadMode('hold'|'toggle') (D-11 — the threshold
    config lives in the capture/VAD layer; this button only selects the mode). Guard the Space binding: ignore
    when document.activeElement is an input/textarea (UI-SPEC §Accessibility). Reflect the micGated gate (D-13):
    when state==='speaking'/'muted-during-playback', render muted (opacity, struck mic) and reject PTT-start with
    the "Aria is speaking" tooltip. Render the "Space · Click to toggle" hint (10px mono) and the aria-label.
    Use the editorial Button/styles; 44×44 circular form per UI-SPEC. Explicitly do NOT use globalShortcut or
    uiohook-napi (D-12 — electron #26301 has no keyup). Run npm run typecheck.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/features/voice/VoicePTTButton.spec.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - source: keydown/keyup Space drives hold (setVadMode('hold')); click drives toggle (setVadMode('toggle')); the input/textarea guard is present
    - source: no import of globalShortcut or uiohook-napi anywhere in the file (D-10/D-12)
    - source: while speaking/muted the button is muted and PTT-start is rejected with the "Aria is speaking" tooltip
    - test-command: `npx vitest run src/renderer/features/voice/VoicePTTButton.spec.tsx -x` passes (hold path, toggle path, speaking-block, focus guard)
    - test-command: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>PTT supports hold + click-toggle via DOM events only (no globalShortcut), switches VAD mode, and respects the half-duplex gate (D-10/D-11/D-12/D-13); spec green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire VoiceHUDBand + VoiceStatusDot + PTT into App shell and Topbar</name>
  <files>src/renderer/app/App.tsx, src/renderer/components/Topbar.tsx</files>
  <read_first>
    - src/renderer/app/App.tsx (the unlocked shell layout; the <TrialBanner/> mount at ~L106 and the AppShellNavigateListener push pattern at ~L201-218)
    - src/renderer/components/Topbar.tsx (the right-cluster flex row: ⌘K button L122, bell L146, AvatarMenu L177)
    - .planning/phases/15-audio-i-o-model-runtime/15-UI-SPEC.md (§ Layout Integration Points — exact placement + the 8px sm-token voice-pair gap)
  </read_first>
  <action>
    In src/renderer/app/App.tsx (unlocked path), mount <VoiceHUDBand/> in-flow immediately before <TrialBanner/>
    (both above <Topbar/>) per UI-SPEC §Layout — NOT a floating overlay (no z-index conflict with
    ToastHost/CommandPalette). Voice errors continue to route through the existing ToastHost (no new z-index
    layer). In src/renderer/components/Topbar.tsx, insert into the right cluster the order ⌘K → bell →
    VoicePTTCompact (28px icon-only, testid aria-topbar-ptt) → VoiceStatusDot (6px, testid aria-topbar-voice-dot)
    → AvatarMenu, wrapping the voice pair in a flex span with an 8px (sm-token) gap (UI-SPEC §Topbar). The compact
    PTT icon expands/starts the HUD session. Keep Topbar padding unchanged. Do not regress existing right-cluster
    elements. Run npm run typecheck.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: App.tsx renders <VoiceHUDBand/> in-flow adjacent to <TrialBanner/> (not a portal/overlay)
    - source: Topbar.tsx right cluster order is ⌘K → bell → PTT compact → VoiceStatusDot → AvatarMenu with an 8px gap span
    - source: existing ⌘K/bell/AvatarMenu elements remain present and unbroken
    - test-command: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The HUD band and Topbar voice cluster are wired into the shell at the D-15/D-14 slots; existing chrome intact; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript text → DOM | transcript is user-derived and rendered in the HUD live region |
| keyboard events → app | Space PTT binding must not hijack typing in inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-21 | Tampering | transcript injection into HUD | mitigate | transcript rendered as plain text content (React text node), never dangerouslySetInnerHTML; aria-live content is text-only |
| T-15-22 | Denial of Service | Space-key hijack of text inputs | mitigate | the PTT keydown handler ignores events when activeElement is an input/textarea (UI-SPEC §Accessibility focus guard) |
| T-15-23 | Elevation of Privilege | voice UI bypassing the gate | mitigate | the button rejects PTT-start while micGated/speaking; the gate authority lives in useVoiceSession (Plan 15-06), the UI only reflects it |
</threat_model>

<verification>
- `npx vitest run src/renderer/features/voice/VoiceStatusDot.spec.tsx src/renderer/features/voice/VoiceHUDBand.spec.tsx src/renderer/features/voice/VoicePTTButton.spec.tsx -x` green.
- `npm run typecheck` clean after App.tsx + Topbar.tsx wiring.
</verification>

<success_criteria>
- Mic state is always visible via the Topbar VoiceStatusDot (D-14, VOICE-07); the in-flow VoiceHUDBand shows live per-utterance transcription (D-15, VOICE-01).
- PTT supports hold + click-toggle with no globalShortcut and reflects the half-duplex gate (D-10/D-13).
- prefers-reduced-motion fallback present (D-16); no new design tokens; existing chrome intact.
</success_criteria>

<output>
After completion, create `.planning/phases/15-audio-i-o-model-runtime/15-07-SUMMARY.md`
</output>
</content>
