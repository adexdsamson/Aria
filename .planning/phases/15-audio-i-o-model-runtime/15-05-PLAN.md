---
phase: 15-audio-i-o-model-runtime
plan: 05
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - src/main/ipc/voice.ts
  - src/main/ipc/voice.spec.ts
  - src/main/index.ts
autonomous: true
requirements: [VOICE-01, VOICE-04]
user_setup: []

must_haves:
  truths:
    - "registerVoiceHandlers wires VOICE_FEED_AUDIO (renderer→main PCM), VOICE_GET_MODEL_STATUS, VOICE_DOWNLOAD_MODEL, VOICE_CANCEL_TTS exactly like registerEntitlementHandlers (D-01 pipeline)"
    - "feedAudio routes PCM into the SttSidecarManager and pushes VOICE_TRANSCRIPT_DELTA + VOICE_STATE_CHANGED back via emitToRenderer (VOICE-01 live transcription)"
    - "Handlers tolerate db === null pre-unlock (model status defaults gracefully) and re-register post-unlock per the IPC db-null skip-trap pattern"
    - "Sidecar suspend/resume and download pause/resume are registered with powerMonitor at this wiring layer (D-03/D-09)"
    - "src/main/ipc/voice.ts never reaches the write chokepoints (Phase 15 has no write paths; Ratchet B covers all of src/main)"
  artifacts:
    - path: "src/main/ipc/voice.ts"
      provides: "IPC handlers + push wiring for the voice pipeline (analog: ipc/entitlement.ts)"
      contains: "registerVoiceHandlers"
  key_links:
    - from: "src/main/ipc/voice.ts VOICE_FEED_AUDIO handler"
      to: "SttSidecarManager.transcribe"
      via: "in-process service call"
      pattern: "transcribe"
    - from: "src/main/ipc/voice.ts"
      to: "renderer (VOICE_TRANSCRIPT_DELTA / VOICE_STATE_CHANGED)"
      via: "deps.emitToRenderer"
      pattern: "emitToRenderer"
    - from: "src/main/index.ts bootstrap"
      to: "registerVoiceHandlers + powerMonitor lifecycle"
      via: "registration call wired to mainWindow emitter"
      pattern: "registerVoiceHandlers"
---

<objective>
Wire the main-process voice IPC surface: registerVoiceHandlers connects the renderer capture path
(VOICE_FEED_AUDIO PCM frames) to the SttSidecarManager (Plan 15-02), pushes transcript + state events
back to the renderer (VOICE_TRANSCRIPT_DELTA / VOICE_STATE_CHANGED), exposes model status + download
control (Plan 15-03), and registers the sidecar + download powerMonitor lifecycle at boot (D-03/D-09).

Purpose: this is the seam that makes VOICE-01 live transcription real end-to-end on the main side, and
the in-process service-call discipline (handler → SttSidecarManager, never re-crossing preload) sets up
Phase 17's "same in-process service" requirement. It mirrors registerEntitlementHandlers exactly.
Output: src/main/ipc/voice.ts + its spec + the bootstrap registration in src/main/index.ts.

This plan owns the powerMonitor registration calls for both the sidecar (15-02 exposed pause/resume) and
the download manager (15-03). It depends on 15-02 and 15-03 for the services it injects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-audio-i-o-model-runtime/15-RESEARCH.md
@.planning/phases/15-audio-i-o-model-runtime/15-PATTERNS.md

<interfaces>
<!-- The IPC handler shape to mirror exactly. Extracted from src/main/ipc/entitlement.ts. -->
From src/main/ipc/entitlement.ts (registerEntitlementHandlers + deps shape + push convention):
```typescript
export interface EntitlementHandlersDeps {
  logger: Logger; dbHolder: DbHolder; service: EntitlementService;
  emitToRenderer?: (channel: string, payload?: unknown) => void;
}
export function registerEntitlementHandlers(ipcMain: IpcMain, deps: EntitlementHandlersDeps): void {
  ipcMain.handle(CHANNELS.ENTITLEMENT_GET_STATE, async () => {
    try { return { ok: true, state: await service.getCurrentState() }; }
    catch (err) { return { ok: false, error: (err as Error).message }; }
  });
  // push: deps.emitToRenderer?.(CHANNELS.ENTITLEMENT_STATE_CHANGED, { state });
  // db-null guard: deps.dbHolder.db?.prepare(...)
}
```

From Plan 15-02: SttSidecarManager { transcribe(pcm): Promise<TranscriptDelta>; pause(); resume(); dispose() }
From Plan 15-03: model-download controller { start(); pause(); resume(); cancel(); disclosedSize() }
From Plan 15-01: CHANNELS.VOICE_*; src/main/voice/prefs.ts getVoiceModelStatus(db)
From src/main/lifecycle/powerMonitor.ts: registerLifecycleCallbacks({ onSuspend, onResume })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: registerVoiceHandlers — feedAudio/model-status/download/cancel + push events</name>
  <files>src/main/ipc/voice.ts, src/main/ipc/voice.spec.ts</files>
  <behavior>
    - VOICE_FEED_AUDIO handler receives a PCM ArrayBuffer, forwards it to the injected SttSidecarManager.transcribe, and on result pushes VOICE_TRANSCRIPT_DELTA + a VOICE_STATE_CHANGED transition (listening→processing→idle) via emitToRenderer
    - VOICE_GET_MODEL_STATUS returns getVoiceModelStatus(db) and tolerates db === null (returns the not-ready default, no throw)
    - VOICE_DOWNLOAD_MODEL starts the injected download controller; VOICE_CANCEL_TTS resolves ok (TTS playback lives in the renderer — this just acks/flags the gate intent for symmetry)
    - every handler returns the { ok: true, … } / { ok: false, error } envelope like entitlement handlers
    - the spec injects fake services + a recording emitToRenderer; no real binary, no real db
  </behavior>
  <action>
    Create src/main/ipc/voice.ts mirroring src/main/ipc/entitlement.ts EXACTLY. Define VoiceHandlersDeps
    { logger; dbHolder: DbHolder; sttSidecar; downloadController; emitToRenderer? } and
    registerVoiceHandlers(ipcMain, deps). Implement the four invoke handlers (VOICE_FEED_AUDIO,
    VOICE_GET_MODEL_STATUS, VOICE_DOWNLOAD_MODEL, VOICE_CANCEL_TTS) with the try/catch envelope.
    feedAudio: forward the PCM to sttSidecar.transcribe and push VOICE_TRANSCRIPT_DELTA + VOICE_STATE_CHANGED.
    model-status: getVoiceModelStatus(deps.dbHolder.db) with db-null tolerance (entitlement.ts:84 db?.prepare
    pattern). Per project memory (IPC db-null skip trap): if any handler is db-dependent and registered
    pre-unlock, keep the skip.add inside the if(db) guard and ensure re-registration post-unlock. Note: this file
    is NOT under src/main/voice/** so the staging ratchet does not fence it, but it must still never reach the
    write chokepoints (Ratchet B chokepoint-caller-allow-list covers all of src/main — do not import
    sendApprovedEmail/applyCalendarChange/pushApprovedMeetingActions). Run npm run typecheck.
  </action>
  <verify>
    <automated>npx vitest run src/main/ipc/voice.spec.ts tests/static/chokepoint-caller-allow-list.spec.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - source: registerVoiceHandlers registers all four VOICE_* invoke channels with the ok/error envelope
    - source: feedAudio calls sttSidecar.transcribe and pushes VOICE_TRANSCRIPT_DELTA + VOICE_STATE_CHANGED via deps.emitToRenderer
    - source: VOICE_GET_MODEL_STATUS tolerates db === null (returns default, no throw); no chokepoint imports
    - test-command: `npx vitest run src/main/ipc/voice.spec.ts -x` passes (feedAudio→transcribe→push, model-status db-null default, download start, cancel ack)
    - test-command: `npx vitest run tests/static/chokepoint-caller-allow-list.spec.ts -x` stays green
    - test-command: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>registerVoiceHandlers connects feedAudio→sidecar→transcript-push and exposes model status/download/cancel with db-null tolerance, mirroring entitlement handlers; chokepoint ratchet green.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap wiring — register handlers + emitter + powerMonitor lifecycle</name>
  <files>src/main/index.ts</files>
  <read_first>
    - src/main/index.ts (find where registerEntitlementHandlers is called and where makeRendererEmitter is wired to the main window; find the bootstrap/unlock sequence and the existing powerMonitor/registerLifecycleCallbacks usage)
    - src/main/ipc/voice.ts (Task 1 — the deps shape to satisfy)
    - .planning/phases/15-audio-i-o-model-runtime/15-PATTERNS.md (§ powerMonitor suspend/resume, § Main→renderer push)
  </read_first>
  <action>
    In src/main/index.ts bootstrap, construct the SttSidecarManager (Plan 15-02) and the model-download
    controller (Plan 15-03), build a makeRendererEmitter bound to the main window, and call
    registerVoiceHandlers(ipcMain, { logger, dbHolder, sttSidecar, downloadController, emitToRenderer }) alongside
    the existing registerEntitlementHandlers call. Register powerMonitor lifecycle (D-03/D-09) via
    registerLifecycleCallbacks({ onSuspend: () => { sttSidecar.pause(); downloadController.pause(); },
    onResume: () => { sttSidecar.resume(); downloadController.resume(); } }) and keep the returned unregister fn
    for teardown. Follow the IPC db-null re-registration discipline: if voice handlers are db-dependent and the
    first registration runs pre-unlock, re-register post-unlock in the same place entitlement/background prefs
    handlers do. Do NOT modify the CSP here (Plan 15-01 already added blob:). Run npm run typecheck.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: src/main/index.ts calls registerVoiceHandlers with a window-bound emitToRenderer
    - source: powerMonitor registerLifecycleCallbacks drives sttSidecar.pause/resume + downloadController.pause/resume (D-03/D-09)
    - source: voice handlers follow the same db-null/post-unlock registration discipline as entitlement handlers
    - test-command: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Voice handlers + window emitter + powerMonitor lifecycle are wired into bootstrap mirroring entitlement, with db-null re-registration discipline; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (VOICE_FEED_AUDIO) | PCM ArrayBuffer is untrusted renderer input crossing the preload bridge |
| transcript text → renderer | transcript is user-derived; pushed back over IPC for rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Tampering | VOICE_FEED_AUDIO PCM input | mitigate | the handler treats the payload as raw PCM only (length-bounded buffer passed to the WAV writer); it is never eval'd, never used as a path or shell arg — the sidecar args are app-controlled (Plan 15-02), the WAV path is generated server-side |
| T-15-15 | Tampering | transcript injection downstream | accept | transcript text is rendered as plain text content in the HUD (Plan 15-06), never as HTML; LLM-prompt injection is a Phase 16 concern (noted, out of scope here — Phase 15 has no LLM/write path) |
| T-15-16 | Elevation of Privilege | voice handler reaching write paths | mitigate | no chokepoint imports; Ratchet B (chokepoint-caller-allow-list) covers all of src/main and stays green |
| T-15-17 | Denial of Service | sidecar lifecycle on sleep | mitigate | powerMonitor onSuspend parks the sidecar + download; onResume restores them (D-03/D-09) |
</threat_model>

<verification>
- `npx vitest run src/main/ipc/voice.spec.ts tests/static/chokepoint-caller-allow-list.spec.ts tests/static/voice-routes-through-staging.spec.ts -x` green.
- `npm run typecheck` clean after the index.ts wiring.
</verification>

<success_criteria>
- feedAudio → SttSidecarManager → VOICE_TRANSCRIPT_DELTA/STATE push works end-to-end on the main side (VOICE-01).
- Model status/download/cancel handlers present with db-null tolerance; in-process service-call discipline holds.
- powerMonitor drives sidecar + download lifecycle (D-03/D-09); chokepoint ratchet green (VOICE-04).
</success_criteria>

<output>
After completion, create `.planning/phases/15-audio-i-o-model-runtime/15-05-SUMMARY.md`
</output>
