---
phase: 15-audio-i-o-model-runtime
plan: 08
type: execute
wave: 3
depends_on: [15-01, 15-03]
files_modified:
  - src/renderer/features/voice/VoiceModelDownload.tsx
  - src/renderer/features/voice/VoiceModelDownload.spec.tsx
  - src/renderer/features/onboarding/OnboardingWizard.tsx
  - src/renderer/features/onboarding/OnboardingWizard.spec.tsx
autonomous: true
requirements: [VOICE-04]
user_setup: []

must_haves:
  truths:
    - "First-run model download is a designed flow: size disclosed BEFORE download, a progress bar, pause/resume, and a graceful 'voice unavailable until ready' state — not a bare spinner (D-07/SC4)"
    - "Two entry points exist: a skippable 'Set up voice' step appended to OnboardingWizard, and a lazy first-PTT modal for users who skipped (D-07)"
    - "'Voice unavailable until ready' is a disabled PTT affordance that routes to the download flow, mirroring the TrialBanner/entitlement disabled-gate (D-08)"
    - "The download UI subscribes to VOICE_MODEL_PROGRESS and reflects readiness from the settings KV prefs (D-08)"
    - "The onboarding voice step is skippable and never blocks seal (UI-SPEC §OnboardingWizard)"
  artifacts:
    - path: "src/renderer/features/voice/VoiceModelDownload.tsx"
      provides: "download flow component — onboarding-step + lazy-modal variants with size disclosure + progress + pause/resume (D-07/SC4)"
      contains: "DOWNLOAD SIZE"
    - path: "src/renderer/features/onboarding/OnboardingWizard.tsx"
      provides: "skippable 'voice' step hosting VoiceModelDownload (D-07)"
      contains: "voice"
  key_links:
    - from: "src/renderer/features/voice/VoiceModelDownload.tsx"
      to: "VOICE_DOWNLOAD_MODEL + VOICE_MODEL_PROGRESS + VOICE_GET_MODEL_STATUS"
      via: "window.aria voice IPC + onVoiceModelProgress subscription"
      pattern: "onVoiceModelProgress"
    - from: "src/renderer/features/onboarding/OnboardingWizard.tsx"
      to: "VoiceModelDownload (onboarding-step variant)"
      via: "new 'voice' Step rendering the component"
      pattern: "VoiceModelDownload"
---

<objective>
Build the first-run model-download UX (SC4) — the designed flow that makes voice setup feel intentional,
not a bare spinner. VoiceModelDownload renders in two contexts (D-07): (a) a skippable "Set up voice" step
inside OnboardingWizard, and (b) a lazy first-PTT modal for users who skipped. Both carry the SC4 qualities:
size disclosed BEFORE download, a progress bar bound to VOICE_MODEL_PROGRESS, pause/resume, and a graceful
"voice unavailable until ready" disabled-PTT affordance (D-08) that mirrors the TrialBanner entitlement gate.

Purpose: SC4 ("first-run model download is a designed flow — progress + resumable + size disclosure +
graceful unavailable state") and the user-facing half of VOICE-04 (on-device model acquisition) live here.
Output: the VoiceModelDownload component (both variants) and the skippable OnboardingWizard 'voice' step.

Depends on Plan 15-03 (the download manager + progress push) and Plan 15-01 (VOICE_* channels + prefs).
File ownership is disjoint from Plan 15-07 (live surface) so both Wave-3 plans run in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-audio-i-o-model-runtime/15-PATTERNS.md
@.planning/phases/15-audio-i-o-model-runtime/15-UI-SPEC.md

<interfaces>
<!-- TrialBanner disabled-gate + OnboardingWizard step machine to mirror. -->
From src/renderer/features/entitlement/TrialBanner.tsx — the disabled-gate tone + editorial card the "voice unavailable" affordance mirrors (D-08).
From src/renderer/features/onboarding/OnboardingWizard.tsx — Step union (loading → name → show → confirm → news-picker → password → sealing → done) + the buffer-then-persist pattern (newsSelection persisted in seal()). Insert a 'voice' step after 'password', before 'sealing'; it must be skippable and NOT block seal.
From src/renderer/components/editorial/Modal.tsx — size="md" for the lazy variant (xl from Phase 9 is too large).
From Plan 15-01: CHANNELS.VOICE_DOWNLOAD_MODEL / VOICE_GET_MODEL_STATUS / VOICE_MODEL_PROGRESS; window.aria.onVoiceModelProgress.
UI-SPEC §5 / §Copywriting: exact copy — "DOWNLOAD SIZE", "Whisper large-v3-turbo — 574 MB", "Stored in your app data folder. One-time download.", "Download now", "Set up later", "Continue →", "Voice ready", "NN% · X.X MB / 574 MB". Testids: voice-model-download-step, voice-model-download-modal, voice-download-cta, voice-download-skip, voice-download-progress-bar, voice-download-size-disclosure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VoiceModelDownload — size disclosure + progress + pause/resume (both variants)</name>
  <files>src/renderer/features/voice/VoiceModelDownload.tsx, src/renderer/features/voice/VoiceModelDownload.spec.tsx</files>
  <behavior>
    - renders the size disclosure block ("DOWNLOAD SIZE" / "Whisper large-v3-turbo — 574 MB" / "Stored in your app data folder. One-time download.") BEFORE any download starts (SC4 size disclosure)
    - "Download now" triggers VOICE_DOWNLOAD_MODEL; while downloading, a role=progressbar reflects VOICE_MODEL_PROGRESS (aria-valuenow=percent) with the "NN% · X.X MB / 574 MB" label; pause/resume link toggles
    - on completion shows the "Voice ready" moss checkmark and the CTA becomes "Continue →" (step variant) / closes (modal variant)
    - "Set up later" / "Skip" dismisses without downloading and leaves model-readiness pending
    - a `variant: 'step' | 'modal'` prop switches chrome (onboarding card vs editorial Modal size="md"); testids per UI-SPEC
    - progress bar fill animation is suppressed under prefers-reduced-motion (instant jump)
    - spec mocks window.aria voice IPC + onVoiceModelProgress and asserts size-before-start, progress binding, pause/resume, complete-state, skip
  </behavior>
  <action>
    Create src/renderer/features/voice/VoiceModelDownload.tsx with a variant prop ('step' | 'modal'). Mirror the
    TrialBanner editorial tone (D-08) and use the Modal primitive at size="md" for the lazy variant. Implement the
    SC4 flow per UI-SPEC §5: mandatory size disclosure BEFORE download; "Download now" → window.aria voice
    download IPC; subscribe to onVoiceModelProgress and render a role=progressbar (aria-valuenow/min/max) +
    "NN% · X.X MB / 574 MB" (tabular-nums); pause/resume link; "Voice ready" moss completion + "Continue →";
    "Set up later"/"Skip" dismiss leaving readiness pending. Use only existing editorial tokens + the exact
    UI-SPEC copy + testids. Suppress the progress fill animation under prefers-reduced-motion. Read readiness via
    VOICE_GET_MODEL_STATUS. Run npm run typecheck.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/features/voice/VoiceModelDownload.spec.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - source: the size-disclosure block (testid voice-download-size-disclosure) renders before any download starts; copy matches UI-SPEC verbatim
    - source: a role=progressbar with aria-valuenow bound to VOICE_MODEL_PROGRESS; pause/resume control present; variant prop switches step vs Modal size="md"
    - source: progress fill suppressed under @media (prefers-reduced-motion: reduce)
    - test-command: `npx vitest run src/renderer/features/voice/VoiceModelDownload.spec.tsx -x` passes (size-before-start, progress binding, pause/resume, complete→Voice ready, skip→pending)
    - test-command: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>VoiceModelDownload renders the designed SC4 flow in both step + modal variants with size disclosure, progress, pause/resume, and graceful skip (D-07/D-08); spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skippable 'voice' step in OnboardingWizard (never blocks seal)</name>
  <files>src/renderer/features/onboarding/OnboardingWizard.tsx, src/renderer/features/onboarding/OnboardingWizard.spec.tsx</files>
  <behavior>
    - the Step union gains 'voice' inserted after 'password' and before 'sealing'; the render chain renders VoiceModelDownload variant='step'
    - "Set up later"/"Skip" on the voice step advances directly to 'sealing' (seal is NOT blocked on voice — UI-SPEC §OnboardingWizard)
    - "Continue →" (after a completed download) advances to 'sealing'
    - existing steps (name/show/confirm/news-picker/password/sealing/done) still flow in order; existing onboarding spec assertions still pass
    - spec drives the wizard to the voice step and asserts both the skip→sealing and continue→sealing transitions
  </behavior>
  <action>
    Modify src/renderer/features/onboarding/OnboardingWizard.tsx: add 'voice' to the Step union and insert it in
    the render chain after 'password', before 'sealing'. Render VoiceModelDownload (variant='step', Task 1).
    "Set up later"/"Skip" → setStep('sealing') (the lazy first-PTT modal becomes mandatory later, D-07); a
    completed download → "Continue →" → setStep('sealing'). The voice step must NOT block seal (it is a skippable
    convenience, not a gate). Preserve the existing buffer-then-persist pattern — the voice step does not need to
    write a pref itself (the download manager flips readiness via the settings KV prefs, Plan 15-03). Update the
    existing OnboardingWizard spec for the new step in the sequence. Run npm run typecheck.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/features/onboarding/OnboardingWizard.spec.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - source: Step union includes 'voice' between 'password' and 'sealing'; the step renders VoiceModelDownload variant='step'
    - source: skip and continue both transition to 'sealing'; no code path blocks seal on the voice step
    - test-command: `npx vitest run src/renderer/features/onboarding/OnboardingWizard.spec.tsx -x` passes (existing steps intact + skip→sealing + continue→sealing)
    - test-command: `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>OnboardingWizard has a skippable 'voice' step hosting the download flow that never blocks seal (D-07); spec green incl. existing steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer UI → main (download control) | the download IPC commands cross to the main-process manager (Plan 15-03 validates) |
| onboarding flow → seal | the voice step must not be able to block or corrupt the seal sequence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-24 | Denial of Service | voice step blocking seal | mitigate | the voice step is skippable and both skip + continue transition to 'sealing'; the spec asserts no path blocks seal (UI-SPEC §OnboardingWizard) |
| T-15-25 | Information Disclosure | size/destination disclosure | mitigate | SC4 discloses size + "Stored in your app data folder" before download — informed-consent surface; the actual fetch integrity is handled in Plan 15-03 (T-15-08) |
| T-15-26 | Tampering | download command abuse | accept | the UI only issues a parameterless VOICE_DOWNLOAD_MODEL; the URL + destination are app-controlled in the main manager (Plan 15-03), not passed from the renderer |
</threat_model>

<verification>
- `npx vitest run src/renderer/features/voice/VoiceModelDownload.spec.tsx src/renderer/features/onboarding/OnboardingWizard.spec.tsx -x` green.
- `npm run typecheck` clean.
</verification>

<success_criteria>
- The model download is a designed flow with size-before-start, progress, pause/resume, and a graceful unavailable state (SC4/D-07).
- Two entry points: skippable onboarding step + lazy first-PTT modal; disabled-PTT affordance routes to the flow (D-08).
- The onboarding voice step never blocks seal.
</success_criteria>

<output>
After completion, create `.planning/phases/15-audio-i-o-model-runtime/15-08-SUMMARY.md`
</output>
