---
phase: 05-outlook-parity-email-calendar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/main/db/migrations/011_provider_accounts.sql
  - src/main/db/migrations/012_message_provider_key.sql
  - src/main/db/migrations/runner.ts
  - src/main/secrets/safeStorage.ts
  - src/main/integrations/microsoft/auth.ts
  - src/main/integrations/microsoft/cache.ts
  - src/main/integrations/microsoft/client.ts
  - src/main/integrations/microsoft/errors.ts
  - src/main/integrations/microsoft/mail.ts
  - src/main/integrations/microsoft/sync-mail.ts
  - src/main/integrations/microsoft/calendar.ts
  - src/main/integrations/microsoft/sync-calendar.ts
  - src/main/integrations/microsoft/recurrence-graph.ts
  - src/main/integrations/microsoft/write-event.ts
  - src/main/integrations/microsoft/send.ts
  - src/main/integrations/microsoft/provider-account.ts
  - src/main/integrations/microsoft/identity.ts
  - src/main/scheduling/self-only-gate.ts
  - src/main/scheduling/propose.ts
  - src/main/ipc/microsoft.ts
  - src/main/scheduler/queues.ts
  - src/renderer/features/settings/IntegrationsSection.tsx
  - tests/unit/main/db/migrations-011-012.spec.ts
  - tests/unit/main/integrations/microsoft/auth.test.ts
  - tests/unit/main/integrations/microsoft/recurrence-graph.test.ts
  - tests/unit/main/integrations/microsoft/sync-mail.test.ts
  - tests/unit/main/integrations/microsoft/sync-calendar.test.ts
  - tests/unit/main/integrations/microsoft/write-event.test.ts
  - tests/unit/main/integrations/microsoft/self-only-gate-graph.test.ts
  - tests/integration/microsoft/live-tenant.smoke.test.ts
  - tests/fixtures/microsoft/graph-responses.json
  - tests/setup/msw-graph.ts
autonomous: false
requirements: [EMAIL-02, CAL-02, CAL-08]
requirements_addressed: [EMAIL-02, CAL-02, CAL-08]
tags: [outlook, msal, graph, oauth, recurrence, migration, multi-account]
user_setup:
  - service: microsoft-365-developer-tenant
    why: "Live integration tests + manual UAT require an M365 tenant. Free Developer Program tenant is the cheapest viable option (Open Q 1). If signup blocked, fallback is a $6/mo Business Basic seat. Test cannot exercise sendMail or delta queries without a live mailbox."
    env_vars: []
  - service: azure-ad-app-registration
    why: "Public-client app registration at portal.azure.com supplies MS_OAUTH_CLIENT_ID. Redirect URI must include http://localhost (port wildcarded for loopback)."
    env_vars:
      - name: MS_OAUTH_CLIENT_ID
        source: "Azure portal -> App registrations -> {Aria app} -> Overview -> Application (client) ID"
must_haves:
  truths:
    - "SQLite user_version advances to 12 after both migrations; existing message/calendar_event rows backfilled with provider_key='google' and account_id=<existing email>"
    - "Migration 012 also backfills existing approval rows with kind IN ('calendar_change','email_send'): provider_key='google' and account_id from the Google singleton; AFTER migration no approval row has NULL provider_key or NULL account_id for those kinds (R5)"
    - "Migration 011 adds identity_set_json TEXT and recurrence_unsupported BOOLEAN columns in their final shape; no later task edits migration 011 (R2)"
    - "provider_account row created on Outlook connect with account_id=homeAccountId, provider_key='microsoft', display_email from /me, status='ok'"
    - "provider_sync_state row created per (account_id, resource); cursor persists across app restart"
    - "MSAL cache encrypted via safeStorage under providerTokens:microsoft:<homeAccountId>; never logged"
    - "Outlook mail delta sync upserts canonical Message rows tagged with provider_key='microsoft'; tombstones delete by externalId"
    - "Outlook calendar delta sync upserts canonical Event rows; recurrence converted from Graph pattern to RFC5545 RRULE at the boundary"
    - "recurrence-graph.ts refuses lossy patterns and returns {unsupported:true,reason} (per CONTEXT D-§Recurring event normalization)"
    - "Self-only gate against Outlook events compares organizer.emailAddress.address (or isOrganizer flag) AND attendee set against {UPN, mail, ...proxyAddresses} (Pitfall 8)"
    - "assertSelfOnly retains backward-compatible signature: accepts (event, string) OR (event, IdentitySet); existing Phase 4 Google call sites compile + pass unchanged (R3)"
    - "Token-invalid on Microsoft does not stop Google sync; account.status flips to 'needs-auth'; per-(provider,account) p-queue isolated"
    - "On powerMonitor suspend, Outlook cron ticks cancel; on resume, one-shot catch-up tick per (account,resource) runs"
    - "Internal write-event.ts is the ONLY file in microsoft/ calling graph events PATCH/POST; static grep enforces this (mirrors P-04-01 ratchet but scoped to this adapter)"
    - "All field-projections in delta $select are baked at initial call: mail [id,conversationId,subject,from,receivedDateTime,isRead,importance,bodyPreview,internetMessageId,categories,parentFolderId]; events [id,iCalUId,subject,body,start,end,location,organizer,attendees,recurrence,seriesMasterId,type,isOrganizer,isCancelled,showAs,recurrenceTimeZone] (Pitfall 4)"
    - "410 / syncStateNotFound on delta triggers fullResyncWindow with 7-day backfill, then advances to fresh deltaLink (Pitfall 1)"
    - "Initial backfill uses narrow time window filter + p-queue intervalCap:2/interval:1000 to avoid 429 burst (Pitfall 3)"
    - "On 401 from Graph after silent token, one forceRefresh:true retry before flipping to needs-auth (Pitfall 6)"
    - "Delta body field is bodyPreview only; full body fetched on-demand via singleton GET with Prefer header (Pitfall 7)"
  artifacts:
    - path: src/main/db/migrations/011_provider_accounts.sql
      provides: "provider_account + provider_sync_state tables + identity_set_json + recurrence_unsupported (frozen after Task 1)"
      contains: "CREATE TABLE provider_account, CREATE TABLE provider_sync_state, identity_set_json TEXT, recurrence_unsupported BOOLEAN"
    - path: src/main/db/migrations/012_message_provider_key.sql
      provides: "Add provider_key + account_id columns to message, calendar_event, and approval; backfill existing rows to ('google', <existing-email>)"
      contains: "ALTER TABLE message ADD COLUMN provider_key, UPDATE message SET provider_key='google', UPDATE approval SET provider_key='google'"
    - path: src/main/integrations/microsoft/auth.ts
      provides: "connectMicrosoft() — MSAL-node PKCE loopback flow; returns {accountId,email,displayName}"
      exports: ["connectMicrosoft", "OAuthConfigMissingError"]
    - path: src/main/integrations/microsoft/cache.ts
      provides: "createSafeStorageCachePlugin(accountId) — MSAL ICachePlugin wrapping providerTokens:microsoft:<id>"
      exports: ["createSafeStorageCachePlugin", "getOrBuildPca"]
    - path: src/main/integrations/microsoft/client.ts
      provides: "createGraphClient(accountId) — Client.initWithMiddleware with authProvider doing acquireTokenSilent + forceRefresh retry on 401"
      exports: ["createGraphClient"]
    - path: src/main/integrations/microsoft/mail.ts
      provides: "listMessagesDelta, getMessage (full body), sendMail — all over graph-client"
      exports: ["listMessagesDelta", "getMessage", "sendMailViaGraph"]
    - path: src/main/integrations/microsoft/sync-mail.ts
      provides: "tickMail({db, accountId}) — delta loop with cursor advance in single db.transaction"
      exports: ["tickMail", "fullResyncMailWindow"]
    - path: src/main/integrations/microsoft/calendar.ts
      provides: "listEventsDelta, patchEvent, insertEvent, eventInstances, freeBusy over graph-client; recurrence converted at boundary"
      exports: ["listEventsDelta", "patchEvent", "insertEvent", "eventInstances", "freeBusy"]
    - path: src/main/integrations/microsoft/sync-calendar.ts
      provides: "tickCalendar({db, accountId}) — mirrors google/sync-calendar.ts; atomic cursor advance"
      exports: ["tickCalendar", "fullResyncCalendarWindow"]
    - path: src/main/integrations/microsoft/recurrence-graph.ts
      provides: "rruleToGraphRecurrence + graphRecurrenceToRrule — bidirectional with refuse-on-lossy"
      exports: ["rruleToGraphRecurrence", "graphRecurrenceToRrule", "GraphRecurrenceUnsupported"]
    - path: src/main/integrations/microsoft/write-event.ts
      provides: "Internal adapter calling provider.calendar PATCH/POST; Plan 05-02a dispatch to here via shared chokepoint"
      exports: ["applyMicrosoftCalendarWrite"]
    - path: src/main/integrations/microsoft/send.ts
      provides: "Internal adapter for /me/sendMail; Plan 05-02a dispatches here via shared sendApprovedEmail chokepoint"
      exports: ["sendMicrosoftApprovedEmail"]
    - path: src/main/integrations/microsoft/identity.ts
      provides: "fetchSelfIdentity(graph) — pulls /me?$select=mail,userPrincipalName,proxyAddresses,displayName; persisted into provider_account at connect time (Pitfall 8 mitigation)"
      exports: ["fetchSelfIdentity", "loadSelfIdentitySet"]
    - path: src/main/integrations/microsoft/provider-account.ts
      provides: "CRUD over provider_account rows; status transitions ok/degraded/needs-auth"
      exports: ["createProviderAccount", "getProviderAccount", "markAccountStatus", "listProviderAccounts"]
    - path: src/main/scheduler/queues.ts
      provides: "Per-(providerKey,accountId) PQueue map; hard-pause on TokenInvalid; soft-retry+degraded on transient (CONTEXT failure isolation)"
      exports: ["getQueue", "enqueueForAccount", "pauseAccount", "resumeAccount"]
    - path: src/main/scheduling/self-only-gate.ts
      provides: "Generalized assertSelfOnly: overloaded signature (event,string)|(event,IdentitySet); preserves Phase 4 Google call sites"
      exports: ["assertSelfOnly", "isSelfOnly", "loadSelfIdentity", "IdentitySet"]
  key_links:
    - from: "src/main/integrations/microsoft/sync-mail.ts:tickMail"
      to: "src/main/integrations/microsoft/mail.ts:listMessagesDelta"
      via: "delta cursor read from provider_sync_state"
      pattern: "listMessagesDelta\\(.*cursor"
    - from: "src/main/integrations/microsoft/sync-calendar.ts:tickCalendar"
      to: "src/main/integrations/microsoft/recurrence-graph.ts:graphRecurrenceToRrule"
      via: "boundary conversion before upsert"
      pattern: "graphRecurrenceToRrule"
    - from: "src/main/integrations/microsoft/auth.ts:connectMicrosoft"
      to: "src/main/integrations/microsoft/identity.ts:fetchSelfIdentity"
      via: "post-token-acquire identity bake (Pitfall 8)"
      pattern: "fetchSelfIdentity\\("
    - from: "src/main/secrets/safeStorage.ts:setProviderTokens"
      to: "providerTokens namespace (RESEARCH Per-Provider Keyring section)"
      via: "key = `${providerKey}:${accountId}`"
      pattern: "providerTokens"
    - from: "src/main/scheduler/queues.ts"
      to: "src/main/integrations/microsoft/sync-mail.ts:tickMail"
      via: "enqueueForAccount('microsoft', accountId, () => tickMail(...))"
      pattern: "enqueueForAccount"

threats:
  - id: T-05-01-01
    category: Spoofing
    component: src/main/integrations/microsoft/auth.ts loopback callback
    disposition: mitigate
    mitigation: "32-byte crypto-random state validated on callback (mirror Phase 2 pattern); reject mismatched state with explicit error before token exchange"
  - id: T-05-01-02
    category: InformationDisclosure
    component: MSAL token cache at rest
    disposition: mitigate
    mitigation: "Cache serialized as opaque blob into safeStorage providerTokens:microsoft:<id>; on Linux basic_text backend, surface existing Phase 1 warning before connect proceeds (Open Q 4)"
  - id: T-05-01-03
    category: ElevationOfPrivilege
    component: BrowserWindow opening login.microsoftonline.com
    disposition: mitigate
    mitigation: "openSandboxedBrowserWindow with nodeIntegration:false, contextIsolation:true, sandbox:true, NO preload (mirror Phase 2 T-02-01-03)"
  - id: T-05-01-04
    category: Tampering
    component: Approval bypass via direct Graph call
    disposition: mitigate
    mitigation: "write-event.ts + send.ts in microsoft/ have NO public exports invoked by feature code; only Plan 05-02a chokepoint dispatches to them. Plan 05-02a two-layer static-grep ratchet enforces post-unification."
  - id: T-05-01-05
    category: InformationDisclosure
    component: pino logs around MSAL cache + Graph requests
    disposition: mitigate
    mitigation: "Add redactor keys: msalCacheEnc, accessToken, refreshToken, authorization. Log accountId + redacted email handle (first 2 chars + domain) only."
  - id: T-05-01-06
    category: DenialOfService
    component: Graph throttling on initial backfill
    disposition: mitigate
    mitigation: "Pitfall 3 — p-queue intervalCap:2/interval:1000; rely on graph-client RetryHandler for Retry-After"
---

<objective>
Build a Microsoft Graph (Outlook Mail + Calendar) adapter as a peer of the existing Google adapter, with its own MSAL OAuth flow, delta-based sync, recurrence boundary conversion, internal write chokepoints, and multi-account persistence. This plan does NOT yet introduce the unified Provider interface (deferred to Plan 05-02a) — the two adapters coexist with brief duplication, per CONTEXT D-§Retrofit strategy.

Closes: EMAIL-02 (incremental Outlook mail ingest), CAL-02 (Outlook calendar ingest + write capability proved in isolation), CAL-08 (recurring event normalization to RFC5545 canonical form). Generalization across both providers is addressed by Plans 05-02a / 05-02b and the unified UI surfaces by Plan 05-03.

Purpose: Establish the Outlook code path end-to-end (auth → sync → write internal → audit) with full test coverage and live-tenant smoke before any cross-provider refactor. Honor every Pitfall from RESEARCH and every applicable Phase 4 L-lesson.

Output:
- 2 SQL migrations (011, 012) + backfill of existing rows (including approval rows — R5)
- New src/main/integrations/microsoft/ subtree (10 files)
- Per-provider keyring helpers (providerTokens namespace) in safeStorage.ts
- Per-(provider,account) p-queue scheduler with failure isolation
- Self-only gate generalized with backward-compatible overloaded signature (R3)
- Unit + integration tests; one live-tenant smoke test gated by env var
- Settings UI "Add Outlook account" action wired
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-outlook-parity-email-calendar/05-CONTEXT.md
@.planning/phases/05-outlook-parity-email-calendar/05-RESEARCH.md
@.planning/phases/04-calendar-smart-scheduling-google/04-LEARNINGS.md
@.planning/phases/04-calendar-smart-scheduling-google/04-01-SUMMARY.md
@.planning/phases/04-calendar-smart-scheduling-google/04-02-SUMMARY.md
@.planning/phases/04-calendar-smart-scheduling-google/04-03-SUMMARY.md
@.planning/phases/02-gmail-ingest-daily-briefing-mvp/02-SUMMARY.md
@src/main/integrations/google/auth.ts
@src/main/integrations/google/calendar.ts
@src/main/integrations/google/sync-calendar.ts
@src/main/integrations/google/sync-gmail.ts
@src/main/integrations/google/write-event.ts
@src/main/integrations/google/send.ts
@src/main/integrations/google/recurrence.ts
@src/main/scheduling/self-only-gate.ts
@src/main/scheduling/propose.ts
@src/main/db/migrations/010_calendar_writeback.sql
@CLAUDE.md

<interfaces>
<!-- Canonical types this adapter conforms to. Plan 05-02a will lift these to src/shared/provider.ts.
     For Plan 05-01, declare them locally inside src/main/integrations/microsoft/types.ts to keep
     blast radius scoped; Plan 05-02a will move + re-export. -->

CanonicalMessage:
  externalId, threadId, fromAddr, subject, snippet, receivedAtUtc,
  labels[], isUnread, isImportant, bodyText?

CanonicalEvent (Phase 4 row shape extended):
  externalId, iCalUId?, parentExternalId?, summary, location?, description?,
  startUtc, endUtc, startDate?, timeZone?, recurrence?: string[] (RFC5545),
  organizerEmail?, organizerSelf?, attendees[], etag?, sequence?,
  status?: 'confirmed'|'tentative'|'cancelled'

IdentitySet (new in this plan; Pitfall 8):
  primaryEmail: string
  aliases: string[]            // additional addresses (UPN, mail, proxyAddresses)
  // .allAddresses() helper returns [primaryEmail, ...aliases] lowercased + deduped

DeltaResult<T>:
  items: T[]
  tombstones: string[]
  cursor: string
  hadFullResync: boolean

Error taxonomy (src/main/integrations/microsoft/errors.ts):
  - TokenInvalidError({ reason: 'revoked' | 'expired' | 'consent_required' })  — triggers needs-auth
  - DeltaExpiredError                                                          — triggers fullResync
  - EtagMismatchError                                                          — propagates to chokepoint
  - GraphRecurrenceUnsupported({ reason })                                     — refuse-on-write
  - TransientGraphError                                                        — soft-retry, account 'degraded'
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Provision M365 dev tenant + Azure AD app registration</name>
  <what-built>Pre-task: this is the one Outlook-side dependency Claude cannot automate. Required before any subsequent task can run a live network call.</what-built>
  <how-to-verify>
    1. Sign up for Microsoft 365 Developer Program at https://developer.microsoft.com/microsoft-365/dev-program (or provision $6/mo Business Basic seat if Dev Program signup is blocked — see RESEARCH Open Q 1 and Assumption A5).
    2. In the resulting tenant, register a new app at https://portal.azure.com -> Azure Active Directory -> App registrations -> New registration:
       - Name: "Aria (dev)"
       - Supported account types: "Accounts in any organizational directory and personal Microsoft accounts (multi-tenant)"
       - Redirect URI: Public client/native (mobile & desktop) -> http://localhost
    3. From the app Overview page, copy "Application (client) ID" into .env.local as MS_OAUTH_CLIENT_ID=...
    4. Under "API permissions" add delegated permissions: Mail.Read, Mail.Send, Calendars.ReadWrite, User.Read, offline_access. Grant admin consent (button at top of API permissions page).
    5. Confirm a working Outlook mailbox exists in the tenant (Microsoft 365 admin -> Users -> at least one user with Exchange Online license; Dev Program seats come with this by default).
  </how-to-verify>
  <resume-signal>Type "MS_OAUTH_CLIENT_ID is set in .env.local and consent granted" or describe blockers</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Migrations 011 + 012 (frozen SQL) + Per-provider keyring helpers</name>
  <files>
    src/main/db/migrations/011_provider_accounts.sql,
    src/main/db/migrations/012_message_provider_key.sql,
    src/main/db/migrations/runner.ts,
    src/main/secrets/safeStorage.ts,
    tests/unit/main/db/migrations-011-012.spec.ts,
    tests/unit/main/secrets/providerTokens.spec.ts
  </files>
  <behavior>
    - Test: applying migrations from a Phase 4 (user_version=10) database advances to user_version=12 and creates provider_account, provider_sync_state tables WITH identity_set_json + recurrence_unsupported columns present from this single migration (R2).
    - Test (fresh-install path): applying migrations on a brand-new DB (no gmail_account row, no calendar_account row, zero message rows, zero calendar_event rows, zero approval rows) succeeds; UPDATE statements affect zero rows but do not error; user_version still advances to 12 (R1).
    - Test (singleton-present path): existing message rows pre-migration with NULL provider_key get backfilled to ('google', <gmail_account.email>); existing calendar_event rows get backfilled to ('google', <calendar_account.email>) (R1 happy path).
    - Test (approval backfill — R5): existing approval rows with kind IN ('calendar_change','email_send') and provider_key IS NULL get backfilled to provider_key='google' with account_id = (gmail_account.email for email_send, calendar_account.email for calendar_change). Assert post-migration: `SELECT count(*) FROM approval WHERE kind IN ('calendar_change','email_send') AND (provider_key IS NULL OR account_id IS NULL) = 0`.
    - Test: idempotent reruns are no-ops (PRAGMA user_version guard).
    - Test: setProviderTokens('microsoft:abc', blob) then getProviderTokens('microsoft:abc') returns blob; clearProviderTokens removes it.
    - Test: listProviderTokenKeys returns both legacy googleTokens-synthesized keys AND new providerTokens keys.
    - Test: writing a Google token via the new helper ALSO writes the legacy googleTokens subtree (back-compat write-through; CONTEXT-locked retrofit strategy).
    - Test (frozen schema invariant): after this task lands, the SHA-256 of migrations/011_provider_accounts.sql is captured in a snapshot test (`tests/unit/main/db/migration-011-frozen.snap`); any later task edit fails CI.
  </behavior>
  <action>
    Per L-04-01: every column added in migrations 011 + 012 (provider_key, account_id on message and calendar_event; identity_set_json + recurrence_unsupported on provider_account; provider_key + account_id on approval) MUST be wired to corresponding sync/write paths in subsequent tasks; this task verifies the schema + backfill only.

    **Migration 011 — frozen SQL to embed verbatim** (R2 — every column added here; no later task may modify this file):

    ```sql
    -- 011_provider_accounts.sql
    BEGIN;

    CREATE TABLE IF NOT EXISTS provider_account (
      account_id          TEXT NOT NULL,            -- homeAccountId (microsoft) or email (google)
      provider_key        TEXT NOT NULL CHECK (provider_key IN ('google','microsoft')),
      display_email       TEXT NOT NULL,
      display_label       TEXT,                     -- user-editable; default = email handle
      display_color       TEXT,                     -- hex; auto-assigned from palette hash
      status              TEXT NOT NULL DEFAULT 'ok' CHECK (status IN ('ok','degraded','needs-auth','disconnected')),
      identity_set_json   TEXT,                     -- {primaryEmail, aliases:[UPN, mail, ...proxyAddresses]} (Pitfall 8)
      last_synced_at      TEXT,
      last_error          TEXT,
      last_error_at       TEXT,
      created_at          TEXT NOT NULL DEFAULT (datetime('now')),
      capabilities_json   TEXT NOT NULL,            -- {mail:bool, calendar:bool}
      PRIMARY KEY (provider_key, account_id)
    );

    CREATE TABLE IF NOT EXISTS provider_sync_state (
      provider_key   TEXT NOT NULL,
      account_id     TEXT NOT NULL,
      resource       TEXT NOT NULL CHECK (resource IN ('mail','calendar')),
      cursor         TEXT,                          -- opaque deltaLink / historyId / syncToken
      last_sync_at   TEXT,
      last_error     TEXT,
      PRIMARY KEY (provider_key, account_id, resource),
      FOREIGN KEY (provider_key, account_id) REFERENCES provider_account(provider_key, account_id) ON DELETE CASCADE
    );

    CREATE INDEX IF NOT EXISTS idx_provider_account_status ON provider_account(status);

    -- recurrence_unsupported flag on canonical event rows (Plan 05-01 Task 4 writes it;
    -- Plan 05-03 UI surfaces "View in {provider}" deep link based on this flag).
    ALTER TABLE calendar_event ADD COLUMN recurrence_unsupported INTEGER NOT NULL DEFAULT 0;

    PRAGMA user_version = 11;
    COMMIT;
    ```

    **Migration 012 — frozen SQL to embed verbatim** (R1 + R5):

    ```sql
    -- 012_message_provider_key.sql
    BEGIN;

    ALTER TABLE message       ADD COLUMN provider_key TEXT;
    ALTER TABLE message       ADD COLUMN account_id   TEXT;
    ALTER TABLE calendar_event ADD COLUMN provider_key TEXT;
    ALTER TABLE calendar_event ADD COLUMN account_id   TEXT;
    ALTER TABLE approval      ADD COLUMN provider_key TEXT;
    ALTER TABLE approval      ADD COLUMN account_id   TEXT;

    CREATE INDEX IF NOT EXISTS idx_message_provider_account       ON message(provider_key, account_id);
    CREATE INDEX IF NOT EXISTS idx_calendar_event_provider_account ON calendar_event(provider_key, account_id);
    CREATE INDEX IF NOT EXISTS idx_approval_provider_account       ON approval(provider_key, account_id);

    -- Backfill: only if singletons exist (fresh-install safe — UPDATEs match 0 rows).
    UPDATE message
       SET provider_key = 'google',
           account_id   = (SELECT email FROM gmail_account LIMIT 1)
     WHERE provider_key IS NULL
       AND EXISTS (SELECT 1 FROM gmail_account);

    UPDATE calendar_event
       SET provider_key = 'google',
           account_id   = (SELECT email FROM calendar_account LIMIT 1)
     WHERE provider_key IS NULL
       AND EXISTS (SELECT 1 FROM calendar_account);

    -- R5: backfill in-flight approvals so the generalized chokepoint (Plan 05-02a)
    -- never encounters NULL provider_key / account_id rows. Email sends key off the
    -- Gmail singleton; calendar changes key off the Calendar singleton.
    UPDATE approval
       SET provider_key = 'google',
           account_id   = (SELECT email FROM gmail_account LIMIT 1)
     WHERE provider_key IS NULL
       AND kind = 'email_send'
       AND EXISTS (SELECT 1 FROM gmail_account);

    UPDATE approval
       SET provider_key = 'google',
           account_id   = (SELECT email FROM calendar_account LIMIT 1)
     WHERE provider_key IS NULL
       AND kind = 'calendar_change'
       AND EXISTS (SELECT 1 FROM calendar_account);

    -- Backfill seeds the corresponding provider_account row if missing
    -- (so the registry can find an account_id match for in-flight approvals).
    INSERT OR IGNORE INTO provider_account (
      account_id, provider_key, display_email, status, capabilities_json
    )
    SELECT email, 'google', email, 'ok', '{"mail":true,"calendar":false}'
      FROM gmail_account;

    INSERT OR IGNORE INTO provider_account (
      account_id, provider_key, display_email, status, capabilities_json
    )
    SELECT email, 'google', email, 'ok', '{"mail":false,"calendar":true}'
      FROM calendar_account;

    -- Merge capabilities if same email exists in both singletons (rare but possible).
    UPDATE provider_account
       SET capabilities_json = '{"mail":true,"calendar":true}'
     WHERE provider_key = 'google'
       AND account_id IN (SELECT email FROM gmail_account)
       AND account_id IN (SELECT email FROM calendar_account);

    PRAGMA user_version = 12;
    COMMIT;
    ```

    Behavior assertion at the end of Task 1 tests (R5):
    ```sql
    SELECT count(*) FROM approval
      WHERE kind IN ('calendar_change','email_send')
        AND (provider_key IS NULL OR account_id IS NULL);
    -- Must equal 0 on the singleton-present test fixture; ALSO 0 on fresh-install (zero rows).
    ```

    In `src/main/secrets/safeStorage.ts` add `setProviderTokens(key, blob)`, `getProviderTokens(key)`, `clearProviderTokens(key)`, `listProviderTokenKeys()` wrapping the existing encrypted JSON file; key format `${providerKey}:${accountId}` is opaque. Per RESEARCH Per-Provider Keyring section: on read of `providerTokens.google:*` synthesize from legacy `googleTokens` if missing; on write of Google tokens via the new helper, write BOTH (dual-write back-compat — removed in Plan 05-02b). Do NOT touch existing `googleTokens` readers — Plan 05-02b conforms Google to the new helper.

    **Frozen-schema enforcement (R2):** add `tests/unit/main/db/migration-011-frozen.spec.ts` that hashes `011_provider_accounts.sql` and asserts equality with `tests/unit/main/db/migration-011-frozen.snap`. Any later task that modifies the migration breaks CI.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/db/migrations-011-012.spec.ts tests/unit/main/db/migration-011-frozen.spec.ts tests/unit/main/secrets/providerTokens.spec.ts --reporter=dot</automated>
  </verify>
  <done>Migrations idempotent + fresh-install + singleton-present + approval-backfill paths all verified; identity_set_json + recurrence_unsupported live in 011; providerTokens namespace usable; legacy googleTokens reads unchanged; 011 frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MSAL auth + cache + Graph client + identity bake</name>
  <files>
    package.json,
    src/main/integrations/microsoft/auth.ts,
    src/main/integrations/microsoft/cache.ts,
    src/main/integrations/microsoft/client.ts,
    src/main/integrations/microsoft/errors.ts,
    src/main/integrations/microsoft/identity.ts,
    src/main/integrations/microsoft/provider-account.ts,
    tests/unit/main/integrations/microsoft/auth.test.ts,
    tests/unit/main/integrations/microsoft/cache.test.ts,
    tests/unit/main/integrations/microsoft/client.test.ts,
    tests/unit/main/integrations/microsoft/identity.test.ts,
    tests/setup/msw-graph.ts,
    tests/fixtures/microsoft/graph-responses.json
  </files>
  <behavior>
    - Test (auth): with stubbed PublicClientApplication, connectMicrosoft completes the loopback dance and returns {accountId, email, displayName}; state mismatch throws; missing MS_OAUTH_CLIENT_ID throws OAuthConfigMissingError; window is closed in finally.
    - Test (cache): beforeCacheAccess deserializes from getProviderTokens; afterCacheAccess only writes when cacheHasChanged.
    - Test (client): getAccessToken calls acquireTokenSilent; on InteractionRequiredAuthError throws TokenInvalidError({reason:'expired'}); on 401-from-Graph wrapper invokes acquireTokenSilent({forceRefresh:true}) exactly ONCE before propagating (Pitfall 6).
    - Test (identity): fetchSelfIdentity returns {upn, mail, proxyAddresses[], displayName} from /me?$select=mail,userPrincipalName,proxyAddresses,displayName; missing fields default to empty arrays.
    - Test (provider-account): createProviderAccount persists row with identity set serialized into identity_set_json (column from Task 1's frozen 011); markAccountStatus updates status + last_error_at.
  </behavior>
  <action>
    Add deps: `npm install @azure/msal-node@^3 @microsoft/microsoft-graph-client@^3 isomorphic-fetch@^3` and `npm install -D @types/microsoft-graph @typescript-eslint/typescript-estree`. (`@typescript-eslint/typescript-estree` is required by Plan 05-02a Task 2's AST-based ratchet — adding it as a devDep here keeps the new install confined to one task.) Pin majors in package.json per CLAUDE.md tech stack snapshot.

    Implement auth.ts mirroring src/main/integrations/google/auth.ts structure exactly (loopback HTTP server + sandboxed BrowserWindow + crypto-random state + finally-close), per RESEARCH Pattern 1. Tenant = 'common'; scopes = SCOPES.all per RESEARCH. Use prompt:'select_account' so multi-account UX works. Implement cache.ts per RESEARCH Pattern 2 (ICachePlugin wrapping providerTokens namespace from Task 1). Implement client.ts per RESEARCH Pattern 3 (Client.initWithMiddleware) but wrap authProvider.getAccessToken with retry-on-401 (Pitfall 6 mitigation, Assumption A4 — verify behavior in spike during this task).

    Implement identity.ts (fetchSelfIdentity); persist identity set into provider_account.identity_set_json (column already present from Task 1's frozen migration 011; **do NOT edit migration 011** — R2). Set up MSW handlers (tests/setup/msw-graph.ts) for graph.microsoft.com to enable hermetic unit tests. Per L-04-02 lesson: include at least one integration test that exercises the real graph-client middleware against MSW with a recorded response fixture. Add MS_OAUTH_CLIENT_ID validation early; surface a clear error if Task 0 was skipped.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/integrations/microsoft/auth.test.ts tests/unit/main/integrations/microsoft/cache.test.ts tests/unit/main/integrations/microsoft/client.test.ts tests/unit/main/integrations/microsoft/identity.test.ts --reporter=dot</automated>
  </verify>
  <done>connectMicrosoft round-trips against MSW; cache persists across PCA recreate; client retries on 401 once; identity set persisted to provider_account.identity_set_json (existing column, no migration edits). Audit log redactor keys updated (msalCacheEnc, accessToken). @typescript-eslint/typescript-estree available as devDep for Plan 05-02a.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Recurrence boundary conversion (RFC5545 ↔ Graph)</name>
  <files>
    src/main/integrations/microsoft/recurrence-graph.ts,
    tests/unit/main/integrations/microsoft/recurrence-graph.test.ts,
    tests/fixtures/microsoft/recurrence-cases.json
  </files>
  <behavior>
    - Test: each of the 5 supported pattern types (daily, weekly byWeekday, absoluteMonthly, relativeMonthly w/ first/second/third/fourth/last weekIndex, absoluteYearly, relativeYearly) round-trips Graph → RRULE → Graph without loss.
    - Test: each range type (endDate → UNTIL, numbered → COUNT, noEnd → infinite) round-trips.
    - Test: range.recurrenceTimeZone is honored when computing UNTIL UTC timestamps (Pitfall 2) — explicit unit test contrasting event.start.timeZone vs range.recurrenceTimeZone with different values.
    - Test: BYHOUR/BYMINUTE/BYSECOND/BYWEEKNO/BYYEARDAY → returns {unsupported:true,reason} (refuse-on-write).
    - Test: RRuleSet (RDATE/EXDATE) → returns {unsupported:true,reason}.
    - Test: weekIndex='last' maps to BYSETPOS=-1; round-trip stable across 12-month property-based fuzz (Open Q 5).
    - Test: rruleToGraphRecurrence preserves WKST when daysOfWeek present + firstDayOfWeek set on the Graph pattern.
  </behavior>
  <action>
    Pure module; no I/O. Use `rrule` (already in deps from Phase 4). Implement bidirectional conversion per the table in RESEARCH Pattern 7 and the lossy refusal list. Refusal text on the consumed side ("This recurrence pattern isn't supported in Aria — please edit it in {provider} directly.") is rendered by the approval card, not here — this module returns the structured `{unsupported, reason}` object only. Per L-04-03: surface `reason` in the structured response; do NOT replace with a hardcoded constant. Per Open Q 5: include a 12-month property-based test using fast-check (add as devDep if not present) that round-trips every supported pattern monthly over a calendar year.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/integrations/microsoft/recurrence-graph.test.ts --reporter=dot</automated>
  </verify>
  <done>All 5 patterns × 3 ranges round-trip; all lossy cases refuse with explicit reason; recurrenceTimeZone distinction tested; CAL-08 canonical conversion proved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Mail + Calendar adapters (read paths) + delta sync</name>
  <files>
    src/main/integrations/microsoft/mail.ts,
    src/main/integrations/microsoft/sync-mail.ts,
    src/main/integrations/microsoft/calendar.ts,
    src/main/integrations/microsoft/sync-calendar.ts,
    tests/unit/main/integrations/microsoft/sync-mail.test.ts,
    tests/unit/main/integrations/microsoft/sync-calendar.test.ts,
    tests/fixtures/microsoft/graph-responses.json
  </files>
  <behavior>
    - Test (mail): listMessagesDelta with null cursor calls /me/mailFolders/Inbox/messages/delta with the full $select projection from Pitfall 4; paginates @odata.nextLink; persists @odata.deltaLink only after final page.
    - Test (mail): tombstones in delta (@removed:{reason:'deleted'}) become DeltaResult.tombstones[]; sync-mail deletes by externalId in same db.transaction as upserts.
    - Test (mail): every upserted message row has provider_key='microsoft' and account_id=homeAccountId.
    - Test (mail): bodyText is NOT requested in delta projection; full body fetched on-demand via getMessage with Prefer header (Pitfall 7).
    - Test (calendar): listEventsDelta uses the full $select projection from Pitfall 4 including recurrence + recurrenceTimeZone + @odata.etag.
    - Test (calendar): recurrence is converted via graphRecurrenceToRrule at the boundary BEFORE upsert; canonical event row stores RFC5545 strings (Pitfall 2; CONTEXT-locked canonical form).
    - Test (calendar): on graphRecurrenceToRrule={unsupported}, event still persists with recurrence=null AND recurrence_unsupported=1 (column from Task 1 frozen migration 011 — R2).
    - Test (calendar): cancellation tombstone (@removed) becomes DeltaResult.tombstones[]; cancelled events keep their row with status='cancelled' if they appear in delta WITHOUT @removed (Outlook distinction).
    - Test: both adapters: 410 / syncStateNotFound → DeltaExpiredError → sync-*.ts catches and runs fullResync*Window(7-day backfill) then advances to fresh deltaLink (Pitfall 1).
    - Test: 429 with Retry-After delegates to graph-client's RetryHandler (verify via MSW timing).
    - Test: cursor advance + row upserts are atomic in db.transaction; on throw mid-loop, cursor does not advance (mirrors Phase 2 atomic discipline).
  </behavior>
  <action>
    Mirror src/main/integrations/google/sync-gmail.ts and src/main/integrations/google/sync-calendar.ts structurally — same row shape, same atomicity discipline. The delta cursor is opaque; never parse. Implement per RESEARCH Pattern 4. Bake the FULL $select projection on the initial call (Pitfall 4); the projection arrays should be exported constants so a future "add a field" PR forces a deliberate full-resync. The full-body Prefer header `header('Prefer', 'outlook.body-content-type="text"')` goes on the singleton getMessage GET, NOT on delta (Pitfall 7). For tombstones, Outlook delta uses @removed envelope; transform to canonical DeltaResult.tombstones[]. For initial backfill, use a narrow time-window pre-filter on first call: mail `?$filter=receivedDateTime ge {now-7d}`, calendar `?startDateTime={now}&endDateTime={now+90d}`. Recurrence conversion is at the boundary in sync-calendar.ts upsert path — call graphRecurrenceToRrule(event.recurrence, event.start) before persisting; on {unsupported:true} log a warning, persist event with recurrence=null + recurrence_unsupported=1 (column already added in 011). Per L-04-01 explicit verification: write integration test asserting that every column on calendar_event (provider_key, account_id, etag, i_cal_uid, sequence, organizer_email, organizer_self, recurrence_json, recurrence_unsupported) is correctly populated on a synced row.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/integrations/microsoft/sync-mail.test.ts tests/unit/main/integrations/microsoft/sync-calendar.test.ts --reporter=dot</automated>
  </verify>
  <done>Delta loops complete for both resources against MSW fixtures; cursor atomicity verified; all canonical columns wired (L-04-01 mitigation); 410 → full resync proved; recurrence conversion at boundary verified; recurrence_unsupported flag written from existing 011 column.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Internal write chokepoints (calendar + mail) + self-only gate generalization (overloaded signature, R3)</name>
  <files>
    src/main/integrations/microsoft/write-event.ts,
    src/main/integrations/microsoft/send.ts,
    src/main/integrations/microsoft/calendar.ts,
    src/main/integrations/microsoft/mail.ts,
    src/main/scheduling/self-only-gate.ts,
    src/main/scheduling/propose.ts,
    tests/unit/main/integrations/microsoft/write-event.test.ts,
    tests/unit/main/integrations/microsoft/send.test.ts,
    tests/unit/main/integrations/microsoft/self-only-gate-graph.test.ts,
    tests/unit/main/scheduling/self-only-gate.spec.ts
  </files>
  <behavior>
    - Test (write-event): applyMicrosoftCalendarWrite for scope='this' on a recurring event calls patchEvent with the singleton instance id (NOT the series id); scope='all' patches series; scope='future' splits via rrule UNTIL on the series + new series insert.
    - Test (write-event): isRecurring guard uses `Boolean(before.parentExternalId)`, NOT a substring check on event id (L-04-06 generalization; Pitfall 5).
    - Test (write-event): on EtagMismatchError (412 from Graph), error propagates unchanged; no retry, no silent overwrite.
    - Test (write-event): converts canonical body (Partial<CanonicalEvent> with recurrence: RFC5545 strings) to Graph wire shape (subject, start, recurrence: pattern+range) inside this module — keeps caller pure-canonical (RESEARCH Option A).
    - Test (write-event): on rruleToGraphRecurrence → {unsupported}, throws GraphRecurrenceUnsupported BEFORE any network call.
    - Test (send): sendMicrosoftApprovedEmail POSTs /me/sendMail with text body + recipient shape from RESEARCH Pattern 6; thread reply uses createReply+send two-call flow when inReplyToExternalId present (mail.draftFlow capability flag).
    - Test (self-only-gate — string overload): `assertSelfOnly(event, 'user@gmail.com')` STILL works identically to Phase 4; all existing Phase 4 tests pass unchanged (R3 — backward-compatible signature).
    - Test (self-only-gate — IdentitySet overload): `assertSelfOnly(event, {primaryEmail:'user@contoso.com', aliases:['UserX@contoso.com','alias@contoso.com']})` accepts a Graph event whose organizer.emailAddress.address is the alias and attendees are in the set; rejects if any attendee is outside.
    - Test (self-only-gate): isOrganizer flag is honored — when organizer.emailAddress.address is missing but isOrganizer===true, gate accepts (Pitfall 8 fallback).
    - Test (call-site regression — R3): grep test asserts only 2 call sites of `assertSelfOnly` in src/ (the gate's own self-test plus propose.ts); both compile under `tsc --noEmit`.
  </behavior>
  <action>
    These adapters are INTERNAL to microsoft/ — they are not yet wired into the shared chokepoint (that's Plan 05-02a's job). However, they MUST behave identically to how the shared chokepoint will dispatch to them: take a canonical body, throw on lossy recurrence, throw on etag mismatch, never retry approval state. Per L-04-05: do NOT call transitionTo from inside these adapters — that ordering is the chokepoint's responsibility and Plan 05-02a will preserve the L-04-05 fix. Per L-04-06: replace any substring check on event id with `Boolean(before.parentExternalId)` predicate (Pitfall 5). Mail send: 202 Accepted returns no message id → return `{providerMessageId: undefined}`; on thread reply branch (capability flag mail.draftFlow='twoCall'), createReply returns a draft with id, then send on /me/messages/{draftId}/send.

    **Self-only-gate generalization (R3 — overloaded, backward-compatible):**

    Existing signature: `assertSelfOnly(event: SelfOnlyEvent, userEmail: string): void`.

    New signature (overload):
    ```ts
    export interface IdentitySet {
      primaryEmail: string;
      aliases: string[];                                   // additional addresses (UPN, mail, proxyAddresses)
    }

    export function assertSelfOnly(event: SelfOnlyEvent, userEmail: string): void;
    export function assertSelfOnly(event: SelfOnlyEvent, identitySet: IdentitySet): void;
    export function assertSelfOnly(
      event: SelfOnlyEvent,
      userEmailOrSet: string | IdentitySet,
    ): void {
      const idSet: IdentitySet =
        typeof userEmailOrSet === 'string'
          ? { primaryEmail: userEmailOrSet, aliases: [] }
          : userEmailOrSet;
      // ...existing logic, but match against idSet.allAddresses() (case-insensitive).
      // For Graph events: also prefer event.isOrganizer flag when present.
    }
    ```

    `isSelfOnly` gets the same overloaded shape. Internal predicate: `isInIdentitySet(email, idSet) => [primaryEmail, ...aliases].map(toLowerCase).includes(email.toLowerCase())`.

    **Blast radius enumeration (R3 — enumerate up front):** run `grep -rn 'assertSelfOnly' src/main/scheduling/` BEFORE editing. Expected sites: exactly 2 — `src/main/scheduling/self-only-gate.ts` (definition) and `src/main/scheduling/propose.ts` (call site). The propose.ts call site continues passing a string (the Google account email) — NO call-site update required because the string overload preserves existing behavior. For Microsoft, future call sites in Plan 05-02a's generalized propose pipeline pass an IdentitySet loaded from `provider_account.identity_set_json`.

    **Verification (R3):** Task 5's `<verify>` block runs `npx tsc --noEmit` to confirm both overload signatures compile cleanly against all current call sites.

    Self-only gate body update: organizer match is `isInIdentitySet(organizer.emailAddress.address)` OR `event.isOrganizer === true`; attendee check is `attendees.every(a => isInIdentitySet(a.email))` (Pitfall 8). Preserves L-04-08 fix path: when identity set has only one address (Google), behavior is identical to the old emailOnly path.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/integrations/microsoft/write-event.test.ts tests/unit/main/integrations/microsoft/send.test.ts tests/unit/main/integrations/microsoft/self-only-gate-graph.test.ts tests/unit/main/scheduling/self-only-gate.spec.ts --reporter=dot && npx tsc --noEmit</automated>
  </verify>
  <done>Both write paths exercised against MSW; self-only gate is overloaded (string|IdentitySet); Phase 4 Google call sites compile + pass without modification (R3 backward-compat); `tsc --noEmit` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Sync orchestrator + per-account queues + powerMonitor + IPC + Settings UI</name>
  <files>
    src/main/scheduler/queues.ts,
    src/main/ipc/microsoft.ts,
    src/renderer/features/settings/IntegrationsSection.tsx,
    src/renderer/components/StatusPanel.tsx,
    tests/unit/main/scheduler/queues.test.ts,
    tests/unit/main/ipc/microsoft.test.ts,
    tests/unit/renderer/features/settings/IntegrationsSection.test.tsx,
    tests/integration/registry-isolation.test.ts
  </files>
  <behavior>
    - Test (queues): TokenInvalidError thrown inside one account's queue marks ONLY that account as 'needs-auth' (hard-pause); other queues continue ticking.
    - Test (queues): TransientGraphError marks account 'degraded' but queue continues with exponential backoff.
    - Test (queues): on powerMonitor 'suspend' event, scheduled but unstarted cron ticks are cancelled; on 'resume', a one-shot catch-up tick per (account, resource) is enqueued (mirrors Phase 2 XCUT-01 invariant).
    - Test (ipc): connectMicrosoft IPC handler is rate-limited via the new queue; full happy path returns {ok:true, accountId, email}.
    - Test (ipc): syncMicrosoftNow IPC handler enqueues a one-shot tickMail + tickCalendar.
    - Test (renderer): IntegrationsSection now has "Add Outlook account" button alongside existing Gmail rows. (Multi-provider account picker UI is Plan 05-02b's concern; this plan needs only the entry point.)
    - Test (renderer): StatusPanel shows a row per provider_account with status chip (ok/degraded/needs-auth); status pulled from provider_account via IPC.
    - Test (integration): TokenInvalid simulated on Microsoft does NOT pause Google's sync loop — CONTEXT failure-isolation invariant.
    - Test (renderer reachability per L-04-04 / S-04-02): grep test confirms IntegrationsSection is imported by SettingsScreen.tsx AND StatusPanel is imported by an active route.
  </behavior>
  <action>
    Implement queues.ts per RESEARCH Pattern 8 — one PQueue per `${providerKey}:${accountId}`, concurrency 1, intervalCap 2, interval 1000ms. State map per RESEARCH (CONTEXT failure isolation §). On error from fn: instanceof checks TokenInvalidError → state='needs-auth' + mark provider_account; isTransient(err) → state='degraded'; otherwise unset.

    Implement powerMonitor wiring per RESEARCH Polling Cadence § — re-use Phase 2's cronRegistry pattern (don't double-schedule). The orchestrator iterates all provider_account rows on startup; for each, schedules node-cron tasks (5min mail, 15min calendar) — but ONLY for accounts with status='ok'; 'needs-auth' accounts do not schedule until reconnect.

    Implement IPC handlers (microsoft.ts) mirroring google IPC handlers. Wire to renderer IntegrationsSection — add an "Add Outlook account" button that calls connectMicrosoft and surfaces the StatusPanel row on success. Per L-04-04 / S-04-02: after writing IntegrationsSection updates, immediately update SettingsScreen.tsx to import + render the new button block; add an explicit grep test (tests/static/component-reachability.test.ts) asserting that any new *Section.tsx in src/renderer/features/settings/ is imported by SettingsScreen.tsx. Same for StatusPanel rows.

    Per L-04-10: any time/event display in StatusPanel uses `rules.timeZone` for display (not UTC). For Plan 05-01 this is "last_synced_at" timestamps only.

    Linux basic_text safeStorage warning (Open Q 4): on connectMicrosoft, before persisting cache, check safeStorage.getSelectedStorageBackend() and surface the existing Phase 1 warning if 'basic_text'; user must explicitly acknowledge before continue.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/scheduler/queues.test.ts tests/unit/main/ipc/microsoft.test.ts tests/unit/renderer/features/settings/IntegrationsSection.test.tsx tests/integration/registry-isolation.test.ts tests/static/component-reachability.test.ts --reporter=dot</automated>
  </verify>
  <done>User can click "Add Outlook account" → loopback flow → see Outlook row in StatusPanel with 'ok'. Token-invalid on one provider does not affect the other. powerMonitor sleep/wake does not double-schedule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Live tenant smoke — connect + sync + read (ingest-only)</name>
  <what-built>
    All of Tasks 1-6 land. App launches with Phase 4 functionality unchanged. The dev tenant from Task 0 is reachable. Outlook ingest round-trips end-to-end.

    **Scope clarification (M1):** This live smoke verifies INGEST only (mail + calendar list rows in local DB). Outlook SEND and Outlook calendar WRITE-BACK are deferred to Plan 05-02a, when the shared chokepoint dispatches to the Microsoft internal adapters built in Task 5.
  </what-built>
  <how-to-verify>
    1. Launch the app: `npm run dev` (Electron + Vite).
    2. Open Settings → Integrations. Confirm existing Gmail/Google Calendar rows still present and 'ok'. (Phase 4 regression check.)
    3. Click "Add Outlook account". Microsoft loopback page opens in a sandboxed window. Sign in with the dev-tenant user. Consent screen lists Mail.Read, Mail.Send, Calendars.ReadWrite, User.Read, offline_access. Approve.
    4. Window closes. StatusPanel shows a new row: "Outlook · {your-dev-tenant-email}" with status chip 'ok' and "Last synced: just now".
    5. Within 5 minutes (or click "Sync now"), open SQLite shell. Run `SELECT count(*) FROM message WHERE provider_key='microsoft';` — should be >= 1 if dev mailbox has any messages.
    6. Run `SELECT count(*) FROM calendar_event WHERE provider_key='microsoft';` — should be >= 1 if dev calendar has events. Create a recurring event in Outlook web ("Weekly standup Mondays 10am for 4 weeks"); wait 15min OR click Sync now; re-query; row exists with `recurrence_json` containing RRULE:FREQ=WEEKLY;BYDAY=MO;COUNT=4.
    7. In Outlook web, send yourself a test email. Within 5min OR after manual sync, the row shows up in the message table.
    8. Revoke the Aria app's consent. Trigger another sync. StatusPanel Outlook row flips to 'needs-auth'. Gmail row stays 'ok'. (CONTEXT failure-isolation SC.)
    9. Quit app. Restart. Outlook row reappears with same status. Cursor persists.

    Run live integration test (gated by env var):
    `MS_LIVE_TENANT_TEST=1 npx vitest run tests/integration/microsoft/live-tenant.smoke.test.ts`
  </how-to-verify>
  <resume-signal>Type "Outlook ingest live smoke passes; failure-isolation verified" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm test` (full suite) green.
2. `npx vitest run tests/static/component-reachability.test.ts` green (L-04-04 / S-04-02 mitigation).
3. `npx vitest run tests/unit/main/db/migration-011-frozen.spec.ts` green (R2 frozen-schema invariant).
4. `npx tsc --noEmit` green (R3 overloaded signature compiles against existing call sites).
5. `MS_LIVE_TENANT_TEST=1 npx vitest run tests/integration/microsoft/live-tenant.smoke.test.ts` green when dev tenant available.
6. Manual smoke per Task 7 completed.
7. No new top-level deps beyond `@azure/msal-node`, `@microsoft/microsoft-graph-client`, `isomorphic-fetch`, `@types/microsoft-graph` (dev), `@typescript-eslint/typescript-estree` (dev — used by Plan 05-02a).
8. SQLite user_version = 12 after migration; behavior assertion `SELECT count(*) FROM approval WHERE kind IN ('calendar_change','email_send') AND (provider_key IS NULL OR account_id IS NULL) = 0` returns 0 (R5).
9. providerTokens keychain entries written for connected Microsoft account; legacy googleTokens entries unchanged.
10. pino logs do not contain `msalCacheEnc`, `accessToken`, or `refreshToken` substrings on any line.
</verification>

<success_criteria>
1. **Outlook ingest (mail + calendar) round-trips end-to-end** — user connects an Outlook dev-tenant account; mail and events appear in SQLite with provider_key='microsoft' (CAL-02 ingest half, EMAIL-02 ingest half). **Outlook SEND + calendar WRITE-BACK land in Plan 05-02a** (M1 — scope clarified).
2. A recurring Outlook event round-trips Graph → RFC5545 in `recurrence_json` (CAL-08 canonicalization proved).
3. Revoking Outlook consent flips the account to needs-auth without affecting Google (CONTEXT failure isolation; SC-4 from ROADMAP partial).
4. App restart preserves delta cursors; no full-resync on healthy sync (XCUT-01 carry-over).
5. Self-only gate's overloaded signature: string overload preserves Phase 4 behavior (regression-safe); IdentitySet overload accepts an Outlook event organized by the connected user (UPN or alias) and refuses one with an external attendee (R3).
6. Migrations 011 + 012 SQL frozen (R2); approval backfill verified (R5); fresh-install path verified (R1).
7. All RESEARCH Pitfalls 1-8 have an exercised test path.
8. All applicable Phase 4 lessons (L-04-01, L-04-02, L-04-04, L-04-06, L-04-08, L-04-10) honored.
9. EMAIL-02 send path exists at the Graph wire level (sendMicrosoftApprovedEmail unit-tested) but is NOT yet wired into the shared sendApprovedEmail chokepoint — that wiring is Plan 05-02a and completes the EMAIL-02 acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/05-outlook-parity-email-calendar/05-01-SUMMARY.md` per template. Highlight:
- Pitfalls exercised + any new pitfalls discovered during spike
- Any Assumption (A1-A9) downgraded or upgraded
- Migration 011 SHA-256 (frozen baseline for downstream tasks)
- Cross-phase carry-overs (F-1 audit; L-04-09 time-of-day; L-04-05 transition race — flagged for Plan 05-02a as the right place to fix)
</output>

<cross_phase_notes>
- F-2 (L-04-05 transition race): Plan 05-02a generalizes the chokepoints with the safer transition-after-write ordering AND a 'sending' interim state for email_send (B2 — duplicate-send hazard mitigation).
- F-4 (L-04-09 time-of-day fidelity): Phase 5 propose flow re-uses Phase 4 parseIntent. Same bug inherits. Recommend Phase 4.5 fix OR explicit followup task in Plan 05-03.
- F-9 (verifier reachability grep): Plan 05-01 ships the project-local test (component-reachability.test.ts); tooling-wide gsd-sdk fix is separate.
- L-04-07 (4-shot prompting for small models): no LLM call in Plan 05-01 directly; Plan 05-03 unified-view does not invoke LLM either. parseIntent prompt unchanged in this phase.
</cross_phase_notes>
</content>
</invoke>