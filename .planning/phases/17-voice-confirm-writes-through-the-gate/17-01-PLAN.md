---
phase: 17-voice-confirm-writes-through-the-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db/migrations/137_approval_cancelled_state.sql
  - src/main/db/migrations/embedded.ts
  - src/main/approvals/state.ts
  - src/shared/ipc-contract.ts
  - src/main/ipc/voice.ts
  - src/main/voice/prefs.ts
  - src/main/ipc/approvals.ts
  - src/renderer/features/approvals/ApprovalCard.tsx
autonomous: true
requirements:
  - VOICE-11
  - VOICE-05
  - VOICE-08
  - VOICE-09

must_haves:
  truths:
    - "transitionTo(db, id, 'cancelled') succeeds without SQLite CHECK constraint violation (D-11)"
    - "ApprovalState union and ALLOWED map both include 'cancelled' as terminal state (D-11)"
    - "CHANNELS object contains VOICE_CONFIRM_APPROVAL, VOICE_CANCEL_APPROVAL, VOICE_GET_PREFS, VOICE_SET_PREFS (D-04/D-09/D-16)"
    - "handler-count invariant stays green: Object.keys(CHANNELS).length == registered handlers (D-04/D-16)"
    - "voice/prefs.ts exposes speed/voiceId/useCloud/cloudAudio.consented/cloudAudio.consentedAt KV keys (D-16)"
    - "ApprovalCard treats 'cancelled' as a terminal state (isTerminal) (D-11)"
    - "DEFAULT_LIST_STATES in ipc/approvals.ts includes 'cancelled' for audit visibility (D-11)"
    - "embedded.ts canonical DDL matches migration 137 'cancelled' CHECK (D-11)"
  artifacts:
    - path: "src/main/db/migrations/137_approval_cancelled_state.sql"
      provides: "SQLite table-rebuild adding 'cancelled' to state CHECK constraint"
      contains: "PRAGMA legacy_alter_table=ON"
    - path: "src/main/approvals/state.ts"
      provides: "ApprovalState union + 'cancelled' terminal transition"
      exports: ["ApprovalState", "APPROVAL_STATES", "assertTransition"]
    - path: "src/shared/ipc-contract.ts"
      provides: "4 new CHANNELS + CHANNEL_METHODS + AriaApi method signatures"
      contains: "VOICE_CONFIRM_APPROVAL"
    - path: "src/main/ipc/voice.ts"
      provides: "4 stub handlers for new channels (handler-count invariant)"
  key_links:
    - from: "src/main/db/migrations/137_approval_cancelled_state.sql"
      to: "src/main/db/migrations/embedded.ts"
      via: "canonical DDL must match migration 137 state CHECK"
      pattern: "'cancelled'"
    - from: "src/shared/ipc-contract.ts CHANNELS"
      to: "src/main/ipc/voice.ts registerVoiceHandlers"
      via: "every CHANNELS entry needs exactly one handler"
      pattern: "VOICE_CONFIRM_APPROVAL.*handle"
---

<objective>
Wave 0 contract foundation: migration 137 adds 'cancelled' to the approval state machine; new IPC channels land with stub handlers so the handler-count invariant stays green; voice prefs are extended for D-16; ApprovalCard and approvals IPC list acknowledge 'cancelled'.

Purpose: Everything that MUST exist before any write-path voice code can run. The 'cancelled' state machine change is structural — any Phase 17 feature code that cancels a staged approval will throw at runtime unless this lands first. The 4 new CHANNELS must land together with handlers so tests/unit/main/ipc/index.spec.ts does not regress.

Output:
- 137_approval_cancelled_state.sql (migration)
- embedded.ts updated (canonical new-install DDL)
- state.ts with 'cancelled' terminal state
- ipc-contract.ts with 4 new CHANNELS + CHANNEL_METHODS + AriaApi entries
- ipc/voice.ts with 4 stub handlers (upgraded to real in later plans)
- voice/prefs.ts extended with speed/voiceId/useCloud/cloudAudio.* keys
- ipc/approvals.ts DEFAULT_LIST_STATES includes 'cancelled'
- ApprovalCard.tsx isTerminal includes 'cancelled'
</objective>

<execution_context>
@C:/Users/HomePC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-CONTEXT.md
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-RESEARCH.md
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-PATTERNS.md

<interfaces>
<!-- Exact contracts the executor needs — extracted from codebase. -->

From src/main/approvals/state.ts (current):
```typescript
export type ApprovalState =
  | 'pending' | 'generating' | 'ready' | 'approved'
  | 'rejected' | 'snoozed' | 'interrupted' | 'sent'
  | 'sending' | 'failed' | 'needs-operator-decision';

// Current ALLOWED ready entry (must add 'cancelled'):
ready: ['approved', 'rejected', 'snoozed'],
```

From src/shared/ipc-contract.ts CHANNELS (Phase 16 tail, lines 192–200):
```typescript
VOICE_TTS_CHUNK: 'aria:voice:tts-chunk',
VOICE_ABORT: 'aria:voice:abort',
DIAGNOSTICS_VOICE_LATENCY: 'aria:diagnostics:voice-latency',
VOICE_FEED_ANSWER: 'aria:voice:feed-answer',
VOICE_LATENCY_MARK: 'aria:voice:latency-mark',
```

From src/shared/ipc-contract.ts CHANNEL_METHODS (Phase 16 tail, lines 1511–1516):
```typescript
VOICE_TTS_CHUNK: 'onVoiceTtsChunk',
VOICE_ABORT: 'voiceAbort',
DIAGNOSTICS_VOICE_LATENCY: 'diagnosticsVoiceLatency',
VOICE_FEED_ANSWER: 'voiceFeedAnswer',
VOICE_LATENCY_MARK: 'voiceLatencyMark',
```

From src/main/db/migrations/134_voice_explicit_path.sql (verbatim template for migration 137):
The file is the exact template. state CHECK currently ends at 'needs-operator-decision'. Only change: add ,'cancelled' to the list.

From src/main/voice/prefs.ts (Phase 15, current KV keys):
KEY_PREFIX = 'voice.'
type VoicePrefKey = 'modelReady' | 'modelPath' | 'modelState'

From src/main/ipc/approvals.ts DEFAULT_LIST_STATES (~line 38):
const DEFAULT_LIST_STATES: ApprovalUiState[] = [ 'pending', 'generating', 'ready', 'approved',
  'sending', 'failed', 'needs-operator-decision', 'interrupted', 'snoozed' ];

From src/renderer/features/approvals/ApprovalCard.tsx (~line 241):
const isTerminal = row.state === 'sent' || row.state === 'failed';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 137 + state.ts 'cancelled' state machine</name>
  <files>
    src/main/db/migrations/137_approval_cancelled_state.sql,
    src/main/db/migrations/embedded.ts,
    src/main/approvals/state.ts
  </files>
  <behavior>
    - assertTransition('ready', 'cancelled') succeeds (no throw)
    - assertTransition('cancelled', 'approved') throws invalid-transition
    - 'cancelled' is not in ALLOWED['approved'] or any other non-ready list
    - APPROVAL_STATES array includes 'cancelled'
  </behavior>
  <action>
    Create src/main/db/migrations/137_approval_cancelled_state.sql using src/main/db/migrations/134_voice_explicit_path.sql as the verbatim template. The ONLY difference from migration 134: add ,'cancelled' to the state CHECK constraint list (after 'needs-operator-decision'). All columns, all 5 indexes, PRAGMA ordering, INSERT...SELECT column list — everything else identical. The state CHECK becomes: 'pending','generating','ready','approved','rejected','snoozed','interrupted','sent','sending','failed','needs-operator-decision','cancelled'.

    Update src/main/db/migrations/embedded.ts: find the approval table DDL block (the one matching migration 134, around line 1534) and add ,'cancelled' to the same state CHECK constraint in the embedded DDL. This is the canonical new-install schema — it MUST match migration 137.

    Update src/main/approvals/state.ts: add | 'cancelled' to the ApprovalState union (after 'needs-operator-decision'). Change ready entry in ALLOWED map from ['approved', 'rejected', 'snoozed'] to ['approved', 'rejected', 'snoozed', 'cancelled']. Add terminal entry cancelled: []. APPROVAL_STATES updates automatically via Object.keys(ALLOWED). Do NOT add 'cancelled' to any other ALLOWED entry — it is terminal.

    Tests: create tests/unit/main/approvals/state.spec.ts with: assertTransition('ready','cancelled') does not throw; assertTransition('cancelled','approved') throws with message matching /invalid-transition/; APPROVAL_STATES includes 'cancelled'.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/approvals/state.spec.ts --no-file-parallelism</automated>
  </verify>
  <done>state.spec.ts: 3 assertions green. migration 137 SQL file exists with PRAGMA legacy_alter_table=ON and 'cancelled' in state CHECK. embedded.ts contains 'cancelled' in approval state CHECK. state.ts ALLOWED['ready'] includes 'cancelled'; ALLOWED['cancelled'] = [].</done>
</task>

<task type="auto">
  <name>Task 2: New IPC channels + voice prefs extension + consumer updates</name>
  <files>
    src/shared/ipc-contract.ts,
    src/main/ipc/voice.ts,
    src/main/voice/prefs.ts,
    src/main/ipc/approvals.ts,
    src/renderer/features/approvals/ApprovalCard.tsx
  </files>
  <action>
    STEP A — src/shared/ipc-contract.ts: Append 4 entries to the CHANNELS const after VOICE_LATENCY_MARK (Phase 16 tail). Comment block: "Phase 17 — Voice-Confirm + Writes Through the Gate (17-01)". Entries: VOICE_CONFIRM_APPROVAL: 'aria:voice:confirm-approval', VOICE_CANCEL_APPROVAL: 'aria:voice:cancel-approval', VOICE_GET_PREFS: 'aria:voice:get-prefs', VOICE_SET_PREFS: 'aria:voice:set-prefs'. Add corresponding CHANNEL_METHODS entries after VOICE_LATENCY_MARK: VOICE_CONFIRM_APPROVAL: 'voiceConfirmApproval', VOICE_CANCEL_APPROVAL: 'voiceCancelApproval', VOICE_GET_PREFS: 'voiceGetPrefs', VOICE_SET_PREFS: 'voiceSetPrefs'. Add AriaApi method signatures after the Phase 16 methods (~line 1093): voiceConfirmApproval(req: { approvalId: string }): Promise<{ ok: true } | { error: string }>; voiceCancelApproval(req: { approvalId: string }): Promise<{ ok: true } | { error: string }>; voiceGetPrefs(): Promise<VoicePrefsDto>; voiceSetPrefs(patch: Partial<VoicePrefsPatchDto>): Promise<VoicePrefsDto | { error: string }>;. Export VoicePrefsDto and VoicePrefsPatchDto interfaces: VoicePrefsDto = { speed: number; voiceId: string; useCloud: boolean }; VoicePrefsPatchDto = { speed?: number; voiceId?: string; useCloud?: boolean }.

    STEP B — src/main/voice/prefs.ts: Extend VoicePrefKey type to add 'speed' | 'voiceId' | 'useCloud' | 'cloudAudio.consented' | 'cloudAudio.consentedAt'. Add VOICE_PREF_DEFAULTS: { speed: 1.0, voiceId: '', useCloud: false }. Add getVoicePrefs(db: Db | null): VoicePrefsDto that reads speed/voiceId/useCloud from KV (parseFloat for speed, '1'==='1' for useCloud boolean). Add writeVoicePref(db: Db, key: VoicePrefKey, value: string): void — thin wrapper over existing writeStr. Preserve all existing Phase-15 functions (getModelReadiness, setModelReadiness, etc.) unchanged.

    STEP C — src/main/ipc/voice.ts: Add 4 STUB handlers in registerVoiceHandlers for VOICE_CONFIRM_APPROVAL, VOICE_CANCEL_APPROVAL, VOICE_GET_PREFS, VOICE_SET_PREFS. These stubs return { error: 'NOT_IMPLEMENTED' } (VOICE_CONFIRM_APPROVAL, VOICE_CANCEL_APPROVAL) or VOICE_PREF_DEFAULTS (VOICE_GET_PREFS) or { error: 'NOT_IMPLEMENTED' } (VOICE_SET_PREFS). Real implementations land in Plans 04 (prefs) and 05 (confirm/cancel). Stubs MUST be registered so handler-count stays green NOW.

    STEP D — src/main/ipc/approvals.ts: Add 'cancelled' to DEFAULT_LIST_STATES array (per D-11 for audit visibility). Cast as ApprovalUiState after verifying the type union accepts it (it will once state.ts is updated).

    STEP E — src/renderer/features/approvals/ApprovalCard.tsx: Find the isTerminal line (~line 241) and add || row.state === 'cancelled'. This makes 'cancelled' rows non-interactive (no approve/reject buttons).

    No code fences needed: these are precise additive changes following established patterns from PATTERNS.md.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism</automated>
  </verify>
  <done>
    handler-count invariant test green (Object.keys(CHANNELS).length === registered handlers count, now +4 from Phase 16 baseline).
    ipc-contract.ts has VOICE_CONFIRM_APPROVAL, VOICE_CANCEL_APPROVAL, VOICE_GET_PREFS, VOICE_SET_PREFS in CHANNELS, CHANNEL_METHODS, and AriaApi.
    voice/prefs.ts exports getVoicePrefs and writeVoicePref.
    ApprovalCard.tsx isTerminal includes 'cancelled'.
    pnpm typecheck shows 0 new errors above 84-error baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema layer → application | SQLite CHECK constraint is the DB-level enforcement; state.ts assertTransition is the app-level enforcement; both must agree |
| renderer → main (IPC) | New channels accept untrusted renderer payloads; stubs return static errors — no write risk at this plan level |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | migration 137 table-rebuild | mitigate | Use PRAGMA legacy_alter_table=ON to prevent dangling FK references (per reference_sqlite_rename_fk_rewrite memory); run foreign_key_check after migration in integration test |
| T-17-02 | Elevation of Privilege | 'cancelled' state terminal | accept | Terminal state: ALLOWED['cancelled']=[] means no further transitions possible; assertApproved already throws 'not-approved' for non-approved states |
| T-17-03 | Denial of Service | handler-count invariant | mitigate | 4 stub handlers registered atomically in same plan so index.spec.ts never sees miscount between commits |
</threat_model>

<verification>
Run in order after task completion:

1. Migration round-trip (integration):
   pnpm vitest run tests/integration/voice-confirm.spec.ts --no-file-parallelism
   (This spec is scaffolded in Plan 07 — at this wave, verify via state.spec.ts)

2. State machine unit test:
   npx vitest run tests/unit/main/approvals/state.spec.ts --no-file-parallelism

3. Handler-count invariant:
   npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism

4. Typecheck (0 new errors above 84-error baseline):
   pnpm typecheck 2>&1 | tail -5
</verification>

<success_criteria>
- migration 137 SQL exists and uses PRAGMA legacy_alter_table=ON + table-rebuild pattern verbatim from migration 134
- embedded.ts canonical DDL includes 'cancelled' in approval state CHECK
- state.ts: ApprovalState includes 'cancelled'; ALLOWED['ready'] includes 'cancelled'; ALLOWED['cancelled'] = []
- ipc-contract.ts: 4 new CHANNELS + CHANNEL_METHODS + AriaApi entries for VOICE_CONFIRM_APPROVAL, VOICE_CANCEL_APPROVAL, VOICE_GET_PREFS, VOICE_SET_PREFS
- voice/prefs.ts: speed/voiceId/useCloud/cloudAudio.* KV keys; getVoicePrefs() exported
- ipc/voice.ts: 4 stub handlers registered; handler-count invariant green
- ipc/approvals.ts: 'cancelled' in DEFAULT_LIST_STATES
- ApprovalCard.tsx: isTerminal includes 'cancelled'
- pnpm typecheck: 0 new errors
- tests/unit/main/approvals/state.spec.ts: green
- tests/unit/main/ipc/index.spec.ts: green
</success_criteria>

<output>
After completion, create .planning/phases/17-voice-confirm-writes-through-the-gate/17-01-SUMMARY.md
</output>
