---
phase: 17-voice-confirm-writes-through-the-gate
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/rag/ask-service.ts
  - src/main/ipc/ask.ts
autonomous: true
requirements:
  - VOICE-09

must_haves:
  truths:
    - "performAsk() in ask-service.ts contains all inner routing logic extracted from ipc/ask.ts (D-02)"
    - "ipc/ask.ts handler becomes a thin wrapper calling performAsk() with the same DI deps (D-02)"
    - "ask.spec.ts passes UNCHANGED after extraction — zero test file modifications (D-02)"
    - "classifyFrontierError helper is co-located with or re-exported from ask-service.ts (D-02)"
    - "VoiceIntentRouter can import performAsk from rag/ask-service.ts without crossing the preload bridge (SC1)"
  artifacts:
    - path: "src/main/rag/ask-service.ts"
      provides: "performAsk() extracted service function + AskServiceDeps interface + classifyFrontierError"
      exports: ["performAsk", "AskServiceDeps", "classifyFrontierError"]
    - path: "src/main/ipc/ask.ts"
      provides: "thin handler wrapper that constructs AskServiceDeps and calls performAsk()"
    - path: "tests/unit/main/rag/ask-service.spec.ts"
      provides: "Direct behavioral coverage of performAsk() LOCAL/FRONTIER/fallback/writeLog paths"
  key_links:
    - from: "src/main/ipc/ask.ts"
      to: "src/main/rag/ask-service.ts"
      via: "import { performAsk } from '../rag/ask-service'"
      pattern: "performAsk"
    - from: "tests/unit/main/ipc/ask.spec.ts"
      to: "src/main/ipc/ask.ts"
      via: "DI interface AskDeps must remain unchanged at handler boundary"
      pattern: "AskDeps"
---

<objective>
Extract the inner logic of ipc/ask.ts into a standalone ask-service.ts so VoiceIntentRouter can call it in-process without re-crossing the preload bridge (SC1/VOICE-09/D-02). This is a pure refactor — the ask.spec.ts tests must pass unmodified.

Purpose: The VoiceIntentRouter needs to handle 'ask' domain intents by calling the exact same function the ASK_ARIA IPC handler calls, not by firing window.aria.askAria() (which would cross the preload bridge). The extraction creates a shared performAsk() that both the IPC handler and the router import.

Output:
- src/main/rag/ask-service.ts (new) — performAsk() + AskServiceDeps + classifyFrontierError
- src/main/ipc/ask.ts (modified) — thin wrapper over performAsk(); entitlement gate stays
- tests/unit/main/rag/ask-service.spec.ts (new) — direct behavioral spec for performAsk()
</objective>

<execution_context>
@C:/Users/HomePC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-CONTEXT.md
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-RESEARCH.md
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-PATTERNS.md

<interfaces>
<!-- Full current src/main/ipc/ask.ts — the extraction source of truth. -->

From src/main/ipc/ask.ts (complete file, read 2026-06-08):

AskDeps interface (lines 48–59):
```typescript
export interface AskDeps {
  logger: Logger;
  dbHolder: DbHolder;
  router?: LLMRouter;
  getLocalModelFn?: typeof getLocalModel;
  getFrontierModelFn?: typeof getFrontierModel;
  generateTextFn?: typeof generateText;
}
```

classifyFrontierError helper (lines 61–77):
Classifies error as 'network' | 'auth' | 'rate-limited-or-down'.
Checks FrontierUnavailableError, ENOTFOUND/ECONNREFUSED/ETIMEDOUT, HTTP status codes.

Handler body structure (lines 93–247):
1. Entitlement gate (stays in handler — NOT in service)
2. Payload parse: prompt, source, startedAt
3. router.classify({ prompt, source }) → RoutingDecision
4. writeLog closure capturing db, promptHashValue, logger
5. LOCAL path: localModelFactory() → gen() → writeLog ok:1 → return answer
6. FRONTIER path + LOCAL fallback (two-level try/catch)

What STAYS in ipc/ask.ts after extraction:
- AskDeps interface (must remain for ask.spec.ts)
- Entitlement gate (assertEntitled call)
- Thin wrapper: construct deps, call performAsk(deps, prompt, source, startedAt)

What MOVES to ask-service.ts:
- classifyFrontierError (private helper — moves with the logic)
- router construction (lines 80–92): new LLMRouter + factory wiring
- Payload parse: prompt, source, startedAt
- router.classify() call (step 3 above)
- writeLog closure (step 4)
- LOCAL path (step 5)
- FRONTIER path + fallback (step 6)
- Return type: { answer: string; route: Route; reason: string; latency_ms: number } | { error: string }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ask-service.ts with extracted performAsk() + direct behavioral spec</name>
  <files>src/main/rag/ask-service.ts, tests/unit/main/rag/ask-service.spec.ts</files>
  <behavior>
    - performAsk with LOCAL route returns { answer, route: 'LOCAL', reason, latency_ms }
    - performAsk with FRONTIER route returns { answer, route: 'FRONTIER', reason, latency_ms }
    - performAsk when router.classify throws NoLlmProviderError returns { error: 'no-llm-provider' }
    - performAsk when LOCAL gen throws returns { error: 'ollama-unreachable' } or reason string
    - performAsk when FRONTIER gen throws falls back to LOCAL (LLM-05)
    - writeLog is called with prompt_hash (not raw prompt) for every path
  </behavior>
  <action>
    Create src/main/rag/ask-service.ts. The file starts with the same import block as ipc/ask.ts (lines 14–46): generateText from 'ai'; LLMRouter, NoLlmProviderError, RoutingDecision from llm/router; getLocalModel, getFrontierModel, OllamaUnavailableError, FrontierUnavailableError, DEFAULT_LOCAL_MODEL, defaultModelIdFor from llm/providers; writeRoutingLog, hashPrompt, RoutingLogInput from llm/routingLog; Route from shared/ipc-contract; type Logger from pino.

    Export the AskServiceDeps interface — this is a SUPERSET of AskDeps fields that the service needs directly (not the dbHolder wrapper, but a lazy accessor):
    export interface AskServiceDeps {
      logger: Logger;
      dbGetter: () => import('better-sqlite3-multiple-ciphers').Database | null;
      router: LLMRouter;
      localModelFactory: typeof getLocalModel;
      frontierModelFactory: typeof getFrontierModel;
      gen: typeof generateText;
    }

    Export classifyFrontierError function (verbatim from lines 61–77 of ask.ts).

    Export performAsk(deps: AskServiceDeps, prompt: string, source: string | undefined, startedAt: number): Promise returning AskResponse | IpcError shape. The body is the inner logic from ask.ts lines 113–244: hashPrompt, router.classify, writeLog closure, LOCAL path, FRONTIER path + fallback. The entitlement gate is NOT included — it stays in the handler.

    AskResponse and IpcError types: import from shared/ipc-contract. Return type is the same union: { answer: string; route: Route; reason: string; latency_ms: number } | { error: string }.

    The key: AskServiceDeps uses dbGetter: () => Db | null (lazy accessor) rather than dbHolder: DbHolder. This lets ask.ts pass () => dbHolder.db as the accessor, preserving the same lazy pattern.

    Do NOT export AskDeps — that stays in ipc/ask.ts.

    CREATE tests/unit/main/rag/ask-service.spec.ts: a direct behavioral spec for performAsk(). Use vi.fn() stubs for all AskServiceDeps fields. Tests must assert the behavior cases listed in the behavior block above:
    - Stub router.classify returning 'LOCAL' decision; stub gen returning an answer string; call performAsk → assert result has route='LOCAL', answer matches, latency_ms is a number.
    - Stub router.classify returning 'FRONTIER' decision; stub gen returning an answer; call performAsk → assert route='FRONTIER'.
    - Stub router.classify throwing NoLlmProviderError; call performAsk → assert result.error === 'no-llm-provider'.
    - Stub router.classify returning 'LOCAL'; stub gen throwing OllamaUnavailableError; call performAsk → assert result has error string containing 'ollama-unreachable' or similar.
    - Stub router.classify returning 'FRONTIER'; stub gen (first call) throwing FrontierUnavailableError; stub gen (second call, the fallback) returning an answer; call performAsk → assert result.route === 'LOCAL' (fallback succeeded).
    - In all success paths: assert that writeRoutingLog was called exactly once with a payload whose prompt_hash is a string (not the raw prompt text).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/rag/ask-service.spec.ts --no-file-parallelism</automated>
  </verify>
  <done>tests/unit/main/rag/ask-service.spec.ts green. Exports AskServiceDeps, performAsk, classifyFrontierError. LOCAL/FRONTIER/fallback/writeLog behaviors all pass. No TypeScript errors in this file.</done>
</task>

<task type="auto">
  <name>Task 2: Thin-wrap ipc/ask.ts handler to call performAsk()</name>
  <files>src/main/ipc/ask.ts</files>
  <action>
    Modify src/main/ipc/ask.ts to become a thin wrapper. The AskDeps interface (lines 48–59) MUST remain UNCHANGED — ask.spec.ts depends on it. The classifyFrontierError function is REMOVED from ask.ts (it now lives in ask-service.ts). Add import { performAsk, AskServiceDeps, classifyFrontierError } from '../rag/ask-service'; at the top (or just performAsk since classifyFrontierError is no longer used directly in the handler).

    The registerAskHandlers function body changes: keep lines 80–92 (router construction, localModelFactory, frontierModelFactory, gen wiring — these deps form AskServiceDeps). The ipcMain.handle block becomes:
    1. Entitlement gate (lines 97–108) — unchanged
    2. Payload parse (lines 109–114) — unchanged
    3. Call performAsk with AskServiceDeps: { logger, dbGetter: () => dbHolder.db, router, localModelFactory: localModelFactory as typeof getLocalModel, frontierModelFactory: frontierModelFactory as typeof getFrontierModel, gen: gen as typeof generateText }, prompt, source, performance.now()
    4. Return the result

    The handler no longer contains any routing logic — just the entitlement gate + dep construction + performAsk call.

    Preservation constraint: The existing tests mock deps.router, deps.getLocalModelFn, deps.getFrontierModelFn, deps.generateTextFn via the AskDeps interface. These field names must map through to AskServiceDeps fields: getLocalModelFn → localModelFactory, getFrontierModelFn → frontierModelFactory, generateTextFn → gen. Since the test overrides register via AskDeps (dbHolder/router/getLocalModelFn/getFrontierModelFn/generateTextFn), the handler constructs the AskServiceDeps from those same deps fields. The mapping is: localModelFactory = deps.getLocalModelFn ?? getLocalModel; frontierModelFactory = deps.getFrontierModelFn ?? getFrontierModel; gen = deps.generateTextFn ?? generateText. This preserves the test injection surface exactly.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/ipc/ask.spec.ts --no-file-parallelism</automated>
  </verify>
  <done>ask.spec.ts passes all test cases UNCHANGED (zero modifications to the spec file). ipc/ask.ts no longer contains routing logic — only entitlement gate + performAsk call. pnpm typecheck shows 0 new errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC handler → service | Entitlement gate MUST stay in the handler, not in performAsk — service is callable in-process without the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Elevation of Privilege | performAsk entitlement bypass | mitigate | Entitlement gate assertEntitled('rag_ask') stays in ipc/ask.ts handler; performAsk has no gate (voice path has its own entitlement). The VoiceIntentRouter call to performAsk is an in-process call — the existing voice entitlement gate in voice IPC handlers covers it. |
| T-17-05 | Info Disclosure | prompt_hash vs raw prompt | accept | writeLog uses hashPrompt(prompt) — raw prompt never written to routing_log. Preserved verbatim from ask.ts. |
</threat_model>

<verification>
1. Ask-service behavioral spec (Task 1 target):
   npx vitest run tests/unit/main/rag/ask-service.spec.ts --no-file-parallelism

2. Ask wrapper spec passes unmodified (Task 2 target):
   npx vitest run tests/unit/main/ipc/ask.spec.ts --no-file-parallelism

3. Typecheck no new errors:
   pnpm typecheck 2>&1 | tail -5

4. ask-service.ts exports are importable (no circular dep):
   grep -r "ask-service" src/main/ (should show ipc/ask.ts import only at this wave)
</verification>

<success_criteria>
- src/main/rag/ask-service.ts exists with performAsk(), AskServiceDeps, classifyFrontierError exported
- tests/unit/main/rag/ask-service.spec.ts green (LOCAL/FRONTIER/fallback/writeLog paths asserted)
- ipc/ask.ts is a thin wrapper: entitlement gate + performAsk() call; no routing logic
- tests/unit/main/ipc/ask.spec.ts green with ZERO changes to the test file
- pnpm typecheck: 0 new errors above baseline
</success_criteria>

<output>
After completion, create .planning/phases/17-voice-confirm-writes-through-the-gate/17-02-SUMMARY.md
</output>
