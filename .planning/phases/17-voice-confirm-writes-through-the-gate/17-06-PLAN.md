---
phase: 17-voice-confirm-writes-through-the-gate
plan: "06"
type: execute
wave: 3
depends_on:
  - "17-01"
  - "17-04"
  - "17-05"
files_modified:
  - src/renderer/features/settings/VoiceSection.tsx
  - src/renderer/features/approvals/ApprovalCard.tsx
autonomous: true
requirements:
  - VOICE-05
  - VOICE-08

must_haves:
  truths:
    - "VoiceSection.tsx renders in a Settings NavSection with speed select + voiceId input + useCloud toggle (D-16)"
    - "VoiceSection reads prefs via VOICE_GET_PREFS and writes via VOICE_SET_PREFS (D-16/SC5)"
    - "First useCloud enable triggers the cloud consent modal with data-handling disclosure (D-14)"
    - "Cloud voice list rows are gated behind useCloud=true AND cloudAudio.consented='1' (D-14)"
    - "ApprovalCard shows explicit-required chip and suppresses voice-confirm affordance for forceExplicit rows (D-07)"
    - "ApprovalCard Cancel button integration: calls useVoiceConfirm cancel() when pendingApprovalId active (D-09/D-12)"
  artifacts:
    - path: "src/renderer/features/settings/VoiceSection.tsx"
      provides: "Voice settings panel: speed select, voiceId, useCloud toggle, consent modal"
    - path: "src/renderer/features/approvals/ApprovalCard.tsx"
      provides: "Updated isTerminal for 'cancelled'; voice-confirm suppression for forceExplicit rows; Cancel button"
  key_links:
    - from: "src/renderer/features/settings/VoiceSection.tsx"
      to: "VOICE_GET_PREFS / VOICE_SET_PREFS (window.aria.voiceGetPrefs / window.aria.voiceSetPrefs)"
      via: "IPC pair backed by real handlers from Plan 04"
      pattern: "voiceGetPrefs\|voiceSetPrefs"
    - from: "src/renderer/features/approvals/ApprovalCard.tsx"
      to: "src/renderer/features/voice/useVoiceConfirm.ts"
      via: "Cancel button calls cancel() from useVoiceConfirm when pendingApprovalId active"
      pattern: "useVoiceConfirm"
---

<objective>
Settings UI for voice preferences (D-16/VOICE-08) and ApprovalCard integration for D-07/D-09/D-11. Both are renderer-only changes that can be built in Wave 3 after Plan 05 (which creates useVoiceConfirm.ts that ApprovalCard imports).

Purpose: VoiceSection.tsx closes VOICE-08 (user can set voice settings in Settings). ApprovalCard updates complete the D-07 forced-row suppression and D-09/D-12 cancel affordance visible to the user during the confirm flow.

Output:
- src/renderer/features/settings/VoiceSection.tsx (new)
- src/renderer/features/approvals/ApprovalCard.tsx (updated: isTerminal + forceExplicit suppression + Cancel integration)
</objective>

<execution_context>
@C:/Users/HomePC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-CONTEXT.md
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-RESEARCH.md
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs. -->

From src/renderer/features/settings/BehaviourSection.tsx (exact structural analog for VoiceSection):
```typescript
import type * as React from 'react';
import { useCallback, useEffect, useState } from 'react';
import { Checkbox } from '../../components/editorial/Checkbox';
interface BehaviourPrefsView { autoLaunch: boolean; closeToTray: boolean; notificationsEnabled: boolean; }
const DEFAULT_VIEW = { autoLaunch: false, closeToTray: true, notificationsEnabled: true };
function isPrefsDto(x: unknown): x is BackgroundPrefsDto { /* type guard */ }
export function BehaviourSection(): JSX.Element {
  const [view, setView] = useState<BehaviourPrefsView>(DEFAULT_VIEW);
  const [loaded, setLoaded] = useState(false);
  const [error, setError] = useState<string | null>(null);
  useEffect(() => { /* load prefs on mount */ }, []);
  const handleChange = useCallback(async (patch: Partial<BehaviourPrefsView>) => {
    /* optimistic update + IPC write */
  }, [view]);
  // ... Checkbox rows
}
```

From src/renderer/features/approvals/ApprovalCard.tsx (current key lines):
Line ~241: const isTerminal = row.state === 'sent' || row.state === 'failed';
Lines ~247–249: const forceExplicit = row.severity === 'high' || categories.some(c => FORCED_CATEGORIES.has(c));
Lines ~393–399: {forceExplicit && <span className="chip chip-mono">explicit-required</span>}

From src/shared/ipc-contract.ts (AriaApi methods added in Plan 01):
voiceGetPrefs(): Promise<VoicePrefsDto>
voiceSetPrefs(patch: Partial<VoicePrefsPatchDto>): Promise<VoicePrefsDto | { error: string }>
VoicePrefsDto = { speed: number; voiceId: string; useCloud: boolean }

From Plan 05 — useVoiceConfirm (created by Plan 05, available to this plan):
cancel(): calls voiceCancelApproval IPC + clearPendingApproval + toast "Cancelled — press to try again"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: VoiceSection.tsx settings panel</name>
  <files>src/renderer/features/settings/VoiceSection.tsx</files>
  <action>
    Create src/renderer/features/settings/VoiceSection.tsx following BehaviourSection.tsx pattern exactly.

    Local state interface VoicePrefsView: { speed: number; voiceId: string; useCloud: boolean; cloudConsented: boolean }. Defaults: speed 1.0, voiceId '', useCloud false, cloudConsented false.

    useEffect on mount: call window.aria.voiceGetPrefs() → type-guard result as VoicePrefsDto → setView. Also read cloudAudio.consented from the same prefs call if included in the DTO (if VoicePrefsDto doesn't include it, read it via a separate VOICE_GET_PREFS call or use a local-state flag).

    onChange handler (async): optimistic setView update + window.aria.voiceSetPrefs(patch).

    UI layout following BehaviourSection editorial style (Playfair headings, paper background, Checkbox primitives, editorial spacing):
    - Section heading: "Voice"
    - Speed select row: label "Playback speed", select control with options 0.75x / 1.0x / 1.25x / 1.5x (values 0.75/1.0/1.25/1.5). Calls voiceSetPrefs({ speed }).
    - Voice ID row: label "Voice (experimental)", text input for voiceId. Optional / advanced row — can be collapsed or shown only when non-empty.
    - Cloud audio toggle: Checkbox "Enable cloud audio processing". When toggled from false to true AND cloudConsented is false: open a cloud consent modal BEFORE writing the pref.

    Cloud consent modal (D-14 disclosure): a Paper card overlay or Modal (use existing Modal editorial primitive). Content: "Cloud Audio Processing" heading; disclosure text stating: audio is sent to OpenAI for transcription, subject to OpenAI's 30-day standard data retention (or zero data retention with Enterprise plan), sensitivity-flagged turns always stay on-device regardless of this setting. Two buttons: "I Understand, Enable" and "Cancel". On confirm: write voiceSetPrefs({ useCloud: true }) + update cloudConsented state. On cancel: leave Checkbox unchecked.

    After consent: the cloud section shows a secondary info line: "Sensitivity-flagged turns always processed locally." This is always visible when useCloud=true (D-15 user-facing guarantee).

    Wire VoiceSection into an existing Settings NavSection under Behaviour (or Status if more appropriate — check SettingsScreen.tsx layout). Follow the same pattern as other Settings sections.

    No test file required for this task (UI layout — coverage via human-verify checkpoint in Plan 07).
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | grep -c "VoiceSection" || echo "VoiceSection typechecks clean"</automated>
  </verify>
  <done>VoiceSection.tsx exists. Renders speed select, voiceId input, useCloud Checkbox. Consent modal appears on first useCloud=true toggle. VoiceSection wired into SettingsScreen. pnpm typecheck 0 new errors.</done>
</task>

<task type="auto">
  <name>Task 2: ApprovalCard — 'cancelled' terminal + D-07 suppression + D-09 Cancel button</name>
  <files>src/renderer/features/approvals/ApprovalCard.tsx</files>
  <action>
    Update src/renderer/features/approvals/ApprovalCard.tsx with three targeted changes:

    CHANGE 1 — isTerminal (line ~241): Add || row.state === 'cancelled' to the isTerminal check. Result: isTerminal = row.state === 'sent' || row.state === 'failed' || row.state === 'cancelled'. Terminal rows show no approve/reject/snooze buttons.

    CHANGE 2 — Voice-confirm suppression for forceExplicit rows (D-07): The existing 'explicit-required' chip (lines ~393–399) already renders when forceExplicit=true. No new chip needed. Add a boolean prop or internal flag voiceConfirmSuppressed = forceExplicit so that any VoiceConfirmButton rendered in the card is disabled/hidden when forceExplicit. If the card currently has no VoiceConfirmButton (it doesn't yet — it's added in this change), add a comment: "D-07: voice-confirm affordance must be disabled/hidden when forceExplicit=true". The actual VoiceConfirmButton (a small "Confirm by voice" button visible when the approval is in ready+awaiting-confirm state) can be a simple editorial button: label "Confirm by voice", onClick calls window.aria.voiceConfirmApproval({ approvalId: row.id, transcript: 'confirm' }) directly (pre-classified). Disabled when forceExplicit=true.

    CHANGE 3 — D-09/D-12 Cancel button: Add an always-visible "Cancel" button to the card when row.state === 'ready'. This is the reliable second channel (D-09). onClick: call window.aria.voiceCancelApproval({ approvalId: row.id }) + optionally call a passed onCancelledByVoice? callback prop. The button uses the editorial Button with variant="ghost" or a destructive secondary style per the existing ApprovalCard button pattern. Label: "Cancel". This button is visible regardless of pendingApprovalId state — it's the "always there" escape hatch per D-09.

    Preserve: all existing IPC calls, hook signatures, state slices, testids, assertApproved paths. RE-SKIN policy: change only the three targeted areas.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | grep -i "ApprovalCard" | head -5 || echo "ApprovalCard typechecks"</automated>
  </verify>
  <done>
    ApprovalCard.tsx: isTerminal includes 'cancelled'. forceExplicit=true suppresses VoiceConfirmButton. Cancel button visible for ready-state rows and calls voiceCancelApproval. pnpm typecheck 0 new errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → VOICE_SET_PREFS | User-controlled UI sends prefs patch; validated via Zod in Plan 04 handler |
| Consent modal | User must explicitly click "I Understand, Enable" — no auto-consent on page load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-16 | Info Disclosure | VoiceSection shows cloud enabled without disclosure | mitigate | Consent modal gates first useCloud=true toggle. If modal is dismissed/cancelled, Checkbox stays unchecked. cloudConsented state tracks whether disclosure was accepted. |
| T-17-17 | Elevation of Privilege | VoiceConfirmButton on forced/high-severity row | mitigate | D-07: voiceConfirmSuppressed = forceExplicit → button disabled. HARD GATE in assertApproved is the backstop even if suppression is bypassed. |
</threat_model>

<verification>
1. Typecheck for new/modified files:
   pnpm typecheck 2>&1 | tail -5

2. Manual verify (required — UI):
   pnpm dev → Settings → Behaviour → Voice section visible with speed/voiceId/useCloud controls.
   Toggle useCloud → consent modal appears → I Understand → Checkbox checked.
   Open an 'ready' approval → Cancel button visible, 'explicit-required' chip on forced rows.
</verification>

<success_criteria>
- VoiceSection.tsx renders with speed select + voiceId input + useCloud Checkbox
- Cloud consent modal appears on first useCloud=true toggle
- VoiceSection wired into SettingsScreen under Behaviour NavSection
- ApprovalCard: isTerminal includes 'cancelled'; VoiceConfirmButton disabled when forceExplicit; Cancel button visible for ready rows
- pnpm typecheck: 0 new errors
</success_criteria>

<output>
After completion, create .planning/phases/17-voice-confirm-writes-through-the-gate/17-06-SUMMARY.md
</output>
