---
phase: 17-voice-confirm-writes-through-the-gate
plan: "07"
type: execute
wave: 4
depends_on:
  - "17-03"
  - "17-05"
  - "17-06"
files_modified:
  - tests/static/voice-streaming-no-write.spec.ts
  - tests/integration/voice-write-path.spec.ts
  - tests/integration/voice-confirm.spec.ts
autonomous: false
requirements:
  - VOICE-05
  - VOICE-08
  - VOICE-09
  - VOICE-11

must_haves:
  truths:
    - "D-17 ratchet updated: 'voiceConfirm' removed from WRITE_CHOKEPOINTS; raw write chokepoints still banned from voice/**  (D-17)"
    - "Static ratchet description updated to reflect Phase 17 write-capability (D-17)"
    - "voice-write-path.spec.ts integration test proves voice→stage→confirm→assertApproved→write path (SC2)"
    - "voice-confirm.spec.ts covers: ready→cancelled via VOICE_CANCEL_APPROVAL (SC3/D-11); assertApproved throws for cancelled rows; migration 137 FK check clean"
    - "All four automated SC validations have passing specs before human-verify checkpoint"
    - "D-01 through D-17 all covered by passing automated tests or explicitly named human-verify items"
  artifacts:
    - path: "tests/static/voice-streaming-no-write.spec.ts"
      provides: "Updated ratchet: voiceConfirm removed from WRITE_CHOKEPOINTS; raw chokepoints still banned (D-17)"
    - path: "tests/integration/voice-write-path.spec.ts"
      provides: "End-to-end voice→stage→voiceConfirm→assertApproved→write integration test (SC2)"
    - path: "tests/integration/voice-confirm.spec.ts"
      provides: "Cancel path + migration 137 round-trip (SC3/D-11)"
  key_links:
    - from: "tests/static/voice-streaming-no-write.spec.ts"
      to: "src/main/voice/voice-intent-router.ts"
      via: "ratchet scans src/main/voice/** — verifies router does NOT import raw chokepoints"
      pattern: "WRITE_CHOKEPOINTS"
    - from: "tests/integration/voice-write-path.spec.ts"
      to: "src/main/voice/confirm.ts"
      via: "voiceConfirm() called via VOICE_CONFIRM_APPROVAL handler → assertApproved in write chokepoint"
      pattern: "voiceConfirm"
---

<objective>
Safety net: D-17 ratchet update + integration tests that prove the full voice write path works and the cancel path terminates at 'cancelled'. This is the final automated gate before the human-verify checkpoint.

Purpose: The ratchet update (D-17) closes the Phase 16 read-only ratchet for write-capability by removing 'voiceConfirm' from the banned list while keeping raw write chokepoints banned from voice modules. The integration tests give the human-verify checkpoint hard evidence that the voice→write pipeline is functional.

Output:
- tests/static/voice-streaming-no-write.spec.ts (updated D-17)
- tests/integration/voice-write-path.spec.ts (new — SC2 end-to-end)
- tests/integration/voice-confirm.spec.ts (extended — SC3 cancel + migration round-trip)

Then: human-verify checkpoint for SC2/SC3/SC4/SC5 live acoustic flows.
</objective>

<execution_context>
@C:/Users/HomePC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HomePC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-CONTEXT.md
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-VALIDATION.md
@.planning/phases/17-voice-confirm-writes-through-the-gate/17-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs for these tests. -->

From tests/static/voice-streaming-no-write.spec.ts (current Phase 16 state):
```typescript
const WRITE_CHOKEPOINTS = [
  'sendApprovedEmail',
  'applyCalendarChange',
  'pushApprovedMeetingActions',
  'assertApproved',
  'voiceConfirm',    // ← REMOVE this entry (D-17)
] as const;
// Scan dirs: src/main/voice/** and src/renderer/features/voice/**
// Exclusion: confirm.ts (it IS the chokepoint implementation)
```

From src/main/voice/confirm.ts (Phase 14):
```typescript
export function voiceConfirm(db: Db, approvalId: string): void {
  transitionTo(db, approvalId, 'approved', { approval_path: 'voice-explicit' });
}
```

Migration 137 pattern (for integration test setup):
Use better-sqlite3-multiple-ciphers. Apply embedded.ts migrations in order up to 137.
After applying: INSERT an approval row with state='ready', then call voiceConfirm(db, id).
Verify: SELECT state FROM approval WHERE id=? → 'approved'; SELECT approval_path → 'voice-explicit'.
Then: call transitionTo(db, id, 'cancelled') on a NEW ready row — verify state='cancelled'.
Run: PRAGMA foreign_key_check (must return empty).

From tests/integration/voice-confirm.spec.ts (partially scaffolded in Plan 05):
Extend with: migration 137 FK check; 'cancelled' insert accepted by CHECK constraint; assertApproved throws 'not-approved' for cancelled row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-17 ratchet update</name>
  <files>tests/static/voice-streaming-no-write.spec.ts</files>
  <action>
    Update tests/static/voice-streaming-no-write.spec.ts per the minimal change specified in RESEARCH Pattern 6 and PATTERNS.md:

    Remove 'voiceConfirm' from the WRITE_CHOKEPOINTS const. The remaining entries stay: 'sendApprovedEmail', 'applyCalendarChange', 'pushApprovedMeetingActions', 'assertApproved'.

    Update the describe() block description to reflect Phase 17 write-capability. Replace current description with: "Phase 17 voice modules stage approvals via voiceConfirm (allowed) but must never directly call raw write chokepoints". This makes the ratchet intent clear: voiceConfirm is the allowed seam; the raw chokepoints are still banned from voice modules.

    Add a comment before WRITE_CHOKEPOINTS explaining the D-17 boundary: "voiceConfirm is intentionally NOT banned here (Phase 17 D-17): it is the approved voice seam, called from ipc/voice.ts which is OUTSIDE the scan scope (src/main/voice/**). The raw write chokepoints below remain banned from all voice modules."

    Keep the confirm.ts file exclusion unchanged.

    Verify the test still correctly fails if any of the 4 remaining chokepoints are imported by a voice module.
  </action>
  <verify>
    <automated>npx vitest run tests/static/voice-streaming-no-write.spec.ts --no-file-parallelism</automated>
  </verify>
  <done>voice-streaming-no-write.spec.ts green. 'voiceConfirm' not in WRITE_CHOKEPOINTS. Raw write chokepoints (sendApprovedEmail, applyCalendarChange, pushApprovedMeetingActions, assertApproved) still banned from voice/**. voice-intent-router.ts passes the scan (imports no banned identifiers).</done>
</task>

<task type="auto">
  <name>Task 2: voice-write-path integration test (SC2) + extend voice-confirm.spec.ts (SC3)</name>
  <files>
    tests/integration/voice-write-path.spec.ts,
    tests/integration/voice-confirm.spec.ts
  </files>
  <action>
    CREATE tests/integration/voice-write-path.spec.ts. Use better-sqlite3-multiple-ciphers with in-memory DB. Apply embedded migrations (from embedded.ts) to create the schema including migration 137. This tests the full voice→stage→voiceConfirm→assertApproved pipeline end-to-end.

    Test setup: create helper createTestDb() that runs all embedded migrations on an in-memory db. Create an approval row with insertApproval(db, { kind: 'email_send', idempotencyKey: 'test-vc-1', ...defaults }).

    Test "voice confirm path — ready→approved with voice-explicit path":
    1. Insert a 'ready' approval row (state='ready', approval_path='explicit')
    2. Call voiceConfirm(db, row.id)
    3. Read the row: verify state='approved', approval_path='voice-explicit'
    4. Call assertApproved(db, row.id) — must NOT throw (row is approved)

    Test "forced category row — voice confirm blocked at assertApproved":
    1. Insert a 'ready' approval row with categories_json containing 'legal' + approval_path='explicit'
    2. Simulate voiceConfirm stamp: transitionTo(db, id, 'approved', { approval_path: 'voice-explicit' })
    3. Call assertApproved(db, id) — must throw ApprovalGateError with code matching /voice-forbidden-forced/

    Test "cancel path — ready→cancelled":
    1. Insert a 'ready' approval row
    2. Call transitionTo(db, id, 'cancelled')
    3. Read the row: verify state='cancelled'
    4. Call assertApproved(db, id) — must throw with message matching /not-approved/

    EXTEND tests/integration/voice-confirm.spec.ts (scaffolded in Plan 05): add:
    Test "migration 137 FK check": after running all migrations including 137, execute PRAGMA foreign_key_check — result must be empty (no dangling FKs). This is the Plan 01 mandate.
    Test "'cancelled' CHECK constraint accepted": INSERT ... VALUES('cancelled') in a raw db.prepare INSERT — must succeed without throwing.
    Test "assertTransition('ready','cancelled') from state.ts": call assertTransition('ready','cancelled') — must not throw; call assertTransition('cancelled','approved') — must throw invalid-transition.

    Both spec files use the same createTestDb() helper — extract to a shared fixture in tests/helpers/db-fixtures.ts or inline per spec.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/voice-write-path.spec.ts tests/integration/voice-confirm.spec.ts --no-file-parallelism</automated>
  </verify>
  <done>voice-write-path.spec.ts: voiceConfirm test passes; forced-category assertApproved test throws voice-forbidden-forced; cancel-path test passes. voice-confirm.spec.ts: FK check clean; 'cancelled' CHECK accepts insert; state machine transitions correct.</done>
</task>

</tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 7 plans complete. Voice write path is live:
    - D-01/D-03: VoiceIntentRouter stages approvals (not writes)
    - D-02: ask-service extraction — ask.spec.ts passing unchanged
    - D-04/D-06: VOICE_CONFIRM_APPROVAL wired to voiceConfirm → assertApproved
    - D-05: Read-back template uses resolved ApprovalRow fields
    - D-09/D-10/D-11: Cancel flow: PTT/Cancel button → 'cancelled' state
    - D-12: Toast "Cancelled — press to try again"
    - D-13/D-15: Cloud STT via experimental_transcribe + sensitivity gate
    - D-14: Consent KV recorded (action_audit_log is VIEW — settings KV only, noted in code)
    - D-16: VOICE_GET/SET_PREFS + VoiceSection in Settings
    - D-17: Ratchet updated — voiceConfirm allowed; raw chokepoints banned
  </what-built>
  <how-to-verify>
    1. SC1 — Voice drives triage/schedule/draft/ask via same in-process services:
       Say "ask Aria what my priorities are today" while PTT held.
       Aria should answer using the same RAG path as /ask — no preload bridge crossing.

    2. SC2 — Read-back + dual-channel confirm:
       Say "schedule a meeting for Thursday at 3pm" (or "draft a reply to the last email from X").
       Aria should: (a) stage an approval row, (b) speak the resolved entity read-back ("Move event to Thu Jun 11 3:00 PM. Say yes to confirm..."), (c) re-arm mic for confirmation.
       Say "yes" — verify the action executes (calendar change / draft staged).
       The on-screen ApprovalCard should show the staged row while awaiting confirm.

    3. SC3 — Cancel mid-confirm:
       Trigger a write command. When Aria is reading back the action, press PTT (or click Cancel on the ApprovalCard).
       Verify: action does NOT execute; approval row state becomes 'cancelled'; toast "Cancelled — press to try again" appears.

    4. SC4 — Cloud opt-in + sensitivity stays local:
       Settings → Voice → Enable cloud audio processing → consent modal → I Understand.
       Speak a normal command — verify it goes to cloud STT (check logs or network tab).
       Then speak something sensitive (mention "salary" or "legal matter").
       Verify: sensitive turn routes local despite cloud opt-in enabled (check voice_latency_log or add ARIA_DEBUG=1).

    5. SC5 — Voice settings per-turn:
       Change speed to 1.5x in Settings → Voice. Start a voice answer.
       Aria's TTS playback should be noticeably faster.
       Change useCloud to false mid-session and start a new turn — routes local.

    6. Forced/high-severity row suppression (D-07):
       Find or create an approval with categories=['legal'] or severity='high'.
       Verify: ApprovalCard shows 'explicit-required' chip; voice-confirm affordance is absent or disabled.

    Type "approved" to mark SC1–SC6 passing, or describe any issues for gap closure.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which SC items need gap closure</resume-signal>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static ratchet → voice module source | ratchet reads source files directly — no runtime trust boundary; correctness by text scan |
| integration test → in-memory SQLite | tests run with real DB schema; no network calls; isolated per test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-18 | Repudiation | No audit trail for voice-confirm actions | mitigate | approval.approval_path='voice-explicit' on approved rows provides audit trail. voice-confirm.spec.ts verifies the stamp. |
| T-17-19 | Elevation of Privilege | Ratchet removal of voiceConfirm allowing bypass | mitigate | voiceConfirm removed from WRITE_CHOKEPOINTS only because it is called from ipc/voice.ts (outside scan scope). voice-intent-router.ts is inside scan scope and still bans raw chokepoints — verified by updated ratchet test. |
</threat_model>

<verification>
1. D-17 ratchet green:
   npx vitest run tests/static/voice-streaming-no-write.spec.ts --no-file-parallelism

2. Integration tests green:
   npx vitest run tests/integration/voice-write-path.spec.ts tests/integration/voice-confirm.spec.ts --no-file-parallelism

3. Full suite + typecheck before human-verify:
   npx vitest run
   pnpm typecheck 2>&1 | tail -5

4. Handler count invariant (final check):
   npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism
</verification>

<success_criteria>
- voice-streaming-no-write.spec.ts green with voiceConfirm removed from WRITE_CHOKEPOINTS
- voice-write-path.spec.ts green: all 3 test cases (confirm, forced-block, cancel-path)
- voice-confirm.spec.ts green: FK check, CHECK constraint, state machine transitions
- Full vitest suite green (no regressions)
- pnpm typecheck: 0 new errors above 84-error baseline
- Human-verify checkpoint: all 6 SC items confirmed
</success_criteria>

<output>
After completion, create .planning/phases/17-voice-confirm-writes-through-the-gate/17-07-SUMMARY.md
</output>
