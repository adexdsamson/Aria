---
phase: 09-implement-product-ui-from-anthropic-design-system-design-ref
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/app/theme/globals.css
  - src/renderer/app/theme/tokens.ts
  - src/renderer/assets/fonts/.gitkeep
  - src/renderer/components/editorial/Logo.tsx
  - src/renderer/components/editorial/MonogramSquare.tsx
  - src/renderer/components/editorial/Avatar.tsx
  - src/renderer/components/editorial/StatusDot.tsx
  - src/renderer/components/editorial/RouteBadge.tsx
  - src/renderer/components/editorial/KbdHint.tsx
  - src/renderer/components/editorial/LabelRule.tsx
  - src/renderer/components/editorial/Card.tsx
  - src/renderer/components/editorial/Button.tsx
  - src/renderer/components/editorial/Input.tsx
  - src/renderer/components/editorial/Modal.tsx
  - src/renderer/components/editorial/index.ts
  - package.json
autonomous: true
requirements: []
tags: [ui, design-system, phase-9]
must_haves:
  truths:
    - "Editorial CSS variables (--ivory, --ink, --gold, --rule, etc.) are defined in globals.css alongside legacy --aria-* variables"
    - "Playfair Display, Source Sans 3, IBM Plex Mono load from bundled woff2 files via Fontsource, NOT from Google Fonts CDN"
    - "Editorial primitive components (MonogramSquare, Avatar, StatusDot, Button, Card, Input, Modal, LabelRule, RouteBadge, KbdHint, Logo) exist and export from src/renderer/components/editorial/index.ts"
    - "A new <AppLogo> component renders the monogram squircle + Playfair wordmark per the design study V (editorial lockup)"
  artifacts:
    - path: "src/renderer/app/theme/globals.css"
      provides: "Editorial CSS variables + .smallcaps / .label-rule / .btn / .card primitive classes per 09-DESIGN-TOKENS.md"
      contains: "--ivory: #FAFAF8"
    - path: "src/renderer/components/editorial/index.ts"
      provides: "Barrel export for editorial primitives"
      exports: ["MonogramSquare", "Avatar", "StatusDot", "Button", "Card", "Input", "Modal", "LabelRule", "RouteBadge", "KbdHint", "AppLogo"]
    - path: "package.json"
      provides: "@fontsource/playfair-display, @fontsource/source-sans-3, @fontsource/ibm-plex-mono dependencies"
      contains: "@fontsource"
  key_links:
    - from: "src/renderer/app/theme/globals.css"
      to: "Fontsource @font-face declarations"
      via: "@import '@fontsource/...' at top of file"
      pattern: "@fontsource"
    - from: "src/renderer/components/editorial/index.ts"
      to: "individual primitive files"
      via: "re-export"
      pattern: "export.*MonogramSquare"
---

<objective>
Establish the editorial design-system foundation: tokens, locally-bundled fonts,
primitive CSS classes, and a small React component library that every other
Phase 9 plan consumes. After this plan ships, all subsequent plans can import
from `@/renderer/components/editorial` and use `var(--ink)` / `var(--gold)` /
etc. — no bespoke styles per screen, no font-CDN requests.

Purpose: Single source of truth for the new visual language so 09-02..09-05
become consistent re-skins instead of five separate restyling exercises.
Output: globals.css with editorial tokens + classes, 11 primitive components
under `components/editorial/`, three Fontsource packages installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-CONTEXT.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-DESIGN-TOKENS.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/shared.css
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-shell.jsx
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/logo.html
@src/renderer/app/theme/globals.css
@src/renderer/app/theme/tokens.ts

<interfaces>
Existing globals.css already defines `--aria-bg`, `--aria-fg`, `--aria-accent`,
`--aria-border`, `--aria-gray-*`, `--aria-type-*`, `--aria-radius-*`,
`--aria-space-*` under `:root` plus a `prefers-color-scheme: dark` override.
Plan 09-01 ADDS the editorial variables alongside (D-13). Do not remove the
`--aria-*` variables — they still drive Tailwind theme extension via
tokens.ts and are referenced from feature files that will be cleaned in later
plans.

The dark-mode override on `--aria-*` MUST be left untouched. The editorial
palette is a single light theme; no dark variant for Phase 9.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install bundled fonts and add editorial tokens to globals.css</name>
  <files>package.json, src/renderer/app/theme/globals.css, src/renderer/assets/fonts/.gitkeep</files>
  <action>
    1. Add Fontsource font packages (D-09): run `pnpm add @fontsource/playfair-display@latest @fontsource/source-sans-3@latest @fontsource/ibm-plex-mono@latest`. These are runtime deps (not devDependencies).
    2. Create directory `src/renderer/assets/fonts/` with an empty `.gitkeep` (placeholder; Fontsource serves the woff2 files from node_modules so we don't need to copy them — verify Vite bundles them at build).
    3. At the TOP of `src/renderer/app/theme/globals.css`, BEFORE the existing `@tailwind` directives, add these imports (one per weight we actually use — keep the import surface tight):
       ```
       @import '@fontsource/playfair-display/400.css';
       @import '@fontsource/playfair-display/400-italic.css';
       @import '@fontsource/playfair-display/500.css';
       @import '@fontsource/playfair-display/600.css';
       @import '@fontsource/playfair-display/700.css';
       @import '@fontsource/source-sans-3/300.css';
       @import '@fontsource/source-sans-3/400.css';
       @import '@fontsource/source-sans-3/500.css';
       @import '@fontsource/source-sans-3/600.css';
       @import '@fontsource/source-sans-3/700.css';
       @import '@fontsource/ibm-plex-mono/300.css';
       @import '@fontsource/ibm-plex-mono/400.css';
       @import '@fontsource/ibm-plex-mono/500.css';
       @import '@fontsource/ibm-plex-mono/600.css';
       ```
    4. Under the existing `:root` block, ADD the editorial palette variables verbatim per 09-DESIGN-TOKENS.md (all colors, all type vars, all radii, all motion vars). Do NOT touch the existing `--aria-*` declarations (D-13).
    5. After the variables block, ADD the editorial primitive class declarations verbatim from shared.css: `.display`, `.smallcaps`, `.smallcaps-gold`, `.smallcaps-ink`, `.label-rule` + descendants, `.container`, `.container-wide`, `.hr`, `.hr-strong`, `.btn`, `.btn-primary`, `.btn-outline`, `.btn-ghost`, `.card`, `.card-accent-top`, `.card-hover`, `:focus-visible`, `.muted`, `.ink`, `.gold`, `.tnum`, `.italic`, `.serif`, `.mono`, `.sans`, `.dropcap::first-letter`, `.fleuron`, `::selection`. Also add the heading defaults (`h1`–`h4`) but scope them under `:where(.editorial)` to avoid bleeding into legacy screens until 09-03 ships them. (Once 09-02 swaps `<body>` to carry the `editorial` class, the headings apply globally.)
    6. Add the scrollbar styling (`::-webkit-scrollbar*` per shared.css). Do not slim them down to the `.app-root` 8px variant yet — that lands in 09-02 with the shell.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run --reporter=dot src/renderer/app/theme 2>&amp;1 || true &amp;&amp;
      grep -c -- "--ivory" src/renderer/app/theme/globals.css | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -c -- "--gold" src/renderer/app/theme/globals.css | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -c "@fontsource/playfair-display" src/renderer/app/theme/globals.css | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -c "@fontsource" package.json | awk '{ if ($1 &gt;= 3) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -c -- "--aria-bg" src/renderer/app/theme/globals.css | awk '{ if ($1 &gt;= 1) print "OK kept --aria-*"; else { print "FAIL --aria-* were removed"; exit 1 } }'
    </automated>
  </verify>
  <done>
    Editorial CSS variables + primitive classes coexist with `--aria-*` in globals.css. Three Fontsource packages declared as dependencies. `pnpm typecheck` clean. Existing `--aria-*` variables UNCHANGED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port editorial React primitives under src/renderer/components/editorial/</name>
  <files>
    src/renderer/components/editorial/Logo.tsx,
    src/renderer/components/editorial/MonogramSquare.tsx,
    src/renderer/components/editorial/Avatar.tsx,
    src/renderer/components/editorial/StatusDot.tsx,
    src/renderer/components/editorial/RouteBadge.tsx,
    src/renderer/components/editorial/KbdHint.tsx,
    src/renderer/components/editorial/LabelRule.tsx,
    src/renderer/components/editorial/Card.tsx,
    src/renderer/components/editorial/Button.tsx,
    src/renderer/components/editorial/Input.tsx,
    src/renderer/components/editorial/Modal.tsx,
    src/renderer/components/editorial/index.ts,
    src/renderer/components/editorial/__tests__/primitives.test.tsx
  </files>
  <behavior>
    - MonogramSquare(size=28): renders an ivory squircle with Playfair "A" + gold underline rule. size prop scales letter (62% of size) and radius (22% of size).
    - Avatar(initials, size=28, gold=false): renders a circle with mono-spaced initials. Ink fill by default, gold fill when `gold` prop is true.
    - StatusDot(kind: 'ok'|'warn'|'err'|'idle'): renders a 6px dot — moss / gold / rose / gray-faint — with a soft halo at `1A` (10%) alpha.
    - RouteBadge(route: 'LOCAL'|'FRONTIER'): renders a small mono uppercase pill with the route label. LOCAL = gold bg-tint; FRONTIER = ink bg-tint. (See design-ref `app-screen-briefing.jsx` for visual reference.)
    - KbdHint(children): mono mini-pill, ivory-deep bg, rule border, 10px text, used for "⌘K" and "esc" hints.
    - LabelRule(label, align='center'|'left'): renders a flanking-rule section label per `.label-rule` CSS class. The `align="left"` variant suppresses the leading rule.
    - Card({ accent?: 'top', hover?: boolean, children }): wraps children in `.card`, applies `.card-accent-top` and `.card-hover` modifiers.
    - Button({ variant: 'primary'|'outline'|'ghost', children, ...props }): applies the matching `.btn .btn-*` classes; forwards onClick / disabled / type.
    - Input({ label?, hint?, error?, ...props }): editorial-styled input — 44px tall, paper bg, rule border, focus turns border to gold and outline 2px gold/offset 2px.
    - Modal({ open, onClose, title, children, size='md' }): editorial modal — paper card, 8px radius, top-2px gold accent rule, mono uppercase title-eyebrow + Playfair title. Close on Esc and backdrop click. (Used by Plan 09-05 for DisconnectConfirmDialog re-skin per D-12 — preserves all existing ARIA roles + data-testid attributes.)
    - AppLogo({ variant: 'sidebar'|'header'|'splash' }): renders the appropriate Logo treatment. `sidebar` = MonogramSquare(26) + small Playfair "Aria" + italic "chief of staff". `header` = the editorial lockup (Study V): Aria + italic "Est. 2026" + gold underline + "A Chief of Staff" mono tag. `splash` = the italic Study II treatment.

    Tests (single file `__tests__/primitives.test.tsx` using vitest + @testing-library/react):
    - Test 1: MonogramSquare renders an "A" letter and a span with `--gold` background.
    - Test 2: StatusDot with kind="err" applies `--rose` color.
    - Test 3: RouteBadge with route="LOCAL" renders text "LOCAL" and uses gold-tinted background.
    - Test 4: Button variant="primary" applies both `btn` and `btn-primary` classes; onClick fires.
    - Test 5: Card accent="top" applies `.card-accent-top` class.
    - Test 6: Modal open=false renders nothing; open=true renders title; pressing Escape calls onClose.
    - Test 7: AppLogo variant="sidebar" renders both the monogram (single "A") and the wordmark "Aria"; variant="header" renders "Est. 2026" alongside.
    - Test 8: index.ts re-exports all 11 components (assert each is a function/component reference, not undefined).
  </behavior>
  <action>
    1. Create each component as a small typed function component. Use the inline-style + className patterns from `app-shell.jsx` lines 18-128 verbatim where they exist (MonogramSquare 18-38, Avatar 40-51, StatusDot 53-64) — they're already idiomatic React. Convert the prototype's JS to TypeScript with explicit prop types.
    2. Logo / AppLogo: derive from `design-ref/project/logo.html` Studies III (monogram), V (editorial lockup), II (italic). Composed entirely from CSS-variable styles — no font-CDN, no external images.
    3. Modal: Use `createPortal` to `document.body`. Maintain `role="dialog"` and `aria-modal="true"`. The visual structure: dim backdrop (rgba(26,26,26,0.45) + 2px blur), centered paper card, `--gold` 2px top border, ivory-deep header strip with mono uppercase title-eyebrow, Playfair title below, body, optional footer slot.
    4. `index.ts` re-exports all primitives: `export { MonogramSquare } from './MonogramSquare'; export { Avatar } from './Avatar'; ...`
    5. NEVER import anything from `features/*` here — these primitives have no upstream dependencies. They can be imported by anything, including tests in isolation.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/components/editorial/__tests__/primitives.test.tsx &amp;&amp;
      grep -E "^export \{" src/renderer/components/editorial/index.ts | wc -l | awk '{ if ($1 &gt;= 11) print "OK"; else { print "FAIL exports="$1; exit 1 } }'
    </automated>
  </verify>
  <done>
    All 11 editorial primitives compile, are exported from `index.ts`, and pass the 8 behaviour tests. No primitive imports from `src/renderer/features/`.
  </done>
</task>

</tasks>

<verification>
- Editorial tokens exist alongside `--aria-*` in `globals.css`.
- Fontsource bundles (no Google Fonts CDN request on launch — verifiable by running the dev build and watching DevTools Network tab for `fonts.googleapis.com` hits; there should be zero).
- All 11 primitives exported, tests pass.
- `pnpm typecheck` and `pnpm lint` clean.
- Existing renderer test suite still passes (these primitives have not been wired anywhere yet, so they cannot have regressed anything).
</verification>

<success_criteria>
- Plan 09-02 can `import { AppLogo, MonogramSquare, Avatar, StatusDot, Button, Card, KbdHint } from '../components/editorial'` and use them in `SideNav.tsx` + `Topbar.tsx` without touching any styles.
- Plan 09-03..09-05 can use `var(--ink)`, `var(--gold)`, `.smallcaps`, `.label-rule`, `.card` directly in feature files.
- Zero feature files have been touched yet (re-skin work hasn't started).
</success_criteria>

<output>
After completion, create `.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-01-SUMMARY.md`.
</output>
