---
phase: 09-implement-product-ui-from-anthropic-design-system-design-ref
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - src/renderer/features/settings/SettingsScreen.tsx
  - src/renderer/features/settings/StatusPanel.tsx
  - src/renderer/features/settings/FrontierKeySection.tsx
  - src/renderer/features/settings/OllamaSection.tsx
  - src/renderer/features/settings/IntegrationsSection.tsx
  - src/renderer/features/settings/NewsSourcesSection.tsx
  - src/renderer/features/settings/BriefingSettingsSection.tsx
  - src/renderer/features/settings/DiagnosticsSection.tsx
  - src/renderer/features/settings/RoutingLogPanel.tsx
  - src/renderer/features/settings/AskAriaBox.tsx
  - src/renderer/features/settings/RagIndexSection.tsx
  - src/renderer/features/settings/RagDisconnectedSection.tsx
  - src/renderer/features/settings/SchedulingRulesSection.tsx
  - src/renderer/features/settings/InsightsSection.tsx
  - src/renderer/features/settings/LearnedPreferencesSection.tsx
  - src/renderer/features/settings/UpdatesSection.tsx
  - src/renderer/features/settings/sections/SubscriptionSection.tsx
  - src/renderer/features/diagnostics/RoutingLogScreen.tsx
  - src/renderer/features/entitlement/TrialBanner.tsx
  - src/renderer/features/entitlement/PaywallScreen.tsx
  - src/renderer/features/entitlement/ActivateLicenseForm.tsx
  - src/renderer/features/entitlement/RestoreLicenseSection.tsx
  - src/renderer/features/onboarding/OnboardingWizard.tsx
  - src/renderer/features/onboarding/MnemonicShow.tsx
  - src/renderer/features/onboarding/MnemonicConfirm.tsx
  - src/renderer/features/onboarding/CountrySectorPicker.tsx
  - src/renderer/features/onboarding/UnlockScreen.tsx
  - src/renderer/features/onboarding/RestoreScreen.tsx
  - src/renderer/features/onboarding/BackupRestoreSection.tsx
  - src/renderer/components/DisconnectConfirmDialog.tsx
autonomous: true
requirements: []
tags: [ui, settings, onboarding, entitlement, diagnostics, phase-9]
must_haves:
  truths:
    - "SettingsScreen renders left rail tab list + main panel per design-ref/project/app-screen-settings.jsx — 14 tabs: Status, Frontier key, Local model, Integrations, News sources, Briefing, RAG index, Scheduling rules, Insights, Learned preferences, Subscription, Restore license, Updates, Diagnostics"
    - "RoutingLogScreen (full-page) shows KPIs + route/source/sensitivity filters + log rows in editorial style"
    - "TrialBanner renders above the topbar with escalating info → warn → urgent visual at day 50 / 55 / 59 and clock-skew variant"
    - "PaywallScreen renders editorial paywall with plan card, Stripe subscribe button, license-key entry (ARIA-… format), read-only escape hatches"
    - "OnboardingWizard 4-step flow (Mnemonic show → Mnemonic confirm 3-position challenge → Country/sector picker → Daily password seal) renders editorial typography per design-ref/project/app-screen-onboarding.jsx"
    - "DisconnectConfirmDialog re-skinned with editorial Modal; 3-assertion test contract still passes unchanged"
  artifacts:
    - path: "src/renderer/features/settings/SettingsScreen.tsx"
      provides: "Re-skinned settings shell with 14 tabs"
      contains: "tab"
    - path: "src/renderer/features/entitlement/PaywallScreen.tsx"
      provides: "Re-skinned paywall"
      contains: "paywall"
    - path: "src/renderer/features/onboarding/OnboardingWizard.tsx"
      provides: "Re-skinned 4-step onboarding"
      contains: "onboarding"
    - path: "src/renderer/components/DisconnectConfirmDialog.tsx"
      provides: "Editorial-styled destructive consent dialog"
      contains: "confirm"
  key_links:
    - from: "SettingsScreen.tsx"
      to: "components/editorial"
      via: "import"
      pattern: "from.*components/editorial"
    - from: "DisconnectConfirmDialog.tsx"
      to: "Modal primitive"
      via: "import"
      pattern: "Modal"
---

<objective>
Re-skin remaining surfaces: Settings (14 tabs), Routing log (full-page),
Entitlement (TrialBanner + PaywallScreen + ActivateLicenseForm + Restore),
Onboarding (4 steps + UnlockScreen + RestoreScreen + BackupRestoreSection),
and the DisconnectConfirmDialog primitive.

Purpose: These are lower-traffic but still ship as the v1 user experience.
The DisconnectConfirmDialog and Onboarding ship destructive/sensitive flows
that need the editorial trust voice MORE than the workspace screens, not less.
Output: 29 files re-skinned. All destructive action consent contracts preserved.
All entitlement state-machine behaviour preserved. Onboarding mnemonic +
3-position challenge cryptographic behaviour untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-CONTEXT.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-DESIGN-TOKENS.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-screen-settings.jsx
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-screen-onboarding.jsx
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-screen-diagnostics.jsx
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-entitlement.jsx
@src/renderer/features/settings/SettingsScreen.tsx
@src/renderer/features/onboarding/OnboardingWizard.tsx
@src/renderer/features/entitlement/PaywallScreen.tsx
@src/renderer/components/DisconnectConfirmDialog.tsx

<interfaces>
DisconnectConfirmDialog has 3 existing assertion tests (modal mounts, typed
confirmation required, callback only fires after confirm). The re-skin
preserves all 3 — only visual treatment changes.

OnboardingWizard 4 steps in order: BIP39 12-word mnemonic show → 3-position
challenge confirm → CountrySectorPicker → daily password seal. The seal step
has the "Sealing…" perceived-hang issue logged in memory; do NOT change the
seal logic. Visual: replace the silent spinner with an editorial loading
state (Playfair italic "Sealing your vault…" + mono "5–15s on this machine").

TrialBanner has 4 visual states (info / warn / urgent / clock-skew). The state
selector is in the TweakSelect in design-ref. Code already implements the
state machine; re-skin only.

PaywallScreen is shown when entitlement is locked AND the route is not in
the READ_ONLY_ALLOW_LIST. Read-only escape hatches: links back to the allow-
listed routes. Already implemented; re-skin only.

RoutingLogScreen is BOTH a full-page route (/routing-log) AND a settings
tab (DiagnosticsSection embeds RoutingLogPanel). Re-skin both; share the same
underlying row renderer.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin SettingsScreen shell + all 14 sub-sections + DiagnosticsSection</name>
  <files>
    src/renderer/features/settings/SettingsScreen.tsx,
    src/renderer/features/settings/StatusPanel.tsx,
    src/renderer/features/settings/FrontierKeySection.tsx,
    src/renderer/features/settings/OllamaSection.tsx,
    src/renderer/features/settings/IntegrationsSection.tsx,
    src/renderer/features/settings/NewsSourcesSection.tsx,
    src/renderer/features/settings/BriefingSettingsSection.tsx,
    src/renderer/features/settings/DiagnosticsSection.tsx,
    src/renderer/features/settings/RoutingLogPanel.tsx,
    src/renderer/features/settings/AskAriaBox.tsx,
    src/renderer/features/settings/RagIndexSection.tsx,
    src/renderer/features/settings/RagDisconnectedSection.tsx,
    src/renderer/features/settings/SchedulingRulesSection.tsx,
    src/renderer/features/settings/InsightsSection.tsx,
    src/renderer/features/settings/LearnedPreferencesSection.tsx,
    src/renderer/features/settings/UpdatesSection.tsx,
    src/renderer/features/settings/sections/SubscriptionSection.tsx
  </files>
  <action>
    1. SettingsScreen layout per `app-screen-settings.jsx`:
       - Two-column layout. Left rail (220px wide): Playfair "Settings" + vertical tab list, each tab = NavItem-like editorial row (gold active rail). Group tabs into 4 NavSections: "Status" (Status, Diagnostics), "Connections" (Frontier key, Local model, Integrations, News sources, RAG index), "Behaviour" (Briefing, Scheduling rules, Insights, Learned preferences), "Account" (Subscription, Restore license, Updates). Note: the "Restore license" tab renders `RestoreLicenseSection` imported from `src/renderer/features/entitlement/RestoreLicenseSection.tsx` (NOT `features/settings/`). All other tabs render section components from `features/settings/` or `features/settings/sections/`.
       - Right pane: Playfair tab title + mono eyebrow + section body. Body uses `.container` wrapper at 64rem; cards stack vertically.
    2. For each of the 16 section files, re-skin in place:
       - Replace Tailwind classes with editorial classes/inline-style.
       - Wrap form blocks in `<Card>` with `<LabelRule label="…" align="left" />` headers.
       - Buttons: Button primary for the main action, Button outline for secondary, Button ghost for tertiary.
       - Inputs: editorial Input primitive (44px, paper bg, rule border, gold focus ring).
       - Toggles: editorial switch — small horizontal track, gold when on.
       - Status indicators: StatusDot kinds.
    3. **Specific section notes:**
       - StatusPanel: 4 rows (Ollama, Frontier, Gmail, Calendar — extend if more providers in code), each with StatusDot + mono uppercase label + value + "Reconnect" action when error.
       - IntegrationsSection: per-provider account list with AccountChip, "Add account" button (primary), "Disconnect" button (outline rose-tinted text — invokes DisconnectConfirmDialog).
       - LearnedPreferencesSection: typed tree view of learned prefs + per-field reset (gated by DisconnectConfirmDialog) + paginated signal log.
       - SubscriptionSection: tier badge, plan card, manage / refresh / sign-out (outline buttons), Stripe portal link, clock-skew warning when applicable.
       - UpdatesSection: channel badge (mono "TESTER" / "STABLE"), Check / Download / progress bar / Install &amp; restart, release notes.
       - AskAriaBox (embedded in DiagnosticsSection): editorial Input + Playfair italic placeholder "Ask Aria a question…" + route badge in result.
       - RoutingLogPanel: tabular log with mono columns (timestamp, route, sensitivity, model, cost) + filter chips for route / source / sensitivity. Used by both DiagnosticsSection AND the full-page RoutingLogScreen (Task 2).
    4. **Behaviour invariants:** all IPC, all form validation, all field handlers, all CASA / safeStorage / Stripe / license flows — unchanged. Tab routing inside Settings (URL hash or state) unchanged.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/features/settings 2&gt;&amp;1 | tail -30 &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/features/settings/SettingsScreen.tsx | grep -c "components/editorial" | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      node -e "const fs=require('fs');const files=['StatusPanel','FrontierKeySection','IntegrationsSection','LearnedPreferencesSection','UpdatesSection'];const rx=/components\/editorial|--ink|--gold|--rule|\.card|\.label-rule|\.btn/;for(const f of files){const src=fs.readFileSync('src/renderer/features/settings/'+f+'.tsx','utf8').split('\n').filter(l=&gt;!/^\s*\/\//.test(l)).join('\n');if(!rx.test(src)){console.error('FAIL '+f+'.tsx not re-skinned');process.exit(1);}}console.log('OK all 5 spot-checks pass');"
    </automated>
  </verify>
  <done>
    Settings shell + 16 sections re-skinned. Tab navigation works. All section tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin full-page RoutingLogScreen + Entitlement (TrialBanner, PaywallScreen, ActivateLicenseForm, RestoreLicenseSection) + DisconnectConfirmDialog</name>
  <files>
    src/renderer/features/diagnostics/RoutingLogScreen.tsx,
    src/renderer/features/entitlement/TrialBanner.tsx,
    src/renderer/features/entitlement/PaywallScreen.tsx,
    src/renderer/features/entitlement/ActivateLicenseForm.tsx,
    src/renderer/features/entitlement/RestoreLicenseSection.tsx,
    src/renderer/components/DisconnectConfirmDialog.tsx
  </files>
  <action>
    1. RoutingLogScreen full-page per `app-screen-diagnostics.jsx`:
       - Header: Playfair "Routing log" + mono eyebrow + KPI strip (4 cards: total decisions today, FRONTIER %, LOCAL %, avg cost) — each card with Playfair big number + mono caption.
       - Filter strip: route chips (All / FRONTIER / LOCAL), source chips (All / classifier / hard-rule / fallback), sensitivity chips (All / safe / pii / financial / health-flag).
       - Body: reuse RoutingLogPanel from Task 1 for the actual row rendering.
    2. TrialBanner per `app-entitlement.jsx`:
       - Renders above Topbar (positioned by Layout — Layout already mounts TrialBanner above Topbar if entitlement state is trial-*).
       - 4 visual states:
         - **info** (day 0-49): paper bg, mono uppercase "TRIAL · {days} of 60", italic Playfair "{days} days remaining", small "Subscribe early" Button ghost on the right, dismiss × icon.
         - **warn** (day 50-54): ivory-deep bg with thin gold top rule, gold mono label, "Subscribe" Button outline.
         - **urgent** (day 55-59): rose-tinted bg with rose top rule, rose mono label, Playfair italic "{1-5} days remaining", "Subscribe now" Button primary.
         - **clock-skew**: rose-tinted bg, mono "CLOCK SKEW DETECTED", explanation copy + Help link.
       - Dismissible per-session (existing logic).
    3. PaywallScreen per `app-entitlement.jsx`:
       - Full-screen lock card (paper bg, 8px radius, 2px gold top accent).
       - Hero: Playfair display "Your trial has ended" (or "Subscription required" for pro-locked) + Source Sans subhead.
       - Plan card: Playfair plan name, price (large Playfair italic), 5-bullet feature list with gold ‣ marker, primary "Subscribe with Stripe" button (Button primary, full-width).
       - "Have a license key?" disclosure → ActivateLicenseForm.
       - Below: "Or continue read-only —" + grid of links to allow-listed read-only routes (Briefing, Approvals, Recap, Settings).
    4. ActivateLicenseForm: editorial Input with mono placeholder "ARIA-XXXX-XXXX-XXXX-XXXX", inline format validation (ARIA-…), Activate button primary.
    5. RestoreLicenseSection: paste form + help link.
    6. DisconnectConfirmDialog re-skin:
       - Replace any prior modal markup with editorial Modal primitive from 09-01.
       - Title: Playfair "Disconnect {service}?" with rose top rule.
       - Body: explanation prose (Source Sans), mono "Type the service name to confirm" hint.
       - Input: editorial Input, required pattern match.
       - Footer: Cancel (Button ghost) + Disconnect (Button primary with rose tint when confirm input matches).
       - **3 assertion tests** (mounts on open, typed confirmation required, callback only fires after match) MUST PASS UNCHANGED. This is non-negotiable per D-12.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/features/diagnostics src/renderer/features/entitlement 2&gt;&amp;1 | tail -30 &amp;&amp;
      pnpm test --run src/renderer/components/DisconnectConfirmDialog 2&gt;&amp;1 | tail -20 &amp;&amp;
      node -e "const fs=require('fs');const s=fs.readFileSync('src/renderer/components/__tests__/DisconnectConfirmDialog.test.tsx','utf8');const m=(s.match(/\b(test|it)\s*\(/g)||[]).length;if(m&lt;3){console.error('FAIL only '+m+' test() or it() calls in DisconnectConfirmDialog.test.tsx; D-12 requires ≥3 assertion tests');process.exit(1);}console.log('OK '+m+' assertion tests present');" &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/components/DisconnectConfirmDialog.tsx | grep -c "Modal\|components/editorial" | awk '{ if ($1 &gt;= 1) print "OK editorial Modal wired"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/features/entitlement/PaywallScreen.tsx | grep -c "components/editorial\|--gold\|--ink" | awk '{ if ($1 &gt;= 2) print "OK"; else { print "FAIL"; exit 1 } }'
    </automated>
  </verify>
  <done>
    Routing log full-page, 4-state TrialBanner, PaywallScreen, ActivateLicenseForm, RestoreLicenseSection, DisconnectConfirmDialog all re-skinned. 3-assertion contract preserved. Entitlement state-machine tests pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-skin OnboardingWizard 4 steps + UnlockScreen + RestoreScreen + BackupRestoreSection</name>
  <files>
    src/renderer/features/onboarding/OnboardingWizard.tsx,
    src/renderer/features/onboarding/MnemonicShow.tsx,
    src/renderer/features/onboarding/MnemonicConfirm.tsx,
    src/renderer/features/onboarding/CountrySectorPicker.tsx,
    src/renderer/features/onboarding/UnlockScreen.tsx,
    src/renderer/features/onboarding/RestoreScreen.tsx,
    src/renderer/features/onboarding/BackupRestoreSection.tsx
  </files>
  <action>
    1. OnboardingWizard per `app-screen-onboarding.jsx`:
       - Full window (no sidebar/topbar — D-07 already enforced in 09-02 Task 1).
       - Centered container max-width 36rem.
       - Top-left: AppLogo variant="header" (Editorial Lockup study V).
       - Step rail (right-aligned at top): 4 dots, gold for current, gray-faint for upcoming, moss for complete. Mono labels below each.
       - Step body: Playfair display step title + Source Sans description + step-specific form.
       - Footer: Back (Button ghost) + Continue (Button primary).
    2. MnemonicShow (step 1):
       - Playfair "Your recovery phrase" + mono eyebrow "Step 1 of 4".
       - 12-word grid (4 cols × 3 rows), each word in a paper card with mono index (01, 02, …) + Playfair word.
       - Warning copy: rose-tinted card with mono "IMPORTANT" + Source Sans "Write this down. Aria cannot recover it for you."
       - "I've written it down" Button primary advances. No new behaviour.
    3. MnemonicConfirm (step 2):
       - Three random positions challenged (existing logic).
       - For each position: mono "Word #5" label + editorial Input.
       - On mismatch: rose-tinted inline error.
    4. CountrySectorPicker (step 3): existing country bundle UI restyled with editorial Select-like dropdowns.
    5. Final step (daily password seal — existing in OnboardingWizard.tsx):
       - Editorial password Input + Playfair italic loading state "Sealing your vault…" + mono caption "5–15 seconds on this machine" (per memory `project_aria_onboarding_seal_ux.md` — addresses the perceived-hang).
       - On error: rose-tinted card + "Forgot password?" recovery link → RestoreScreen.
    6. UnlockScreen: same editorial visual as the seal step. After 5 failed attempts, surface the "Forgot password?" link prominently.
    7. RestoreScreen: 12-word mnemonic input grid + Restore button primary.
    8. BackupRestoreSection (in Settings → it's referenced from onboarding too): editorial Card with VACUUM-INTO Backup / Restore buttons.
    9. **Behaviour invariants:** mnemonic generation, scrypt vault sealing, BIP39 validation, 3-position challenge cryptographic checks, VACUUM-INTO backup/restore IPC, country bundle data — all untouched.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/features/onboarding 2&gt;&amp;1 | tail -30 &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/features/onboarding/OnboardingWizard.tsx | grep -c "components/editorial\|AppLogo" | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -c "Sealing\|sealing" src/renderer/features/onboarding/OnboardingWizard.tsx | awk '{ if ($1 &gt;= 1) print "OK perceived-hang fix applied"; else { print "FAIL"; exit 1 } }'
    </automated>
  </verify>
  <done>
    All 7 onboarding files re-skinned. 4-step flow visually editorial. Seal step has explicit loading state addressing perceived-hang. UnlockScreen + RestoreScreen + BackupRestoreSection consistent with onboarding voice.
  </done>
</task>

</tasks>

<verification>
- Snapshot strategy invariant: snapshot tests are updated via `pnpm test -u` AFTER a human spot-check confirms the new snapshot matches the intended design; behavioural tests pass UNMODIFIED — if a behavioural test breaks, fix the re-skin, not the test (D-10).
- `pnpm typecheck`, `pnpm lint`, `pnpm test --run` all green.
- DisconnectConfirmDialog 3 assertion tests pass without modification.
- Manually walk: every Settings tab, the full-page Routing log, TrialBanner (force each state via state setter), PaywallScreen, all 4 onboarding steps, UnlockScreen, RestoreScreen.
- Reachability ratchet: every component in `files_modified` is imported by a Screen which is mounted by a route OR is rendered conditionally by Layout (TrialBanner, PaywallScreen).
</verification>

<success_criteria>
- 14 Settings tabs visually consistent with design-ref.
- TrialBanner shows correctly at each entitlement state.
- Paywall correctly locks non-allow-listed routes.
- Onboarding ships the perceived-hang fix copy.
- DisconnectConfirmDialog still gates destructive flows with the 3-assertion contract intact.
</success_criteria>

<output>
After completion, create `.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-05-SUMMARY.md`.
</output>
