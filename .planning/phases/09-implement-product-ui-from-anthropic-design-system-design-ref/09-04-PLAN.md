---
phase: 09-implement-product-ui-from-anthropic-design-system-design-ref
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - src/renderer/features/meetings/TranscriptCaptureScreen.tsx
  - src/renderer/features/meetings/NoteView.tsx
  - src/renderer/features/meetings/NoteReviewScreen.tsx
  - src/renderer/features/meetings/CitationHighlighter.tsx
  - src/renderer/features/tasks/TasksScreen.tsx
  - src/renderer/features/tasks/TaskRow.tsx
  - src/renderer/features/ask/AskScreen.tsx
  - src/renderer/features/ask/AnswerCard.tsx
  - src/renderer/features/ask/CitationList.tsx
  - src/renderer/features/ask/SourcePreview.tsx
  - src/renderer/features/recap/RecapScreen.tsx
  - src/renderer/features/recap/RecapEditor.tsx
autonomous: true
requirements: []
tags: [ui, screens, meetings, tasks, ask, recap, phase-9]
must_haves:
  truths:
    - "TranscriptCaptureScreen + NoteView + NoteReviewScreen + CitationHighlighter render with editorial styling, highlighted transcript spans + side rail of commitments/decisions per design-ref/project/app-screen-meetings.jsx"
    - "TasksScreen + TaskRow render Todoist + meeting-source unified list with priority pip, due chip, project chip, labels, 'open meeting note' link"
    - "AskScreen + AnswerCard + CitationList + SourcePreview render threads sidebar + per-account filter chips + cited answers with routing badges per design-ref/project/app-screen-ask.jsx"
    - "RecapScreen + RecapEditor render list of past recaps with status pills + 5-section editor (Meetings · Commitments · Wins · Upcoming · What Aria did this week with verbatim audit list) + Save/Finalize/Export DOCX/Export PDF buttons"
    - "All IPC, TipTap editor wiring, citation IPC, and Todoist sync logic unchanged (re-skin only)"
  artifacts:
    - path: "src/renderer/features/meetings/CitationHighlighter.tsx"
      provides: "Re-skinned transcript citation highlighter with editorial visual"
      contains: "highlight"
    - path: "src/renderer/features/recap/RecapEditor.tsx"
      provides: "Re-skinned recap editor with editorial section labels"
      contains: "What Aria did"
    - path: "src/renderer/features/ask/AnswerCard.tsx"
      provides: "Re-skinned cited answer with route badge"
      contains: "AnswerCard"
  key_links:
    - from: "TasksScreen.tsx"
      to: "components/editorial"
      via: "import"
      pattern: "from.*components/editorial"
    - from: "AskScreen.tsx"
      to: "components/editorial"
      via: "import"
      pattern: "from.*components/editorial"
---

<objective>
Re-skin batch 2: Meetings, Tasks, Ask Aria, Recap. These share a common pattern
of "left rail of items / main reading pane" (Meetings transcript + actions rail,
Ask threads + answer reading pane, Recap list + editor pane). Doing them
together keeps that pattern consistent.

Purpose: Heaviest reading screens in the product. They deserve the cleanest
editorial treatment because users will spend the most time here.
Output: Twelve files re-skinned. Existing TipTap, citation, Todoist, RAG IPC
plumbing untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-CONTEXT.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-DESIGN-TOKENS.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-screen-meetings.jsx
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-screen-tasks.jsx
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-screen-ask.jsx
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-screen-recap.jsx
@src/renderer/features/meetings/TranscriptCaptureScreen.tsx
@src/renderer/features/meetings/NoteReviewScreen.tsx
@src/renderer/features/tasks/TasksScreen.tsx
@src/renderer/features/ask/AskScreen.tsx
@src/renderer/features/recap/RecapScreen.tsx

<interfaces>
Q-01 from 09-CONTEXT.md: design shows hover popovers over citations in
Meetings; current CitationHighlighter is inline only. **Default for this
task: keep inline-only behaviour.** If product/user explicitly asks for
hover popovers during execution, treat as a follow-on plan, not Phase 9.

Recap currently has 4 editable sections (Meetings, Commitments, Wins,
Upcoming) PLUS a 5th "What Aria did this week" section composed of editable
narrative + verbatim read-only audit list (per chat3.md). All 5 sections
already exist in code — re-skin only.

Ask screen has threads sidebar + per-provider-account filter chips + cited
answers with routing badges + disambiguation card — all already exist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin Meetings (TranscriptCaptureScreen, NoteView, NoteReviewScreen, CitationHighlighter)</name>
  <files>
    src/renderer/features/meetings/TranscriptCaptureScreen.tsx,
    src/renderer/features/meetings/NoteView.tsx,
    src/renderer/features/meetings/NoteReviewScreen.tsx,
    src/renderer/features/meetings/CitationHighlighter.tsx
  </files>
  <action>
    1. Read each existing file. Inventory IPC, transcript normalization hooks, citation data shape, calendar-event linking heuristic UI hook.
    2. TranscriptCaptureScreen per `app-screen-meetings.jsx`:
       - Header: Playfair "Meeting capture" + mono eyebrow "Paste or upload a transcript"
       - Card containing the paste area (large editorial textarea with Playfair italic placeholder "Paste transcript here, or drop a file…") + format hint mono row ("Supports plain text, VTT, SRT, JSON, Markdown, Otter export").
       - Below paste area: "Link to meeting" selector (autocomplete over calendar events, editorial Input variant), then primary button "Extract action items".
       - MEET-06 no-bot guardrail copy as a smallcaps note at the bottom.
    3. NoteReviewScreen: two-column layout — left = transcript with CitationHighlighter; right = side rail with Playfair "Commitments", "Decisions", "Action items" subsections, each containing extracted items with cited spans (click to scroll the left pane). Top of right rail: "Push as task batch" Button primary (routes to approval queue, no behaviour change).
    4. NoteView (read-only view of a saved note): same two-column layout, no edit affordances.
    5. CitationHighlighter: inline span highlighting only. Highlighted spans get a thin gold underline (1.5px, --gold @ 60% opacity) and a subtle gold-tinted background on hover. **Do NOT add hover popovers** (Q-01 default).
    6. Citation badge inside the rail: mono uppercase "turn 14" or "12:08–12:11" tracking 0.14em, gray.
    7. **Behaviour invariants:** transcript normalization, all 6 format detection paths, calendar-event linking heuristic, MEET-06 guardrail enforcement, push-as-task-batch IPC — all untouched.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/features/meetings 2&gt;&amp;1 | tail -30 &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/features/meetings/NoteReviewScreen.tsx | grep -c "components/editorial" | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -c "MEET-06\|no-bot\|no bot\|do not join" src/renderer/features/meetings/TranscriptCaptureScreen.tsx | awk '{ if ($1 &gt;= 1) print "OK MEET-06 copy preserved"; else { print "FAIL"; exit 1 } }'
    </automated>
  </verify>
  <done>
    All 4 meetings files re-skinned. Transcript paste → extract → review flow intact. MEET-06 guardrail copy preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin Tasks (TasksScreen, TaskRow) and Ask Aria (AskScreen, AnswerCard, CitationList, SourcePreview)</name>
  <files>
    src/renderer/features/tasks/TasksScreen.tsx,
    src/renderer/features/tasks/TaskRow.tsx,
    src/renderer/features/ask/AskScreen.tsx,
    src/renderer/features/ask/AnswerCard.tsx,
    src/renderer/features/ask/CitationList.tsx,
    src/renderer/features/ask/SourcePreview.tsx
  </files>
  <action>
    1. Read each file. Inventory IPC, source-of-task discriminator (Todoist / meeting-action), filter state, RAG IPC, citation IPC.
    2. TasksScreen per `app-screen-tasks.jsx`:
       - Top filter strip: source chips (All / Todoist / Meeting actions), state chips (Open / Completed), project filter (Todoist project list as dropdown).
       - Body: vertical list of TaskRow items.
    3. TaskRow:
       - Editorial checkbox (rounded square, gold check, 18px).
       - Playfair task title (medium 500, struck-through when completed).
       - Mono meta row underneath: priority pip (4px gold dot for P1, ink dot for P2, etc.), due chip (mono "DUE THU 22", gold when imminent, rose when overdue), project chip (paper bg, rule border, gray text), labels chips (mono uppercase, gray).
       - Right side: "open meeting note" link (only for meeting-action source tasks, gold text + arrow icon).
    4. AskScreen per `app-screen-ask.jsx`:
       - Left rail: "Threads" Playfair, list of past threads with snippet + mono timestamp. New thread button at top.
       - Main column: header with per-provider-account filter chips (account chips with checkboxes), composer at the bottom (editorial Input variant, multi-line, Playfair italic placeholder), conversation messages stacked above.
       - User message: Source Sans, right-aligned bubble with ivory-deep bg and 6px radius.
       - Aria message: AnswerCard.
       - Disambiguation card (when person name ambiguous): paper card with rose top-rule, "Multiple people match 'David'" + radio list of candidates.
    5. AnswerCard:
       - card-accent-top, mono uppercase "ANSWER · [{route}] · {model}" header strip with RouteBadge.
       - Playfair italic question echoed at top (small, gray-soft).
       - Source Sans answer prose (15px, 1.6 line-height).
       - CitationList below: chunk-level citations with kind tag (mono uppercase EMAIL/MEETING/CALENDAR/TASK), source label (underlined, gray rule-strong decoration, 3px offset), span timestamp/range.
    6. CitationList: vertical list of citation rows, click-to-expand SourcePreview.
    7. SourcePreview: modal-like inline expansion showing the cited chunk with surrounding context, kind-specific metadata (sender for email, attendees for meeting, etc.). Use editorial Modal primitive if it's a true modal; otherwise an inline card.
    8. **Behaviour invariants:** Todoist push, RAG IPC, hybrid retrieval BM25+vector+RRF, sensitivity routing for query, model-swap reconciler — all untouched.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/features/tasks src/renderer/features/ask 2&gt;&amp;1 | tail -30 &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/features/tasks/TasksScreen.tsx | grep -c "components/editorial" | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/features/ask/AnswerCard.tsx | grep -c "components/editorial" | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }'
    </automated>
  </verify>
  <done>
    Tasks + Ask Aria re-skinned. Filter chips, threads sidebar, AnswerCard with citation list and disambiguation card all render in editorial style.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-skin Recap (RecapScreen, RecapEditor)</name>
  <files>
    src/renderer/features/recap/RecapScreen.tsx,
    src/renderer/features/recap/RecapEditor.tsx
  </files>
  <action>
    1. Read both files. RecapEditor is built on TipTap — DO NOT swap the editor library. The toolbar / chrome around it is what changes.
    2. RecapScreen per `app-screen-recap.jsx`:
       - Header: Playfair "Weekly recap" + mono eyebrow with date range of the current week.
       - List of past recaps: cards with status pill (Draft = gray; Finalized = moss) + week range + last-edited time + click-to-open.
       - "Start new recap" button (Button primary) at the top.
    3. RecapEditor (5 sections — confirmed in chat3.md):
       - Section header for each: `<LabelRule label="Meetings" align="left" />`, then TipTap editor surface for that section's editable content. Same for Commitments, Wins, Upcoming.
       - Fifth section "What Aria did this week": LabelRule "What Aria did" + editable Playfair italic narrative AT TOP + below it a **verbatim read-only audit list** (rendered as a definition list with mono left labels for action type and gray text for details). The read-only audit list is sourced from the unified `action_audit_log` — DO NOT make it editable.
       - Footer button row: Save (Button outline) · Finalize (Button primary, opens DisconnectConfirmDialog-style confirmation modal — D-12) · Export DOCX (Button outline) · Export PDF (Button outline).
       - Status pill at the top right showing Draft / Finalized.
    4. **Behaviour invariants:** TipTap state, save/finalize IPC, export IPC, audit log fetch IPC, finalize confirm dialog (3-assertion contract) — all untouched.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/features/recap 2&gt;&amp;1 | tail -30 &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/features/recap/RecapEditor.tsx | grep -c "components/editorial" | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -c "What Aria did\|action_audit\|audit log\|auditLog" src/renderer/features/recap/RecapEditor.tsx | awk '{ if ($1 &gt;= 1) print "OK trust-anchor preserved"; else { print "FAIL"; exit 1 } }'
    </automated>
  </verify>
  <done>
    Recap re-skinned. 5-section editor with editable narrative + read-only audit list. Existing tests pass.
  </done>
</task>

</tasks>

<verification>
- Snapshot strategy invariant: snapshot tests are updated via `pnpm test -u` AFTER a human spot-check confirms the new snapshot matches the intended design; behavioural tests pass UNMODIFIED — if a behavioural test breaks, fix the re-skin, not the test (D-10).
- `pnpm typecheck`, `pnpm lint`, `pnpm test --run` all green.
- Walk each screen in dev build. Compare layout/typography/spacing against design-ref screenshots.
- Verifier reachability: every component in `files_modified` is imported by a Screen which is mounted by a route.
</verification>

<success_criteria>
- Meetings, Tasks, Ask Aria, Recap render in editorial style.
- TipTap editor still works, citations still highlight, Todoist push still works, RAG citations still resolve.
- DisconnectConfirmDialog 3-assertion contract still passes wherever it gates a destructive action.
</success_criteria>

<output>
After completion, create `.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-04-SUMMARY.md`.
</output>
