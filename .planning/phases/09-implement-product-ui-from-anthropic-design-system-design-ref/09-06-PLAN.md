---
phase: 09-implement-product-ui-from-anthropic-design-system-design-ref
plan: 06
type: execute
wave: 6
depends_on: ["09-05"]
files_modified:
  - tests/integration/phase-9-reachability.spec.ts
  - tests/integration/phase-9-visual-walkthrough.spec.ts
  - .planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-UAT.md
autonomous: false
requirements: []
tags: [qa, polish, reachability, phase-9]
must_haves:
  truths:
    - "Every editorial primitive (MonogramSquare, Avatar, StatusDot, Button, Card, Input, Modal, LabelRule, RouteBadge, KbdHint, AppLogo, SidebarStatus, Topbar) is imported by at least one Screen or route, verified by a programmatic grep ratchet"
    - "No feature file imports from Google Fonts CDN (`fonts.googleapis.com`) or references the legacy `aria-*` Tailwind utility classes for new editorial work"
    - "A Playwright _electron walkthrough mounts every route in turn and asserts no console errors, no missing-component warnings, and a visible editorial token (--ink or --gold) is applied"
    - "Snapshot tests across all touched feature files are committed in their updated form, and a human reviewer has spot-checked each"
    - "UAT plan exists with 14-tab settings walk + 10-screen workspace walk + 4-step onboarding + 3 entitlement state walks + DisconnectConfirmDialog destructive-flow recheck"
  artifacts:
    - path: "tests/integration/phase-9-reachability.spec.ts"
      provides: "Reachability ratchet — every editorial primitive is consumed by an in-route Screen"
      contains: "reachability"
    - path: "tests/integration/phase-9-visual-walkthrough.spec.ts"
      provides: "Playwright _electron walkthrough mounting every route"
      contains: "_electron"
    - path: ".planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-UAT.md"
      provides: "Phase 9 UAT script"
      contains: "UAT"
  key_links:
    - from: "phase-9-reachability.spec.ts"
      to: "src/renderer/components/editorial/index.ts"
      via: "static analysis of import statements"
      pattern: "components/editorial"
---

<objective>
Visual QA, reachability ratchet, snapshot consolidation, and UAT execution.
Closes Phase 9.

Purpose: Phase 4 verifier blindspot (`feedback_verifier_blindspot_ui_wiring`)
mandates a reachability check. Phase 5 reviews lessons mandate a human walk-
through under live conditions. This plan ships both as enforceable tests +
human-driven UAT.
Output: Two integration tests + 09-UAT.md + final snapshot updates committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-CONTEXT.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-DESIGN-TOKENS.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-01-SUMMARY.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-02-SUMMARY.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-03-SUMMARY.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-04-SUMMARY.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-05-SUMMARY.md
@playwright.config.ts
@src/renderer/components/editorial/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add reachability ratchet integration test</name>
  <files>tests/integration/phase-9-reachability.spec.ts</files>
  <action>
    1. Vitest integration test that performs static analysis:
       - Read `src/renderer/components/editorial/index.ts` and parse the list of named exports.
       - For each named export, grep the entire `src/renderer/{features,app,components}` tree (excluding the editorial/ directory itself and __tests__/*) for any import statement that references it.
       - Assert: every exported name appears in ≥1 non-test file outside `components/editorial/`. Failure message names the orphaned component.
    2. Additional assertions in the same test file:
       - No file under `src/renderer/features/` imports from `fonts.googleapis.com` (grep for the URL substring; expect 0 hits).
       - No file under `src/renderer/features/` references `--aria-accent` via inline style or className composition for NEW work — narrower assertion: count occurrences of `var(--aria-accent)` and `bg-accent` / `text-accent` Tailwind classes, snapshot the count, and ratchet downward only. (Store the initial count in a constant in the test; this is a one-way ratchet.)
       - Every feature directory has ≥1 file importing from `components/editorial`.
    3. Use Node's fs.readdirSync + simple regex; no need for AST tools. Test runs in &lt;2s.
  </action>
  <verify>
    <automated>
      pnpm test --run tests/integration/phase-9-reachability.spec.ts
    </automated>
  </verify>
  <done>
    Test passes; orphaned primitives flagged; legacy-token usage ratcheted.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Playwright _electron visual walkthrough</name>
  <files>tests/integration/phase-9-visual-walkthrough.spec.ts</files>
  <action>
    1. Playwright `_electron` test that launches the packaged dev build (mirror the Phase 1 smoke test setup).
    2. Unlocks the test vault (use the existing test fixture vault if it exists; otherwise the test creates a fresh one).
    3. Walks every route in order: /briefing, /approvals, /calendar, /meetings, /tasks, /scheduling, /ask, /recap, /routing-log, /settings, /settings/frontier-key, /settings/integrations, /settings/subscription, /settings/diagnostics. For each route:
       - Wait for the Topbar display title to render the expected text.
       - Assert no `console.error` was emitted since the previous step.
       - Assert at least one element on the page has computed `font-family` containing "Playfair Display" OR "Source Sans 3" OR "IBM Plex Mono".
       - Take a screenshot to `test-results/phase-9-{route}.png` for human review.
    4. Walk entitlement states by setting the test fixture state to `trial-active-day0`, `trial-locked`, `pro-active`, `pro-locked` and asserting TrialBanner / PaywallScreen render correctly.
    5. Test should be marked `@slow` and excluded from the default CI run; runs only on `pnpm test:e2e:phase-9`.
  </action>
  <verify>
    <automated>
      pnpm exec playwright test tests/integration/phase-9-visual-walkthrough.spec.ts 2&gt;&amp;1 | tail -40
    </automated>
  </verify>
  <done>
    All routes mount without console errors, editorial fonts present on every screen, screenshots saved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual walkthrough + snapshot review + DisconnectConfirmDialog destructive-flow recheck</name>
  <what-built>
    Phases 09-01 through 09-05 have re-skinned the entire renderer to the
    editorial design system. Plan 09-06 Tasks 1 + 2 have added programmatic
    reachability and visual-walkthrough tests. This checkpoint is the human
    verification before declaring the phase complete.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and open the app.
    2. Walk every screen in the sidebar in order. For each screen, compare side-by-side with the matching `design-ref/project/app-screen-*.jsx` prototype. Note any spacing / typography / color drift &gt;2px / 1 shade.
    3. Open ⌘K palette from both the sidebar trigger AND the topbar trigger AND the keyboard shortcut. All three should open the same overlay.
    4. Trigger every destructive flow that uses DisconnectConfirmDialog:
       - Settings → Integrations → Disconnect a Google account (you can cancel before final confirm).
       - Settings → Learned preferences → Reset a single field.
       - Recap → open a draft → Finalize (you can cancel).
       Each should require typed confirmation and the modal must look editorial (rose top rule, Playfair title, mono "Type … to confirm" hint).
    5. Walk Onboarding from a fresh vault (use a test profile or clear the dev vault). Verify the seal step shows "Sealing your vault…" Playfair italic + mono "5–15 seconds on this machine" caption.
    6. Force each TrialBanner state by editing dev fixture or via DevTools entitlement override:
       - info (day 0-49)
       - warn (day 50-54)
       - urgent (day 55-59)
       - clock-skew
       Each should render with the documented visual.
    7. Confirm no feature regression in: briefing regenerate, approval approve/reject, calendar event display, meeting transcript paste→extract, task push to Todoist (if you have a test token), Ask Aria query → cited answer.
    8. Open `test-results/phase-9-*.png` and review every screenshot.
    9. Run `pnpm test --run` once more — every test must be green.

    Document any drift / issues in `09-UAT.md` (Task 4 will scaffold the file).
  </how-to-verify>
  <resume-signal>Type "approved — phase 9 closing" or list the issues blocking close-out.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write 09-UAT.md capturing the walkthrough result</name>
  <files>.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-UAT.md</files>
  <action>
    1. Scaffold `09-UAT.md` with the structure used in prior phases (06-UAT.md, see commit ffaaaee):
       - Header: Phase + date + executor.
       - "Scope" section listing the screens, components, entitlement states, and destructive flows verified.
       - "Walkthrough log" section — one entry per screen / flow, status (PASS / PASS-W-CAVEATS / ISSUE) + notes.
       - "Issues found" section — any drift, regression, or design-vs-implementation discrepancy. Each issue with severity (BLOCKER / MAJOR / MINOR / COSMETIC) and a routing decision (fix-in-09-06 / route-to-phase-10 / defer).
       - "Success criteria" check — confirm each of D-01 through D-17 from 09-CONTEXT.md is honored.
       - "Close-out summary" — PASS / GAPS / BLOCKED with one-line rationale.
    2. After human walkthrough completes (Task 3 resume), fill in the actual results. If gaps were found, route them appropriately:
       - BLOCKER → loop back to relevant plan, do not close phase.
       - MAJOR → schedule as 09.1 sub-phase.
       - MINOR / COSMETIC → log in memory + add to backlog.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-UAT.md &amp;&amp;
      grep -c "Walkthrough log\|Issues found\|Close-out summary" .planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-UAT.md | awk '{ if ($1 &gt;= 3) print "OK structure present"; else { print "FAIL"; exit 1 } }'
    </automated>
  </verify>
  <done>
    09-UAT.md committed with walkthrough log, issue list, close-out summary.
  </done>
</task>

</tasks>

<verification>
- `pnpm test --run` full suite green.
- `pnpm exec playwright test tests/integration/phase-9-visual-walkthrough.spec.ts` green.
- Human walkthrough completed; UAT.md filled.
- No BLOCKER issues outstanding.
</verification>

<success_criteria>
- Every editorial primitive consumed by ≥1 in-route Screen (Phase 4 verifier blindspot does not recur).
- Every route mounts cleanly in Playwright; no console errors.
- Human-verified pixel-perfect-enough match to design-ref.
- All DisconnectConfirmDialog destructive flows preserve the 3-assertion consent contract.
- Phase 9 closes with no BLOCKER gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-06-SUMMARY.md` AND mark the phase complete in `.planning/ROADMAP.md`.
</output>
