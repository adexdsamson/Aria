---
phase: 09-implement-product-ui-from-anthropic-design-system-design-ref
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/renderer/app/Layout.tsx
  - src/renderer/components/SideNav.tsx
  - src/renderer/components/Topbar.tsx
  - src/renderer/components/CommandPalette.tsx
  - src/renderer/components/ProviderStatusTray.tsx
  - src/renderer/components/editorial/SidebarStatus.tsx
  - src/renderer/components/__tests__/Topbar.test.tsx
autonomous: true
requirements: []
tags: [ui, shell, navigation, phase-9]
must_haves:
  truths:
    - "Sidebar shows the editorial brand row (MonogramSquare + Playfair 'Aria' + italic 'chief of staff') and the nav order matches app-shell.jsx (Briefing · Approvals · Calendar · Meetings · Tasks · Scheduling · Ask Aria · Weekly Recap · Settings · Routing log)"
    - "Active nav item shows a 2px gold left rail and gold icon"
    - "Cmd-K palette opens with the editorial overlay (ivory card, Playfair input, gold search icon)"
    - "Topbar component renders a mono uppercase eyebrow + Playfair display title that switches per route"
    - "Onboarding and Unlock routes render WITHOUT sidebar or topbar chrome (D-07)"
    - "Sidebar footer shows the live provider status rows (Ollama, Frontier, Gmail, Calendar) in editorial style"
  artifacts:
    - path: "src/renderer/components/Topbar.tsx"
      provides: "Per-route editorial topbar (eyebrow + display title + cmdK button + bell + avatar)"
      contains: "export function Topbar"
    - path: "src/renderer/components/SideNav.tsx"
      provides: "Re-skinned sidebar with editorial brand, ⌘K trigger, NavSection/NavItem composition"
      contains: "MonogramSquare"
  key_links:
    - from: "src/renderer/app/Layout.tsx"
      to: "src/renderer/components/Topbar.tsx"
      via: "import + render between SideNav and AppRoutes"
      pattern: "<Topbar"
    - from: "src/renderer/components/SideNav.tsx"
      to: "src/renderer/components/editorial"
      via: "import"
      pattern: "from.*components/editorial"
---

<objective>
Re-skin the app shell: SideNav, the new Topbar, Layout chrome, the Cmd-K
CommandPalette overlay, and the sidebar status footer. After this plan ships,
the visual frame around every screen matches `design-ref/project/app-shell.jsx`
even though feature screens themselves are still in the old styling.

Purpose: A unified chrome makes the in-progress state of 09-03..09-05 readable
— users see the new brand around old content, which is preferable to a torn
half-state.
Output: Updated SideNav.tsx + new Topbar.tsx + restyled CommandPalette.tsx +
Layout.tsx conditional chrome for onboarding/unlock routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-CONTEXT.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-DESIGN-TOKENS.md
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-shell.jsx
@.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/design-ref/project/app-icons.jsx
@src/renderer/components/SideNav.tsx
@src/renderer/components/CommandPalette.tsx
@src/renderer/components/ProviderStatusTray.tsx
@src/renderer/app/Layout.tsx
@src/renderer/app/routes.tsx

<interfaces>
SideNav.tsx today is a react-router NavLink list. It must keep the same
NavLink behaviour and routes. The new visual is composed via editorial primitives.

CommandPalette.tsx today implements the global Cmd/Ctrl+K hotkey, the IPC
call to RAG, and the answer rendering. KEEP all IPC and state logic — only the
JSX + styles change.

Layout.tsx today renders SideNav + AppRoutes + CommandPalette. It must now
also render Topbar AND conditionally hide all chrome on /onboarding and any
unlock-screen routes. The existing App.tsx gating may already short-circuit
those routes before Layout mounts; CHECK App.tsx and only add the conditional
suppression here if App.tsx does not already handle it.

ProviderStatusTray is the existing footer status component used in
SideNav today. The re-skin replaces its visual with a tighter editorial
SidebarStatus matching app-shell.jsx lines 210-228. Keep ProviderStatusTray
exported (it's used in StatusPanel.tsx tests); add SidebarStatus as a new
sibling that consumes the same hook/IPC.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Topbar component + wire into Layout with onboarding chrome-suppression</name>
  <files>
    src/renderer/components/Topbar.tsx,
    src/renderer/components/__tests__/Topbar.test.tsx,
    src/renderer/app/Layout.tsx
  </files>
  <behavior>
    - Topbar reads the current pathname via `useLocation()` and renders an eyebrow (mono uppercase, gold) + Playfair display title pair.
    - The titles map mirrors `app-shell.jsx` lines 233-245 exactly: briefing → "The Morning · {today formatted} / Today's Briefing", approvals → "Approval queue / Awaiting your call", calendar → "Unified calendar / Week of {ISO week of today}", meetings → "Meeting capture / Transcripts &amp; action items", tasks → "Tasks / Todoist + meeting actions", scheduling → "Scheduling / Tell Aria what to move", ask → "Ask Aria / Cited Q&amp;A over your data", recap → "Weekly recap / The week in brief", settings → "Settings / Preferences &amp; status", routing-log → "Diagnostics / Routing log".
    - Unknown routes fall back to the briefing title.
    - Renders a ⌘K trigger button on the right (KbdHint + Button-like), a decorative bell icon (D-06 — non-interactive), and the user Avatar.
    - Layout uses `useLocation()` to detect `/onboarding` and any path starting with `/unlock` (current App.tsx behavior is the source of truth; only add the suppression here if App.tsx renders Layout for those paths). When chrome-suppressed, render `<AppRoutes />` standalone.

    Tests:
    - Topbar with pathname '/briefing' renders the text "Today's Briefing" and "The Morning" eyebrow.
    - Topbar with pathname '/recap' renders "The week in brief".
    - Topbar with pathname '/unknown' falls back to briefing title.
    - Pressing the ⌘K button dispatches the same global Cmd-K open event that CommandPalette listens for (use a shared event emitter or simulate the keydown).
  </behavior>
  <action>
    1. **FIRST ACTION (before writing any code):** Read `src/renderer/app/App.tsx` and decide which of two branches applies:
       - **Branch A:** App.tsx already short-circuits onboarding/unlock BEFORE Layout mounts (route-level gate). In this case, Layout DOES NOT add a `useLocation` chrome-suppression check.
       - **Branch B:** App.tsx renders Layout for onboarding/unlock paths. In this case, Layout ADDS the `useLocation` chrome-suppression check inside Layout itself.
       Write the chosen branch (A or B) + a one-line rationale into `.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-02-SUMMARY.md` under a heading `## Chrome-suppression branch decision` BEFORE writing any code. Then implement exactly one branch — never both, never neither.
    2. Create `Topbar.tsx`: a thin presentational component. Date formatting: use `Intl.DateTimeFormat` to render "Tuesday 20 May" (locale-aware, no new dep). Week-of: ISO week number. Use editorial primitives — `<KbdHint>⌘K</KbdHint>`, `<Avatar>` — for the right cluster.
    3. The cmdK trigger button: dispatch a custom event (`window.dispatchEvent(new CustomEvent('aria:cmdk-toggle'))`) AND/OR call a context-provided callback. Match whatever pattern CommandPalette.tsx already uses for hotkey wiring. If CommandPalette uses a `useEffect` listening for keydown, add a `aria:cmdk-toggle` listener there in Task 3.
    4. Update `Layout.tsx` to render `<SideNav />` + a `<div>` column containing `<Topbar />` and `<main><AppRoutes /></main>`. Background color: `var(--ivory)`. Keep the existing `data-testid="aria-layout"`.
    5. Per the branch chosen in step 1: if Branch B, add the `useLocation()` check inside Layout and render `<AppRoutes />` only when `pathname !== '/onboarding' &amp;&amp; !pathname.startsWith('/unlock')`. If Branch A, do nothing here — App.tsx already handles it.
    6. Snapshot tests in this file will be NEW (no prior snapshots). Do not generate snapshots — use targeted assertions.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/components/__tests__/Topbar.test.tsx &amp;&amp;
      grep -c "Topbar" src/renderer/app/Layout.tsx | awk '{ if ($1 &gt;= 1) print "OK reachable"; else { print "FAIL Topbar not imported by Layout"; exit 1 } }' &amp;&amp;
      grep -v "^//" src/renderer/components/Topbar.tsx | grep -c "useLocation" | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      node -e "const fs=require('fs');const app=fs.readFileSync('src/renderer/app/App.tsx','utf8').split('\n').filter(l=&gt;!/^\s*\/\//.test(l)).join('\n');const lay=fs.readFileSync('src/renderer/app/Layout.tsx','utf8').split('\n').filter(l=&gt;!/^\s*\/\//.test(l)).join('\n');const rx=/onboarding|unlock/;const appGate=rx.test(app);const layGate=rx.test(lay);if(appGate&amp;&amp;layGate){console.error('FAIL chrome-suppression duplicated in BOTH App.tsx and Layout.tsx — pick exactly one branch (see 09-02 Task 1 step 1)');process.exit(1);}if(!appGate&amp;&amp;!layGate){console.error('FAIL chrome-suppression in NEITHER App.tsx nor Layout.tsx — onboarding/unlock will render with sidebar/topbar chrome');process.exit(1);}console.log('OK exactly one chrome-suppression gate ('+(appGate?'App.tsx':'Layout.tsx')+')');"
    </automated>
  </verify>
  <done>
    Topbar exists, is imported by Layout.tsx (reachability per D-11), title map matches design-ref. Onboarding path renders without chrome.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-skin SideNav with editorial brand, NavSection/NavItem, sidebar status</name>
  <files>
    src/renderer/components/SideNav.tsx,
    src/renderer/components/editorial/SidebarStatus.tsx
  </files>
  <behavior>
    - SideNav renders, top-to-bottom: AppLogo(variant="sidebar"), Cmd-K trigger button (paper bg, rule border, search icon, "Ask Aria" label, KbdHint ⌘K), NavSection "Workspace" with 8 NavItems (briefing, approvals with gold badge for approval count, calendar, meetings, tasks with neutral badge for task count, scheduling, ask, recap), NavSection "System" with 2 NavItems (settings, routing-log), spacer, footer (first-run wizard link if relevant + SidebarStatus).
    - NavItem: react-router NavLink wrapped to render the editorial style (active = ivory-deep bg + 2px gold left rail + gold icon + ink text font-weight 500; idle = transparent bg + gray-soft icon + gray text).
    - SidebarStatus consumes the same hook ProviderStatusTray uses today (ollama / frontier / gmail / calendar). Renders 4 rows: StatusDot + mono uppercase 56px-wide label + mono value text. No header, no actions — it's a passive footer.
    - Approval count + task count come from the existing IPC selectors (whatever ApprovalsScreen and TasksScreen subscribe to). Reuse those hooks — do not duplicate IPC plumbing.

    Tests (extend existing SideNav.test.tsx if present, else create one):
    - Renders all 10 NavLink labels (Briefing, Approvals, Calendar, Meetings, Tasks, Scheduling, Ask Aria, Weekly Recap, Settings, Routing log).
    - Active route shows the gold left rail (assert via data-active attribute or class).
    - Cmd-K trigger button fires the same handler as the global hotkey.
  </behavior>
  <action>
    1. Read existing SideNav.tsx to inventory the IPC hooks/selectors it uses for badges and the brand row.
    2. Port the NavSection + NavItem composition from `app-shell.jsx` lines 66-128 into SideNav. NavItem internally uses `NavLink` with a `className` callback for active state.
    3. Brand row: `<AppLogo variant="sidebar" />` (D-03).
    4. Extract `SidebarStatus` into `src/renderer/components/editorial/SidebarStatus.tsx` (it's reusable; not in the editorial primitives index because it's data-bound). Mirror `app-shell.jsx` lines 210-228 visually but bind to real provider state.
    5. Keep ProviderStatusTray.tsx — it may still be referenced elsewhere. SidebarStatus is a sibling, not a replacement, for v1. (Plan 09-05 may remove it during settings re-skin if it's only used inside StatusPanel.)
    6. Confirm reachability: `grep -rn "from.*components/SideNav" src/renderer | grep -v test` returns ≥1.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/components 2&gt;&amp;1 | tail -40 &amp;&amp;
      grep -c "AppLogo" src/renderer/components/SideNav.tsx | awk '{ if ($1 &gt;= 1) print "OK brand wired"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      grep -c "SidebarStatus" src/renderer/components/SideNav.tsx | awk '{ if ($1 &gt;= 1) print "OK"; else { print "FAIL"; exit 1 } }' &amp;&amp;
      node -e "const fs=require('fs');const s=fs.readFileSync('src/renderer/components/SideNav.tsx','utf8');const labels=['Briefing','Approvals','Calendar','Meetings','Tasks','Scheduling','Ask Aria','Weekly Recap','Settings'];for(const l of labels){if(!s.includes(l)){console.error('FAIL missing nav label: '+l);process.exit(1);}}console.log('OK all 9 labels present');"
    </automated>
  </verify>
  <done>
    SideNav renders editorial brand + 10 NavLinks + footer status. All existing SideNav tests pass. Reachability grep clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-skin CommandPalette overlay (editorial visual; preserve IPC, behaviour, hotkey)</name>
  <files>src/renderer/components/CommandPalette.tsx</files>
  <action>
    1. Re-read `app-shell.jsx` lines 373-509 (CommandPaletteOverlay) as the visual reference. Backdrop: rgba(26,26,26,0.45) + 2px backdrop blur, fixed inset 0, z-index 200. Modal: ivory bg, 1px rule-strong border, 10px radius, 0 30px 80px shadow.
    2. Form row: 14px 18px padding, rule bottom border, gold search icon (18px), Playfair 20px input (no border, transparent bg, ink color), "ephemeral" and "esc" KbdHint chips on the right.
    3. Body section: maxHeight 50vh overflow auto. Idle state shows "Try" smallcaps label and 4 example queries as italic Playfair quoted suggestions; loading state shows mono "Searching" gold + italic Playfair "BM25 + nomic-embed-text v1.5…"; answer state shows mono uppercase "Answer · [{route}] · {model}" header, then ink Source-Sans answer prose, then citation links (mono kind tag + underlined label + mono span), then "Expand to chat →" Button(primary) and the disclaimer "Cmd-K answers don't appear in /ask threads".
    4. **KEEP the existing IPC plumbing verbatim.** Do not rewire how queries reach the RAG backend, how cites are decoded, or how Cmd/Ctrl+K is registered. Only the JSX + inline styles change.
    5. If the existing CommandPalette uses Tailwind classes (`bg-gray-*` etc.), replace those with inline `style={{ ... }}` using editorial CSS variables OR with the editorial primitive classes from globals.css. Do not invent new colors.
    6. Listen for `window.addEventListener('aria:cmdk-toggle', ...)` in addition to the existing keydown listener (so Topbar's ⌘K trigger button works).
    7. Existing CommandPalette tests: run them. Update assertions ONLY for visual text the test checks AGAINST a hard-coded string that changed (e.g., placeholder text was "Ask anything…" before, now "Ask anything across your mail, meetings, tasks…"). If a test asserts on a class name like `bg-aria-bg`, update it to the new class. Do NOT change tests that assert on IPC behaviour, error handling, citation rendering, or hotkey behaviour.
  </action>
  <verify>
    <automated>
      pnpm typecheck &amp;&amp;
      pnpm test --run src/renderer/components/CommandPalette 2&gt;&amp;1 | tail -20 &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/components/CommandPalette.tsx | grep -c -- "--gold\|--ivory\|--ink\|--rule" | awk '{ if ($1 &gt;= 3) print "OK editorial vars in use"; else { print "FAIL only "$1" editorial vars"; exit 1 } }' &amp;&amp;
      grep -v "^[[:space:]]*//" src/renderer/components/CommandPalette.tsx | grep -c "aria:cmdk-toggle" | awk '{ if ($1 &gt;= 1) print "OK Topbar trigger wired"; else { print "FAIL"; exit 1 } }'
    </automated>
  </verify>
  <done>
    CommandPalette renders with editorial visual; existing tests pass; Topbar ⌘K button opens it.
  </done>
</task>

</tasks>

<verification>
- Visual: launch dev build and manually walk every route. Sidebar + topbar look like `design-ref/project/app-shell.jsx`. Cmd-K palette opens with editorial visual. Onboarding route renders without sidebar/topbar.
- `pnpm typecheck`, `pnpm lint`, `pnpm test --run` (full renderer suite) all green.
- Behavioural tests for CommandPalette and SideNav still pass without modification.
</verification>

<success_criteria>
- A user opening the app sees the editorial brand, the new nav, the per-screen topbar with display title + eyebrow, and the gold ⌘K palette. The screens themselves still look like the legacy treatment (that's 09-03..09-05).
- Reachability: SideNav and Topbar both import from `components/editorial`; both are referenced by Layout.tsx.
- No new dependencies introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/09-implement-product-ui-from-anthropic-design-system-design-ref/09-02-SUMMARY.md`.
</output>
