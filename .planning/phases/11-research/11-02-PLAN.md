---
phase: 11-research
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/main/ipc/research.ts
  - src/main/ipc/index.ts
  - src/main/ipc/transcripts.ts
  - src/main/index.ts
  - tests/static/single-entitlement-gate-site.test.ts
  - src/renderer/app/routes.tsx
  - src/renderer/features/settings/IntegrationsSection.tsx
  - src/renderer/features/research/ResearchScreen.tsx
  - src/renderer/features/research/NewResearchJobModal.tsx
  - src/renderer/features/research/ReportDocumentView.tsx
  - src/renderer/features/research/ReportDashboardView.tsx
  - src/renderer/features/research/FeedbackBar.tsx
  - src/renderer/features/research/RerunModal.tsx
autonomous: true
requirements:
  - RES-01
  - RES-04
  - RES-05
  - RES-06
  - RES-07
  - RES-08

must_haves:
  truths:
    - "All 12 research IPC channels respond (no 'No handler for aria:research:*' in console)"
    - "assertEntitled is called in research.ts for both RESEARCH_JOB_CREATE and RESEARCH_JOB_RUN handlers"
    - "Static entitlement ratchet fully green: GATED_SITES whitelist updated + research.ts has both assertEntitled calls"
    - "transcripts.ts fires detectResearchTopics as fire-and-forget after ingestTranscriptNote"
    - "Route /research renders ResearchScreen when navigated to"
    - "Document/Dashboard view toggle switches render different layout"
    - "NewResearchJobModal Start Research button is disabled when both API keys absent"
    - "Settings -> Integrations shows Brave Search and Exa rows for API key entry"
    - "FeedbackBar thumbs call researchFeedbackSave IPC"
    - "RerunModal Re-run button calls researchJobRun with feedbackContext"
  artifacts:
    - path: "src/main/ipc/research.ts"
      provides: "registerResearchHandlers with all 12 IPC channels + assertEntitled on create+run"
      exports: ["registerResearchHandlers"]
    - path: "tests/static/single-entitlement-gate-site.test.ts"
      provides: "GATED_SITES updated with research.ts entries; all per-site tests GREEN"
    - path: "src/renderer/features/research/ResearchScreen.tsx"
      provides: "Two-column 240px rail + right panel; push event subscription via ipcRenderer.on"
    - path: "src/renderer/features/research/FeedbackBar.tsx"
      provides: "Thumbs up/down + inline note expander calling researchFeedbackSave"
    - path: "src/renderer/app/routes.tsx"
      provides: "/research route in READ_ONLY_ALLOW_LIST + AppRoutes"
  key_links:
    - from: "src/main/ipc/index.ts"
      to: "src/main/ipc/research.ts"
      via: "registerResearchHandlers import + channel block"
      pattern: "registerResearchHandlers"
    - from: "src/main/ipc/transcripts.ts"
      to: "src/main/services/ResearchService.ts"
      via: "detectResearchTopics fire-and-forget import"
      pattern: "detectResearchTopics"
    - from: "src/renderer/features/research/ResearchScreen.tsx"
      to: "window.aria.researchJobList"
      via: "IPC call in useEffect"
      pattern: "researchJobList"
---

<objective>
Wire all 12 IPC handlers, update the entitlement static ratchet, connect the transcript post-ingest hook, register the /research route, and build all renderer components needed to create jobs, view reports, give feedback, and trigger re-runs.

Purpose: This plan makes the Research feature functionally complete end-to-end: from IPC through to the UI. Wave 1 built the service and data layers; this plan makes them reachable. The GATED_SITES update is placed here (not Plan 01) so per-site tests are GREEN immediately -- research.ts and the whitelist are created in the same task.

Output: research.ts IPC handlers (12 channels), GATED_SITES whitelist update (fully GREEN), index.ts registration, transcripts.ts hook, routes.tsx /research, IntegrationsSection key rows, ResearchScreen + 5 child components (NewResearchJobModal, ReportDocumentView, ReportDashboardView, FeedbackBar, RerunModal).
</objective>

<execution_context>
@C:\Users\HomePC\Documents\GitHub\Aria\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\Documents\GitHub\Aria\.planning\phases\11-research\11-CONTEXT.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Aria\.planning\ROADMAP.md
@C:\Users\HomePC\Documents\GitHub\Aria\.planning\phases\11-research\11-RESEARCH.md
@C:\Users\HomePC\Documents\GitHub\Aria\.planning\phases\11-research\11-PATTERNS.md
@C:\Users\HomePC\Documents\GitHub\Aria\.planning\phases\11-research\11-01-SUMMARY.md

<interfaces>
<!-- Key contracts established by Plan 01 -->

From src/shared/ipc-contract.ts (after Plan 01):
  CHANNELS.RESEARCH_JOB_CREATE, RESEARCH_JOB_LIST, RESEARCH_JOB_GET, RESEARCH_JOB_UPDATE,
  RESEARCH_JOB_DELETE, RESEARCH_JOB_RUN, RESEARCH_REPORT_GET, RESEARCH_REPORT_LIST,
  RESEARCH_FEEDBACK_SAVE, RESEARCH_SUGGESTIONS_GET, RESEARCH_SUGGESTION_APPROVE,
  RESEARCH_SUGGESTION_DISMISS, RESEARCH_REPORT_DONE

  export interface ResearchJobDto { id, title, goals, domains, status, scheduleInterval, nextRunAt, createdAt, updatedAt }
  export interface ResearchReportDto { id, jobId, version, status, trigger, summary, confidenceScore, errorMessage, generatedAt, sections }
  export interface ResearchReportSectionDto { id, reportId, sectionType, ordinal, contentJson, feedback? }
  export interface ResearchFeedbackDto { id, reportId, sectionId, thumb, note, createdAt }

From src/main/entitlement/gate.ts (after Plan 01):
  EntitlementAction union includes 'research_create' and 'research_run'

From src/main/services/ResearchService.ts (after Plan 01):
  export function createResearchJob(db, input): Promise<{ id: string }>
  export function runResearchJob(db, jobId, deps, opts?): Promise<void>
  export function detectResearchTopics(db, noteId, noteTitle, emitToRenderer?): Promise<void>
  export function scheduleResearchJob(jobId, interval, deps): ScheduledTask
  export function cancelResearchJobSchedule(jobId, scheduler): void

From src/main/ipc/insights.ts (handler registration pattern):
  export interface InsightHandlerDeps { logger, dbHolder, scheduler?, emitToRenderer? }
  export function registerInsightsHandlers(ipcMain, deps): void
  // Each handler: db guard -> try { assertEntitled? + logic } catch -> { error: msg }

From src/main/ipc/entitlement.ts:
  export function makeRendererEmitter(win): (channel, payload?) => void
  // Already used in index.ts for updater and entitlement

From src/main/ipc/transcripts.ts (existing hook point):
  export interface TranscriptHandlerDeps { logger: Logger; dbHolder: DbHolder; }
  // ingestTranscriptNote + createTaskBatchApprovalForNote are called synchronously
  // New emitToRenderer?: field must be optional to not break existing tests

From src/renderer/app/routes.tsx:
  - READ_ONLY_ALLOW_LIST is a string[] -- append '/research'
  - Route wrapped in <LockedGuard> -- follow pattern of other feature routes
  - Import ResearchScreen from '../features/research/ResearchScreen'

From src/renderer/features/briefing/BriefingScreen.tsx (editorial pattern):
  const EASE_OUT = 'cubic-bezier(0.23, 1, 0.32, 1)';
  // Use var(--gold), var(--rule), var(--gray-soft), var(--f-serif), var(--f-mono), var(--bg)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: research.ts IPC handlers + GATED_SITES whitelist + index.ts registration + transcripts.ts hook</name>
  <files>
    src/main/ipc/research.ts
    src/main/ipc/index.ts
    src/main/ipc/transcripts.ts
    tests/static/single-entitlement-gate-site.test.ts
  </files>
  <read_first>
    - src/main/ipc/insights.ts -- complete file (handler registration pattern, db-locked guard, error shape, assertEntitled usage)
    - src/main/ipc/index.ts -- complete file (how handler blocks are appended, makeRendererEmitter import, getScheduler usage)
    - src/main/ipc/transcripts.ts -- complete file (existing deps interface, ingestTranscriptNote call site, existing test imports to verify optional dep is safe)
    - src/main/entitlement/gate.ts -- assertEntitled signature (confirm 'research_run' and 'research_create' are in union from Plan 01)
    - tests/static/single-entitlement-gate-site.test.ts -- GATED_SITES array structure and CALLSITE_RE logic (to understand how to add research.ts entries)
    - src/main/secrets/safeStorage.ts -- setProviderTokens / getProviderTokens signatures (for secrets handler implementation)
  </read_first>
  <behavior>
    research.ts exports registerResearchHandlers(ipcMain, deps: ResearchHandlerDeps):
    - RESEARCH_JOB_CREATE: db guard -> assertEntitled(db, 'research_create') -> validate input (title non-empty, domains array) -> createResearchJob(db, input) -> if scheduleInterval set, scheduleResearchJob -> return { jobId }
    - RESEARCH_JOB_LIST: db guard -> query all research_job rows -> return { jobs: ResearchJobDto[] }
    - RESEARCH_JOB_GET: db guard -> query by id -> return ResearchJobDto | { error }
    - RESEARCH_JOB_UPDATE: db guard -> UPDATE research_job set relevant fields -> if schedule changed, cancel + reschedule -> { ok: true }
    - RESEARCH_JOB_DELETE: db guard -> DELETE research_job (cascade deletes report + sections + feedback) -> cancel schedule -> { ok: true }
    - RESEARCH_JOB_RUN: db guard -> assertEntitled(db, 'research_run') -> void runResearchJob(db, jobId, { logger, emitToRenderer, scheduler }, { trigger: 'manual', feedbackContext }) -> return { ok: true }
    - RESEARCH_REPORT_GET: db guard -> query report + sections + feedback -> return ResearchReportDto | { error }
    - RESEARCH_REPORT_LIST: db guard -> query all reports for jobId -> return { reports }
    - RESEARCH_FEEDBACK_SAVE: db guard -> INSERT research_feedback -> { ok: true }
    - RESEARCH_SUGGESTIONS_GET: db guard -> query research_job WHERE status='draft' -> return { jobs }
    - RESEARCH_SUGGESTION_APPROVE: db guard -> UPDATE research_job status='done' -> { ok: true }
    - RESEARCH_SUGGESTION_DISMISS: db guard -> DELETE research_job -> { ok: true }

    GATED_SITES update in single-entitlement-gate-site.test.ts:
    - Add `{ file: 'src/main/ipc/research.ts', action: 'research_create' }` to GATED_SITES
    - Add `{ file: 'src/main/ipc/research.ts', action: 'research_run' }` to GATED_SITES
    - Since both entries point to the same file, ALLOWED_CALLERS Set deduplicates automatically
    - After this task: vitest run single-entitlement-gate-site.test.ts passes FULLY GREEN (all per-site tests pass because research.ts now exists with both assertEntitled calls)

    index.ts: researchChannels array with all 12 channel keys; conditional block registers registerResearchHandlers with { logger, dbHolder, scheduler: getScheduler(), emitToRenderer: makeRendererEmitter(deps.mainWindow ?? null) }; channels added to skip Set.

    transcripts.ts: add optional `emitToRenderer?: (channel: string, payload?: unknown) => void` to TranscriptHandlerDeps. After assembling return value, add fire-and-forget: `void detectResearchTopics(db, result.noteId, result.title ?? '', deps.emitToRenderer).catch(err => logger.warn({ scope: 'research', err: String(err) }, 'auto-detect failed silently'))`.
  </behavior>
  <action>
    Create `src/main/ipc/research.ts`. Follow the ResearchHandlerDeps interface from PATTERNS.md exactly. The db-locked guard must be the first check in every handler body. assertEntitled must be called for RESEARCH_JOB_CREATE and RESEARCH_JOB_RUN only -- no other handlers gate on entitlement (per design spec).

    Modify `tests/static/single-entitlement-gate-site.test.ts`: add two entries to the GATED_SITES array:
      { file: 'src/main/ipc/research.ts', action: 'research_create' }
      { file: 'src/main/ipc/research.ts', action: 'research_run' }
    The ALLOWED_CALLERS Set is derived from GATED_SITES automatically -- since both entries reference the same file, the Set deduplicates. Both per-site assertions will pass GREEN immediately because research.ts is created in this same task with both assertEntitled literals present.

    Modify `src/main/ipc/index.ts`: add `import { registerResearchHandlers } from './research';` near other handler imports. Add the researchChannels block after the updater block, following the exact skip-set pattern.

    Modify `src/main/ipc/transcripts.ts`: make emitToRenderer optional in TranscriptHandlerDeps. Import `detectResearchTopics` from `../../services/ResearchService`. Add fire-and-forget call after the return value is assembled (but before the return statement). Ensure the return statement is unchanged.
  </action>
  <verify>
    <automated>npx vitest run tests/static/single-entitlement-gate-site.test.ts --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    research.ts exists and exports registerResearchHandlers with 12 IPC handlers. assertEntitled called for 'research_create' and 'research_run'. index.ts registers research handlers. transcripts.ts has optional emitToRenderer in deps and fires detectResearchTopics. tests/static/single-entitlement-gate-site.test.ts GATED_SITES updated with 2 research.ts entries. Static ratchet test passes fully GREEN -- all assertions including both new per-site tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Renderer components -- ResearchScreen + 5 child components + route + Integrations key rows</name>
  <files>
    src/renderer/app/routes.tsx
    src/renderer/features/settings/IntegrationsSection.tsx
    src/renderer/features/research/ResearchScreen.tsx
    src/renderer/features/research/NewResearchJobModal.tsx
    src/renderer/features/research/ReportDocumentView.tsx
    src/renderer/features/research/ReportDashboardView.tsx
    src/renderer/features/research/FeedbackBar.tsx
    src/renderer/features/research/RerunModal.tsx
  </files>
  <read_first>
    - src/renderer/app/routes.tsx -- READ_ONLY_ALLOW_LIST location + Route pattern + LockedGuard import
    - src/renderer/features/settings/IntegrationsSection.tsx -- existing key input row pattern
    - src/renderer/features/settings/FrontierKeySection.tsx -- password input + save button + "Key saved" confirmation pattern
    - src/renderer/features/briefing/BriefingScreen.tsx -- editorial layout pattern, EASE_OUT, section cascade, isErr utility, ipcRenderer.on push subscription pattern
    - src/renderer/features/recap/RecapScreen.tsx -- two-pane layout, stat card pattern
    - src/renderer/features/scheduling/SchedulingChat.tsx -- slide-over panel + submit pattern
    - src/renderer/features/briefing/BriefingFeedbackChips.tsx -- chipStyle function + note expander pattern (exact analog for FeedbackBar)
    - src/renderer/components/DisconnectConfirmDialog.tsx -- confirm modal shell + entry animation (analog for RerunModal)
    - src/shared/ipc-contract.ts -- ResearchJobDto, ResearchReportDto, ResearchFeedbackDto shapes
    - .planning/phases/11-research/11-PATTERNS.md -- full code excerpts for each component
  </read_first>
  <action>
    routes.tsx: Append '/research' to READ_ONLY_ALLOW_LIST. Import ResearchScreen. Add `<Route path="/research" element={<LockedGuard><ResearchScreen /></LockedGuard>} />` following the same pattern as /ask or /recap routes.

    IntegrationsSection.tsx: Add two new key input row sections below the existing integration rows, labeled "Research -- Brave Search" and "Research -- Exa". Each row uses the FrontierKeySection password-input pattern: `<input type="password">`, save button, "Key saved" confirmation message on success. On save call `window.aria.secretsSetResearchKey({ provider: 'brave' | 'exa', key })`. Key presence check uses `window.aria.researchSecretsHas({})` which returns `{ hasBrave: boolean, hasExa: boolean }`.

    ResearchScreen.tsx: Two-column shell per PATTERNS.md -- 240px aside rail + flex-1 main. Left rail: "Suggested" collapsible section (research_job rows with status='draft') with Approve/Dismiss buttons; job list below (cards with title, domain chips, status badge, timestamp; active item gets 2px gold left-border). Right panel: when no job selected, show empty state; when job selected and report loaded, show Document/Dashboard toggle + ReportDocumentView or ReportDashboardView. Subscribe to RESEARCH_REPORT_DONE push event via `window.aria.onResearchReportDone` (follow BriefingScreen's ipcRenderer.on pattern). Include "+ New Research" button at top of left rail that opens NewResearchJobModal.

    NewResearchJobModal.tsx: Slide-over panel from right (position: fixed, right: 0, width: 440). Fields: title (required, text), goals (textarea), domains (tag input -- simple comma-separated string parsed to array), optional supplementUrls (textarea, one per line), scheduleInterval select (none/daily/weekly). Key presence check on mount via `researchSecretsHas`; Start Research button disabled with tooltip when both keys absent (per RES-08, per D-01 in CONTEXT.md). Submit calls `window.aria.researchJobCreate(...)`. On success call onCreated + onClose.

    ReportDocumentView.tsx: Summary card with gold left-border (3px solid var(--gold), padding-left 16px, background rgba(184,134,11,0.04)). Summary text in var(--f-serif) italic. Key Findings list: BriefingItem-style cards (one per finding from sections with sectionType='finding'). Sources table: simple HTML table with title, domain, URL columns. Per-section FeedbackBar below each finding. Version nav footer strip: "Generated [timestamp] -- Version N of M -- <- Older / Newer ->". Use section cascade animation with animationDelay per index.

    ReportDashboardView.tsx: 4-column stat cards row (Sources found / Domains covered / Key findings / Confidence score -- values derived from report data). CSS-only coverage chart (no library -- div with width: `${pct}%` in var(--gold) per domain, per PATTERNS.md). 2-column findings grid. Sources table. All with editorial tokens (var(--f-mono) labels, var(--f-serif) values).

    FeedbackBar.tsx: Copy chipStyle from BriefingFeedbackChips.tsx verbatim. Props: `{ reportId: string; sectionId: string | null }`. State: picked (-1|0|1), noteOpen, note. thumbs up/down call `window.aria.researchFeedbackSave({ reportId, sectionId, sentiment: thumb === 1 ? 'up' : 'down', note: null })`. "Add note" link expands a textarea; note save button calls same IPC with note value.

    RerunModal.tsx: Centered dialog (position: fixed, inset 0, flex center, rgba(0,0,0,0.4) backdrop, zIndex 200). Card 480px wide, dialogEntry animation. Read-only section summarizing feedbackItems (count of up/down per section). Guidance textarea (placeholder: "Additional direction for this re-run..."). Re-run Research button calls `onRerun({ feedbackContext: guidance })` then onClose. Clicking backdrop closes. Pending state while running.

    All components use const EASE_OUT = 'cubic-bezier(0.23, 1, 0.32, 1)' and editorial CSS token variables. No raw color values anywhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "src/renderer/features/research|src/renderer/app/routes|src/renderer/features/settings/IntegrationsSection" | head -20</automated>
  </verify>
  <done>
    routes.tsx has '/research' in READ_ONLY_ALLOW_LIST and Route registered. IntegrationsSection has Brave and Exa key input rows. ResearchScreen renders two-column layout with left rail job list and right panel. NewResearchJobModal Start Research button is disabled when no keys. FeedbackBar thumbs wire to researchFeedbackSave. RerunModal passes feedbackContext on confirm. All components use editorial CSS tokens exclusively.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer -> research IPC handlers | Renderer input; validated by Zod/type guards at IPC layer |
| research.ts -> ResearchService | Internal boundary; assertEntitled enforced before reaching service |
| IntegrationsSection -> safeStorage | API keys flow through IPC -> setProviderTokens; never stored in DOM state beyond the password input field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Elevation | RESEARCH_JOB_RUN handler -- missing assertEntitled | mitigate | assertEntitled(db, 'research_run') called as first action after db guard; static ratchet GATED_SITES enforces this |
| T-11-08 | Elevation | RESEARCH_JOB_CREATE handler -- missing assertEntitled | mitigate | assertEntitled(db, 'research_create') called as first action after db guard |
| T-11-09 | Information Disclosure | IntegrationsSection renders API key | mitigate | Input type="password"; key presence checked via researchSecretsHas boolean (never returns raw key to renderer) |
| T-11-10 | Tampering | transcripts hook -- detectResearchTopics blocks IPC response | mitigate | Hook is fire-and-forget (void + .catch); IPC response returned before hook resolves |
</threat_model>

<verification>
After both tasks complete:
1. `npx vitest run tests/static/single-entitlement-gate-site.test.ts` -- all assertions pass fully GREEN (including 2 new research.ts per-site entries)
2. `npx vitest run tests/static/research-running-ratchet.spec.ts` -- passes
3. `npx tsc --noEmit` -- no new type errors in research IPC or renderer files
4. App navigates to /research -- ResearchScreen renders (no console errors)
5. Settings -> Integrations shows Brave and Exa API key rows
6. NewResearchJobModal: Start Research button disabled when no keys configured
</verification>

<success_criteria>
- research.ts IPC file with 12 handlers + assertEntitled on create and run
- tests/static/single-entitlement-gate-site.test.ts GATED_SITES updated + fully GREEN (not RED)
- index.ts research handler block registered in skip-set pattern with all 12 channels
- transcripts.ts fires detectResearchTopics as fire-and-forget; emitToRenderer optional in deps
- /research route in READ_ONLY_ALLOW_LIST and AppRoutes
- All 6 renderer components created with editorial CSS tokens
- FeedbackBar wires to researchFeedbackSave IPC
- RerunModal passes feedbackContext to researchJobRun
- Static entitlement ratchet fully green
</success_criteria>

<output>
After completion, create `.planning/phases/11-research/11-02-SUMMARY.md` with:
- Files created/modified
- IPC channel count confirmed: 12 handlers
- Static ratchet state: all green
- Any deviations from plan
</output>
