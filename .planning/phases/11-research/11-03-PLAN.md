---
phase: 11-research
plan: "03"
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/renderer/components/editorial/SideNav.tsx
  - tests/unit/main/research-service.spec.ts
  - tests/unit/main/search-provider-service.spec.ts
  - tests/unit/main/research-screen.spec.tsx
  - tests/integration/research-ipc.spec.ts
autonomous: false
requirements:
  - RES-01
  - RES-02
  - RES-03
  - RES-04
  - RES-05
  - RES-06
  - RES-07
  - RES-08

must_haves:
  truths:
    - "SideNav has a Research nav link that navigates to /research"
    - "ResearchService unit tests cover happy path, graceful degradation, and auto-detect"
    - "SearchProviderService unit tests cover Brave/Exa retry logic and Jina timeout"
    - "Integration test verifies a research report is written to SQLite after researchJobRun IPC call"
    - "UI tests verify Document/Dashboard toggle, FeedbackBar submit, and disabled button state"
    - "Full vitest suite passes with no new test failures"
  artifacts:
    - path: "tests/unit/main/research-service.spec.ts"
      provides: "Unit tests for createResearchJob, runResearchJob, detectResearchTopics"
    - path: "tests/unit/main/search-provider-service.spec.ts"
      provides: "Unit tests for searchBrave, searchExa, fetchWithJina, deduplicateByUrl"
    - path: "tests/unit/main/research-screen.spec.tsx"
      provides: "UI tests for ResearchScreen toggle, FeedbackBar submit, disabled button"
    - path: "tests/integration/research-ipc.spec.ts"
      provides: "Integration test researchJobRun against real SQLite"
  key_links:
    - from: "src/renderer/components/editorial/SideNav.tsx"
      to: "/research"
      via: "NavLink to /research"
      pattern: "research"
    - from: "tests/integration/research-ipc.spec.ts"
      to: "src/main/db/migrations/132_research.sql"
      via: "real SQLite + runMigrations in test setup"
      pattern: "research_report"
---

<objective>
Complete the test suite for Phase 11 and wire the SideNav Research entry. This plan fleshes out the unit/integration/UI test stubs from Plan 01, adds the integration test against real SQLite, and makes the Research feature reachable from the sidebar navigation.

Purpose: Phase gate cannot close without green tests. The test stubs from Plan 01 gave structure; this plan provides the implementations. The SideNav link ensures the feature is reachable from the product chrome.

Output: Fully implemented unit tests (MSW for Brave/Exa/LLM), integration test against real SQLite, UI component tests, SideNav Research link, and a human-verify UAT checkpoint.
</objective>

<execution_context>
@C:\Users\HomePC\Documents\GitHub\Aria\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\Documents\GitHub\Aria\.planning\phases\11-research\11-CONTEXT.md
</execution_context>

<context>
@C:\Users\HomePC\Documents\GitHub\Aria\.planning\ROADMAP.md
@C:\Users\HomePC\Documents\GitHub\Aria\.planning\phases\11-research\11-RESEARCH.md
@C:\Users\HomePC\Documents\GitHub\Aria\.planning\phases\11-research\11-02-SUMMARY.md

<interfaces>
<!-- Patterns to follow for testing -->

From tests/unit/main/briefing/ (MSW pattern):
  - MSW server setup: `beforeAll(() => server.listen())`, `afterEach(() => server.resetHandlers())`, `afterAll(() => server.close())`
  - Mock HTTP: `http.get(url, resolver)` and `http.post(url, resolver)` from 'msw'
  - Return: `HttpResponse.json(payload)` from 'msw'

From tests/integration/ (real SQLite pattern):
  - Setup: create temp DB, run migrations, insert seed rows
  - Call IPC handler directly (not via electron IPC bridge) by importing handler functions
  - Teardown: close DB

From tests/unit/main/ (vitest structure):
  - Import from vitest: describe, it, expect, beforeEach, afterEach, vi
  - Use vi.fn() for mocked deps (logger, emitToRenderer)

From src/renderer/features/briefing/BriefingFeedbackChips.tsx test analog:
  - Use @testing-library/react render + userEvent
  - Check IPC call: spy on window.aria method with vi.fn()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SideNav Research link + complete unit + integration tests</name>
  <files>
    src/renderer/components/editorial/SideNav.tsx
    tests/unit/main/research-service.spec.ts
    tests/unit/main/search-provider-service.spec.ts
    tests/integration/research-ipc.spec.ts
  </files>
  <read_first>
    - src/renderer/components/editorial/SideNav.tsx — existing NavLink structure; find the "Workspace" NavSection to add Research link; locate icon pattern (existing nav items use SVG or lucide-react icons)
    - tests/unit/main/briefing/schedule.spec.ts — MSW server setup + cron test pattern
    - tests/unit/main/insights/ — any existing insights unit test (generateObject mock pattern)
    - tests/integration/research-ipc.spec.ts — read current stub structure from Plan 01
    - tests/unit/main/research-service.spec.ts — read current stub structure from Plan 01
    - tests/unit/main/search-provider-service.spec.ts — read current stub structure from Plan 01
    - src/main/db/migrations/embedded.ts — confirm migration 132 is registered (for integration test)
  </read_first>
  <behavior>
    SideNav: Research link added to the "Workspace" NavSection with a flask/microscope SVG icon (or pick from existing icon set). Path: /research. Label: "Research". data-testid="nav-research". Follows same active-state gold-rail pattern as other nav items.

    research-service.spec.ts (implement):
    - createResearchJob: valid input inserts row, returns id; empty title throws or returns error
    - runResearchJob: MSW mocks Brave (returns 2 results), Exa (returns 2 results), Jina (returns markdown), LLM generateObject (returns valid synthesis); assert report row written with status='done'; assert emitToRenderer called with RESEARCH_REPORT_DONE
    - runResearchJob: one provider returns 429 twice → graceful degradation (still produces report from other provider)
    - runResearchJob: generateObject throws → report.status='failed', job.status='failed'
    - detectResearchTopics: LLM returns 2 topics → 2 draft research_job rows inserted; emitToRenderer called
    - detectResearchTopics: LLM throws → no rows inserted; no error propagated (silent)

    search-provider-service.spec.ts (implement):
    - searchBrave: HTTP 200 with valid body → returns BraveResult[]
    - searchBrave: HTTP 429 → waits → retries → if second also 429 → returns []
    - searchBrave: HTTP 500 → returns []
    - searchExa: same shape as Brave tests
    - fetchWithJina: HTTP 200 → returns text
    - fetchWithJina: AbortSignal.timeout fires (mock with vi.useFakeTimers + immediate abort) → returns null
    - fetchWithJina: HTTP 404 → returns null
    - deduplicateByUrl: array with 2 duplicate urls → deduped to 1

    research-ipc.spec.ts (integration against real SQLite):
    - Setup: create in-memory SQLite via better-sqlite3, run migrations (import runMigrations from embedded.ts), insert a research_job row with status='draft'
    - Call researchJobRun handler (import registerResearchHandlers, create mock deps, call handler directly)
    - Assert: research_report row exists for the job; research_report_section rows > 0; job.status = 'done' or 'failed'
    - Teardown: db.close()
    - Note: LLM and HTTP calls must be mocked (MSW) to avoid real network calls in CI
  </behavior>
  <action>
    Modify SideNav.tsx: locate the Workspace NavSection and add a NavLink for /research with label "Research" and a simple SVG icon (a magnifying-glass or flask shape, inline SVG, 16px, following the exact style of existing nav icons). Add data-testid="nav-research".

    Implement tests/unit/main/research-service.spec.ts: MSW server setup at top. Use `vi.fn()` for logger and emitToRenderer deps. Use a real in-memory better-sqlite3 DB per test (open(':memory:'), run migration 132 SQL directly from the imported sql file). Cover all 6 behaviors listed above. Each test must use beforeEach to create a fresh DB.

    Implement tests/unit/main/search-provider-service.spec.ts: MSW server setup. No DB needed. Mock Brave endpoint `http.get('https://api.search.brave.com/*', ...)` and Exa endpoint `http.post('https://api.exa.ai/search', ...)`. Cover all 8 behaviors listed.

    Implement tests/integration/research-ipc.spec.ts: Use real SQLite (better-sqlite3 ':memory:'). Import runMigrations or apply migration SQL directly. Import registerResearchHandlers and create a minimal IpcMain mock. Call the RESEARCH_JOB_RUN handler via the handler function, MSW-mocking all HTTP calls. Assert DB state after run.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/research-service.spec.ts tests/unit/main/search-provider-service.spec.ts tests/integration/research-ipc.spec.ts --passWithNoTests 2>&1 | tail -40</automated>
  </verify>
  <done>
    SideNav has Research link with data-testid="nav-research". Unit tests for ResearchService and SearchProviderService have at minimum 6 and 8 passing tests respectively. Integration test asserts research_report row exists after researchJobRun. All passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UI component tests</name>
  <files>
    tests/unit/main/research-screen.spec.tsx
  </files>
  <read_first>
    - tests/unit/main/research-screen.spec.tsx — read current stub structure from Plan 01
    - src/renderer/features/research/ResearchScreen.tsx — actual rendered structure from Plan 02
    - src/renderer/features/research/NewResearchJobModal.tsx — disabled-button logic from Plan 02
    - src/renderer/features/research/FeedbackBar.tsx — thumbs click handler from Plan 02
    - src/renderer/features/research/ReportDocumentView.tsx — toggle prop from Plan 02
    - tests/unit/renderer/ (any existing) — testing-library setup pattern (render, userEvent, vi.fn for window.aria)
  </read_first>
  <behavior>
    - ResearchScreen renders without error when window.aria.researchJobList returns empty { jobs: [] }
    - ResearchScreen renders job cards in left rail when jobs returned
    - Document/Dashboard toggle: clicking "Dashboard" passes view='dashboard' to child; clicking "Document" passes view='document'
    - NewResearchJobModal: when window.aria.researchSecretsHas returns { hasBrave: false, hasExa: false }, Start Research button has disabled attribute
    - NewResearchJobModal: when at least one key present, button is not disabled
    - FeedbackBar: clicking thumbs-up calls window.aria.researchFeedbackSave with { thumb: 1 } (or sentiment: 'up' per spec IPC contract)
    - FeedbackBar: clicking thumbs-down calls window.aria.researchFeedbackSave with { thumb: -1 } (or sentiment: 'down')
  </behavior>
  <action>
    Implement tests/unit/main/research-screen.spec.tsx using @testing-library/react and vitest. At top of file, mock `window.aria` with vi.fn() stubs for all research IPC methods. Use `render` from @testing-library/react. For the ResearchScreen tests, mock `window.aria.researchJobList` to return canned data. For NewResearchJobModal tests, render the modal in isolation and mock `window.aria.researchSecretsHas`. For FeedbackBar tests, render FeedbackBar with fixed props and spy on `window.aria.researchFeedbackSave`. Use userEvent.click for interactions.

    Follow the exact setup pattern from existing renderer tests in tests/unit/renderer/ if they exist; otherwise follow the @testing-library/react standard setup.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/research-screen.spec.tsx --passWithNoTests 2>&1 | tail -30</automated>
  </verify>
  <done>
    7 UI tests pass: ResearchScreen empty state, ResearchScreen with jobs, toggle, disabled button (no keys), enabled button (key present), FeedbackBar thumbs-up, FeedbackBar thumbs-down.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Research feature end-to-end</name>
  <what-built>
    Full Research feature: IPC handlers, ResearchService pipeline (Brave + Exa + Jina + LLM synthesis), renderer UI with Document/Dashboard views, per-section feedback, re-run modal, transcript auto-detect hook, Settings → Integrations key rows, SideNav Research link, static ratchets, and test suite.
  </what-built>
  <how-to-verify>
    1. Start the app (`pnpm dev` or `npm run dev`)
    2. Navigate to Settings → Integrations — confirm "Research — Brave Search" and "Research — Exa" key input rows are visible
    3. Enter a Brave Search API key (or Exa key) and save — confirm "Key saved" confirmation appears
    4. Click "Research" in the SideNav — confirm /research route renders with left rail + right panel layout
    5. Click "+ New Research" — confirm slide-over modal opens with title/goals/domains/schedule fields; if only one key configured, Start Research should be enabled
    6. Create a job (title: "AI in healthcare 2025", goals: "Understand latest trends", domains: "healthcare, AI") — confirm job appears in left rail with status badge
    7. Click the job in the rail — click "Run Research" — confirm status changes to "running" then "done" (requires real API keys; if keys absent, confirm the button is disabled with tooltip)
    8. Open a report — confirm Document view shows: summary card with gold left border, findings list, sources table, FeedbackBar per section
    9. Click Dashboard toggle — confirm stat cards + coverage chart + findings grid render
    10. Click thumbs-down on a finding — click "Add note" — add a note — confirm save works without error
    11. Click "Re-run Research" — confirm RerunModal opens showing feedback summary and guidance textarea; click Re-run Research — confirm new version created and navigable via version footer strip
    12. Paste a meeting transcript in /meetings and ingest — check console for "auto-detect" log; if LLM available, check /research Suggested section for detected topics
    13. Run full test suite: `pnpm test` or `npx vitest run --passWithNoTests` — confirm all tests pass
  </how-to-verify>
  <resume-signal>Type "approved" if feature works end-to-end, or describe specific issues to address</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test MSW handlers → real network | Tests must not make real HTTP calls; MSW intercepts all Brave/Exa/Jina calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering | Integration tests — real network calls to Brave/Exa | mitigate | MSW server intercepts all external HTTP in test environment; no real API keys consumed in CI |
</threat_model>

<verification>
Full verification:
1. `npx vitest run tests/unit/main/research-service.spec.ts tests/unit/main/search-provider-service.spec.ts tests/unit/main/research-screen.spec.tsx tests/integration/research-ipc.spec.ts tests/static/research-running-ratchet.spec.ts tests/static/single-entitlement-gate-site.test.ts` — all green
2. `npx vitest run --passWithNoTests` — full suite green with no regressions
3. SideNav shows Research link reachable from sidebar (reachability grep: `grep -r "nav-research" src/renderer`)
</verification>

<success_criteria>
- Unit tests: ResearchService ≥ 6 passing, SearchProviderService ≥ 8 passing
- Integration test: researchJobRun writes research_report row to real SQLite
- UI tests: 7 passing (ResearchScreen + NewResearchJobModal + FeedbackBar)
- Static ratchets: both fully green (research-running-ratchet + single-entitlement-gate-site)
- SideNav has nav-research testid link to /research
- Full vitest suite green with no regressions
- Human verify: Report Document + Dashboard views render correct content; feedback and re-run flows work end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/11-research/11-03-SUMMARY.md` with:
- Test counts (unit / integration / UI / static)
- SideNav entry confirmed
- Any behaviors deferred or caveated
- Human verify outcome
</output>
