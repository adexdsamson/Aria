---
phase: 16-streaming-cascade-barge-in-read-only
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/shared/ipc-contract.ts
  - src/preload/index.ts
  - src/main/ipc/voice.ts
  - src/main/ipc/index.ts
  - src/main/db/migrations/136_voice_latency_log.sql
  - src/main/db/migrations/embedded.ts
  - tests/unit/main/ipc/index.spec.ts
  - tests/unit/main/voice/tts-segmenter.spec.ts
  - tests/unit/main/voice/voice-latency-log.spec.ts
  - tests/unit/main/voice/voice-session-manager.spec.ts
  - tests/unit/renderer/voice/useReadAloudQueue.spec.ts
autonomous: true
requirements: [VOICE-02, VOICE-03, VOICE-06]

must_haves:
  truths:
    # D-05 / D-06 / handler-count invariant — D-01 / D-02 / D-03 / D-06 channel set
    - "CHANNELS has exactly five new entries (VOICE_TTS_CHUNK + VOICE_ABORT + DIAGNOSTICS_VOICE_LATENCY + VOICE_FEED_ANSWER + VOICE_LATENCY_MARK per D-05/D-06/D-02/D-03); tests/unit/main/ipc/index.spec.ts asserts Object.keys(CHANNELS).length and all five stub handlers are registered so the invariant stays green from Wave 0"
    - "migration 136 creates voice_latency_log with session_id + four t_* timestamp columns per D-06"
    - "Five new IPC stub handlers registered atomically in voice.ts (VOICE_TTS_CHUNK no-op, VOICE_ABORT abort-map lookup, DIAGNOSTICS_VOICE_LATENCY empty-list, VOICE_FEED_ANSWER no-op, VOICE_LATENCY_MARK fire-and-forget no-op) so the handler-count invariant remains green"
    # Wave-0 test scaffolds — D-04 / D-05 / D-06 / D-03/D-11/D-12 (BLOCKER 2 fix)
    - "tests/unit/main/voice/tts-segmenter.spec.ts describes the D-04 segmenter contract (first-chunk flush + abbreviation deny-list) as failing skeletons"
    - "tests/unit/main/voice/voice-latency-log.spec.ts describes the D-06 write function contract as failing skeletons"
    - "tests/unit/main/voice/voice-session-manager.spec.ts describes the D-03/D-11/D-12 onChunk accumulator + barge-in persistence contract as failing skeletons; three behaviors: (a) onChunk accumulates spokenSoFar as text-deltas arrive, (b) fast abort does NOT clear the accumulator (AI SDK #8088), (c) onBargeIn writes synthetic interrupted turn via appendTurn (D-12)"
    - "tests/unit/renderer/voice/useReadAloudQueue.spec.ts describes the D-05 queue ordering + cancel contract as failing skeletons"
    # WARNING 2 fix — renderer timing path declared (D-06 SC2)
    - "VOICE_LATENCY_MARK channel (fire-and-forget send renderer→main) declared in CHANNELS + AriaApi so renderer can report t_kokoro_synth_start and t_first_audio_out; all four t_* columns populatable"
  artifacts:
    - path: "src/shared/ipc-contract.ts"
      provides: "5 new CHANNELS + CHANNEL_METHODS + AriaApi methods for D-05/D-06/D-02/D-03"
      contains: "VOICE_TTS_CHUNK"
    - path: "src/preload/index.ts"
      provides: "onVoiceTtsChunk push subscription override (mirrors onVoiceTranscript)"
      contains: "onVoiceTtsChunk"
    - path: "src/main/db/migrations/136_voice_latency_log.sql"
      provides: "D-06 voice_latency_log table DDL"
      contains: "voice_latency_log"
    - path: "src/main/db/migrations/embedded.ts"
      provides: "migration 136 embedded in EMBEDDED_MIGRATIONS array"
      contains: "136"
    - path: "tests/unit/main/voice/voice-session-manager.spec.ts"
      provides: "D-03/D-11/D-12 VoiceSessionManager failing spec scaffold (BLOCKER 2)"
      contains: "onBargeIn"
  key_links:
    - from: "src/shared/ipc-contract.ts CHANNELS.VOICE_TTS_CHUNK"
      to: "src/preload/index.ts onVoiceTtsChunk override"
      via: "ipcRenderer.on(CHANNELS.VOICE_TTS_CHUNK, handler)"
      pattern: "VOICE_TTS_CHUNK"
    - from: "src/shared/ipc-contract.ts CHANNELS.VOICE_LATENCY_MARK"
      to: "src/main/ipc/voice.ts VOICE_LATENCY_MARK stub handler"
      via: "ipcMain.handle(CHANNELS.VOICE_LATENCY_MARK, ...) — fire-and-forget; real handler in 16-04a updates VoiceSession timing fields"
      pattern: "VOICE_LATENCY_MARK"
    - from: "src/main/ipc/voice.ts VOICE_ABORT handler"
      to: "VoiceHandlersDeps.sessionAbortControllers Map"
      via: "deps.sessionAbortControllers?.get(req.sessionId)?.abort()"
      pattern: "sessionAbortControllers"
    - from: "src/main/ipc/voice.ts VOICE_FEED_ANSWER stub"
      to: "VoiceHandlersDeps.voiceSessionManager (added in 16-04a)"
      via: "deps.voiceSessionManager?.startAnswer({ sessionId, question }) — stub returns { ok: true } until 16-04a wires real manager"
      pattern: "VOICE_FEED_ANSWER"
---

<objective>
Land the Wave-0 contract foundation that all subsequent plans build against: the five new IPC
channels, migration 136 (voice_latency_log), stub handlers that satisfy the handler-count
invariant, and failing test scaffolds for the four pure-logic units introduced in Waves 1–2.

This plan mirrors Phase 15's Wave-0 pattern: contracts + stubs first so all invariants stay
green, implementation lands in later waves without ordering constraints.

Purpose: the handler-count invariant WILL fail if any plan adds CHANNELS entries without
simultaneously adding registered handlers. This plan does both atomically so Wave 1 and
Wave 2 plans can proceed without breaking CI. Five channels are required: VOICE_TTS_CHUNK
(main→renderer push), VOICE_ABORT (renderer→main invoke), DIAGNOSTICS_VOICE_LATENCY
(renderer→main read), VOICE_FEED_ANSWER (renderer→main invoke, the streaming trigger),
and VOICE_LATENCY_MARK (renderer→main fire-and-forget, carrying t_kokoro_synth_start and
t_first_audio_out timing marks so SC2 per-stage telemetry is fully populatable).

Output: 5 new IPC channels wired end-to-end (contract+preload+stub handlers), migration 136
schema, updated handler-count test (now derived from Object.keys(CHANNELS).length — no
hardcoded number), and 4 failing spec scaffolds including voice-session-manager.spec.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-CONTEXT.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-RESEARCH.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-VALIDATION.md

<interfaces>
<!-- Exact patterns to follow. Extracted from codebase via PATTERNS.md research. -->

From src/shared/ipc-contract.ts — Phase 15 CHANNELS block (the exact insertion point for Phase 16):
```
// Phase 15 — Voice I/O + Model Runtime (15-01)
VOICE_FEED_AUDIO: 'aria:voice:feed-audio',
VOICE_GET_MODEL_STATUS: 'aria:voice:model-status',
VOICE_DOWNLOAD_MODEL: 'aria:voice:download-model',
VOICE_CANCEL_TTS: 'aria:voice:cancel-tts',
VOICE_TRANSCRIPT_DELTA: 'aria:voice:transcript-delta',
VOICE_STATE_CHANGED: 'aria:voice:state-changed',
VOICE_MODEL_PROGRESS: 'aria:voice:model-progress',
```
Phase 16 adds AFTER VOICE_MODEL_PROGRESS — all five new channels:
```
VOICE_TTS_CHUNK: 'aria:voice:tts-chunk',          // push: main → renderer
VOICE_ABORT: 'aria:voice:abort',                    // invoke: renderer → main
DIAGNOSTICS_VOICE_LATENCY: 'aria:diagnostics:voice-latency', // invoke: renderer → main (read)
VOICE_FEED_ANSWER: 'aria:voice:feed-answer',        // invoke: renderer → main (trigger streaming)
VOICE_LATENCY_MARK: 'aria:voice:latency-mark',      // send: renderer → main (fire-and-forget timing)
```
CHANNEL_METHODS additions (same pattern as existing Phase 15 entries):
```
VOICE_TTS_CHUNK: 'onVoiceTtsChunk',
VOICE_ABORT: 'voiceAbort',
DIAGNOSTICS_VOICE_LATENCY: 'diagnosticsVoiceLatency',
VOICE_FEED_ANSWER: 'voiceFeedAnswer',
VOICE_LATENCY_MARK: 'voiceLatencyMark',
```
AriaApi additions (after existing onVoiceModelProgress):
```
onVoiceTtsChunk?: (cb: (chunk: { text: string; sessionId: string }) => void) => () => void;
voiceAbort(req: { sessionId: string }): Promise<{ ok: true } | IpcError>;
diagnosticsVoiceLatency(req?: { limit?: number }): Promise<VoiceLatencyLogRow[] | IpcError>;
voiceFeedAnswer(req: { sessionId: string; question: string }): Promise<{ ok: true } | IpcError>;
voiceLatencyMark(req: { sessionId: string; mark: 'kokoro_synth_start' | 'first_audio_out'; t: number }): Promise<void>;
```
VoiceLatencyLogRow must be defined (add interface inline in ipc-contract.ts).

VOICE_LATENCY_MARK is a fire-and-forget send from renderer. Because it is in CHANNELS it needs a
registered handler stub. Use ipcMain.handle with a no-op returning undefined.

From src/preload/index.ts — onVoiceTranscript push override template (lines 54-58):
```typescript
(api as unknown as Record<string, ((cb: (d: unknown) => void) => () => void)>)
  .onVoiceTranscript = (cb: (d: unknown) => void) => {
  const handler = (_e: unknown, d: unknown) => cb(d);
  ipcRenderer.on(CHANNELS.VOICE_TRANSCRIPT_DELTA, handler);
  return () => ipcRenderer.removeListener(CHANNELS.VOICE_TRANSCRIPT_DELTA, handler);
};
```
Copy replacing .onVoiceTranscript → .onVoiceTtsChunk and VOICE_TRANSCRIPT_DELTA → VOICE_TTS_CHUNK.
VOICE_ABORT, DIAGNOSTICS_VOICE_LATENCY, VOICE_FEED_ANSWER, VOICE_LATENCY_MARK are invoke/send
channels (renderer→main) — auto-mapped by buildApi(), NO manual override needed.

From src/main/ipc/voice.ts — VOICE_CANCEL_TTS ack stub pattern (template for new stubs):
```typescript
ipcMain.handle(CHANNELS.VOICE_CANCEL_TTS, async () => {
  return { ok: true };
});
```

Handler-count invariant (tests/unit/main/ipc/index.spec.ts lines 76-77):
```typescript
const expected = Object.keys(CHANNELS).length;  // auto-derives — no hardcoded number
expect(handlers.size).toBe(expected);
```
The test already uses Object.keys(CHANNELS).length dynamically. DO NOT change the assertion
formula — it self-adjusts when new channels are added. Only verify it passes after stubs land.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IPC contract — 5 new channels + preload push subscription</name>
  <files>src/shared/ipc-contract.ts, src/preload/index.ts</files>
  <read_first>
    - src/shared/ipc-contract.ts (locate CHANNELS const near line 183-192 for Phase 15 voice block; locate CHANNEL_METHODS near line 1466-1473; locate AriaApi push subscription block for onVoiceModelProgress near line 1073-1075)
    - src/preload/index.ts (locate the three Phase 15 push channel override blocks, lines 54-73; confirm buildApi() at lines 13-21 auto-maps invoke channels)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md (§ src/shared/ipc-contract.ts — exact CHANNELS/CHANNEL_METHODS/AriaApi excerpts)
  </read_first>
  <action>
    In src/shared/ipc-contract.ts: add five entries to CHANNELS after VOICE_MODEL_PROGRESS —
    VOICE_TTS_CHUNK ('aria:voice:tts-chunk'), VOICE_ABORT ('aria:voice:abort'),
    DIAGNOSTICS_VOICE_LATENCY ('aria:diagnostics:voice-latency'),
    VOICE_FEED_ANSWER ('aria:voice:feed-answer'),
    VOICE_LATENCY_MARK ('aria:voice:latency-mark').
    Add matching CHANNEL_METHODS entries: VOICE_TTS_CHUNK → 'onVoiceTtsChunk',
    VOICE_ABORT → 'voiceAbort', DIAGNOSTICS_VOICE_LATENCY → 'diagnosticsVoiceLatency',
    VOICE_FEED_ANSWER → 'voiceFeedAnswer', VOICE_LATENCY_MARK → 'voiceLatencyMark'.
    Add to AriaApi: onVoiceTtsChunk push subscription (optional, returns () => void),
    voiceAbort invoke, diagnosticsVoiceLatency invoke, voiceFeedAnswer invoke, and
    voiceLatencyMark fire-and-forget (returns Promise<void>).
    Define VoiceLatencyLogRow interface inline in ipc-contract.ts (session_id string,
    t_stt_done number, t_llm_first_token/t_first_sentence_ready/t_kokoro_synth_start/t_first_audio_out
    all number or null, recorded_at string) — avoids a circular import.

    In src/preload/index.ts: add one manual push override block for onVoiceTtsChunk using the
    exact onVoiceTranscript template (copy-rename CHANNELS.VOICE_TRANSCRIPT_DELTA →
    CHANNELS.VOICE_TTS_CHUNK, method name .onVoiceTtsChunk). VOICE_ABORT, DIAGNOSTICS_VOICE_LATENCY,
    VOICE_FEED_ANSWER, and VOICE_LATENCY_MARK are invoke/send channels — auto-mapped by buildApi(),
    no override needed.

    Run pnpm typecheck after edits. The handler-count invariant test will turn RED after this task
    (expected — all 5 handlers are added in Task 2).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: grep -c "VOICE_TTS_CHUNK\|VOICE_ABORT\|DIAGNOSTICS_VOICE_LATENCY\|VOICE_FEED_ANSWER\|VOICE_LATENCY_MARK" src/shared/ipc-contract.ts returns at least 10 (5 in CHANNELS + 5 in CHANNEL_METHODS)
    - source: grep "onVoiceTtsChunk" src/preload/index.ts returns a match in an ipcRenderer.on override block
    - source: grep "VOICE_ABORT\|VOICE_FEED_ANSWER\|VOICE_LATENCY_MARK" src/preload/index.ts returns NO ipcRenderer.on block (invoke/send channels auto-mapped)
    - source: VoiceLatencyLogRow interface declared in ipc-contract.ts
    - test-command: pnpm typecheck exits 0 (84-error baseline; 0 new errors)
  </acceptance_criteria>
  <done>5 new CHANNELS+CHANNEL_METHODS+AriaApi methods in ipc-contract.ts; onVoiceTtsChunk push override in preload/index.ts; VoiceLatencyLogRow defined; typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 136 + stub handlers + handler-count invariant verification</name>
  <files>src/main/db/migrations/136_voice_latency_log.sql, src/main/db/migrations/embedded.ts, src/main/ipc/voice.ts, src/main/ipc/index.ts, tests/unit/main/ipc/index.spec.ts</files>
  <read_first>
    - src/main/db/migrations/embedded.ts (read tail to confirm last migration is version 135; find EMBEDDED_MIGRATIONS array append pattern)
    - src/main/db/migrations/135_repair_approval_child_fks.sql (shape reference for migration DDL conventions)
    - src/main/ipc/voice.ts (read full file — registerVoiceHandlers signature, VoiceHandlersDeps interface, VOICE_CANCEL_TTS stub pattern at lines 144-147, emitToRenderer usage)
    - src/main/ipc/index.ts (find where registerVoiceHandlers is called; find db-null skip-set pattern)
    - tests/unit/main/ipc/index.spec.ts (lines 71-78: confirm the assertion is Object.keys(CHANNELS).length, NOT a hardcoded integer)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md (§ voice.ts Phase 16 additions — VoiceHandlersDeps extension, handler patterns)
  </read_first>
  <action>
    Create src/main/db/migrations/136_voice_latency_log.sql with DDL: CREATE TABLE IF NOT EXISTS
    voice_latency_log (id INTEGER PRIMARY KEY AUTOINCREMENT, session_id TEXT NOT NULL,
    t_stt_done INTEGER NOT NULL, t_llm_first_token INTEGER, t_first_sentence_ready INTEGER,
    t_kokoro_synth_start INTEGER, t_first_audio_out INTEGER,
    recorded_at TEXT NOT NULL DEFAULT (datetime('now'))). Add CREATE INDEX IF NOT EXISTS
    idx_voice_latency_session ON voice_latency_log(session_id). Append the migration entry to
    EMBEDDED_MIGRATIONS in embedded.ts as { version: 136, file: '136_voice_latency_log.sql',
    sql: `...the exact DDL above...` }.

    Extend VoiceHandlersDeps in voice.ts to add sessionAbortControllers?: Map<string, AbortController>
    and voiceSessionManager?: { startAnswer(args: { sessionId: string; question: string }): Promise<void>; onBargeIn(args: { sessionId: string }): void } (the real implementation
    comes in 16-04a; this plan just declares the slots). Add five stub handlers in
    registerVoiceHandlers:
    (1) VOICE_TTS_CHUNK: ipcMain.handle returning { ok: true } (push-only channel — stub satisfies
    the handler-count invariant; actual push direction is webContents.send, not ipcMain.handle).
    (2) VOICE_ABORT: look up deps.sessionAbortControllers?.get(req.sessionId)?.abort(); call
    deps.voiceSessionManager?.onBargeIn({ sessionId: req.sessionId }); return { ok: true as const }.
    (3) DIAGNOSTICS_VOICE_LATENCY: if !db return []; stub returns [] with TODO referencing
    voice-latency-log.ts for Wave 2.
    (4) VOICE_FEED_ANSWER: validate payload shape ({ sessionId: string; question: string });
    call deps.voiceSessionManager?.startAnswer({ sessionId, question }); return { ok: true }.
    (5) VOICE_LATENCY_MARK: no-op stub — receives { sessionId, mark, t } and returns undefined.
    The real implementation (updating VoiceSession timing fields) lands in 16-04a.

    Ensure all handlers are inside registerVoiceHandlers — do NOT register outside (IPC db-null
    skip trap: skip.add must be co-located with registration). VOICE_ABORT, VOICE_FEED_ANSWER,
    DIAGNOSTICS_VOICE_LATENCY, VOICE_TTS_CHUNK, and VOICE_LATENCY_MARK must NOT be added to the
    db-null skip set (they are safe pre-unlock: abort/latency are read-only or no-op;
    feed-answer stubs out to no-op when voiceSessionManager is absent).

    In ipc/index.ts: confirm new handlers are reached by verifying registerVoiceHandlers is called
    and no new db-null skip entries are added for these five channels.

    IMPORTANT: do NOT change the handler-count assertion formula in tests/unit/main/ipc/index.spec.ts.
    The formula already reads Object.keys(CHANNELS).length — it self-adjusts. Simply run the test to
    confirm it passes now that all five channels have corresponding handlers.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - source: src/main/db/migrations/136_voice_latency_log.sql contains "CREATE TABLE IF NOT EXISTS voice_latency_log" and "t_stt_done INTEGER NOT NULL"
    - source: grep "136" src/main/db/migrations/embedded.ts returns a match (migration appended)
    - source: grep "VOICE_ABORT\|DIAGNOSTICS_VOICE_LATENCY\|VOICE_TTS_CHUNK\|VOICE_FEED_ANSWER\|VOICE_LATENCY_MARK" src/main/ipc/voice.ts returns 5 ipcMain.handle calls
    - source: grep "sessionAbortControllers\|voiceSessionManager" src/main/ipc/voice.ts confirms both Map fields in VoiceHandlersDeps
    - test-command: npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism passes (the dynamic Object.keys(CHANNELS).length assertion is satisfied — 5 new channels + 5 new handlers)
    - source: tests/unit/main/ipc/index.spec.ts assertion formula is Object.keys(CHANNELS).length (NOT a hardcoded integer)
  </acceptance_criteria>
  <done>Migration 136 created and embedded; 5 stub handlers registered in voice.ts; handler-count invariant stays green via dynamic assertion (no hardcoded count to maintain).</done>
</task>

<task type="auto">
  <name>Task 3: Failing test scaffolds for Wave 1–2 pure-logic units (incl. voice-session-manager)</name>
  <files>tests/unit/main/voice/tts-segmenter.spec.ts, tests/unit/main/voice/voice-latency-log.spec.ts, tests/unit/main/voice/voice-session-manager.spec.ts, tests/unit/renderer/voice/useReadAloudQueue.spec.ts</files>
  <read_first>
    - tests/unit/renderer/voice/useVoiceSession.spec.ts (renderer spec structure — imports, describe/it pattern, vi.fn() usage; structural template for useReadAloudQueue spec)
    - tests/unit/main/rag/answer-service.spec.ts OR any tests/unit/main/ spec (main-process spec structure — async service test with DI; template for voice-session-manager spec)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-VALIDATION.md (§ Per-Task Verification Map — the exact behaviors each spec must test)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-RESEARCH.md (§ Pattern 2 — TtsSegmenter D-04 contract; § Pattern 7 — D-11/D-12 thread persistence; § AI SDK #8088 — onChunk accumulator, NOT onAbort)
  </read_first>
  <action>
    Create tests/unit/main/voice/tts-segmenter.spec.ts as a failing skeleton. Import TtsSegmenter
    from 'src/main/voice/tts-segmenter' (file does not exist yet — import will fail RED). Write
    describe blocks for: (a) first-chunk regime — push(delta) flushes a chunk when buffer reaches
    ~8 words on a word boundary; push() before 8 words returns empty array; (b) sentence regime —
    push accumulates until sentence boundary (period+space); does NOT split on "Dr. Smith"
    (abbreviation deny-list); does NOT split on "3.14" (decimal pattern); (c) flush() — returns
    remaining buffer, resets state. All assertions use expect(...).toBe/toEqual/toHaveLength — no
    .todo() or .skip().

    Create tests/unit/main/voice/voice-latency-log.spec.ts as a failing skeleton. Import
    writeVoiceLatencyLog and readRecentVoiceLatencyLog from 'src/main/voice/voice-latency-log'
    (file does not exist — RED). Use an in-memory better-sqlite3 Database (import Database from
    'better-sqlite3'; create + run migration SQL inline). Tests: (a) writeVoiceLatencyLog is a
    no-op when ARIA_DEBUG !== '1'; (b) when ARIA_DEBUG='1', inserts a row; (c)
    readRecentVoiceLatencyLog returns rows ordered by id desc.

    Create tests/unit/main/voice/voice-session-manager.spec.ts as a failing skeleton. Import
    createVoiceSessionManager from 'src/main/voice/voice-session-manager' (file does not exist
    — RED). The scaffold MUST assert three behaviors (D-03/D-11/D-12 — AI SDK #8088 mitigation):
    (a) onChunk accumulator: when streamVoiceAnswer calls onChunk() with a series of text deltas,
    the VoiceSession.spokenSoFar ref accumulates them in order (test by mocking streamVoiceAnswer
    to call onChunk synchronously with "Hello ", "world", " foo", then verify spokenSoFar ===
    "Hello world foo" before onDone fires);
    (b) fast abort does NOT clear the accumulator: if the AbortController is aborted after two
    onChunk calls but before onDone, spokenSoFar still holds the two accumulated deltas (confirms
    the accumulator lives in onChunk, not onAbort, per #8088);
    (c) onBargeIn writes the synthetic interrupted turn: given a session with spokenSoFar =
    "Hello world", calling onBargeIn({ sessionId }) must call appendTurn with role:'assistant'
    and text containing '[interrupted:' and 'Hello world' (D-12).
    Mock appendTurn, createThread, streamVoiceAnswer, TtsSegmenter, and writeVoiceLatencyLog as
    vi.fn(). Use at least 3 it() blocks. No .todo() or .skip().

    Create tests/unit/renderer/voice/useReadAloudQueue.spec.ts as a failing skeleton. Import
    useReadAloudQueue from 'src/renderer/features/voice/useReadAloudQueue' (file does not exist
    — RED). Mock KokoroPlayerHandle with vi.fn() speak that resolves immediately and vi.fn() cancel.
    Tests: (a) enqueue() resolves promises in order (verify via a calls array); (b) cancel()
    calls player.cancel() and resets queue; (c) enqueue after cancel works normally.

    All four specs must have at least 3 it() blocks each. They MUST fail (not error on syntax) so
    the RED phase is clear. After writing, run the full group to confirm they fail for the right
    reason (module not found or similar import error, NOT a syntax error).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/voice/tts-segmenter.spec.ts tests/unit/main/voice/voice-latency-log.spec.ts tests/unit/main/voice/voice-session-manager.spec.ts tests/unit/renderer/voice/useReadAloudQueue.spec.ts --no-file-parallelism 2>&1 | Select-String "FAIL|Cannot find module|Error"</automated>
  </verify>
  <acceptance_criteria>
    - source: each spec file has at least 3 it() blocks with expect() assertions (grep "it(" returns 3+ per file)
    - source: no .todo() or .skip() calls in the new spec files
    - source: voice-session-manager.spec.ts contains "spokenSoFar" (accumulator test), "aborted\|abort" (fast-abort test), and "interrupted\|appendTurn" (D-12 synthetic turn test)
    - behavior: running the specs produces FAIL due to "Cannot find module" or import error, NOT a syntax error — confirms scaffold is valid TypeScript pointing at not-yet-existing implementations
    - source: tests/unit/main/voice/ directory has three spec files; tests/unit/renderer/voice/ has useReadAloudQueue.spec.ts alongside the existing useVoiceSession.spec.ts
  </acceptance_criteria>
  <done>Four failing spec scaffolds exist describing D-04, D-05, D-06, D-03/D-11/D-12 contracts; they fail RED for the right reason; voice-session-manager.spec.ts asserts the AI SDK #8088 accumulator safety and D-12 interrupted turn; no skip/todo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | VOICE_ABORT, VOICE_FEED_ANSWER, VOICE_LATENCY_MARK, DIAGNOSTICS_VOICE_LATENCY cross here; session IDs and timing marks from renderer are untrusted |
| main DB → renderer read | voice_latency_log rows returned via DIAGNOSTICS_VOICE_LATENCY; debug-only, no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | VOICE_ABORT sessionId lookup | accept | Abort is fire-and-forget; worst case is aborting a wrong session (no security escalation, no write path); sessionId is a random hex string, not user-controlled data in a meaningful way |
| T-16-02 | Information Disclosure | voice_latency_log rows via DIAGNOSTICS_VOICE_LATENCY | accept | Table stores only timing integers + session IDs; no PII; debug-gated (rows only exist when ARIA_DEBUG=1); same posture as DIAGNOSTICS_ROUTING_LOG |
| T-16-03 | Elevation of Privilege | New IPC channels in write-chokepoint ratchet scope | mitigate | D-13 static ratchet (Plan 16-05) explicitly covers src/main/voice/ and src/renderer/features/voice/ — catches any future import of assertApproved/voiceConfirm |
| T-16-15 | Tampering | VOICE_LATENCY_MARK timing values from renderer | accept | Timing marks are debug-only integers; no security implication if renderer sends bogus timestamps; only affects telemetry accuracy, not application state |
</threat_model>

<verification>
- pnpm typecheck exits 0 after Task 1.
- npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism passes (dynamic Object.keys(CHANNELS).length assertion satisfied by 5 new stubs) after Task 2.
- Four spec scaffolds fail RED (module not found) after Task 3 — confirms Waves 1–2 have RED tests to turn GREEN.
- grep "voice_latency_log" src/main/db/migrations/136_voice_latency_log.sql returns a match.
- grep "136" src/main/db/migrations/embedded.ts returns a match.
</verification>

<success_criteria>
- ipc-contract.ts has 5 new CHANNELS (VOICE_TTS_CHUNK, VOICE_ABORT, DIAGNOSTICS_VOICE_LATENCY, VOICE_FEED_ANSWER, VOICE_LATENCY_MARK) + matching CHANNEL_METHODS + AriaApi methods; preload has onVoiceTtsChunk push override.
- Migration 136 voice_latency_log exists and is embedded in EMBEDDED_MIGRATIONS.
- 5 stub handlers in voice.ts satisfy the handler-count invariant (dynamic assertion green).
- 4 failing spec scaffolds exist as the RED baseline for Wave 1–2 implementations (incl. voice-session-manager.spec.ts for SC3/SC4 coverage path).
</success_criteria>

<output>
After completion, create `.planning/phases/16-streaming-cascade-barge-in-read-only/16-01-SUMMARY.md`
</output>
