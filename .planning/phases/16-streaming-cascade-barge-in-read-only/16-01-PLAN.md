---
phase: 16-streaming-cascade-barge-in-read-only
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/shared/ipc-contract.ts
  - src/preload/index.ts
  - src/main/ipc/voice.ts
  - src/main/ipc/index.ts
  - src/main/db/migrations/136_voice_latency_log.sql
  - src/main/db/migrations/embedded.ts
  - tests/unit/main/ipc/index.spec.ts
  - tests/unit/main/voice/tts-segmenter.spec.ts
  - tests/unit/main/voice/voice-latency-log.spec.ts
  - tests/unit/renderer/voice/useReadAloudQueue.spec.ts
autonomous: true
requirements: [VOICE-02, VOICE-03, VOICE-06]

must_haves:
  truths:
    # D-05 / D-06 / handler-count invariant
    - "CHANNELS has exactly 152 entries (149 + VOICE_TTS_CHUNK + VOICE_ABORT + DIAGNOSTICS_VOICE_LATENCY per D-05/D-06); tests/unit/main/ipc/index.spec.ts updated to assert 152 and green"
    - "migration 136 creates voice_latency_log with session_id + four t_* timestamp columns per D-06"
    - "Three new IPC handlers registered (VOICE_TTS_CHUNK push-stub, VOICE_ABORT fire-and-forget stub, DIAGNOSTICS_VOICE_LATENCY stub) in voice.ts so the handler-count invariant stays green"
    # Wave-0 test scaffolds — these MUST fail on first run then pass with stubs
    - "tests/unit/main/voice/tts-segmenter.spec.ts describes the D-04 segmenter contract (first-chunk flush + abbreviation deny-list) as failing skeletons"
    - "tests/unit/main/voice/voice-latency-log.spec.ts describes the D-06 write function contract as failing skeletons"
    - "tests/unit/renderer/voice/useReadAloudQueue.spec.ts describes the D-05 queue ordering + cancel contract as failing skeletons"
  artifacts:
    - path: "src/shared/ipc-contract.ts"
      provides: "3 new CHANNELS + CHANNEL_METHODS + AriaApi methods for D-05/D-06"
      contains: "VOICE_TTS_CHUNK"
    - path: "src/preload/index.ts"
      provides: "onVoiceTtsChunk push subscription override (mirrors onVoiceTranscript)"
      contains: "onVoiceTtsChunk"
    - path: "src/main/db/migrations/136_voice_latency_log.sql"
      provides: "D-06 voice_latency_log table DDL"
      contains: "voice_latency_log"
    - path: "src/main/db/migrations/embedded.ts"
      provides: "migration 136 embedded in EMBEDDED_MIGRATIONS array"
      contains: "136"
    - path: "tests/unit/main/ipc/index.spec.ts"
      provides: "handler-count invariant updated to 152"
      contains: "152"
  key_links:
    - from: "src/shared/ipc-contract.ts CHANNELS.VOICE_TTS_CHUNK"
      to: "src/preload/index.ts onVoiceTtsChunk override"
      via: "ipcRenderer.on(CHANNELS.VOICE_TTS_CHUNK, handler)"
      pattern: "VOICE_TTS_CHUNK"
    - from: "src/main/ipc/voice.ts VOICE_ABORT handler"
      to: "VoiceHandlersDeps.sessionAbortControllers Map"
      via: "deps.sessionAbortControllers?.get(req.sessionId)?.abort()"
      pattern: "sessionAbortControllers"
---

<objective>
Land the Wave-0 contract foundation that all subsequent plans build against: the three new IPC
channels, migration 136 (voice_latency_log), stub handlers that satisfy the handler-count
invariant, and failing test scaffolds for the three pure-logic units introduced in Wave 1.

This plan mirrors Phase 15's 16-01 Wave-0 pattern: contracts + stubs first so all invariants
stay green, implementation lands in later waves without ordering constraints.

Purpose: the handler-count invariant (149→152) WILL fail if any plan adds CHANNELS entries
without simultaneously adding registered handlers. This plan does both atomically so Wave 1
and Wave 2 plans can proceed without breaking CI.

Output: 3 new IPC channels wired end-to-end (contract+preload+stub handlers), migration 136
schema, updated handler-count test, and 3 failing spec scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-CONTEXT.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-RESEARCH.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-VALIDATION.md

<interfaces>
<!-- Exact patterns to follow. Extracted from codebase via PATTERNS.md research. -->

From src/shared/ipc-contract.ts — Phase 15 CHANNELS block (the exact insertion point for Phase 16):
```
// Phase 15 — Voice I/O + Model Runtime (15-01)
VOICE_FEED_AUDIO: 'aria:voice:feed-audio',
VOICE_GET_MODEL_STATUS: 'aria:voice:model-status',
VOICE_DOWNLOAD_MODEL: 'aria:voice:download-model',
VOICE_CANCEL_TTS: 'aria:voice:cancel-tts',
VOICE_TRANSCRIPT_DELTA: 'aria:voice:transcript-delta',
VOICE_STATE_CHANGED: 'aria:voice:state-changed',
VOICE_MODEL_PROGRESS: 'aria:voice:model-progress',
```
Phase 16 adds AFTER VOICE_MODEL_PROGRESS:
```
VOICE_TTS_CHUNK: 'aria:voice:tts-chunk',
VOICE_ABORT: 'aria:voice:abort',
DIAGNOSTICS_VOICE_LATENCY: 'aria:diagnostics:voice-latency',
```
CHANNEL_METHODS additions (same pattern as existing Phase 15 entries):
```
VOICE_TTS_CHUNK: 'onVoiceTtsChunk',
VOICE_ABORT: 'voiceAbort',
DIAGNOSTICS_VOICE_LATENCY: 'diagnosticsVoiceLatency',
```
AriaApi additions (after existing onVoiceModelProgress?):
```
onVoiceTtsChunk?: (cb: (chunk: { text: string; sessionId: string }) => void) => () => void;
voiceAbort(req: { sessionId: string }): Promise<{ ok: true } | IpcError>;
diagnosticsVoiceLatency(req?: { limit?: number }): Promise<VoiceLatencyLogRow[] | IpcError>;
```
VoiceLatencyLogRow must be defined (add to ipc-contract.ts exports or import from voice-latency-log.ts once created).

From src/preload/index.ts — onVoiceTranscript push override template (lines 54-58):
```typescript
(api as unknown as Record<string, ((cb: (d: unknown) => void) => () => void)>)
  .onVoiceTranscript = (cb: (d: unknown) => void) => {
  const handler = (_e: unknown, d: unknown) => cb(d);
  ipcRenderer.on(CHANNELS.VOICE_TRANSCRIPT_DELTA, handler);
  return () => ipcRenderer.removeListener(CHANNELS.VOICE_TRANSCRIPT_DELTA, handler);
};
```
Copy this block replacing .onVoiceTranscript → .onVoiceTtsChunk and VOICE_TRANSCRIPT_DELTA → VOICE_TTS_CHUNK.
VOICE_ABORT and DIAGNOSTICS_VOICE_LATENCY are invoke-direction (renderer→main) — auto-mapped by buildApi(), NO manual override needed.

From src/main/ipc/voice.ts — VOICE_CANCEL_TTS ack stub pattern (the template for VOICE_ABORT stub):
```typescript
ipcMain.handle(CHANNELS.VOICE_CANCEL_TTS, async () => {
  return { ok: true };
});
```
VOICE_ABORT stub adds sessionAbortControllers lookup (see PATTERNS.md §voice.ts Phase 16 additions).

From src/main/db/migrations/embedded.ts — append at tail of EMBEDDED_MIGRATIONS array:
```typescript
{ version: 136, file: '136_voice_latency_log.sql', sql: `...DDL...` }
```

Handler-count invariant (tests/unit/main/ipc/index.spec.ts line 77):
```typescript
expect(handlers.size).toBe(Object.keys(CHANNELS).length);  // currently 149 → must become 152
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IPC contract — 3 new channels + preload push subscription</name>
  <files>src/shared/ipc-contract.ts, src/preload/index.ts</files>
  <read_first>
    - src/shared/ipc-contract.ts (locate CHANNELS const near line 183-192 for Phase 15 voice block; locate CHANNEL_METHODS near line 1466-1473; locate AriaApi push subscription block for onVoiceModelProgress near line 1073-1075)
    - src/preload/index.ts (locate the three Phase 15 push channel override blocks, lines 54-73; confirm buildApi() at lines 13-21 auto-maps invoke channels)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md (§ src/shared/ipc-contract.ts — exact CHANNELS/CHANNEL_METHODS/AriaApi excerpts)
  </read_first>
  <action>
    In src/shared/ipc-contract.ts: add three entries to CHANNELS after VOICE_MODEL_PROGRESS — VOICE_TTS_CHUNK ('aria:voice:tts-chunk'), VOICE_ABORT ('aria:voice:abort'), DIAGNOSTICS_VOICE_LATENCY ('aria:diagnostics:voice-latency'). Add matching CHANNEL_METHODS entries: VOICE_TTS_CHUNK → 'onVoiceTtsChunk', VOICE_ABORT → 'voiceAbort', DIAGNOSTICS_VOICE_LATENCY → 'diagnosticsVoiceLatency'. Add to AriaApi: onVoiceTtsChunk push subscription (optional, returns () => void), voiceAbort invoke, and diagnosticsVoiceLatency invoke. Define VoiceLatencyLogRow interface inline in ipc-contract.ts (session_id string, t_stt_done number, t_llm_first_token/t_first_sentence_ready/t_kokoro_synth_start/t_first_audio_out all number or null, recorded_at string) — this avoids a circular import from the not-yet-created voice-latency-log.ts.

    In src/preload/index.ts: add one manual push override block for onVoiceTtsChunk using the exact onVoiceTranscript template (copy-rename CHANNELS.VOICE_TRANSCRIPT_DELTA → CHANNELS.VOICE_TTS_CHUNK, method name .onVoiceTtsChunk). VOICE_ABORT and DIAGNOSTICS_VOICE_LATENCY are invoke-direction — auto-mapped by buildApi(), no override needed.

    Run pnpm typecheck after edits. The handler-count invariant test will turn RED after this task (expected — handlers are added in Task 2).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: grep -c "VOICE_TTS_CHUNK\|VOICE_ABORT\|DIAGNOSTICS_VOICE_LATENCY" src/shared/ipc-contract.ts returns at least 6 (3 in CHANNELS + 3 in CHANNEL_METHODS)
    - source: grep "onVoiceTtsChunk" src/preload/index.ts returns a match in an ipcRenderer.on override block
    - source: grep "VOICE_ABORT\|DIAGNOSTICS_VOICE_LATENCY" src/preload/index.ts returns NO ipcRenderer.on block (invoke channels auto-mapped)
    - source: VoiceLatencyLogRow interface declared in ipc-contract.ts
    - test-command: pnpm typecheck exits 0 (84-error baseline; 0 new errors)
  </acceptance_criteria>
  <done>3 new CHANNELS+CHANNEL_METHODS+AriaApi methods in ipc-contract.ts; onVoiceTtsChunk push override in preload/index.ts; VoiceLatencyLogRow defined; typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 136 + stub handlers + handler-count invariant update</name>
  <files>src/main/db/migrations/136_voice_latency_log.sql, src/main/db/migrations/embedded.ts, src/main/ipc/voice.ts, src/main/ipc/index.ts, tests/unit/main/ipc/index.spec.ts</files>
  <read_first>
    - src/main/db/migrations/embedded.ts (read tail to confirm last migration is version 135; find EMBEDDED_MIGRATIONS array append pattern)
    - src/main/db/migrations/135_repair_approval_child_fks.sql (shape reference for migration DDL conventions)
    - src/main/ipc/voice.ts (read full file — registerVoiceHandlers signature, VoiceHandlersDeps interface, VOICE_CANCEL_TTS stub pattern at lines 144-147, emitToRenderer usage)
    - src/main/ipc/index.ts (find where registerVoiceHandlers is called; find db-null skip-set pattern)
    - tests/unit/main/ipc/index.spec.ts (line 77: the handler-count assertion — confirm current value is 149)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md (§ voice.ts Phase 16 additions — VoiceHandlersDeps extension, VOICE_ABORT handler, DIAGNOSTICS_VOICE_LATENCY handler)
  </read_first>
  <action>
    Create src/main/db/migrations/136_voice_latency_log.sql with DDL: CREATE TABLE IF NOT EXISTS voice_latency_log (id INTEGER PRIMARY KEY AUTOINCREMENT, session_id TEXT NOT NULL, t_stt_done INTEGER NOT NULL, t_llm_first_token INTEGER, t_first_sentence_ready INTEGER, t_kokoro_synth_start INTEGER, t_first_audio_out INTEGER, recorded_at TEXT NOT NULL DEFAULT (datetime('now'))). Add CREATE INDEX IF NOT EXISTS idx_voice_latency_session ON voice_latency_log(session_id). Append the migration entry to EMBEDDED_MIGRATIONS in embedded.ts as { version: 136, file: '136_voice_latency_log.sql', sql: `...the exact DDL above...` }.

    Extend VoiceHandlersDeps in voice.ts to add sessionAbortControllers?: Map<string, AbortController> (the real payload comes in Wave 2 Plan 16-04; this plan just declares the slot). Add three stub handlers in registerVoiceHandlers: VOICE_TTS_CHUNK (push-only channel — register as ipcMain.handle with a no-op returning { ok: true }; this satisfies the handler-count invariant without affecting the push direction), VOICE_ABORT (look up deps.sessionAbortControllers?.get(req.sessionId)?.abort(); return { ok: true as const }), and DIAGNOSTICS_VOICE_LATENCY (db-null guard: if !db return []; import and call readRecentVoiceLatencyLog(db, limit) — but since voice-latency-log.ts does not exist yet, stub with return []; add a TODO comment referencing voice-latency-log.ts for Wave 2). Ensure all new handlers are registered before the closing of registerVoiceHandlers — do NOT add them outside (IPC db-null skip trap: handlers must be co-located with registration).

    In ipc/index.ts: confirm new handlers are registered by verifying registerVoiceHandlers is called in the same place and the three new CHANNELS are not added to the db-null skip set (they are safe to run pre-unlock: abort and latency-log are read-only/fire-and-forget; tts-chunk is push-only).

    Update tests/unit/main/ipc/index.spec.ts: change the handler-count assertion from 149 to 152.

    Run: npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - source: src/main/db/migrations/136_voice_latency_log.sql contains "CREATE TABLE IF NOT EXISTS voice_latency_log" and "t_stt_done INTEGER NOT NULL"
    - source: grep "136" src/main/db/migrations/embedded.ts returns a match (migration appended)
    - source: grep "VOICE_ABORT\|DIAGNOSTICS_VOICE_LATENCY\|VOICE_TTS_CHUNK" src/main/ipc/voice.ts returns 3 ipcMain.handle calls
    - source: grep "sessionAbortControllers" src/main/ipc/voice.ts confirms the Map field in VoiceHandlersDeps
    - test-command: npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism passes with assertion 152
    - source: tests/unit/main/ipc/index.spec.ts contains "152" (not "149")
  </acceptance_criteria>
  <done>Migration 136 created and embedded; 3 stub handlers registered in voice.ts; handler-count invariant updated to 152 and green.</done>
</task>

<task type="auto">
  <name>Task 3: Failing test scaffolds for Wave 1 pure-logic units</name>
  <files>tests/unit/main/voice/tts-segmenter.spec.ts, tests/unit/main/voice/voice-latency-log.spec.ts, tests/unit/renderer/voice/useReadAloudQueue.spec.ts</files>
  <read_first>
    - tests/unit/renderer/voice/useVoiceSession.spec.ts (renderer spec structure — imports, describe/it pattern, vi.fn() usage; this is the structural template for useReadAloudQueue spec)
    - tests/unit/main/rag/answer-service.spec.ts OR any tests/unit/main/ spec (main-process spec structure — no Electron mock needed for pure utils)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-VALIDATION.md (§ Per-Task Verification Map — the exact behaviors each spec must test)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-RESEARCH.md (§ Pattern 2 — TtsSegmenter D-04 contract; § Pattern 3 — D-05 queue contract)
  </read_first>
  <action>
    Create tests/unit/main/voice/tts-segmenter.spec.ts as a failing skeleton. Import TtsSegmenter from 'src/main/voice/tts-segmenter' (file does not exist yet — import will throw at run time, making tests fail RED as required). Write describe blocks for: (a) first-chunk regime — push(delta) flushes a chunk when buffer reaches ~8 words on a word boundary; push() before 8 words returns empty array; (b) sentence regime — after first chunk flushed, push accumulates until sentence boundary (period+space); does NOT split on "Dr. Smith" (abbreviation deny-list); does NOT split on "3.14" (decimal pattern); (c) flush() — returns remaining buffer, resets state; all assertions use expect(...).toBe/toEqual/toHaveLength — no .todo() or .skip().

    Create tests/unit/main/voice/voice-latency-log.spec.ts as a failing skeleton. Import writeVoiceLatencyLog and readRecentVoiceLatencyLog from 'src/main/voice/voice-latency-log' (file does not exist — RED). Use an in-memory better-sqlite3 database (import Database from 'better-sqlite3'; create + run migration SQL inline). Tests: (a) writeVoiceLatencyLog is a no-op when ARIA_DEBUG !== '1'; (b) when ARIA_DEBUG='1', inserts a row; (c) readRecentVoiceLatencyLog returns rows ordered by id desc.

    Create tests/unit/renderer/voice/useReadAloudQueue.spec.ts as a failing skeleton. Import useReadAloudQueue from 'src/renderer/features/voice/useReadAloudQueue' (file does not exist — RED). Mock KokoroPlayerHandle with a vi.fn() speak that resolves immediately. Tests: (a) enqueue() resolves promises in order (verify order via a calls array); (b) cancel() resets the queue — enqueued items after cancel do not call speak; (c) enqueue after cancel works normally.

    All three specs must have at least 3 it() blocks each. They MUST fail (not error on syntax) so the RED phase is clear. After writing, run the full group to confirm they fail for the right reason (module not found, not a syntax error).
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/voice/tts-segmenter.spec.ts tests/unit/main/voice/voice-latency-log.spec.ts tests/unit/renderer/voice/useReadAloudQueue.spec.ts --no-file-parallelism 2>&1 | Select-String "FAIL|Cannot find module|Error"</automated>
  </verify>
  <acceptance_criteria>
    - source: each spec file has at least 3 it() blocks with expect() assertions (grep "it(" returns 3+ per file)
    - source: no .todo() or .skip() calls in the new spec files
    - behavior: running the specs produces FAIL due to "Cannot find module" (file not found) or similar import error, NOT a syntax error — confirms scaffold is valid TypeScript pointing at not-yet-existing implementations
    - source: tests/unit/main/voice/ directory exists with two spec files; tests/unit/renderer/voice/ directory exists with useReadAloudQueue.spec.ts alongside the existing useVoiceSession.spec.ts
  </acceptance_criteria>
  <done>Three failing spec scaffolds exist describing the D-04, D-05, D-06 contracts; they fail RED for the right reason (missing implementation files); no skip/todo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | VOICE_ABORT and DIAGNOSTICS_VOICE_LATENCY cross here; session IDs from renderer are untrusted |
| main DB → renderer read | voice_latency_log rows returned via DIAGNOSTICS_VOICE_LATENCY; debug-only, no PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | VOICE_ABORT sessionId lookup | accept | Abort is fire-and-forget; worst case is aborting a wrong session (no security escalation, no write path); sessionId is a random hex string, not user-controlled data in a meaningful way |
| T-16-02 | Information Disclosure | voice_latency_log rows via DIAGNOSTICS_VOICE_LATENCY | accept | Table stores only timing integers + session IDs; no PII; debug-gated (rows only exist when ARIA_DEBUG=1); same posture as DIAGNOSTICS_ROUTING_LOG |
| T-16-03 | Elevation of Privilege | New IPC channels in write-chokepoint ratchet scope | mitigate | D-13 static ratchet (Plan 16-05) explicitly covers src/main/voice/ and src/renderer/features/voice/ — catches any future import of assertApproved/voiceConfirm |
</threat_model>

<verification>
- pnpm typecheck exits 0 after Task 1.
- npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism passes with assertion 152 after Task 2.
- Three spec scaffolds fail RED (module not found) after Task 3 — confirms Wave 1 has RED tests to turn GREEN.
- grep "voice_latency_log" src/main/db/migrations/136_voice_latency_log.sql returns a match.
- grep "136" src/main/db/migrations/embedded.ts returns a match.
</verification>

<success_criteria>
- ipc-contract.ts has 3 new CHANNELS (VOICE_TTS_CHUNK, VOICE_ABORT, DIAGNOSTICS_VOICE_LATENCY) + matching CHANNEL_METHODS + AriaApi methods; preload has onVoiceTtsChunk push override.
- Migration 136 voice_latency_log exists and is embedded in EMBEDDED_MIGRATIONS.
- 3 stub handlers in voice.ts satisfy the handler-count invariant (149→152 green).
- 3 failing spec scaffolds exist as the RED baseline for Wave 1 implementations.
</success_criteria>

<output>
After completion, create `.planning/phases/16-streaming-cascade-barge-in-read-only/16-01-SUMMARY.md`
</output>
