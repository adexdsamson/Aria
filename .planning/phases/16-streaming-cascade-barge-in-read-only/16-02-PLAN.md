---
phase: 16-streaming-cascade-barge-in-read-only
plan: 02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - src/main/voice/tts-segmenter.ts
  - src/main/voice/voice-latency-log.ts
  - src/main/rag/answer-service.ts
  - tests/unit/main/voice/tts-segmenter.spec.ts
  - tests/unit/main/voice/voice-latency-log.spec.ts
autonomous: true
requirements: [VOICE-03, VOICE-06]

must_haves:
  truths:
    # D-04 TtsSegmenter
    - "TtsSegmenter.push() flushes first ~8-word boundary fragment immediately (D-04 first-chunk regime), driving first-audio p50 < 900ms for SC2"
    - "TtsSegmenter.push() accumulates full sentences in steady-state and does NOT split on 'Dr. Smith', 'Prof.', '3.14' (D-04 abbreviation deny-list)"
    # D-06 latency log
    - "writeVoiceLatencyLog is a no-op when ARIA_DEBUG !== '1' (zero overhead in production) per D-06"
    - "writeVoiceLatencyLog inserts a row into voice_latency_log when ARIA_DEBUG=1 per D-06"
    # D-03/D-11/D-12 streamVoiceAnswer
    - "streamVoiceAnswer accumulates spokenSoFar via onChunk (NOT onAbort) per D-03 — AI SDK #8088 mitigation"
    - "streamVoiceAnswer is scoped to LOCAL route only (PII round-trip deferred per Open Question 1/Pitfall 8)"
    - "streamVoiceAnswer enforces the same 4096-char question cap as ask() per ASVS V5"
    - "streamVoiceAnswer calls appendTurn for both user and assistant roles per D-11 thread persistence"
  artifacts:
    - path: "src/main/voice/tts-segmenter.ts"
      provides: "D-04 hybrid first-chunk+sentence segmenter (TtsSegmenter class)"
      contains: "TtsSegmenter"
    - path: "src/main/voice/voice-latency-log.ts"
      provides: "D-06 writeVoiceLatencyLog + readRecentVoiceLatencyLog (mirrors routingLog.ts)"
      contains: "writeVoiceLatencyLog"
    - path: "src/main/rag/answer-service.ts"
      provides: "streamVoiceAnswer alongside ask() (D-03/D-05 streaming path)"
      contains: "streamVoiceAnswer"
  key_links:
    - from: "src/main/voice/tts-segmenter.ts TtsSegmenter.push()"
      to: "src/main/voice/voice-session-manager.ts streaming loop (Plan 16-04)"
      via: "segmenter.push(textDelta) → chunks → emitToRenderer(VOICE_TTS_CHUNK)"
      pattern: "TtsSegmenter"
    - from: "src/main/rag/answer-service.ts streamVoiceAnswer"
      to: "src/main/voice/voice-session-manager.ts (Plan 16-04)"
      via: "streamVoiceAnswer(deps, { question, threadId, signal, onChunk, onDone })"
      pattern: "streamVoiceAnswer"
    - from: "src/main/voice/voice-latency-log.ts writeVoiceLatencyLog"
      to: "src/main/voice/voice-session-manager.ts (Plan 16-04)"
      via: "writeVoiceLatencyLog(db, { session_id, t_stt_done, t_llm_first_token, ... })"
      pattern: "writeVoiceLatencyLog"
---

<objective>
Implement the three pure main-process logic units that the Wave 2 session manager orchestrates:
the D-04 TtsSegmenter (hybrid first-chunk + sentence boundary logic), the D-06 voice latency
log writer/reader, and the D-03/D-11 streamVoiceAnswer function in answer-service.ts.

All three are independently unit-testable with no Electron/IPC/renderer dependencies, which
is why they land in Wave 1 alongside Plan 16-03 (renderer logic) — no file conflicts.

Purpose: turning the failing Wave-0 spec scaffolds GREEN for the segmenter and latency log;
adding streamVoiceAnswer as a new export alongside ask() so the Wave 2 orchestrator has all
building blocks ready without any changes to the existing ask() path.

Output: tts-segmenter.ts (class), voice-latency-log.ts (pure utility), streamVoiceAnswer added
to answer-service.ts, two spec files turned GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-CONTEXT.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-RESEARCH.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from codebase + PATTERNS.md. -->

From src/main/llm/routingLog.ts — the exact structural template for voice-latency-log.ts:
The file exports writeRoutingLog(db, entry) gated by no debug flag (always writes),
and readRecentRoutingLog(db, limit=100) with safeLimit = Math.max(1, Math.min(1000, ...)).
INSERT_SQL is a module-level constant. No class, no DI, pure exported functions.

ARIA_DEBUG gate pattern (from src/main/scheduling/propose.ts line 162):
  if (process.env.ARIA_DEBUG !== '1') return;

From src/main/rag/answer-service.ts — key signatures the executor MUST NOT change:
  export interface AnswerService { ask(req: RagAskRequest): Promise<RagAskResponse>; }
  export function createAnswerService(deps: AnswerServiceDeps): AnswerService
  ask() calls: hybridRetrieve, getThread(db, threadId, {lastN:6}), routeAnswer(), buildLocalPrompt/buildFrontierPrompt, appendTurn
  ask() applies: question.length > 4096 guard, tokenizeForFrontier on FRONTIER route

streamVoiceAnswer is a new STANDALONE exported function (NOT added to AnswerService interface):
  export async function streamVoiceAnswer(
    deps: Pick<AnswerServiceDeps, 'db' | 'embedClient' | 'vectorStore'>,
    args: StreamVoiceAnswerArgs
  ): Promise<void>

streamText API (ai@6.0.185, VERIFIED):
  streamText({ model, prompt, abortSignal, onChunk: ({chunk}) => { if(chunk.type==='text-delta') ... }, onError })
  Drain via: for await (const _ of result.textStream) {}
  D-03 critical: spokenSoFar accumulated in onChunk, NOT in onAbort (AI SDK #8088 fast-abort)

From src/main/rag/threads.ts (VERIFIED signatures):
  createThread(db, { title?: string }): ThreadRow
  appendTurn(db, { threadId, role, text, citations?, routing? }): TurnRow
  getThread(db, threadId, { lastN?: number }): { thread, turns } | null

From src/main/rag/answer-router.ts:
  routeAnswer(question, chunks) returns { route: 'LOCAL' | 'FRONTIER', ... }
  buildLocalPrompt({ question, chunks, threadHistory })
  buildFrontierPrompt({ question, chunks, threadHistory }, redactor)
  streamVoiceAnswer is LOCAL-only: skip buildFrontierPrompt / tokenizeForFrontier

TtsSegmenter from RESEARCH.md Pattern 2 / PATTERNS.md §tts-segmenter.ts:
  Deny-list RE: /\b(Mr|Mrs|Dr|Prof|Sr|Jr|vs|etc|i\.e|e\.g\.)$/i
  Decimal RE: /\d\.\d$/
  Sentence end RE: /[.!?](?:\s|$)/g
  First chunk: flush on word boundary at firstChunkWords (default 8); split(/\s+/)
  flush(): returns remaining buffer, resets firstChunkFlushed + buffer
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TtsSegmenter — D-04 hybrid first-chunk + sentence segmenter</name>
  <files>src/main/voice/tts-segmenter.ts, tests/unit/main/voice/tts-segmenter.spec.ts</files>
  <behavior>
    - push() with fewer than 8 words returns [] (no premature flush)
    - push() with 8+ words flushes first chunk on a word boundary, returns 1-element array
    - second push() before a sentence boundary returns [] (accumulating)
    - push() with sentence-ending punctuation (". ", "! ", "? ") flushes sentence(s), returns array
    - push() with "Dr. Smith said" does NOT flush at "Dr." (abbreviation deny-list)
    - push() with "3.14 is pi" does NOT flush at "3." (decimal deny-list)
    - flush() returns remaining buffer contents and resets state
  </behavior>
  <read_first>
    - tests/unit/main/voice/tts-segmenter.spec.ts (the Wave-0 scaffold — update it to test the real implementation)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-RESEARCH.md (§ Pattern 2 — full TtsSegmenter pseudocode with deny-list REs)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md (§ src/main/voice/tts-segmenter.ts — structural analog is routingLog.ts; algorithm from RESEARCH.md Pattern 2)
    - src/main/llm/routingLog.ts (structural reference: pure class/functions, no imports from electron or ai)
  </read_first>
  <action>
    Create src/main/voice/tts-segmenter.ts. Export TtsSegmenter class with private fields: buffer (string, default ''), firstChunkFlushed (boolean, default false), and constructor parameter firstChunkWords (number, default 8). No imports from electron, ai, or any project service — pure TypeScript.

    push(delta: string): string[] — appends delta to buffer. If firstChunkFlushed is false: count words by splitting on /\s+/.filter(Boolean); if count >= firstChunkWords, find the nth whitespace boundary (index of the N-th space+), slice buffer up to that boundary as the first chunk, set firstChunkFlushed=true, remainder stays in buffer. If firstChunkFlushed is true: run regex /[.!?](?:\s|$)/g over buffer; for each match, check the text up to the match end does NOT match ABBREVIATION_RE (/\b(Mr|Mrs|Dr|Prof|Sr|Jr|vs|etc|i\.e|e\.g\.)$/i) AND does NOT match DECIMAL_RE (/\d\.\d$/); if safe, flush that sentence to output, advance lastEnd; after loop, trim consumed prefix from buffer. Return all flushed chunks as string[].

    flush(): string — returns buffer.trim(), resets buffer='', firstChunkFlushed=false.

    Update tests/unit/main/voice/tts-segmenter.spec.ts to test the real TtsSegmenter import. All 7 behavior cases above must pass. The abbreviation test must show "Dr. Smith said hello world foo." does not split at "Dr." and does flush the full sentence at the final period-space.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/voice/tts-segmenter.spec.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - source: src/main/voice/tts-segmenter.ts exports TtsSegmenter class with push(delta:string):string[] and flush():string methods
    - source: grep "ABBREVIATION_RE\|Mr\|Mrs\|Dr\|Prof" src/main/voice/tts-segmenter.ts confirms deny-list
    - source: grep "DECIMAL_RE\|\\\\d\\.\\\\d" src/main/voice/tts-segmenter.ts confirms decimal guard
    - source: no import from 'electron' or 'ai' in tts-segmenter.ts
    - test-command: npx vitest run tests/unit/main/voice/tts-segmenter.spec.ts --no-file-parallelism passes (7+ it() blocks green)
    - test-command: pnpm typecheck exits 0 (0 new errors beyond 84-error baseline)
  </acceptance_criteria>
  <done>TtsSegmenter class exported; D-04 first-chunk flush + abbreviation/decimal deny-list tested and green; no electron/ai imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: voice-latency-log — D-06 ARIA_DEBUG-gated writer + reader</name>
  <files>src/main/voice/voice-latency-log.ts, tests/unit/main/voice/voice-latency-log.spec.ts</files>
  <behavior>
    - writeVoiceLatencyLog returns immediately (no DB write) when process.env.ARIA_DEBUG !== '1'
    - writeVoiceLatencyLog inserts a row to voice_latency_log when ARIA_DEBUG=1; optional t_* fields store null when not provided
    - readRecentVoiceLatencyLog returns rows in descending id order; limit clamps to [1,1000]
    - readRecentVoiceLatencyLog returns [] on empty table
  </behavior>
  <read_first>
    - tests/unit/main/voice/voice-latency-log.spec.ts (the Wave-0 scaffold — update to test real implementation)
    - src/main/llm/routingLog.ts (exact structural template — INSERT_SQL constant, writeRoutingLog, readRecentRoutingLog, safeLimit clamping, Db type usage)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md (§ src/main/voice/voice-latency-log.ts — exact field mapping + ARIA_DEBUG gate)
    - src/main/db/migrations/136_voice_latency_log.sql (the table DDL that the spec must CREATE before testing)
  </read_first>
  <action>
    Create src/main/voice/voice-latency-log.ts mirroring src/main/llm/routingLog.ts structure exactly. Import Db type from src/main/db. Export VoiceLatencyInput interface: { session_id: string; t_stt_done: number; t_llm_first_token?: number | null; t_first_sentence_ready?: number | null; t_kokoro_synth_start?: number | null; t_first_audio_out?: number | null }. Export VoiceLatencyRow type (matches DB columns). Define INSERT_SQL and SELECT_RECENT_SQL as module-level constants.

    writeVoiceLatencyLog(db: Db, e: VoiceLatencyInput): void — first line: if (process.env.ARIA_DEBUG !== '1') return; then db.prepare(INSERT_SQL).run(...). readRecentVoiceLatencyLog(db: Db, limit = 100): VoiceLatencyRow[] — clamp limit with Math.max(1, Math.min(1000, Math.round(limit))).

    Update tests/unit/main/voice/voice-latency-log.spec.ts: use in-memory better-sqlite3 Database, run the 136 migration SQL inline in beforeEach, test all 4 behavior cases. Use process.env.ARIA_DEBUG manipulation (set/restore in beforeEach/afterEach).

    Also update src/main/ipc/voice.ts to replace the Task 2 stub for DIAGNOSTICS_VOICE_LATENCY: import readRecentVoiceLatencyLog from src/main/voice/voice-latency-log and call it (replaces the return [] stub). Run pnpm typecheck.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/voice/voice-latency-log.spec.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - source: grep "ARIA_DEBUG" src/main/voice/voice-latency-log.ts confirms debug gate as first statement in writeVoiceLatencyLog
    - source: grep "readRecentVoiceLatencyLog" src/main/ipc/voice.ts confirms the stub is replaced with a real import
    - test-command: npx vitest run tests/unit/main/voice/voice-latency-log.spec.ts --no-file-parallelism passes (4+ it() blocks green)
    - test-command: npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism still passes at 152
    - test-command: pnpm typecheck exits 0
  </acceptance_criteria>
  <done>voice-latency-log.ts exports writeVoiceLatencyLog (ARIA_DEBUG-gated) and readRecentVoiceLatencyLog; spec green; DIAGNOSTICS_VOICE_LATENCY handler in voice.ts wired to real reader.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: streamVoiceAnswer — D-03 streaming LLM path alongside ask()</name>
  <files>src/main/rag/answer-service.ts</files>
  <behavior>
    - streamVoiceAnswer returns early with onDone('') when question.length > 4096
    - streamVoiceAnswer calls onChunk for each text-delta received from streamText (D-03 accumulator)
    - streamVoiceAnswer calls appendTurn for the user turn before streaming and assistant turn after onDone
    - streamVoiceAnswer uses buildLocalPrompt (LOCAL-only, not buildFrontierPrompt) per scope decision
    - streamVoiceAnswer passes args.signal as abortSignal to streamText (D-03 AbortController wire-up)
    - streamVoiceAnswer does NOT modify, break, or change the signature of existing ask()
  </behavior>
  <read_first>
    - src/main/rag/answer-service.ts (FULL FILE — read the entire ask() implementation; understand ask()'s DI deps, the hybridRetrieve call, routeAnswer, buildLocalPrompt, appendTurn patterns; locate the createAnswerService factory to understand where to add streamVoiceAnswer)
    - src/main/rag/answer-llm.ts (confirm it uses generateObject; streamVoiceAnswer must NOT touch this file)
    - src/main/rag/threads.ts (createThread, appendTurn, getThread exact signatures)
    - src/main/rag/answer-router.ts (routeAnswer, buildLocalPrompt signatures)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md (§ src/main/rag/answer-service.ts — streamVoiceAnswer signature + streamText call pattern)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-RESEARCH.md (§ Critical Finding: answer-llm.ts uses generateObject; § AI SDK 6 streamText API; § Pitfall 8 — PII LOCAL-only scope)
  </read_first>
  <action>
    Add streamVoiceAnswer as a new standalone exported async function in src/main/rag/answer-service.ts. It is NOT added to the AnswerService interface and NOT inside createAnswerService — it is a module-level export. Use Pick<AnswerServiceDeps, 'db' | 'embedClient' | 'vectorStore'> as the deps type (does NOT need localLlm or llm — it imports getLocalModel() directly). Export StreamVoiceAnswerArgs interface: { db: Db; question: string; threadId: string; signal: AbortSignal; onChunk: (textDelta: string) => void; onDone: (fullText: string) => void }.

    Implementation order inside streamVoiceAnswer: (1) question.length > 4096 guard → call onDone('') and return; (2) appendTurn user turn (threadId, role:'user', text:question); (3) hybridRetrieve(question, db, embedClient, vectorStore) — use the same call signature as ask(); (4) getThread(db, threadId, { lastN: 6 }) for thread history; (5) buildLocalPrompt({ question, chunks: routerChunks, threadHistory }); (6) import streamText from 'ai' and call streamText({ model: getLocalLlmModel() or equivalent, prompt, abortSignal: args.signal, onChunk: ({chunk}) => { if (chunk.type==='text-delta') { spokenSoFar += chunk.textDelta; args.onChunk(chunk.textDelta); } }, onError: ({ error }) => { /* D-03: spokenSoFar safe in accumulator */ void error; } }); (7) drain: for await (const _ of result.textStream) {}; (8) appendTurn assistant turn with the accumulated spokenSoFar.

    To get the local model: look at how ask() calls localLlm.generate() — find how localLlm is constructed in the deps and replicate getting the model. The exact pattern is available in createAnswerService deps binding — use a similar approach for streamVoiceAnswer's deps Pick.

    Important: do NOT touch the AnswerService interface, do NOT change ask(), do NOT change answer-llm.ts. The existing ask() path must be entirely unmodified.

    There is no dedicated spec file for this function (it is integration-tested via voice-session-manager.spec.ts in Wave 2). The verify step is typecheck only — confirm the new export compiles and ask() tests remain unbroken.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - source: grep "export.*streamVoiceAnswer\|export async function streamVoiceAnswer" src/main/rag/answer-service.ts returns a match
    - source: grep "export interface StreamVoiceAnswerArgs" src/main/rag/answer-service.ts returns a match
    - source: grep "AnswerService" src/main/rag/answer-service.ts — the AnswerService interface does NOT contain streamVoiceAnswer (it's a standalone export)
    - source: grep "abortSignal\|args\.signal" src/main/rag/answer-service.ts returns a match in streamVoiceAnswer body (D-03 AbortController wired)
    - source: grep "onChunk\|text-delta" src/main/rag/answer-service.ts returns a match (accumulator pattern)
    - source: grep "buildFrontierPrompt\|tokenizeForFrontier" src/main/rag/answer-service.ts — these do NOT appear inside streamVoiceAnswer (LOCAL-only scope per Pitfall 8)
    - test-command: pnpm typecheck exits 0 (0 new errors)
  </acceptance_criteria>
  <done>streamVoiceAnswer exported alongside ask() in answer-service.ts; LOCAL-only route; 4096-char guard; onChunk accumulator wired; appendTurn for both turns; ask() unchanged; typechecks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| voice question text → streamText | Question text from the STT transcript enters the LLM prompt; no eval, length-capped at 4096 |
| streamText response → onChunk callback | LLM output flows back to caller via callback; no write path touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | streamVoiceAnswer prompt injection via spoken question | mitigate | 4096-char cap mirrors ask(); spoken text is treated as data (wrapped in prompt template, not eval'd); same V5 input-validation posture as ask() |
| T-16-05 | Information Disclosure | streamVoiceAnswer LOCAL-only scope | accept | LOCAL route runs on-device; no PII leaves the machine; FRONTIER streaming deferred to Phase 17 per Pitfall 8 / Open Question 1 |
| T-16-06 | Information Disclosure | voice_latency_log timing data | accept | Stores only timing integers + random session IDs; debug-gated; no query text, no audio, no PII |
</threat_model>

<verification>
- npx vitest run tests/unit/main/voice/tts-segmenter.spec.ts --no-file-parallelism: all green.
- npx vitest run tests/unit/main/voice/voice-latency-log.spec.ts --no-file-parallelism: all green.
- npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism: still 152 green (DIAGNOSTICS stub upgraded, count unchanged).
- pnpm typecheck exits 0.
- Existing ask() spec (if present) still passes: npx vitest run tests/unit/main/rag/ --no-file-parallelism.
</verification>

<success_criteria>
- TtsSegmenter exported and spec green: D-04 first-chunk regime + abbreviation/decimal deny-list verified.
- writeVoiceLatencyLog/readRecentVoiceLatencyLog exported and spec green: D-06 ARIA_DEBUG gate + DB insert/select verified.
- streamVoiceAnswer exported alongside ask(): LOCAL-only, 4096 guard, onChunk accumulator, appendTurn both turns, abortSignal wired. ask() untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/16-streaming-cascade-barge-in-read-only/16-02-SUMMARY.md`
</output>
