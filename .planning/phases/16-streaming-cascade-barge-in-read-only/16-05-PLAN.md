---
phase: 16-streaming-cascade-barge-in-read-only
plan: 05
type: execute
wave: 3
depends_on: [16-04a, 16-04b]
files_modified:
  - tests/static/voice-streaming-no-write.spec.ts
autonomous: true
requirements: [VOICE-02, VOICE-03, VOICE-06]

must_haves:
  truths:
    # D-13 read-only static ratchet
    - "tests/static/voice-streaming-no-write.spec.ts walks src/main/voice/ and src/renderer/features/voice/ and asserts zero imports of assertApproved/voiceConfirm/sendApprovedEmail/applyCalendarChange/pushApprovedMeetingActions per D-13"
    - "The ratchet passes GREEN on the Phase 16 codebase (all voice streaming modules are read-only)"
    - "The ratchet is structurally identical to tests/static/voice-routes-through-staging.spec.ts (walk + stripComments + identifier RE pattern)"
    - "The ratchet uses missing-dir guards so it passes even if src/main/voice/ or src/renderer/features/voice/ is absent (W-1 guard from the existing template)"
  artifacts:
    - path: "tests/static/voice-streaming-no-write.spec.ts"
      provides: "D-13 read-only static ratchet for Phase 16 voice streaming modules"
      contains: "voice-streaming-no-write"
  key_links:
    - from: "tests/static/voice-streaming-no-write.spec.ts"
      to: "src/main/voice/ (all .ts files)"
      via: "walk() + stripComments() + WRITE_CHOKEPOINTS RE"
      pattern: "assertApproved"
    - from: "tests/static/voice-streaming-no-write.spec.ts"
      to: "src/renderer/features/voice/ (all .ts/.tsx files)"
      via: "walk() + stripComments() + WRITE_CHOKEPOINTS RE"
      pattern: "voiceConfirm"
---

<objective>
Land the D-13 read-only static ratchet that proves the Phase 16 voice streaming modules never
import or call write chokepoints.

This plan runs in Wave 3 (after both Wave 2 plans 16-04a and 16-04b have shipped all voice
modules onto disk) so the ratchet tests against the real files — a clean GREEN pass proves
the zero-write-risk guarantee holds.

Purpose: the Phase 14 voice-routes-through-staging ratchet proved voice staging never
auto-executes; this Phase 16 ratchet proves the conversational streaming loop (the new
surface area) cannot accidentally reach write paths. Defensive programming at CI.

Output: tests/static/voice-streaming-no-write.spec.ts — passes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-CONTEXT.md
@.planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md

<interfaces>
<!-- Exact structural template from the existing Phase 14 ratchet. -->

From tests/static/voice-routes-through-staging.spec.ts (the exact template — copy structure):
  walk(dir, out=[]) function: fs.existsSync guard (W-1); readdirSync withFileTypes; skip node_modules + dotfiles; recurse dirs; push .ts/.tsx/.mts/.cts files
  stripComments(src): remove /* ... */ blocks and // line comments
  CHOKEPOINT_NAMES array as const
  For each file: read + strip comments + test each chokepoint name with identifier-boundary RE
  /(?:^|[^A-Za-z0-9_$])${name}(?:[^A-Za-z0-9_$]|$)/

Phase 16 additions (from PATTERNS.md §voice-streaming-no-write.spec.ts):
  WRITE_CHOKEPOINTS extends Phase 14's set with assertApproved and voiceConfirm:
  ['sendApprovedEmail', 'applyCalendarChange', 'pushApprovedMeetingActions', 'assertApproved', 'voiceConfirm']

  Walk TWO directories:
  const MAIN_VOICE_ROOT = path.resolve(__dirname, '../..', 'src', 'main', 'voice');
  const RENDERER_VOICE_ROOT = path.resolve(__dirname, '../..', 'src', 'renderer', 'features', 'voice');

  Exclude test files (.spec.ts, .test.ts) from the scan (they are allowed to reference the chokepoint names in describe/it strings).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-13 read-only static ratchet for voice streaming modules</name>
  <files>tests/static/voice-streaming-no-write.spec.ts</files>
  <behavior>
    - Passes GREEN when src/main/voice/ contains no write-chokepoint imports
    - Passes GREEN when src/renderer/features/voice/ contains no write-chokepoint imports
    - Passes GREEN with missing-dir guard (if either dir absent, 0 offenders = pass)
    - WOULD FAIL if any .ts/.tsx file in either dir imports assertApproved (validate by reading the spec's RE logic)
    - Does not scan .spec.ts or .test.ts files (test-file exclusion)
  </behavior>
  <read_first>
    - tests/static/voice-routes-through-staging.spec.ts (FULL FILE — this is the exact structural template; copy walk/stripComments/identifier RE pattern verbatim, then adapt for Phase 16 directories and chokepoints)
    - tests/static/chokepoint-caller-allow-list.spec.ts (understand how it handles the existing chokepoints — this ratchet must NOT duplicate its work, just extend to voice renderer dir + add assertApproved/voiceConfirm)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-PATTERNS.md (§ voice-streaming-no-write.spec.ts — full code excerpt with WRITE_CHOKEPOINTS + two-dir walk)
    - .planning/phases/16-streaming-cascade-barge-in-read-only/16-RESEARCH.md (§ Pattern 6 D-13 Static Ratchet — structural detail; note on chokepoint-caller-allow-list.spec.ts scope)
  </read_first>
  <action>
    Create tests/static/voice-streaming-no-write.spec.ts. Use the exact walk()/stripComments()
    functions from voice-routes-through-staging.spec.ts (copy verbatim — they are identical
    utilities). Define WRITE_CHOKEPOINTS = ['sendApprovedEmail', 'applyCalendarChange',
    'pushApprovedMeetingActions', 'assertApproved', 'voiceConfirm'] as const. Define
    MAIN_VOICE_ROOT = path.resolve(__dirname, '../..', 'src', 'main', 'voice') and
    RENDERER_VOICE_ROOT = path.resolve(__dirname, '../..', 'src', 'renderer', 'features', 'voice').
    Collect files from both dirs via walk() — each walk() call uses the missing-dir guard
    (fs.existsSync check, returns [] if absent).

    For the file scan: exclude .spec.ts and .test.ts files (filter out filenames ending in
    .spec.ts or .test.ts) so that the ratchet itself and other spec files don't self-invalidate.

    For each production file, read source, strip comments, and test each WRITE_CHOKEPOINT name
    with the identifier-boundary RE: /(?:^|[^A-Za-z0-9_$])${name}(?:[^A-Za-z0-9_$]|$)/.
    Collect offenders as { file, chokepoint } pairs.

    Write one describe block "Phase 16 voice streaming modules are read-only" with one it() per
    chokepoint (or one it() total with a single offenders assertion — mirror
    voice-routes-through-staging.spec.ts structure). Use:
    expect(offenders, `${offenders.map(o => o.file).join('\n')}`).toHaveLength(0).

    Add a comment at the top: "D-13 read-only guard — extends the Phase-14 voice-routes-through-staging
    ratchet to cover Phase 16 streaming modules (src/main/voice/ + src/renderer/features/voice/)."

    After writing, run the spec to confirm it passes GREEN (not just compiles).
  </action>
  <verify>
    <automated>npx vitest run tests/static/voice-streaming-no-write.spec.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - test-command: npx vitest run tests/static/voice-streaming-no-write.spec.ts --no-file-parallelism passes GREEN
    - source: grep "WRITE_CHOKEPOINTS\|assertApproved\|voiceConfirm" tests/static/voice-streaming-no-write.spec.ts confirms the extended chokepoint set
    - source: grep "RENDERER_VOICE_ROOT\|renderer.*features.*voice" tests/static/voice-streaming-no-write.spec.ts confirms two-directory scan
    - source: grep "spec\.ts\|test\.ts" tests/static/voice-streaming-no-write.spec.ts confirms test-file exclusion filter
    - source: grep "existsSync" tests/static/voice-streaming-no-write.spec.ts confirms missing-dir W-1 guard
    - test-command: npx vitest run tests/unit/main/ipc/index.spec.ts --no-file-parallelism still passes (handler count unchanged)
  </acceptance_criteria>
  <done>D-13 static ratchet tests/static/voice-streaming-no-write.spec.ts passes GREEN; walks both main/voice and renderer/features/voice; 5 write chokepoints asserted absent; missing-dir and test-file exclusion guards in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI test runner → source files | Static grep at build time; no runtime boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-14 | Elevation of Privilege | Future developer accidentally imports assertApproved in a voice streaming module | mitigate | D-13 static ratchet catches the import at CI before merge; same pattern proven effective in Phase 14 voice-routes-through-staging ratchet |
</threat_model>

<verification>
- npx vitest run tests/static/voice-streaming-no-write.spec.ts --no-file-parallelism: GREEN.
- npx vitest run tests/static/ --no-file-parallelism: all Phase 14/15/16 static ratchets green together.
- npx vitest run: full suite green (no regressions across all waves).
- pnpm typecheck: 0 new errors above 84-error baseline.
</verification>

<success_criteria>
- tests/static/voice-streaming-no-write.spec.ts exists and passes GREEN.
- Ratchet walks src/main/voice/ + src/renderer/features/voice/ and asserts zero write-chokepoint imports.
- Full test suite green. pnpm typecheck clean. Phase 16 ready for /gsd-verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/16-streaming-cascade-barge-in-read-only/16-05-SUMMARY.md`
</output>
