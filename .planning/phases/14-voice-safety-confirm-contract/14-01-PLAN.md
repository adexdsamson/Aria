---
phase: 14-voice-safety-confirm-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/approvals/persist.ts
  - src/main/db/migrations/134_voice_explicit_path.sql
  - src/main/db/migrations/embedded.ts
autonomous: true
requirements: [VOICE-10]

must_haves:
  # CONTEXT.md decisions covered by this plan: D-01, D-03, D-07
  truths:
    - "A fresh (embedded-snapshot) DB accepts an INSERT into approval with approval_path='voice-explicit' without a raw SQLite CHECK error"
    - "A migrated (.sql chain) DB at user_version 134 accepts the same INSERT"
    - "The ApprovalPath TypeScript union admits 'voice-explicit' so downstream code compiles"
  artifacts:
    - path: "src/main/db/migrations/134_voice_explicit_path.sql"
      provides: "CHECK-widening table-rebuild migration that adds 'voice-explicit' to approval_path"
      contains: "approval_path IN ('explicit','silent','voice-explicit')"
    - path: "src/main/db/migrations/embedded.ts"
      provides: "version-134 EMBEDDED_MIGRATIONS entry byte-equivalent to the .sql"
      contains: "version: 134"
    - path: "src/main/approvals/persist.ts"
      provides: "ApprovalPath union including 'voice-explicit'"
      contains: "'voice-explicit'"
  key_links:
    - from: "src/main/db/migrations/134_voice_explicit_path.sql"
      to: "src/main/db/migrations/embedded.ts"
      via: "byte-equivalent SQL string copied into version-134 entry"
      pattern: "version: 134"
---

<objective>
Lay the schema + type foundation for the voice-confirm contract: widen the `approval_path` CHECK constraint to admit the new `'voice-explicit'` value (D-01, D-03) so a voice-staged/confirmed row can be inserted at all, and extend the `ApprovalPath` TypeScript union to match.

Purpose: Without the widened CHECK, inserting a voice row throws a raw SQLite error BEFORE the gate runs — so the entire safety contract is unreachable. This plan unblocks the gate branch (Plan 02) and both static ratchets (Plan 03). The migration MUST update BOTH the live `.sql` chain AND the bundled `embedded.ts` snapshot, or fresh test DBs and migrated DBs split-brain (D-03).

Output: New migration `134_voice_explicit_path.sql`, a matching `version: 134` entry in `embedded.ts`, and the one-line `ApprovalPath` union extension in `persist.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-voice-safety-confirm-contract/14-CONTEXT.md
@.planning/phases/14-voice-safety-confirm-contract/14-PATTERNS.md

<interfaces>
The migration runner (`src/main/db/migrations/runner.ts`) detects an explicit transaction
via `/\bBEGIN;/` and, when present, runs `db.exec(sql)` then `db.pragma('user_version=134')`
WITHOUT wrapping in its own transaction. The 124 analog uses the `PRAGMA foreign_keys=OFF; BEGIN; ... COMMIT; PRAGMA foreign_keys=ON;` envelope — copy that envelope so the runner takes the explicit-txn branch.

EMBEDDED_MIGRATIONS entries are `{ version: number, file: string, sql: string }`. The four
historical `approval` table snapshots at embedded.ts lines 157/254/464/685 are FROZEN past
versions — DO NOT edit them. Only append the NEW version-134 entry after the version-133 entry.

ApprovalPath today (persist.ts:25): `export type ApprovalPath = 'explicit' | 'silent';`
`insertApproval` defaults approval_path to `'explicit'` (persist.ts:135) — leave that default.
`ALLOWED_PATCH_COLS` already contains `'approval_path'` (persist.ts:171) — no change there.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the ApprovalPath union (D-01)</name>
  <read_first>
    - src/main/approvals/persist.ts (the file being modified; read lines 1-50 and 130-175 to confirm the union at line 25, the insertApproval default at line 135, and ALLOWED_PATCH_COLS at line 171)
  </read_first>
  <action>
    In src/main/approvals/persist.ts, change the `ApprovalPath` type alias at line 25 from `'explicit' | 'silent'` to `'explicit' | 'silent' | 'voice-explicit'`. This is the ONLY change in this file. Do NOT touch the `insertApproval` default (`'explicit'` at line 135) — voice never sets the path through `insertApproval` (D-07: staging reuses insertApproval, the confirm seam sets the path). Do NOT touch `ALLOWED_PATCH_COLS` — it already contains `'approval_path'` so the widened value flows through `transitionTo` unchanged.
  </action>
  <acceptance_criteria>
    - `src/main/approvals/persist.ts` line 25 ApprovalPath union literally contains the three members `'explicit'`, `'silent'`, `'voice-explicit'`
    - `insertApproval` still defaults approval_path to `'explicit'` (unchanged)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>ApprovalPath admits 'voice-explicit'; typecheck passes; no other persist.ts change.</done>
</task>

<task type="auto">
  <name>Task 2: Author the v134 CHECK-widening table-rebuild migration (D-03)</name>
  <read_first>
    - src/main/db/migrations/124_meeting_extraction_approvals.sql (the exact table-rebuild idiom + verbatim 30+ column list including `beta_voice INTEGER NOT NULL DEFAULT 0` at line 79; the INSERT...SELECT column list; the index recreation block; the `BEGIN;`/`COMMIT;` envelope)
    - src/main/db/migrations/runner.ts (lines 37-114 — confirm `parseMigrationVersion` maps `134_` to version 134, and the `/\bBEGIN;/` explicit-transaction branch)
    - src/main/db/migrations/006_approvals_and_tier.sql (line 15 — original `approval_path CHECK IN ('explicit','silent')` to confirm the constraint being widened)
  </read_first>
  <action>
    Create src/main/db/migrations/134_voice_explicit_path.sql as a full table-rebuild (SQLite cannot ALTER a CHECK). Copy the 124 idiom exactly: open with `PRAGMA foreign_keys=OFF;` then `BEGIN;`, `ALTER TABLE approval RENAME TO approval_old;`, `CREATE TABLE approval (...)` with the COMPLETE column list copied VERBATIM from 124 (all columns through `meeting_note_id`, including `beta_voice INTEGER NOT NULL DEFAULT 0`), changing ONLY the approval_path line to `approval_path TEXT NOT NULL DEFAULT 'explicit' CHECK (approval_path IN ('explicit','silent','voice-explicit'))`. Then `INSERT INTO approval (...) SELECT (...) FROM approval_old;` with the identical column list (all real columns map straight across — there is no NULL-padded new column this time, unlike 124's meeting_note_id), `DROP TABLE approval_old;`, recreate all five approval indexes (`idx_approval_state`, `idx_approval_kind_state`, `idx_approval_updated_at`, `idx_approval_provider_account`, `idx_approval_meeting_note`) with `CREATE INDEX IF NOT EXISTS`, then `COMMIT;` and `PRAGMA foreign_keys=ON;`. Do NOT recreate the meeting_summary/meeting_action tables (those already exist from 124) — this migration touches the approval table only. A drifted column list will lose data on the INSERT...SELECT, so copy the 124 list character-for-character.
  </action>
  <acceptance_criteria>
    - `src/main/db/migrations/134_voice_explicit_path.sql` exists and contains the literal string `approval_path IN ('explicit','silent','voice-explicit')`
    - The file contains a `BEGIN;` (so the runner takes the explicit-transaction branch)
    - The CREATE TABLE column list matches the 124 column list (same column count including `beta_voice` and `meeting_note_id`)
    - The INSERT column list and the SELECT column list are identical (no positional drift)
    - All five `idx_approval_*` indexes are recreated with `IF NOT EXISTS`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/main/db/migrations/134_voice_explicit_path.sql','utf8');if(!s.includes(\"approval_path IN ('explicit','silent','voice-explicit')\"))process.exit(1);if(!/\bBEGIN;/.test(s))process.exit(2);console.log('ok')"</automated>
  </verify>
  <done>v134 migration rebuilds the approval table with the widened CHECK, preserving every existing column and all indexes.</done>
</task>

<task type="auto">
  <name>Task 3: Append the version-134 embedded snapshot entry, byte-equivalent to the .sql (D-03)</name>
  <read_first>
    - src/main/db/migrations/embedded.ts (lines 1-40 for the EmbeddedMigration shape + header docblock warning "Keep in sync with the .sql files ... drift between the two will fail in CI"; lines 1450-1448 region for the version-133 entry shape to mirror; lines 157/254/464/685 to confirm those are FROZEN historical approval snapshots that must NOT change)
    - src/main/db/migrations/134_voice_explicit_path.sql (the file authored in Task 2 — its SQL must be copied byte-equivalent into the embedded entry)
    - src/main/db/migrations/runner.ts (lines 51-68 — confirm fresh/packaged DBs run EMBEDDED_MIGRATIONS, .sql files only run when an explicit `dir` is supplied)
  </read_first>
  <action>
    In src/main/db/migrations/embedded.ts, append a new array element after the version-133 entry: `{ version: 134, file: '134_voice_explicit_path.sql', sql: \`...\` }` where the `sql` template-literal body is a byte-equivalent copy of the 134_voice_explicit_path.sql authored in Task 2. The header docblock requires byte-equivalence — the migrations test reads the .sql files directly and diffs against embedded, so any drift fails CI. DO NOT edit the four frozen historical approval snapshots at lines 157/254/464/685 — only the new version-134 rebuild widens the CHECK going forward; past snapshots stay pinned at `('explicit','silent')`. Run `npm run typecheck` after the edit (esbuild skips tsc per project memory).
  </action>
  <acceptance_criteria>
    - `src/main/db/migrations/embedded.ts` contains a `version: 134` entry with `file: '134_voice_explicit_path.sql'`
    - The version-134 entry's `sql` body contains `approval_path IN ('explicit','silent','voice-explicit')`
    - The four historical snapshots at lines 157/254/464/685 still read `approval_path IN ('explicit','silent')` (unchanged)
    - `npm run typecheck` exits 0
    - The existing migrations parity test (the spec that diffs .sql vs embedded) passes: `npx vitest run` against the migrations test file exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/main/db/migrations</automated>
  </verify>
  <done>Fresh DBs (embedded) and migrated DBs (.sql) both reach the widened CHECK at version 134; no historical snapshot mutated; parity test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration chain → live DB | DDL crosses into the authorization-critical `approval` table; a wrong column list here silently destroys approval history |
| embedded snapshot ↔ .sql chain | fresh-install DBs run embedded; dev/test DBs run .sql — divergence = split-brain CHECK constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `approval` table CHECK constraint | mitigate | Widen CHECK to admit `'voice-explicit'` as a DISTINCT value (never folded into `'explicit'`) so the gate in Plan 02 can still reject it for forced/high — the distinguishability is the security property (PITFALLS §§23-30) |
| T-14-02 | Tampering / Repudiation | embedded.ts vs .sql split-brain | mitigate | Byte-equivalent embedded copy + the existing migrations parity test (reads .sql, diffs embedded) fails CI on drift; only the NEW v134 entry widens, historical snapshots frozen |
| T-14-03 | Denial of Service | INSERT...SELECT column drift in rebuild | mitigate | Copy the 124 column list verbatim incl. `beta_voice`; acceptance criteria assert identical INSERT/SELECT column lists; migrations test seeds + re-reads a row to prove no data loss |

**Residual risk:** A future migration that rebuilds `approval` again could re-narrow the CHECK if a contributor copies a pre-134 column block. Mitigated downstream by Plan 03's static ratchet asserting voice never writes `'explicit'`, but not by a schema-level guard — accepted (low likelihood, caught by the SC2 gate test if it regressed).
</threat_model>

<verification>
- `npm run typecheck` exits 0 after all edits.
- `npx vitest run src/main/db/migrations` passes (embedded↔.sql parity + a migrated DB reaching version 134).
- A direct `INSERT INTO approval (... approval_path ...) VALUES (..., 'voice-explicit')` against a freshly-migrated test DB succeeds (no raw SQLite CHECK error) — exercised by the Plan 02 gate test, which depends on this plan.
</verification>

<success_criteria>
- ApprovalPath union admits `'voice-explicit'` (D-01).
- v134 migration widens the CHECK via a verbatim-column table rebuild, in both `.sql` and `embedded.ts` (D-03).
- No historical embedded snapshot mutated.
- Fresh and migrated DBs converge at version 134 with the widened constraint.
</success_criteria>

<output>
After completion, create `.planning/phases/14-voice-safety-confirm-contract/14-01-SUMMARY.md`
</output>
