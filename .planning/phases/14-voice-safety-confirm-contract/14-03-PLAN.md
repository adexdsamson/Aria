---
phase: 14-voice-safety-confirm-contract
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - tests/static/voice-routes-through-staging.spec.ts
  - tests/static/chokepoint-caller-allow-list.spec.ts
  - .planning/research/ARCHITECTURE.md
autonomous: true
requirements: [VOICE-10]

must_haves:
  # CONTEXT.md decisions covered by this plan: D-04, D-08, D-09, D-09a, D-13
  truths:
    - "The build FAILS (a static spec goes red) if any src/main/voice/** file is a direct caller of the three write-module chokepoint entry points"
    - "The build FAILS if any file outside the three known IPC chokepoints calls sendApprovedEmail / applyCalendarChange / pushApprovedMeetingActions"
    - "The build FAILS if any src/main/voice/** file writes approval_path:'explicit' (it must write 'voice-explicit')"
    - "ARCHITECTURE.md no longer describes voice-confirm as producing approval_path='explicit'"
  artifacts:
    - path: "tests/static/voice-routes-through-staging.spec.ts"
      provides: "named voice ratchet reading like SC3 + no-explicit-literal-in-voice assertion"
      contains: "voice-explicit"
    - path: "tests/static/chokepoint-caller-allow-list.spec.ts"
      provides: "caller allow-list fencing the three chokepoint entry points to the known IPC callers"
      contains: "sendApprovedEmail"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "corpus correction from 'explicit' to 'voice-explicit' at lines 122/306/314-316"
      contains: "voice-explicit"
  key_links:
    - from: "tests/static/chokepoint-caller-allow-list.spec.ts"
      to: "src/main/integrations/send.ts sendApprovedEmail"
      via: "ALLOWED_CALLERS set + offenders === [] assertion matching import AND call site"
      pattern: "ALLOWED_CALLERS"
---

<objective>
Ship the two complementary build-time ratchets (D-08/D-09) and reconcile the stale ARCHITECTURE.md corpus (D-13). Ratchet B (the real hole-closer): a caller allow-list fencing the three exported write-module chokepoint entry points — `sendApprovedEmail`, `applyCalendarChange`, `pushApprovedMeetingActions` — to ONLY the three known IPC chokepoints, fail-closed against any rogue caller (voice or otherwise) from day one. Ratchet A: a named `voice-routes-through-staging.spec.ts` keyed to `src/main/voice/**`, phrased like SC3 so an auditor can point to it, which ALSO asserts no voice file writes `approval_path:'explicit'` (D-09a, mirroring D-04).

Purpose: The existing write-site ratchets guard only the low-level SDK surface (`events.patch`, `messages.send`) — NOT the exported chokepoint entry points, which is exactly the gap a voice handler would exploit (D-09). These two specs close that hole and document intent. The ARCHITECTURE.md correction removes the lone corpus outlier that says voice produces `'explicit'`, which directly contradicts SC2 (a forced voice confirm MUST be rejectable, requiring a distinguishable path value).

Output: Two new `tests/static/*.spec.ts` ratchets and the corrected ARCHITECTURE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-voice-safety-confirm-contract/14-CONTEXT.md
@.planning/phases/14-voice-safety-confirm-contract/14-PATTERNS.md

<interfaces>
The three chokepoint entry points (heterogeneous signatures — regex must match BOTH import and call site, D-09):
- `sendApprovedEmail(db, approvalId, deps)` — positional — src/main/integrations/send.ts:140
- `applyCalendarChange(db, approvalId, deps)` — positional — src/main/integrations/write-event.ts:88
- `pushApprovedMeetingActions({ db, approvalId, client })` — single options object — src/main/integrations/todoist/push-actions.ts:25

The ONLY legitimate callers TODAY (verified via grep against src/main):
- sendApprovedEmail ← src/main/ipc/gmail-send.ts (import :22, call :129)
- applyCalendarChange ← src/main/ipc/approvals.ts (import :23, call :223)
- pushApprovedMeetingActions ← src/main/ipc/todoist.ts (import :8, call :107)
Doc-only mentions in src/main/scheduling/propose.ts and src/main/learning/sources/approval.ts
are inside COMMENTS — stripComments removes them, so they will NOT register as callers.

The definition sites themselves (send.ts / write-event.ts / push-actions.ts) must be EXCLUDED
from the offender walk (a function declaring itself is not a caller).

Ratchet idiom (copy verbatim from tests/static/single-entitlement-gate-site.test.ts:20-94):
- `ROOT = path.resolve(__dirname, '../..', 'src', 'main')`
- `abs(rel)` normalizes with `.replace(/\\/g, '/')` (Windows)
- recursive `walk` skipping node_modules/dotfiles, matching `/\.(ts|tsx|mts|cts)$/`
- `stripComments`: block comments `/\/\*[\s\S]*?\*\//g` FIRST, then line comments `/\/\/[^\n]*/g` (order matters)
- `ALLOWED_CALLERS = new Set([...].map(abs))`; collect offenders where RE matches && !ALLOWED_CALLERS.has(norm); `expect(offenders, msg).toEqual([])`
- a POSITIVE assertion that the RE still matches the real allowed site (guards against a regex that silently matches nothing) — see single-calendar-write-site.test.ts:33-71
phase12-bans.spec.ts:34,58-72 is the banned-literal idiom for the no-'explicit'-in-voice assertion.

ARCHITECTURE.md stale lines (D-13): 122, 306, and the 314-316 "Non-negotiables" bullet — all say
voice-confirm produces approval_path='explicit'. ROADMAP.md, research/SUMMARY.md §73, and
research/PITFALLS.md §§23-30 are AUTHORITATIVE and already specify 'voice-explicit'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chokepoint caller allow-list ratchet — Ratchet B (D-08/D-09)</name>
  <read_first>
    - tests/static/single-entitlement-gate-site.test.ts (the caller allow-list idiom to copy verbatim: ROOT, abs, walk, stripComments, ALLOWED_CALLERS Set, offenders === [] assertion)
    - tests/static/single-calendar-write-site.test.ts (the positive-match assertion idiom proving the regex still matches the real site)
    - src/main/integrations/send.ts (line 140 — sendApprovedEmail definition, to EXCLUDE from offenders)
    - src/main/integrations/write-event.ts (line 88 — applyCalendarChange definition, to EXCLUDE)
    - src/main/integrations/todoist/push-actions.ts (line 25 — pushApprovedMeetingActions definition, to EXCLUDE)
    - src/main/ipc/gmail-send.ts (import :22, call :129 — the allowed sendApprovedEmail caller)
    - src/main/ipc/approvals.ts (import :23, call :223 — the allowed applyCalendarChange caller)
    - src/main/ipc/todoist.ts (import :8, call :107 — the allowed pushApprovedMeetingActions caller)
    - src/main/scheduling/propose.ts + src/main/learning/sources/approval.ts (confirm the chokepoint mentions are inside comments so stripComments removes them)
  </read_first>
  <action>
    Create tests/static/chokepoint-caller-allow-list.spec.ts copying the single-entitlement-gate-site idiom. For EACH of the three entry points define its name, its definition-site file (excluded from offenders), and its single ALLOWED_CALLER file: sendApprovedEmail → src/main/ipc/gmail-send.ts; applyCalendarChange → src/main/ipc/approvals.ts; pushApprovedMeetingActions → src/main/ipc/todoist.ts. Walk src/main, stripComments (block then line), and for each entry point flag any file (other than the definition site and the allowed caller) that references the entry-point identifier as an import or call. Because the signatures are heterogeneous, match the bare identifier reference (import specifier OR call) rather than a positional-arg shape — assert offenders.toEqual([]) with the offender list interpolated into the message. Add a POSITIVE assertion per entry point that the allowed caller file DOES match (guards against a regex matching nothing). EXCLUDE `.test.ts`/`.spec.ts` files and the definition sites from the offender set. Normalize all paths with `.replace(/\\/g, '/')`. This fails CLOSED: any future src/main/voice/** file (or any other rogue module) calling these entry points directly turns the spec red, proving voice routes through the same staging the UI uses. Run `npm run typecheck` after the edit.
  </action>
  <acceptance_criteria>
    - `tests/static/chokepoint-caller-allow-list.spec.ts` defines an ALLOWED_CALLERS mapping with exactly the three known IPC chokepoints (gmail-send.ts, approvals.ts, todoist.ts)
    - For each of sendApprovedEmail / applyCalendarChange / pushApprovedMeetingActions there is an offenders===[] assertion AND a positive "allowed caller still matches" assertion
    - The three definition sites (send.ts, write-event.ts, push-actions.ts) are excluded from offenders
    - The comment-only mentions in scheduling/propose.ts and learning/sources/approval.ts do NOT register as offenders (stripComments removes them)
    - `npx vitest run tests/static/chokepoint-caller-allow-list.spec.ts` exits 0 against the current tree
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/static/chokepoint-caller-allow-list.spec.ts</automated>
  </verify>
  <done>The three write-module chokepoint entry points are fenced to their known IPC callers; any rogue caller (incl. future voice) fails the build.</done>
</task>

<task type="auto">
  <name>Task 2: Named voice-routes-through-staging ratchet — Ratchet A + D-09a (D-08)</name>
  <read_first>
    - tests/static/single-calendar-write-site.test.ts (the canonical walk + stripLineComments + offenders idiom + positive-match guard)
    - tests/static/phase12-bans.spec.ts (lines 34, 58-72 — the banned-literal idiom for the no-'explicit'-in-voice assertion)
    - src/main/voice/confirm.ts (the only src/main/voice/** file shipped this phase — confirm it writes 'voice-explicit', never 'explicit')
    - .planning/ROADMAP.md (§ Phase 14 SC3 wording — phrase the test description to read like SC3 so an auditor can point to it)
  </read_first>
  <action>
    Create tests/static/voice-routes-through-staging.spec.ts keyed to the src/main/voice/** namespace, copying the single-calendar-write-site idiom (ROOT, walk, stripComments, offenders===[]). The describe/it descriptions must read like SC3 ("the voice handler is not a direct caller of the write modules — voice routes through the same staging the UI uses"). Two guarantees: (1) no file under src/main/voice/** imports or calls sendApprovedEmail / applyCalendarChange / pushApprovedMeetingActions directly — assert offenders===[]; this overlaps Ratchet B intentionally but documents intent at the voice namespace specifically (D-08A: A documents intent, B closes the hole). (2) D-09a banned-literal: no file under src/main/voice/** contains the literal `approval_path:'explicit'` or `approval_path: 'explicit'` (with optional whitespace) — voice must write 'voice-explicit' (mirrors D-04). **MISSING-DIR GUARD (W-1): the copied `walk` idiom roots at `src/main` and does an UNGUARDED `fs.readdirSync` — but this spec scopes to `VOICE_ROOT = path.resolve(ROOT, 'voice')`, which does NOT exist until Plan 14-02 ships confirm.ts. Since 14-02 and 14-03 are both Wave 2 (parallel-eligible, depends_on:[01]), this spec MUST guard with `if (!fs.existsSync(VOICE_ROOT)) { /* no voice files yet → zero offenders */ }` and treat a missing dir as an empty file list (offenders===[]), NOT call readdirSync on it. Do not rely on 14-02 landing first.** With the guard, the spec stays green whether src/main/voice/ is absent or contains only confirm.ts, and fails the moment a future voice file violates either rule. Run `npm run typecheck` after the edit.
  </action>
  <acceptance_criteria>
    - `tests/static/voice-routes-through-staging.spec.ts` walks `src/main/voice/**` and asserts no direct call/import of the three chokepoint entry points (offenders===[])
    - The spec asserts no src/main/voice/** file contains `approval_path:'explicit'` (whitespace-tolerant) — the banned-literal D-09a guard
    - At least one test description is phrased to read like SC3 (voice routes through staging, not a direct write-module caller)
    - The spec guards a missing `src/main/voice/` directory with `fs.existsSync(VOICE_ROOT)` and yields zero offenders (does NOT throw ENOENT) — verifiable by the spec passing even if run before Plan 14-02 creates confirm.ts
    - `npx vitest run tests/static/voice-routes-through-staging.spec.ts` exits 0 against the current tree (confirm.ts writes 'voice-explicit')
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/static/voice-routes-through-staging.spec.ts</automated>
  </verify>
  <done>A named, auditor-pointable ratchet proves voice routes through staging and never stamps 'explicit'.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the stale ARCHITECTURE.md corpus (D-13)</name>
  <read_first>
    - .planning/research/ARCHITECTURE.md (lines 118-128, 300-320 — the stale voice-confirm descriptions at 122, 306, and the 314-316 "Non-negotiables" bullet)
    - .planning/research/PITFALLS.md (§§23-30 — AUTHORITATIVE: 'voice-explicit' must NOT satisfy forced-explicit)
    - .planning/research/SUMMARY.md (§73 — corroborates 'voice-explicit')
  </read_first>
  <action>
    Correct .planning/research/ARCHITECTURE.md (reconcile-via-addendum, the established "spec vs codebase reality" loop): (1) Line ~122: change "turns a spoken 'yes, send it' into the exact same approval row transition (state→approved, approval_path='explicit')" so the path value reads `'voice-explicit'`. (2) Line ~306: change "→ state='approved', approval_path='explicit'" to `'voice-explicit'`. (3) Lines ~314-316 "Non-negotiables" bullet "Voice-confirm produces approval_path='explicit' … A voice 'yes' is a first-class explicit approval" — rewrite to the 'voice-explicit' design: voice confirm produces `'voice-explicit'`, which clears the gate for low/med actions but is REJECTED for forced/high-severity (financial/legal/HR) by the dedicated `voice-forbidden-forced` branch — a voice 'yes' is NOT a first-class explicit approval for high-stakes actions. Preserve confirm.ts's "load-bearing trust decision" framing (that part is correct). Do not alter ROADMAP.md, SUMMARY.md, or PITFALLS.md — they are authoritative and already correct; ARCHITECTURE.md is the lone outlier.
  </action>
  <acceptance_criteria>
    - `.planning/research/ARCHITECTURE.md` no longer contains a voice-confirm description producing `approval_path='explicit'`
    - The corrected lines reference `'voice-explicit'` and state that forced/high-severity voice confirms are REJECTED (forcing the on-screen click)
    - The "load-bearing trust decision" framing for confirm.ts is preserved
    - No edits to ROADMAP.md / SUMMARY.md / PITFALLS.md
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/research/ARCHITECTURE.md','utf8');if(/approval_path\s*=\s*'explicit'/.test(s)){console.error('stale explicit ref remains');process.exit(1)}if(!s.includes('voice-explicit')){console.error('missing voice-explicit');process.exit(2)}console.log('ok')"</automated>
  </verify>
  <done>ARCHITECTURE.md aligns with ROADMAP/PITFALLS/SUMMARY: voice-confirm produces 'voice-explicit', rejectable for forced/high.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any src/main module → write-module chokepoint | a rogue caller (esp. a future voice handler) could call a write entry point directly, bypassing staging |
| planning corpus → implementer mental model | a self-contradictory doc (ARCHITECTURE.md) could mislead a future phase into reopening the voice path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Elevation of Privilege | direct call to sendApprovedEmail/applyCalendarChange/pushApprovedMeetingActions | mitigate | Caller allow-list ratchet (Ratchet B) fails the build for ANY caller outside the three known IPC chokepoints; the existing write-site ratchets only guarded the low-level SDK surface, leaving the entry points open (D-09) — this closes it |
| T-14-08 | Tampering | voice file stamping approval_path:'explicit' to dodge the gate | mitigate | D-09a banned-literal assertion in the named voice ratchet fails the build if any src/main/voice/** file writes `'explicit'` — voice must write `'voice-explicit'` (the gate-rejectable value) |
| T-14-09 | Repudiation / Spoofing | stale ARCHITECTURE.md claiming voice='explicit' | mitigate | D-13 corrects 122/306/314-316; the verify step greps for any residual `approval_path='explicit'` voice description and fails if present |

**Residual risk:** The ratchets are static-grep, not AST — an obfuscated indirect call (e.g. via a dynamic dispatch table) could evade detection. Accepted: consistent with the repo's existing ratchet family posture (per CONTEXT "no AST gate" precedent from Phase 5); the gate test (Plan 02) is the runtime backstop.
</threat_model>

<verification>
- `npx vitest run tests/static/chokepoint-caller-allow-list.spec.ts tests/static/voice-routes-through-staging.spec.ts` passes against the current tree.
- Both ratchets fail-closed: introducing a direct chokepoint call in a src/main/voice/** file (or any non-allowed module) turns a spec red.
- ARCHITECTURE.md greps clean of `approval_path='explicit'` voice descriptions and contains `'voice-explicit'`.
- `npm run typecheck` exits 0.
</verification>

<success_criteria>
- Ratchet B fences the three chokepoint entry points to the three known IPC callers, matching import AND call sites (D-08/D-09).
- Ratchet A is a named, SC3-phrased voice ratchet that also bans `approval_path:'explicit'` in voice files (D-08A/D-09a).
- ARCHITECTURE.md reconciled to the 'voice-explicit' design (D-13).
</success_criteria>

<output>
After completion, create `.planning/phases/14-voice-safety-confirm-contract/14-03-SUMMARY.md`
</output>
