---
phase: 14-voice-safety-confirm-contract
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/main/approvals/gate.ts
  - src/main/voice/confirm.ts
  - src/main/voice/confirm.spec.ts
  - tests/integration/voice-confirm-gate.spec.ts
autonomous: true
requirements: [VOICE-10]

must_haves:
  truths:
    - "assertApproved REJECTS a forced/high-severity row whose approval_path='voice-explicit' with the SPECIFIC error code 'voice-forbidden-forced' (not the generic 'forced-explicit-missing')"
    - "assertApproved ACCEPTS a low/med non-forced row whose approval_path='voice-explicit' (returns without throwing)"
    - "voiceConfirm(db, approvalId) transitions a 'ready' row to 'approved' with approval_path='voice-explicit', the SAME ready→approved edge the UI fires (SC4 by construction)"
    - "After voiceConfirm, sendApprovedEmail runs its unchanged first-line assertApproved at send.ts:146"
  artifacts:
    - path: "src/main/voice/confirm.ts"
      provides: "pure dormant headless voiceConfirm seam wrapping transitionTo"
      exports: ["voiceConfirm"]
      min_lines: 15
    - path: "src/main/approvals/gate.ts"
      provides: "named voice-forbidden-forced rejection branch + extended error-code union"
      contains: "voice-forbidden-forced"
    - path: "tests/integration/voice-confirm-gate.spec.ts"
      provides: "SC2 failing-then-passing gate test + SC4 same-transition/unchanged-adapter test"
  key_links:
    - from: "src/main/voice/confirm.ts"
      to: "src/main/approvals/persist.ts transitionTo"
      via: "transitionTo(db, id, 'approved', { approval_path: 'voice-explicit' })"
      pattern: "transitionTo\\(.*'approved'.*voice-explicit"
    - from: "src/main/approvals/gate.ts"
      to: "ApprovalGateError"
      via: "throw new ApprovalGateError('voice-forbidden-forced', ...)"
      pattern: "voice-forbidden-forced"
---

<objective>
Land the two load-bearing trust decisions: (1) the named `voice-forbidden-forced` rejection branch in `assertApproved` (D-02) that blocks a voice confirm from authorizing a forced/high-severity action, and (2) the pure, dormant, headless `voiceConfirm(db, approvalId)` seam (D-10/D-11) that performs the SAME `ready→approved` transition the Approvals UI performs, stamping `approval_path='voice-explicit'`. Ship the SC2 failing-then-passing gate test and the SC4 same-transition/unchanged-adapter integration test (D-12).

Purpose: This is the phase centerpiece — the authorization boundary itself. The named branch is intentional defense-in-depth duplicating the existing generic forced-explicit rule, so a future refactor cannot silently reopen the voice path. `voiceConfirm` ships fully tested with ZERO callers this phase (the `writeSendLog` precedent) so the contract is frozen before Phase 17 wires audio.

Output: Modified `gate.ts` (error-code union + named branch), new `src/main/voice/confirm.ts` + its unit spec, and the SC2+SC4 integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-voice-safety-confirm-contract/14-CONTEXT.md
@.planning/phases/14-voice-safety-confirm-contract/14-PATTERNS.md

<interfaces>
gate.ts:21-24 — `ApprovalGateErrorCode = 'not-found' | 'not-approved' | 'forced-explicit-missing'`.
gate.ts:26-33 — `ApprovalGateError(code, message)` carries `code` verbatim; no constructor change.
gate.ts:35-39 — `FORCED_CATEGORIES = Set(['financial','legal','hr'])`.
gate.ts:41-100 — `assertApproved(db, approvalId): void`. The SELECT already pulls `approval_path`
(line 44). The generic forced branch (lines 84-99): `isForced = parseFailed || severity===null ||
severity==='high' || cats.some(c=>FORCED_CATEGORIES.has(c))`; then `if (isForced && approval_path !== 'explicit') throw 'forced-explicit-missing'`.

persist.ts:211-239 — `transitionTo(db, id, to, patch={})` runs in a transaction, validates via
assertTransition, applies only ALLOWED_PATCH_COLS keys (approval_path IS allowed at line 171),
then UPDATEs. `type Db = Database.Database` from 'better-sqlite3-multiple-ciphers'.

state.ts:25-37 — `ready: ['approved','rejected','snoozed']`; `approved` is reachable ONLY from
`ready`. This is exactly what the UI fires (ipc/approvals.ts:199) → SC4 true by construction.

persist.ts:281-316 — `writeSendLog` is the dormant-contract docblock precedent ("dormant ...
exists so the contract is fixed up front") to mirror in confirm.ts's docblock.

send.ts:140-146 — `sendApprovedEmail(db, approvalId, deps={})` calls `assertEntitled` then
`assertApproved(db, approvalId)` at line 146. Use the `deps`/`SendApprovedDeps` injection seam
(`deps.buildGmailClient`) to avoid a real Gmail call in the SC4 test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the named voice-forbidden-forced gate branch (D-02)</name>
  <read_first>
    - src/main/approvals/gate.ts (the file being modified — full read; the error-code union lines 21-24, ApprovalGateError lines 26-33, FORCED_CATEGORIES lines 35-39, the generic forced branch lines 84-99)
    - src/main/approvals/persist.ts (lines 1-50 — confirm ApprovalPath now includes 'voice-explicit' from Plan 01)
    - .planning/research/PITFALLS.md (§§23-30 — AUTHORITATIVE: 'voice-explicit' must NOT satisfy forced-explicit; the named branch must be distinguishable)
  </read_first>
  <behavior>
    - A forced row (category in financial/legal/hr) with approval_path='voice-explicit' → throws ApprovalGateError code 'voice-forbidden-forced'
    - A severity='high' row with approval_path='voice-explicit' → throws code 'voice-forbidden-forced'
    - A null-severity row with approval_path='voice-explicit' → throws code 'voice-forbidden-forced'
    - A low/med non-forced row with approval_path='voice-explicit' → returns without throwing
    - A forced row with approval_path='explicit' → still passes (existing behavior unchanged)
    - A forced row with approval_path='silent' → still throws the GENERIC 'forced-explicit-missing' (existing behavior unchanged for non-voice paths)
  </behavior>
  <action>
    In src/main/approvals/gate.ts: (1) extend the `ApprovalGateErrorCode` union (lines 21-24) by adding the literal member `'voice-forbidden-forced'`. (2) Inside `assertApproved`, AFTER computing `isForced` (line 84-88) and BEFORE the existing generic `if (isForced && row.approval_path !== 'explicit')` throw at line 89, insert a new named branch: `if (isForced && row.approval_path === 'voice-explicit') { throw new ApprovalGateError('voice-forbidden-forced', \`forced/high-severity action cannot be authorized by voice; got path=voice-explicit\`); }`. Ordering it BEFORE the generic check is mandatory (D-02) so a future refactor of the generic branch cannot silently reopen the voice path. Do NOT replace or weaken the existing generic branch — `'voice-explicit'` is also `!== 'explicit'` so it would be caught generically today; the named branch makes the rejection auditable and refactor-proof (intentional defense-in-depth duplication). Do NOT change the SELECT (approval_path already pulled at line 44) or the constructor. Run `npm run typecheck` after the edit.
  </action>
  <acceptance_criteria>
    - `src/main/approvals/gate.ts` ApprovalGateErrorCode union contains `'voice-forbidden-forced'`
    - The named branch fires for `isForced && approval_path === 'voice-explicit'` and is positioned textually BEFORE the line-89 generic `approval_path !== 'explicit'` throw
    - The existing generic branch and its `'forced-explicit-missing'` code remain present and unmodified
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>assertApproved throws the dedicated 'voice-forbidden-forced' code for forced/high voice-explicit rows; generic behavior intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the dormant headless voiceConfirm seam (D-10/D-11)</name>
  <read_first>
    - src/main/approvals/persist.ts (transitionTo at lines 211-239, ALLOWED_PATCH_COLS at line 171, the `type Db = Database.Database` alias at lines 14/20, the writeSendLog dormant-contract docblock at lines 281-316 to mirror its voice)
    - src/main/approvals/state.ts (lines 25-43 — confirm ready→approved is the only edge into approved and assertTransition validates it)
    - src/main/db/migrations/embedded.ts (confirm version-134 entry exists from Plan 01 so a fresh test DB admits 'voice-explicit')
  </read_first>
  <behavior>
    - voiceConfirm(db, id) on a 'ready' row → row.state becomes 'approved' AND row.approval_path becomes 'voice-explicit'
    - voiceConfirm(db, id) on a non-'ready' row (e.g. 'pending') → throws the assertTransition invalid-transition error (proves it routes through transitionTo, not raw SQL)
    - voiceConfirm has exactly the signature (db: Db, approvalId: string): void — two args, no read-back payload
  </behavior>
  <action>
    Create src/main/voice/confirm.ts exporting `export function voiceConfirm(db: Db, approvalId: string): void`. Import `transitionTo` from '../approvals/persist' and `type Database from 'better-sqlite3-multiple-ciphers'` with `type Db = Database.Database`. The body is a single call: `transitionTo(db, approvalId, 'approved', { approval_path: 'voice-explicit' });`. Freeze the signature at exactly `(db, approvalId)` (D-11) — do NOT add a read-back payload type (deferred to Phase 17; defining it before a resolver exists invites a fictional schema). Add a docblock mirroring the writeSendLog precedent: state it is the dormant, zero-caller, headless trust seam that exists so the contract is fixed up front; the first caller is Phase 17. Never write approval.state or approval_path via raw SQL — always through transitionTo. Then create src/main/voice/confirm.spec.ts (vitest, in-memory or temp DB seeded via runMigrations to version 134 + insertApproval): assert the happy-path transition writes state='approved' and approval_path='voice-explicit', and assert that calling voiceConfirm on a 'pending' row throws an invalid-transition error. Run `npm run typecheck` after the edit.
  </action>
  <acceptance_criteria>
    - `src/main/voice/confirm.ts` exports `voiceConfirm` with signature `(db: Db, approvalId: string): void` (exactly two params)
    - Body calls `transitionTo(db, approvalId, 'approved', { approval_path: 'voice-explicit' })` and nothing else mutates the row
    - No read-back payload type is defined in the file
    - `npx vitest run src/main/voice/confirm.spec.ts` exits 0 (happy-path transition + invalid-transition-throws cases)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/main/voice/confirm.spec.ts</automated>
  </verify>
  <done>voiceConfirm is a pure two-arg seam wrapping transitionTo to the ready→approved edge with approval_path='voice-explicit', fully unit-tested, zero callers.</done>
</task>

<task type="auto">
  <name>Task 3: SC2 failing-then-passing gate test + SC4 same-transition/unchanged-adapter test (D-12)</name>
  <read_first>
    - src/main/approvals/gate.ts (the assertApproved behavior under test, post-Task-1)
    - src/main/voice/confirm.ts (the seam under test, post-Task-2)
    - src/main/integrations/send.ts (lines 140-160 — the SendApprovedDeps injection seam, the assertEntitled+assertApproved first two lines; use deps to stub buildGmailClient so no real Gmail call happens)
    - src/main/approvals/persist.ts (insertApproval for seeding rows; ApprovalRow shape)
    - src/main/db/migrations/runner.ts + embedded.ts (to migrate the test DB to version 134 so 'voice-explicit' inserts are legal)
    - tests/integration (browse a sibling integration spec, e.g. meeting-to-todoist.spec.ts, for the in-memory-DB + deps-injection harness idiom this repo uses)
  </read_first>
  <action>
    Create tests/integration/voice-confirm-gate.spec.ts with two describe blocks.

    SC2 (failing-then-passing on the SPECIFIC code): seed a forced row — category 'financial' (or severity 'high') — into a version-134 test DB, drive it to approved via voiceConfirm (so approval_path='voice-explicit'), then assert `assertApproved(db, id)` throws an `ApprovalGateError` whose `.code === 'voice-forbidden-forced'` — assert the SPECIFIC code, NOT the generic 'forced-explicit-missing' (this is the centerpiece, per `<specifics>`). Add a comment documenting the two-state expectation: this test FAILS before the Task-1 branch lands (it would throw the generic code) and PASSES after. Add a companion passing case: a low/med non-forced row with approval_path='voice-explicit' → `assertApproved` returns without throwing.

    SC4 (same transition + unchanged adapter): seed a low/med non-forced email_send 'ready' row, call `voiceConfirm(db, id)` (the SAME ready→approved edge the UI fires), then call `sendApprovedEmail(db, id, deps)` with `deps` stubbing buildGmailClient to a fake that records the call — assert the send adapter's first-line `assertApproved` (send.ts:146) runs and the flow reaches the stubbed client unchanged (no gate change for low/med voice). Add a negative companion: a forced email_send row confirmed via voice is REJECTED at sendApprovedEmail's assertApproved with code 'voice-forbidden-forced' (proves the gate holds through the real adapter path).

    Run `npm run typecheck` after the edit.
  </action>
  <acceptance_criteria>
    - `tests/integration/voice-confirm-gate.spec.ts` asserts `error.code === 'voice-forbidden-forced'` (the SPECIFIC code) for a forced voice-explicit row — a `toBe('forced-explicit-missing')` assertion here is FORBIDDEN
    - The file contains a comment documenting the failing-before / passing-after two-state expectation for the SC2 case
    - The SC4 block calls voiceConfirm then sendApprovedEmail with a stubbed `deps.buildGmailClient` and asserts assertApproved ran (low/med passes through; forced is rejected with 'voice-forbidden-forced')
    - `npx vitest run tests/integration/voice-confirm-gate.spec.ts` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/integration/voice-confirm-gate.spec.ts</automated>
  </verify>
  <done>SC2 proves the dedicated rejection code on forced/high voice rows and the low/med pass; SC4 proves voiceConfirm fires the UI's ready→approved edge and the unchanged send.ts:146 assertApproved gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| voice confirm → approval state | a voice "yes" attempts to authorize a write; the gate is the last line of defense |
| voiceConfirm → write adapters (Phase 17) | the seam must stamp a path value the gate can REJECT for high-stakes actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Elevation of Privilege | `assertApproved` voice path | mitigate | Named `voice-forbidden-forced` branch ordered BEFORE the generic forced check rejects any forced/high row whose path is `'voice-explicit'`; SC2 test asserts the SPECIFIC code so a refactor that folds it into the generic branch is caught |
| T-14-05 | Tampering | `voiceConfirm` writing state via raw SQL | mitigate | voiceConfirm wraps `transitionTo` exclusively (transactional + assertTransition-validated + ALLOWED_PATCH_COLS-restricted); the confirm.spec invalid-transition case proves it cannot bypass the state machine |
| T-14-06 | Repudiation | low/med voice confirm vs UI approve divergence | accept | SC4 is true by construction — `ready→approved` is the ONLY edge into approved and is the exact edge the UI fires (D-05); zero new state means no parallel path to diverge (D-06: no new draft state added). Residual: provenance is carried only by approval_path, not a separate audit column (deferred confirm_channel, v2.1) |

**Residual risk:** voiceConfirm is dormant with zero callers this phase, so the live attack surface is nil until Phase 17 wires a caller. The frozen `(db, approvalId)` signature + the gate test fix the contract now so Phase 17 cannot re-litigate it.
</threat_model>

<verification>
- `npm run typecheck` exits 0 after all edits.
- `npx vitest run src/main/voice/confirm.spec.ts tests/integration/voice-confirm-gate.spec.ts` passes.
- SC2: forced/high voice-explicit row → `voice-forbidden-forced` (specific code); low/med voice-explicit row → passes.
- SC4: voiceConfirm then sendApprovedEmail reaches the unchanged assertApproved at send.ts:146.
</verification>

<success_criteria>
- gate.ts has the named `voice-forbidden-forced` branch ordered before the generic forced check (D-02).
- voiceConfirm is a pure, dormant, two-arg seam wrapping transitionTo (D-10/D-11).
- SC2 failing-then-passing test asserts the SPECIFIC code (D-12).
- SC4 test proves the UI-identical transition + unchanged send adapter (D-12).
</success_criteria>

<output>
After completion, create `.planning/phases/14-voice-safety-confirm-contract/14-02-SUMMARY.md`
</output>
