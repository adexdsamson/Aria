---
phase: 04-calendar-smart-scheduling-google
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db/migrations/010_calendar_writeback.sql
  - src/main/db/migrations/runner.ts
  - src/main/integrations/google/auth.ts
  - src/main/integrations/google/calendar.ts
  - src/main/integrations/google/recurrence.ts
  - src/main/integrations/google/write-event.ts
  - src/main/approvals/persist.ts
  - src/main/scheduling/audit.ts
  - src/renderer/features/settings/IntegrationsSection.tsx
  - tests/static/single-calendar-write-site.test.ts
  - tests/unit/main/db/migrations.spec.ts
  - tests/unit/main/integrations/google/recurrence.test.ts
  - tests/unit/main/integrations/google/write-event.test.ts
  - tests/unit/main/integrations/google/write-event-etag.test.ts
  - tests/fixtures/google/recurring-events.json
  - package.json
autonomous: true
requirements: [CAL-04, APPR-02]
requirements_addressed: [CAL-04, APPR-02]
tags: [calendar, oauth, recurring, chokepoint, migration]
user_setup:
  - service: google-calendar
    why: "calendar.events write scope must be granted by user via re-consent banner (real consent screen only reachable in dev with a live Google account)"
    env_vars: []
must_haves:
  truths:
    - "SQLite user_version advances to 10 after migration; existing approval rows preserved"
    - "calendar_event table has etag, i_cal_uid, sequence, organizer_email, organizer_self, recurrence_json columns"
    - "scheduling_rules singleton row exists with default empty rules + UTC TZ"
    - "calendar_action_log table accepts proposed/pre_write/post_write/failed/override phases"
    - "approval.kind CHECK accepts both 'email_send' and 'calendar_change'"
    - "auth.ts SCOPES.calendar includes calendar.events write scope"
    - "write-event.ts is the ONLY file calling googleapis events.patch / events.insert (static grep)"
    - "applyCalendarChange throws if approval state != 'approved' BEFORE any Google API call"
    - "Recurring write computes correct ops for scope=this (instance ID), scope=all (parent), scope=future (UNTIL split via rrule.js)"
    - "etag mismatch (412) propagates as EtagMismatchError, no silent overwrite"
  artifacts:
    - path: src/main/db/migrations/010_calendar_writeback.sql
      provides: "approval kind widen + calendar_event additive cols + scheduling_rules + calendar_action_log"
    - path: src/main/integrations/google/write-event.ts
      provides: "applyCalendarChange(db, client, approvalId) — APPR-02 chokepoint"
      exports: ["applyCalendarChange", "ApplyCalendarChangeDeps"]
    - path: src/main/integrations/google/recurrence.ts
      provides: "computeRecurringWrite({scope, event, change}) — returns ops array for this/future/all"
      exports: ["computeRecurringWrite", "RecurringWritePlan"]
    - path: src/main/scheduling/audit.ts
      provides: "logCalendarAction(db, row) append-only writer to calendar_action_log"
      exports: ["logCalendarAction", "CalendarActionPhase"]
    - path: tests/static/single-calendar-write-site.test.ts
      provides: "static-grep ratchet asserting only write-event.ts contains events.patch/insert literals"
  key_links:
    - from: "src/main/integrations/google/write-event.ts:applyCalendarChange"
      to: "src/main/approvals/gate.ts:assertApproved"
      via: "first executable line"
      pattern: "assertApproved\\(db, approvalId\\)"
    - from: "src/main/integrations/google/write-event.ts"
      to: "src/main/integrations/google/recurrence.ts:computeRecurringWrite"
      via: "before any Google API call"
      pattern: "computeRecurringWrite\\("
    - from: "src/main/integrations/google/write-event.ts"
      to: "src/main/scheduling/audit.ts:logCalendarAction"
      via: "pre_write + post_write OR failed phases"
      pattern: "logCalendarAction"
---

<objective>
Wave 0 + write-back chokepoint for Phase 4. Pays back Phase 2 etag debt via migration 010, extends polymorphic approval table for kind='calendar_change', adds calendar.events OAuth scope, installs rrule.js, and lands the single APPR-02 chokepoint `applyCalendarChange` with full this/future/all recurring semantics. Static-grep ratchet locks the chokepoint as the sole call site for events.patch/insert.

Purpose: All downstream Phase 4 work (rules engine, NL agent) depends on the data shape + write chokepoint existing. Without this plan, no calendar can be written.
Output: Migration 010 applied; write-event.ts chokepoint live; static-grep test passing; recurrence handling proven by unit tests; OAuth re-consent banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calendar-smart-scheduling-google/04-CONTEXT.md
@.planning/phases/04-calendar-smart-scheduling-google/04-RESEARCH.md
@.planning/phases/04-calendar-smart-scheduling-google/04-PATTERNS.md
@.planning/phases/03-approval-queue-sensitivity-router-email-triage-drafting-send/03-04-SUMMARY.md
@src/main/integrations/google/send.ts
@src/main/integrations/google/auth.ts
@src/main/integrations/google/calendar.ts
@src/main/db/migrations/006_approvals_and_tier.sql
@src/main/db/migrations/003_calendar.sql
@src/main/approvals/gate.ts
@src/main/approvals/persist.ts
@src/main/approvals/state.ts
@tests/static/single-send-call-site.test.ts

<interfaces>
From src/main/approvals/gate.ts (REUSE AS-IS):
  export function assertApproved(db: Db, approvalId: string): void
  export class ApprovalGateError extends Error { code: 'not-found' | 'not-approved' | ... }

From src/main/approvals/persist.ts (EXTEND):
  export type ApprovalKind = 'email_send'  // widen to: 'email_send' | 'calendar_change'
  export interface NewApprovalInput { kind: ApprovalKind; state: ApprovalState; ... }
  // ADD optional calendar_* fields (NULL when kind='email_send')

From src/main/integrations/google/send.ts (ANALOG — copy file-header docblock + structure verbatim):
  // file-header declares "ONLY call site for gmail.users.messages.send", cites static-grep test
  export async function sendApprovedEmail(db, approvalId, deps): Promise<SendResult>
  //   line 1: assertApproved(db, approvalId)
  //   audit-log on success AND failure paths
  //   transitionTo('sent') only on success

From src/main/integrations/google/calendar.ts (EXTEND):
  // Add methods to CalendarClient wrapper, each wrapped in try/catch + maybeThrowTokenInvalid:
  //   patchEvent({eventId, requestBody, ifMatch, sendUpdates?}): Promise<calendar_v3.Schema$Event>
  //   insertEvent({requestBody, sendUpdates?}): Promise<calendar_v3.Schema$Event>
  //   eventsInstances({eventId, timeMin, timeMax}): Promise<calendar_v3.Schema$Event[]>
  //   freebusyQuery({timeMin, timeMax, calendarIds}): Promise<FreeBusyResult>
  //   getCalendarSettings(): Promise<{timeZone: string, workingHoursIfAvailable?: ...}>
  // Add: export class EtagMismatchError extends Error { code: 'etag-mismatch' }
  // Add: export class InvalidInstanceIdError extends Error { code: 'invalid-instance-id' }

New IntentSchema-style type for recurrence ops (DEFINE in recurrence.ts):
  export type RecurringWriteOp =
    | { kind: 'patch'; id: string; etag?: string; body: calendar_v3.Schema$Event; sendUpdates: 'none' }
    | { kind: 'insert'; body: calendar_v3.Schema$Event; sendUpdates: 'none' };
  export interface RecurringWritePlan { ops: RecurringWriteOp[]; rollback?: () => RecurringWriteOp[] }
  export function computeRecurringWrite(input: {
    scope: 'this' | 'future' | 'all';
    event: { id: string; parentId?: string; recurrence?: string[]; etag?: string; startUtc: string; ... };
    change: { startUtc?: string; endUtc?: string; ... };
  }): RecurringWritePlan;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 010 + rrule.js install + migration runner test bump</name>
  <files>
    src/main/db/migrations/010_calendar_writeback.sql,
    src/main/db/migrations/runner.ts,
    tests/unit/main/db/migrations.spec.ts,
    src/main/approvals/persist.ts,
    package.json
  </files>
  <read_first>
    - src/main/db/migrations/006_approvals_and_tier.sql (analog — RENAME/CREATE/INSERT/DROP idiom for kind CHECK widening; verbatim index names)
    - src/main/db/migrations/003_calendar.sql (current calendar_event shape; columns to ADD)
    - src/main/db/migrations/runner.ts (advance applied version to 10)
    - tests/unit/main/db/migrations.spec.ts (current asserts use range [1..9] / user_version === 9 — bump to [1..10] / 10)
    - src/main/approvals/persist.ts (NewApprovalInput, ApprovalKind union — widen)
    - .planning/phases/04-calendar-smart-scheduling-google/04-RESEARCH.md "Pattern 2: Polymorphic Approval Extension" (canonical migration sketch)
  </read_first>
  <behavior>
    - Migration 010 applies cleanly on a DB at user_version=9 and advances to 10
    - approval table after migration has kind CHECK IN ('email_send','calendar_change') and 8 new calendar_* nullable columns
    - All existing approval rows preserved with NULL calendar_* values
    - calendar_event gains etag, i_cal_uid, sequence, organizer_email, organizer_self (INTEGER 0/1), recurrence_json columns
    - scheduling_rules singleton row exists with id=1, default empty rules JSON, time_zone='UTC'
    - calendar_action_log table accepts phase enum {proposed, pre_write, post_write, failed, override}
    - persist.ts ApprovalKind widened to 'email_send' | 'calendar_change'; NewApprovalInput accepts optional calendar_event_id, calendar_action, recurring_scope, before_json, after_json, conflicts_json, alternatives_json, rule_overrides_json
    - rrule installed in package.json (latest 2.x); version captured
  </behavior>
  <action>
    Install rrule via `npm install rrule` (capture installed version in package.json deps). Author migration 010 per RESEARCH §Pattern 2 lines 261–319: (a) RENAME/CREATE/INSERT/DROP to widen approval.kind CHECK and add the 8 nullable calendar columns (calendar_event_id TEXT, calendar_action TEXT CHECK move/create/find-time/NULL, recurring_scope TEXT CHECK this/future/all/NULL, before_json TEXT, after_json TEXT, conflicts_json TEXT, alternatives_json TEXT, rule_overrides_json TEXT) — copy column list from 006 verbatim, then INSERT SELECT with NULL padding; recreate idx_approval_state / idx_approval_kind_state / idx_approval_updated_at exactly as 006 names them; (b) six ALTER TABLE calendar_event ADD COLUMN statements (etag TEXT, i_cal_uid TEXT, sequence INTEGER, organizer_email TEXT, organizer_self INTEGER, recurrence_json TEXT); (c) CREATE TABLE scheduling_rules with id INTEGER PRIMARY KEY CHECK (id = 1), rules_json TEXT NOT NULL, time_zone TEXT NOT NULL, updated_at TEXT NOT NULL, plus INSERT OR IGNORE default row; (d) CREATE TABLE calendar_action_log with AUTOINCREMENT id, approval_id FK, phase CHECK enum, event_id, recurring_scope, before_json, after_json, rule_overrides_json, google_etag, google_error, created_at NOT NULL, plus two indexes (approval_id, event_id). Update runner.ts max version to 10. Update migrations.spec.ts version asserts: range [1..10], user_version === 10, and add assertions for (i) approval row count preserved across migration, (ii) scheduling_rules has one row, (iii) approval.kind CHECK accepts 'calendar_change' (try INSERT). Widen ApprovalKind union in persist.ts to 'email_send' | 'calendar_change'; extend NewApprovalInput with the 8 nullable calendar fields; ensure insertApproval SQL writes them. Per A4/A6 assumptions and Pitfall 7. No fenced code in implementation — follow 006 patterns exactly.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/main/db/migrations.spec.ts tests/unit/main/approvals/persist.test.ts</automated>
  </verify>
  <done>
    Migration test green; user_version reaches 10; old approval rows preserved; ApprovalKind union widened; rrule installed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Calendar wrapper extension + OAuth scope + recurrence module</name>
  <files>
    src/main/integrations/google/auth.ts,
    src/main/integrations/google/calendar.ts,
    src/main/integrations/google/recurrence.ts,
    src/renderer/features/settings/IntegrationsSection.tsx,
    tests/unit/main/integrations/google/recurrence.test.ts,
    tests/fixtures/google/recurring-events.json
  </files>
  <read_first>
    - src/main/integrations/google/auth.ts SCOPES (current calendar entry is readonly-only — see PATTERNS §auth.ts diff)
    - src/main/integrations/google/calendar.ts (existing wrapper; error-translation envelope lines 75–116; SyncTokenInvalidatedError class shape lines 29–35)
    - src/main/integrations/google/sync-calendar.ts (TZ helper style; computeTodayBoundsUtc reuse for UTC formatting)
    - src/renderer/features/settings/IntegrationsSection.tsx (Phase 3 03-04 added gmail.send re-consent banner — copy that block for calendar.events)
    - .planning/phases/04-calendar-smart-scheduling-google/04-RESEARCH.md §Pattern 3 (this/future/all matrix) + §Pitfalls 2, 3, 4 (instance ID drift, parent-vs-instance, UNTIL split rollback)
    - rrule README — RRule.fromString, rule.options, rule.toString, UNTIL semantics
  </read_first>
  <behavior>
    - SCOPES.calendar in auth.ts contains BOTH calendar.readonly AND calendar.events (incremental consent)
    - CalendarClient.patchEvent passes ifMatch + sendUpdates='none' to googleapis events.patch; 412 response throws EtagMismatchError; 401/invalid_grant throws via maybeThrowTokenInvalid
    - CalendarClient.insertEvent passes sendUpdates='none' default
    - CalendarClient.eventsInstances calls events.instances(parentId, timeMin, timeMax) and returns event array
    - CalendarClient.freebusyQuery calls freebusy.query with up to N calendarIds in one batch
    - CalendarClient.getCalendarSettings returns at minimum {timeZone}; gracefully returns workingHours=undefined when API does not expose it (per Open Q 1)
    - computeRecurringWrite({scope:'this', event, change}) returns ops=[{kind:'patch', id:instanceId, etag, body, sendUpdates:'none'}] where instanceId contains an underscore
    - computeRecurringWrite({scope:'all', event:{parentId,...}, change}) returns ops=[{kind:'patch', id:parentId, etag, body}]
    - computeRecurringWrite({scope:'future', event:{parentId,recurrence:[RRULE],...}, change}) returns ops=[{kind:'patch', id:parentId, body:{recurrence:[RRULE_with_UNTIL]}}, {kind:'insert', body:{...newEvent, recurrence:[newRRULE_cleared_UNTIL]}}] with a rollback function that restores original RRULE on insert failure
    - InvalidInstanceIdError thrown if scope='this' but event.id has no underscore separator
    - IntegrationsSection renders a calendar re-consent banner when user has only readonly grant but write is required (use existing banner pattern from gmail.send precedent)
  </behavior>
  <action>
    Edit auth.ts SCOPES.calendar to add 'https://www.googleapis.com/auth/calendar.events' alongside readonly per Pattern I (PATTERNS §auth.ts). In calendar.ts, add three exported error classes mirroring SyncTokenInvalidatedError shape: EtagMismatchError, InvalidInstanceIdError. Add five wrapper methods (patchEvent, insertEvent, eventsInstances, freebusyQuery, getCalendarSettings) each wrapped in try/catch with maybeThrowTokenInvalid(err); throw err — Pattern D from PATTERNS. patchEvent: detect HTTP 412 (err.code === 412 or err.response.status === 412) and translate to EtagMismatchError before the generic throw. Defensive guard in patchEvent: if requestBody marker indicates `recurring_scope === 'this'` is being honored by caller, also enforce eventId contains '_' separator (planner notes the check belongs in write-event.ts instead — keep wrapper plain; document deference in JSDoc). Author recurrence.ts as a pure-functional module using rrule (RRule.fromString / rule.options spread / rule.toString); implement three branches per RESEARCH §Pattern 3 table; for 'future' use rrule.js to clone parent RRULE, set options.until to (instance startUtc - 1 day) inclusive boundary handling, regenerate string; produce ops + rollback. Build fixture file `tests/fixtures/google/recurring-events.json` containing three representative event payloads: single non-recurring, weekly recurring (no exceptions), weekly recurring with one previously-moved exception. Write `recurrence.test.ts` covering: (a) scope=this on plain instance returns instance-ID patch op; (b) scope=this on previously-modified instance requires caller to supply the exception event id (test asserts InvalidInstanceIdError when caller passes a constructed templated ID instead); (c) scope=all returns parentId patch op only; (d) scope=future returns 2 ops with UNTIL on parent + new RRULE on insert; (e) rollback function returns single op that restores original RRULE. Update IntegrationsSection.tsx: when the calendar status indicates writeScopeMissing (a new field surfaced via existing IPC status hook — wire it via existing googleStatus channel without API change, derived from SCOPES introspection), render a re-consent banner with copy: "Aria needs permission to make changes to your calendar. Reconnect Google Calendar." Mirror gmail.send banner structure exactly.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/main/integrations/google/recurrence.test.ts tests/unit/renderer/features/settings/IntegrationsSection.spec.tsx</automated>
  </verify>
  <done>
    SCOPES.calendar includes calendar.events; CalendarClient has 5 new methods + 2 error classes; recurrence.test.ts green covering all 3 scopes + rollback; IntegrationsSection banner appears when write scope absent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: APPR-02 chokepoint write-event.ts + audit log + static-grep ratchet</name>
  <files>
    src/main/integrations/google/write-event.ts,
    src/main/scheduling/audit.ts,
    tests/static/single-calendar-write-site.test.ts,
    tests/unit/main/integrations/google/write-event.test.ts,
    tests/unit/main/integrations/google/write-event-etag.test.ts
  </files>
  <read_first>
    - src/main/integrations/google/send.ts (entire file — copy file-header docblock structure, imports pattern, chokepoint shape, audit-log-on-both-paths, DI seam, transitionTo-on-success-only)
    - src/main/approvals/gate.ts (assertApproved signature + error classes)
    - src/main/approvals/persist.ts (getApproval, transitionTo, post-Task 1 calendar_* columns)
    - src/main/approvals/state.ts (transitions: approved → sent path)
    - tests/static/single-send-call-site.test.ts (verbatim template — copy file, swap regex + allow-list)
    - src/main/db/migrations/010_calendar_writeback.sql (calendar_action_log schema for audit.ts INSERT)
    - .planning/phases/04-calendar-smart-scheduling-google/04-RESEARCH.md §Code Examples (write-back chokepoint sketch) + §Pitfalls 1 (etag), 3 (this-scope guard), 9 (static grep mandate)
    - .planning/phases/04-calendar-smart-scheduling-google/04-PATTERNS.md §write-event.ts mapping
  </read_first>
  <behavior>
    - applyCalendarChange(db, client, approvalId) FIRST executable line is assertApproved(db, approvalId) — verified by source assertion test
    - Calling applyCalendarChange when approval.state !== 'approved' throws ApprovalGateError code='not-approved' (or upstream error); zero Google API calls made
    - On success: pre_write row written BEFORE Google call; post_write row written AFTER; approval.state transitions to 'sent'
    - On Google API failure: failed row written with google_error; approval state does NOT transition (remains 'approved'); error rethrown
    - On 412 EtagMismatchError: caught, failed row written, EtagMismatchError rethrown unchanged (consumer in Plan 04-03 will surface as a refresh-card prompt)
    - Scope guard: if recurring_scope='this', calendar_event_id MUST contain '_'; else throw InvalidInstanceIdError before any API call
    - sendUpdates='none' is hard-coded literal in this file (enforced by static grep secondary regex)
    - logCalendarAction(db, row) appends one row to calendar_action_log; phase in {proposed, pre_write, post_write, failed, override}
    - Static grep test: scans src/main/**, asserts `events.patch(` and `events.insert(` literals exist exclusively in src/main/integrations/google/write-event.ts AND src/main/integrations/google/calendar.ts (the wrapper file is allow-listed since the chokepoint enforces approval at the function boundary, not the literal site — per PATTERNS planner decision). Wrapper count > 0; non-allow-list count == 0; chokepoint file references writeEventApprovalId via assertApproved literal.
  </behavior>
  <action>
    Author write-event.ts mirroring send.ts structurally (PATTERNS §Pattern A): file-header docblock declaring it as the only chokepoint for events.patch/insert and citing tests/static/single-calendar-write-site.test.ts; imports including assertApproved, getApproval, transitionTo, computeRecurringWrite, logCalendarAction, CalendarClient type. Export interface ApplyCalendarChangeDeps { buildCalendarClient?: () => Promise<CalendarClient> } per send.ts DI seam (lines 73–77). Export async function applyCalendarChange(db, approvalId, deps?). Line 1 of body: assertApproved(db, approvalId). Then: read approval row, parse before_json/after_json/recurring_scope, validate scope='this' implies '_' in calendar_event_id (throw InvalidInstanceIdError), call computeRecurringWrite() to derive plan, write pre_write audit row with before_json/after_json. Use try/catch around the ops loop: for each op, call client.patchEvent or client.insertEvent with sendUpdates:'none' literal. On success: write post_write audit row including google_etag from response, then transitionTo(db, approvalId, 'sent'). On catch: write failed audit row with google_error (String(err)) and rollback ops if RecurringWritePlan.rollback exists (best-effort attempt; log second failed row if rollback throws); rethrow original error. Author scheduling/audit.ts per Pattern K: export type CalendarActionPhase, export function logCalendarAction(db, {approval_id, phase, event_id?, recurring_scope?, before_json?, after_json?, rule_overrides_json?, google_etag?, google_error?}) — single prepared INSERT, returns rowid. Copy tests/static/single-send-call-site.test.ts verbatim to tests/static/single-calendar-write-site.test.ts; swap SEND_RE to TWO regexes (PATCH_RE and INSERT_RE per PATTERNS lines 280–283); swap allowed-paths array to ['src/main/integrations/google/write-event.ts', 'src/main/integrations/google/calendar.ts']; add a third assertion that sendUpdates literal 'all' is NEVER present in write-event.ts (defense in depth per Pitfall 5/Security Threat). Write write-event.test.ts with deps-injected fake CalendarClient: (a) assertApproved-throws test (approval state='pending' → throws, zero client calls); (b) happy path scope='all' → 1 patch call, transitions to sent, 2 audit rows; (c) happy path scope='this' → eventId with '_' accepted; (d) scope='this' without '_' in id → InvalidInstanceIdError, zero client calls; (e) Google API failure → failed audit row + state unchanged + rethrow. Write write-event-etag.test.ts: (f) fake client throws EtagMismatchError on patch → failed row written with google_error containing 'etag-mismatch' + EtagMismatchError rethrown + state unchanged. Per APPR-02 + Pitfall 1 + Pitfall 9.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/static/single-calendar-write-site.test.ts tests/unit/main/integrations/google/write-event.test.ts tests/unit/main/integrations/google/write-event-etag.test.ts tests/unit/main/scheduling/audit.test.ts</automated>
  </verify>
  <done>
    Static grep passes (exactly one chokepoint file + wrapper allow-listed; no 'all' sendUpdates literal); applyCalendarChange enforces assertApproved as line 1; all error/success/etag paths covered with audit-log writes on both branches; state transition only on success.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main IPC | Untrusted approval-id and chat strings cross; main re-validates state via assertApproved + Zod |
| Main → Google API | Outbound write; etag enforces optimistic concurrency; sendUpdates='none' avoids attendee notification |
| Main → SQLite | Single-writer; migration 010 widens schema; CHECK constraints enforce kind + phase enums |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Elevation of Privilege | write-event.ts:applyCalendarChange | mitigate | assertApproved is FIRST executable line; static-grep test ratchets sole call site; CI gate fails if a second site is added |
| T-04-01-02 | Tampering | calendar_event two-writer race | mitigate | etag column on calendar_event (migration 010); patchEvent passes ifMatch; 412 → EtagMismatchError; consumer surfaces refresh prompt (Plan 04-03) |
| T-04-01-03 | Repudiation | calendar writes lack audit trail | mitigate | logCalendarAction writes pre_write + post_write rows on success; failed row on error; both contain google_etag + before/after JSON for forensic replay |
| T-04-01-04 | Repudiation | RRULE manipulation creates orphan future series | mitigate | rrule.js handles UNTIL math; computeRecurringWrite returns rollback ops; insert-failure path attempts rollback + logs second failed row (Pitfall 4) |
| T-04-01-05 | Information Disclosure | calendar.events scope grants more than needed | mitigate | Use narrowest write scope (calendar.events); do NOT request full `calendar` or `calendar.app.created`; incremental consent re-prompt explains why |
| T-04-01-06 | Spoofing | Renderer constructs fake approvalId | mitigate | applyCalendarChange reads approval row from main-process DB; state machine transitions only via persist.transitionTo which validates current state |
| T-04-01-07 | Tampering | "this instance" patch hits parent | mitigate | InvalidInstanceIdError when scope='this' but eventId lacks '_' separator; unit test covers (Pitfall 3) |
| T-04-01-08 | Information Disclosure | sendUpdates='all' silently invites notifications to attendees | accept→mitigate | Static grep asserts 'sendUpdates: all' literal absent from write-event.ts; combined with self-only gate in Plan 04-03 |
</threat_model>

<verification>
- Run `npm run test:unit -- tests/unit/main/db/migrations.spec.ts` — green, user_version=10 asserted
- Run `npm run test:unit -- tests/unit/main/integrations/google/recurrence.test.ts` — green, three scopes covered
- Run `npm run test:unit -- tests/static/single-calendar-write-site.test.ts` — green, allow-list enforced
- Run `npm run test:unit -- tests/unit/main/integrations/google/write-event.test.ts tests/unit/main/integrations/google/write-event-etag.test.ts` — green
- Source assertion: line 1 of applyCalendarChange body contains assertApproved (grep `-n 'assertApproved' src/main/integrations/google/write-event.ts`)
</verification>

<success_criteria>
- Migration 010 applied; user_version=10
- approval.kind CHECK accepts 'calendar_change'
- calendar_event has etag/i_cal_uid/sequence/organizer_*/recurrence_json
- scheduling_rules + calendar_action_log tables exist
- SCOPES.calendar includes calendar.events
- write-event.ts is sole chokepoint; static grep enforces
- recurrence.ts covers all three scopes + rollback
- ApprovalKind widened in persist.ts
</success_criteria>

<output>
After completion, create `.planning/phases/04-calendar-smart-scheduling-google/04-01-SUMMARY.md` capturing: rrule version installed, migration 010 final schema diff vs sketch, any deviation from RESEARCH §Pattern 3, working-hours API resolution (if discovered during getCalendarSettings impl).
</output>
