---
phase: 04-calendar-smart-scheduling-google
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/main/scheduling/rules.ts
  - src/main/scheduling/conflict.ts
  - src/shared/scheduling-rules.ts
  - src/main/ipc/scheduling.ts
  - src/shared/ipc-contract.ts
  - src/main/preload/index.ts
  - src/renderer/features/settings/SchedulingRulesSection.tsx
  - tests/unit/main/scheduling/rules.test.ts
  - tests/unit/main/scheduling/conflict.test.ts
  - tests/unit/main/scheduling/conflict-alternatives.test.ts
  - tests/unit/main/scheduling/conflict-prime-time.test.ts
  - tests/unit/main/scheduling/timezone.test.ts
  - tests/unit/renderer/features/settings/SchedulingRulesSection.spec.tsx
autonomous: true
requirements: [CAL-05, CAL-06, CAL-07]
requirements_addressed: [CAL-05, CAL-06, CAL-07]
tags: [scheduling, rules, conflict, settings, prime-time]
must_haves:
  truths:
    - "User can edit scheduling rules (focus blocks, buffers, no-meeting windows, prime-time, time-zone) in Settings → Scheduling"
    - "Hard rule (focus block, no-meeting, busy block, outside working hours) blocks initial proposal — detectConflictsAndAlternatives reports primaryFeasible=false"
    - "Soft rule (buffer violation) does NOT block — primaryFeasible=true but conflicts array includes buffer entry"
    - "detectConflictsAndAlternatives returns up to 3 alternatives ranked by proximity to requested time"
    - "Prime-time bonus only applies to high-value events (>30min AND external attendees OR explicit tag)"
    - "Time-zone canonical = Google primary-calendar TZ; OS fallback warns via returned warning field"
    - "Zod RulesSchema validates IPC payloads on get/set; invalid JSON returns error to renderer"
  artifacts:
    - path: src/shared/scheduling-rules.ts
      provides: "RulesSchema (Zod) + Rules type — shared by renderer + main"
      exports: ["RulesSchema", "Rules", "DEFAULT_RULES"]
    - path: src/main/scheduling/rules.ts
      provides: "getRules(db) / setRules(db, rules) — CRUD against scheduling_rules singleton; loadActiveRules() helper"
      exports: ["getRules", "setRules", "loadActiveRules"]
    - path: src/main/scheduling/conflict.ts
      provides: "detectConflictsAndAlternatives({target, rules, calendarClient}) → {conflicts, alternatives, primaryFeasible}"
      exports: ["detectConflictsAndAlternatives", "ConflictType", "ConflictReport"]
    - path: src/renderer/features/settings/SchedulingRulesSection.tsx
      provides: "Settings form with focus blocks / buffers / no-meeting / prime-time editors + advanced JSON drawer"
  key_links:
    - from: "src/renderer/features/settings/SchedulingRulesSection.tsx"
      to: "src/shared/scheduling-rules.ts:RulesSchema"
      via: "client-side Zod parse before IPC SCHEDULING_RULES_SET"
      pattern: "RulesSchema.safeParse"
    - from: "src/main/ipc/scheduling.ts"
      to: "src/main/scheduling/rules.ts"
      via: "SCHEDULING_RULES_GET / SCHEDULING_RULES_SET handlers"
      pattern: "getRules\\(|setRules\\("
    - from: "src/main/scheduling/conflict.ts"
      to: "src/main/scheduling/rules.ts:loadActiveRules"
      via: "loaded once per proposal"
      pattern: "loadActiveRules\\("
---

<objective>
Scheduling rules engine + conflict detector. Lands the typed-JSON rules schema (focus blocks, buffers, no-meeting windows, prime-time windows, time-zone), Settings UI to edit them, and the pure-function conflict detector that classifies hard vs soft conflicts and proposes up to 3 alternatives by proximity. CAL-07 prime-time bonus implemented in the slot scorer for high-value events only. No Google write-back here — Plan 04-03 wires this into the NL pipeline.

Purpose: SC-3 requires Aria to refuse focus-block scheduling without explicit override. That refusal is generated by `detectConflictsAndAlternatives`. CAL-06 surface (rules editing) and CAL-05 alternatives also live here.
Output: Rules CRUD + Settings UI + conflict engine fully unit-tested with pure-function inputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-calendar-smart-scheduling-google/04-CONTEXT.md
@.planning/phases/04-calendar-smart-scheduling-google/04-RESEARCH.md
@.planning/phases/04-calendar-smart-scheduling-google/04-PATTERNS.md
@.planning/phases/04-calendar-smart-scheduling-google/04-01-PLAN.md
@src/main/integrations/google/sync-calendar.ts
@src/main/integrations/google/calendar.ts
@src/main/ipc/approvals.ts
@src/main/preload/index.ts
@src/shared/ipc-contract.ts
@src/renderer/features/settings/BriefingSettingsSection.tsx
@src/main/approvals/tier.ts

<interfaces>
From src/shared/scheduling-rules.ts (DEFINE — shared schema):
  export const RulesSchema = z.object({
    focusBlocks: z.array(z.object({
      day: z.union([z.enum(['mon','tue','wed','thu','fri','sat','sun']), z.literal('all')]),
      start: z.string().regex(/^\d{2}:\d{2}$/),
      end: z.string().regex(/^\d{2}:\d{2}$/),
    })),
    buffers: z.object({ beforeMin: z.number().int().min(0).max(120), afterMin: z.number().int().min(0).max(120) }),
    noMeetingWindows: z.array(z.object({ day, start, end, label: z.string().max(60) })),
    primeTimeWindows: z.array(z.object({ day, start, end })),
    timeZone: z.string(), // IANA — validated at boundary
    workingHours: z.object({
      start: z.string().regex(/^\d{2}:\d{2}$/),
      end: z.string().regex(/^\d{2}:\d{2}$/),
      weekdays: z.array(z.number().int().min(0).max(6)),
    }).optional(), // AMENDED per RESEARCH Q1 RESOLVED — authoritative fallback when Google API does not expose working-hours
  });
  export type Rules = z.infer<typeof RulesSchema>;
  export const DEFAULT_RULES: Rules;

From src/main/scheduling/conflict.ts (DEFINE):
  export type ConflictType = 'busy' | 'focus-block' | 'buffer' | 'no-meeting-window' | 'outside-working-hours';
  export type ConflictSeverity = 'hard' | 'soft';
  export interface ConflictReport {
    type: ConflictType;
    severity: ConflictSeverity;
    windowStartUtc: string;
    windowEndUtc: string;
    label?: string; // for no-meeting-window or focus-block name
  }
  export interface AlternativeSlot {
    startUtc: string;
    endUtc: string;
    score: number;
    primeTimeMatched: boolean;
    bufferPenalty: number;
  }
  export interface DetectInput {
    target: { startUtc: string; endUtc: string; eventId?: string; isHighValue: boolean };
    rules: Rules;
    busyIntervals: Array<{ startUtc: string; endUtc: string }>; // pre-fetched via freebusy.query
    workingHoursPerDay?: Record<string, { start: 'HH:mm'; end: 'HH:mm' }>;
  }
  export interface DetectResult {
    primaryFeasible: boolean;
    conflicts: ConflictReport[];
    alternatives: AlternativeSlot[];
    warnings: string[]; // e.g. 'os-tz-fallback'
  }
  export function detectConflictsAndAlternatives(input: DetectInput): DetectResult;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RulesSchema (shared) + rules CRUD + IPC + Settings UI</name>
  <files>
    src/shared/scheduling-rules.ts,
    src/main/scheduling/rules.ts,
    src/main/ipc/scheduling.ts,
    src/shared/ipc-contract.ts,
    src/main/preload/index.ts,
    src/renderer/features/settings/SchedulingRulesSection.tsx,
    tests/unit/main/scheduling/rules.test.ts,
    tests/unit/renderer/features/settings/SchedulingRulesSection.spec.tsx
  </files>
  <read_first>
    - src/main/db/migrations/010_calendar_writeback.sql (post-04-01: scheduling_rules singleton schema)
    - src/main/approvals/tier.ts (analog: typed config CRUD against singleton table; copy shape)
    - src/renderer/features/settings/BriefingSettingsSection.tsx (analog: form + dirty-flag + save IPC pattern + advanced-JSON drawer)
    - src/main/ipc/approvals.ts (analog: registerXHandlers + DbHolder + notReady guard — PATTERNS §Pattern H)
    - src/shared/ipc-contract.ts (CHANNELS layout — add SCHEDULING_* constants)
    - src/main/preload/index.ts (exposure pattern; window.aria.scheduling.* binding)
    - .planning/phases/04-calendar-smart-scheduling-google/04-CONTEXT.md "Rules engine design" (canonical schema shape)
  </read_first>
  <behavior>
    - RulesSchema.safeParse rejects bad day enum, bad HH:mm regex, negative buffers, buffer > 120
    - DEFAULT_RULES has empty focusBlocks/noMeetingWindows/primeTimeWindows, buffers {before:0, after:0}, timeZone='UTC', workingHours undefined
    - RulesSchema accepts optional workingHours { start: HH:mm, end: HH:mm, weekdays: number[] } — fallback used when Google's CalendarSettings does not expose working-hours (per RESEARCH Q1 RESOLVED)
    - getRules(db) returns DEFAULT_RULES when row JSON is the default insert (covered by migration 010 INSERT OR IGNORE)
    - setRules(db, rules) overwrites row + updates updated_at; throws if Zod parse fails
    - loadActiveRules() = thin wrapper over getRules with main-process DB handle
    - IPC SCHEDULING_RULES_GET returns {rules, timeZone, updatedAt}; SCHEDULING_RULES_SET accepts payload, validates via RulesSchema, persists; returns {ok:true} or {error:'INVALID_RULES', issues}
    - Preload exposes window.aria.scheduling.rules.get() / .set()
    - SchedulingRulesSection renders sections: Focus Blocks (add/remove rows), Buffers (two numeric inputs), No-Meeting Windows (rows with label), Prime-Time Windows, Time-Zone (IANA text + Google-detected hint), Advanced JSON drawer (collapsed by default). Save button disabled until form is dirty AND Zod parse succeeds client-side.
    - On save failure (server-side Zod), inline error appears under the advanced-JSON drawer
  </behavior>
  <action>
    Define src/shared/scheduling-rules.ts with RulesSchema (Zod) and DEFAULT_RULES per CONTEXT.md "Rules engine design" canonical shape. Use a Day union ('mon'|...|'sun'|'all'). Validate HH:mm via regex. Export Rules type via z.infer. Implement src/main/scheduling/rules.ts: getRules(db): Rules — SELECT rules_json, time_zone FROM scheduling_rules WHERE id=1, JSON.parse, run RulesSchema.parse, override .timeZone with the row's time_zone column (column is canonical). setRules(db, rules) — RulesSchema.parse then UPDATE scheduling_rules SET rules_json=?, time_zone=?, updated_at=datetime('now') WHERE id=1. loadActiveRules(db) — alias of getRules. Add SCHEDULING_RULES_GET, SCHEDULING_RULES_SET to src/shared/ipc-contract.ts CHANNELS. Create src/main/ipc/scheduling.ts exporting registerSchedulingHandlers(ipcMain, {dbHolder, logger}) — copy approvals.ts skeleton lines 51–60: DbHolder + notReady() guard; two handlers wire to rules.ts; SCHEDULING_RULES_SET runs RulesSchema.safeParse and returns {error:'INVALID_RULES', issues} on failure. Wire registration into main entry (planner inspects existing ipc registration site in main/index.ts). Expose window.aria.scheduling.rules in preload/index.ts mirroring window.aria.approvals shape. Build SchedulingRulesSection.tsx using BriefingSettingsSection as the form analog: useState for rules + isDirty; load on mount via window.aria.scheduling.rules.get(); render five subsections + advanced JSON textarea (RulesSchema.safeParse client-side); Save button calls .set() and shows toast on success / inline issues on failure. Tests: rules.test.ts covers (a) DEFAULT_RULES round-trip; (b) setRules persists + getRules returns same; (c) invalid Zod (negative buffer) throws; (d) timeZone column kept consistent with rules_json.timeZone. SchedulingRulesSection.spec.tsx covers (e) renders with loaded rules; (f) adding a focus block updates state + Save enables; (g) bad advanced-JSON disables Save.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/main/scheduling/rules.test.ts tests/unit/renderer/features/settings/SchedulingRulesSection.spec.tsx</automated>
  </verify>
  <done>
    RulesSchema shared between main + renderer; CRUD round-trip green; IPC channels wired; Settings section renders and persists.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conflict detector + alternatives ranker + prime-time scorer + TZ canonical</name>
  <files>
    src/main/scheduling/conflict.ts,
    tests/unit/main/scheduling/conflict.test.ts,
    tests/unit/main/scheduling/conflict-alternatives.test.ts,
    tests/unit/main/scheduling/conflict-prime-time.test.ts,
    tests/unit/main/scheduling/timezone.test.ts
  </files>
  <read_first>
    - src/shared/scheduling-rules.ts (RulesSchema + Rules type — from Task 1)
    - src/main/integrations/google/sync-calendar.ts (TZ helpers — computeTodayBoundsUtc; reuse Intl.DateTimeFormat-based pattern; pure window math)
    - .planning/phases/04-calendar-smart-scheduling-google/04-RESEARCH.md §Pattern 4 (conflict detection + top-3 algorithm) + Open Q 3 (single freebusy.query for 14-day window)
    - .planning/phases/04-calendar-smart-scheduling-google/04-CONTEXT.md "Conflict detection + alternatives" (slot scorer inputs: proximity, buffer adherence, prime-time bonus)
  </read_first>
  <behavior>
    - detectConflictsAndAlternatives is a pure function: given target, rules, pre-fetched busyIntervals, optional workingHoursPerDay → returns DetectResult
    - Hard conflict (busy block / focus block / no-meeting window / outside working hours) → primaryFeasible=false, conflicts include severity='hard'
    - Soft conflict (buffer violation) → primaryFeasible=true, conflicts include severity='soft', bufferPenalty surfaced via alternatives only
    - Alternatives: search starts at target.startUtc; walk forward in 15-min increments up to 14 days; skip outside-working-hours; skip hard conflicts; score = -|distanceFromRequestedMs| + (target.isHighValue && primeTimeMatched ? +5*60*1000 : 0) - bufferPenalty; stop at first 3 viable
    - Prime-time bonus only fires when target.isHighValue === true (test isolates this)
    - Time-zone: target/busy intervals all UTC ISO; rules.timeZone is the canonical zone for day-of-week and HH:mm interpretation
    - If workingHoursPerDay (from Google CalendarSettings) is undefined, function falls back to rules.workingHours when present — expands the single { start, end, weekdays } into a per-day map for the working-hours gate (per RESEARCH Q1 RESOLVED). If BOTH workingHoursPerDay AND rules.workingHours are undefined, function operates without working-hours gate and adds 'working-hours-unavailable' to warnings.
  </behavior>
  <action>
    Implement detectConflictsAndAlternatives in src/main/scheduling/conflict.ts as a pure function — no DB access, no Google API calls (caller fetches busyIntervals via freebusy.query separately, wired in Plan 04-03). Use Intl.DateTimeFormat with rules.timeZone to map UTC instants to day-of-week + local HH:mm for rule comparisons (mirroring sync-calendar.ts pattern; do NOT hand-roll TZ math). Resolve effective working-hours map: if input.workingHoursPerDay is provided use it directly; else if input.rules.workingHours is set, expand {start,end,weekdays} into a per-weekday record; else treat as unrestricted and push 'working-hours-unavailable' warning. Steps: (1) primary check — for each rule type intersect target window with busy intervals + each focus block instance for the target's local day + each no-meeting window + working hours; classify; (2) if any hard conflict found primaryFeasible=false; (3) buffer check — measure gap to nearest adjacent busy interval; if either side gap < rules.buffers.beforeMin/afterMin, emit soft buffer conflict; (4) alternatives loop — same checks at incrementing 15-min UTC starts, duration preserved from target; for each viable candidate, compute score per behavior block; collect first 3; return. Tests: conflict.test.ts — fixtures for each conflict type, asserts type/severity/window. conflict-alternatives.test.ts — busy 3pm event with rules empty, request 3pm move → alternatives are 3:15, 3:30, 3:45 (or skip past busy) ranked by proximity ascending. conflict-prime-time.test.ts — same calendar, high-value vs low-value events; assert prime-time candidate ranks above closer-in-time-but-outside-prime ONLY when isHighValue=true. timezone.test.ts — same UTC instant, rules.timeZone='America/Los_Angeles' vs 'America/New_York' produces different focus-block hits (covers the canonical-TZ invariant); workingHoursPerDay omitted → warnings includes 'working-hours-unavailable'.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/main/scheduling/conflict.test.ts tests/unit/main/scheduling/conflict-alternatives.test.ts tests/unit/main/scheduling/conflict-prime-time.test.ts tests/unit/main/scheduling/timezone.test.ts</automated>
  </verify>
  <done>
    All four conflict tests green; pure-function shape verified (no IO mocks needed); prime-time bonus isolated to high-value branch; TZ canonical drives day-of-week mapping.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main IPC (SCHEDULING_RULES_SET) | Untrusted typed JSON crosses; main re-validates via RulesSchema.safeParse |
| Main → SQLite | Single writer to scheduling_rules singleton |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | renderer sends malformed rules JSON | mitigate | RulesSchema.safeParse on both client (UX) and main (authoritative); main rejects with {error:INVALID_RULES} |
| T-04-02-02 | Tampering | bypassing focus block via direct DB write | accept | Renderer cannot write DB directly (Electron isolation); main-process callers go through setRules which validates |
| T-04-02-03 | Repudiation | user overrides hard rule | mitigate | Plan 04-03 wires override into calendar_action_log phase='override' with reason string |
| T-04-02-04 | Information Disclosure | rules JSON includes PII | accept | Rules are user-authored time-windows + IANA TZ; no PII surface; stored only locally in encrypted SQLCipher DB |
| T-04-02-05 | Denial of Service | alternative search loop iterates 14d × 4/hour × 24 = 1344 steps per call | mitigate | Pure-function with early-exit at 3 candidates; conflict.test.ts asserts O(n) bound; loop runs in main process, not blocking renderer |
</threat_model>

<verification>
- `npm run test:unit -- tests/unit/main/scheduling/ tests/unit/renderer/features/settings/SchedulingRulesSection.spec.tsx` — all green
- Settings UI manual smoke: open Settings → Scheduling, add a focus block, save, reopen — persists
</verification>

<success_criteria>
- Rules CRUD persists; default singleton row from migration 010 loads correctly
- Settings UI renders all five sections + advanced JSON drawer
- Conflict detector classifies hard vs soft per CONTEXT.md
- Top-3 alternatives ranked by proximity
- Prime-time bonus only for high-value (CAL-07)
- TZ canonical = rules.timeZone for day-of-week mapping
</success_criteria>

<output>
After completion, create `.planning/phases/04-calendar-smart-scheduling-google/04-02-SUMMARY.md` documenting: working-hours API resolution status (continued from 04-01), any deferred rule types, Settings UI structure.
</output>
