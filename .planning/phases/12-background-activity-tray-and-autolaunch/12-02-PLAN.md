---
phase: 12-background-activity-tray-and-autolaunch
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - build/icon.svg
  - build/icon-badged.svg
  - build/tray-icon.ico
  - build/tray-icon-badged.ico
  - build/tray-iconTemplate.png
  - build/tray-iconTemplate@2x.png
  - package.json
  - src/main/tray/index.ts
  - src/main/tray/menu.ts
  - src/main/tray/icons.ts
  - src/main/lifecycle/onUnlock.ts
  - src/main/lifecycle/pendingCatchup.ts
  - src/main/index.ts
  - src/main/ipc/onboarding.ts
  - src/main/briefing/schedule.ts
  - src/main/insights/schedule.ts
  - src/main/recap/schedule.ts
  - src/main/learning/schedule.ts
  - src/main/entitlement/schedule.ts
  - src/main/ipc/gmail.ts
  - src/main/ipc/calendar.ts
  - src/main/folder-ingestion/lifecycle.ts
  - tests/unit/main/tray/index.spec.ts
  - tests/unit/main/tray/menu.spec.ts
  - tests/unit/main/tray/single-instance-tray.spec.ts
  - tests/unit/main/lifecycle/onUnlock.spec.ts
  - tests/unit/main/lifecycle/pendingCatchup.spec.ts
  - tests/static/cron-seal-guard.spec.ts
  - tests/static/no-bare-cron-schedule.spec.ts
autonomous: false
requirements:
  - BG-02
  - BG-04
  - BG-05
  - BG-06

# Requirement mapping (ROADMAP semantics):
#   BG-02 auto-launch toggle surfaced in tray menu  - 12-01 owns prefs+IPC+mirror; this plan surfaces it via menu/state-machine
#   BG-04 sealed-skip + badge + catchup             - owned here (Task 3 cron guards + Task 1/2 catchup drain)
#   BG-05 rich tray menu                            - owned here (Task 1 buildContextMenu)
#   BG-06 platform tray icon assets                 - owned here (Task 0)

must_haves:
  truths:
    - "Tray icon present on Win (left-click->Show Aria, right-click->menu) and Mac (click->menu)"
    - "Tray menu has 6 items: Show Aria / Generate briefing now / Sync now submenu (Gmail+Calendar+Todoist) / Open approvals / separator / Quit Aria"
    - "Menu items that touch DB are enabled:false when dbHolder.db is null"
    - "All 8 cron callsites carry the `if (!db) { pendingCatchup.add(channel); tray.setBadge(); return; }` prelude BEFORE any DB access (static-grep ratchet enforces)"
    - "First unlock fires fireOnUnlock(db) which drains pendingCatchup single-shot per channel and calls tray.clearBadge()"
    - "Tray instance is held in module-level let _tray: Tray|null (GC pinning per Electron #33040 convention)"
    - "Quit Aria menu item sets appIsQuitting via before-quit and calls app.quit()"
    - "Single-instance second-launch raises existing window without constructing a second Tray (Tray creation is INSIDE acquireSingleInstanceLock-gated bootstrap; single-instance-tray.spec.ts enforces createTray-called-at-most-once across two simulated bootstrap invocations)"
    - "macOS dock NEVER hidden - app.dock.hide does not appear anywhere in src/main"
    - "powerMonitor cron tasks still go through scheduler.cronRegistry + registerLifecycleCallbacks (no bypass); no-bare-cron-schedule.spec.ts enforces every cron.schedule callsite registers with scheduler.cronRegistry"
  artifacts:
    - path: "src/main/tray/index.ts"
      provides: "createTray(deps), TrayHandle { setBadge, clearBadge, rebuildMenu, dispose }"
      exports: ["createTray", "TrayHandle", "TrayDeps"]
    - path: "src/main/tray/menu.ts"
      provides: "buildContextMenu(deps): Menu"
      exports: ["buildContextMenu"]
    - path: "src/main/tray/icons.ts"
      provides: "loadTrayIcon(variant: 'plain'|'badged'): NativeImage"
      exports: ["loadTrayIcon"]
    - path: "src/main/lifecycle/onUnlock.ts"
      provides: "registerOnUnlock(cb), fireOnUnlock(db, logger)"
      exports: ["registerOnUnlock", "fireOnUnlock"]
    - path: "src/main/lifecycle/pendingCatchup.ts"
      provides: "Set<CatchupChannel> with add/drain/clear/has"
      exports: ["pendingCatchup", "CatchupChannel"]
    - path: "tests/static/cron-seal-guard.spec.ts"
      provides: "static grep ratchet that all 8 cron callsites carry the seal-guard prelude"
    - path: "tests/static/no-bare-cron-schedule.spec.ts"
      provides: "static grep ratchet that every cron.schedule / cronImpl.schedule callsite registers with scheduler.cronRegistry"
    - path: "tests/unit/main/tray/single-instance-tray.spec.ts"
      provides: "asserts createTray is called at most once across two simulated bootstrap invocations (second-instance lock-fail path)"
    - path: "build/tray-icon.ico"
      provides: "Windows plain tray icon (16/24/32/48 multi-size)"
    - path: "build/tray-icon-badged.ico"
      provides: "Windows badged tray icon (gold dot overlay)"
    - path: "build/tray-iconTemplate.png"
      provides: "macOS Template PNG @1x (22x22)"
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/tray/index.ts"
      via: "createTray(deps) inside bootstrap, AFTER acquireSingleInstanceLock"
      pattern: "createTray\\("
    - from: "src/main/ipc/onboarding.ts"
      to: "src/main/lifecycle/onUnlock.ts"
      via: "fireOnUnlock(db, logger) immediately after holder.db = db"
      pattern: "fireOnUnlock\\("
    - from: "all 8 cron callsites"
      to: "src/main/lifecycle/pendingCatchup.ts"
      via: "pendingCatchup.add(channel) + tray.setBadge() in the if(!db) branch"
      pattern: "pendingCatchup\\.add"
---

<objective>
Ship the tray icon, the rich menu, the sealed-DB cron skip + tray-badge state machine, and the catchup-on-unlock callback registry. This is the centre of mass of Phase 12.

Purpose: With 12-01's prefs in place, this plan delivers the actual visual + behavioural background-mode experience. After it lands, a closed-window Aria is reachable again (via tray); sealed-DB autolaunched Aria shows the gold badge; first unlock drains the catchup queue.

Output: src/main/tray/ module (3 files), src/main/lifecycle/onUnlock.ts + pendingCatchup.ts, 8 cron-callsite edits with seal-guards, fireOnUnlock hook in onboarding.ts, tray construction in bootstrap, 5 unit specs + 2 static ratchets + 1 single-instance spec, 4 tray icon assets generated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/12-background-activity-tray-and-autolaunch/12-CONTEXT.md
@.planning/phases/12-background-activity-tray-and-autolaunch/12-01-PLAN.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-background-activity-tray-and-autolaunch/12-RESEARCH.md

<interfaces>
<!-- Extracted from codebase for executor reference -->

From src/main/ipc/onboarding.ts (line 53):
  - `holder.db = db;` - fireOnUnlock(db, logger) inserts IMMEDIATELY AFTER this assignment.

From src/main/lifecycle/scheduler.ts (existing):
  - cronRegistry is a `Map<string, ScheduledTask>` populated by `scheduleCron(key, expr, cb)`.
  - registerLifecycleCallbacks({ onSuspend, onResume }) handles powerMonitor - DO NOT bypass.

8 cron callsites identified via grep (per RESEARCH Pattern 2):
  1. src/main/briefing/schedule.ts (briefing daily)
  2. src/main/insights/schedule.ts (insights-nightly)
  3. src/main/recap/schedule.ts (recap-monday)
  4. src/main/learning/schedule.ts (learning-nightly)
  5. src/main/entitlement/schedule.ts (entitlement-refresh)
  6. src/main/ipc/gmail.ts (gmail-sync - registered inside IPC handler)
  7. src/main/ipc/calendar.ts (calendar-sync - registered inside IPC handler)
  8. src/main/folder-ingestion/lifecycle.ts (knowledge-folder-sweep)

From src/main/single-instance.ts (lines 36-69):
  - acquireSingleInstanceLock(): boolean - second instance exits BEFORE bootstrap resolves.
  - On second-launch event, raises existing window. Tray creation happens once inside bootstrap (after lock acquired).

Electron Tray API (Electron 41):
  - `new Tray(image)` - MODULE-LEVEL ref required (GC pinning, #33040).
  - `tray.on('click', fn)` - Win: left-click only. Mac: any click.
  - `tray.setContextMenu(Menu)` - Win: right-click opens. Mac: explicit popUpContextMenu() needed on click.
  - `tray.setImage(nativeImage)` - swap for badge variant.
  - `nativeImage.createFromPath(path)` - load .ico (Win) or PNG (Mac). Mac Template inferred from `*Template*` filename.
  - NO `nativeImage.composite()` - does not exist. Pre-bake variants (RESEARCH Pitfall 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Generate tray icon assets (one-shot) - delivers BG-06</name>
  <files>
    build/icon-badged.svg
    build/tray-icon.ico
    build/tray-icon-badged.ico
    build/tray-iconTemplate.png
    build/tray-iconTemplate@2x.png
    package.json
  </files>
  <read_first>
    - build/icon.svg - existing monogram (per memory: brand icon mirrors MonogramSquare)
    - .planning/phases/12-background-activity-tray-and-autolaunch/12-RESEARCH.md Runtime State Inventory
  </read_first>
  <action>
    1. Author build/icon-badged.svg by copying build/icon.svg and adding a small gold circle (#C9A04B or the existing Aria gold) in the bottom-right quadrant (~25% of canvas).
    2. Add "icons:tray" script to package.json that runs `npx --yes electron-icon-builder --input=build/icon.svg --output=build/ --flatten` and a second invocation for the badged variant. Document command in script comment.
    3. Run the script locally; commit the 4 output files: tray-icon.ico, tray-icon-badged.ico, tray-iconTemplate.png (filename MUST include `Template` so macOS auto-inverts), tray-iconTemplate@2x.png.
    4. If electron-icon-builder fails on this Windows box, fall back to hand-authoring via imagemagick `magick convert icon.png -define icon:auto-resize=48,32,24,16 tray-icon.ico`. Document the path taken in 12-02-SUMMARY.md.
    5. Update electron-builder config in package.json: add `"extraResources": ["build/tray-*"]` so the assets are bundled with the packaged app.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const required=['build/tray-icon.ico','build/tray-icon-badged.ico','build/tray-iconTemplate.png','build/tray-iconTemplate@2x.png']; for (const f of required) { if (!fs.existsSync(f)) { console.error('MISSING',f); process.exit(1); } } console.log('OK: all 4 tray assets exist');"</automated>
  </verify>
  <done>
    Four tray asset files exist on disk. icon-badged.svg checked in. package.json has `icons:tray` script. extraResources includes `build/tray-*` so electron-builder bundles them. Delivers BG-06 (platform tray icon assets).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Tray module (icons + menu + index) + lifecycle hooks (onUnlock + pendingCatchup)</name>
  <files>
    src/main/tray/icons.ts
    src/main/tray/menu.ts
    src/main/tray/index.ts
    src/main/lifecycle/onUnlock.ts
    src/main/lifecycle/pendingCatchup.ts
    tests/unit/main/tray/icons.spec.ts
    tests/unit/main/tray/menu.spec.ts
    tests/unit/main/tray/index.spec.ts
    tests/unit/main/lifecycle/onUnlock.spec.ts
    tests/unit/main/lifecycle/pendingCatchup.spec.ts
  </files>
  <read_first>
    - src/main/single-instance.ts - acquireSingleInstanceLock contract
    - src/main/lifecycle/scheduler.ts - cronRegistry shape
    - src/main/ipc/onboarding.ts (lines 38-67) - DbHolder type, holder.db = db insertion point at L53
    - .planning/phases/12-background-activity-tray-and-autolaunch/12-RESEARCH.md Patterns 1-3 + Code Examples
    - src/shared/ipc-contract.ts - channel names for force-sync + BRIEFING_GENERATE_NOW
  </read_first>
  <behavior>
    icons.ts: loadTrayIcon(variant: 'plain'|'badged'): NativeImage. Win -> .ico variants, darwin -> tray-iconTemplate.png (badge via setTitle('*') toggle for V1; 'badged' returns same image), linux -> darwin path. Resolves via `app.isPackaged ? process.resourcesPath : path.join(__dirname, '../../../build')`.

    menu.ts: buildContextMenu(deps): Menu returns a 6-item template per Decision 3: Show Aria / Generate briefing now (enabled iff dbHolder.db !== null) / Sync now submenu (Gmail/Calendar/Todoist; each enabled iff dbOpen AND deps.connected[provider]) / Open approvals / separator / Quit Aria.

    index.ts: Module-level `let _tray: Tray | null = null` (GC pinning). createTray(deps: TrayDeps): TrayHandle with deps = { getMainWindow, dbHolder, scheduler, connected, ipc, navigate, logger, platform? }. Win click -> show(); Mac click -> popUpContextMenu(). Returns { setBadge, clearBadge, rebuildMenu, dispose }. setBadge Win -> setImage(badged); Mac -> setTitle('*'). clearBadge inverse.

    lifecycle/pendingCatchup.ts: type CatchupChannel = 'briefing' | 'insights' | 'recap' | 'learning' | 'entitlement' | 'gmail-sync' | 'calendar-sync' | 'knowledge-folder-sweep'. Module-singleton with add/has/drain/clear backed by Set.

    lifecycle/onUnlock.ts: registerOnUnlock(cb): () => void returns unsubscribe. fireOnUnlock(db, logger) iterates callbacks; per-callback try/catch with logger.warn on throw; never rethrows.

    Tests cover: icon path-per-platform; menu 6-item count + disable logic; createTray installs platform-correct click handler; setBadge/clearBadge image swap; rebuildMenu; dispose; onUnlock callback ordering + throw isolation; pendingCatchup add idempotent / drain / has.
  </behavior>
  <action>
    Create all 5 source files per RESEARCH Patterns 1-3 verbatim (with TS types). Create all 5 test files. Mock `electron` via vi.mock with app/Tray/Menu/nativeImage stubs. Make index.ts use process.platform directly but accept an optional `platform` override for tests.

    Register a catchup callback inside createTray (or at bootstrap-time in 12-01-edited index.ts - Task 2 below): callback receives db, drains pendingCatchup, runs one job per channel via the channel-runner registry. Single-shot per Decision 1.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/tray tests/unit/main/lifecycle --passWithNoTests 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    All 5 source files exist with exports matching must_haves.artifacts. All 5 test files pass. Module-level `let _tray` present (grep verifies). No app.dock.hide, openAsHidden, nativeImage.composite anywhere in src/main.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire tray creation into bootstrap + fireOnUnlock into onboarding + single-instance guard spec</name>
  <files>
    src/main/index.ts
    src/main/ipc/onboarding.ts
    tests/unit/main/ipc/onboarding-fireOnUnlock.spec.ts
    tests/unit/main/tray/single-instance-tray.spec.ts
  </files>
  <read_first>
    - src/main/index.ts - bootstrap shape (after 12-01 edits: appIsQuitting, registerBackgroundHandlers, decideCloseAction)
    - src/main/ipc/onboarding.ts (lines 45-67) - DbHolder + holder.db = db site
    - src/main/single-instance.ts - acquireSingleInstanceLock contract
  </read_first>
  <behavior>
    - In bootstrap(), INSIDE the acquireSingleInstanceLock true-branch and AFTER registerBackgroundHandlers, call createTray({...}) and hold the handle at module scope.
    - Register the catchup-drain callback exactly once at bootstrap: registerOnUnlock(async (db) => { drain pendingCatchup, run each channel once via runChannelOnce, then tray.clearBadge() + tray.rebuildMenu() }).
    - In src/main/ipc/onboarding.ts IMMEDIATELY after `holder.db = db;` at L53, add `void fireOnUnlock(db, logger);`.
    - Add a second-instance listener: on app.on('second-instance', ...), raise the existing main window. Tray is NOT recreated.
    - Add `app.on('before-quit', () => tray?.dispose())` BEFORE the existing stopKnowledgeFolderLifecycle line. Keep both calls.
    - rebuildMenu trigger: also call tray.rebuildMenu() from BG_SET_PREFS handler if provider-connection state could have changed.
    - Test in onboarding-fireOnUnlock.spec.ts: mock fireOnUnlock; assert it is called with the db AFTER holder.db is set (sequence assertion); registered callbacks fire and drain pendingCatchup.
    - Test in single-instance-tray.spec.ts: mock acquireSingleInstanceLock to return true on first call and false on the second call. Run the lock-gated bootstrap entrypoint twice (extract a small runLockGatedBootstrap() helper from index.ts if needed). Assert createTray (mocked) was called EXACTLY ONCE across both invocations. A regression that hoisted tray construction above the lock check would surface as createTray=2 and fail this test.
  </behavior>
  <action>
    Edit src/main/index.ts: import createTray + registerOnUnlock + pendingCatchup. Module-level _trayHandle. Inside bootstrap (post-lock, post-registerBackgroundHandlers) construct _trayHandle. Register catchup drain via registerOnUnlock. Define runChannelOnce(chan, db, logger) switching on the 8 channels. Extend before-quit: appIsQuitting=true; _trayHandle?.dispose(); void stopKnowledgeFolderLifecycle(getLogger()).

    Edit src/main/ipc/onboarding.ts: import fireOnUnlock. After holder.db = db; add `void fireOnUnlock(db, logger);`.

    Create tests/unit/main/ipc/onboarding-fireOnUnlock.spec.ts: register a callback via registerOnUnlock; simulate unlock; assert callback fires with the right db AFTER holder.db assignment.

    Create tests/unit/main/tray/single-instance-tray.spec.ts: vi.mock single-instance.js (acquireSingleInstanceLock sequenced returns: true, false) AND vi.mock tray/index.js (createTray spy). Invoke the lock-gated bootstrap factory twice. Assert createTray spy was called exactly 1 time; second invocation short-circuits before tray construction.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/main/ipc/onboarding-fireOnUnlock.spec.ts tests/unit/main/tray/single-instance-tray.spec.ts --passWithNoTests 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    bootstrap constructs tray INSIDE single-instance-lock branch. registerOnUnlock + pendingCatchup drain wired. fireOnUnlock(db, logger) called immediately after holder.db = db. before-quit disposes tray. onboarding-fireOnUnlock.spec.ts passes. single-instance-tray.spec.ts proves createTray-called-at-most-once across two simulated bootstrap runs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sealed-DB cron guards (8 callsites) + static-grep ratchets (seal-guard + no-bare-cron-schedule)</name>
  <files>
    src/main/briefing/schedule.ts
    src/main/insights/schedule.ts
    src/main/recap/schedule.ts
    src/main/learning/schedule.ts
    src/main/entitlement/schedule.ts
    src/main/ipc/gmail.ts
    src/main/ipc/calendar.ts
    src/main/folder-ingestion/lifecycle.ts
    tests/static/cron-seal-guard.spec.ts
    tests/static/no-bare-cron-schedule.spec.ts
    tests/unit/main/briefing/schedule-sealed-skip.spec.ts
  </files>
  <read_first>
    - src/main/briefing/schedule.ts (lines 60-100) - existing cron callback shape + dbHolder access
    - All 7 other callsite files (open each, identify the cron callback)
    - tests/static/single-entitlement-gate-site.test.ts - existing static-grep ratchet pattern (precedent)
    - .planning/phases/12-background-activity-tray-and-autolaunch/12-RESEARCH.md Pattern 2
    - src/main/lifecycle/scheduler.ts - cronRegistry.set callsite shape
  </read_first>
  <behavior>
    For each of the 8 cron callsites, the cron callback MUST begin with the seal-guard prelude:
        const db = dbHolder.db;
        if (!db) { pendingCatchup.add('<channel>'); trayBus.setBadge(); return; }
    BEFORE any other DB access. The <channel> literal must match the CatchupChannel union.

    Tray ref injection: export a trayBus module from src/main/tray/index.ts (or new src/main/tray/bus.ts) with mutable setBadge/clearBadge no-op-when-uninitialised functions; createTray installs the real impls.

    Static ratchet (cron-seal-guard.spec.ts): for each of the 8 callsite files, locate the cron callback block via regex `cronImpl\.schedule\([^,]+,\s*async\s*\([^)]*\)\s*=>\s*\{`. Within the first 6 lines of the callback body, assert presence of BOTH (a) `const db = dbHolder.db` or equivalent dbHolder.db reference in if-condition AND (b) `pendingCatchup.add(` with the expected channel literal. Fail with explicit file-naming message. Also assert NO line before the if-guard contains `dbHolder.db.prepare` or `db.prepare`. Hardcoded (file -> expected channel literal) table in the spec.

    Static ratchet (no-bare-cron-schedule.spec.ts): scans the repo for callsites matching `cron\.schedule\(|cronImpl\.schedule\(|require\(['\"]node-cron`. For each match, asserts the enclosing function ALSO calls `scheduler.cronRegistry.set` (search forward up to ~30 lines within the same file scope, OR the match lives inside one of the existing scheduler helpers in src/main/lifecycle/scheduler.ts that itself calls cronRegistry.set). Maintains an allowlist of test files (`tests/**`) plus `src/main/lifecycle/scheduler.ts` (the registration helper itself). Prevents future cron tasks bypassing the lifecycle plumbing.

    Unit smoke (briefing/schedule-sealed-skip.spec.ts): mock dbHolder.db = null, fire the cron callback once, assert pendingCatchup.has('briefing') and trayBus.setBadge spy was called, and that no DB read attempt occurred.
  </behavior>
  <action>
    1. Export a trayBus singleton from src/main/tray/index.ts (or new src/main/tray/bus.ts) with mutable setBadge/clearBadge slots; createTray installs the real impls on construction.
    2. Open each of the 8 files in turn. Locate the cron callback. Prepend the seal-guard block per the behavior section. Use the channel literals: 'briefing' / 'insights' / 'recap' / 'learning' / 'entitlement' / 'gmail-sync' / 'calendar-sync' / 'knowledge-folder-sweep'.
    3. Verify no DB access (db.prepare, db.transaction, db.exec) happens before the guard. If existing code reads dbHolder.db twice, refactor to a single `const db = dbHolder.db; if (!db) { ... return; }` at the top.
    4. Create tests/static/cron-seal-guard.spec.ts following the pattern of single-entitlement-gate-site.test.ts. Use fs.readFileSync. Hardcoded callsite-file -> channel-literal table. Regex-extract the cron callback body and assert guard presence within the first 6 lines.
    5. Create tests/static/no-bare-cron-schedule.spec.ts: walk src/ via a recursive fs walker (skip node_modules/dist/out/build), regex-match the three cron-schedule patterns. For each match, verify the enclosing function also contains `scheduler.cronRegistry.set` (or the file is the registration helper itself). Allowlist: tests/**, src/main/lifecycle/scheduler.ts.
    6. Create tests/unit/main/briefing/schedule-sealed-skip.spec.ts - dbHolder.db=null mock, invoke the briefing cron callback, assert pendingCatchup.has('briefing') AND trayBus.setBadge() spy was called.
    7. DO NOT register new cron tasks outside scheduler.cronRegistry / registerLifecycleCallbacks. powerMonitor lifecycle must remain unchanged.
  </action>
  <verify>
    <automated>npx vitest run tests/static/cron-seal-guard.spec.ts tests/static/no-bare-cron-schedule.spec.ts tests/unit/main/briefing/schedule-sealed-skip.spec.ts --passWithNoTests 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    All 8 cron callsites carry the seal-guard prelude. cron-seal-guard ratchet passes. no-bare-cron-schedule ratchet passes (every cron.schedule callsite routes through scheduler.cronRegistry). Unit smoke for briefing sealed-skip passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT - Windows tray + sealed-DB badge + catchup drain</name>
  <what-built>
    Tray icon on Windows, rich menu, sealed-DB cron skip with gold badge, first-unlock catchup drain. Mac UAT deferred to a future mac-hardware session.
  </what-built>
  <how-to-verify>
    Build and launch Aria on Windows:
    1. `npm run dev` (or packaged: `npm run build:win && open dist/win-unpacked/Aria.exe`)
    2. Don't unlock yet - confirm tray icon appears in system tray. Right-click -> 6 menu items visible. "Generate briefing now" greyed/disabled with tooltip "Unlock Aria first". Sync now submenu items also disabled.
    3. Wait through one briefing cron tick. Tray icon should show the gold badge overlay variant.
    4. Click tray "Show Aria" -> window appears. Unlock vault.
    5. After unlock, tray badge clears (catchup drain ran). Check log for scope:'onUnlock' and scope:'catchup' entries.
    6. Close window via X. Window disappears. Tray icon remains. Right-click tray -> "Show Aria" -> window returns with state intact.
    7. Right-click tray -> "Quit Aria" -> app fully exits.
    8. Single-instance: launch Aria.exe a second time while running -> second invocation exits cleanly, no second tray icon appears, existing window raises.
    9. Toggle Settings -> Behaviour -> "Keep Aria running when I close" OFF -> click X -> app should now QUIT (not hide).

    Mark macOS UAT as deferred in 12-02-SUMMARY.md.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tray menu click -> IPC | Tray menu items invoke main-process IPC handlers; same trust as renderer IPC |
| Catchup-on-unlock callback -> DB writes | Callbacks run with elevated unlocked-DB access; must not run for any channel not previously skipped |
| Tray icon asset load -> filesystem | loadTrayIcon reads from process.resourcesPath (packaged) or build/ (dev); paths are hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | Tray menu fires DB write while sealed | mitigate | Menu items with DB side-effects are `enabled: false` when dbHolder.db === null; Quit Aria is db-null-safe |
| T-12-06 | Elevation | Pre-construction trayBus.setBadge() call | mitigate | trayBus exports no-op setBadge/clearBadge until createTray installs real impls |
| T-12-07 | Denial of Service | Catchup queue grows unbounded | accept | pendingCatchup keyed by channel (max 8 entries); single-shot drain |
| T-12-08 | Tampering | Second-instance fires duplicate cron registrations | mitigate | Bootstrap only runs after acquireSingleInstanceLock returns true; single-instance-tray.spec.ts enforces createTray-once invariant |
| T-12-09 | Information Disclosure | Tray icon asset path traversal | accept | Paths are hardcoded constants in icons.ts |
</threat_model>

<verification>
1. `npx vitest run tests/unit/main/tray tests/unit/main/lifecycle tests/static/cron-seal-guard.spec.ts tests/static/no-bare-cron-schedule.spec.ts tests/unit/main/briefing/schedule-sealed-skip.spec.ts tests/unit/main/ipc/onboarding-fireOnUnlock.spec.ts --passWithNoTests` passes
2. phase12-bans static ratchet from 12-01 still green (no new occurrences of app.dock.hide / openAsHidden / nativeImage.composite)
3. `grep -n 'let _tray' src/main/tray/index.ts` returns one match (GC pin verified)
4. `npx tsc --noEmit` produces no new errors
5. Both static ratchets (cron-seal-guard + no-bare-cron-schedule) pass
6. single-instance-tray.spec.ts proves createTray-called-at-most-once
7. Manual UAT on Windows (Task 4) approved; macOS deferred
</verification>

<success_criteria>
- Tray icon visible on Windows; menu has 6 items with correct enable-state logic
- All 8 cron callsites carry the seal-guard prelude (static ratchet enforces)
- no-bare-cron-schedule ratchet enforces every cron.schedule routes through scheduler.cronRegistry
- single-instance-tray spec passes (createTray exactly once across two bootstrap invocations)
- fireOnUnlock(db, logger) called immediately after holder.db = db in onboarding.ts
- Catchup drains single-shot per channel; tray.clearBadge fires after drain
- Module-level `let _tray` for GC pinning
- Tray creation inside acquireSingleInstanceLock branch
- before-quit disposes tray AND still calls stopKnowledgeFolderLifecycle
- Manual UAT (Win) approved
</success_criteria>

<risks>
- electron-icon-builder pipeline. Fallback to imagemagick (Task 0 action).
- Tray asset packaging. extraResources must bundle build/tray-* into the packaged app.
- trayBus pre-construction calls. Cron may register BEFORE bootstrap constructs tray; trayBus must be safe to call before createTray (no-op defaults).
- Single-instance race on Windows fast-user-switching - inherits existing acquireSingleInstanceLock contract.
- macOS UAT deferred. Tracked as explicit deferral in 12-02-SUMMARY.md.

## Rollback

- Revert Task 3 commits first (cron guards) - most invasive across the codebase.
- Then revert Task 2 (bootstrap wiring + fireOnUnlock).
- Then revert Task 1 (tray + lifecycle modules).
- Task 0 (assets) can stay or be reverted independently.
</risks>

<output>
After completion, create `.planning/phases/12-background-activity-tray-and-autolaunch/12-02-SUMMARY.md` with:
- Files created/modified
- Tray asset generation path (electron-icon-builder vs hand-authored)
- Manual UAT result on Windows
- Explicit deferral note: "macOS UAT deferred to mac-hardware session"
- Verification that both static ratchets (cron-seal-guard, no-bare-cron-schedule) cover their invariants
- single-instance-tray.spec.ts result
</output>
