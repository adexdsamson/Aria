---
phase: 07-rag-q-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db/migrations/126_rag_index.sql
  - src/main/db/migrations/embedded.ts
  - src/main/rag/chunk-types.ts
  - src/main/rag/chunk-text.ts
  - src/main/rag/source-harvesters.ts
  - src/main/rag/chunk-strategies.ts
  - src/main/rag/chunking-spike.ts
  - tests/unit/main/db/migrations-126-rag.spec.ts
  - tests/unit/main/rag/chunk-text.test.ts
  - tests/unit/main/rag/source-harvesters.test.ts
  - tests/unit/main/rag/chunk-strategies.test.ts
  - tests/unit/main/rag/chunking-spike.test.ts
autonomous: false
requirements: [RAG-01, RAG-05]
requirements_addressed: [RAG-01, RAG-05]
tags: [rag, indexing, chunking, fts5, spike, local-first]
---

<objective>
Build the Phase 7 indexing foundation and run the required chunking strategy spike so downstream embedding and retrieval work has a stable chunk schema, source-harvesting boundary, and documented chunking decision.
</objective>

<context>
Read:
- `.planning/phases/07-rag-q-a/07-CONTEXT.md`
- `.planning/REQUIREMENTS.md` RAG-01..05
- `src/main/db/migrations/runner.ts`
- `src/main/db/migrations/embedded.ts`
- Phase 2/5 source schemas for mail and calendar entities
- Phase 6 note/transcript/action schemas
</context>

<constraints>
- Embeddings are always local; do not add frontier embedding support.
- Keep raw source text out of logs.
- Use stable source references: `sourceKind`, `sourceId`, `providerKey`, `accountId`, `charStart`, `charEnd`.
- The chunking spike must run deterministically against fixtures or local sample data and write a decision record to `07-SPIKE-CHUNKING.md`.
- If sqlite-vec extension loading is uncertain on Windows/SQLCipher, record the compatibility risk for wave 2 instead of blocking this schema/chunking wave.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 126 RAG index schema</name>
  <behavior>
    - Adds `rag_chunk`, `rag_embedding`, `rag_index_state`, and SQLite FTS5 storage for chunk text.
    - `rag_chunk.source_kind` supports `email`, `event`, `note`, and `action`.
    - Chunk rows include source identity, provider/account scope, parent reference, optional speaker hint, char offsets, token count, dirty flag, and timestamps.
    - Embedding rows are versioned by `model_id`, `dim`, and `embedded_at`.
    - Deletes can remove chunks, embeddings, and FTS rows for a source without orphaning rows.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/db/migrations-126-rag.spec.ts --reporter=dot</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Text cleanup and chunk primitives</name>
  <behavior>
    - Adds shared chunk types and deterministic token-count approximation.
    - Normalizes whitespace while preserving citation offsets against the stored source text.
    - Adds email cleanup for quoted replies and common signatures before chunking.
    - Adds helpers to map cleaned text spans back to original char offsets where practical.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/chunk-text.test.ts --reporter=dot</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Source harvesters for v1 corpus</name>
  <behavior>
    - Harvests email bodies from Gmail and Outlook canonical mail rows.
    - Harvests calendar event titles/descriptions.
    - Harvests meeting notes, transcript segments, summaries when present, and extracted meeting actions.
    - Harvests Todoist-backed task descriptions when locally stored.
    - Produces normalized source documents without writing chunks yet.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/source-harvesters.test.ts --reporter=dot</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Compare three chunking strategies</name>
  <behavior>
    - Implements strategy A: per-message/per-event/per-note-section chunks.
    - Implements strategy B: per-thread or full-note rolled chunks with truncation.
    - Implements strategy C: hybrid token windows around 512 tokens with about 64-token overlap, respecting message/turn boundaries when possible.
    - Ensures every chunk has valid source offsets and parent references.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/chunk-strategies.test.ts --reporter=dot</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Chunking spike decision record</name>
  <behavior>
    - Adds a deterministic spike runner that compares A/B/C using labeled fixture questions.
    - Reports recall@10 and MRR for each strategy.
    - Estimates chunk counts and storage growth for the winning strategy.
    - Writes `.planning/phases/07-rag-q-a/07-SPIKE-CHUNKING.md` with the winner, evidence, and downstream assumptions.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/chunking-spike.test.ts --reporter=dot</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
- `node node_modules/vitest/vitest.mjs run tests/unit/main/db/migrations-126-rag.spec.ts tests/unit/main/rag/chunk-text.test.ts tests/unit/main/rag/source-harvesters.test.ts tests/unit/main/rag/chunk-strategies.test.ts tests/unit/main/rag/chunking-spike.test.ts --reporter=dot`
- `node node_modules/typescript/bin/tsc -p tsconfig.node.json --noEmit`
</verification>

<done>
Create `07-01-SUMMARY.md` with migration version, supported source corpus, chosen chunking strategy, spike metrics, storage estimate, and any sqlite-vec compatibility risks carried into wave 2.
</done>
