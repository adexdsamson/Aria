---
phase: 07-rag-q-a
plan: 02
type: execute
wave: 2
depends_on: [07-01-PLAN.md]
files_modified:
  - package.json
  - src/main/rag/vector-store.ts
  - src/main/rag/sqlite-vec-loader.ts
  - src/main/rag/ollama-embeddings.ts
  - src/main/rag/index-writer.ts
  - src/main/rag/index-worker.ts
  - src/main/rag/reindex-scheduler.ts
  - src/main/rag/model-swap-reconciler.ts
  - src/main/rag/backfill.ts
  - src/main/rag/people-directory.ts
  - src/main/rag/sensitivity-cache.ts
  - src/main/ipc/rag.ts
  - src/main/ipc/index.ts
  - src/main/index.ts
  - src/shared/ipc-contract.ts
  - src/renderer/features/settings/RagIndexSection.tsx
  - src/renderer/features/settings/SettingsScreen.tsx
  - tests/integration/rag/sqlite-vec-load.spec.ts
  - tests/integration/rag/ollama-roundtrip.test.ts
  - tests/integration/rag/brute-force-90k-bench.test.ts
  - tests/unit/main/rag/vector-store.test.ts
  - tests/unit/main/rag/ollama-embeddings.test.ts
  - tests/unit/main/rag/index-writer.test.ts
  - tests/unit/main/rag/index-worker.test.ts
  - tests/unit/main/rag/reindex-scheduler.test.ts
  - tests/unit/main/rag/model-swap-reconciler.test.ts
  - tests/unit/main/rag/backfill.test.ts
  - tests/unit/main/rag/people-directory.test.ts
  - tests/unit/main/rag/sensitivity-cache.test.ts
  - tests/unit/renderer/features/settings/RagIndexSection.spec.tsx
autonomous: false
requirements: [RAG-01, RAG-04, RAG-05]
requirements_addressed: [RAG-01, RAG-04, RAG-05]
research_refs: [§1, §2, §3, §6, §11, §12, §13, §15, Pitfall-1, Pitfall-2, Pitfall-4, Pitfall-5]
migration_numbering: { previous: 126, this_plan: none, next_available: 127 }
tags: [rag, embeddings, ollama, sqlite-vec, vector-store, fallback, backfill, reindex, people-directory, boot-reconciler, sensitivity-cache]
review_concerns_addressed: [C2, C3, C5, C10, C11, C13]
---

<objective>
Bring the index online: ship the VectorStore interface with both `SqliteVecStore` and `BruteForceStore` (each writes to its own store — C11 single-write), with a live load probe deciding which is active and the brute-force fallback benchmarked at the realistic 90k-chunk corpus size (C2 — not 10k); the Ollama `/api/embed` client with a MANDATORY live roundtrip (C13 — no silent skip); transactional index writer that populates the cached sensitivity classification at index time (C5); background embedding worker; incremental + model-swap reindex scheduler PLUS a boot-time `reconcileModelSwap` step that recovers from crashes in the flip window (C3); opt-in backfill for existing rows; and people-directory build piggy-backing on the mail cron with incremental updates on new-mail arrival (C10). Settings → RAG Index UI exposes status and is wired through `SettingsScreen.tsx` (L-04-04 reachability).
</objective>

<context>
Read:
- `.planning/phases/07-rag-q-a/REVIEWS.md` (C2/C3/C5/C10/C11/C13 — addressed in this plan)
- `.planning/phases/07-rag-q-a/07-RESEARCH.md` (§1 vec spike, §2 Ollama /api/embed, §3 backfill, §6 FTS5+RRF, §11 disconnect, §12 model swap, §13 cross-phase, §15 eval; Pitfalls 1/2/4/5; Assumptions A1/A2/A6)
- `.planning/phases/07-rag-q-a/07-CONTEXT.md` (re-index dual-mode triggers; versioning; rebuild — note: corrected by plan 07-01 Task 0.5)
- `.planning/phases/07-rag-q-a/07-01-SUMMARY.md` (migration 126 applied incl. C7 columns; chunking-strategy winner + chunk-size/overlap config)
- `.planning/phases/07-rag-q-a/07-SPIKE-CHUNKING.md` (winning strategy parameters)
- `src/main/rag/chunk-types.ts`, `src/main/rag/source-harvesters.ts`, `src/main/rag/chunk-strategies.ts` (Wave 1)
- `src/main/llm/providers.ts` (DEFAULT_OLLAMA_BASE_URL; L-04-02 fix)
- `src/main/llm/router.ts` (Phase 3 sensitivity classifier — `classify()` reused by sensitivity-cache Task)
- `src/main/single-instance.ts` (Phase 5 — model-swap state machine depends on this lock; C3)
- `src/main/briefing/schedule.ts` (mail cron pattern to piggyback for people-directory rebuild)
- `src/renderer/features/settings/SettingsScreen.tsx` (mount RagIndexSection here — reachability)
</context>

<interfaces>
<!-- Wave 2 OWNS these; Wave 3 consumes them. -->

From `src/main/rag/vector-store.ts` (NEW — Task 1):
```typescript
export interface Hit { chunkId: string; score: number; }

export interface VectorStore {
  /** Upsert a vector tagged with the model that produced it. */
  upsert(chunkId: string, vector: Float32Array, modelId: string): void;
  /** k-NN search over the CURRENTLY ACTIVE modelId (read from rag_index_state). Pitfall 4. */
  query(queryVector: Float32Array, k: number): Hit[];
  /** Cascade-aware delete of a chunk's embedding rows across all modelIds. */
  deleteByChunkId(chunkId: string): void;
  /** Sweep job after atomic flip — remove rows for any non-active modelId. */
  deleteByModelId(modelId: string): void;
  /** Diagnostic. */
  backendName(): 'sqlite-vec' | 'fallback';
}

export function getVectorStore(db: Database, opts?: { force?: 'sqlite-vec' | 'fallback' }): VectorStore;
```

From `src/main/rag/ollama-embeddings.ts` (NEW — Task 2):
```typescript
export interface EmbedClient {
  embed(inputs: string[]): Promise<Float32Array[]>;
  modelId: string;
  dim: number;
}
export function createEmbedClient(opts?: { baseUrl?: string; modelId?: string; batchSize?: number; }): EmbedClient;
```

From `src/main/rag/index-writer.ts` (NEW — Task 3):
```typescript
export interface IndexWriter {
  upsertSource(doc: SourceDoc): { inserted: number; deleted: number; dirtied: number; classified: number; };
  deleteSource(sourceKind: SourceKind, sourceId: string): { deletedChunks: number; };
}
```

From `src/main/rag/sensitivity-cache.ts` (NEW — Task 3.5):
```typescript
export type SensitivityClass =
  | 'none'
  | 'pii:low' | 'pii:med' | 'pii:high'
  | 'hr:low' | 'hr:med' | 'hr:high'
  | 'legal:low' | 'legal:med' | 'legal:high'
  | 'financial:low' | 'financial:med' | 'financial:high';

/** Classify text once at index time; cached on `rag_chunk.sensitivity`. Reads cache first; only calls Phase 3 classifier on miss or model-mismatch. */
export function classifyChunkSensitivity(db: Db, chunk: { id: string; text: string }, classifierModelId: string): Promise<SensitivityClass>;

/** Bulk variant used by IndexWriter — single transaction, sequential classifier calls bounded by p-queue. */
export function classifyChunksBulk(db: Db, chunks: Array<{ id: string; text: string }>, classifierModelId: string): Promise<Map<string, SensitivityClass>>;
```

From `src/main/rag/model-swap-reconciler.ts` (NEW — Task 5.5 / C3):
```typescript
export function reconcileModelSwap(db: Db, logger: Logger): Promise<{
  recovered: 'completed-flip' | 'resumed-drain' | 'ambiguous-noop' | 'noop';
  detail?: string;
}>;
```
</interfaces>

<constraints>
- Embeddings via local Ollama POST `${DEFAULT_OLLAMA_BASE_URL}/embed` — endpoint is `/api/embed` (NOT legacy `/api/embeddings`). Pitfall 1.
- The Ollama roundtrip integration test is MANDATORY (C13). The test MUST either run live (when `OLLAMA_AVAILABLE=1`) OR fail with the clear message "set OLLAMA_AVAILABLE=1 to run this required test" — silent `skipIf` is forbidden. CI sets the var; local dev gets the helpful error. The test also writes `tests/fixtures/rag/ollama-roundtrip-evidence.json` containing `{ timestamp, modelId, dim, l2norm, latency_ms }` — done block greps for the file's existence + freshness (≤24h).
- `VectorStore.query` MUST filter by `rag_index_state.active_model_id` — Pitfall 4; no caller may pass modelId in for "convenience".
- Renderer must never embed; embeddings happen in main only.
- Worker pauses on sleep (`powerMonitor`) and resumes without a cron storm (Phase 2 pattern).
- Backfill is opt-in — never auto-saturates Ollama on first launch (Pitfall 5 + RESEARCH §3).
- `RagIndexSection.tsx` MUST be imported by `SettingsScreen.tsx` (L-04-04 — verifier reachability check).
- Source delete cascades chunks → embeddings (FK ON DELETE CASCADE in migration 126) → FTS5 rows (via triggers from §6).
- C11 single-store rule: `SqliteVecStore` writes vectors ONLY to the `vec0` virtual table. `BruteForceStore` writes vectors ONLY to `rag_embedding.vector` BLOB. No dual-write. Backend choice is sticky in `rag_index_state.vector_backend`; admin-initiated swap reuses the model-swap rebuild path.
- C3 invariant: `reconcileModelSwap(db, logger)` MUST be called at startup AFTER `openDb` + migrations and BEFORE `IndexWorker.start`. Model-swap depends on `app.requestSingleInstanceLock()` already holding (Phase 5) — call this out explicitly in the boot sequence.
- C5 invariant: sensitivity classification runs AT INDEX TIME, not at query time. The query path reads `rag_chunk.sensitivity`; only re-classifies if (a) `sensitivity IS NULL` or (b) `sensitivity_model != current classifier modelId`.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sqlite-vec load probe + VectorStore dual-impl (C11 single-store; RESEARCH §1, Pitfalls 2/4)</name>
  <files>package.json, src/main/rag/sqlite-vec-loader.ts, src/main/rag/vector-store.ts, tests/integration/rag/sqlite-vec-load.spec.ts, tests/unit/main/rag/vector-store.test.ts</files>
  <behavior>
    - `npm i sqlite-vec@0.1.9` (exact pin per RESEARCH §Standard Stack; pre-v1 — no caret).
    - Add `"asarUnpack": ["**/node_modules/sqlite-vec/dist/native/**"]` to `electron-builder` config (Pitfall 2 — pre-emptive). If config block lives outside `package.json`, edit the right file and document.
    - `sqlite-vec-loader.ts`: `tryLoadSqliteVec(db): { ok: true } | { ok: false; reason: string }`. Imports `* as sqliteVec from 'sqlite-vec'`; calls `sqliteVec.load(db)` AFTER SQLCipher open sequence (caller must hold an already-keyed `Db`). Catches and stringifies any throw; never rethrows.
    - Live integration probe `sqlite-vec-load.spec.ts`: opens an encrypted test DB via `openDb()`; calls `tryLoadSqliteVec`; on `ok:true` creates `CREATE VIRTUAL TABLE vec_probe USING vec0(emb float[768])`, inserts 3 known vectors, runs `SELECT ... ORDER BY distance LIMIT 1` KNN, asserts ordering. On `ok:false` skips with the captured reason recorded as a test annotation (so VERIFICATION cycle sees WHY fallback was picked). Updates `rag_index_state.vector_backend` accordingly.
    - `VectorStore` interface in `vector-store.ts` per `<interfaces>` block above.
    - C11 single-store implementations:
      - `SqliteVecStore`: `vec0(chunk_id TEXT PRIMARY KEY, model_id TEXT, emb float[<dim>])` virtual-table layout. `upsert` writes ONLY to `vec0`. `query` reads `active_model_id` + `active_model_dim` from `rag_index_state`, runs `SELECT chunk_id FROM vec0 WHERE model_id = ? AND emb MATCH ? ORDER BY distance LIMIT k`. `deleteByChunkId` / `deleteByModelId` operate ONLY on vec0. `rag_embedding` table receives metadata-only rows (chunk_id, model_id, dim, embedded_at, embedding_norm) WITHOUT the BLOB vector — that is, `vector` column is NULL when backend = 'sqlite-vec'. Document this NULL-vector convention in the file header.
      - `BruteForceStore`: stores vectors ONLY in `rag_embedding.vector` BLOB. `upsert` also computes + writes `embedding_norm` (the cached L2 norm column from migration 126 / C7) so per-query recompute is avoided. `query` reads `SELECT chunk_id, vector, embedding_norm FROM rag_embedding WHERE model_id = ?` with active model, cosine in-memory over `Float32Array.from(blob)` using cached norm.
    - C2 perf target (REVISED — REVIEWS): brute-force fallback p95 ≤ 300ms over a synthetic **90k-chunk** corpus on the dev machine (matches RESEARCH §2 realistic v1 estimate, NOT the original 10k). Pure-JS brute force at 90k × 768 dims × 4 bytes ≈ 270 MB read + ~70M float ops — feasible only with the L2-norm cache (C7) and a single pre-allocated `Float32Array` scratch buffer reused across queries (no per-query allocation; document the GC-pressure mitigation).
    - C2 chosen fallback strategy (locked here so plan-checker pass 3 sees a concrete pick): **Option D — tiered. Default: pure-JS brute force with norm-cache + scratch buffer (covers the 90k corpus at ≤300ms p95 per benchmark in Task 1.5). Hard cap at 250k chunks: if `count(rag_chunk where deleted_at IS NULL) > 250000`, surface a `StatusPanel` banner "Vector index unavailable; brute-force fallback is at capacity. Consider reinstalling Aria from a build that ships sqlite-vec native binaries." and refuse new embeddings until the user either swaps backend (model-swap path) or shrinks the index via the soft-delete sweep.** Document trade-off vs Options A (SQLite UDF) / B (refuse outright) / C (WASM build of sqlite-vec) in the file header — chose D because it keeps the v1 perf budget intact without shipping a second native binary or a non-deterministic UDF.
    - Pitfall 4 test: seed two model_ids in `rag_embedding` (or two rows in vec0 in the sqlite-vec impl); flip `active_model_id`; assert `query` only returns active-model hits.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/integration/rag/sqlite-vec-load.spec.ts tests/unit/main/rag/vector-store.test.ts --reporter=dot</automated>
  </verify>
  <done>Probe runs and decides backend; SqliteVecStore writes only to vec0; BruteForceStore writes only to rag_embedding.vector with cached `embedding_norm`; both impls pass their unit suite; Pitfall-4 mixed-model test green; `rag_index_state.vector_backend` correctly reflects the decision; 250k-chunk capacity banner exercised by a unit test that stubs the count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.5: Brute-force 90k-chunk benchmark (REVIEWS C2 — perf target verification)</name>
  <files>tests/integration/rag/brute-force-90k-bench.test.ts</files>
  <behavior>
    - Seeds a temp encrypted DB with 90,000 `rag_chunk` rows + 90,000 `rag_embedding` rows (random normalized 768-dim Float32Array blobs, `embedding_norm` populated). One model_id, active. Test fixture is generated programmatically — no committed binary blobs.
    - Times 20 cold-cache queries through `BruteForceStore.query(randomQueryVec, k=50)`. Asserts p95 latency ≤ 300ms on the dev machine. The test records measured p50 / p95 / p99 + memory delta into `tests/fixtures/rag/brute-force-bench-evidence.json` for the SUMMARY.
    - If p95 > 300ms, test fails with the recorded numbers and a remediation hint: "consider enabling sqlite-vec, or escalate to Option C (WASM sqlite-vec) per plan 07-02 Task 1 file header."
    - Gated behind `RAG_BENCH=1` env so normal CI doesn't pay the 90k seed cost on every PR; the gate failure message tells you how to opt in. The plan-checker requires this test exists and is runnable — perf gate is enforced at phase verification, not on every CI run.
  </behavior>
  <verify>
    <automated>RAG_BENCH=1 node node_modules/vitest/vitest.mjs run tests/integration/rag/brute-force-90k-bench.test.ts --reporter=dot</automated>
  </verify>
  <done>90k-chunk benchmark exists and p95 ≤ 300ms; evidence JSON written; if fail, escalation path documented in failure message.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ollama /api/embed client + MANDATORY live roundtrip (RESEARCH §2, REVIEWS C13)</name>
  <files>src/main/rag/ollama-embeddings.ts, tests/unit/main/rag/ollama-embeddings.test.ts, tests/integration/rag/ollama-roundtrip.test.ts</files>
  <behavior>
    - `createEmbedClient({ baseUrl=DEFAULT_OLLAMA_BASE_URL, modelId='nomic-embed-text:v1.5', batchSize=16 })`. Import `DEFAULT_OLLAMA_BASE_URL` from `src/main/llm/providers.ts` — do NOT redefine. Pitfall 1.
    - `embed(inputs)` POSTs `${baseUrl}/embed` body `{ model, input: inputs }` (array form; batch up to `batchSize`; split into multiple requests if larger). Parses `{ embeddings: number[][] }`; returns `Float32Array[]`.
    - Vectors from `/api/embed` are L2-normalized — do NOT re-normalize. Test asserts a sample vector's L2 norm is within `1.0 ± 1e-3`.
    - Timeout per request: 30s; abort signal on timeout; clear error type `OllamaEmbedError` with `{ kind: 'connection_refused' | 'model_not_found' | 'timeout' | 'http' }` for caller dispatch.
    - Unit tests mock fetch and assert: URL is exactly `http://127.0.0.1:11434/api/embed` (URL-equality assertion — L-04-02 echo guard); `model` is exactly `nomic-embed-text:v1.5`; batching splits a 40-input call into 3 requests at `batchSize=16`; never logs `input` array content (capture pino sink, assert no chunk text appears).
    - C13 — MANDATORY live roundtrip `tests/integration/rag/ollama-roundtrip.test.ts`:
      - At top of the `describe` block: `if (!process.env.OLLAMA_AVAILABLE) throw new Error("set OLLAMA_AVAILABLE=1 to run this required test (Phase 7 REVIEWS C13 — no silent skip)");` — this fires at test-collection time, NOT inside a `test.skipIf` that vitest reports as "passed: 0, skipped: 1" with no signal.
      - When the env var is set: POST `["hello", "world"]`, assert response 200, `embeddings.length === 2`, each is 768 floats, L2-norm ≈ 1.0. On success, write `tests/fixtures/rag/ollama-roundtrip-evidence.json` containing `{ timestamp: <ISO>, modelId, dim: 768, l2norm: <number>, latency_ms: <number> }` so the SUMMARY can reference it.
      - CI workflow sets `OLLAMA_AVAILABLE=1` (and is responsible for having Ollama reachable at the time of the run). Local devs without Ollama hit the actionable error and know to start the sidecar.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/ollama-embeddings.test.ts --reporter=dot</automated>
    <automated>OLLAMA_AVAILABLE=1 node node_modules/vitest/vitest.mjs run tests/integration/rag/ollama-roundtrip.test.ts --reporter=dot</automated>
    <automated>node -e "const s=require('node:fs').statSync('tests/fixtures/rag/ollama-roundtrip-evidence.json'); if(Date.now()-s.mtimeMs > 24*3600*1000) { console.error('evidence stale'); process.exit(1) }"</automated>
  </verify>
  <done>URL/model/batch assertions green; live roundtrip green with 768-dim L2-normed vectors; evidence JSON written with timestamp + L2 norm; freshness gate ≤24h passes; no input text in logs; error taxonomy exposed for the worker (Task 4).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Transactional index writer + cascade delete + sensitivity classification at index time (RESEARCH §6 triggers, §14 cascades, REVIEWS C5)</name>
  <files>src/main/rag/index-writer.ts, tests/unit/main/rag/index-writer.test.ts</files>
  <behavior>
    - `upsertSource(doc)` runs the winning chunking strategy from 07-SPIKE-CHUNKING.md against the SourceDoc, then inside a single `db.transaction`:
      1. Delete existing `rag_chunk WHERE source_kind=? AND source_id=?` (FTS5 + embeddings cascade via triggers / FK).
      2. Insert all new chunks with `dirty=1, created_at=now, updated_at=now, title=<from chunk>`.
      3. C5 / sensitivity at index time: after insert, call `classifyChunksBulk(db, newChunks, activeClassifierModelId)` (Task 3.5). Persist results to `rag_chunk.sensitivity`, `sensitivity_model`, `sensitivity_at`. This is part of the same transaction so a chunk row never exists without its sensitivity classification persisted (eventually-consistent OK only on classifier failure — see Task 3.5 fallback).
      4. INSERT INTO `rag_source_dirty` for the chunk source (target_model_id=NULL → embed against active model).
    - Soft-delete vs hard-delete: `deleteSource(kind, id)` deletes chunks; FTS5 cleared by trigger; embeddings cascade via FK; returns `{ deletedChunks: n }`. Audit-history mode is gated on a future flag — for now hard-delete remains the default (C7 `deleted_at` column reserved for future use).
    - Test asserts: shrinking a source from 5 chunks to 3 leaves only 3 in `rag_chunk` and 3 in FTS5; deleting a source removes all chunks AND fires Pitfall-3 trigger so FTS row count goes to 0; deleting a source kills its `rag_embedding` rows. Sensitivity populated on every inserted chunk; re-upsert with a different `activeClassifierModelId` re-classifies and updates `sensitivity_model` + `sensitivity_at`.
    - Never log chunk text on insert/delete paths. Log id + count only.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/index-writer.test.ts --reporter=dot</automated>
  </verify>
  <done>Source upsert/delete is transactional, cascade-correct, and FTS5-consistent across all three triggers. Every newly-inserted chunk carries a non-null `sensitivity` + `sensitivity_model` + `sensitivity_at`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.5: Chunk sensitivity cache (REVIEWS C5 — eliminate N=10 query-time fan-out)</name>
  <files>src/main/rag/sensitivity-cache.ts, tests/unit/main/rag/sensitivity-cache.test.ts</files>
  <behavior>
    - `classifyChunkSensitivity(db, chunk, classifierModelId)`:
      1. Read `SELECT sensitivity, sensitivity_model FROM rag_chunk WHERE id=?`.
      2. If row.sensitivity is non-null AND row.sensitivity_model === classifierModelId → return the cached value (HIT — zero LLM calls).
      3. Else (MISS or model-mismatch): call Phase 3 router's `classify(text)` (REUSE — do not re-implement); UPDATE `rag_chunk SET sensitivity=?, sensitivity_model=?, sensitivity_at=? WHERE id=?`; return the new value.
    - `classifyChunksBulk(db, chunks, classifierModelId)`: same logic, batched. Uses a single transaction for the writes. Sequential classifier calls bounded by the existing classifier p-queue (concurrency=1 to avoid Ollama saturation per Phase 4 OOM lesson).
    - Failure mode: if classifier throws, leave `sensitivity=NULL` in the DB (do NOT poison the cache); the answer-router (plan 07-03) treats NULL as "unknown → force LOCAL route" (fail-closed). Logged + surfaced via RagIndexSection error banner.
    - C5 perf assertion test: pre-populate 10 chunks with cached `sensitivity='none'` and `sensitivity_model='router-v1'`; call `classifyChunksBulk(chunks, 'router-v1')`; assert ZERO calls to the mocked classify function (all cache hits). Then call with `classifierModelId='router-v2'` and assert 10 calls happen (model bump invalidates cache).
    - Classifier `modelId` is sourced from `src/main/llm/router.ts` — expose the current classifier modelId via a getter so cache invalidation is automatic when Phase 3 router upgrades its classifier model.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/sensitivity-cache.test.ts --reporter=dot</automated>
  </verify>
  <done>Cache hit path makes ZERO LLM calls; model-id mismatch triggers re-classification; classifier failure leaves NULL (fail-closed for answer router).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Background embedding worker (RESEARCH §2 batch, §12 active-model)</name>
  <files>src/main/rag/index-worker.ts, tests/unit/main/rag/index-worker.test.ts</files>
  <behavior>
    - `IndexWorker.start(db, embedClient, vectorStore, { concurrency=2, batchSize=16 })`:
      - Drains `rag_source_dirty` in `enqueued_at` order; for each row reads the corresponding chunks (`dirty=1` per source); batches up to 16 texts per embed call.
      - Writes embeddings via `vectorStore.upsert(chunkId, vec, activeModelId)` — read `active_model_id` + `active_model_dim` from `rag_index_state` BEFORE each batch (Pitfall 4; survives a model swap mid-drain by picking up new active model on next iteration).
      - During a model-swap rebuild (when row's `target_model_id IS NOT NULL`): use the `target_model_id` instead of active. Increments `rag_index_state.rebuild_progress_done` atomically (single UPDATE in the same transaction as the embedding insert) — this is the invariant the reconciler (Task 5.5) relies on.
      - On success: `UPDATE rag_chunk SET dirty=0, updated_at=now WHERE id IN (...)`; DELETE the source-dirty row.
      - On failure (`OllamaEmbedError`): increment `attempts`; leave row in queue; exponential backoff; chunks stay `dirty=1` — idempotent restart (Pitfall 5 + Assumption A2 — if returned dim ≠ `active_model_dim`, throw and mark `rag_index_state` with a status banner; do NOT silently store mismatched vectors).
    - Concurrency via existing `p-queue` instance (separate from briefing/scheduling queues).
    - `powerMonitor` integration: on `suspend` → pause queue; on `resume` → resume (no cron storm).
    - Exposes `getProgress(): { dirtyTotal, dirtyDone, perMinute, lastErrorKind?, lastErrorMessage? }` for the settings UI (Task 7).
    - Test: 100 fake dirty rows + MSW-mocked embed client → drains; with a programmable error injection on batch #3, queue retries and eventually completes; dim-mismatch sets banner without polluting DB.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/index-worker.test.ts --reporter=dot</automated>
  </verify>
  <done>Worker drains queue, survives transient Ollama errors idempotently, never stores wrong-dim vectors, increments `rebuild_progress_done` atomically, exposes typed progress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Reindex scheduler — dual-mode triggers + model-swap atomic flip (CONTEXT, RESEARCH §12)</name>
  <files>src/main/rag/reindex-scheduler.ts, tests/unit/main/rag/reindex-scheduler.test.ts</files>
  <behavior>
    - Hook points called from Phase 2/5/6 ingest paths AFTER each canonical-row write:
      - Synchronous (immediate): meeting_note edits, meeting_note_segment edits, recent-mail (received ≤ 7d) edits AND deletes — call `indexWriter.upsertSource` / `deleteSource` inline before returning to caller.
      - Background: older mail / calendar edits — call `markDirty(sourceKind, sourceId)` only; worker drains on the next mail-cron (5min) or calendar-cron (15min) tick.
    - `startModelSwap(newModelId, newDim)`:
      1. `UPDATE rag_index_state SET rebuild_in_progress=1, rebuild_target_model_id=?, rebuild_target_dim=?, rebuild_started_at=now, rebuild_progress_total=(SELECT count(*) FROM rag_chunk WHERE deleted_at IS NULL), rebuild_progress_done=0`.
      2. INSERT every existing chunk's source into `rag_source_dirty` with `target_model_id=<new>`.
      3. Worker drains (Task 4); on each success, increments `rebuild_progress_done`.
      4. **Caller-watched flip-check** (per-tick from the scheduler heartbeat AND on worker queue-empty): when `rebuild_progress_done >= rebuild_progress_total AND rebuild_progress_total > 0`, in a single transaction `UPDATE rag_index_state SET active_model_id=?, active_model_dim=?, rebuild_in_progress=0, rebuild_completed_at=now;` — atomic flip.
      5. Sweep cron tick: `vectorStore.deleteByModelId(oldModelId)` AND `DELETE FROM rag_embedding WHERE model_id=<old>`.
    - The flip in step 4 is the C3 hot window — Task 5.5 owns the boot-time recovery for crashes between done==total and the UPDATE actually executing.
    - Concurrency invariant: `startModelSwap` is guarded by `app.requestSingleInstanceLock()` (Phase 5) — no second Aria instance can race the swap. The scheduler asserts the lock is held at startModelSwap entry and throws otherwise.
    - SC-4 test: seed `rag_embedding` with vectors stamped `old-model` AND `new-model`; query before flip returns old-model vectors only; after flip returns new-model only; sweep removes the old rows.
    - During rebuild, queries continue working (active_model unchanged until flip).
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/reindex-scheduler.test.ts --reporter=dot</automated>
  </verify>
  <done>Sync vs background triggers behave as spec; model-swap state-machine completes atomic flip with no race; single-instance lock asserted at swap entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.5: Boot reconciler — recover model-swap from crash-in-flip-window (REVIEWS C3)</name>
  <files>src/main/rag/model-swap-reconciler.ts, src/main/index.ts, tests/unit/main/rag/model-swap-reconciler.test.ts</files>
  <behavior>
    - `reconcileModelSwap(db, logger)`:
      ```
      Read rag_index_state row 1.
      If (rebuild_in_progress == 1 AND rebuild_target_model_id IS NOT NULL):
        progressDone = SELECT count(*) FROM rag_embedding WHERE model_id = <target_model_id>
        progressTotal = SELECT count(*) FROM rag_chunk WHERE deleted_at IS NULL
        IF (progressDone >= progressTotal AND progressTotal > 0):
          // Case A: crashed AFTER worker drained, BEFORE active_model_id flip.
          BEGIN TRANSACTION;
            UPDATE rag_index_state
               SET active_model_id  = rebuild_target_model_id,
                   active_model_dim = rebuild_target_dim,
                   rebuild_in_progress = 0,
                   rebuild_completed_at = strftime('%Y-%m-%dT%H:%M:%fZ','now'),
                   updated_at = strftime('%Y-%m-%dT%H:%M:%fZ','now');
          COMMIT;
          schedule sweep of <old_model_id> on next tick (do NOT block boot on the sweep);
          logger.info({event:'model-swap-recovery-completed-flip', oldModel, newModel, total: progressTotal});
          return { recovered: 'completed-flip', detail: ... };
        ELSE IF (progressDone < progressTotal):
          // Case B: worker mid-drain — leave state intact, IndexWorker will resume from rag_source_dirty.
          logger.info({event:'model-swap-recovery-resumed-drain', progressDone, progressTotal});
          return { recovered: 'resumed-drain', detail: ... };
        ELSE:
          // Case C: ambiguous — progressTotal==0 yet rebuild_in_progress=1. Operator decision required.
          logger.error({event:'model-swap-recovery-ambiguous', state: <row dump minus secrets>});
          return { recovered: 'ambiguous-noop' };
      ELSE:
        return { recovered: 'noop' };
      ```
    - Called from `src/main/index.ts` boot sequence: AFTER `openDb()` + `runMigrations()`, AFTER `requestSingleInstanceLock()` confirms primary instance, BEFORE `IndexWorker.start()`. Add the call with a comment naming REVIEWS C3.
    - Tests (each test simulates a crash at the boundary, then runs `reconcileModelSwap`, then asserts post-state):
      1. Crash AFTER `done == total` BEFORE UPDATE → reconciler runs Case A → active_model_id flipped, rebuild_in_progress=0, returns `completed-flip`.
      2. Crash with `done == total - 1` (worker mid-drain) → Case B → no state change, returns `resumed-drain`, IndexWorker on subsequent start drains the last source.
      3. Crash with `done == 0` and a queue full of dirty rows → Case B same as above.
      4. Edge case: `rebuild_in_progress=1` but `rebuild_progress_total=0` (corrupt state) → Case C → logged error, no-op, return `ambiguous-noop`.
      5. Happy path: no rebuild in flight → returns `noop` immediately.
    - Test 1 also asserts the sweep of the OLD modelId is SCHEDULED (not blocking) — the test mocks the sweep scheduler and asserts a call was queued.
    - Same architectural pattern as Phase 5 single-instance + boot reconciliation. Cite Phase 5 C4 lesson in the file header.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/model-swap-reconciler.test.ts --reporter=dot</automated>
  </verify>
  <done>Reconciler runs at boot before IndexWorker.start; all four crash-boundary tests + happy-path test green; ambiguous case logs without touching state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Opt-in backfill for existing rows (RESEARCH §3 / Pitfall 5)</name>
  <files>src/main/rag/backfill.ts, tests/unit/main/rag/backfill.test.ts, src/shared/ipc-contract.ts, src/main/ipc/rag.ts</files>
  <behavior>
    - `seedBackfill(db, { batchSize=500 }): { enqueuedBySourceKind: Record<SourceKind,number> }`:
      - For each source kind: `SELECT id FROM <canonical_table>` paginated; for each batch, in a single transaction INSERT INTO `rag_source_dirty(source_kind, source_id, target_model_id=NULL, enqueued_at)` SELECT … FROM that batch. Skip rows already present in `rag_chunk` (resumable on relaunch).
      - Throttle: `setTimeout(0)` between batches; `powerMonitor.isOnBatteryPower()` → space batches 5s apart (RESEARCH §3 conservative default).
      - Persists state in `app_meta` key `rag_backfill_state` ∈ `pending | in_progress | done | skipped`. On launch, if `in_progress` resume; if `pending` show prompt; if `done`/`skipped` do nothing.
    - ETA probe (RESEARCH §3 / REVIEWS C9): before enqueuing the bulk, embed a 50-chunk probe through `embedClient.embed()`, measure wall-clock, store `app_meta.rag_backfill_eta_seconds_per_chunk`. The UI modal renders `eta = chunks_remaining × seconds_per_chunk` (live-updating, not a one-shot literal). Worst-case clamp surfaces "≥1h estimated — consider running overnight."
    - IPC `ragBackfillStatus(): { state, enqueuedBySourceKind, dirtyRemaining, etaSecondsRemaining }` and `ragBackfillStart()` / `ragBackfillSkip()`.
    - Test: 1200-row email table → backfill enqueues 1200 in 3 batches with throttling; mid-run kill + restart resumes from where it stopped (no double-enqueue thanks to PRIMARY KEY on `rag_source_dirty(source_kind, source_id, target_model_id)`); ETA probe stored in `app_meta` and exposed via status IPC.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/backfill.test.ts --reporter=dot</automated>
  </verify>
  <done>Backfill enqueues canonical rows in throttled batches, is resumable, persists state + measured ETA in `app_meta`, exposes IPC for the UI prompt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Settings → RAG Index UI + reachability (L-04-04 / M3 error copy)</name>
  <files>src/renderer/features/settings/RagIndexSection.tsx, src/renderer/features/settings/SettingsScreen.tsx, tests/unit/renderer/features/settings/RagIndexSection.spec.tsx</files>
  <behavior>
    - `RagIndexSection.tsx`: shows backend (`sqlite-vec` / `fallback`), active model id + dim, indexing progress (`X / Y chunks embedded`), rebuild state if `rebuild_in_progress=1`, estimated storage (chunks × 4 KB).
    - Live-updating backfill ETA derived from `ragBackfillStatus().etaSecondsRemaining`.
    - First-launch-post-upgrade modal OR first /ask visit modal (gated by `rag_backfill_state='pending'`): copy from RESEARCH §3 — "Aria can build a search index over your existing mail, calendar, and meeting notes. Roughly N MB, ~M minutes (live estimate). [Build now] [Later]"; "Later" sets `skipped` (revisitable from this section); "Build now" calls `ragBackfillStart()`.
    - "Wipe RAG data for disconnected accounts" button per RESEARCH §11: enumerates disconnected accounts (existing Phase 5 chip pattern), shows `count(*)` from `rag_chunk WHERE provider_key=? AND account_id=?`, button calls `ragWipeAccountData(providerKey, accountId)` IPC.
    - Error copy (L-04-03): when Ollama is down, show distinct red banner "Aria couldn't reach the local model — please check Ollama is running" — DO NOT reuse the refusal copy. Surface `lastErrorMessage` in dev console only.
    - Brute-force capacity banner (C2 Option D): when `vector_backend='fallback' AND alive_chunk_count > 200000`, surface a yellow warning "Fallback index approaching capacity ({n}/250k chunks). Consider reinstalling Aria from a build that ships sqlite-vec." When > 250000, red banner + indexing pause (referencing Task 1 capacity behavior).
    - `SettingsScreen.tsx`: import + render `<RagIndexSection/>` between existing `<OllamaSection/>` and `<DiagnosticsSection/>` — fixes L-04-04 reachability gap.
    - Reachability grep test in this task's vitest spec: programmatic file read of `SettingsScreen.tsx` containing `import { RagIndexSection }` AND `<RagIndexSection` JSX usage. Fails build if section is added without being mounted.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/renderer/features/settings/RagIndexSection.spec.tsx --reporter=dot</automated>
    <automated>node_modules/.bin/rg -nF "RagIndexSection" src/renderer/features/settings/SettingsScreen.tsx | grep -v '^#' | grep -c "RagIndexSection" | awk '{ exit ($1 >= 2) ? 0 : 1 }'</automated>
  </verify>
  <done>RagIndexSection renders all status fields incl. live ETA + capacity banner; backfill prompt + wipe button wired; SettingsScreen imports and mounts it (grep gate green); error vs refusal copies distinct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: People directory — build + alias map + incremental rebuild (RAG-04 prep, REVIEWS C10 / RESEARCH §13)</name>
  <files>src/main/rag/people-directory.ts, tests/unit/main/rag/people-directory.test.ts, tests/fixtures/rag/people-directory-10.json</files>
  <behavior>
    - `rebuildPeopleDirectory(db, { since? })`:
      - Reads from `gmail_message` + `outlook_message` headers (`from_addr`, `to_addr`, `cc_addr`), `calendar_event.attendees` JSON column, `meeting_note_segment.speaker` field.
      - Canonical email is the lowercased email portion; display name is the most-seen "Name" variant in headers for that email; aliases captured for every distinct `displayname`/`shortname`/`email` variant observed.
      - Upserts into `person` (RESEARCH §14 schema) and `person_alias`. `observed_count` and `seen_count` increment; `first_seen_at` / `last_seen_at` updated.
    - REVIEWS C10 freshness — TWO update paths:
      1. **Cron piggyback** (existing): every Nth mail cron tick (N=12 → roughly hourly when mail cron is 5min) calls `rebuildPeopleDirectory({since: lastRun})`. Stores `last_people_directory_rebuild_at` in `app_meta`.
      2. **Incremental on-arrival** (C10): when a new mail row is INSERTed by the ingest path, call `upsertPersonFromHeaders(db, msgRow)` synchronously. This is a cheap single-header parse (no LLM, no joins beyond the row itself) so it does not block ingest. Aliases not in the directory at ingest time become resolvable at query time within seconds, not within ~1 hour.
    - `resolvePersonMention(db, mention)` query-time helper returns `{ confident: Person } | { ambiguous: Person[] } | { none }`. Ordering for ambiguous candidates: `ORDER BY last_seen_at DESC, observed_count DESC LIMIT 5` (deterministic; documented in this task's summary). LLM-fallback for ambiguous cases is implemented in plan 07-03 — Task 8 here just exposes the disambiguation shape.
    - Query-time staleness signal: if `last_people_directory_rebuild_at` is older than 24h, expose a `directoryStale: true` flag on `resolvePersonMention` return so plan 07-03's UI can surface a "people directory rebuilding…" indicator in `RagAnswerResult.routing.reason`. Queries still work against the (potentially stale) snapshot.
    - SC-3 prep: fixture `tests/fixtures/rag/people-directory-10.json` — 7 unambiguous + 3 ambiguous cases. Test asserts: directory build over the fixture corpus produces ≥9/10 correct top-1 matches via `resolvePersonMention`.
    - C10 test: insert a new gmail_message row with a brand-new sender; assert `upsertPersonFromHeaders` ran inline (mocked spy) and `resolvePersonMention(newSenderName)` returns `{ confident }` in the same test tick (not after waiting an hour for cron).
    - Logging hygiene: never log full email body / segment text; log `messageId, count` only.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/rag/people-directory.test.ts --reporter=dot</automated>
  </verify>
  <done>Directory builds idempotently from mail+calendar+notes AND incrementally on new-mail arrival (C10); alias map populated; 10-case eval ≥9/10 top-1 (unambig); ambiguous returns surface candidate list ordered by last_seen_at DESC, observed_count DESC; `directoryStale` flag exposed for 07-03 UI.</done>
</task>

</tasks>

<verification>
- `node node_modules/vitest/vitest.mjs run tests/unit/main/rag tests/integration/rag/sqlite-vec-load.spec.ts --reporter=dot` (all unit + non-live integration)
- `OLLAMA_AVAILABLE=1 node node_modules/vitest/vitest.mjs run tests/integration/rag/ollama-roundtrip.test.ts --reporter=dot` (MANDATORY live per C13; record outcome in 07-02-SUMMARY; evidence file referenced)
- `RAG_BENCH=1 node node_modules/vitest/vitest.mjs run tests/integration/rag/brute-force-90k-bench.test.ts --reporter=dot` (90k brute-force perf gate per C2; record p50/p95/p99 in summary)
- `node node_modules/typescript/bin/tsc -p tsconfig.node.json --noEmit`
- `node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit`
- Grep gates:
  - `node_modules/.bin/rg -n "api/embeddings" src/main` returns NOTHING (Pitfall 1 — legacy endpoint must not appear).
  - `node_modules/.bin/rg -n "RagIndexSection" src/renderer/features/settings/SettingsScreen.tsx | grep -v '^#'` returns ≥2 lines (import + JSX mount; L-04-04 reachability).
  - `node_modules/.bin/rg -n "reconcileModelSwap" src/main/index.ts` returns ≥1 line (C3 — boot reconciler wired).
  - `node_modules/.bin/rg -n "console\\.log.*chunk\\.text|logger.*chunk\\.text|input:.*chunk\\.text" src/main/rag` returns NOTHING.
- Live verification (recorded in summary): start Ollama; run a 50-row backfill on the dev DB; observe RagIndexSection progress count tick; trigger a model swap to `nomic-embed-text:v1.5` (no-op identity) and verify atomic flip leaves `rag_embedding` consistent; simulate a kill at the flip window and confirm next boot's `reconcileModelSwap` recovers.
</verification>

<success_criteria>
- VectorStore live behind a probe; both backends are SINGLE-store (C11); Pitfall-4 mixed-model guard green.
- Brute-force fallback p95 ≤ 300ms at 90k chunks (C2); 250k capacity banner exercised.
- Embedding pipeline talks to `/api/embed` (verified by URL-equality assertion AND a mandatory live 768-dim L2-norm roundtrip with evidence JSON ≤24h fresh — C13).
- Index writer cascades correctly; FTS5 stays consistent across all three triggers; every chunk row carries a non-null cached `sensitivity` populated at index time (C5).
- Sensitivity-cache hit path: ZERO LLM calls when classifier modelId is unchanged.
- Worker drains dirty queue idempotently across restarts and Ollama-down windows; increments `rebuild_progress_done` atomically.
- Reindex scheduler runs both sync (transcripts / notes / recent mail) and background paths; model swap completes atomic flip with no mixed-model leakage.
- `reconcileModelSwap` at boot recovers all four crash boundaries (C3 — completed-flip / resumed-drain / ambiguous-noop / noop).
- Backfill is opt-in, throttled, resumable, with state + measured ETA in `app_meta`.
- `RagIndexSection` mounted in `SettingsScreen` (reachability grep gate green); capacity banner wired.
- People directory ≥9/10 unambiguous; ambiguous candidate list ordered by last_seen_at DESC, observed_count DESC; new-mail incremental upsert path exercised (C10).
</success_criteria>

<done>
Create `07-02-SUMMARY.md` with: vector backend decision + probe outcome (with reason if fallback), 90k brute-force benchmark p50/p95/p99 from `brute-force-bench-evidence.json`, live Ollama roundtrip evidence path + timestamp + L2-norm value, worker progress metrics across a sample drain, reindex sync/bg behavior table, model-swap atomic-flip evidence (before/after row counts per model), reconciler-test verdict for all four crash boundaries (C3), backfill resume + ETA evidence, sensitivity-cache zero-LLM-hit demonstration (C5), people-directory 10-case eval result + new-mail incremental upsert verdict (C10), and `SettingsScreen.tsx` reachability grep output.
</done>
