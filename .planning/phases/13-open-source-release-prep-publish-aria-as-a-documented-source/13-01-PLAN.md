---
phase: 13-open-source-release-prep-publish-aria-as-a-documented-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-01-FINDINGS.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "No secrets (Ed25519 private keys, Stripe sk_/whsec_, Cloudflare tokens, GH_TOKEN, APPLE_ID/APPLE_APP_SPECIFIC_PASSWORD/APPLE_TEAM_ID, Google/MS OAuth client secrets) appear in any git commit across full history"
    - "User has reviewed the .planning/ sensitivity flagged list and signed off on each item (keep or redact)"
    - "Stray catalog_*.json + build_catalog.js root files have a decided disposition (.gitignored or deleted)"
    - "The tracked .claude/launch.json file has a decided disposition (keep tracked, add to .gitignore, or delete)"
    - "A short findings report exists in the phase directory summarising scan results and all decisions"
  artifacts:
    - path: ".planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-01-FINDINGS.md"
      provides: "Pre-publish safety report for user sign-off"
      contains: "## Git History Scan"
  key_links:
    - from: "13-01-FINDINGS.md"
      to: "user sign-off"
      via: "checkpoint:human-verify gate"
      pattern: "FINDINGS\\.md"
---

<objective>
Run the pre-publish safety pass and produce a findings report. This is the HARD GATE for Phase 13 — nothing downstream (README, docs, metadata) ships until the user reviews and signs off on this report.

Purpose: Going public is irreversible for anything in git history. A secret committed once is exposed permanently. This task scans every commit for sensitive material and reviews .planning/ for any content that reads badly in public.

Output: 13-01-FINDINGS.md with scan results, .planning/ flagged items, stray-file disposition, and recommendations. The checkpoint blocks execution until the user signs off.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-CONTEXT.md
@docs/superpowers/specs/2026-06-02-open-source-prep-design.md
@.gitignore
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full git-history secret scan and .planning sensitivity review</name>
  <files>
    .planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-01-FINDINGS.md
  </files>
  <read_first>
    - .planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-CONTEXT.md (Pre-Publish Safety Pass section — exact secret categories)
    - docs/superpowers/specs/2026-06-02-open-source-prep-design.md (Phase 1 section — tooling + deliverable spec)
    - .env.example (confirms which env vars are expected — none should appear with real values in history)
    - .gitignore (confirms .env.* ignore — already verified clean, document this finding)
  </read_first>
  <action>
    Run a full git-history secret scan using the following approach in order of preference:

    1. Check if gitleaks is available: run "gitleaks version" — if available, run "gitleaks detect --source . --log-opts='--all'" to scan all branches and full history.
    2. If gitleaks is unavailable, perform a structured regex sweep: run "git log --all -p --diff-filter=A" and pipe through a grep for each pattern class:
       - Ed25519 private key headers: "BEGIN.*PRIVATE" / "-----BEGIN"
       - Stripe secrets: "sk_live_[A-Za-z0-9]" / "sk_test_[A-Za-z0-9]" / "whsec_[A-Za-z0-9]"
       - Cloudflare tokens: "Bearer [A-Za-z0-9_-]{40}" patterns
       - GH_TOKEN / GITHUB_TOKEN with a non-empty value assignment
       - APPLE_ID= with an email-like value
       - APPLE_APP_SPECIFIC_PASSWORD= / APPLE_TEAM_ID= with non-empty values
       - GOOGLE_OAUTH_CLIENT_SECRET= / MICROSOFT_OAUTH_CLIENT_SECRET= with non-empty values
       - Generic high-entropy strings (40+ char hex or base64 not in a test fixture)

    Separately, run a .planning/ content review: enumerate all .planning/ files with "git ls-files .planning/" — scan filenames and spot-check content for: real personal names (other than the developer's own), customer/company names that appear to be real clients, internal business metrics or financial data, any content that would be embarrassing or legally sensitive if public. Produce a flagged list (file path + reason) for each suspicious item.

    Stray-file audit: identify that catalog_raw.json, catalog_clean.json, catalog_final.json, and build_catalog.js are untracked root files (confirm with "git status --short"). Determine their content nature (are they autogenerated build artifacts? do they contain sensitive data?). Check if .claude/launch.json is the only tracked file under .claude/ with "git ls-files .claude/". Note .claude/settings.local.json and .claude/worktrees/ are untracked.

    Write 13-01-FINDINGS.md with these sections:
    - "## Git History Scan" — tool used, date, result (CLEAN / FINDINGS). If findings, list each: commit hash, file, matched pattern, recommendation (rotate + accept vs filter-repo scrub).
    - "## .gitignore Verification" — confirm .env.* ignore + !.env.example; confirm .env.local never committed.
    - "## .planning/ Sensitivity Review" — total files counted, flagged items (path + reason + recommended disposition: keep-as-is / redact / note-as-internal). If no flags, state "No sensitive content found."
    - "## Stray Files at Root" — list catalog_*.json + build_catalog.js, their nature, recommended disposition (add to .gitignore / delete).
    - "## Tracked .claude/ Files" — list .claude/launch.json, note it contains only VS Code debug launch config (no secrets), recommended disposition (keep tracked / add to .gitignore).
    - "## Summary" — overall verdict: READY TO PROCEED or BLOCKERS FOUND. List any blockers with remediation options.

    Do NOT make any git history modifications. Do NOT delete or gitignore files in this task — only report findings. Actions will follow after user sign-off.
  </action>
  <verify>
    <automated>
      Test-Path C:\Users\HomePC\Documents\GitHub\Aria\.planning\phases\13-open-source-release-prep-publish-aria-as-a-documented-source\13-01-FINDINGS.md
    </automated>
    Check: 13-01-FINDINGS.md contains "## Git History Scan", "## .planning/ Sensitivity Review", "## Stray Files at Root", "## Summary"
  </verify>
  <done>
    13-01-FINDINGS.md exists with all required sections. Git history scan completed (tool documented). .planning/ sensitivity review produced a keep/redact disposition for every flagged item (or "No sensitive content found"). Stray catalog files and .claude/launch.json have a documented recommended disposition.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 produced 13-01-FINDINGS.md — a complete pre-publish safety report covering: (a) full git-history secret scan result, (b) .gitignore verification, (c) .planning/ sensitivity review with flagged items, (d) stray root-file disposition recommendation, (e) .claude/launch.json disposition recommendation, and (f) a READY TO PROCEED / BLOCKERS FOUND verdict.
  </what-built>
  <how-to-verify>
    1. Read .planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-01-FINDINGS.md in full.
    2. Review the "## Git History Scan" result. If FINDINGS (not CLEAN): choose between (A) git filter-repo/BFG history rewrite to remove the secret, or (B) rotate the affected key and accept the exposure. State your choice so Claude can execute it.
    3. Review the "## .planning/ Sensitivity Review" flagged list. For each item, confirm: keep-as-is, redact before publish, or add a note. If no items flagged, confirm "No action needed."
    4. Review the "## Stray Files at Root" section. Confirm: add catalog_*.json + build_catalog.js to .gitignore, or delete them from the working tree.
    5. Review "## Tracked .claude/ Files". Confirm: keep .claude/launch.json tracked (it is a VS Code launch config, not sensitive), or add it to .gitignore.
    6. Overall: confirm you are satisfied the repo is safe to make public, OR list any remaining concerns.
  </how-to-verify>
  <resume-signal>
    Reply "APPROVED — ready to proceed" (plus any remediation choices from step 2-5 above), or describe any issues to fix before proceeding. Claude will action the .gitignore additions and any other discrete file changes from this sign-off before moving to Wave 2.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply stray-file disposition and .gitignore updates per user sign-off</name>
  <files>
    .gitignore
  </files>
  <read_first>
    - .gitignore (current content — must append without breaking existing rules)
    - .planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-01-FINDINGS.md (confirmed user sign-off decisions)
  </read_first>
  <action>
    Based on user sign-off from the checkpoint, apply the agreed dispositions. The default dispositions are:

    1. If user confirmed .gitignore for catalog_*.json + build_catalog.js: append to .gitignore under a "# Scratch / build catalog files" comment: "catalog_*.json" and "build_catalog.js". Do NOT delete these files from the working tree (user keeps them locally).

    2. If user confirmed keeping .claude/launch.json tracked: no change needed. If user confirmed adding it to .gitignore: append "!.claude/launch.json" inversion is NOT needed — instead, since .claude/ is not currently gitignored, add ".claude/settings.local.json" and ".claude/worktrees/" as new gitignore entries to ensure those sensitive/ephemeral paths stay untracked. Do NOT add .claude/launch.json itself to .gitignore unless user explicitly requested it.

    3. If user chose history remediation (filter-repo or key rotation), document the decision as a comment in 13-01-FINDINGS.md under "## Remediation Taken". Actual history rewrite is a separate human-action step — note the exact command to run.

    Only modify .gitignore. No other files in this task. Keep all existing .gitignore rules intact; append new entries at the bottom under a new comment block.
  </action>
  <verify>
    <automated>
      Select-String -Path C:\Users\HomePC\Documents\GitHub\Aria\.gitignore -Pattern "catalog" -Quiet
    </automated>
    Check: .gitignore now includes the agreed entries without removing existing rules.
  </verify>
  <done>
    .gitignore updated per sign-off. The repo is confirmed safe-to-publish (or remediation path is documented). Wave 2 plans (docs authoring) are now unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history → public GitHub | Any secret ever committed becomes permanently public once the repo flips to public |
| .planning/ content → public readers | Planning notes written for private dev context become publicly readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | git history | mitigate | Scan full history for secrets before publish; rotate any found or run filter-repo |
| T-13-02 | Information Disclosure | .planning/ 213 files | mitigate | Sensitivity review produces flagged list; user signs off on each item |
| T-13-03 | Information Disclosure | .env.example | accept | Already verified: .env.* gitignored, !.env.example whitelist, no real values in source |
| T-13-04 | Information Disclosure | .claude/launch.json | accept | Contains only VS Code debug config; no credentials or tokens |
| T-13-05 | Information Disclosure | catalog_*.json root files | mitigate | Gitignore or delete before publish; review content for any embedded credentials |
</threat_model>

<verification>
After plan completion:
- Run: git log --all -p | grep -E "(sk_live|sk_test|whsec_|BEGIN PRIVATE|APPLE_APP_SPECIFIC|GH_TOKEN=.)" | head -20 — expect 0 matches
- Confirm: 13-01-FINDINGS.md exists and contains "## Summary"
- Confirm: .gitignore contains "catalog_*.json" (or user chose deletion path)
- Confirm: checkpoint was approved by user before Wave 2 started
</verification>

<success_criteria>
Pre-publish safety report produced. No secrets in git history (or remediation chosen and documented). .planning/ sensitivity reviewed and user-signed-off. Stray catalog files and .claude/settings.local.json / .claude/worktrees/ have agreed disposition in .gitignore. Repo is confirmed safe to flip to public.
</success_criteria>

<output>
After completion, create .planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-01-SUMMARY.md
</output>
