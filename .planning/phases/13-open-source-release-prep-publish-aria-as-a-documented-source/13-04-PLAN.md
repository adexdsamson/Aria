---
phase: 13-open-source-release-prep-publish-aria-as-a-documented-source
plan: 04
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.md
  - .github/ISSUE_TEMPLATE/feature_request.md
  - .github/PULL_REQUEST_TEMPLATE.md
  - package.json
  - AGENTS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "package.json carries license, repository, homepage, author, and bugs fields"
    - "package.json still has private: true (desktop app — not an npm package)"
    - "GitHub issue and PR templates exist under .github/"
    - "AGENTS.md references Claude Code consistently (not a mix of Codex and Claude)"
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug_report.md"
      provides: "Structured bug report template"
      contains: "Steps to Reproduce"
    - path: ".github/ISSUE_TEMPLATE/feature_request.md"
      provides: "Feature request template"
      contains: "feature_request"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR template"
      contains: "## Description"
    - path: "package.json"
      provides: "Updated package metadata"
      contains: "\"license\": \"MIT\""
  key_links:
    - from: "package.json"
      to: "LICENSE"
      via: "license field"
      pattern: "\"license\""
    - from: "package.json"
      to: "GitHub repository URL"
      via: "repository field"
      pattern: "\"repository\""
---

<objective>
Add GitHub community scaffolding (.github/ issue and PR templates) and update package.json with the required OSS metadata fields. Also tidy AGENTS.md to remove the stray "Codex" references that make it inconsistent.

Purpose: GitHub shows a "Community Standards" checklist for public repos — issue templates and a PR template fulfill the contribution-flow items. Package.json metadata (license, repository, homepage, author, bugs) is what tooling (GitHub's license detection, npm audit, etc.) reads. These are small but visible signals of a well-maintained project.

Output: Three new .github/ template files, updated package.json, and a tidied AGENTS.md.
</objective>

<execution_context>
@C:\Users\HomePC\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\HomePC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-CONTEXT.md
@docs/superpowers/specs/2026-06-02-open-source-prep-design.md
@package.json
@AGENTS.md
@.planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github issue templates and PR template</name>
  <files>
    .github/ISSUE_TEMPLATE/bug_report.md
    .github/ISSUE_TEMPLATE/feature_request.md
    .github/PULL_REQUEST_TEMPLATE.md
  </files>
  <read_first>
    - docs/superpowers/specs/2026-06-02-open-source-prep-design.md (Phase 3 GitHub Scaffolding section — confirms minimal, showcase-appropriate)
    - .planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-CONTEXT.md (GitHub Scaffolding decision)
    - .github/workflows/build.yml (confirm the directory structure is already established — do not touch workflows)
  </read_first>
  <action>
    Create the .github/ISSUE_TEMPLATE/ directory (it does not exist yet) and write three template files.

    .github/ISSUE_TEMPLATE/bug_report.md:
    GitHub issue template YAML front matter:
      name: Bug report
      about: Report a problem with Aria
      labels: bug
      assignees: ''
    Body sections (use markdown headings and checkbox lists where helpful):
    - "## Description" — short description of the bug
    - "## Steps to Reproduce" — numbered list placeholder
    - "## Expected Behavior"
    - "## Actual Behavior"
    - "## Environment" — fields: OS (Windows 11 / macOS version), Electron version (run Help → About or check package.json devDependencies.electron), Node version (node --version), pnpm version, whether Ollama is running, which LLM providers are configured (Anthropic / OpenAI / Google AI)
    - "## Logs" — "Attach pino log file from the app's userData directory (Help → Show Logs or check Electron userData path for aria.log)."
    - "## Additional Context"

    .github/ISSUE_TEMPLATE/feature_request.md:
    YAML front matter:
      name: Feature request
      about: Suggest an improvement or new capability
      labels: enhancement
      assignees: ''
    Body sections:
    - "## Summary" — one-line description
    - "## Problem / Motivation" — what problem does this solve?
    - "## Proposed Solution" — how you imagine it working
    - "## Alternatives Considered"
    - "## Additional Context" — links, screenshots, examples
    Note at bottom: "Please note this is a solo-developer showcase project. Feature requests are tracked but not all will be implemented."

    .github/PULL_REQUEST_TEMPLATE.md:
    (No YAML front matter for PR template — it is a plain markdown file)
    Sections:
    - "## Description" — what does this PR change and why?
    - "## Type of Change" — checkboxes: [ ] Bug fix, [ ] Feature, [ ] Documentation, [ ] Refactoring, [ ] Other
    - "## Testing" — "Describe the tests you ran. Run `pnpm run test:unit` and `pnpm run typecheck` before submitting. Paste the output or confirm they pass."
    - "## Checklist" — checkboxes:
      - [ ] pnpm run typecheck passes
      - [ ] pnpm run lint:guard passes
      - [ ] pnpm run test:unit passes
      - [ ] No secrets or credentials introduced
      - [ ] Changes are limited in scope (no unrelated refactoring mixed in)
    Keep the PR template under 30 lines — it should not be burdensome.
  </action>
  <verify>
    <automated>
      (Test-Path C:\Users\HomePC\Documents\GitHub\Aria\.github\ISSUE_TEMPLATE\bug_report.md) -and (Test-Path C:\Users\HomePC\Documents\GitHub\Aria\.github\ISSUE_TEMPLATE\feature_request.md) -and (Test-Path C:\Users\HomePC\Documents\GitHub\Aria\.github\PULL_REQUEST_TEMPLATE.md)
    </automated>
    Check: bug_report.md contains "Steps to Reproduce". feature_request.md contains "feature_request" in front matter. PULL_REQUEST_TEMPLATE.md contains "## Description" and "pnpm run typecheck".
  </verify>
  <done>
    Three files created: .github/ISSUE_TEMPLATE/bug_report.md (with OS/Electron/Node/Ollama environment fields and log attachment guidance), .github/ISSUE_TEMPLATE/feature_request.md (with solo-maintainer note), .github/PULL_REQUEST_TEMPLATE.md (with 5-item checklist including typecheck + lint:guard).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add package.json OSS metadata and tidy AGENTS.md</name>
  <files>
    package.json
    AGENTS.md
  </files>
  <read_first>
    - package.json (read current content in full — must preserve ALL existing fields exactly; only ADD new fields)
    - AGENTS.md (read in full — identify every occurrence of "Codex" that should be "Claude Code"; confirm the GSD workflow sections are correct)
    - .planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-CONTEXT.md (Metadata Cleanup section — exact fields to add; private:true must stay)
  </read_first>
  <action>
    Update package.json by adding the following fields after the existing "description" field. Preserve all other fields exactly — only add, do not remove or reorder anything.

    Fields to add (use sensible placeholder values for URL-based fields since the actual GitHub URL is not confirmed — use "https://github.com/mainlandtech/aria" as the placeholder for all GitHub URLs):
    - "license": "MIT"
    - "author": "Mainland Tech <ai@mainlandtech.com>"
    - "homepage": "https://github.com/mainlandtech/aria#readme"
    - "repository": { "type": "git", "url": "https://github.com/mainlandtech/aria.git" }
    - "bugs": { "url": "https://github.com/mainlandtech/aria/issues" }

    Confirm that "private": true remains present after the edit. Do NOT change the existing "name", "version", "description", "main", "scripts", "dependencies", "devDependencies", or "build" fields in any way.

    Update AGENTS.md: the file contains stray references to "Codex" from when GSD generated it with a different profile. Replace:
    - "Solo dev + Codex" → "Solo dev + Claude Code" (in the Constraints section)
    - "fastest Claude Code pairing" → leave as-is (this is already correct)
    - "Codex pairing" in the UI section → "Claude Code pairing"
    - "generate-Codex-profile" in the Developer Profile section → "generate-claude-profile"
    - ".Codex/skills/" in the Project Skills section → ".claude/skills/"

    Check every occurrence of "Codex" in AGENTS.md and replace with "Claude Code" where it refers to the AI agent. Do NOT replace "Codex" where it is part of a rejected alternative or third-party tool name (there are none in this file, but be careful). After the edit, "Codex" should not appear anywhere in AGENTS.md.
  </action>
  <verify>
    <automated>
      (Select-String -Path C:\Users\HomePC\Documents\GitHub\Aria\package.json -Pattern '"license": "MIT"' -Quiet) -and (Select-String -Path C:\Users\HomePC\Documents\GitHub\Aria\package.json -Pattern '"private": true' -Quiet) -and (Select-String -Path C:\Users\HomePC\Documents\GitHub\Aria\package.json -Pattern '"repository"' -Quiet)
    </automated>
    Check: package.json contains "license": "MIT", "private": true, "repository", "author", "homepage", "bugs". AGENTS.md no longer contains "Codex" (run: Select-String -Path AGENTS.md -Pattern "Codex" — expect 0 matches).
  </verify>
  <done>
    package.json has new fields: license (MIT), author (Mainland Tech), homepage, repository (git URL), bugs (issues URL). private: true preserved. All existing fields untouched. AGENTS.md has no remaining "Codex" references — all replaced with "Claude Code" or ".claude/" as appropriate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → public readers + tooling | License field is read by GitHub's license detection and npm audit; must be accurate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Repudiation | package.json private:true | mitigate | Verify private:true remains after edit to prevent accidental npm publish |
| T-13-12 | Information Disclosure | PR template checklist | accept | Checklist reminds contributors to run lint:guard — catches credential patterns via grep-no-fixture-leak.mjs |
</threat_model>

<verification>
After plan completion:
- All three .github/ template files exist
- package.json has "license": "MIT", "repository", "homepage", "author", "bugs" — AND still has "private": true
- AGENTS.md: Select-String -Path AGENTS.md -Pattern "Codex" returns 0 matches
- .github/workflows/build.yml is unchanged (not touched by this plan)
</verification>

<success_criteria>
GitHub community scaffolding in place (.github/ISSUE_TEMPLATE/ × 2, PULL_REQUEST_TEMPLATE.md). package.json carries MIT license + repo/homepage/author/bugs metadata, private: true preserved. AGENTS.md is consistent — no stray "Codex" references remain.
</success_criteria>

<output>
After completion, create .planning/phases/13-open-source-release-prep-publish-aria-as-a-documented-source/13-04-SUMMARY.md
</output>
