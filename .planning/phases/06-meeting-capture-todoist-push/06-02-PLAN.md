---
phase: 06-meeting-capture-todoist-push
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/main/db/migrations/124_meeting_extraction_approvals.sql
  - src/main/db/migrations/embedded.ts
  - src/main/transcripts/extract.ts
  - src/main/transcripts/citations.ts
  - src/main/transcripts/chunk.ts
  - src/main/transcripts/dedupe-actions.ts
  - src/main/approvals/persist.ts
  - src/main/approvals/state.ts
  - src/main/ipc/transcripts.ts
  - src/shared/ipc-contract.ts
  - src/renderer/features/meetings/NoteReviewScreen.tsx
  - src/renderer/features/meetings/CitationHighlighter.tsx
  - src/renderer/features/approvals/ApprovalCard.tsx
  - tests/unit/main/transcripts/extract.test.ts
  - tests/unit/main/transcripts/citations.test.ts
  - tests/unit/main/transcripts/chunk.test.ts
  - tests/unit/main/transcripts/dedupe-actions.test.ts
  - tests/unit/main/db/migrations-124.spec.ts
  - tests/unit/renderer/features/meetings/NoteReviewScreen.test.tsx
  - tests/unit/renderer/features/approvals/ApprovalCard-task-batch.test.tsx
autonomous: false
requirements: [MEET-02, MEET-03, MEET-04, MEET-05]
requirements_addressed: [MEET-02, MEET-03, MEET-04]
tags: [meetings, extraction, approval-queue, citations, llm-routing]
---

<objective>
Extract cited meeting summaries and action items from notes, persist editable review rows, and route a per-note task-batch approval card through the existing Approval Queue. Todoist push is still deferred to Plan 06-03.
</objective>

<context>
Read:
- `06-01-SUMMARY.md`
- `06-CONTEXT.md`
- `06-RESEARCH.md`
- `src/main/llm/router.ts`
- `src/main/llm/sensitivityClassifier.ts`
- `src/main/approvals/persist.ts`
- `src/renderer/features/approvals/ApprovalCard.tsx`
</context>

<constraints>
- Use migration filename/version `124_meeting_extraction_approvals.sql`.
- Every summary/action/follow-up/open-question item must carry a citation `{ start, end }`.
- Citation spans must refer to `meeting_note.normalized_text` character offsets.
- Transcript extraction calls must never log transcript text.
- Task-batch approvals must not push to Todoist yet; they only transition extracted actions to approved-for-push.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 124 extraction + task-batch approval schema</name>
  <behavior>
    - Creates `meeting_summary`, `meeting_summary_item`, and `meeting_action`.
    - Adds approval kind `task_batch` while preserving existing approval rows.
    - Adds nullable `meeting_note_id` to approval rows.
    - Adds indexes for note, approval, due date, and push status.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/db/migrations-124.spec.ts --reporter=dot</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chunking + extraction schema</name>
  <behavior>
    - `chunkTranscriptForExtraction` chunks long normalized text with overlap and offset preservation.
    - `extractMeetingArtifacts` uses `generateObject` + Zod schema from context.
    - Local/frontier routing uses existing sensitivity classifier and router; medium/high sensitive transcript chunks use local route.
    - Due hints resolve relative to linked event date or note ingest date.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/transcripts/chunk.test.ts tests/unit/main/transcripts/extract.test.ts --reporter=dot</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Citation validation + action dedupe</name>
  <behavior>
    - Rejects/generated-fixes invalid citation spans before persistence.
    - Dedupe repeated actions across chunks by normalized text similarity and overlapping citation ranges.
    - Owner inference supports `self`, `follow-up`, and `unassigned`.
    - `unassigned` actions are not pushable until edited.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/main/transcripts/citations.test.ts tests/unit/main/transcripts/dedupe-actions.test.ts --reporter=dot</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Note review UI + citation highlighting</name>
  <behavior>
    - Note review screen shows 5 sections: Topics, Decisions, Actions, Follow-ups, Open Questions.
    - Clicking any citation scrolls/highlights the transcript span.
    - Actions are editable: text, owner, follow-up-with, due, priority.
    - Individual actions can be rejected before approval.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/renderer/features/meetings/NoteReviewScreen.test.tsx --reporter=dot</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Task-batch approval card</name>
  <behavior>
    - ApprovalCard branches on `kind === 'task_batch'`.
    - Card shows all extracted actions, owners, due hints, priority, and citation buttons.
    - Approve marks selected actions `approved`; reject marks selected actions `rejected`.
    - No Todoist network call exists in this plan.
  </behavior>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/unit/renderer/features/approvals/ApprovalCard-task-batch.test.tsx --reporter=dot</automated>
  </verify>
</task>

</tasks>

<verification>
Run:
- `node node_modules/vitest/vitest.mjs run tests/unit/main/transcripts/ tests/unit/renderer/features/meetings/ tests/unit/renderer/features/approvals/ApprovalCard-task-batch.test.tsx tests/unit/main/db/migrations-124.spec.ts --reporter=dot`
- Static grep: no Todoist push/send call from extraction path.
- Typechecks, accepting only known baseline errors if unchanged.
</verification>

<done>
Create `06-02-SUMMARY.md` with extraction route evidence, citation validation coverage, and Approval Queue behavior.
</done>
