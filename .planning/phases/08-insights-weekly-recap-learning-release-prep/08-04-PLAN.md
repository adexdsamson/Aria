---
phase: 08-insights-weekly-recap-learning-release-prep
plan: 04
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .planning/REQUIREMENTS.md
  - src/main/release/updater.ts
  - src/main/release/updater.test.ts
  - src/main/release/backup-hook.ts
  - src/main/release/backup-hook.test.ts
  - src/main/release/verify-migration.ts
  - src/main/release/verify-migration.integration.test.ts
  - src/main/db/migrations/runner.ts
  - src/main/ipc/updater.ts
  - src/main/ipc/index.ts
  - src/shared/ipc-contract.ts
  - src/preload/index.ts
  - src/renderer/features/settings/sections/UpdatesSection.tsx
  - src/renderer/features/settings/sections/UpdatesSection.test.tsx
  - src/renderer/features/settings/SettingsScreen.tsx
  - src/main/rag/answer-llm.ts
  - src/main/rag/answer-llm.test.ts
  - src/main/ipc/index.ts
  - src/main/ipc/rag.ts
  - tests/integration/rag-ask-smoke.spec.ts
  - tests/integration/phase8-happy-path.spec.ts
  - docs/RELEASE-RUNBOOK.md
  - build/entitlements.mac.plist
  - .env.example
autonomous: false
user_setup:
  - service: github-releases
    why: "electron-builder --publish github writes latest.yml + installer to GH Releases; tester + GA streams use prerelease flag"
    env_vars:
      - name: GH_TOKEN
        source: "GitHub → Settings → Developer settings → Personal access tokens (classic) → scope: `repo`"
    dashboard_config:
      - task: "Verify repo is public OR token has repo scope"
        location: "GitHub repo settings"
  - service: apple-developer-id
    why: "macOS notarization at v1 release per CONTEXT (XCUT-05 unchanged for macOS)"
    env_vars:
      - name: APPLE_ID
        source: "Apple ID email used for Developer Program"
      - name: APPLE_APP_SPECIFIC_PASSWORD
        source: "appleid.apple.com → Sign-In and Security → App-Specific Passwords"
      - name: APPLE_TEAM_ID
        source: "developer.apple.com → Membership → Team ID"
    dashboard_config:
      - task: "Enroll in Apple Developer Program ($99/yr) if not already; download Developer ID Application certificate to keychain"
        location: "developer.apple.com"
requirements: [XCUT-04, XCUT-05, RAG-02]

must_haves:
  truths:
    - "AnswerService is wired into IPC; RAG_ASK returns real cited answers (kind: 'answer' | 'refusal' | 'disambiguation'), not 'Q&A service not ready'"
    - "Pre-migration backup + row-count verify wraps every migration runner invocation"
    - "Failed migration restores from snapshot (db.close → rename → reopenDb) per research Pitfall 3"
    - "electron-updater wired with GitHub provider + channel='tester' default; renderer receives available/progress/downloaded events"
    - "Updates Settings section shows current channel + 'Check for updates' + 'Install and restart' (reachable from SettingsScreen)"
    - "REQUIREMENTS.md amended: XCUT-05 + Phase 8 SC-5 split into tester-ship-unsigned-Windows + GA-OV-sign clauses"
    - "macOS build passes notarization + spctl --assess; documented in release runbook"
    - "Final happy-path E2E (7 steps from CONTEXT) runs against packaged build and passes"
    - "RAG_ASK smoke test (Playwright _electron) proves AnswerService wiring after Phase 7 closure"
    - "Missing runtime deps added: electron-updater (Stream 4), and Stream 2's deps are already added by Plan 08-02"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "XCUT-05 amended to split tester-vs-GA Windows signing"
      contains: "Windows OV signing required for GA release"
    - path: "src/main/release/backup-hook.ts"
      provides: "runMigrationsWithBackup wrapper around runner.runMigrations"
      exports: ["runMigrationsWithBackup", "type MigrationFailedError", "type RowCountDriftError"]
    - path: "src/main/release/verify-migration.ts"
      provides: "row-count comparator with per-migration `expects_drop:<table>` directive"
      exports: ["verifyRowCounts"]
    - path: "src/main/release/updater.ts"
      provides: "autoUpdater wiring with logger + channel + IPC event push"
      exports: ["startAutoUpdater"]
    - path: "src/renderer/features/settings/sections/UpdatesSection.tsx"
      provides: "reachable Settings tab for check / install"
    - path: "src/main/rag/answer-llm.ts"
      provides: "concrete LlmInvocation impl bridging answer-router prompts to the LLM router (Phase 7 closure)"
      exports: ["makeAnswerLlmInvocation"]
    - path: "tests/integration/phase8-happy-path.spec.ts"
      provides: "7-step E2E codifying 'I used Aria today'"
    - path: "tests/integration/rag-ask-smoke.spec.ts"
      provides: "smoke proving RAG_ASK returns real answer post-wiring"
    - path: "docs/RELEASE-RUNBOOK.md"
      provides: "GH PAT scope, notarytool creds, AV submission portals, SmartScreen seed, tester→stable channel flip"
  key_links:
    - from: "src/main/ipc/index.ts"
      to: "src/main/rag/answer-service.ts via getAnswerService factory"
      via: "lazy memoised factory passed to registerRagHandlers (research Pattern 4)"
      pattern: "getOrCreateAnswerService"
    - from: "src/main/db/migrations/runner.ts"
      to: "src/main/release/backup-hook.ts"
      via: "runner export wrapped at boot init by runMigrationsWithBackup"
      pattern: "runMigrationsWithBackup"
    - from: "src/main/release/updater.ts"
      to: "GitHub Releases (github provider)"
      via: "electron-updater autoUpdater + publish config in package.json build.publish"
      pattern: "provider.*github"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "UpdatesSection"
      via: "imported and rendered — reachable"
      pattern: "UpdatesSection"
---

<objective>
(W-3 — plan-split decision, locked at revision time): 08-04 STAYS AS ONE PLAN. Considered splitting into 08-04a (RAG closure: Tasks 2+3) + 08-04b (release: Tasks 1, 4–9 + checkpoints). Rejected because: (i) Task 4 (migration backup) and Task 2 (AnswerService factory) both touch `src/main/ipc/index.ts` — a split would force sequential waves anyway, eliminating the parallelism benefit; (ii) the human-action checkpoint at the top naturally pauses the plan, giving a built-in break without a wave boundary; (iii) the final human-verify checkpoint requires ALL release artifacts (RAG smoke + happy-path + notarize), so 04a and 04b would re-converge. Single plan is correct.

Close v1. Wire the deferred Phase 7 AnswerService → IPC factory; add electron-updater with GitHub Releases + tester channel; wrap migrations with pre-migration backup + row-count verify + atomic restore; amend REQUIREMENTS.md for the staged Windows signing decision; produce macOS notarized build; document the full release pipeline; codify "I used Aria today" as a 7-step Playwright E2E that ships green.

Purpose: Earn XCUT-04 + XCUT-05 (amended) + close RAG-02's last wiring gap. This is the only plan that ends in a shippable artifact.

Output:
- package.json: electron-updater dep + build config (mac notarize + win unsigned tester + nsis + asarUnpack)
- Lazy-memoised AnswerService factory + LlmInvocation impl
- Pre-migration backup wrapper around runMigrations
- electron-updater wiring + UpdatesSection (reachable Settings)
- REQUIREMENTS.md amendment landed
- RAG_ASK smoke + Phase-8 happy-path E2E
- docs/RELEASE-RUNBOOK.md
- One human-action checkpoint for cert acquisition (macOS) — clearly flagged
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-CONTEXT.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-RESEARCH.md
@CLAUDE.md

@src/main/rag/answer-service.ts
@src/main/ipc/rag.ts
@src/main/ipc/index.ts
@src/main/db/migrations/runner.ts
@src/main/db/backup.ts

<interfaces>
<!-- Critical contracts the executor must respect. -->

From src/main/rag/answer-service.ts:
- `createAnswerService(deps: AnswerServiceDeps): AnswerService`
- `AnswerServiceDeps = { db, logger, embedClient, vectorStore, localLlm?, llm: LlmInvocation, accountStatus?, getActiveEmbedModelId?, newRequestKey? }`
- `LlmInvocation.generate({ prompt, route, requestKey }): Promise<{ answer, citations: number[] } | null>` — throws on transport error

From src/main/ipc/rag.ts:
- `RagIpcDeps.getAnswerService?: () => AnswerService | null` — currently optional + always returning null because `src/main/ipc/index.ts:342` calls `registerRagHandlers(ipcMain, { logger, dbHolder })` WITHOUT the factory.

From src/main/db/migrations/runner.ts:
- `runMigrations(db): number[]` — executor MUST grep the real export shape (could be different); wrap that exact function.

From src/main/db/backup.ts:
- `createBackup(db, { outPath })` — VACUUM INTO based; preserves SQLCipher key (research Don't-Hand-Roll row).

From electron-updater 6.x:
- `autoUpdater.checkForUpdates()`, `autoUpdater.downloadUpdate()`, `autoUpdater.quitAndInstall()`, `autoUpdater.channel`, events: `update-available`, `download-progress`, `update-downloaded`, `error`.

From research §Pattern 4 — full factory code sketch lines 366–400. Copy it.
From research §Code Examples — backup-hook + updater + electron-builder JSON skeleton lines 484–649. Copy.
From research §Phase 7 Boot-Wiring Closure (lines 663–681) — verification steps 1–5.

From MEMORY `project_aria_phase3_executed`:
- Phase 3 CR-01 fail-OPEN is a SEPARATE BLOCKER — do NOT subsume in this plan. Document the cross-reference.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-needed>
    Apple Developer ID certificate + notarytool credentials required for macOS notarization (XCUT-05). This is the one item Claude cannot acquire — it requires a paid Apple Developer Program enrollment ($99/year) and a human at apple.com to enroll, generate the cert, and produce an app-specific password.
  </what-needed>
  <how-to-complete>
    1. Enroll in Apple Developer Program at https://developer.apple.com/programs/ if not already ($99/yr).
    2. Download "Developer ID Application" certificate to your keychain (Xcode → Settings → Accounts → Manage Certificates → Developer ID Application).
    3. Generate an App-Specific Password at https://appleid.apple.com → Sign-In and Security → App-Specific Passwords. Label it "Aria notarytool".
    4. Find your Team ID at https://developer.apple.com/account → Membership.
    5. Add to your shell rc (or `.env.local`):
       - `APPLE_ID=<your apple-id email>`
       - `APPLE_APP_SPECIFIC_PASSWORD=<password from step 3>`
       - `APPLE_TEAM_ID=<team id from step 4>`
    6. Sanity-check with `xcrun notarytool history --apple-id $APPLE_ID --team-id $APPLE_TEAM_ID --password $APPLE_APP_SPECIFIC_PASSWORD` — should return a list (even if empty).
  </how-to-complete>
  <resume-signal>Type "creds ready" or "skip macOS notarize for now" (skip means Task 9 macOS build will fail; tester ship is still possible without notarize on Linux/Win-unsigned).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add electron-updater dep + version-verify all Stream 2/4 new deps</name>
  <files>package.json, pnpm-lock.yaml</files>
  <behavior>
    - Test 1: `pnpm list electron-updater` shows ^6.x.
    - Test 2: All Stream 2 deps (docx, @react-pdf/renderer, @tiptap/core, @tiptap/react, @tiptap/starter-kit) installed (defensive — Plan 08-02 should have added them).
    - Test 3: electron-builder is in devDependencies; electron-updater is in dependencies (research Anti-Patterns: "Don't bundle electron-updater + electron-builder dev-only").
    - Test 4: `pnpm typecheck` passes after install.
  </behavior>
  <action>
    Run `npm view electron-updater version` then `pnpm add electron-updater@<resolved-major>`. Verify Stream 2 deps via `pnpm list`; if any missing, add them now (defense against execution-order surprises).
  </action>
  <verify>
    <automated>pnpm list electron-updater &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>electron-updater in dependencies; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AnswerService → IPC factory wiring + LlmInvocation impl (Phase 7 closure)</name>
  <files>src/main/rag/answer-llm.ts, src/main/rag/answer-llm.test.ts, src/main/ipc/index.ts, src/main/ipc/rag.ts</files>
  <behavior>
    - Test 1: makeAnswerLlmInvocation({ router }) returns object with .generate.
    - Test 2: .generate({ prompt, route, requestKey }) for route='LOCAL' calls generateObject with the local model from router; returns `{ answer, citations: number[] }`.
    - Test 3: .generate for route='FRONTIER' calls generateObject with the frontier model from router; returns shape.
    - Test 4: throws on transport error (Ollama 5xx, ECONNREFUSED) — answer-service catches.
    - Test 5: factory in ipc/index.ts is lazy + memoised; first call with db=null returns null; first call after unlock builds the service and caches it.
    - Test 6: getAccountStatus is wired by lifting the existing per-provider lookup (search the codebase for current `provider-accounts` IPC; reuse).
    - Test 7: RAG_ASK against a service-built (vs null) factory returns kind in ('answer','refusal','disambiguation','error') — NEVER `{ kind: 'error', text: 'Q&A service not ready' }` after Task 2 lands.
  </behavior>
  <action>
    Per research §Pattern 4: build `makeAnswerLlmInvocation({ router })`. Internally calls `generateObject({ model: router.modelFor(route), schema: AnswerCitationsSchema, prompt })`. AnswerCitationsSchema = `{ answer: z.string().max(8000), citations: z.array(z.number().int().min(1).max(20)) }`.

    In src/main/ipc/index.ts BEFORE the ragChannels block (~line 326), add:
    ```
    let _answerService: AnswerService | null = null;
    function getOrCreateAnswerService(): AnswerService | null { ... }
    ```
    Per research Pattern 4 code sketch lines 378–400. Update the `registerRagHandlers(ipcMain, { logger, dbHolder })` call to also pass `getAnswerService: getOrCreateAnswerService` AND `getAccountStatus: makeAccountStatusLookup(dbHolder.db ?? null)`.

    `openVectorStore`, `makeOllamaEmbedClient`, `readActiveModelId`, `makeAccountStatusLookup` — confirm exact import paths via grep against the Phase 7 codebase.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/rag/answer-llm.test.ts</automated>
  </verify>
  <done>Factory wired; RAG_ASK now returns real shapes; no 'Q&A service not ready' from the IPC handler when DB is unlocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RAG_ASK smoke (Playwright _electron) — proves Task 2 wiring against real Ollama</name>
  <files>tests/integration/rag-ask-smoke.spec.ts</files>
  <behavior>
    (B-6 — dual-mode smoke: CI default mocked, pre-release real-Ollama gated by env)

    **Mode A — CI pass (default; mocked LLM):**
    - Test 1: launch packaged app via _electron (or dev build harness used elsewhere — confirm).
    - Test 2: seed DB with at least one indexed chunk (use existing Phase 7 backfill harness OR direct INSERT into rag_chunk + rag_chunk_vec).
    - Test 3: open /ask UI; type a question whose entity matches a chunk.
    - Test 4 (B-6 assertion A): assert response `kind` is NOT `'Q&A service not ready'` AND NOT the dark-handler error shape; `kind` is one of `'answer' | 'refusal' | 'disambiguation'`.
    - Test 5 (B-6 assertion B): assert `citations.length >= 1` in the response payload.
    - Test 6 (B-6 assertion C): assert `getOrCreateAnswerService` was invoked at least once during the test (spy on the factory; proves wiring, not LLM quality).
    - Test 7: click citation → asserts in-app navigation OR shell.openExternal call.

    **Mode B — Local pre-release pass (gated by env `ARIA_E2E_REAL_LLM=true`; required pre-tag step):**
    - Test 8: same as Mode A Tests 1–3, but LlmInvocation is NOT mocked — uses real local Ollama at `http://127.0.0.1:11434/api`.
    - Test 9: assert `answer` string is non-empty, length >= 20 chars, AND does NOT match `/^(I (don't|do not) know|N\/?A|undefined|null)$/i` (catches degenerate empty-answer LLM outputs).
    - Mode B runs as a required pre-tag step documented in RELEASE-RUNBOOK.md §Pre-flight (added by Task 9). When `ARIA_E2E_REAL_LLM` is unset/false, Mode B tests are `.skip`'d (vitest/playwright skip pattern) so CI stays green without Ollama.
  </behavior>
  <action>
    Mirror existing Playwright `_electron` specs (research Validation Architecture).

    Mode A injects a stub LlmInvocation via the same `ARIA_TEST_MODE=true` env flag used by the happy-path spec. Mode B does NOT inject — it uses the real router + real Ollama. The Mode-A/Mode-B split is implemented as two `test.describe` blocks; Mode B's describe gates on `process.env.ARIA_E2E_REAL_LLM === 'true'`.

    Cross-reference Task 9 (RELEASE-RUNBOOK.md) — Mode B is added to the runbook's §Pre-flight checks as a REQUIRED step before any `git tag v*` push. Cross-reference Task 10 (human-verify checkpoint) — the resume-signal includes "Mode B real-LLM smoke green".
  </action>
  <verify>
    <automated>pnpm test:e2e --grep rag-ask-smoke</automated>
  </verify>
  <done>Mode A green in CI (default). Mode B passes locally with `ARIA_E2E_REAL_LLM=true pnpm test:e2e --grep rag-ask-smoke` against running Ollama; documented in RELEASE-RUNBOOK.md and the final human-verify checkpoint resume-signal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Pre-migration backup wrapper + row-count verify + atomic restore</name>
  <files>src/main/release/backup-hook.ts, src/main/release/backup-hook.test.ts, src/main/release/verify-migration.ts, src/main/release/verify-migration.integration.test.ts, src/main/db/migrations/runner.ts</files>
  <behavior>
    - Test 1: runMigrationsWithBackup(db, liveDbPath, { dataDir, retainCount: 5 }) takes VACUUM INTO snapshot under `<dataDir>/backups/<stamp>-v<prev>.ariabackup` BEFORE applying migrations.
    - Test 2: records pre-counts for CRITICAL_TABLES list (gmail_message, calendar_event, meeting_note, meeting_action, approval, send_log, calendar_action_log, rag_chunk).
    - Test 3: on success: post counts >= pre counts (per critical table) unless migration declares `expects_drop:<table>` directive; otherwise throws RowCountDriftError.
    - Test 4: on migration throw: NO restore happens inside the wrapper — wrapper rethrows MigrationFailedError carrying backupPath; CALLER decides to restore (per research Pitfall 3 — restore requires db.close FIRST).
    - Test 5: restore helper: `restoreFromBackup(backupPath, liveDbPath, dbHolder)` → dbHolder.close(); fs.renameSync(backupPath copy → liveDbPath); dbHolder.reopen(key).
    - Test 6: keeps last 5 backups; older ones pruned (test with 7 backups → 5 remain, oldest 2 deleted).
    - Test 7: Pitfall 3 reproduced: attempt fs.copyFile over an open handle WITHOUT close → test asserts this is NEVER the path taken; restore-helper MUST close handle first (assert via spy on db.close called before fs.rename).
    - Test 8: integration: simulated migration that drops a row in a critical table without declaring expects_drop → wrapper throws RowCountDriftError + backupPath preserved.
    - Test 9 (B-5 — end-to-end close→rename→reopen with held key; cross-ref MEMORY `project_aria_seal_not_atomic`): full integration against a real test-isolated SQLCipher DB:
        a. Open a real vault via `openDb(testKey)` (test-isolated under tmp dir; NOT the user's real DB).
        b. Acquire a `dbHolder` handle; perform a known write (e.g. `INSERT INTO app_meta(k,v) VALUES('test_marker','before')`); confirm via SELECT.
        c. Capture a pre-migration backup via runMigrationsWithBackup with a held-key reference (key cached in memory; NO user re-prompt path).
        d. Force a migration that throws (test-only migration registered for this test) → catch MigrationFailedError.
        e. Invoke `restoreFromBackup(backupPath, liveDbPath, dbHolder)` and assert IN ORDER (use spies):
            1. `dbHolder.close()` called BEFORE `fs.renameSync` (Pitfall 3 guard).
            2. `fs.renameSync` called exactly once with (tmpRestore, liveDbPath).
            3. `dbHolder.reopen(testKey)` called with the SAME cached key — assert key reference equality, NOT a re-prompt.
        f. After restore: `SELECT v FROM app_meta WHERE k='test_marker'` returns 'before' (pre-throw state restored).
        g. The user-facing recovery dialog from the boot path is NOT invoked (assert via spy on the IPC channel `recovery:show-dialog` or equivalent — confirm name via grep).
        Rationale: MEMORY `project_aria_seal_not_atomic` flagged a Phase 1 echo where vault.json persisted but openDb crashed → user stranded with no working password. This test pins the dual-atomic invariant: close-before-rename AND reopen-with-cached-key (NO re-prompt) succeed together or both throw.
  </behavior>
  <action>
    Per research §Code Examples lines 484–545. Implement wrapper. CRITICAL_TABLES list as research specifies. Pruning by mtime asc, keep last 5.

    Migration directive: per-migration `-- @expects_drop: <table>` SQL comment at the top of the migration file; verify-migration reads applied migration source from the runner's tracked list and extracts the directive. If easier: pass an `expectedDrops: Record<migrationId, string[]>` map directly to runMigrationsWithBackup — confirm pattern with executor judgment.

    The migration runner (`runner.ts`) is NOT modified to call backup-hook itself — instead, `src/main/index.ts` (or wherever runMigrations is invoked at boot) is updated to call `runMigrationsWithBackup` instead. Grep for the existing call site.

    Restore path: integrated into the boot init — on catch(MigrationFailedError | RowCountDriftError) → show recovery dialog → on user confirm, call restoreFromBackup → reopenDb → continue.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/release/backup-hook.test.ts src/main/release/verify-migration.integration.test.ts</automated>
  </verify>
  <done>Pre-migration backup written every boot when new migrations pending; row-count drift triggers wrapped error; restore helper closes-before-rename; pruning keeps 5; end-to-end close→rename→reopen integration (Test 9) succeeds with cached key and no re-prompt, cross-referencing MEMORY `project_aria_seal_not_atomic`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: electron-updater wiring + IPC + UpdatesSection (reachable)</name>
  <files>src/main/release/updater.ts, src/main/release/updater.test.ts, src/main/ipc/updater.ts, src/main/ipc/index.ts, src/shared/ipc-contract.ts, src/preload/index.ts, src/renderer/features/settings/sections/UpdatesSection.tsx, src/renderer/features/settings/sections/UpdatesSection.test.tsx, src/renderer/features/settings/SettingsScreen.tsx</files>
  <behavior>
    - Test 1: startAutoUpdater({ logger, window }) sets autoUpdater.logger to a pino-shim adapter (research Code Examples lines 590–595).
    - Test 2: autoUpdater.autoDownload = false (user-prompted), autoInstallOnAppQuit = true.
    - Test 3: channel defaults to process.env.ARIA_UPDATE_CHANNEL ?? 'tester'.
    - Test 4: update-available / download-progress / update-downloaded events forwarded to renderer via webContents.send.
    - Test 5: UPDATER_CHECK IPC → autoUpdater.checkForUpdates(); returns the update-info or null.
    - Test 6: UPDATER_DOWNLOAD IPC → autoUpdater.downloadUpdate().
    - Test 7: UPDATER_RESTART IPC → autoUpdater.quitAndInstall().
    - Test 8: UpdatesSection renders "Channel: tester" badge, "Check for updates" button, progress bar (when downloading), "Install and restart" button (when downloaded).
    - Test 9 (reachability): grep asserts UpdatesSection imported by SettingsScreen.
    - Test 10: updater error → renderer toast; never throws into the main process loop.
  </behavior>
  <action>
    Per research §Code Examples lines 582–612. autoUpdater.logger is a pino adapter to keep logs in the same scope. Channel via env var per research Open Q #3 + Assumption A4.

    UpdatesSection UI mirrors NewsSourcesSection shape. Wire into SettingsScreen.

    Add CHANNELS UPDATER_CHECK / UPDATER_DOWNLOAD / UPDATER_RESTART + push-event channel name like `updater:available`. Preload exposes `window.api.updater.*`.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/release/updater.test.ts src/renderer/features/settings/sections/UpdatesSection.test.tsx</automated>
  </verify>
  <done>autoUpdater configured + boot-started; IPC round-trip works; UpdatesSection reachable + functional UI present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: electron-builder config — mac notarize + win unsigned tester + nsis + publish github</name>
  <files>package.json, build/entitlements.mac.plist, .env.example</files>
  <behavior>
    - Test 1: `package.json` build block has appId='com.aria.desktop', productName='Aria', publish.provider='github'.
    - Test 2: build.mac has hardenedRuntime: true, entitlements: 'build/entitlements.mac.plist', notarize.teamId set from $APPLE_TEAM_ID env (or hardcoded if user picked the latter — document in runbook).
    - Test 3: build.win.target = ['nsis']; certificateFile + certificateSubjectName ABSENT (per amended XCUT-05 staged signing).
    - Test 4: build.nsis: oneClick=false, perMachine=false, allowToChangeInstallationDirectory=true.
    - Test 5: build.asarUnpack preserves existing sqlite-vec native unpacks (verify present per current package.json).
    - Test 6: entitlements.mac.plist contains com.apple.security.cs.allow-jit (Electron requirement) + com.apple.security.network.client.
    - Test 7: .env.example documents GH_TOKEN, APPLE_ID, APPLE_APP_SPECIFIC_PASSWORD, APPLE_TEAM_ID, ARIA_UPDATE_CHANNEL — NEVER real values committed.
  </behavior>
  <action>
    Per research §Code Examples lines 615–649. Read current package.json build block (asarUnpack already present per research) and EXTEND it; don't replace. Generate a minimal `build/entitlements.mac.plist` (Electron canonical; Phase 1 may already have one — grep first).

    Update .env.example with documented vars; do NOT commit real values.
  </action>
  <verify>
    <automated>pnpm vitest run -t "package.json build config" 2>&amp;1 || node -e "const p=require('./package.json'); ['mac','win','nsis','publish'].forEach(k=>{ if(!p.build[k]) { console.error('missing',k); process.exit(1) }})"</automated>
  </verify>
  <done>build block valid; entitlements file exists; env example documented.</done>
</task>

<task type="auto">
  <name>Task 7: REQUIREMENTS.md amendment for XCUT-05 + Phase 8 SC-5</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    Per CONTEXT &lt;requirements_amendment&gt; section: amend two lines.

    1. In `.planning/REQUIREMENTS.md` line 114 (planner-verified at revision time: exact current text is `- [ ] **XCUT-05**: Application is code-signed and notarized (macOS) and signed (Windows OV cert)`):
       Replace EXACTLY:
       `- [ ] **XCUT-05**: Application is code-signed and notarized (macOS) and signed (Windows OV cert)`
       With EXACTLY (this is the canonical amended line text — Task 7's verify grep anchors on this full string):
       `- [ ] **XCUT-05**: Application is code-signed and notarized (macOS) at v1 release. Windows OV signing applies at **GA release**; initial v1 tester build ships Windows-unsigned with documented SmartScreen warning. OV cert acquired and Windows signing wired after tester usage period (user-confirmed staged approach 2026-05-17).`

    2. In `.planning/ROADMAP.md` Phase 8 Success Criteria (lines 211–212):
       Append clarification to SC-5:
       `5. macOS build passes notarization; Windows build is OV-signed and installs cleanly past SmartScreen reputation seed *at GA release*. Initial v1 tester build ships Windows-unsigned (amended per CONTEXT 2026-05-17 staged signing decision).`

    Do NOT alter any other requirement. Do NOT renumber. Commit alongside Task 6 deliverables.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); const anchor='- [ ] **XCUT-05**: Application is code-signed and notarized (macOS) at v1 release. Windows OV signing applies at **GA release**; initial v1 tester build ships Windows-unsigned with documented SmartScreen warning. OV cert acquired and Windows signing wired after tester usage period (user-confirmed staged approach 2026-05-17).'; if(!r.includes(anchor)){ console.error('XCUT-05 amendment text not found verbatim'); process.exit(1) } if(r.includes('and signed (Windows OV cert)')){ console.error('Old XCUT-05 text still present'); process.exit(1) }"</automated>
  </verify>
  <done>Amendment text present in both files; no other requirements touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Phase 8 happy-path E2E — the 7-step "I used Aria today" spec</name>
  <files>tests/integration/phase8-happy-path.spec.ts</files>
  <behavior>
    - Step 1: Connect Google account (use stubbed OAuth — research Open Q #8 + Phase 2 pattern).
    - Step 2: Connect Outlook account (stubbed; Phase 5 OAuth harness).
    - Step 3: Ingest mail; briefing renders with sections + (if 14d seeded) insights section.
    - Step 4: Draft an email; approve via the approval queue; mocked Gmail send returns 200; send_log row written.
    - Step 5: Schedule a meeting via NL command; approve calendar write; mocked Google Calendar accepts; calendar_action_log row.
    - Step 6: Paste a transcript; approve task batch; mocked Todoist push returns task IDs; meeting_action_task_link rows.
    - Step 7: Run RAG query → assert NOT 'Q&A service not ready'; click citation; verify in-app navigation.
    - Step 8 (added beyond the 7-step list): view weekly recap, edit one section, verify weekly_recap_section_edit row written.
    - Step 9 (added): SC-4 verify — simulated migration failure restores from backup; app relaunches with prior data intact.
    - Whole-spec assertion: action_audit_log VIEW reflects every action taken (one row per: email send, calendar move, task push, approval declined if any).
  </behavior>
  <action>
    Per CONTEXT &lt;decisions&gt; "Final integration tests" 7-step list + bonus steps 8/9 covering Phase 8 stream outputs. Use Playwright `_electron` against the packaged build (research Sampling Rate: "Phase gate: pnpm test (unit + E2E) green").

    OAuth stubs: confirm by reading the Phase 2 `02-01` SUMMARY for the stub pattern. Mocked provider clients are deps-injected at app boot in test mode (env `ARIA_TEST_MODE=true`).
  </action>
  <verify>
    <automated>pnpm test:e2e --grep phase8-happy-path</automated>
  </verify>
  <done>Spec runs against packaged build (or test-mode dev build) and passes; covers all 5 success criteria.</done>
</task>

<task type="auto">
  <name>Task 9: docs/RELEASE-RUNBOOK.md + production smoke command set</name>
  <files>docs/RELEASE-RUNBOOK.md</files>
  <action>
    Author runbook per research Open Q #8. Sections:

    1. **Pre-flight checks** (env vars present, branch is master, tests green, AND the Mode-B real-Ollama RAG_ASK smoke green: `ARIA_E2E_REAL_LLM=true pnpm test:e2e --grep rag-ask-smoke` — REQUIRED before any `git tag v*` push per B-6)
    2. **Tag + publish:** `git tag v1.0.0 && pnpm run release` where `release` script chains `electron-builder --publish always`. Document the `--prerelease` flag for tester channel.
    3. **macOS notarization smoke:** `spctl --assess --verbose=4 build/mac-arm64/Aria.app` → must print "accepted, source=Notarized Developer ID" (research Pitfall 4).
    4. **Windows tester ship:** unsigned NSIS installer goes to GH Release; document SmartScreen warning in tester onboarding doc (link).
    5. **AV submission portals:**
       - Microsoft Defender: https://www.microsoft.com/en-us/wdsi/filesubmission
       - Common AVs runbook (research Pitfall 5).
    6. **SmartScreen reputation seeding strategy** (research Pitfall 5): publisher CN stability, preemptive submission, expectation for first 1–2 GA releases.
    7. **Channel flip:** tester → stable: set `ARIA_UPDATE_CHANNEL=stable` in updater config; flip GH Release `prerelease=false`.
    8. **GH token scope** (PAT classic, `repo` scope).
    9. **Rollback procedure:** if a release crashes for users, mark Release as draft; users on prior version unaffected (electron-updater won't downgrade silently).
    10. **EV-cert deferral note** (research Pitfall 9): why OV not EV; the yubikey-CI tradeoff if ever revisited.
  </action>
  <verify>
    <automated>test -f docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "spctl --assess" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "SmartScreen" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "channel" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "notarytool" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "AV submission" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "rollback" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "ARIA_E2E_REAL_LLM" docs/RELEASE-RUNBOOK.md</automated>
  </verify>
  <done>Runbook covers all 10 sections; smoke command sequence reproducible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    1. electron-builder produces a macOS notarized DMG (`pnpm run release:mac` — runs `electron-builder -m --publish never` first, then `--publish always` when ready).
    2. spctl --assess prints "accepted, source=Notarized Developer ID" against the produced .app.
    3. Windows NSIS installer built unsigned; SmartScreen warning expected and documented.
    4. RAG_ASK smoke + Phase 8 happy-path E2E both green in CI.
  </what-built>
  <how-to-verify>
    1. From clean working tree on master: `pnpm install && pnpm run build && pnpm run release:mac` (or whatever script name maps to electron-builder -m).
    2. `spctl --assess --verbose=4 build/mac-arm64/Aria.app` — assert output contains "Notarized Developer ID".
    3. `open build/Aria-*.dmg` → drag to Applications → launch → assert NO Gatekeeper "verify with Apple" warning.
    4. Install the Windows NSIS exe in a clean Windows VM → confirm SmartScreen warning appears as expected → proceed past it → launch app → assert briefing renders.
    5. CI run of `pnpm test:e2e` green (RAG_ASK smoke + happy-path).
    6. Confirm autoUpdater check in dev tools → "no update available" against current GH Release.
  </how-to-verify>
  <resume-signal>Type "release verified" if mac notarize + Win install + E2E (Mode A) + Mode-B real-Ollama RAG_ASK smoke all pass; OR list specific failures for triage.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Releases → installed app | electron-updater verifies signature against publisher cert chain (works fully only when builds are signed; Win unsigned-tester has elevated MITM risk per CONTEXT) |
| Apple notary service → CI | App-specific password in env; never logged |
| Migration runner → file system | Backup file written under userData; inherits OS perms |
| RAG_ASK renderer → IPC | factory returns null pre-unlock; post-unlock LlmInvocation runs in main process; no token leaks (hashPrompt only in routing log) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Tampering | Rogue latest.yml from MITM (Win unsigned tester) | accept | Documented in tester onboarding; OV signing for GA closes this |
| T-08-17 | Information Disclosure | Apple creds leaking via build logs | mitigate | electron-builder + @electron/notarize redact secrets in stdout; runbook says don't echo $APPLE_APP_SPECIFIC_PASSWORD |
| T-08-18 | Tampering | Migration backup readable to other OS users | mitigate | Inherits userData perms (single-user OS dir on macOS/Win); chmod 600 on Linux best-effort |
| T-08-19 | Denial of Service | Migration failure leaves DB unrecoverable | mitigate | runMigrationsWithBackup + restore helper (Task 4) closes-before-rename per Pitfall 3 |
| T-08-20 | Spoofing | RAG_ASK accepts requests pre-unlock | accept | factory returns null pre-unlock; renderer shows "Q&A unavailable" — same shape as briefing/sync IPCs |
| T-08-21 | Information Disclosure | GH_TOKEN leaking in CI logs | mitigate | Documented as GitHub Secrets in runbook; never echoed |
</threat_model>

<verification>
**Phase-level gate (this plan is the last):**

1. All unit + integration tests pass: `pnpm test`.
2. All grep ratchets pass: `pnpm run lint:guard` (chains insights-prose-no-raw + no-network-from-signals + assertApproved + audit-view + any existing ratchets).
3. Phase 8 happy-path E2E green.
4. RAG_ASK smoke green; manually open /ask in the running app; type a question; receive cited answer.
5. spctl --assess prints Notarized on macOS build artifact.
6. Windows NSIS install completes on a clean Windows VM (SmartScreen warning expected).
7. scheduler.cronRegistry.size invariant: across phases 1+2+3+4, expected size = `gmail-sync + calendar-sync + briefing + insights-nightly + recap-monday + learning-nightly` = 6 (verify by reading the registry state from a running app in dev tools).
8. action_audit_log VIEW returns rows after a full happy-path run; recap regenerate produces a row with non-empty whatAriaDid.auditRowRefs.
9. REQUIREMENTS.md amendment present; ROADMAP SC-5 reflects staged signing.
10. Pre-migration backup confirmed: introduce a deliberately-failing test migration → verify wrapper preserves backup file and restore-helper recovers.

**Sentinel cross-references (NOT in scope of this plan, but verify they remain open):**
- (W-2) XCUT-02 (drafts persist across crashes, never auto-transition to sent) is OWNED by Plan 08-03 Task 6 (crash-persistence spec). Plan 08-04 does NOT re-assert it. The Phase 8 happy-path E2E (Task 8) exercises the approval state machine but does NOT simulate crash — that's 08-03's responsibility.
- Phase 3 CR-01 gate fail-OPEN — still open per MEMORY; do not subsume.
- Phase 5 Outlook send chokepoint — when it lands, action_audit_log VIEW arm 1's `'gmail'` will need to become `sl.provider` (research Assumption A7).
</verification>

<success_criteria>
- XCUT-04: pre-migration backup + restore demonstrated in test + manual.
- XCUT-05 (amended): macOS notarized; Windows tester-unsigned with documented runbook for GA OV signing.
- RAG-02: AnswerService wired; RAG_ASK returns real cited answers (closes Phase 7 deferred wiring).
- All 4 Phase 8 streams shipped: insights (08-01) + recap (08-02) + learning (08-03) + release (08-04).
- 5 SCs from ROADMAP demonstrated:
  - SC-1 (recap audit section) — covered by Plan 08-02 + e2e step 8.
  - SC-2 (insight after 14d) — covered by Plan 08-01 + e2e step 3.
  - SC-3 (voice match — manual eval flag in runbook, not blocked here).
  - SC-4 (auto-updater + migration backup) — covered by Tasks 4+5 + e2e step 9.
  - SC-5 (notarize/signing) — covered by Task 6 + checkpoint + runbook.
- Phase 8 final checkpoint verified by human.
</success_criteria>

<output>
After completion, write `.planning/phases/08-insights-weekly-recap-learning-release-prep/08-04-SUMMARY.md` covering:
- Phase 7 closure (AnswerService factory + LlmInvocation + smoke) — call out as a deferred-Phase-7 closure
- electron-updater wiring + UpdatesSection
- Migration backup hook + restore atomicity
- electron-builder config delta
- REQUIREMENTS amendment text
- Release runbook + remaining manual steps (Apple cert acquisition tracked in user_setup)
- v1 ship-readiness checklist:
  - [ ] All 4 plans landed
  - [ ] All grep ratchets green
  - [ ] Happy-path E2E green
  - [ ] macOS notarized build verified
  - [ ] Windows unsigned tester build produced + uploaded to GH Release with prerelease=true
  - [ ] Tester onboarding doc references SmartScreen warning
- Cross-phase open items:
  - Phase 3 CR-01 gate hardening (separate plan 03-05)
  - Phase 5 Outlook send → action_audit_log VIEW arm 1 update (when it ships)
  - Phase 9 product UI rollout (next phase)

Then write `.planning/phases/08-insights-weekly-recap-learning-release-prep/08-SUMMARY.md` (phase-level) consolidating all four plan summaries + a "v1 SHIPPABLE" status line.
</output>
