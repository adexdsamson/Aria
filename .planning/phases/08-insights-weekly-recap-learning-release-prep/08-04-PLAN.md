---
phase: 08-insights-weekly-recap-learning-release-prep
plan: 04
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .planning/REQUIREMENTS.md
  - src/main/release/updater.ts
  - src/main/release/updater.test.ts
  - src/main/release/backup-hook.ts
  - src/main/release/backup-hook.test.ts
  - src/main/release/verify-migration.ts
  - src/main/release/verify-migration.integration.test.ts
  - src/main/db/migrations/runner.ts
  - src/main/db/connect.ts
  - src/main/db/restore.ts
  - src/main/ipc/onboarding.ts
  - src/main/ipc/backup.ts
  - src/main/index.ts
  - src/main/ipc/updater.ts
  - src/main/ipc/index.ts
  - src/shared/ipc-contract.ts
  - src/preload/index.ts
  - src/renderer/features/settings/sections/UpdatesSection.tsx
  - src/renderer/features/settings/sections/UpdatesSection.test.tsx
  - src/renderer/features/settings/SettingsScreen.tsx
  - src/renderer/App.tsx
  - src/main/rag/answer-llm.ts
  - src/main/rag/answer-llm.test.ts
  - src/main/rag/answer-service-factory.ts
  - src/main/rag/answer-service-factory.test.ts
  - src/main/ipc/rag.ts
  - tests/integration/rag-ask-smoke.spec.ts
  - tests/integration/phase8-happy-path.spec.ts
  - tests/fixtures/999_force_fail.sql
  - docs/RELEASE-RUNBOOK.md
  - .planning/phases/08-insights-weekly-recap-learning-release-prep/08-CONTEXT.md
  - build/entitlements.mac.plist
  - .env.example
autonomous: false
user_setup:
  - service: github-releases
    why: "electron-builder --publish github writes latest.yml + installer to GH Releases"
    env_vars:
      - name: GH_TOKEN
        source: "GitHub → Settings → Developer settings → Personal access tokens (classic) → scope: `repo`"
    dashboard_config:
      - task: "Verify repo is public OR token has repo scope"
        location: "GitHub repo settings"
  - service: apple-developer-id
    why: "macOS notarization at v1 release per CONTEXT (XCUT-05 unchanged for macOS)"
    env_vars:
      - name: APPLE_ID
        source: "Apple ID email used for Developer Program"
      - name: APPLE_APP_SPECIFIC_PASSWORD
        source: "appleid.apple.com → Sign-In and Security → App-Specific Passwords"
      - name: APPLE_TEAM_ID
        source: "developer.apple.com → Membership → Team ID"
    dashboard_config:
      - task: "Enroll in Apple Developer Program ($99/yr); download Developer ID Application certificate to keychain"
        location: "developer.apple.com"
requirements: [XCUT-04, XCUT-05, RAG-02]

must_haves:
  truths:
    - "AnswerService is wired into IPC; RAG_ASK returns real cited answers, not 'Q&A service not ready'"
    - "AnswerService factory emits a one-time log-line on first construction; CI smoke asserts the log line appears (B-2 round 2)"
    - "Migration is invoked from a single boot-level site (NOT from inside openDb) so the snapshot/rollback wrapper can wrap it without recursion (B-1 round 2)"
    - "Pre-migration backup + row-count verify wraps every migration runner invocation"
    - "Failed migration restores from snapshot (db.close → rename → reopenDb with cached key, runMigrationsOnOpen=false)"
    - "electron-updater wired with GitHub provider + channel='tester' default"
    - "Updates Settings section shows current channel + 'Check for updates' + 'Install and restart'"
    - "REQUIREMENTS.md amended: XCUT-05 + Phase 8 SC-5 split into tester-ship-unsigned-Windows + GA-OV-sign clauses"
    - "macOS build passes notarization + spctl --assess"
    - "Final happy-path E2E (7 steps from CONTEXT) runs against packaged build"
    - "RAG_ASK smoke test proves AnswerService wiring after Phase 7 closure"
    - "08-CONTEXT.md auto-updater 'runs previous installer' promise softened to match electron-updater's actual capability (M-2 round 2)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "XCUT-05 amended to split tester-vs-GA Windows signing"
      contains: "Windows OV signing required for GA release"
    - path: "src/main/release/backup-hook.ts"
      provides: "runMigrationsWithBackup wrapper; takes expectedDrops map (H-3 round 2 commitment)"
      exports: ["runMigrationsWithBackup", "restoreFromBackup", "type MigrationFailedError", "type RowCountDriftError"]
    - path: "src/main/release/verify-migration.ts"
      provides: "row-count comparator using expectedDrops: Record<number, string[]> map argument"
      exports: ["verifyRowCounts"]
    - path: "src/main/release/updater.ts"
      provides: "autoUpdater wiring with logger + channel + IPC event push"
      exports: ["startAutoUpdater"]
    - path: "src/renderer/features/settings/sections/UpdatesSection.tsx"
      provides: "reachable Settings tab for check / install"
    - path: "src/main/rag/answer-service-factory.ts"
      provides: "exported, log-emitting AnswerService factory (B-2 round 2)"
      exports: ["createAnswerServiceFactory", "type AnswerServiceFactory"]
    - path: "src/main/rag/answer-llm.ts"
      provides: "concrete LlmInvocation impl (Phase 7 closure)"
      exports: ["makeAnswerLlmInvocation"]
    - path: "tests/integration/phase8-happy-path.spec.ts"
      provides: "7-step E2E codifying 'I used Aria today'"
    - path: "tests/integration/rag-ask-smoke.spec.ts"
      provides: "smoke proving RAG_ASK returns real answer; asserts factory log-line"
    - path: "tests/fixtures/999_force_fail.sql"
      provides: "deliberately-failing migration used by E2E migration-failure restore test (gated by ARIA_E2E_FORCE_MIGRATION_FAIL env, NEVER registered in embedded.ts)"
    - path: "docs/RELEASE-RUNBOOK.md"
      provides: "GH PAT scope, notarytool creds, AV submission portals, SmartScreen seed, tester→stable channel flip"
    - path: "src/main/db/connect.ts"
      provides: "openDb supports runMigrationsOnOpen: true | false | 'deferred' (B-1 round 2 fix)"
  key_links:
    - from: "src/main/ipc/index.ts"
      to: "src/main/rag/answer-service-factory.ts"
      via: "registerHandlers imports the factory module; factory module owns the memo cell + log line"
      pattern: "createAnswerServiceFactory"
    - from: "src/main/index.ts"
      to: "src/main/release/backup-hook.ts"
      via: "boot path opens DB with runMigrationsOnOpen='deferred', then calls runMigrationsWithBackup(db, ...)"
      pattern: "runMigrationsWithBackup"
    - from: "src/main/release/updater.ts"
      to: "GitHub Releases (github provider)"
      via: "electron-updater autoUpdater + publish config in package.json build.publish"
      pattern: "provider.*github"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "UpdatesSection"
      via: "imported and rendered — reachable"
      pattern: "UpdatesSection"
---

<objective>
(W-3) 08-04 STAYS AS ONE PLAN. Single plan is correct.

Close v1. Wire the deferred Phase 7 AnswerService → IPC factory; add electron-updater with GitHub Releases + tester channel; wrap migrations with pre-migration backup + row-count verify + atomic restore; amend REQUIREMENTS.md for the staged Windows signing decision; produce macOS notarized build; document the full release pipeline; codify "I used Aria today" as a 7-step Playwright E2E.

## Peer-review round 2 fixes incorporated

- **B-1:** Migration extracted from inside `openDb()` (currently runs at `src/main/db/connect.ts:67`) to a single explicit boot call site (NEW Task 4a). Onboarding + restore + backup paths all updated.
- **B-2:** AnswerService factory hoisted out of the module-local closure inside `registerHandlers` into a dedicated, exported `src/main/rag/answer-service-factory.ts` module. Factory emits a one-time pino log line on first construction; Mode A smoke asserts the log line appears (replaces the unspy-able closure spy).
- **H-2:** XCUT-05 verify-grep anchors on short invariant substrings (`applies at **GA release**` AND `tester build ships Windows-unsigned`), NOT the full date-bearing line.
- **H-3:** `expectedDrops: Record<number, string[]>` map argument is the only path. SQL-comment-parsing alternative deleted.
- **M-2:** 08-CONTEXT.md auto-updater promise softened — electron-updater does NOT natively re-run previous installers; rollback restores the DB only and may require manual re-install of prior version.
- **M-3:** E2E migration-failure fixture is concrete: `tests/fixtures/999_force_fail.sql` gated by env `ARIA_E2E_FORCE_MIGRATION_FAIL=true`. NEVER registered in `embedded.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-CONTEXT.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-RESEARCH.md
@CLAUDE.md

@src/main/rag/answer-service.ts
@src/main/ipc/rag.ts
@src/main/ipc/index.ts
@src/main/db/migrations/runner.ts
@src/main/db/connect.ts
@src/main/db/restore.ts
@src/main/ipc/onboarding.ts
@src/main/ipc/backup.ts
@src/main/db/backup.ts

<interfaces>
From src/main/rag/answer-service.ts:
- `createAnswerService(deps: AnswerServiceDeps): AnswerService`
- `LlmInvocation.generate({ prompt, route, requestKey })`

From src/main/ipc/rag.ts:
- `RagIpcDeps.getAnswerService?: () => AnswerService | null` — currently always null because the factory was never wired in Phase 7.

From src/main/db/connect.ts (planner-verified at revision round 2):
- `openDb(opts: OpenDbOptions): Db` where `OpenDbOptions.runMigrationsOnOpen?: boolean` (default `true`).
- **Line 67:** `if (runMigrationsOnOpen) runMigrations(db);` — this is the SINGLE site where `runMigrations` is invoked across the entire codebase. There is no top-level `runMigrations()` call in `src/main/index.ts`.
- `src/main/ipc/onboarding.ts:191`, `src/main/db/restore.ts:62`, and `src/main/ipc/backup.ts:82` all flow through `openDb(...)` and inherit the default. Any change cascades to these callers — Task 4a handles all three.

From src/main/db/migrations/runner.ts:
- `runMigrations(db): number[]` — returns applied version numbers.

From src/main/db/backup.ts:
- `createBackup(db, { outPath })` — VACUUM INTO based; preserves SQLCipher key.

From electron-updater 6.x:
- `autoUpdater.checkForUpdates()`, `autoUpdater.downloadUpdate()`, `autoUpdater.quitAndInstall()`, `autoUpdater.channel`, events: `update-available`, `download-progress`, `update-downloaded`, `error`.

From MEMORY `project_aria_phase3_executed`:
- Phase 3 CR-01 fail-OPEN is a SEPARATE BLOCKER.

From MEMORY `project_aria_seal_not_atomic`:
- vault.json persisted even when openDb crashed → user stranded. B-1 round 2 fix MUST NOT regress this — seal must validate migrate succeeds before persisting vault.json.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-needed>
    Apple Developer ID certificate + notarytool credentials required for macOS notarization (XCUT-05).
  </what-needed>
  <how-to-complete>
    1. Enroll in Apple Developer Program at https://developer.apple.com/programs/ ($99/yr).
    2. Download "Developer ID Application" certificate to your keychain.
    3. Generate an App-Specific Password at https://appleid.apple.com.
    4. Find your Team ID at https://developer.apple.com/account → Membership.
    5. Add to your shell rc (or `.env.local`):
       - `APPLE_ID=<your apple-id email>`
       - `APPLE_APP_SPECIFIC_PASSWORD=<password from step 3>`
       - `APPLE_TEAM_ID=<team id from step 4>`
    6. Sanity-check with `xcrun notarytool history --apple-id $APPLE_ID --team-id $APPLE_TEAM_ID --password $APPLE_APP_SPECIFIC_PASSWORD`.
  </how-to-complete>
  <resume-signal>Type "creds ready" or "skip macOS notarize for now".</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add electron-updater dep + version-verify all Stream 2/4 new deps</name>
  <files>package.json, pnpm-lock.yaml</files>
  <behavior>
    - Test 1: `pnpm list electron-updater` shows ^6.x.
    - Test 2: All Stream 2 deps (docx, @react-pdf/renderer, @tiptap/core, @tiptap/react, @tiptap/starter-kit) installed.
    - Test 3: electron-builder is in devDependencies; electron-updater is in dependencies.
    - Test 4: `pnpm typecheck` passes after install.
  </behavior>
  <action>
    Run `npm view electron-updater version` then `pnpm add electron-updater@<resolved-major>`. Verify Stream 2 deps via `pnpm list`.
  </action>
  <verify>
    <automated>pnpm list electron-updater &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>electron-updater in dependencies; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AnswerService factory module (B-2 round 2 — hoisted out of module-local closure, log-line ratchet)</name>
  <files>src/main/rag/answer-llm.ts, src/main/rag/answer-llm.test.ts, src/main/rag/answer-service-factory.ts, src/main/rag/answer-service-factory.test.ts, src/main/ipc/index.ts, src/main/ipc/rag.ts</files>
  <behavior>
    - Test 1: makeAnswerLlmInvocation({ router }) returns object with .generate.
    - Test 2: .generate for route='LOCAL' calls generateObject with the local model; returns `{ answer, citations: number[] }`.
    - Test 3: .generate for route='FRONTIER' calls generateObject with the frontier model.
    - Test 4: throws on transport error (Ollama 5xx, ECONNREFUSED).
    - Test 5 (B-2 round 2 — factory module): `createAnswerServiceFactory({ logger, dbHolder, ... })` returns an object `{ get(): AnswerService | null; isConstructed(): boolean; callCount(): number }`. First `.get()` with `dbHolder.db === null` returns null and emits NO log. First `.get()` with non-null db: builds the AnswerService AND emits exactly ONE pino log entry at level info with `{ scope: 'answer-service', event: 'factory.constructed' }` and message `answer-service factory constructed; route active`.
    - Test 6 (B-2 round 2 — idempotency): second `.get()` returns the same instance AND does NOT emit a second log line. `callCount()` increments on every invocation regardless of cached-vs-new.
    - Test 7: getAccountStatus is wired by lifting the existing per-provider lookup (search for `provider-accounts` IPC; reuse).
    - Test 8: RAG_ASK against a service-built factory returns kind in ('answer','refusal','disambiguation','error') — NEVER `'Q&A service not ready'` after this task lands.
  </behavior>
  <action>
    Per research §Pattern 4: build `makeAnswerLlmInvocation({ router })`. AnswerCitationsSchema = `{ answer: z.string().max(8000), citations: z.array(z.number().int().min(1).max(20)) }`.

    **B-2 round 2 fix:** Do NOT define the factory as a module-local closure inside `registerHandlers` (which is unreachable from Playwright `_electron` test harness — Phase 3 sensitivity-classifier-was-dark bug shape). Instead, create a NEW exported module `src/main/rag/answer-service-factory.ts`:
    ```ts
    export interface AnswerServiceFactory {
      get(): AnswerService | null;
      isConstructed(): boolean;
      callCount(): number;
    }
    export function createAnswerServiceFactory(deps: {
      logger: Logger;
      dbHolder: DbHolder;
      // ... router, openVectorStore, makeOllamaEmbedClient, readActiveModelId, makeAccountStatusLookup
    }): AnswerServiceFactory {
      let cached: AnswerService | null = null;
      let constructed = false;
      let calls = 0;
      return {
        get() {
          calls++;
          if (cached) return cached;
          if (!deps.dbHolder.db) return null;
          cached = createAnswerService({ /* wire deps */ });
          constructed = true;
          deps.logger.info(
            { scope: 'answer-service', event: 'factory.constructed' },
            'answer-service factory constructed; route active',
          );
          return cached;
        },
        isConstructed: () => constructed,
        callCount: () => calls,
      };
    }
    ```
    In `src/main/ipc/index.ts`, import the factory, construct it once near the ragChannels block, and pass `getAnswerService: factory.get` to `registerRagHandlers`.

    The log line is the cross-process ratchet — it survives bundling, survives refactors, and can be asserted by the Mode A smoke test by parsing the captured pino stream. NO closure spy needed.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/rag/answer-llm.test.ts src/main/rag/answer-service-factory.test.ts</automated>
  </verify>
  <done>Factory hoisted to its own module; emits one-time log line; RAG_ASK now returns real shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RAG_ASK smoke (Playwright _electron) — dual-mode; Mode A asserts factory log-line (B-2 round 2)</name>
  <files>tests/integration/rag-ask-smoke.spec.ts</files>
  <behavior>
    **Mode A — CI pass (default; mocked LLM):**
    - Test 1: launch packaged app via _electron.
    - Test 2: seed DB with at least one indexed chunk.
    - Test 3: open /ask UI; type a question whose entity matches a chunk.
    - Test 4: assert response `kind` is one of `'answer' | 'refusal' | 'disambiguation'`; NEVER `'Q&A service not ready'`.
    - Test 5: assert `citations.length >= 1`.
    - Test 6 (B-2 round 2 — log-line ratchet REPLACES closure spy): parse the captured pino stream (either the file the main-process logger writes to under `userData/logs/`, or `electronApp.process().stdout`). Assert exactly ONE entry with `scope === 'answer-service' && event === 'factory.constructed'`. Closes B-2: this proves the factory was actually invoked via the wired IPC path, NOT a hardcoded stub returning the right shape.
    - Test 7: click citation → asserts in-app navigation OR shell.openExternal call.

    **Mode B — Local pre-release pass (gated by env `ARIA_E2E_REAL_LLM=true`):**
    - Test 8: same as Mode A Tests 1–3, but LlmInvocation NOT mocked — uses real local Ollama.
    - Test 9: assert `answer` non-empty, length >= 20, NOT matching `/^(I (don't|do not) know|N\/?A|undefined|null)$/i`.
    - Mode B `.skip`'d when `ARIA_E2E_REAL_LLM` unset.
  </behavior>
  <action>
    Mirror existing Playwright `_electron` specs. Mode A injects a stub LlmInvocation via `ARIA_TEST_MODE=true`. Mode B does NOT inject.

    For Test 6's log-stream assertion: if pino writes to a file destination, the spec opens that file after the IPC call completes; if pino writes to stdout, the spec captures `electronApp.process().stdout` from launch. Either way, parse line-delimited JSON and filter for `scope === 'answer-service' && event === 'factory.constructed'`. EXACTLY one match required (idempotency invariant from Task 2 Test 6 — repeated RAG_ASK calls do NOT emit duplicate construction logs).

    Cross-reference Task 9 — Mode B is a REQUIRED pre-tag step.
  </action>
  <verify>
    <automated>pnpm test:e2e --grep rag-ask-smoke</automated>
  </verify>
  <done>Mode A green in CI; Mode B passes locally with `ARIA_E2E_REAL_LLM=true`; log-line ratchet replaces unreachable closure spy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4a (NEW — B-1 round 2): Extract runMigrations call site from openDb to top-level boot init</name>
  <files>src/main/db/connect.ts, src/main/ipc/onboarding.ts, src/main/db/restore.ts, src/main/ipc/backup.ts, src/main/index.ts</files>
  <behavior>
    - Test 1: `openDb({ ..., runMigrationsOnOpen: false })` opens an encrypted DB without invoking `runMigrations`.
    - Test 2: `openDb({ ..., runMigrationsOnOpen: 'deferred' })` is identical to `false` for the open call AND returns a Db handle ready for a separate `runMigrationsWithBackup(db, ...)` invocation by the caller.
    - Test 3: `openDb({ ..., runMigrationsOnOpen: true })` (default) preserves existing behavior — backwards-compat.
    - Test 4: onboarding seal path opens with `'deferred'` then invokes the new top-level migrate, so a migration crash during seal does NOT leave vault.json persisted with an unmigrated DB (cross-ref MEMORY `project_aria_seal_not_atomic`). Assert: when migration throws inside seal, vault.json is NOT written.
    - Test 5: restore.ts opens the restored snapshot with `runMigrationsOnOpen: false` — the snapshot was taken at the prior schema version; re-migration would defeat the restore by re-applying the failing migration.
    - Test 6: backup.ts (BACKUP_RESTORE path) opens with `runMigrationsOnOpen: false`.
    - Test 7: grep test asserts EXACTLY ONE call to `runMigrationsWithBackup` in `src/main/index.ts` AND ZERO calls to bare `runMigrations(db)` anywhere outside `src/main/db/migrations/runner.ts` itself + `src/main/release/backup-hook.ts` (which wraps it).
    - Test 8 (recursion guard): force a migration to throw in the test boot path; assert the rollback path opens the snapshot with `runMigrationsOnOpen: false` — it does NOT re-enter the migration runner. Verify by spy on `runMigrations`: called exactly ONCE (the initial failing call), NOT twice.
  </behavior>
  <action>
    Peer review B-1 round 2 caught that round 1 said "Grep for the existing call site… in `src/main/index.ts` (or wherever runMigrations is invoked at boot)" — but the actual single call site is `src/main/db/connect.ts:67` inside `openDb()`, NOT a top-level boot site. Wrapping that with a snapshot-rollback wrapper creates a recursion problem: the rollback needs `dbHolder.close()` + `dbHolder.reopen(key, { runMigrationsOnOpen: false })`, but `openDb` itself is what builds the handle and doesn't know about the holder. And on rollback the new handle would re-enter `openDb` → re-enter `runMigrations` → re-fail.

    **Decision (peer review's recommended option b):** EXTRACT migration out of `openDb` into a separate boot call site. Onboarding + restore + backup all flow through `openDb` with the new `runMigrationsOnOpen: false` (or `'deferred'`) option.

    Changes in this task:

    1. **src/main/db/connect.ts:** widen `OpenDbOptions.runMigrationsOnOpen` to `boolean | 'deferred'`. `'deferred'` behaves identically to `false` at open time but signals intent. Default remains `true` for backwards-compat in test harnesses that genuinely want open+migrate in one shot.

    2. **src/main/index.ts** (boot path): change the unlock-handler to open with `'deferred'`, then call `runMigrationsWithBackup(dbHolder.db, dbPath, { dataDir, retainCount: 5, expectedDrops: {} })` once. On `MigrationFailedError | RowCountDriftError`: show the recovery dialog → on user confirm, call `restoreFromBackup(backupPath, dbPath, dbHolder)` (which closes + renames + reopens with `runMigrationsOnOpen: false`).

    3. **src/main/ipc/onboarding.ts** (seal path, ~line 191): change `openDb(...)` to pass `runMigrationsOnOpen: 'deferred'`, then explicitly call `runMigrationsWithBackup(...)` AFTER `openDb` returns but BEFORE persisting `vault.json`. If migrate fails during seal, throw — caller bubbles up; vault.json is NEVER written. Closes the seal-not-atomic regression.

    4. **src/main/db/restore.ts** (~line 62): change `openDb(...)` to pass `runMigrationsOnOpen: false`.

    5. **src/main/ipc/backup.ts** (~line 82, BACKUP_RESTORE path): change to `runMigrationsOnOpen: false`.

    6. Add the grep test (Test 7) to lock the new invariant.

    This task lands BEFORE Task 4. Task 4 wraps the now-extracted single call site without recursion risk.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/db/connect.test.ts src/main/ipc/onboarding.test.ts src/main/db/restore.test.ts</automated>
  </verify>
  <done>Migration runs from ONE explicit boot call site; openDb supports deferred mode; onboarding/restore/backup all updated; recursion-guard test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Pre-migration backup wrapper + row-count verify + atomic restore (now wrapping the extracted boot site)</name>
  <files>src/main/release/backup-hook.ts, src/main/release/backup-hook.test.ts, src/main/release/verify-migration.ts, src/main/release/verify-migration.integration.test.ts</files>
  <behavior>
    - Test 1: runMigrationsWithBackup(db, liveDbPath, { dataDir, retainCount: 5, expectedDrops }) takes VACUUM INTO snapshot under `<dataDir>/backups/<stamp>-v<prev>.ariabackup` BEFORE applying migrations.
    - Test 2: records pre-counts for CRITICAL_TABLES list (gmail_message, calendar_event, meeting_note, meeting_action, approval, send_log, calendar_action_log, rag_chunk).
    - Test 3 (H-3 round 2 — map argument committed): `expectedDrops: Record<number, string[]>` map argument is the ONLY supported path. Test passes `{ 999: ['gmail_message'] }` and verifies a migration that drops gmail_message rows under version 999 succeeds. SQL-comment-parsing alternative DELETED — runner does not need to know about the directive.
    - Test 4: on migration throw: NO restore happens inside the wrapper — rethrows MigrationFailedError carrying backupPath; CALLER decides to restore.
    - Test 5: restore helper: `restoreFromBackup(backupPath, liveDbPath, dbHolder)` → dbHolder.close(); fs.renameSync(backupPath copy → liveDbPath); dbHolder.reopen(key, { runMigrationsOnOpen: false }).
    - Test 6: keeps last 5 backups; older ones pruned.
    - Test 7: Pitfall 3 guard: restore-helper MUST close handle BEFORE rename (assert via spy on db.close called before fs.rename).
    - Test 8: integration: simulated migration drops critical-table rows without declaring expects_drop → wrapper throws RowCountDriftError.
    - Test 9 (B-5 — end-to-end close→rename→reopen with held key; cross-ref MEMORY `project_aria_seal_not_atomic`):
        a. Open vault via `openDb(testKey, { runMigrationsOnOpen: 'deferred' })`.
        b. Acquire `dbHolder`; perform known write; confirm via SELECT.
        c. Capture pre-migration backup via runMigrationsWithBackup with cached-key reference.
        d. Force migration throw → catch MigrationFailedError.
        e. Invoke `restoreFromBackup(backupPath, liveDbPath, dbHolder)` and assert IN ORDER:
            1. `dbHolder.close()` called BEFORE `fs.renameSync` (Pitfall 3 guard).
            2. `fs.renameSync` called exactly once.
            3. `dbHolder.reopen(testKey, { runMigrationsOnOpen: false })` called with SAME cached key — assert key reference equality, NOT a re-prompt.
        f. After restore: `SELECT v FROM app_meta WHERE k='test_marker'` returns pre-throw value.
        g. User-facing recovery dialog NOT invoked during the test path itself (separate concern — boot handler shows it).
  </behavior>
  <action>
    Per research §Code Examples lines 484–545. CRITICAL_TABLES list as research specifies. Pruning by mtime asc, keep last 5.

    **H-3 round 2 commitment:** Migration directive is the `expectedDrops: Record<number, string[]>` map argument ONLY. No SQL-comment-parsing fallback. Phase 8 has zero migrations that intentionally drop critical-table rows (128/129/130 are additive), so the map is `{}` at ship time. The map exists for future migrations to declare drops without modifying the runner.

    Wrapper signature: `runMigrationsWithBackup(db, liveDbPath, { dataDir, retainCount, expectedDrops })`. Returns the same `number[]` as `runMigrations`.

    Caller responsibility (Task 4a updated `src/main/index.ts` boot path): on catch(MigrationFailedError | RowCountDriftError) → show recovery dialog → on user confirm, call `restoreFromBackup(backupPath, liveDbPath, dbHolder)` → continue boot.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/release/backup-hook.test.ts src/main/release/verify-migration.integration.test.ts</automated>
  </verify>
  <done>Pre-migration backup written every boot when new migrations pending; row-count drift triggers wrapped error; restore helper closes-before-rename; expectedDrops map is the committed shape; Test 9 succeeds with cached key and no re-prompt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: electron-updater wiring + IPC + UpdatesSection (reachable)</name>
  <files>src/main/release/updater.ts, src/main/release/updater.test.ts, src/main/ipc/updater.ts, src/main/ipc/index.ts, src/shared/ipc-contract.ts, src/preload/index.ts, src/renderer/features/settings/sections/UpdatesSection.tsx, src/renderer/features/settings/sections/UpdatesSection.test.tsx, src/renderer/features/settings/SettingsScreen.tsx, src/renderer/App.tsx</files>
  <behavior>
    - Test 1: startAutoUpdater({ logger, window }) sets autoUpdater.logger to a pino-shim adapter.
    - Test 2: autoUpdater.autoDownload = false, autoInstallOnAppQuit = true.
    - Test 3: channel defaults to process.env.ARIA_UPDATE_CHANNEL ?? 'tester'.
    - Test 4: update-available / download-progress / update-downloaded events forwarded to renderer via webContents.send.
    - Test 5: UPDATER_CHECK IPC → autoUpdater.checkForUpdates(); returns the update-info or null.
    - Test 6: UPDATER_DOWNLOAD IPC → autoUpdater.downloadUpdate().
    - Test 7: UPDATER_RESTART IPC → autoUpdater.quitAndInstall().
    - Test 8: UpdatesSection renders "Channel: tester" badge, "Check for updates" button, progress bar, "Install and restart" button.
    - Test 9 (reachability — section level): grep asserts UpdatesSection imported by SettingsScreen.
    - Test 10 (L-2 round 2 — parent-orphan guard): grep asserts `/settings` route exists in `src/renderer/App.tsx`. Phase 4 MEMORY `feedback_verifier_blindspot_ui_wiring` was about sub-sections being orphan; this closes the parent-orphan class. One-time, cheap.
    - Test 11: updater error → renderer toast; never throws into the main process loop.
  </behavior>
  <action>
    Per research §Code Examples lines 582–612. autoUpdater.logger is a pino adapter. Channel via env var.

    UpdatesSection UI mirrors NewsSourcesSection shape. Wire into SettingsScreen.

    Add CHANNELS UPDATER_CHECK / UPDATER_DOWNLOAD / UPDATER_RESTART + push-event channel name like `updater:available`. Preload exposes `window.api.updater.*`.

    Add one-time grep assertion (Test 10) for `path="/settings"` (or whatever route literal Phase 1 uses — grep `App.tsx` first to find the exact pattern) in `src/renderer/App.tsx`. This closes peer-review L-2 round 2 cheaply.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/release/updater.test.ts src/renderer/features/settings/sections/UpdatesSection.test.tsx</automated>
  </verify>
  <done>autoUpdater configured + boot-started; IPC round-trip works; UpdatesSection reachable + parent /settings route asserted to exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: electron-builder config — mac notarize + win unsigned tester + nsis + publish github</name>
  <files>package.json, build/entitlements.mac.plist, .env.example</files>
  <behavior>
    - Test 1: `package.json` build block has appId='com.aria.desktop', productName='Aria', publish.provider='github'.
    - Test 2: build.mac has hardenedRuntime: true, entitlements: 'build/entitlements.mac.plist', notarize.teamId set from $APPLE_TEAM_ID env.
    - Test 3: build.win.target = ['nsis']; certificateFile + certificateSubjectName ABSENT (per amended XCUT-05 staged signing).
    - Test 4: build.nsis: oneClick=false, perMachine=false, allowToChangeInstallationDirectory=true.
    - Test 5: build.asarUnpack preserves existing sqlite-vec native unpacks.
    - Test 6: entitlements.mac.plist contains com.apple.security.cs.allow-jit + com.apple.security.network.client.
    - Test 7: .env.example documents GH_TOKEN, APPLE_ID, APPLE_APP_SPECIFIC_PASSWORD, APPLE_TEAM_ID, ARIA_UPDATE_CHANNEL.
  </behavior>
  <action>
    Per research §Code Examples lines 615–649. Read current package.json build block and EXTEND it.

    Update .env.example; never commit real values.

    (L-1 round 2 — cheap cosmetic fix): Task 6 verify command uses `&&` (not `||`) so a vitest failure does NOT mask via the node-e fallback. See `<verify>` below.
  </action>
  <verify>
    <automated>pnpm vitest run -t "package.json build config" &amp;&amp; node -e "const p=require('./package.json'); ['mac','win','nsis','publish'].forEach(k=>{ if(!p.build[k]) { console.error('missing',k); process.exit(1) }})"</automated>
  </verify>
  <done>build block valid; entitlements file exists; env example documented; verify chain uses && (not || swallow).</done>
</task>

<task type="auto">
  <name>Task 7: REQUIREMENTS.md amendment for XCUT-05 + Phase 8 SC-5 + CONTEXT softening (M-2 round 2)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/08-insights-weekly-recap-learning-release-prep/08-CONTEXT.md</files>
  <action>
    Per CONTEXT &lt;requirements_amendment&gt; section: amend three places.

    1. In `.planning/REQUIREMENTS.md` line 114 (planner-verified at revision round 2: exact current text is `- [ ] **XCUT-05**: Application is code-signed and notarized (macOS) and signed (Windows OV cert)`):
       Replace with:
       `- [ ] **XCUT-05**: Application is code-signed and notarized (macOS) at v1 release. Windows OV signing applies at **GA release**; initial v1 tester build ships Windows-unsigned with documented SmartScreen warning. OV cert acquired and Windows signing wired after tester usage period (user-confirmed staged approach 2026-05-17).`

    2. In `.planning/ROADMAP.md` Phase 8 Success Criteria (lines 211–212): append clarification to SC-5:
       `5. macOS build passes notarization; Windows build is OV-signed and installs cleanly past SmartScreen reputation seed *at GA release*. Initial v1 tester build ships Windows-unsigned (amended per CONTEXT 2026-05-17 staged signing decision).`

    3. (M-2 round 2) In `.planning/phases/08-insights-weekly-recap-learning-release-prep/08-CONTEXT.md` line 106 (or wherever the "auto-updater wraps this" paragraph lives — grep `runs the previous installer` to find it): SOFTEN the promise. Replace:
       `Auto-updater wraps this: pre-update snapshot, post-migration verify, rollback path runs the previous installer if needed.`
       With:
       `Auto-updater wraps this: pre-update snapshot, post-migration verify. Rollback path restores the DB from the snapshot via `runMigrationsWithBackup` + `restoreFromBackup`. NOTE: electron-updater does not retain previous installers; if the new binary's migration is unrecoverable, the user must manually download a prior release from GitHub Releases and reinstall. RELEASE-RUNBOOK §9 documents this path.`

       Closes peer-review M-2 round 2 — CONTEXT no longer promises a capability electron-updater does not deliver natively.

    Do NOT alter any other requirement. Do NOT renumber. Commit alongside Task 6 deliverables.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); if(!r.includes('applies at **GA release**')){ console.error('XCUT-05: missing GA-release anchor'); process.exit(1) } if(!r.includes('tester build ships Windows-unsigned')){ console.error('XCUT-05: missing tester-unsigned anchor'); process.exit(1) } if(r.includes('and signed (Windows OV cert)')){ console.error('Old XCUT-05 text still present'); process.exit(1) } const c=fs.readFileSync('.planning/phases/08-insights-weekly-recap-learning-release-prep/08-CONTEXT.md','utf8'); if(c.includes('runs the previous installer')){ console.error('CONTEXT M-2: stale promise still present'); process.exit(1) }"</automated>
  </verify>
  <done>Amendment text present (H-2 round 2: anchored on two short invariant substrings, NOT a date-bearing full line); CONTEXT M-2 softening applied; old XCUT-05 wording removed; no date-decoration drift breaks the verify.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Phase 8 happy-path E2E — the 7-step "I used Aria today" spec + migration-failure restore E2E (M-3 round 2)</name>
  <files>tests/integration/phase8-happy-path.spec.ts, tests/fixtures/999_force_fail.sql</files>
  <behavior>
    - Step 1: Connect Google account (stubbed OAuth).
    - Step 2: Connect Outlook account (stubbed; Phase 5 OAuth harness).
    - Step 3: Ingest mail; briefing renders with sections + (if 14d seeded) insights section.
    - Step 4: Draft an email; approve; mocked Gmail send returns 200; send_log row written.
    - Step 5: Schedule a meeting via NL command; approve; mocked Google Calendar accepts; calendar_action_log row.
    - Step 6: Paste a transcript; approve task batch; mocked Todoist push returns task IDs; meeting_action_task_link rows.
    - Step 7: Run RAG query → assert NOT 'Q&A service not ready'; click citation.
    - Step 8: view weekly recap, edit one section, verify weekly_recap_section_edit row written.
    - Step 9 (M-3 round 2 — concrete migration-failure fixture): create `tests/fixtures/999_force_fail.sql` containing a deliberately failing statement (e.g. `CREATE TABLE force_fail (id INTEGER, id INTEGER);` — duplicate column raises sqlite syntax error). The E2E harness mounts this fixture into the runner's queue ONLY when `ARIA_E2E_FORCE_MIGRATION_FAIL=true` is set in the spawn env. The fixture is NEVER imported by `src/main/db/migrations/embedded.ts` (assertion: grep that `embedded.ts` does not reference `999_force_fail`). Spec sets the env, relaunches app, asserts: (a) recovery dialog appears, (b) clicking "Restore" invokes restoreFromBackup, (c) after relaunch DB content matches pre-migration snapshot, (d) prior approval/send_log/recap rows present.
    - Whole-spec assertion: action_audit_log VIEW reflects every action taken.
  </behavior>
  <action>
    Use Playwright `_electron` against the packaged build. OAuth + LLM stubs via `ARIA_TEST_MODE=true`.

    **M-3 round 2 — concrete failing-migration mechanism:**

    1. Create `tests/fixtures/999_force_fail.sql` with intentionally invalid SQL (duplicate column declaration is the cleanest — sqlite raises a clear syntax error, no destructive side-effects). File path is OUTSIDE `src/main/db/migrations/` so the prod runner never sees it.

    2. In the test boot harness (a thin wrapper around the prod app entry), check `process.env.ARIA_E2E_FORCE_MIGRATION_FAIL === 'true'`. If true, BEFORE invoking `runMigrationsWithBackup`, push the fixture SQL into the runner's pending-migration list under version 999 via a test-only injection function (`__test__injectMigration(version, sql)` exported from `runner.ts` and gated by `NODE_ENV === 'test'` or `process.env.ARIA_E2E_FORCE_MIGRATION_FAIL === 'true'`).

    3. Spec is unrunnable without the env — packaged production builds NEVER hit the fixture because `embedded.ts` does not include it.

    4. Add the grep assertion at the top of Step 9 in the spec file: `expect(fs.readFileSync('src/main/db/migrations/embedded.ts', 'utf8')).not.toMatch(/999_force_fail/)` — proof that the fixture cannot leak into prod.

    Cross-reference Task 9 (RELEASE-RUNBOOK.md) — packaged-build migration-failure path is separately exercised by a runbook drop-in step (uses a different known-bad migration for the manual runbook test).
  </action>
  <verify>
    <automated>pnpm test:e2e --grep phase8-happy-path</automated>
  </verify>
  <done>Spec runs against packaged build (test-mode dev build for Step 9) and passes; fixture path verified absent from embedded.ts; M-3 round 2 concrete fixture mechanism in place.</done>
</task>

<task type="auto">
  <name>Task 9: docs/RELEASE-RUNBOOK.md + production smoke command set</name>
  <files>docs/RELEASE-RUNBOOK.md</files>
  <action>
    Sections:

    1. **Pre-flight checks** (env vars present, branch is master, tests green, AND the Mode-B real-Ollama RAG_ASK smoke green: `ARIA_E2E_REAL_LLM=true pnpm test:e2e --grep rag-ask-smoke` — REQUIRED before any `git tag v*` push).
    2. **Tag + publish:** `git tag v1.0.0 && pnpm run release`. Document the `--prerelease` flag for tester channel.
    3. **macOS notarization smoke:** `spctl --assess --verbose=4 build/mac-arm64/Aria.app` → must print "accepted, source=Notarized Developer ID".
    4. **Windows tester ship:** unsigned NSIS installer to GH Release; document SmartScreen warning in tester onboarding.
    5. **AV submission portals.**
    6. **SmartScreen reputation seeding strategy.**
    7. **Channel flip:** tester → stable.
    8. **GH token scope** (PAT classic, `repo` scope).
    9. **Rollback procedure (M-2 round 2 — match softened CONTEXT):** if a release crashes for users: (a) mark the GH Release as `draft` immediately (prevents new users from downloading the broken build); (b) electron-updater will NOT auto-rollback already-installed users — they must manually download the prior version from GitHub Releases and reinstall. The in-app DB restore (runMigrationsWithBackup snapshot) recovers user data after manual reinstall of the prior binary. Document the exact user-facing flow: "If Aria fails to launch after auto-update, download the prior installer from https://github.com/{owner}/{repo}/releases, uninstall the broken version, install the prior version. Your data is preserved by the pre-migration snapshot."
    10. **EV-cert deferral note.**
    11. **Migration-failure packaged-build smoke (manual):** runbook step describing how to test the migration-failure path against a real packaged build — drop a known-bad SQL file into a special dev-only location and observe the recovery dialog. Separate from the Task 8 dev-build E2E.
  </action>
  <verify>
    <automated>test -f docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "spctl --assess" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "SmartScreen" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "channel" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "notarytool" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "AV submission" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "rollback" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "ARIA_E2E_REAL_LLM" docs/RELEASE-RUNBOOK.md &amp;&amp; grep -q "manually download the prior" docs/RELEASE-RUNBOOK.md</automated>
  </verify>
  <done>Runbook covers all 11 sections; M-2 round 2 rollback narrative present and matches CONTEXT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    1. electron-builder produces a macOS notarized DMG.
    2. spctl --assess prints "accepted, source=Notarized Developer ID".
    3. Windows NSIS installer built unsigned; SmartScreen warning expected and documented.
    4. RAG_ASK smoke (Mode A + Mode B) + Phase 8 happy-path E2E (incl. migration-failure restore via 999_force_fail fixture) all green.
  </what-built>
  <how-to-verify>
    1. From clean working tree on master: `pnpm install && pnpm run build && pnpm run release:mac`.
    2. `spctl --assess --verbose=4 build/mac-arm64/Aria.app` — assert output contains "Notarized Developer ID".
    3. `open build/Aria-*.dmg` → drag to Applications → launch → assert NO Gatekeeper warning.
    4. Install Windows NSIS exe in a clean VM → confirm SmartScreen warning → proceed → launch → assert briefing renders.
    5. CI run of `pnpm test:e2e` green (RAG_ASK Mode A + happy-path Step 9 migration-failure restore).
    6. Run `ARIA_E2E_REAL_LLM=true pnpm test:e2e --grep rag-ask-smoke` against running Ollama (Mode B pre-tag gate).
    7. Confirm autoUpdater check in dev tools → "no update available".
  </how-to-verify>
  <resume-signal>Type "release verified" if all checks pass; OR list specific failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Releases → installed app | electron-updater verifies signature; Win unsigned-tester has elevated MITM risk per CONTEXT |
| Apple notary service → CI | App-specific password in env; never logged |
| Migration runner → file system | Backup file written under userData |
| RAG_ASK renderer → IPC | factory returns null pre-unlock; post-unlock LlmInvocation runs in main process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Tampering | Rogue latest.yml from MITM (Win unsigned tester) | accept | Documented in tester onboarding; OV signing for GA closes this |
| T-08-17 | Information Disclosure | Apple creds leaking via build logs | mitigate | electron-builder + @electron/notarize redact secrets |
| T-08-18 | Tampering | Migration backup readable to other OS users | mitigate | Inherits userData perms; chmod 600 on Linux best-effort |
| T-08-19 | Denial of Service | Migration failure leaves DB unrecoverable | mitigate | runMigrationsWithBackup + restore helper (Task 4) + Task 4a extraction prevents recursion |
| T-08-20 | Spoofing | RAG_ASK pre-unlock | accept | factory returns null pre-unlock |
| T-08-21 | Information Disclosure | GH_TOKEN leaking in CI logs | mitigate | Documented as GitHub Secrets |
| T-08-23 | Tampering | 999_force_fail fixture leaking into prod build | mitigate | Fixture lives in tests/fixtures/, NEVER referenced by embedded.ts; grep assertion in Step 9 |
</threat_model>

<verification>
**Phase-level gate (this plan is the last):**

1. All unit + integration tests pass: `pnpm test`.
2. All grep ratchets pass: `pnpm run lint:guard`.
3. Phase 8 happy-path E2E green (incl. Step 9 migration-failure restore).
4. RAG_ASK smoke green; manually open /ask in the running app; receive cited answer.
5. spctl --assess prints Notarized.
6. Windows NSIS install completes on clean VM.
7. scheduler.cronRegistry.size invariant: after Phase 8, expected size = `gmail-sync + calendar-sync + briefing + insights-nightly + recap-monday + learning-nightly` (verify the delta is exactly +3 over pre-Phase-8 size — accommodates Phase 5 Outlook-sync cron variant per MEMORY `project_aria_phase5_executed`).
8. action_audit_log VIEW returns rows after a full happy-path run.
9. REQUIREMENTS.md amendment present; CONTEXT M-2 softening applied.
10. Pre-migration backup confirmed via 999_force_fail fixture path (Step 9).
11. Factory log-line ratchet: parse main-process log file after a fresh RAG_ASK and verify exactly one `factory.constructed` entry.

**Sentinel cross-references (NOT in scope of this plan):**
- (W-2) XCUT-02 OWNED by Plan 08-03 Task 6.
- Phase 3 CR-01 gate fail-OPEN — still open per MEMORY.
- Phase 5 Outlook send chokepoint — VIEW arm 1 now reads `sl.provider` (08-02 Task 1 H-4 fix).
</verification>

<success_criteria>
- XCUT-04: pre-migration backup + restore demonstrated.
- XCUT-05 (amended): macOS notarized; Windows tester-unsigned with documented runbook for GA OV signing.
- RAG-02: AnswerService wired; RAG_ASK returns real cited answers; factory log-line ratchet asserts wire is live.
- All 4 Phase 8 streams shipped: insights (08-01) + recap (08-02) + learning (08-03) + release (08-04).
- B-1/B-2/B-3 round 2 fixes landed; H-2/H-3/H-4 + M-1/M-2/M-3/M-4 round 2 fixes landed.
- Phase 8 final checkpoint verified by human.
</success_criteria>

<output>
After completion, write `.planning/phases/08-insights-weekly-recap-learning-release-prep/08-04-SUMMARY.md` covering:
- Phase 7 closure (AnswerService factory + LlmInvocation + smoke + log-line ratchet)
- Migration extraction from openDb (Task 4a — B-1 round 2)
- electron-updater wiring + UpdatesSection
- Migration backup hook + restore atomicity + expectedDrops map (H-3 round 2)
- electron-builder config delta
- REQUIREMENTS amendment + CONTEXT softening (H-2 + M-2 round 2)
- Release runbook + remaining manual steps
- 999_force_fail fixture mechanism (M-3 round 2)
- v1 ship-readiness checklist
- Cross-phase open items: Phase 3 CR-01, Phase 9 product UI

Then write `.planning/phases/08-insights-weekly-recap-learning-release-prep/08-SUMMARY.md` (phase-level).
</output>
</content>
</invoke>