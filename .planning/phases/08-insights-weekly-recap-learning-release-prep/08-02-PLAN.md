---
phase: 08-insights-weekly-recap-learning-release-prep
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/main/db/migrations/128_phase8.sql
  - src/main/db/migrations/embedded.ts
  - src/main/recap/audit-view.ts
  - src/main/recap/audit-view.integration.test.ts
  - src/main/recap/schema.ts
  - src/main/recap/canonical.ts
  - src/main/recap/canonical.test.ts
  - src/main/recap/generate.ts
  - src/main/recap/generate.test.ts
  - src/main/recap/persist.ts
  - src/main/recap/persist.test.ts
  - src/main/recap/schedule.ts
  - src/main/recap/schedule.test.ts
  - src/main/recap/export/docx.ts
  - src/main/recap/export/docx.test.ts
  - src/main/recap/export/pdf.tsx
  - src/main/recap/export/pdf.test.ts
  - src/main/ipc/recap.ts
  - src/main/ipc/index.ts
  - src/shared/ipc-contract.ts
  - src/preload/index.ts
  - src/renderer/features/recap/RecapScreen.tsx
  - src/renderer/features/recap/RecapScreen.test.tsx
  - src/renderer/features/recap/RecapEditor.tsx
  - src/renderer/App.tsx
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [RECAP-01, RECAP-02, RECAP-03, RECAP-04]

must_haves:
  truths:
    - "A SQL VIEW `action_audit_log` exists and unions send_log + calendar_action_log + meeting_action_task_link/todoist_task + approval(rejected)"
    - "Monday-morning cron generates a weekly_recap row covering Mon–Sun of the prior week"
    - "Recap has 5 sections: Meetings held / Actions / Wins / What's coming / What Aria did"
    - "'What Aria did' is rendered DIRECTLY from action_audit_log rows (the trust anchor); LLM narrative paragraph sits ABOVE the list"
    - "User can edit any recap section in a TipTap rich-text editor; edits persist; finalize writes section diffs into a queue Stream 3 will consume"
    - "User can export the recap as DOCX or PDF; both formats round-trip a single RecapCanonical zod-validated JSON shape"
    - "Recap LLM narrative never references an action that does not exist in the structured audit list (two-pass cross-validation)"
  artifacts:
    - path: "src/main/db/migrations/128_phase8.sql"
      provides: "VIEW action_audit_log + weekly_recap + weekly_recap_section tables (appended to Stream 1 migration)"
      contains: "CREATE VIEW action_audit_log"
    - path: "src/main/recap/audit-view.ts"
      provides: "pagination + filter over the VIEW; used by recap generator + RECAP_LIST_AUDIT IPC"
      exports: ["readActionAuditWindow", "type ActionAuditRow"]
    - path: "src/main/recap/canonical.ts"
      provides: "RecapCanonical zod schema = single source of truth for editor ↔ exporters"
      exports: ["RecapCanonicalSchema", "type RecapCanonical", "tiptapJsonToCanonical", "canonicalToTiptapJson"]
    - path: "src/main/recap/generate.ts"
      provides: "weekly orchestrator: gather → 2-pass narrate → persist"
      exports: ["generateWeeklyRecap"]
    - path: "src/main/recap/schedule.ts"
      provides: "Monday 08:00 cron; mirrors briefing/schedule.ts"
      exports: ["scheduleWeeklyRecap", "stopWeeklyRecapSchedule"]
    - path: "src/main/recap/export/docx.ts"
      provides: "RecapCanonical → DOCX Buffer via dolanmiu/docx"
    - path: "src/main/recap/export/pdf.tsx"
      provides: "RecapCanonical → PDF Buffer via @react-pdf/renderer"
    - path: "src/renderer/features/recap/RecapScreen.tsx"
      provides: "reachable /recap route; lists past recaps + opens RecapEditor"
    - path: "src/renderer/features/recap/RecapEditor.tsx"
      provides: "TipTap-based section editor + Export DOCX/PDF buttons + Finalize button"
  key_links:
    - from: "src/main/recap/generate.ts"
      to: "src/main/recap/audit-view.ts"
      via: "audit log rows are the STRUCTURED list passed to narrative LLM; narrative.actionRefs[] must subset the list"
      pattern: "readActionAuditWindow\\("
    - from: "src/main/recap/export/docx.ts"
      to: "src/main/recap/canonical.ts"
      via: "exporter reads canonical, NEVER HTML (per research Pitfall 7)"
      pattern: "RecapCanonical"
    - from: "src/renderer/features/recap/RecapEditor.tsx"
      to: "canonical.ts"
      via: "editor.getJSON() → tiptapJsonToCanonical(); finalize commits canonical via IPC"
      pattern: "tiptapJsonToCanonical"
    - from: "src/renderer/App.tsx"
      to: "RecapScreen"
      via: "route `/recap` added — NOT orphan"
      pattern: "/recap"
    - from: "src/main/lifecycle/scheduler.ts"
      to: "recap-monday"
      via: "scheduler.cronRegistry size grows by exactly 1 after this plan"
      pattern: "recap-monday"
---

<objective>
Ship Stream 2: a weekly recap surface stitched directly from a unified `action_audit_log` SQL VIEW (the trust anchor), with LLM-written narrative above the verbatim action list, editable in TipTap, exportable to DOCX + PDF via a single canonical JSON shape.

Purpose: Earn RECAP-01..04. "What Aria did this week" is the audit-section that compounds user trust week-over-week — it must be unfakeable, which is why CONTEXT mandates the list (not the narrative) as truth and research mandates a VIEW over the existing three audit tables.

Output:
- Migration 128 extension: VIEW + weekly_recap tables
- Monday cron + recap generator with two-pass LLM cross-validation (research Pitfall 6)
- RecapCanonical zod schema as the single interchange between TipTap editor and both exporters
- /recap route + RecapScreen + RecapEditor — fully reachable from App.tsx
- DOCX + PDF export round-trip tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-CONTEXT.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-RESEARCH.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-01-PLAN.md

@src/main/briefing/schedule.ts
@src/main/briefing/generate.ts
@src/main/db/migrations/embedded.ts
@src/main/llm/routingLog.ts

<interfaces>
<!-- Existing tables the VIEW unions. Verify column names against embedded.ts BEFORE writing the SQL. -->

From migration 008/009 (Phase 3) — `send_log`:
- `ts`, `provider`, `recipients_json`, `subject`, `ok`, `error`, `provider_msg_id`, `approval_id`

From migration 010 (Phase 4) — `calendar_action_log`:
- `created_at`, `phase` (CHECK enum is exactly the 5-value set IN ('proposed','pre_write','post_write','failed','override') — planner-verified against migrations/010_calendar_writeback.sql line 119), `event_id`, `recurring_scope`, `before_json`, `after_json`, `google_etag`, `google_error`, `approval_id`

From migration 125 (Phase 6) — `meeting_action_task_link` + `todoist_task`:
- `mal.created_at`, `mal.task_id`, `mal.remote_id`
- `tt.content`, `tt.project_name`, `tt.is_completed`

From migration 006 — `approval`:
- `id`, `kind`, `state`, `updated_at`, `subject`, `rejection_reason`

(EXECUTOR: open embedded.ts and confirm EXACT column names BEFORE writing the VIEW SQL. Per MEMORY `feedback-plan-schema-invention`, do not assume.)

From research §Pattern 2 — full VIEW SQL is provided in the research doc, lines 226–305. Copy it; adjust column names if grep reveals drift.

From research §Pitfall 7 — RecapCanonical sits between editor and exporters. Never `editor.getHTML()` as the intermediate.

From `docx` (dolanmiu) — Document/Section/Paragraph/TextRun tree; `Packer.toBuffer(doc)` returns a Node Buffer.

From `@react-pdf/renderer` — `Document/Page/View/Text` React components; `pdf(<Document/>).toBuffer()` returns a Promise<Buffer>.

From TipTap — `@tiptap/react` `useEditor({ extensions: [StarterKit, ...] })`; `editor.getJSON()` returns the ProseMirror JSON tree.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify base-table columns + author VIEW action_audit_log + weekly_recap tables</name>
  <files>src/main/db/migrations/128_phase8.sql, src/main/db/migrations/embedded.ts, src/main/recap/audit-view.ts, src/main/recap/audit-view.integration.test.ts</files>
  <behavior>
    - Test 1: VIEW selects from send_log returning kind='email_send'; payload_json parses to object with recipients/subject/ok/error.
    - Test 2 (B-1 — explicit enum coverage): VIEW's calendar_action_log UNION arm filters `WHERE phase IN ('post_write','failed','override')`. Test asserts ALL 5 phase values explicitly:
        - 'proposed' rows EXCLUDED (covered by Test 2b)
        - 'pre_write' rows EXCLUDED
        - 'post_write' rows INCLUDED
        - 'failed' rows INCLUDED
        - 'override' rows INCLUDED
        Test inserts one row per phase value into calendar_action_log, then asserts VIEW returns exactly 3 (post_write + failed + override).
    - Test 2b (B-1): seed calendar_action_log with phase='proposed' rows only -> VIEW returns ZERO rows from this arm. Asserts the 'proposed' exclusion specifically (the case missed in the original 4-value enum read).
    - Test 3: VIEW selects meeting_action_task_link joined to todoist_task returning kind='task_pushed'.
    - Test 4: VIEW selects approval rows where state='rejected' returning kind='approval_declined'.
    - Test 5: VIEW orders by occurred_at DESC; LIMIT 200 returns &lt; 200ms on seeded 5k-row fixture (HARD threshold — research Open Question #1 perf assertion).
    - Test 5b (S-1): same query on 50k-row fixture executes in &lt; 1000ms (WARN threshold — not blocking; catches 6-months-of-active-use regressions). Test logs warning if exceeded; only fails if &gt; 5000ms.
    - Test 6 (B-2.1 — per-arm count parity, expanded): `verify:audit-view` script asserts FOR EACH of the 4 UNION arms INDEPENDENTLY:
        - arm 1 (email_send):       `SELECT COUNT(*) FROM send_log` == `SELECT COUNT(*) FROM action_audit_log WHERE kind='email_send'`
        - arm 2 (calendar_change):  `SELECT COUNT(*) FROM calendar_action_log WHERE phase IN ('post_write','failed','override')` == `SELECT COUNT(*) FROM action_audit_log WHERE kind='calendar_change'`
        - arm 3 (task_pushed):      `SELECT COUNT(*) FROM meeting_action_task_link JOIN todoist_task ON ...` == `SELECT COUNT(*) FROM action_audit_log WHERE kind='task_pushed'`
        - arm 4 (approval_declined): `SELECT COUNT(*) FROM approval WHERE state='rejected'` == `SELECT COUNT(*) FROM action_audit_log WHERE kind='approval_declined'`
        Each arm assertion fails independently with a clear message identifying which arm drifted. (Original combined assertion replaced.)
    - Test 6b (B-2.2 — PRAGMA snapshot ratchet): script captures `PRAGMA table_info(<base_table>)` for each of the 4 source tables (`send_log`, `calendar_action_log`, `meeting_action_task_link`, `approval` — note: `todoist_task` joined in arm 3 also captured) and compares against a stored JSON fixture at `scripts/fixtures/audit-view-table-info.json`. Ratchet FAILS if any source table has new columns NOT in the snapshot — forces planner attention when schema drifts (a future migration adding a column to send_log must update the snapshot AND re-review the VIEW SQL). Bootstrap: first run writes the fixture; subsequent runs compare.
    - Test 7: weekly_recap table: id PK, iso_week TEXT UNIQUE, week_start_ymd, generated_at, finalized_at NULLABLE, canonical_json TEXT.
    - Test 8: weekly_recap_section_edit table: id PK, recap_id FK, section_key TEXT, before_text, after_text, category TEXT NULL, created_at (consumed by Stream 3).
  </behavior>
  <action>
    Append to `128_phase8.sql` (Stream 1 wrote the insights table; this task adds VIEW + 2 tables in the SAME migration file — migration runner already applies file atomically).

    First read `embedded.ts` and confirm column names from base tables. The research VIEW SQL (lines 226–305) is the template; substitute actual column names where they differ. Per research Pitfall 7-adjacent caution: hardcoded `'gmail'` in arm 1 must be replaced with `sl.provider` so Phase 5 Outlook send slots in without VIEW changes (research Assumption A7).

    Build `audit-view.ts` with `readActionAuditWindow(db, { fromIso, toIso, limit? })` returning typed `ActionAuditRow[]`.

    The verify-assertion (tests 6 + 6b) runs in tests + at startup as a `pnpm run verify:audit-view` script ratchet. Add to package.json:
    ```
    "verify:audit-view": "node scripts/verify-audit-view.mjs"
    ```
    And extend `lint:guard` (created in Plan 08-01 Task 4) to append ` && node scripts/verify-audit-view.mjs` — final composite shape documented in 08-01 Task 4 action. Use `npm pkg set` or hand-edit; confirm by grep `verify-audit-view` appears in the `lint:guard` value before commit (W-1 anchor).

    Create `scripts/fixtures/audit-view-table-info.json` on first run (test 6b bootstrap).
  </action>
  <verify>
    <automated>pnpm vitest run src/main/recap/audit-view.integration.test.ts &amp;&amp; pnpm run verify:audit-view</automated>
  </verify>
  <done>VIEW applies idempotently; all 10 tests pass (Tests 1, 2, 2b, 3, 4, 5, 5b, 6, 6b, 7, 8); 5k perf assertion under 200ms HARD + 50k under 1000ms WARN; verify-script catches a deliberately-dropped UNION arm per-arm AND a deliberately-added source-table column.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RecapCanonical zod schema + TipTap mappers</name>
  <files>src/main/recap/canonical.ts, src/main/recap/canonical.test.ts, src/main/recap/schema.ts</files>
  <behavior>
    - Test 1: RecapCanonicalSchema validates a 5-section recap shape (meetings/actions/wins/upcoming/whatAriaDid).
    - Test 2: tiptapJsonToCanonical(json) extracts paragraph/heading/list/mark structure into canonical blocks.
    - Test 3: canonicalToTiptapJson(canonical) round-trips (test 2 input == test 3 output for a fixture).
    - Test 4: schema rejects extra top-level sections (closed shape).
    - Test 5: whatAriaDid section contains `auditRowRefs: string[]` (audit row IDs) + `narrative: string` (LLM paragraph); no other shape.
  </behavior>
  <action>
    Per research Pitfall 7: a single canonical JSON shape sits between editor and exporters. Sections: `meetings`, `actions`, `wins`, `upcoming`, `whatAriaDid`. Each section is `{ heading: string; blocks: Block[] }` where Block = `{ kind: 'paragraph' | 'bullet_list' | 'numbered_list'; text?: string; items?: string[] }`. whatAriaDid extends with `auditRowRefs` + `narrative`.

    TipTap mappers convert ProseMirror JSON ↔ canonical. Limited extension set in editor (StarterKit only — no images, no tables in v1) so the surface is small.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/recap/canonical.test.ts</automated>
  </verify>
  <done>Round-trip passes for representative fixture; closed schema rejects unknowns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: generateWeeklyRecap orchestrator with two-pass narrative cross-validation</name>
  <files>src/main/recap/generate.ts, src/main/recap/generate.test.ts, src/main/recap/persist.ts, src/main/recap/persist.test.ts</files>
  <behavior>
    - Test 1: generateWeeklyRecap(db, { isoWeek, weekStartYmd, router, logger }) gathers source data: calendar events (meetings), approvals (actions), sent mail decisions, next-week events, audit log rows.
    - Test 2: PASS 1 — narrative LLM call receives STRUCTURED audit list only; returns `{ narrative, actionRefs: string[] }`.
    - Test 3: PASS 2 — validates every `actionRefs[i]` exists in the audit list IDs; if not, narrative truncated and a `hallucinationDetected: true` flag set (research Pitfall 6).
    - Test 4: persist.ts upserts weekly_recap row keyed on iso_week.
    - Test 5: same iso_week regeneration replaces canonical_json (idempotent).
    - Test 6: routing log entries: source='recap-narrative' and source='recap-section-prose' as appropriate; prompt_hash only.
    - Test 7: redactObject applied to all logger calls in this module.
  </behavior>
  <action>
    Build orchestrator following Phase 2 `briefing/generate.ts` shape. Use `p-queue(concurrency: 1)`. Use `generateObject` with section-specific zod schemas; the `whatAriaDid.narrative` call gets the STRUCTURED audit list (already IDs + summaries — no raw recipient PII; PII is in send_log.recipients which already-redacted helpers expose, so apply redactAllPii defensively).

    Persistence in persist.ts: `saveWeeklyRecap(db, { isoWeek, weekStartYmd, canonical })` upserts; `finalizeRecap(db, { isoWeek, sectionEdits })` writes the finalized_at timestamp and produces section_edit rows (consumed by Stream 3).
  </action>
  <verify>
    <automated>pnpm vitest run src/main/recap/generate.test.ts src/main/recap/persist.test.ts</automated>
  </verify>
  <done>All 7 tests pass; hallucination detection cuts narratives referencing non-existent actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: scheduleWeeklyRecap (Monday 08:00 cron)</name>
  <files>src/main/recap/schedule.ts, src/main/recap/schedule.test.ts</files>
  <behavior>
    - Test 1: scheduleWeeklyRecap registers `'recap-monday'` in scheduler.cronRegistry; size +1.
    - Test 2: `_lastFiredIsoWeek` dedupe — two fires same ISO week run once.
    - Test 3: multi-week suspend does NOT back-fire (only most recent Monday triggers on resume — and only if Monday is the current day; otherwise next Monday).
    - Test 4: cron expr `'0 8 * * 1'` (Monday 08:00 local TZ).
    - Test 5: stopWeeklyRecapSchedule removes the entry on app shutdown.
  </behavior>
  <action>
    Copy `src/main/briefing/schedule.ts` (and verify against Plan 08-01 Task 6 — `insights/schedule.ts` is the same shape — copy whichever is cleaner). Module-level `_lastFiredIsoWeek` (NOT _lastFiredYmd — week-scoped dedupe). Reuse `registerLifecycleCallbacks`. Wire into the boot path next to scheduleBriefing + scheduleInsights.

    Provide `computeIsoWeek(tz, now)` helper (e.g. `"2026-W20"`) — Intl-based.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/recap/schedule.test.ts</automated>
  </verify>
  <done>cronRegistry.size invariant holds; Monday 8am local fires recap generation; multi-week suspend does not back-fire.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: DOCX + PDF exporters from RecapCanonical (round-trip tests)</name>
  <files>src/main/recap/export/docx.ts, src/main/recap/export/docx.test.ts, src/main/recap/export/pdf.tsx, src/main/recap/export/pdf.test.ts, package.json, pnpm-lock.yaml</files>
  <behavior>
    - Test 1 DOCX: exportRecapDocx(canonical) returns a Buffer; opening with `mammoth` (dev-dep for tests; OR `docx`'s own parser) extracts plain text matching every section heading + the audit list.
    - Test 2 DOCX: bullet lists, headings, paragraphs all render distinctly (verify via OOXML structure assertion or mammoth markdown output).
    - Test 3 PDF: exportRecapPdf(canonical) returns a Buffer; pdf-parse extracts text containing every section heading + the audit list verbatim.
    - Test 4 PDF: empty section gracefully renders as "(none)" placeholder (BRIEF-06-style graceful degrade).
    - Test 5 — fixture round-trip: same canonical → DOCX, → PDF; both contain the SAME audit row strings (no format-specific drops, per research Pitfall 7).
    - Test 6 — no HTML intermediate: grep test asserts neither exporter imports/calls `editor.getHTML()` or any DOM-HTML lib.
  </behavior>
  <action>
    Add deps: `docx@^9.x`, `@react-pdf/renderer@^4.x`, `mammoth` (devDep for test parse), `pdf-parse` (devDep). Run `pnpm install` then `npm view <pkg> version` to lock exact versions per research §Standard Stack note.

    docx.ts: imperative tree — Document → Section → Paragraph(s) via TextRun, HeadingLevel.HEADING_2 per section heading, `bullet: { level: 0 }` for bullet items. `Packer.toBuffer(doc)`.

    pdf.tsx: JSX `<Document><Page><View><Text>...</Text></View></Page></Document>`. Use default fonts (Helvetica/Times) per research Open Q #7 — no custom font embedding in v1.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/recap/export/docx.test.ts src/main/recap/export/pdf.test.ts</automated>
  </verify>
  <done>Both exporters produce parseable artifacts containing every canonical block; fixture round-trip identical across formats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: IPC handlers + preload (RECAP_LIST, RECAP_GET, RECAP_REGENERATE, RECAP_SAVE_EDITS, RECAP_FINALIZE, RECAP_EXPORT_DOCX, RECAP_EXPORT_PDF, RECAP_LIST_AUDIT)</name>
  <files>src/main/ipc/recap.ts, src/main/ipc/index.ts, src/shared/ipc-contract.ts, src/preload/index.ts</files>
  <behavior>
    - Test 1: RECAP_LIST returns recent recaps (paginated).
    - Test 2: RECAP_GET returns canonical JSON for an iso_week.
    - Test 3: RECAP_REGENERATE triggers generateWeeklyRecap for the given iso_week; idempotent.
    - Test 4: RECAP_SAVE_EDITS validates canonical via zod; persists.
    - Test 5: RECAP_FINALIZE emits section diffs (before/after) to weekly_recap_section_edit for Stream 3 consumption.
    - Test 6: RECAP_EXPORT_DOCX / RECAP_EXPORT_PDF stream a Buffer to a `dialog.showSaveDialog` path (reuse `BACKUP_CREATE` pattern from Phase 1 `src/main/ipc/backup.ts`).
    - Test 7: RECAP_LIST_AUDIT exposes readActionAuditWindow paginated for UI inspection.
  </behavior>
  <action>
    Add CHANNELS to ipc-contract.ts with DTOs. Implement `registerRecapHandlers(ipcMain, { logger, dbHolder, router })`. Register in ipc/index.ts with the same skip-pattern as RAG channels.

    Save-dialog reuse: confirm shape by reading `src/main/ipc/backup.ts` (BACKUP_CREATE). Mirror.

    Expose on preload as `window.api.recap.*`.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/ipc/recap.test.ts</automated>
  </verify>
  <done>All 7 channels round-trip; export channels return path of saved file; finalize emits diffs to the Stream 3-consumable table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: RecapScreen + RecapEditor (TipTap) + /recap route — reachable</name>
  <files>src/renderer/features/recap/RecapScreen.tsx, src/renderer/features/recap/RecapScreen.test.tsx, src/renderer/features/recap/RecapEditor.tsx, src/renderer/App.tsx, package.json</files>
  <behavior>
    - Test 1: /recap route renders RecapScreen listing past recaps (most recent first).
    - Test 2: clicking a recap opens RecapEditor with TipTap initialized from canonicalToTiptapJson.
    - Test 3: edits round-trip — type a change → editor.getJSON() → tiptapJsonToCanonical → SAVE_EDITS IPC.
    - Test 4: "Export DOCX" + "Export PDF" buttons trigger respective IPC and show toast on completion.
    - Test 5: "Finalize" button calls RECAP_FINALIZE; section becomes read-only.
    - Test 6 (reachability — L-04-04): grep asserts `RecapScreen` is referenced by App.tsx (route registration); ensure nav from main sidebar/menu also reaches /recap.
    - Test 7: "What Aria did" section UI renders narrative paragraph ABOVE the verbatim list of audit rows; list rows show kind + timestamp + summary deterministically.
  </behavior>
  <action>
    Add deps: `@tiptap/core`, `@tiptap/react`, `@tiptap/starter-kit` (research §Standard Stack supporting; verify versions via `npm view`).

    Build RecapEditor with `useEditor({ extensions: [StarterKit] })`. Read-only mode after finalize. Per-section TipTap editor instances to make per-section diff trivial.

    Add `/recap` route in App.tsx (find existing route registration site — likely `<Routes>` block). Add nav entry alongside Briefing/Tasks/Settings.
  </action>
  <verify>
    <automated>pnpm vitest run src/renderer/features/recap/RecapScreen.test.tsx</automated>
  </verify>
  <done>/recap reachable from sidebar; recap editable; both exports trigger save dialog; finalize locks editing; trust-anchor list visually distinct from narrative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer canonical JSON → main IPC | RECAP_SAVE_EDITS / RECAP_FINALIZE — zod-validated before persist |
| audit log → narrative LLM | structured rows (already redacted at write-time per LEARN-02 invariant elsewhere) |
| canonical → DOCX/PDF exporter | both exporters run in main process; never round-trip via HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | Narrative LLM hallucinating actions | mitigate | Two-pass cross-validation (Task 3 test 3); narrative truncated if actionRefs[] not subset of audit IDs |
| T-08-07 | Information Disclosure | Recap export file written to disk | mitigate | dialog.showSaveDialog (user picks path); no auto-save outside userData |
| T-08-08 | Tampering | VIEW silently dropping rows when base table column drifts | mitigate | verify:audit-view script asserts row-count equality per arm |
| T-08-09 | Spoofing | Renderer sending forged canonical JSON | mitigate | Zod schema in RECAP_SAVE_EDITS handler |
| T-08-10 | Information Disclosure | LLM narrative prompt containing raw email body | accept | Audit list rows already contain only summary + IDs, not raw body; narrative call passes those structured rows + `redactAllPii(summary)` |
</threat_model>

<verification>
1. Run all 7 task verify commands green.
2. Manual: with seeded ≥1 week of audit history, RECAP_REGENERATE produces canonical with 5 populated sections + auditRowRefs.
3. Open RecapEditor; edit a section; finalize. Confirm weekly_recap_section_edit rows exist.
4. Export DOCX + PDF; open both in real viewers; confirm same content visible.
5. Suspend over a Monday boundary; resume; confirm exactly one recap row appears for that week (not multiple).
</verification>

<success_criteria>
- All Stream-2 requirement IDs satisfied with concrete tests: RECAP-01 (5 sections), RECAP-02 (audit log section sourced from VIEW), RECAP-03 (edits feed learning — diff rows present), RECAP-04 (DOCX+PDF export).
- VIEW perf < 200ms on seeded 5k-row fixture.
- Static-grep ratchet asserts exporters bypass HTML.
- /recap route reachable from sidebar; not orphan.
</success_criteria>

<output>
After completion, write `08-02-SUMMARY.md` covering: VIEW shape + arms, weekly_recap tables, cron key added, new deps + versions, IPC channels added, route added, follow-ups for Stream 3 (section-edit signal consumer).
</output>
