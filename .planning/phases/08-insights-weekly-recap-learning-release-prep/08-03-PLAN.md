---
phase: 08-insights-weekly-recap-learning-release-prep
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/main/db/migrations/128_phase8.sql
  - src/main/db/migrations/embedded.ts
  - src/main/learning/signal-log.ts
  - src/main/learning/signal-log.test.ts
  - src/main/learning/prefs.ts
  - src/main/learning/prefs.test.ts
  - src/main/learning/aggregate.ts
  - src/main/learning/aggregate.integration.test.ts
  - src/main/learning/schedule.ts
  - src/main/learning/schedule.test.ts
  - src/main/learning/sources/approval.ts
  - src/main/learning/sources/approval.test.ts
  - src/main/learning/sources/briefing.ts
  - src/main/learning/sources/recap.ts
  - src/main/learning/sources/recap.test.ts
  - src/main/learning/sources/qa.ts
  - src/main/learning/sources/qa.test.ts
  - src/main/ipc/learning.ts
  - src/main/ipc/index.ts
  - src/shared/ipc-contract.ts
  - src/preload/index.ts
  - src/renderer/features/settings/sections/LearnedPreferencesSection.tsx
  - src/renderer/features/settings/sections/LearnedPreferencesSection.test.tsx
  - src/renderer/features/settings/SettingsScreen.tsx
  - src/renderer/features/settings/sections/NewsTopicsSection.tsx
  - src/renderer/features/briefing/BriefingFeedbackChips.tsx
  - src/renderer/features/briefing/BriefingScreen.tsx
  - src/renderer/features/rag/AnswerCard.tsx
  - src/renderer/components/DisconnectConfirmDialog.tsx
  - scripts/grep-no-network-from-signals.mjs
  - package.json
  - src/main/sentry/beforeSend.ts
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03, BRIEF-04, BRIEF-05, XCUT-02]

must_haves:
  truths:
    - "Every approval edit/reject/accept, briefing thumbs, recap section edit, Q&A thumb writes a row to learning_signals via redactAllPii"
    - "Nightly aggregator derives a single learned_preferences row from the last 30 days of signals"
    - "User can inspect learned_preferences in Settings → Learned Preferences (tree view + last-updated + signals-counted)"
    - "User can reset ANY field with a confirmation dialog (DisconnectConfirmDialog primitive shape)"
    - "User can reset ALL preferences with a confirmation dialog"
    - "User can view the raw signal log (read-only, paginated)"
    - "No code path writes signals to a network destination — enforced by static-grep ratchet + Sentry beforeSend allowlist"
    - "BRIEF-05 thumbs-up/down chips render on briefing sections; each click writes a signal"
    - "BRIEF-04 News topics UI (add/remove topic chips + RSS list) lives in Settings; topics persisted"
    - "XCUT-02 invariant tested: an interrupted draft persists across simulated crash and never auto-transitions to 'sent' (test consumes Phase 3 state machine)"
  artifacts:
    - path: "src/main/db/migrations/128_phase8.sql"
      provides: "learning_signals + learned_preferences tables + briefing_feedback table appended to Stream 1/2 migration"
      contains: "CREATE TABLE learning_signals"
    - path: "src/main/learning/signal-log.ts"
      provides: "redacting append-only writer for all signal sources"
      exports: ["writeSignal", "listSignals", "type LearningSignal", "type SignalSource"]
    - path: "src/main/learning/prefs.ts"
      provides: "typed-preferences read/write/per-field-reset"
      exports: ["readPreferences", "writePreferences", "resetField", "resetAll", "PreferencesSchema"]
    - path: "src/main/learning/aggregate.ts"
      provides: "nightly: signals → typed prefs upsert (read-window 30d)"
      exports: ["aggregatePreferences"]
    - path: "src/main/learning/sources/*.ts"
      provides: "four signal sources: approval, briefing, recap, qa — each hooks into existing chokepoints"
    - path: "src/renderer/features/settings/sections/LearnedPreferencesSection.tsx"
      provides: "Settings tab: prefs tree + per-field reset + 'Reset all' + 'View signal log' sub-page (reachable)"
    - path: "scripts/grep-no-network-from-signals.mjs"
      provides: "static-grep ratchet — signals/* never imports fetch/axios/http/https/ws/Sentry-non-allowlisted"
  key_links:
    - from: "src/main/approvals/persist.ts (Phase 3 chokepoint)"
      to: "src/main/learning/sources/approval.ts"
      via: "on every persist transition (approved/rejected/edited), emit signal"
      pattern: "writeSignal\\("
    - from: "src/main/recap/persist.ts (Plan 08-02 finalize path)"
      to: "src/main/learning/sources/recap.ts"
      via: "finalizeRecap → emit per-section edit diffs as recap signals"
      pattern: "writeRecapSignals"
    - from: "src/main/rag/threads.ts (Phase 7)"
      to: "src/main/learning/sources/qa.ts"
      via: "new appendTurnFeedback() → emit qa signal"
      pattern: "appendTurnFeedback"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "LearnedPreferencesSection"
      via: "imported and rendered — NOT orphan"
      pattern: "LearnedPreferencesSection"
    - from: "scripts/grep-no-network-from-signals.mjs"
      to: "src/main/learning/**"
      via: "lint-guard: zero network imports under learning/"
      pattern: "grep:no-network-from-signals"
---

<objective>
Ship Stream 3: the preference learning loop. Four signal sources (approval, briefing, recap, Q&A) feed an append-only `learning_signals` table; a nightly aggregator derives a single `learned_preferences` row consumed at draft/recap/briefing time; user can inspect every field and reset per-field or globally. Local-only — enforced by static-grep + Sentry allowlist.

Purpose: Earn LEARN-01..03 + BRIEF-04 + BRIEF-05 + XCUT-02. This is the only stream that makes Aria's behaviour observably converge on the user over time.

Output:
- Migration 128 extension: learning_signals + learned_preferences + briefing_feedback tables
- Four signal-source hooks into existing chokepoints (NOT new chokepoints)
- Nightly aggregator + LearnedPreferencesSection reachable Settings tab
- BRIEF-04 News topics UI + BRIEF-05 thumbs chips on Briefing
- XCUT-02 crash-persistence test (consumes Phase 3 state machine; this plan owns the test)
- Static-grep ratchet `grep:no-network-from-signals` + Sentry beforeSend allowlist update
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-CONTEXT.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-RESEARCH.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-01-PLAN.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-02-PLAN.md

@src/main/briefing/redact.ts
@src/main/briefing/schedule.ts

<interfaces>
<!-- Existing chokepoints the signal sources hook into. -->

From Phase 3 (`src/main/approvals/persist.ts` — confirm path via grep):
- approval state transitions (`generating → ready → approved → sent | rejected`); persist function is the chokepoint. Hook signal emit at the persist boundary, AFTER successful commit (research Anti-Patterns: "signal capture happens AFTER user action commits, never speculatively").

From Plan 08-02 (`src/main/recap/persist.ts`):
- `finalizeRecap` emits `weekly_recap_section_edit` rows. Stream 3 source/recap.ts reads those and writes signal-log rows.

From Phase 7 (`src/main/rag/threads.ts`):
- `appendTurn(db, { ...routing, ... })`. Add a new `appendTurnFeedback(db, { turnId, thumb: -1|0|1 })` that:
  - updates `rag_turn` (NEW column `thumb` per research Open Q #10)
  - emits a learning_signal of kind 'qa_feedback'

From Phase 2 (`src/main/briefing/`):
- Briefing renderer + `briefing_item_dismissed` (existing). New table `briefing_feedback` for per-section thumbs (research Open Q #9 — there is no existing thumbs scaffold).

From `src/main/briefing/redact.ts`:
- `redactAllPii(s)`, `redactObject(o)` — apply at signal-log WRITE time (defense-in-depth per research Anti-Patterns).

From Phase 1 Sentry path (`src/main/sentry/beforeSend.ts` — confirm path via grep; may be `src/main/index.ts` inline):
- `beforeSend(event)` allowlist — extend to drop any event mentioning `learning_signals` or `learned_preferences` tag.

From MEMORY `feedback-destructive-actions-require-consent`:
- Reset operations must use DisconnectConfirmDialog primitive shape (3-assertion test pattern). Already exists per Phase 5.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 128 extension — learning_signals + learned_preferences + briefing_feedback + rag_turn.thumb</name>
  <files>src/main/db/migrations/128_phase8.sql, src/main/db/migrations/embedded.ts</files>
  <behavior>
    - Test 1: learning_signals table: id PK, source TEXT NOT NULL CHECK source IN ('approval','briefing','recap','qa'), kind TEXT, payload_json TEXT, occurred_at TEXT NOT NULL.
    - Test 2: learned_preferences table: id INTEGER PK CHECK id=1 (single-row), payload_json TEXT NOT NULL, updated_at TEXT NOT NULL.
    - Test 3: briefing_feedback table: id PK, briefing_id, section_key TEXT, thumb INTEGER CHECK thumb IN (-1,0,1), created_at.
    - Test 4: ALTER rag_turn ADD COLUMN thumb INTEGER DEFAULT 0 (research Open Q #10).
    - Test 5: index on learning_signals(occurred_at DESC) + (source, occurred_at).
    - Test 6: applying migration 128 a second time is no-op (idempotent under runner's tracked-versions guard).
  </behavior>
  <action>
    Append to `128_phase8.sql`. All Streams (1/2/3) live in this single migration file — researcher's intentional decision (research Runtime State Inventory line: "all in single migration 128"). Per migration runner contract, the file is applied atomically; adding rows to it across plans is safe so long as it hasn't been applied yet during dev. If executor finds migration 128 already applied due to Stream 1/2 dev runs, document a clean reset path.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/db/migrations/runner.test.ts</automated>
  </verify>
  <done>All tables + column present; user_version=128; idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: signal-log.ts + grep-no-network ratchet</name>
  <files>src/main/learning/signal-log.ts, src/main/learning/signal-log.test.ts, scripts/grep-no-network-from-signals.mjs, package.json, src/main/sentry/beforeSend.ts</files>
  <behavior>
    - Test 1: writeSignal(db, { source, kind, payload }) applies redactAllPii to string fields in payload before INSERT.
    - Test 2: listSignals(db, { limit, offset, source?, fromIso?, toIso? }) paginates.
    - Test 3: writeSignal NEVER calls fetch/http/https/ws/net — assert by injecting a spy on globalThis.fetch and verifying it's never called.
    - Test 4: grep-script exits 0 on current learning/*.
    - Test 5: deliberately injecting `import fetch from 'node-fetch'` into a temp copy of learning/signal-log.ts makes the script exit non-zero.
    - Test 6: Sentry beforeSend drops events tagged with `scope: 'learning'` or referencing `learning_signals` in the message.
    - Test 7: 90-day retention purge — running purgeOldSignals(db, { keepDays: 90 }) deletes rows older than 90d unless `learning_signals_keep_forever=1` in `app_setting` (Stream 3 reads existing settings table per Phase 1; if absent, creates row).
  </behavior>
  <action>
    Implement signal-log with one chokepoint export. Redaction at write per research Anti-Patterns. Static-grep ratchet enumerates banned modules: `node:http`, `node:https`, `node:net`, `node:dgram`, `fetch`, `node-fetch`, `axios`, `got`, `undici`, `ws`, `@sentry/electron` (allowed only via the allowlisted beforeSend — but learning/* should never import Sentry directly).

    Extend Sentry beforeSend allowlist — if file doesn't exist at expected path, search for the existing Sentry init via grep and patch it; do not create a parallel module.

    Default retention 90d (research Pitfall 8 + Assumption A5); toggle via `app_setting.learning_signals_keep_forever`.

    (W-1) Wire `grep:no-network-from-signals` into the composite `lint:guard` script created by Plan 08-01 Task 4. Add package.json:
    ```
    "grep:no-network-from-signals": "node scripts/grep-no-network-from-signals.mjs"
    ```
    Then extend the existing `lint:guard` value (created in 08-01) by appending ` && node scripts/grep-no-network-from-signals.mjs` so the composite shape becomes `grep-assert-approved && grep-insight-prose-no-raw && grep-no-network-from-signals && verify-audit-view`. Confirm with `npm pkg get scripts.lint:guard` before commit.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/learning/signal-log.test.ts &amp;&amp; pnpm run grep:no-network-from-signals</automated>
  </verify>
  <done>All 7 tests pass; grep ratchet wired in pretest/lint:guard; Sentry allowlist enforces LEARN-02 at the network boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Four signal sources — hook into existing chokepoints</name>
  <files>src/main/learning/sources/approval.ts, src/main/learning/sources/approval.test.ts, src/main/learning/sources/briefing.ts, src/main/learning/sources/recap.ts, src/main/learning/sources/recap.test.ts, src/main/learning/sources/qa.ts, src/main/learning/sources/qa.test.ts, src/main/approvals/persist.ts, src/main/recap/persist.ts, src/main/rag/threads.ts, src/main/ipc/rag.ts</files>
  <behavior>
    - approval source: when approval state transitions to 'approved' with diff vs draft → signal `{kind: 'approval.edit', payload: { approvalKind, hasEdits: true, editCategory? }}`. On 'rejected' → `{kind: 'approval.reject', payload: { approvalKind, reason }}`. On 'sent' clean → `{kind: 'approval.accept'}`.
    - briefing source: BRIEFING_FEEDBACK IPC + BRIEFING_INSIGHT_DISMISS (stubbed in 08-01 Task 8) → signals.
    - recap source: per-section diff (before/after text) categorized into one of `tone|length|factual|structure` via a tiny heuristic (length-only diff = 'length', char-level diff with same word-count = 'tone'; LLM categorization deferred — research Open Q ack). Emit one signal per edited section.
    - qa source: appendTurnFeedback() updates rag_turn.thumb and emits signal `{kind: 'qa.thumb', payload: { turnId, thumb, route, sensitivity }}`.
    - Test 1 (approval): mock approval persist transition, assert signal row exists.
    - Test 2 (recap): mock finalizeRecap with edits, assert per-section signal rows.
    - Test 3 (qa): appendTurnFeedback updates column AND emits signal.
    - Test 4 (cross): all four sources write through writeSignal (not a custom path) — assert by spying writeSignal.
    - Test 5 (W-6 — same-transaction atomicity): throw inside the wrapping `db.transaction(() => { transitionTo(); writeSignal(); })` rolls back BOTH the state transition AND the signal. Assert both `signalCount === 0` and `state === preThrowState` after the throw. (Replaces the prior "emit AFTER commit" wording — the locked shape is same-transaction, not post-commit.)
    - Test 6 (W-4 backfill): BRIEFING_INSIGHT_DISMISS handler boot reads `app_meta WHERE k LIKE 'briefing_dismiss_log:%'`, replays each into briefing_feedback, deletes the replayed app_meta rows. Seed 3 app_meta dismiss logs + 0 briefing_feedback -> after boot, briefing_feedback has 3 + app_meta has 0 matching.
  </behavior>
  <action>
    (W-6 — per-source signal-emit boundary, ONE shape per source; no "or"):

    1. **approval source (SAME-TRANSACTION emit):** signal write executes INSIDE the same `db.transaction()` block that performs the approval state transition in `src/main/approvals/persist.ts`. A downstream throw rolls BOTH back atomically. This is the post-mortem fix from MEMORY `project_aria_approve_silent_failure` (Phase 4 echo). Concrete shape: `db.transaction(() => { transitionTo(...); writeSignal(...); })()`. NO "commit-then-emit" path for this source.

    2. **recap source (SAME-TRANSACTION emit):** finalizeRecap (Plan 08-02 Task 3) writes both `weekly_recap_section_edit` rows AND `learning_signals` rows inside the same transaction. Stream 3's source/recap.ts is invoked from inside `finalizeRecap`, not as a post-commit hook.

    3. **briefing source (DIRECT WRITE — the click IS the signal):** BRIEFING_FEEDBACK and BRIEFING_INSIGHT_DISMISS IPC handlers each open a transaction that writes the source row (briefing_feedback or briefing_dismiss) AND a learning_signal in one shot. There is no pre-existing chokepoint to wrap; the user click IS the commit boundary. Single transaction per handler.

    4. **qa source (DIRECT WRITE — same as briefing):** appendTurnFeedback (Phase 7 extension) wraps `UPDATE rag_turn SET thumb=?` + learning_signal INSERT in one transaction. The thumb click IS the signal.

    Test 5 ("emit AFTER commit; throw rolls back") is REPLACED with: throw inside the transaction at any point rolls back BOTH state change AND signal — assert via `expect(signalCount).toBe(0)` AND `expect(stateUnchanged)` after thrown transaction.

    Add RAG_TURN_FEEDBACK IPC channel; wire it from AnswerCard.tsx thumbs UI (Task 5).

    Add BRIEFING_FEEDBACK + BRIEFING_INSIGHT_DISMISS channels. The 08-01 stub for BRIEFING_INSIGHT_DISMISS writes to `app_meta(k='briefing_dismiss_log:...')` (per 08-01 Task 8 W-4 fix); Plan 08-03 Task 3's real handler MUST: (a) backfill — read all `app_meta` rows with `k LIKE 'briefing_dismiss_log:%'`, replay into `briefing_feedback`, DELETE the replayed app_meta rows (one-time on first install of Wave 3); (b) thereafter, write directly to briefing_feedback. Add Test 6: backfill test seeds 3 app_meta rows + 0 briefing_feedback rows -> after handler boot, briefing_feedback has 3 rows AND app_meta has 0 matching rows.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/learning/sources/</automated>
  </verify>
  <done>All four sources emit through the chokepoint writer; race test confirms commit-then-emit ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: prefs.ts + nightly aggregate.ts + scheduleLearning</name>
  <files>src/main/learning/prefs.ts, src/main/learning/prefs.test.ts, src/main/learning/aggregate.ts, src/main/learning/aggregate.integration.test.ts, src/main/learning/schedule.ts, src/main/learning/schedule.test.ts</files>
  <behavior>
    - prefs Test 1: PreferencesSchema (zod) matches CONTEXT shape: `{ voice: { avgDraftLengthChars, formality, openerStyle, signOffStyle, terseness }, triage: { vipBoosts, deprioritizedSenders, topicMutes }, scheduling: { bufferPref, focusTimePref, primeTimeWindows }, briefing: { preferredSections, dismissedInsightKinds, newsTopics }, lastUpdatedAt }`.
    - prefs Test 2: readPreferences returns defaults when row absent.
    - prefs Test 3: writePreferences validates payload via zod; rejects unknown fields (closed shape).
    - prefs Test 4: resetField('voice.terseness') zeroes ONLY that field; other fields untouched; lastUpdatedAt bumped.
    - prefs Test 5: resetAll restores schema defaults; signals retained (separate concern).
    - aggregate Test 6: aggregatePreferences(db, { windowDays: 30 }) reads last 30d of signals, derives a learned_preferences row, upserts.
    - aggregate Test 7: with zero signals, aggregator writes defaults (no-op-ish but row exists).
    - aggregate Test 8: derivation: 60% of approval.edit signals with editCategory='length-shorter' → voice.terseness moves toward 1.0 (deterministic transformation function).
    - schedule Test 9: scheduleLearning registers `'learning-nightly'` in cronRegistry; size +1; suspend/resume invariant (mirror briefing/schedule.ts).
  </behavior>
  <action>
    prefs.ts: single-row table read/write helpers. per-field reset via a path-based mutator (`resetField(path: 'voice.terseness' | ...)`) operating on parsed JSON then reserialized.

    aggregate.ts: pure derivation functions per pref-field; each takes filtered signals + returns a value. Composed in `aggregatePreferences()`. Default window 30d (research Pitfall 8). Cron expr `'30 2 * * *'` (2:30am — 30 min after insights nightly so they don't contend for the same p-queue slot).

    schedule.ts: copy briefing/schedule.ts pattern.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/learning/prefs.test.ts src/main/learning/aggregate.integration.test.ts src/main/learning/schedule.test.ts</automated>
  </verify>
  <done>Per-field reset works granularly; aggregator deterministic; learning-nightly cron registered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: IPC + preload + LearnedPreferencesSection (reachable) + Signal-log sub-page</name>
  <files>src/main/ipc/learning.ts, src/main/ipc/index.ts, src/shared/ipc-contract.ts, src/preload/index.ts, src/renderer/features/settings/sections/LearnedPreferencesSection.tsx, src/renderer/features/settings/sections/LearnedPreferencesSection.test.tsx, src/renderer/features/settings/SettingsScreen.tsx, src/renderer/features/briefing/BriefingFeedbackChips.tsx, src/renderer/features/briefing/BriefingScreen.tsx, src/renderer/features/rag/AnswerCard.tsx, src/renderer/components/DisconnectConfirmDialog.tsx</files>
  <behavior>
    - IPC Test 1: LEARN_GET_PREFS returns current row.
    - IPC Test 2: LEARN_RESET_FIELD validates path against PreferencesSchema; zeroes field.
    - IPC Test 3: LEARN_RESET_ALL invokes resetAll.
    - IPC Test 4: LEARN_LIST_SIGNALS paginates.
    - IPC Test 5: BRIEFING_FEEDBACK writes briefing_feedback row + learning_signal.
    - IPC Test 6: RAG_TURN_FEEDBACK updates rag_turn.thumb + writes learning_signal.
    - UI Test 7: LearnedPreferencesSection renders tree-view of prefs with current value, signals-count (from listSignals filtered), last-updated.
    - UI Test 8: Per-field reset opens DisconnectConfirmDialog (3 assertions: title contains field name, body warns "this can be regenerated from your signal log", primary action labelled "Reset {field}").
    - UI Test 9: 'Reset all' opens DisconnectConfirmDialog with stronger warning.
    - UI Test 10: 'View signal log' sub-page renders read-only paginated list.
    - UI Test 11: BriefingFeedbackChips render under each briefing section; thumb-up / thumb-down click writes BRIEFING_FEEDBACK.
    - UI Test 12: AnswerCard (Phase 7) gains thumb-up/down buttons wired to RAG_TURN_FEEDBACK.
    - UI Reachability Test 13 (L-04-04): grep asserts LearnedPreferencesSection imported by SettingsScreen.
  </behavior>
  <action>
    Add CHANNELS: LEARN_GET_PREFS, LEARN_RESET_FIELD, LEARN_RESET_ALL, LEARN_LIST_SIGNALS, BRIEFING_FEEDBACK, BRIEFING_INSIGHT_DISMISS, RAG_TURN_FEEDBACK. Register handlers via skip-pattern in ipc/index.ts.

    LearnedPreferencesSection: tree view (nested) — `<details>`/`<summary>` shape OK or shadcn `Accordion`. Per-field reset via Disconnect-style dialog. "View signal log" navigates to /settings/signal-log (nested route or full-page). Both routes reachable from SettingsScreen.

    NewsTopicsSection (BRIEF-04): topic chips (add/remove) + RSS source list. Already partially shipped in Phase 2 as `NewsSourcesSection` — verify whether topics layer was added; if not, append topic-chip UI to the existing section rather than creating a new one. Topics persist into `learned_preferences.briefing.newsTopics` AND the existing news_source table (depending on which Phase 2 used).

    BriefingFeedbackChips: small +/- chips. Briefing section renderer wraps each section in this component. BRIEF-05 done.

    AnswerCard thumbs: small icon buttons; on click → RAG_TURN_FEEDBACK; show toast on success.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/ipc/learning.test.ts src/renderer/features/settings/sections/LearnedPreferencesSection.test.tsx</automated>
  </verify>
  <done>All 13 tests pass; Settings sub-page reachable; reset dialogs follow primitive; briefing/AnswerCard thumbs wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: XCUT-02 crash-persistence test (consumes Phase 3 state machine)</name>
  <files>tests/integration/xcut-02-draft-crash.spec.ts, src/main/approvals/persist.ts</files>
  <behavior>
    - Test 1: open a draft approval row in 'generating' state.
    - Test 2: simulate process crash (force-quit electron via Playwright `_electron` `app.close({ force: true })`) — or unit-level: kill main-process module by tearing down deps mid-flight.
    - Test 3: relaunch / re-init; the row persists.
    - Test 4: row state is 'interrupted' or 'generating' — NEVER 'sent'.
    - Test 5: there is no send_log row for that draft.
    - Test 6: there is no provider API call recorded (e.g. assertion that no Gmail send mock was invoked post-recover).
  </behavior>
  <action>
    Per CONTEXT cross-cutting: "Phase 8 plan 4 includes a dedicated test asserting this invariant under simulated crash." Researcher attributed this to Stream 4, but CONTEXT decisions put it cross-cutting; this plan owns it because learning's signal log + approval chokepoint share the same persist module under inspection.

    Use existing Phase 3 state machine. If 'interrupted' state doesn't exist, surface it via Phase 3 (MEMORY: project_aria_phase3_executed flagged CR-01 gate fail-OPEN as a still-open Phase 3 hardening item; do NOT subsume — document dependency: if Phase 3 hardening lands first the test asserts 'interrupted'; if not, the test asserts 'generating' or any non-terminal state).

    Test runs as Playwright `_electron` if state-machine boundaries require real process boundary; else as a Vitest integration test against a real DB + mocked main-process lifecycle.
  </action>
  <verify>
    <automated>pnpm test:e2e --grep xcut-02-draft-crash</automated>
  </verify>
  <done>Crash test passes; draft never auto-transitions to sent; no send_log row written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal writer → network | learning/* must never hit network — enforced by static-grep + Sentry beforeSend |
| renderer → IPC reset | reset payloads zod-validated; field paths whitelisted |
| chokepoint → signal emit | emit AFTER commit; never speculatively |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Information Disclosure | Learning signals leaking via Sentry | mitigate | beforeSend allowlist drops events from `scope:'learning'`; static-grep prevents direct import |
| T-08-12 | Information Disclosure | Signal payload containing raw PII | mitigate | redactAllPii at write time (signal-log.ts Task 2 test 1) |
| T-08-13 | Tampering | Renderer crafting malicious field path in resetField | mitigate | Zod-validated path whitelist |
| T-08-14 | Spoofing | Crash mid-draft auto-sends | mitigate | XCUT-02 test (Task 6); state machine never auto-transitions to sent |
| T-08-15 | Repudiation | Pref reset undoable (user wants to recover) | accept | Signal log retained 90d; user can re-run aggregator to re-derive prefs |
</threat_model>

<verification>
1. All 6 task verify commands green.
2. `pnpm run grep:no-network-from-signals` exits 0.
3. Edit + send an approval; confirm row in learning_signals; confirm route='approval', kind='approval.edit'.
4. Add a news topic in Settings; confirm persists into learned_preferences.briefing.newsTopics AND survives nightly aggregator.
5. Click thumb-down on a briefing section; confirm briefing_feedback row + learning_signal row.
6. Click thumb-up on a RAG answer; confirm rag_turn.thumb=1 + learning_signal row.
7. Settings → Learned Preferences → Reset 'voice.terseness': dialog appears; confirm; field zeros; other fields unaffected.
8. XCUT-02 e2e passes against packaged build.
</verification>

<success_criteria>
- LEARN-01: all four signal sources active.
- LEARN-02: static-grep + Sentry allowlist enforce no-network invariant; test asserts globalThis.fetch never invoked from learning/*.
- LEARN-03: per-field + global reset via DisconnectConfirmDialog primitive; signal log inspectable.
- BRIEF-04: News topics UI present and persisted.
- BRIEF-05: thumbs chips wired on briefing sections.
- XCUT-02: crash test passes; draft never auto-sent.
</success_criteria>

<output>
After completion, write `08-03-SUMMARY.md` covering: tables added (learning_signals, learned_preferences, briefing_feedback), rag_turn.thumb migration, cron key added (learning-nightly), grep ratchet wired, Sentry allowlist extended, signal sources hooked, XCUT-02 test landed.
</output>
