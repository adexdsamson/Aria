---
phase: 08-insights-weekly-recap-learning-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db/migrations/128_phase8_insights.sql
  - src/main/db/migrations/embedded.ts
  - src/main/insights/compute.ts
  - src/main/insights/compute.test.ts
  - src/main/insights/gate.ts
  - src/main/insights/gate.test.ts
  - src/main/insights/prose.ts
  - src/main/insights/prose.test.ts
  - src/main/insights/aggregate.ts
  - src/main/insights/aggregate.integration.test.ts
  - src/main/insights/schedule.ts
  - src/main/insights/schedule.test.ts
  - src/main/ipc/insights.ts
  - src/main/ipc/index.ts
  - src/shared/ipc-contract.ts
  - src/preload/index.ts
  - src/main/briefing/generate.ts
  - src/renderer/features/briefing/BriefingScreen.tsx
  - src/renderer/features/settings/sections/InsightsSection.tsx
  - src/renderer/features/settings/SettingsScreen.tsx
  - scripts/grep-insight-prose-no-raw.mjs
  - package.json
autonomous: true
requirements: [INSIGHT-01, INSIGHT-02, INSIGHT-03, BRIEF-02]

must_haves:
  truths:
    - "User sees an 'Insights unlock in N days' placeholder in the briefing when any corpus has <14 days of history"
    - "After 14 days of calendar/email history, the briefing 'This week' section renders at least one insight from user data"
    - "Insight prose generation NEVER sends raw email body / calendar title / transcript text to a frontier API — aggregates only"
    - "Nightly cron computes the 4 insight types and caches rows in `insights` table"
    - "User can recompute insights manually from Settings → Insights"
    - "Insights cron is registered in scheduler.cronRegistry as 'insights-nightly' and survives suspend/resume without growing the registry"
  artifacts:
    - path: "src/main/db/migrations/128_phase8_insights.sql"
      provides: "insights table (kind, computed_at, week_ymd, payload_json) + indices"
      contains: "CREATE TABLE insights"
    - path: "src/main/insights/compute.ts"
      provides: "4 pure aggregation functions: calendarLoadDelta, responseTimeTrend, recurringThemes, approvalEditPattern"
      exports: ["computeCalendarLoadDelta", "computeResponseTimeTrend", "computeRecurringThemes", "computeApprovalEditPattern"]
    - path: "src/main/insights/gate.ts"
      provides: "14-day-per-corpus gate; readable from both nightly aggregator and IPC"
      exports: ["checkInsightGate", "type InsightGateResult"]
    - path: "src/main/insights/prose.ts"
      provides: "aggregates → 1–3 sentences via generateObject; ZERO raw content imports"
      exports: ["insightProse"]
    - path: "src/main/insights/schedule.ts"
      provides: "nightly cron registration; copies briefing/schedule.ts pattern"
      exports: ["scheduleInsights", "stopInsightsSchedule"]
    - path: "src/main/ipc/insights.ts"
      provides: "INSIGHTS_LATEST + INSIGHTS_RECOMPUTE handlers"
    - path: "src/renderer/features/settings/sections/InsightsSection.tsx"
      provides: "Settings → Insights row with manual 'Recompute now' affordance reachable from SettingsScreen"
    - path: "scripts/grep-insight-prose-no-raw.mjs"
      provides: "static-grep ratchet asserting prose.ts never imports gmail_message/calendar_event title/meeting_note_segment"
  key_links:
    - from: "src/main/insights/aggregate.ts"
      to: "src/main/insights/gate.ts"
      via: "checkInsightGate called BEFORE any compute or LLM call"
      pattern: "checkInsightGate\\("
    - from: "src/main/insights/aggregate.ts"
      to: "src/main/insights/prose.ts"
      via: "aggregates → insightProse → row.payload_json.sentences"
      pattern: "insightProse\\("
    - from: "src/main/briefing/generate.ts"
      to: "insights table"
      via: "SELECT FROM insights WHERE week_ymd >= ? — adds 'This week' section to briefing payload"
      pattern: "FROM insights"
    - from: "src/renderer/features/settings/SettingsScreen.tsx"
      to: "InsightsSection"
      via: "imported and rendered in Settings (NOT orphan)"
      pattern: "InsightsSection"
    - from: "package.json"
      to: "scripts/grep-insight-prose-no-raw.mjs"
      via: "added to lint:guard or pretest hook"
      pattern: "grep:insight-prose-no-raw"
---

<objective>
Ship Stream 1 of Phase 8: privacy-preserving insights computation over the user's own data, integrated into the daily briefing as a "This week" section, gated by 14 days of per-corpus history.

Purpose: Earn INSIGHT-01..03 + BRIEF-02. The briefing's claim to be a "chief of staff" depends on it surfacing what the user can't see by reading raw inbox. This phase is where Aria first computes derived signal from their own data.

Output:
- Migration 128 (`128_phase8_insights.sql`) — insights table ONLY. Per peer-review H-1 (round 2): the original "single migration 128 shared across Streams 1/2/3" design was rejected. Each stream now owns its own migration (128 insights / 129 recap / 130 learning) so the runner's `user_version` tracking handles dev-machine re-runs correctly. No reset protocol required.
- 4 pure aggregation modules + 14-day gate + aggregates-only prose generator
- Nightly cron + manual recompute IPC + Settings affordance
- Briefing payload gains a "This week" section
- Static-grep ratchet preventing future contributors from leaking raw content into `insights/prose.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-CONTEXT.md
@.planning/phases/08-insights-weekly-recap-learning-release-prep/08-RESEARCH.md

@src/main/briefing/schedule.ts
@src/main/briefing/generate.ts
@src/main/lifecycle/scheduler.ts
@src/main/lifecycle/powerMonitor.ts
@src/main/db/migrations/runner.ts
@src/main/db/migrations/embedded.ts
@src/main/llm/routingLog.ts
@src/main/briefing/redact.ts

<interfaces>
<!-- Critical existing primitives the executor MUST reuse, not reimplement. -->

From src/main/briefing/schedule.ts (Phase 2 — canonical cron-with-suspend/resume pattern):
- `scheduleBriefing(expr, tz, run, { scheduler, logger, cronImpl? })`
- module-scoped `_lastFiredDate` dedupe; `registerLifecycleCallbacks({ onSuspend, onResume })` calls `task.stop()` / `task.start()`; NEVER `.delete()` on suspend; entry replaced in-place; `cronRegistry.size` invariant.
- `stopBriefingSchedule(scheduler)` — full cleanup including `_unregisterLifecycle()`.
- `computeLocalYmd(tz, now)` — Intl en-CA YYYY-MM-DD; export-reuse from schedule.ts.

From src/main/lifecycle/scheduler.ts:
- `SchedulerHandle.cronRegistry: Map<string, ScheduledTask>`
- New singleton key for this plan: `'insights-nightly'`. Size invariant grows by exactly 1 after this plan ships.

From src/main/llm/routingLog.ts:
- `writeRoutingLog(db, { ts, route, reason, source: 'generic', prompt_hash, model, latency_ms, ok })`
- `hashPrompt(s)` — SHA-256; never log raw prompt.

From src/main/briefing/redact.ts:
- `redactAllPii(s)`, `redactObject(o)` — log-line redaction primitives.

From src/main/db/migrations/runner.ts + embedded.ts:
- Migrations are SQL files in `src/main/db/migrations/*.sql`; `embedded.ts` re-exports them at build time. Current highest version is 127. This plan adds 128 (`128_phase8_insights.sql`). Plans 08-02 / 08-03 add 129 / 130 respectively — no shared migration file (peer-review H-1 round 2).

From `ai` SDK + zod (Phase 2 prior art `src/main/briefing/generate.ts`):
- `generateObject({ model, schema: Z, prompt })` returns `{ object }`.
- Model selection: route via existing `router.classify({ prompt, source })` from Phase 1/3 — DO NOT bypass.

From research §Pattern 2 / §Pitfall 2:
- Prose payload schema: `{ calendarLoadDeltaPct?, medianReplyTimeShiftHours?, topThemes?: string[<=30 chars each>], editedDraftSharePct? }` ONLY. No raw strings beyond cluster LABELS.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 128 + insights table + verify-migration scaffold</name>
  <files>src/main/db/migrations/128_phase8_insights.sql, src/main/db/migrations/embedded.ts, src/main/db/migrations/runner.test.ts, src/main/insights/schema.ts</files>
  <behavior>
    - Test 1: migration 128 applies cleanly on a fresh DB and bumps user_version to 128.
    - Test 2: migration 128 is idempotent (re-run after applied is a no-op).
    - Test 3: `insights` table has columns: id INTEGER PK, kind TEXT NOT NULL CHECK kind IN ('calendar_load','response_time','recurring_themes','approval_edits'), week_ymd TEXT NOT NULL, computed_at TEXT NOT NULL, payload_json TEXT NOT NULL, dismissed INTEGER NOT NULL DEFAULT 0.
    - Test 4: unique index on (kind, week_ymd) so re-compute upserts.
    - Test 5: index on (week_ymd DESC) for briefing reads.
  </behavior>
  <action>
    Create migration `128_phase8_insights.sql` (Stream 1 ONLY — this migration creates the `insights` table + indices and ends with `PRAGMA user_version = 128;`). Register in `embedded.ts` as version 128. Define a `Insight` row type + `InsightKind` union + zod `InsightPayloadSchema` (discriminated union over kind) in `src/main/insights/schema.ts`.

    Per peer-review H-1 (round 2): do NOT add weekly_recap or learning tables here. Plans 08-02 / 08-03 each own a separate migration file (129 / 130) so dev machines that ran Wave 1 first do not silently miss Wave 2/3 schema when the runner skips `user_version` 128 on re-apply.

    Verify migration runner picks up the new file. Per research Pitfall 3, never `fs.copyFile` over an open DB handle (relevant to Stream 4); this task is migration-only and inherits the existing runner.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/db/migrations/runner.test.ts</automated>
  </verify>
  <done>Fresh DB applies migration 128, user_version=128, insights table exists with documented shape and indices; embedded.ts re-exports new file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 14-day-per-corpus gate (gate.ts) + unit tests</name>
  <files>src/main/insights/gate.ts, src/main/insights/gate.test.ts</files>
  <behavior>
    - Test 1: gate returns `{ unlocked: false, blockedKinds: ['calendar_load'], daysRemaining: N }` when MIN(start_ts) FROM calendar_event is &lt; 14 days ago.
    - Test 2: gate returns `{ unlocked: false, blockedKinds: ['response_time'], daysRemaining: N }` when MIN(internal_date) FROM gmail_message is &lt; 14 days ago.
    - Test 3: gate returns `{ unlocked: true }` when all four corpora satisfy the 14-day window.
    - Test 4: gate returns blockedKinds=['recurring_themes'] when neither gmail nor meeting_note has 14d.
    - Test 4b (B-3): gate returns blockedKinds=['recurring_themes'] even with 14d corpus data when `rag_chunk` has &lt; 50 embeddings WHERE source_kind IN ('email','note'). N=50 is the k-means stability threshold (research §Pattern 3 / Assumption A2 — confirm during execution; if research disagrees, ratchet to the documented value).
    - Test 5: gate returns blockedKinds=['approval_edits'] when MIN(created_at) FROM approval is &lt; 14d.
    - Test 6: gate is pure SQL — zero LLM calls in any path (assert via mock router never invoked).
  </behavior>
  <action>
    Implement `checkInsightGate(db, { now, kind? })` → `InsightGateResult = { unlocked: boolean; blockedKinds: InsightKind[]; daysRemaining: number }`. Per-kind corpus map:
    - `calendar_load` → `calendar_event` (column `start_ts` or equivalent — verify against migration 010/embedded.ts)
    - `response_time` → `gmail_message` (column `internal_date` per Phase 2)
    - `recurring_themes` → (gmail OR meeting_note has 14d data) AND (`SELECT COUNT(*) FROM rag_chunk WHERE source_kind IN ('email','note')` >= 50). Both clauses required; either failing -> block this kind.
    - `approval_edits` → `approval.created_at`

    Per research Open Question #6, enforce gate at BOTH query-time (used by briefing read path) AND write-time (used by aggregate.ts). Single function, two callers.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/insights/gate.test.ts</automated>
  </verify>
  <done>All 6 tests pass; gate function exported and consumed by aggregate + briefing read path in later tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Four compute functions (compute.ts) — pure SQL aggregation</name>
  <files>src/main/insights/compute.ts, src/main/insights/compute.test.ts</files>
  <behavior>
    - calendarLoadDelta: input = db + weekStartYmd; output = `{ meetingHoursThisWeek, meetingHoursLastWeek, deltaPct, focusBlockCount }`. Focus block = contiguous gap ≥ 60 min between events on a workday.
    - responseTimeTrend: per-thread sent-vs-received pairing via `In-Reply-To` / `References` headers first, subject-match fallback (per research Don't-Hand-Roll row). Output = `{ medianMinutesThisWeek, medianMinutesLastWeek, deltaMinutes, perPersonTop3: Array&lt;{ contactEmail, medianMinutes }&gt; }`.
    - recurringThemes: k-means (k swept 3..8 with silhouette tuning per research Assumption A2) over nomic embeddings from rag_chunk where source_kind IN ('email','note') AND occurred_at WITHIN 14d. Output = `{ topThemes: string[] }` where each theme is a ≤30-char LABEL only. Each label MUST be derived from the cluster centroid via a separate `generateObject` call with structured input `{ centroidTokens: string[] }` — NEVER raw chunk text in the label-generation prompt; pass top-5 high-TF terms from the cluster instead.
    - approvalEditPattern: read approval table; for state='approved' or 'sent', count edit-diff signals (will land in Stream 3 schema; for this plan use the existing Phase 3 `approval_edit_log` if present, else short-circuit with empty array — Stream 3 will backfill the source). Output = `{ editedDraftSharePct, topEditCategories: string[] }`.

    Tests: snapshot tests with seeded DB fixtures for each function (use existing `withTempDb()` from Phase 2 test utils — confirm by grep).
  </behavior>
  <action>
    Implement four exported pure functions. They take a Db handle + a `weekStartYmd` and return typed aggregate objects matching `InsightPayloadSchema` from Task 1. None of them call LLMs except `recurringThemes` for label generation, and that path uses cluster-term inputs only (per research Pitfall 2 — assert via grep).

    For k-means: pure TS ~50 LOC per research §Alternatives — vector ops use existing helpers from `src/main/rag/vector-store.ts` if available; otherwise implement inline.

    (S-2) All k-means cluster-label LLM calls share the same `p-queue({ concurrency: 1 })` instance owned by Task 5's aggregate orchestrator — pass the queue handle into `computeRecurringThemes`. No parallel label-gen calls.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/insights/compute.test.ts</automated>
  </verify>
  <done>All four compute functions return typed payload matching schema; tests pass against seeded DB fixtures; recurringThemes label-gen prompt asserted to contain ONLY cluster terms (no raw source text) via test inspecting prompt argument.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: insightProse + static-grep ratchet (INSIGHT-03 chokepoint)</name>
  <files>src/main/insights/prose.ts, src/main/insights/prose.test.ts, scripts/grep-insight-prose-no-raw.mjs, package.json</files>
  <behavior>
    - prose.ts test 1: given numeric-only aggregates payload, returns 1–3 sentences via `generateObject` against the chosen route (mocked).
    - prose.ts test 2: ProseOut zod schema rejects sentences &gt; 220 chars and arrays &gt; 3 / &lt; 1.
    - prose.ts test 3: routing log entry written with `source: 'generic'` (research Pattern 3) and `prompt_hash` not raw prompt.
    - prose.ts test 4: when frontier disabled (router returns LOCAL), falls back to local model and STILL succeeds.
    - grep-script test 5: running `node scripts/grep-insight-prose-no-raw.mjs` exits 0 on current `prose.ts`.
    - grep-script test 6: deliberately injecting `import { ... } from '../db/queries/gmail-message'` into a temp copy makes the script exit non-zero with a clear message.
  </behavior>
  <action>
    Implement `insightProse(aggregates, { router, logger, db })` per research §Pattern 3 — `ProseOut` zod schema = `{ sentences: z.array(z.string().max(220)).min(1).max(3) }`. Payload contains only numeric/structural facts + theme LABELS ≤30 chars. Route via `router.classify({ prompt, source: 'insight-prose' })`. Write `writeRoutingLog` with `hashPrompt(promptBody)`.

    Write `scripts/grep-insight-prose-no-raw.mjs`: load `src/main/insights/prose.ts` text, fail if it imports from any of the forbidden modules (regex list: `gmail_message`, `gmail/`, `calendar_event`, `calendar/`, `meeting_note`, `meeting/`, `rag_chunk`, `raw_text`, `body_text`, `subject`, `summary`). Also fail if any string literal in prose.ts contains a candidate raw-content field name (`body`, `subject`, `transcript`).

    Add `package.json` scripts (W-1 — verified by planner against current package.json at revision time: NO `lint:guard` script exists yet and the assertApproved ratchet script does NOT exist yet either; this plan creates the convention):
    ```
    "grep:insight-prose-no-raw": "node scripts/grep-insight-prose-no-raw.mjs",
    "lint:guard": "node scripts/grep-assert-approved.mjs && node scripts/grep-insight-prose-no-raw.mjs"
    ```
    Plan 08-03 Task 2 will extend `lint:guard` with `grep-no-network-from-signals.mjs`; Plan 08-02 Task 1 will extend with `verify-audit-view.mjs`. Final shape after Phase 8: `"lint:guard": "node scripts/grep-assert-approved.mjs && node scripts/grep-insight-prose-no-raw.mjs && node scripts/grep-no-network-from-signals.mjs && node scripts/verify-audit-view.mjs"`.

    The `grep-assert-approved.mjs` script itself does NOT exist today — if missing at execution time, the executor MUST create a minimal version that asserts `assertApproved(` is called in `src/main/approvals/persist.ts` send path AND in any future approve->write surface (calendar/meeting/task). Cross-reference MEMORY `project_aria_approve_silent_failure`.

    Wire `lint:guard` into `pretest` by amending the `test:unit` script: `"test:unit": "npm run lint:guard && vitest run --passWithNoTests"`. Confirm by grep before commit.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/insights/prose.test.ts &amp;&amp; pnpm run grep:insight-prose-no-raw</automated>
  </verify>
  <done>Both tests + grep ratchet pass; prose.ts cannot import raw-content tables; CI hook runs ratchet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: aggregate.ts orchestrator + integration test</name>
  <files>src/main/insights/aggregate.ts, src/main/insights/aggregate.integration.test.ts</files>
  <behavior>
    - Test 1: aggregate(db, { weekStartYmd, router, logger }) calls checkInsightGate first; if `blockedKinds` includes a kind, no row written for that kind AND no compute fn invoked for it (assert via spy on computeRecurringThemes — specifically that `blockedKinds: ['recurring_themes']` causes computeRecurringThemes to NOT be called; closes B-3).
    - Test 2: for each unlocked kind, compute → prose → upsert into `insights` table keyed on (kind, week_ymd).
    - Test 3: re-running aggregate same week is idempotent (upsert, latest computed_at wins).
    - Test 4: if router returns LOCAL for all kinds, all rows still written (offline-capable).
    - Test 5: serialized via p-queue concurrency 1 (assert at most 1 concurrent generateObject call across the 4 kinds).
  </behavior>
  <action>
    Implement orchestrator using `p-queue({ concurrency: 1 })` (research Standard Stack). Iterates over the 4 kinds, calls gate per kind, computes, generates prose, upserts row. All writes inside a single `db.transaction()`. Logs via redactObject; never logs raw aggregates or prose text.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/insights/aggregate.integration.test.ts</automated>
  </verify>
  <done>All 5 tests pass; row count in `insights` table matches unlocked-kind count after seeded 14d+ fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: scheduleInsights (nightly cron) — copy briefing/schedule.ts pattern</name>
  <files>src/main/insights/schedule.ts, src/main/insights/schedule.test.ts</files>
  <behavior>
    - Test 1: scheduleInsights registers `'insights-nightly'` in scheduler.cronRegistry; size grows by exactly 1.
    - Test 2: re-calling scheduleInsights replaces in place; size stays the same.
    - Test 3: suspend → resume cycle: registry.size unchanged across the cycle; task.stop() called on suspend, task.start() called on resume.
    - Test 4: `_lastFiredYmd` dedupe — two fires same local YMD result in only one run.
    - Test 5: multi-day suspend does NOT back-fire missed runs (research Pattern 1 + Pitfall 1).
    - Test 6: stopInsightsSchedule cleans up the lifecycle registration.
  </behavior>
  <action>
    Copy `src/main/briefing/schedule.ts` line-for-line (research §Pattern 1: "Copy it line-for-line; don't generalize prematurely"). Replace `CRON_KEY = 'briefing'` with `'insights-nightly'`, rename exports `scheduleInsights` / `stopInsightsSchedule` / `_resetInsightsScheduleForTests`. Default cron expr: `'0 2 * * *'` (2am local) — but accept `expr` arg for test override. Reuse `computeLocalYmd` (export from briefing/schedule.ts if not already exported; else re-define inline — confirm by grep).

    Wire scheduleInsights call into the existing scheduler bootstrap (search for where `scheduleBriefing` is invoked at boot — likely `src/main/lifecycle/scheduler.ts` or `src/main/index.ts`; mirror that registration site).
  </action>
  <verify>
    <automated>pnpm vitest run src/main/insights/schedule.test.ts</automated>
  </verify>
  <done>All 6 tests pass; cronRegistry.size invariant verified across suspend/resume; nightly cron registered at app boot after unlock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: IPC handlers + preload exposure + briefing read integration</name>
  <files>src/main/ipc/insights.ts, src/main/ipc/index.ts, src/shared/ipc-contract.ts, src/preload/index.ts, src/main/briefing/generate.ts</files>
  <behavior>
    - Test 1: INSIGHTS_LATEST returns rows for the most recent week_ymd; respects gate (returns `{ kind: 'locked', daysRemaining, blockedKinds }` if no week ≥14d).
    - Test 2: INSIGHTS_RECOMPUTE triggers aggregate(db, ...) and returns count of rows written.
    - Test 3 (B-4 — single source of truth): briefing read path FIRST queries `SELECT * FROM insights WHERE week_ymd = ? AND dismissed = 0 ORDER BY computed_at DESC`. Branching:
        (a) rows.length > 0 -> render top-3 from DB rows; DO NOT call checkInsightGate.
        (b) rows.length === 0 -> call checkInsightGate ONCE; render `{ kind: 'insights-locked', daysRemaining, blockedKinds }` placeholder if blocked, else omit the section (empty unlocked state — happens between cron fires).
        No double-gate evaluation. Test asserts checkInsightGate is invoked exactly 0 times when rows exist and exactly 1 time when rows.length===0 (via spy).
    - Test 4: briefing degrades gracefully (BRIEF-06 reuse) when insights table is empty AND gate is unlocked — section omitted (path b empty-unlocked sub-branch), not crashed.
  </behavior>
  <action>
    Add `CHANNELS.INSIGHTS_LATEST` and `CHANNELS.INSIGHTS_RECOMPUTE` to ipc-contract.ts with DTO types. Implement `registerInsightsHandlers(ipcMain, { logger, dbHolder, router })`. Register in `src/main/ipc/index.ts` using the same skip-pattern as RAG channels at line ~341.

    Expose on preload via the existing `window.api.*` namespace (mirror `window.api.rag.*` shape).

    Modify `src/main/briefing/generate.ts`: read top-3 latest insights from current week; if gate blocked, emit a `{ kind: 'insights-locked', daysRemaining, blockedKinds }` placeholder section. The briefing schema (zod) gains an optional `thisWeekInsights` field — verify back-compat with existing Phase 2 briefing snapshot tests; update fixtures.
  </action>
  <verify>
    <automated>pnpm vitest run src/main/ipc/insights.test.ts src/main/briefing/generate.test.ts</automated>
  </verify>
  <done>IPC channels round-trip; briefing payload includes thisWeekInsights or locked placeholder; existing briefing tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Settings → Insights section UI + BriefingScreen "This week" surface</name>
  <files>src/renderer/features/settings/sections/InsightsSection.tsx, src/renderer/features/settings/sections/InsightsSection.test.tsx, src/renderer/features/settings/SettingsScreen.tsx, src/renderer/features/briefing/BriefingScreen.tsx, src/renderer/features/briefing/BriefingScreen.test.tsx</files>
  <behavior>
    - Test 1 (component): InsightsSection renders "Recompute now" button; click invokes `window.api.insights.recompute()`; shows toast on success.
    - Test 2 (component): when gate is locked, InsightsSection shows "Insights unlock in N days" with a per-corpus breakdown.
    - Test 3 (BriefingScreen): when payload has thisWeekInsights, renders a "This week" section with up to 3 cards, each dismissible.
    - Test 4 (BriefingScreen): when payload has insights-locked placeholder, renders that placeholder copy.
    - Test 5 (reachability — L-04-04 memory): grep test asserts `InsightsSection` is imported by `SettingsScreen.tsx`. NOT an orphan component.
    - Test 6 (M-1 collision-safety): seed 100 rapid dismissals in a tight loop (all within same millisecond timestamp) → assert all 100 rows present in app_meta; ZERO PK collision errors. Closes peer-review M-1 round 2.
  </behavior>
  <action>
    Build InsightsSection.tsx as a Settings row mirroring shape of existing `NewsSourcesSection` (Phase 2) and `SchedulingRulesSection` (Phase 4) — both already in SettingsScreen. Wire `SettingsScreen.tsx` to import + render InsightsSection (per L-04-04 reachability invariant from MEMORY `feedback_verifier_blindspot_ui_wiring`).

    Add "This week" rendering to BriefingScreen — dismissible cards write a `briefing_feedback` signal. Stream 3 (Plan 08-03) owns the `briefing_feedback` table.

    (W-4 + M-1) Stream 1's stub handler for `BRIEFING_INSIGHT_DISMISS` MUST persist the dismissal so Stream 3's backfill can replay it. Concrete shape — peer-review M-1 round 2 fix: use a UUID, NOT a small random integer, in the PK suffix to eliminate timestamp+random collision risk on rapid clicks. Use SQLite-native `lower(hex(randomblob(16)))` (128-bit hex string, RFC4122-equivalent entropy):
    ```
    INSERT INTO app_meta(k, v) VALUES(
      'briefing_dismiss_log:' || strftime('%Y-%m-%dT%H:%M:%fZ','now') || ':' || lower(hex(randomblob(16))),
      json_object('briefingId', ?, 'insightKind', ?, 'sectionKey', ?, 'dismissedAt', ?)
    )
    ```
    Use `INSERT` (not `INSERT OR REPLACE`) — the 128-bit suffix makes collision astronomically improbable; if a collision somehow occurs the throw will be observable and worth investigating rather than silently swallowed. Confirm `app_meta(k TEXT PRIMARY KEY, v TEXT)` column shape via grep against `src/main/db/migrations/embedded.ts` (verified: present in migration 001_init.sql). NO fire-and-forget.

    Plan 08-03 Task 3's BRIEFING_INSIGHT_DISMISS implementation MUST: (a) read all `k LIKE 'briefing_dismiss_log:%'` rows on first install, replay them into `briefing_feedback`, then DELETE the replayed app_meta rows; (b) henceforth write directly to `briefing_feedback` instead of app_meta. This plan adds a TODO comment in Task 8 pointing to Plan 08-03 Task 3 for the backfill consumer.

    Dismissals are non-destructive (no confirm dialog needed — single click is recoverable from settings).
  </action>
  <verify>
    <automated>pnpm vitest run src/renderer/features/settings/sections/InsightsSection.test.tsx src/renderer/features/briefing/BriefingScreen.test.tsx &amp;&amp; pnpm vitest run -t "InsightsSection-reachability"</automated>
  </verify>
  <done>UI reachable from SettingsScreen route; briefing renders insights or locked placeholder; reachability grep test passes; 100-rapid-dismissal collision test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local DB → frontier LLM | Aggregates payload crosses here in insightProse — MUST be numeric/labels only |
| renderer → main IPC | INSIGHTS_RECOMPUTE input validated via zod (empty payload — no attack surface) |
| cron task → file system | None new; cron writes only via the existing DB handle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | insights/prose.ts payload | mitigate | Static-grep ratchet (Task 4); ProseOut zod schema rejects long fields; integration test asserts payload shape |
| T-08-02 | Tampering | recurringThemes k-means label gen | mitigate | Label-gen prompt receives top-5 TF terms only; test asserts no raw chunk text in prompt |
| T-08-03 | Denial of Service | nightly cron storms post-suspend | mitigate | `_lastFiredYmd` dedupe + `task.start()` not `task.fire()` on resume (Pattern 1) |
| T-08-04 | Information Disclosure | routing log row | mitigate | `hashPrompt` not raw prompt — Phase 1 prior art |
| T-08-05 | Spoofing | manual recompute IPC invoked by malicious renderer code | accept | Renderer is local + trusted (Electron same-origin); IPC payload is empty, no attack vector |
</threat_model>

<verification>
**Per-task verification covers compute correctness + INSIGHT-03 ratchet + cron invariant + UI reachability.**

Phase-level smoke:
1. Seed a DB with 21 days of synthetic calendar + email; run aggregate → 4 rows in `insights` table.
2. Open Briefing; confirm "This week" section renders with 3 insight cards.
3. Reset DB to 7 days history; reload briefing; confirm "Insights unlock in 7 days" placeholder.
4. From Settings → Insights, click "Recompute now"; confirm rows update.
5. Suspend + resume the laptop; confirm `scheduler.cronRegistry.size` invariant holds (briefing + gmail-sync + calendar-sync + insights-nightly = 4).
6. Inspect routing_log: `model='insight-prose'` or `source='insight-prose'`; `prompt_hash` populated; no raw text fields.
</verification>

<success_criteria>
- All 8 tasks' automated tests pass under `pnpm test:unit`.
- `pnpm run grep:insight-prose-no-raw` exits 0.
- Briefing renders insights section after a seeded 14d corpus; renders locked placeholder otherwise.
- Cron registered at boot; suspend/resume invariant holds.
- INSIGHT-01 (14d gate), INSIGHT-02 (briefing integration), INSIGHT-03 (no raw content to frontier), BRIEF-02 (top-3 priorities — insights surface here as one of the top-3 categories) all satisfied with concrete test acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/08-insights-weekly-recap-learning-release-prep/08-01-SUMMARY.md` covering:
- Tables added (insights), VIEWs introduced (none in this plan), cron keys added (insights-nightly)
- Files touched, requirement IDs closed
- Test commands a verifier can re-run
- Open follow-ups for Stream 2/3 (signal log integration for approval-edit pattern source)
</output>
</content>
</invoke>